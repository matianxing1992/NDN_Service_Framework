\section{应用需求与框架范围}
联网应用通常不仅需要知道服务的位置。无人机操作员可能需要一架能在截止时间前覆盖指定区域的相机，而推理请求者可能要求特定模型、足够的加速器内存及可接受的准备时间。服务通告只能指出潜在执行者，并不表明它当前愿意且能够承担这项任务。NDNSF 区分提供服务的权限、当前参与意愿、一次调用中的执行者选定，以及任务完成。

即使应用通常只使用一台设备，这些区分仍然重要。操作员选定无人机后，希望后续相机命令作用于同一架无人机；改用另一台可用 Provider 会改变命令含义。相反，图像分析请求可以允许任何满足条件的获准计算 Provider 执行。可复用框架需要支持这两种情形，而不要求应用分别实现无关的通信协议。

发现描述包含参与决策所需的信息。感知任务可能需要粗略区域、时间窗口和传感器要求；推理任务可能需要模型身份、预计输入大小和截止时间。完整图像、详细飞行指令和推理提示词可以在选定后交付。这一边界取决于决策需要：判断可行性不可缺少的信息，不能全部推迟到判断之后。应用仍应限制披露精度。例如，获准相机 Provider 可能需要大致搜索区域，而未选中的候选不必获得完整任务路线。描述加密限制获准服务 Provider 之外的读取者，但不能阻止合法接收者获知或主动分享其解密的信息。

NDNSF-UAV 和 NDNSF-DI 提供互补需求。前者包含设备专属操作、动态观测、实时数据和物理动作；后者包含计算选择、模型准备、大对象、中间张量和增量输出。两者都需要授权与有界请求生命周期。这些差异用于检验共同调用和命名对象接口在不同工作负载下是否仍然适用。可复用性应通过共享机制及应用专用代码来评价，而不能仅凭实现两个演示就认定成立。

本研究沿请求从候选发现到执行和关闭的过程推导范围。在每个阶段，先识别应用需要保持的关联，再说明未实施该关联会导致什么失败。

授权、保密性和时效要求贯穿多个阶段。权限变更可能在调用生命周期内影响执行，因此作为跨阶段事项单列。

\subsection{研究缺口与设计重点}
NDN 支持按名称检索 Data 并验证签名，但 Data 名称和有效签名本身不能记录或验证支配某次调用的服务决策。本研究考察的是：如何将运行时服务决策传递到命名 Data 的准入与使用。

候选发现允许 Provider 报告资格和参与意愿，但不会提交执行者。Provider 具备提供某项服务的权限，只能说明其具备该能力；这不会把它分配给某次调用或某个工作流角色。Selection 才是绑定步骤：初版原型为每次调用提交一个 Provider，协作扩展则提交 Provider 与角色及依赖边之间的绑定。

对于有依赖的任务，签名验证只能确认生产者身份，不能证明对象由当前调用中分配的前驱角色生成，也不能证明它满足声明的依赖。因此，消费者还需检查生产者、精确对象名称、依赖边和执行上下文。

已有调用和命令授权系统解决了其中部分问题。NDNSF 研究连接这些决策的可复用协议，使授权覆盖调用和数据获取。

为使这一缺口能够被验证，表~\ref{tab:framework-needs-ch}将每个调用阶段映射到框架必须保持的关联、缺失该关联时的失败，以及第~\ref{chap:evaluation}章计划进行的验证。

\begin{table}[htbp]\singlespacing\centering\small
\caption{从调用需求推导失败情形及拟议验证。}
\label{tab:framework-needs-ch}
\begin{tabularx}{\textwidth}{p{.16\textwidth}XXp{.23\textwidth}}
\toprule 阶段或事项 & 所需关联 & 缺失时的失败 & 拟议检查\\\midrule
候选发现 & 合格候选报告意愿与可行性；限制描述的读取权限 & 选中不合格候选，或向未获准读取者披露描述 & ACK 资格和描述访问\\\midrule
选定与启动 & 预期 Provider 只启动获准且活动的调用 & 错误设备、未授权执行或重复启动 & 权限、Selection 和重放检查\\\midrule
输入与中间 Data & 正确的生产者、对象或流及获准依赖 & 错误、过期或不完整输入推进执行 & 对象／流验证；角色、边和缓存测试\\\midrule
关闭或替换 & 结果对应活动操作；已关闭工作不能推进替换后的操作 & 迟到结果使已关闭工作恢复，或将错误操作标为完成 & 截止时间、取消与替换测试\\\midrule
权限变更 & 验证状态并完成必要密钥更新后应用新权限 & 新授权不可用，或继续接受已撤销权限 & 授予／撤销传播与拒绝延迟\\\bottomrule
\end{tabularx}
\end{table}

该表是从需求到证据的映射，不表示所有拟议的协作检查都已经实现或验证。

评价将检验相关检查是否执行了预期关联，以及所研究工作负载中的代价是否合理。

对于第~\ref{chap:uav-ch}、\ref{chap:di-ch}章的每个工作流，验证将识别适用的需求行，通过对应框架路径测试相关失败情形并记录结果。单 Provider 工作流无需协作角色检查。

第~\ref{chap:evaluation}章说明拟议测试。覆盖范围限于已声明的工作流，其余边界由下文的范围排除项限定。
