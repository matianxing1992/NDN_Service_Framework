# Preparation-to-inference deck review

## Scope

按用户要求以十页英文 slides 解释模型准备、ACK 后 placement、Selection 后材料获取与组装、prefill/decode、缓存和多轮 KV 恢复，最后说明主要资源开销与 Spec189 已验证结果。仅文档更新，`NO_DESIGN_CHANGE`，未修改源码、API、实验配置或资格状态；未运行 MiniNDN、SIF 或模型实验。

产物为 `main.tex`、`main.pdf`。旧版源码和 PDF 移至 `archive/pre-20260922/`，可恢复；旧源码引用的其他 frame 文件仍留在原 slides 目录，恢复编译时应放回原目录。`UPDATES.*` 未改。

## Page-by-page evidence

源码路径以下统一相对仓库根；C++ 前缀为 `NDNSF-DistributedInference/cpp/ndnsf-di/`。

| Page | Purpose | Evidence / correction |
| --- | --- | --- |
| 1 | API 与生命周期总览 | 后续按用户确认的 API 修订更新为 User::prepare → User::request(model, input, options)，多轮为 User::openConversation(model) → Conversation::request。实现由另一对话处理；本轮不声称重新验证该实现。示例省略配置和结果等待，不是完整可运行程序。 |
| 2 | 模型准备与发布 | `Runtime.cpp` source loader/User::prepare、`ModelPreparationCache.cpp`、`NativeRequestCatalog.cpp`。当前 loader 读取配置的模型/initializer 文件，不声称通用 checkpoint→ONNX exporter 已内置。层级材料发布与最终 Provider 分配是不同阶段。 |
| 3 | ACK 与 placement | `PreparedModel.cpp` requestInternal/projectOptions、native planning/Selection 路径、Spec189 r259。正 ACK 是候选反馈，不代表资源预留或执行许可。 |
| 4 | Selection 后执行准备 | `Provider.cpp`、`ProviderArtifactCache.cpp`、`NativeCanonicalOnnxAssembler.cpp`；Spec190 tasks T003/T006。缓存命中仍要验证身份、授权与生命周期；protected Repo 复用尚未完整验收。 |
| 5 | prefill/decode | `NativeEpochCoordinator.cpp`、ONNX runner、Spec189 r259/r260。图中箭头表示逻辑数据依赖，不是未经 Interest 的 Data 推送；两侧各持有本地层的 KV。 |
| 6 | 缓存分类 | ModelPreparationCache、ProviderArtifactCache、request-local runner slot。材料/文件缓存不是已加载 ORT session；Repo 与 Content Store 不混同。 |
| 7 | 跨轮状态 | `Conversation.cpp`、r259 retention/affinity、r260 KV_RESTORED。journal 携带 checkpoint/reference，不传全量 KV。失效状态不得直接复用，不声称已有任意自动迁移/重算。 |
| 8 | runner 驻留边界 | `NativeEpochCoordinator.cpp` 的共享 runner slot 限定本次 role/attempt；r260 70→6 次准备。跨 request resident-session 仍为 Spec190 T007 planned qualification。 |
| 9 | 资源成本 | Spec190 T003 日志边界与实现结构。列出结构性成本，不给未测量的开销排名；同一实验主机上一个 runner 与另一个 Provider 组装/加密处理可产生内存峰值重叠。 |
| 10 | 实测范围 | `specs/189-qwen-two-provider-minindn/evidence/b189-r259-affinity-retention.md`、`b189-r260-runner-reuse.md`，并直接核对 r260 原始 C++ oracle。三轮 10/11/11（含 EOS），prefix 23/51/79；1024 是预算，不是已生成长度。 |

## Evidence boundary

r260 raw oracle:
`.codex-tmp/spec189-qwen-two-provider-20260918/runs/two-provider-runner-reuse-r260/spec189-cpp-oracle.log`

实际输出 `SPEC189_CPP_CACHE_DIAGNOSTIC_PASS`，`scope=cache-compatible-execution`，`fullPathQualification=NOT_RUN`，`materialFetch=NOT_EVALUATED`，providers=2，rounds=3，terminal=true。这是 MiniNDN 两个 Provider 节点，不声称两台物理 GPU 主机。

Selection→checkpoint 总和 241.309→63.011 秒来自一对历史运行的记录，包含准备、计算、传输与 checkpoint；不是纯 decode throughput，也不是重复采样的稳定加速比。三轮使用 requester 进程间 journal 传递，不冒充同一常驻 Conversation handle 验收。

## Validation

- `latexmk -pdf -interaction=nonstopmode -halt-on-error` 构建，输出精确 10 页；最终日志无 Overfull hbox/vbox。
- 十页渲染人工逐页检查；修正第 6/9 页表格密度、断词和第 10 页表格宽度。所有正文、图和表均保留可编辑 TeX，不使用正文截图。
- NDN Slides Review 文本审计（150-word 门限）无 MAJOR；仅一条 `guarantee` 提示人工确认：P3 是“不保证未来资源可用”，不是正面性能承诺。
- 首次 ONNX/MiniNDN 附官方链接；代码/实验依据逐页给出。此技能促使明确 API 名称、缓存层次与实测边界。
- 默认约 100-word 目标在少数机制页略放宽，以满足用户要求的完整十页解释；未以压缩字号掩盖页面溢出。
- 旧 PDF/source 已可恢复归档。构建目录 `/tmp/ndnsf-di-slides-20260922/` 为本次文档产物，不是被测软件来源。

## References

- [ONNX introduction](https://onnx.ai/onnx/intro/)
- [Mini-NDN project](https://github.com/named-data/mini-ndn)

## Next step

## Plain-language review (all ten pages)

逐页独立阅读并渲染检查，保留十页和原实验数值。P1 区分模型句柄与运行中的模型、单次与对话两种入口；P2 解释 Repo、简化 catalog/adapter/lease，区分前置导出、材料发布与分配；P3 展开 ACK，说明 placement 是分层分配，正 ACK 不等于执行授权；P4 定义 runner，区分磁盘文件/内存 session，并明确前后层张量方向；P5 定义 KV、activation、prefill/decode 与 EOS，说明箭头是逻辑依赖；P6 用缓存保存内容/省去工作比较，避免缓存即授权；P7 定义 checkpoint/journal，区分引用记录与实际 KV，不宣称自动迁移；P8 去除正文内部批次编号，明确请求内复用与跨请求目标；P9 补入加解密大缓冲区叠加，而不是声称 ABE 算法已被实测为根因；P10 将 cache-compatible 的含义落实为模型数据复用且完整加密 Repo 路径未测，保留计数分解与 1024 预算边界。

Validation: latexmk 构建十页，最终日志无 Overfull；所有十页图像人工检查无重叠/裁切。仅修改 slides 与本审查记录，未修改 API/源码、未运行模型实验。现有 Git hook 阻止 checkpoint 的情况未解除，不绕过检查。

P10 后续补充计数分解：prior/new-input/output/saved 分别为 0/13/10/23、23/17/11/51、51/17/11/79。首轮输入 13 由已记录的 saved-prefix 23 减生成 10 得到；后两轮新增输入 17 与运行配置记录一致。展示第二轮 23+17+11=51，说明 KV 覆盖 token 数包含输入与输出，不是内存字节数。输出仍含 EOS，三轮均由 EOS 结束。重新构建和 P10 渲染检查通过，保持十页。

后续 P10 按用户要求移除 Spec/run 编号及历史对照，只显示两 Provider 三轮结果、6 次 runner preparation 和 63.01 秒累计 Selection-to-checkpoint 区间。`Prefix tokens` 改为 `Tokens covered by saved KV`，明确是已处理前缀对应的 KV 覆盖长度，不是当轮新增输出。原始 r259/r260 追踪依据仍保留在本审查记录，幻灯片保留 cache-compatible 与长输出未验收边界。

本地 checkpoint commit 被现有 Git hook 阻止：`Commit blocked: development-assistant files or references remain in the Git index`，列出的主要是已有仓库内容。本轮未绕过 hook、未清理其他索引项、未 push；五个 slides/归档文件保留暂存。Spec190 tasks.md 已写入本轮文档进度，但该文件原有大量并行修改，未整体暂存。首次 stage 发现目录忽略规则后，仅对本轮新增归档和审查文件使用精确路径 `git add -f`。

后续只有在 Spec190 的 protected material reuse 和 resident-session 测试提供新证据后，才更新 P4/P8/P10 的完成口径；持续长输出仍需独立实验，不能用 maxNewTokens 预算代替实测 token 数。
