# Core API 参考

声明从源码语法树提取。保留准确类型、参数、默认值、限定符和原始注释；注释不代替运行证据。中文语义契约见开发者指南。protected 扩展点、测试 helper、应用内部接口各自标注。

## ndn-service-framework/CertificateBootstrap.hpp

源码 SHA-256：`914a3ea04901a546e3a29d9a6b27b3ae9b77973efe283f416828621eb4eb0a2e`。

### API-acbc918ee594 · ndn_service_framework::bootstrap_tlv::(anonymous)

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L12)

```cpp
enum
```

### API-d702126bfc1f · ndn_service_framework::bootstrap_tlv::(anonymous)::CertificateBootstrapRequest

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L13)

```cpp
CertificateBootstrapRequest = 0xF510
```

### API-51c3f7701ea5 · ndn_service_framework::bootstrap_tlv::(anonymous)::CertificateBootstrapResponse

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L14)

```cpp
CertificateBootstrapResponse = 0xF511
```

### API-29edec0bc98e · ndn_service_framework::bootstrap_tlv::(anonymous)::CertificateRequest

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L15)

```cpp
CertificateRequest = 0xF512
```

### API-335cd96aab4d · ndn_service_framework::bootstrap_tlv::(anonymous)::IssuedCertificate

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L16)

```cpp
IssuedCertificate = 0xF513
```

### API-a2f521f70a1c · ndn_service_framework::bootstrap_tlv::(anonymous)::EncryptedCertificateBootstrapRequest

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L17)

```cpp
EncryptedCertificateBootstrapRequest = 0xF514
```

### API-db2f3028112d · ndn_service_framework::bootstrap_tlv::(anonymous)::ProofNonce

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L18)

```cpp
ProofNonce = 0xF515
```

### API-dd199964724f · ndn_service_framework::bootstrap_tlv::(anonymous)::ProofSignature

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L19)

```cpp
ProofSignature = 0xF516
```

### API-8eb56bcff6cd · ndn_service_framework::bootstrap_tlv::(anonymous)::CertificateBootstrapProofData

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L20)

```cpp
CertificateBootstrapProofData = 0xF517
```

### API-2332194b23ca · ndn_service_framework::CertificateBootstrapRequest

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L24)

```cpp
struct CertificateBootstrapRequest
```

### API-c4543e9b8ba7 · ndn_service_framework::CertificateBootstrapRequest::identity

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L26)

```cpp
ndn::Name identity;
```

### API-98874cd0b16a · ndn_service_framework::CertificateBootstrapRequest::token

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L27)

```cpp
std::string token;
```

### API-da8f2160748d · ndn_service_framework::CertificateBootstrapRequest::certificateRequest

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L28)

```cpp
ndn::security::Certificate certificateRequest;
```

### API-92ca292426c9 · ndn_service_framework::CertificateBootstrapRequest::proofNonce

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L29)

```cpp
ndn::Buffer proofNonce;
```

### API-0f86d285affa · ndn_service_framework::CertificateBootstrapRequest::proofSignature

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L30)

```cpp
ndn::Buffer proofSignature;
```

### API-1cf9fbf14770 · ndn_service_framework::CertificateBootstrapRequest::wireEncode

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L32)

```cpp
ndn::Block wireEncode() const;
```

### API-4d0de3d9a8b2 · ndn_service_framework::CertificateBootstrapRequest::wireDecode

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L33)

```cpp
bool wireDecode(const ndn::Block& block);
```

### API-046a42d1997a · ndn_service_framework::CertificateBootstrapResponse

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L36)

```cpp
struct CertificateBootstrapResponse
```

### API-d9c39f8cb692 · ndn_service_framework::CertificateBootstrapResponse::status

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L38)

```cpp
bool status = false;
```

### API-a18245a74fbf · ndn_service_framework::CertificateBootstrapResponse::message

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L39)

```cpp
std::string message;
```

### API-958d4b8d603c · ndn_service_framework::CertificateBootstrapResponse::issuedCertificate

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L40)

```cpp
ndn::security::Certificate issuedCertificate;
```

### API-72f786ef617d · ndn_service_framework::CertificateBootstrapResponse::hasIssuedCertificate

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L41)

```cpp
bool hasIssuedCertificate = false;
```

### API-d723f1adb4ce · ndn_service_framework::CertificateBootstrapResponse::wireEncode

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L43)

```cpp
ndn::Block wireEncode() const;
```

### API-6afc3e6f1a38 · ndn_service_framework::CertificateBootstrapResponse::wireDecode

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L44)

```cpp
bool wireDecode(const ndn::Block& block);
```

### API-71e2a33828cb · ndn_service_framework::EncryptedCertificateBootstrapRequest

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L47)

```cpp
struct EncryptedCertificateBootstrapRequest
```

### API-964cd85d596e · ndn_service_framework::EncryptedCertificateBootstrapRequest::recipientCertName

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L49)

```cpp
std::string recipientCertName;
```

### API-c9548e39b1de · ndn_service_framework::EncryptedCertificateBootstrapRequest::algorithm

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L50)

```cpp
std::string algorithm;
```

### API-8b93a617f288 · ndn_service_framework::EncryptedCertificateBootstrapRequest::encryptedAesKey

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L51)

```cpp
ndn::Buffer encryptedAesKey;
```

### API-cfb38ad5424f · ndn_service_framework::EncryptedCertificateBootstrapRequest::iv

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L52)

```cpp
ndn::Buffer iv;
```

### API-72eb0a6fa08e · ndn_service_framework::EncryptedCertificateBootstrapRequest::cipherText

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L53)

```cpp
ndn::Buffer cipherText;
```

### API-198bc524a315 · ndn_service_framework::EncryptedCertificateBootstrapRequest::wireEncode

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L55)

```cpp
ndn::Block wireEncode() const;
```

### API-cc76dd7301fa · ndn_service_framework::EncryptedCertificateBootstrapRequest::wireDecode

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L56)

```cpp
bool wireDecode(const ndn::Block& block);
```

### API-593bcb9b8acf · ndn_service_framework::encryptCertificateBootstrapRequestForCertificate

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L59)

```cpp
EncryptedCertificateBootstrapRequest
encryptCertificateBootstrapRequestForCertificate(const CertificateBootstrapRequest& request,
                                                 const ndn::security::Certificate& recipientCert);
```

### API-b2a5d0f4a3b1 · ndn_service_framework::decryptCertificateBootstrapRequestWithKeyChain

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L63)

```cpp
CertificateBootstrapRequest
decryptCertificateBootstrapRequestWithKeyChain(const EncryptedCertificateBootstrapRequest& encryptedRequest,
                                               const ndn::security::KeyChain& keyChain);
```

### API-1955a9591755 · ndn_service_framework::makeCertificateBootstrapName

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L67)

```cpp
ndn::Name
makeCertificateBootstrapName(const ndn::Name& controllerPrefix,
                             const ndn::Name& identity);
```

### API-69dd18ad79ad · ndn_service_framework::isCertificateSignedByIdentity

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L71)

```cpp
bool
isCertificateSignedByIdentity(const ndn::security::Certificate& certificate,
                              const ndn::Name& signerIdentity);
```

### API-18b032310e78 · ndn_service_framework::requestControllerSignedCertificate

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L75)

```cpp
ndn::security::Certificate
requestControllerSignedCertificate(ndn::Face& face,
                                   ndn::security::KeyChain& keyChain,
                                   const ndn::Name& controllerPrefix,
                                   const ndn::Name& identity,
                                   const std::string& token,
                                   ndn::time::milliseconds timeout =
                                     ndn::time::milliseconds(5000));
```

### API-c6d9fe73b9c3 · ndn_service_framework::requestControllerSignedCertificate

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L84)

```cpp
ndn::security::Certificate
requestControllerSignedCertificate(ndn::Face& face,
                                   ndn::security::KeyChain& keyChain,
                                   const ndn::Name& controllerPrefix,
                                   const ndn::Name& certificateIdentity,
                                   const ndn::Name& bootstrapIdentity,
                                   const std::string& token,
                                   ndn::time::milliseconds timeout =
                                     ndn::time::milliseconds(5000));
```

### API-3b8cb86c6a9a · ndn_service_framework::ensureControllerSignedCertificate

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L94)

```cpp
ndn::security::Certificate
ensureControllerSignedCertificate(ndn::Face& face,
                                  ndn::security::KeyChain& keyChain,
                                  const ndn::Name& controllerPrefix,
                                  const ndn::Name& identity,
                                  const std::string& token,
                                  ndn::time::milliseconds timeout =
                                    ndn::time::milliseconds(5000));
```

### API-328e435ffd05 · ndn_service_framework::ensureControllerSignedCertificate

public / declared-interface；[源码](../../ndn-service-framework/CertificateBootstrap.hpp#L103)

```cpp
ndn::security::Certificate
ensureControllerSignedCertificate(ndn::Face& face,
                                  ndn::security::KeyChain& keyChain,
                                  const ndn::Name& controllerPrefix,
                                  const ndn::Name& certificateIdentity,
                                  const ndn::Name& bootstrapIdentity,
                                  const std::string& token,
                                  ndn::time::milliseconds timeout =
                                    ndn::time::milliseconds(5000));
```

## ndn-service-framework/CertificatePublisher.hpp

源码 SHA-256：`51e3a92ce53f267b544ec75d2786a5ca9519571c482527cea31897cecb0152dc`。

### API-5655e7fe3141 · ndn_service_framework::CertificatePublisher

public / declared-interface；[源码](../../ndn-service-framework/CertificatePublisher.hpp#L13)

```cpp
class CertificatePublisher
```

### API-8eab7074e1a7 · ndn_service_framework::CertificatePublisher::CertificatePublisher

public / declared-interface；[源码](../../ndn-service-framework/CertificatePublisher.hpp#L16)

```cpp
CertificatePublisher(ndn::Face& face,
                       ndn::security::KeyChain& keyChain,
                       const ndn::Name& identityOrCertName,
                       bool registerKeyPrefix = true);
```

### API-cf236f7cbbf6 · ndn_service_framework::CertificatePublisher::getCertificateName

public / declared-interface；[源码](../../ndn-service-framework/CertificatePublisher.hpp#L21)

```cpp
const ndn::Name& getCertificateName() const;
```

### API-7415b68e5c91 · ndn_service_framework::CertificatePublisher::getRegisteredPrefix

public / declared-interface；[源码](../../ndn-service-framework/CertificatePublisher.hpp#L22)

```cpp
const ndn::Name& getRegisteredPrefix() const;
```

## ndn-service-framework/ConfigManager.hpp

源码 SHA-256：`2b07e59c1cc939252ddab3b8c5522cc6eea7ba23b623792de64f457a31134d9d`。

### API-f9f01a6002c3 · ndn_service_framework::ConfigManager

public / declared-interface；[源码](../../ndn-service-framework/ConfigManager.hpp#L21)

```cpp
class ConfigManager
```

### API-9991a900b40d · ndn_service_framework::ConfigManager::ConfigManager

public / declared-interface；[源码](../../ndn-service-framework/ConfigManager.hpp#L23)

```cpp
ConfigManager(const std::string& path = "/etc/ndn/ndnsf.conf")
```

### API-ea1994761a71 · ndn_service_framework::ConfigManager::loadAndIncrement

public / declared-interface；[源码](../../ndn-service-framework/ConfigManager.hpp#L33)

```cpp
int loadAndIncrement(const std::string& groupName, const std::string& nodeName)
```

原始接口说明：

```text
// 加载并增加该组/节点的 sessionId，并持久化写入
```

### API-e5312b70df8e · ndn_service_framework::ConfigManager::getSessionId

public / declared-interface；[源码](../../ndn-service-framework/ConfigManager.hpp#L117)

```cpp
int getSessionId(const std::string& groupName, const std::string& nodeName) const
```

原始接口说明：

```text
// 可选接口：查询 sessionId（仅查询缓存）
```

## ndn-service-framework/ControllerGenerationStore.hpp

源码 SHA-256：`58dff9d644d3cfedffef16086e81b7508f67610e8001876955ef9744ee8dd37f`。

### API-6f0722698102 · ndn_service_framework::ControllerGenerationStore

public / declared-interface；[源码](../../ndn-service-framework/ControllerGenerationStore.hpp#L15)

```cpp
class ControllerGenerationStore
```

### API-7d3d589d8cc8 · ndn_service_framework::ControllerGenerationStore::ControllerGenerationStore

public / declared-interface；[源码](../../ndn-service-framework/ControllerGenerationStore.hpp#L18)

```cpp
explicit ControllerGenerationStore(std::filesystem::path statePath);
```

### API-cc5d4766ec34 · ndn_service_framework::ControllerGenerationStore::~ControllerGenerationStore

public / declared-interface；[源码](../../ndn-service-framework/ControllerGenerationStore.hpp#L19)

```cpp
~ControllerGenerationStore();
```

### API-69f74a8701b4 · ndn_service_framework::ControllerGenerationStore::ControllerGenerationStore

public / declared-interface；[源码](../../ndn-service-framework/ControllerGenerationStore.hpp#L21)

```cpp
ControllerGenerationStore(const ControllerGenerationStore&) = delete;
```

### API-5d4934f60423 · ndn_service_framework::ControllerGenerationStore::operator=

public / declared-interface；[源码](../../ndn-service-framework/ControllerGenerationStore.hpp#L22)

```cpp
ControllerGenerationStore& operator=(const ControllerGenerationStore&) = delete;
```

### API-ada4489ae044 · ndn_service_framework::ControllerGenerationStore::acquireWriter

public / declared-interface；[源码](../../ndn-service-framework/ControllerGenerationStore.hpp#L24)

```cpp
bool acquireWriter(const std::string& owner);
```

### API-93bb63bc3550 · ndn_service_framework::ControllerGenerationStore::ownsWriter

public / declared-interface；[源码](../../ndn-service-framework/ControllerGenerationStore.hpp#L25)

```cpp
bool ownsWriter() const;
```

### API-30ee6889eb42 · ndn_service_framework::ControllerGenerationStore::fencingValue

public / declared-interface；[源码](../../ndn-service-framework/ControllerGenerationStore.hpp#L26)

```cpp
uint64_t fencingValue() const;
```

### API-e53e9c2a7030 · ndn_service_framework::ControllerGenerationStore::releaseWriter

public / declared-interface；[源码](../../ndn-service-framework/ControllerGenerationStore.hpp#L27)

```cpp
void releaseWriter();
```

### API-34e7fa92c4ce · ndn_service_framework::ControllerGenerationStore::load

public / declared-interface；[源码](../../ndn-service-framework/ControllerGenerationStore.hpp#L29)

```cpp
std::optional<ControllerVersion> load() const;
```

### API-f224f1088e89 · ndn_service_framework::ControllerGenerationStore::loadRevocations

public / declared-interface；[源码](../../ndn-service-framework/ControllerGenerationStore.hpp#L30)

```cpp
std::vector<RevocationTarget> loadRevocations() const;
```

### API-eb2827a83c15 · ndn_service_framework::ControllerGenerationStore::startGeneration

public / declared-interface；[源码](../../ndn-service-framework/ControllerGenerationStore.hpp#L31)

```cpp
ControllerVersion startGeneration(
      uint64_t nowMs, const std::vector<RevocationTarget>& revocations = {});
```

### API-6d58ce7f54b1 · ndn_service_framework::ControllerGenerationStore::advanceEpoch

public / declared-interface；[源码](../../ndn-service-framework/ControllerGenerationStore.hpp#L33)

```cpp
ControllerVersion advanceEpoch(
      const std::vector<RevocationTarget>& revocations = {});
```

## ndn-service-framework/ControllerVersion.hpp

源码 SHA-256：`22e0351fc79033862636f7ad473c92cd812b5f1fea48d2031a9c759db1dd9675`。

### API-dc6f69da22ea · ndn_service_framework::ControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/ControllerVersion.hpp#L17)

```cpp
struct ControllerVersion
```

### API-a29492e66f30 · ndn_service_framework::ControllerVersion::TYPE

public / declared-interface；[源码](../../ndn-service-framework/ControllerVersion.hpp#L19)

```cpp
static constexpr uint32_t TYPE = 0xF700;
```

### API-fe5117342fa2 · ndn_service_framework::ControllerVersion::GenerationTimestampType

public / declared-interface；[源码](../../ndn-service-framework/ControllerVersion.hpp#L20)

```cpp
static constexpr uint32_t GenerationTimestampType = 0xF701;
```

### API-445878c779ce · ndn_service_framework::ControllerVersion::EpochType

public / declared-interface；[源码](../../ndn-service-framework/ControllerVersion.hpp#L21)

```cpp
static constexpr uint32_t EpochType = 0xF702;
```

### API-67f804521195 · ndn_service_framework::ControllerVersion::controllerGenerationTimestamp

public / declared-interface；[源码](../../ndn-service-framework/ControllerVersion.hpp#L23)

```cpp
uint64_t controllerGenerationTimestamp = 0;
```

### API-2fe46f464156 · ndn_service_framework::ControllerVersion::controllerEpoch

public / declared-interface；[源码](../../ndn-service-framework/ControllerVersion.hpp#L24)

```cpp
uint64_t controllerEpoch = 0;
```

### API-86c732722f82 · ndn_service_framework::ControllerVersion::isValid

public / declared-interface；[源码](../../ndn-service-framework/ControllerVersion.hpp#L26)

```cpp
bool isValid() const;
```

### API-32c15a88b250 · ndn_service_framework::ControllerVersion::compare

public / declared-interface；[源码](../../ndn-service-framework/ControllerVersion.hpp#L27)

```cpp
int compare(const ControllerVersion& other) const;
```

### API-f37c04288718 · ndn_service_framework::ControllerVersion::operator==

public / declared-interface；[源码](../../ndn-service-framework/ControllerVersion.hpp#L28)

```cpp
bool operator==(const ControllerVersion& other) const;
```

### API-a8eaec9a237b · ndn_service_framework::ControllerVersion::operator!=

public / declared-interface；[源码](../../ndn-service-framework/ControllerVersion.hpp#L29)

```cpp
bool operator!=(const ControllerVersion& other) const;
```

### API-1aa6747c0f90 · ndn_service_framework::ControllerVersion::toString

public / declared-interface；[源码](../../ndn-service-framework/ControllerVersion.hpp#L30)

```cpp
std::string toString() const;
```

### API-32345b72facd · ndn_service_framework::ControllerVersion::wireEncode

public / declared-interface；[源码](../../ndn-service-framework/ControllerVersion.hpp#L32)

```cpp
ndn::Block wireEncode() const;
```

### API-5d58981daa1e · ndn_service_framework::ControllerVersion::wireDecode

public / declared-interface；[源码](../../ndn-service-framework/ControllerVersion.hpp#L33)

```cpp
bool wireDecode(const ndn::Block& block);
```

## ndn-service-framework/ExecutionLease.hpp

源码 SHA-256：`7f4c32fcf13fdcd4e863efbabcab17ef4dfa85681e739b46ac0cc8f6bb9d5601`。

### API-e83d6d6e442f · ndn_service_framework::execution_lease_reason::OK[] = "OK"

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L17)

```cpp
inline constexpr char OK[] = "OK";
```

### API-896dc3c0619f · ndn_service_framework::execution_lease_reason::UNAVAILABLE[] = "LEASE_UNAVAILABLE"

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L18)

```cpp
inline constexpr char UNAVAILABLE[] = "LEASE_UNAVAILABLE";
```

### API-79ba9ad6342a · ndn_service_framework::execution_lease_reason::NOT_FOUND[] = "LEASE_NOT_FOUND"

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L19)

```cpp
inline constexpr char NOT_FOUND[] = "LEASE_NOT_FOUND";
```

### API-9164168d3dc5 · ndn_service_framework::execution_lease_reason::EXPIRED[] = "LEASE_EXPIRED"

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L20)

```cpp
inline constexpr char EXPIRED[] = "LEASE_EXPIRED";
```

### API-696552dc6c04 · ndn_service_framework::execution_lease_reason::STALE_EPOCH[] = "LEASE_STALE_EPOCH"

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L21)

```cpp
inline constexpr char STALE_EPOCH[] = "LEASE_STALE_EPOCH";
```

### API-3060007759bd · ndn_service_framework::execution_lease_reason::IDEMPOTENCY_CONFLICT[] = "LEASE_IDEMPOTENCY_CONFLICT"

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L22)

```cpp
inline constexpr char IDEMPOTENCY_CONFLICT[] = "LEASE_IDEMPOTENCY_CONFLICT";
```

### API-099307a033e0 · ndn_service_framework::execution_lease_reason::INVALID_TRANSITION[] = "LEASE_INVALID_TRANSITION"

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L23)

```cpp
inline constexpr char INVALID_TRANSITION[] = "LEASE_INVALID_TRANSITION";
```

### API-b8082fcaaf5f · ndn_service_framework::execution_lease_reason::REQUESTER_MISMATCH[] = "LEASE_REQUESTER_MISMATCH"

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L24)

```cpp
inline constexpr char REQUESTER_MISMATCH[] = "LEASE_REQUESTER_MISMATCH";
```

### API-75f3908097ad · ndn_service_framework::execution_lease_reason::REQUEST_MISMATCH[] = "LEASE_REQUEST_MISMATCH"

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L25)

```cpp
inline constexpr char REQUEST_MISMATCH[] = "LEASE_REQUEST_MISMATCH";
```

### API-9749592e97ff · ndn_service_framework::execution_lease_reason::SERVICE_MISMATCH[] = "LEASE_SERVICE_MISMATCH"

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L26)

```cpp
inline constexpr char SERVICE_MISMATCH[] = "LEASE_SERVICE_MISMATCH";
```

### API-dc85d02787bc · ndn_service_framework::execution_lease_reason::PLAN_MISMATCH[] = "LEASE_PLAN_MISMATCH"

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L27)

```cpp
inline constexpr char PLAN_MISMATCH[] = "LEASE_PLAN_MISMATCH";
```

### API-46a96c4ca08d · ndn_service_framework::execution_lease_reason::BINDING_MISMATCH[] = "LEASE_BINDING_MISMATCH"

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L28)

```cpp
inline constexpr char BINDING_MISMATCH[] = "LEASE_BINDING_MISMATCH";
```

### API-9a5a8e687c33 · ndn_service_framework::execution_lease_reason::CAPACITY_REJECTED[] = "LEASE_CAPACITY_REJECTED"

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L29)

```cpp
inline constexpr char CAPACITY_REJECTED[] = "LEASE_CAPACITY_REJECTED";
```

### API-bba65e465f95 · ndn_service_framework::execution_lease_reason::INTERNAL_ERROR[] = "LEASE_INTERNAL_ERROR"

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L30)

```cpp
inline constexpr char INTERNAL_ERROR[] = "LEASE_INTERNAL_ERROR";
```

### API-66cc9b807cd7 · ndn_service_framework::ExecutionLeaseState

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L33)

```cpp
enum class ExecutionLeaseState
```

### API-3b7f019739de · ndn_service_framework::ExecutionLeaseState::Prepared

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L35)

```cpp
Prepared
```

### API-bfbe4425b58a · ndn_service_framework::ExecutionLeaseState::Committed

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L36)

```cpp
Committed
```

### API-0a0c67211255 · ndn_service_framework::ExecutionLeaseState::Executing

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L37)

```cpp
Executing
```

### API-4d90040c8ff2 · ndn_service_framework::ExecutionLeaseState::Aborted

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L38)

```cpp
Aborted
```

### API-eb11c47ae7e6 · ndn_service_framework::ExecutionLeaseState::Released

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L39)

```cpp
Released
```

### API-5de2bc96b046 · ndn_service_framework::ExecutionLeaseState::Expired

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L40)

```cpp
Expired
```

### API-754875ad339a · ndn_service_framework::toString

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L43)

```cpp
const char*
toString(ExecutionLeaseState state) noexcept;
```

### API-6cd5c07564fb · ndn_service_framework::GenericExecutionLease

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L46)

```cpp
struct GenericExecutionLease
```

### API-8fcdbd23ff64 · ndn_service_framework::GenericExecutionLease::schema

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L48)

```cpp
std::string schema = "ndnsf-execution-lease-v1";
```

### API-52c12b5c2ae8 · ndn_service_framework::GenericExecutionLease::leaseId

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L49)

```cpp
std::string leaseId;
```

### API-32041e15dda8 · ndn_service_framework::GenericExecutionLease::providerName

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L50)

```cpp
std::string providerName;
```

### API-f7317a3ad0c3 · ndn_service_framework::GenericExecutionLease::providerEpoch

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L51)

```cpp
std::string providerEpoch;
```

### API-e258521871a6 · ndn_service_framework::GenericExecutionLease::requesterName

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L52)

```cpp
std::string requesterName;
```

### API-34185a1d0173 · ndn_service_framework::GenericExecutionLease::requestId

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L53)

```cpp
std::string requestId;
```

### API-0a7f10de63bd · ndn_service_framework::GenericExecutionLease::serviceName

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L54)

```cpp
std::string serviceName;
```

### API-bd8bb45aadb3 · ndn_service_framework::GenericExecutionLease::planDigest

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L55)

```cpp
std::string planDigest;
```

### API-8923cd7a6e17 · ndn_service_framework::GenericExecutionLease::resourceBindingSchema

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L56)

```cpp
std::string resourceBindingSchema;
```

### API-b4a29ebded2c · ndn_service_framework::GenericExecutionLease::resourceBindingProof

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L57)

```cpp
ndn::Buffer resourceBindingProof;
```

### API-033c5c351a40 · ndn_service_framework::GenericExecutionLease::conflictKeys

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L58)

```cpp
std::vector<std::string> conflictKeys;
```

### API-7083e41cc601 · ndn_service_framework::GenericExecutionLease::state

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L59)

```cpp
ExecutionLeaseState state = ExecutionLeaseState::Prepared;
```

### API-9cb02d27b929 · ndn_service_framework::GenericExecutionLease::expiresAtMs

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L60)

```cpp
uint64_t expiresAtMs = 0;
```

### API-2df760a85663 · ndn_service_framework::GenericExecutionLease::executionDeadlineMs

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L61)

```cpp
uint64_t executionDeadlineMs = 0;
```

### API-a0713f5aa567 · ndn_service_framework::GenericExecutionLease::idempotencyKey

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L62)

```cpp
std::string idempotencyKey;
```

### API-8f9739993617 · ndn_service_framework::ExecutionLeaseBinding

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L65)

```cpp
struct ExecutionLeaseBinding
```

### API-eacd6071410f · ndn_service_framework::ExecutionLeaseBinding::requesterName

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L67)

```cpp
std::string requesterName;
```

### API-960709222707 · ndn_service_framework::ExecutionLeaseBinding::requestId

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L68)

```cpp
std::string requestId;
```

### API-edfa3e5ae2df · ndn_service_framework::ExecutionLeaseBinding::serviceName

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L69)

```cpp
std::string serviceName;
```

### API-0517d18b598d · ndn_service_framework::ExecutionLeaseBinding::planDigest

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L70)

```cpp
std::string planDigest;
```

### API-bcc2f9e6d996 · ndn_service_framework::ExecutionLeaseBinding::resourceBindingSchema

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L71)

```cpp
std::string resourceBindingSchema;
```

### API-8213a7496915 · ndn_service_framework::ExecutionLeaseBinding::resourceBindingProof

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L72)

```cpp
ndn::Buffer resourceBindingProof;
```

### API-8abe9d9e1bf0 · ndn_service_framework::ExecutionLeaseResult

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L75)

```cpp
struct ExecutionLeaseResult
```

### API-b419acf425dd · ndn_service_framework::ExecutionLeaseResult::status

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L77)

```cpp
bool status = false;
```

### API-a18bc67d5fe9 · ndn_service_framework::ExecutionLeaseResult::operation

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L78)

```cpp
std::string operation;
```

### API-c6b0d6136d49 · ndn_service_framework::ExecutionLeaseResult::reasonCode

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L79)

```cpp
std::string reasonCode;
```

### API-21abead6b224 · ndn_service_framework::ExecutionLeaseResult::lease

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L80)

```cpp
GenericExecutionLease lease;
```

### API-435be0aeba32 · ndn_service_framework::ExecutionLeaseResult::retryAfterMs

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L81)

```cpp
uint64_t retryAfterMs = 0;
```

### API-c32c844a547a · ndn_service_framework::ExecutionLeaseResult::idempotentReplay

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L82)

```cpp
bool idempotentReplay = false;
```

### API-a79d070a8460 · ndn_service_framework::ExecutionLeaseCounters

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L85)

```cpp
struct ExecutionLeaseCounters
```

### API-e098b860c0f3 · ndn_service_framework::ExecutionLeaseCounters::prepared

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L87)

```cpp
uint64_t prepared = 0;
```

### API-db3350336de7 · ndn_service_framework::ExecutionLeaseCounters::committed

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L88)

```cpp
uint64_t committed = 0;
```

### API-70f193301638 · ndn_service_framework::ExecutionLeaseCounters::activated

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L89)

```cpp
uint64_t activated = 0;
```

### API-dac22497fcb6 · ndn_service_framework::ExecutionLeaseCounters::aborted

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L90)

```cpp
uint64_t aborted = 0;
```

### API-03b7919ef1e6 · ndn_service_framework::ExecutionLeaseCounters::released

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L91)

```cpp
uint64_t released = 0;
```

### API-29a27107b426 · ndn_service_framework::ExecutionLeaseCounters::expired

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L92)

```cpp
uint64_t expired = 0;
```

### API-c74801e5c7d8 · ndn_service_framework::ExecutionLeaseCounters::renewed

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L93)

```cpp
uint64_t renewed = 0;
```

### API-e762e4adc29b · ndn_service_framework::ExecutionLeaseCounters::idempotentReplay

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L94)

```cpp
uint64_t idempotentReplay = 0;
```

### API-a99742152206 · ndn_service_framework::ExecutionLeaseCounters::conflict

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L95)

```cpp
uint64_t conflict = 0;
```

### API-251a6ad0a402 · ndn_service_framework::ExecutionLeaseCounters::staleEpoch

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L96)

```cpp
uint64_t staleEpoch = 0;
```

### API-ac8aa4049096 · ndn_service_framework::ExecutionLeaseCounters::cleanupTimeout

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L97)

```cpp
uint64_t cleanupTimeout = 0;
```

### API-f53df29caf85 · ndn_service_framework::ExecutionLeaseCounters::rejectedByReason

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L98)

```cpp
std::map<std::string, uint64_t> rejectedByReason;
```

### API-7647f26a656d · ndn_service_framework::ExecutionLeaseCounters::activePrepared

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L99)

```cpp
uint64_t activePrepared = 0;
```

### API-0bc876351b16 · ndn_service_framework::ExecutionLeaseCounters::activeCommitted

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L100)

```cpp
uint64_t activeCommitted = 0;
```

### API-7ee65cb857dd · ndn_service_framework::ExecutionLeaseCounters::activeExecuting

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L101)

```cpp
uint64_t activeExecuting = 0;
```

### API-936b431f69a6 · ndn_service_framework::ProviderExecutionLeaseTable

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L104)

```cpp
class ProviderExecutionLeaseTable
```

### API-e48b6f3e8335 · ndn_service_framework::ProviderExecutionLeaseTable::ProviderExecutionLeaseTable

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L107)

```cpp
explicit ProviderExecutionLeaseTable(std::string providerEpoch = {});
```

### API-c1ece08b4c03 · ndn_service_framework::ProviderExecutionLeaseTable::providerEpoch

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L109)

```cpp
const std::string&
  providerEpoch() const noexcept;
```

### API-d154a2bbea78 · ndn_service_framework::ProviderExecutionLeaseTable::prepare

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L112)

```cpp
ExecutionLeaseResult
  prepare(GenericExecutionLease lease, uint64_t nowMs);
```

### API-13c34e09371a · ndn_service_framework::ProviderExecutionLeaseTable::commit

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L115)

```cpp
ExecutionLeaseResult
  commit(const std::string& leaseId, const std::string& providerEpoch,
         const std::string& requesterName, const std::string& idempotencyKey,
         uint64_t nowMs);
```

### API-e156fa6d6cf2 · ndn_service_framework::ProviderExecutionLeaseTable::validateAndActivate

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L120)

```cpp
ExecutionLeaseResult
  validateAndActivate(const std::string& leaseId,
                      const std::string& providerEpoch,
                      const ExecutionLeaseBinding& binding,
                      const std::string& idempotencyKey,
                      uint64_t nowMs,
                      uint64_t executionDeadlineMs);
```

### API-1ba3a45fb6ae · ndn_service_framework::ProviderExecutionLeaseTable::validate

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L128)

```cpp
ExecutionLeaseResult
  validate(const std::string& leaseId,
           const std::string& providerEpoch,
           const ExecutionLeaseBinding& binding,
           uint64_t nowMs);
```

### API-ab1fe3405eb9 · ndn_service_framework::ProviderExecutionLeaseTable::abort

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L134)

```cpp
ExecutionLeaseResult
  abort(const std::string& leaseId, const std::string& providerEpoch,
        const std::string& requesterName, const std::string& idempotencyKey,
        uint64_t nowMs);
```

### API-1d9d887e73b9 · ndn_service_framework::ProviderExecutionLeaseTable::renew

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L139)

```cpp
ExecutionLeaseResult
  renew(const std::string& leaseId, const std::string& providerEpoch,
        const std::string& requesterName, const std::string& idempotencyKey,
        uint64_t nowMs,
        uint64_t expiresAtMs);
```

### API-f52dcb87cd76 · ndn_service_framework::ProviderExecutionLeaseTable::release

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L145)

```cpp
ExecutionLeaseResult
  release(const std::string& leaseId, const std::string& providerEpoch,
          const std::string& requesterName, const std::string& idempotencyKey,
          uint64_t nowMs);
```

### API-3871127c97dd · ndn_service_framework::ProviderExecutionLeaseTable::cleanupExpired

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L150)

```cpp
size_t
  cleanupExpired(uint64_t nowMs);
```

### API-e43c54567dc6 · ndn_service_framework::ProviderExecutionLeaseTable::find

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L153)

```cpp
std::optional<GenericExecutionLease>
  find(const std::string& leaseId) const;
```

### API-ff673f94542f · ndn_service_framework::ProviderExecutionLeaseTable::hasActiveConflictKey

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L156)

```cpp
bool
  hasActiveConflictKey(const std::string& conflictKey, uint64_t nowMs);
```

### API-d32070d8a098 · ndn_service_framework::ProviderExecutionLeaseTable::hasPinnedBindingProof

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L159)

```cpp
bool
  hasPinnedBindingProof(const ndn::Buffer& resourceBindingProof, uint64_t nowMs);
```

### API-98c97965489e · ndn_service_framework::ProviderExecutionLeaseTable::counters

public / declared-interface；[源码](../../ndn-service-framework/ExecutionLease.hpp#L162)

```cpp
ExecutionLeaseCounters
  counters(uint64_t nowMs);
```

## ndn-service-framework/GenericSelectionTxnStore.hpp

源码 SHA-256：`05699eecd903a50fec0fe8303931719209bbf26656e0c9b5de9c662b352d0100`。

### API-c3c914ac4793 · ndn_service_framework::GenericSelectionTxnState

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L19)

```cpp
enum class GenericSelectionTxnState
```

### API-5229d6bfa4b4 · ndn_service_framework::GenericSelectionTxnState::Absent

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L21)

```cpp
Absent
```

### API-4056009f7773 · ndn_service_framework::GenericSelectionTxnState::Validating

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L22)

```cpp
Validating
```

### API-0db818ed151a · ndn_service_framework::GenericSelectionTxnState::Committed

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L23)

```cpp
Committed
```

### API-09c34e0e5a34 · ndn_service_framework::GenericSelectionTxnState::Aborted

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L24)

```cpp
Aborted
```

### API-9fdc879be77d · ndn_service_framework::GenericSelectionTxnState::Tombstoned

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L25)

```cpp
Tombstoned
```

### API-efd8498f4afb · ndn_service_framework::AuthenticatedSelectionContext

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L28)

```cpp
struct AuthenticatedSelectionContext
```

### API-c5212230355c · ndn_service_framework::AuthenticatedSelectionContext::transactionId

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L30)

```cpp
std::string transactionId;
```

### API-4beb4406a0d1 · ndn_service_framework::AuthenticatedSelectionContext::serviceName

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L31)

```cpp
ndn::Name serviceName;
```

### API-ffdbd2ad1558 · ndn_service_framework::AuthenticatedSelectionContext::requestId

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L32)

```cpp
ndn::Name requestId;
```

### API-10b9daf4c024 · ndn_service_framework::AuthenticatedSelectionContext::attempt

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L33)

```cpp
uint64_t attempt = 0;
```

### API-d79014427f36 · ndn_service_framework::AuthenticatedSelectionContext::selectionIdentity

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L34)

```cpp
std::string selectionIdentity;
```

### API-517874881972 · ndn_service_framework::AuthenticatedSelectionContext::selectionPayloadDigest

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L35)

```cpp
std::string selectionPayloadDigest;
```

### API-e922065c0dac · ndn_service_framework::AuthenticatedSelectionContext::providerIdentity

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L36)

```cpp
ndn::Name providerIdentity;
```

### API-20e6036f6399 · ndn_service_framework::AuthenticatedSelectionContext::providerBootEpoch

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L37)

```cpp
std::string providerBootEpoch;
```

### API-3e07f51ab6e3 · ndn_service_framework::AuthenticatedSelectionContext::localDeadline

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L38)

```cpp
std::chrono::steady_clock::time_point localDeadline;
```

### API-34c6851b84c1 · ndn_service_framework::AuthenticatedSelectionContext::expiresAtUnixMs

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L39)

```cpp
uint64_t expiresAtUnixMs = 0;
```

### API-751ab2a3e8b7 · ndn_service_framework::AuthenticatedSelectionContext::providerTokenRecordRef

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L40)

```cpp
std::string providerTokenRecordRef;
```

### API-973be00dea1b · ndn_service_framework::AuthenticatedSelectionContext::leaseRecordRef

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L41)

```cpp
std::optional<std::string> leaseRecordRef;
```

### API-720876002a63 · ndn_service_framework::OpaqueSelectionPrepareResult

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L44)

```cpp
struct OpaqueSelectionPrepareResult
```

### API-201fed6eb6f4 · ndn_service_framework::OpaqueSelectionPrepareResult::participantId

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L46)

```cpp
std::string participantId;
```

### API-e72bf8db6a35 · ndn_service_framework::OpaqueSelectionPrepareResult::participantVersion

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L47)

```cpp
uint32_t participantVersion = 0;
```

### API-09fc2ec5b402 · ndn_service_framework::OpaqueSelectionPrepareResult::commitBlob

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L48)

```cpp
ndn::Buffer commitBlob;
```

### API-997a8dd84cb6 · ndn_service_framework::OpaqueSelectionPrepareResult::commitBlobDigest

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L49)

```cpp
std::string commitBlobDigest;
```

### API-fb9a56bde3e3 · ndn_service_framework::OpaqueSelectionPrepareResult::acceptancePayload

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L50)

```cpp
ndn::Buffer acceptancePayload;
```

### API-1c9cae45fea9 · ndn_service_framework::OpaqueSelectionPrepareResult::acceptancePayloadDigest

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L51)

```cpp
std::string acceptancePayloadDigest;
```

### API-cf439fdb2a49 · ndn_service_framework::GenericCommittedSelectionView

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L54)

```cpp
struct GenericCommittedSelectionView
```

### API-ef6fe084ba83 · ndn_service_framework::GenericCommittedSelectionView::transactionId

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L56)

```cpp
std::string transactionId;
```

### API-1942921d03d7 · ndn_service_framework::GenericCommittedSelectionView::participantId

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L57)

```cpp
std::string participantId;
```

### API-6a6357fbe53f · ndn_service_framework::GenericCommittedSelectionView::participantVersion

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L58)

```cpp
uint32_t participantVersion = 0;
```

### API-2f0d95bee082 · ndn_service_framework::GenericCommittedSelectionView::serviceName

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L59)

```cpp
ndn::Name serviceName;
```

### API-16c147f64bc5 · ndn_service_framework::GenericCommittedSelectionView::requestId

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L60)

```cpp
ndn::Name requestId;
```

### API-165e64a1e819 · ndn_service_framework::GenericCommittedSelectionView::attempt

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L61)

```cpp
uint64_t attempt = 0;
```

### API-1c20fa734f8f · ndn_service_framework::GenericCommittedSelectionView::selectionIdentity

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L62)

```cpp
std::string selectionIdentity;
```

### API-372da3dd773d · ndn_service_framework::GenericCommittedSelectionView::selectionPayloadDigest

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L63)

```cpp
std::string selectionPayloadDigest;
```

### API-21e421f56501 · ndn_service_framework::GenericCommittedSelectionView::providerIdentity

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L64)

```cpp
ndn::Name providerIdentity;
```

### API-b496a49b0aa5 · ndn_service_framework::GenericCommittedSelectionView::providerBootEpoch

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L65)

```cpp
std::string providerBootEpoch;
```

### API-6e4d74fcb03f · ndn_service_framework::GenericCommittedSelectionView::providerTokenRecordRef

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L66)

```cpp
std::string providerTokenRecordRef;
```

### API-ced59dfd43b5 · ndn_service_framework::GenericCommittedSelectionView::leaseRecordRef

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L67)

```cpp
std::optional<std::string> leaseRecordRef;
```

### API-683a4bce4029 · ndn_service_framework::GenericCommittedSelectionView::commitBlob

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L68)

```cpp
ndn::Buffer commitBlob;
```

### API-d9b422fedade · ndn_service_framework::GenericCommittedSelectionView::commitBlobDigest

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L69)

```cpp
std::string commitBlobDigest;
```

### API-ae97ecfb0acb · ndn_service_framework::GenericCommittedSelectionView::acceptancePayload

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L70)

```cpp
ndn::Buffer acceptancePayload;
```

### API-65b6ed6a4856 · ndn_service_framework::GenericCommittedSelectionView::acceptancePayloadDigest

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L71)

```cpp
std::string acceptancePayloadDigest;
```

### API-24fdb2c28533 · ndn_service_framework::GenericCommittedSelectionView::committedAtUnixMs

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L72)

```cpp
uint64_t committedAtUnixMs = 0;
```

### API-8d95fa15881a · ndn_service_framework::GenericCommittedSelectionView::expiresAtUnixMs

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L73)

```cpp
uint64_t expiresAtUnixMs = 0;
```

### API-80cdb1d9b7ca · ndn_service_framework::OpaqueSelectionParticipant

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L76)

```cpp
class OpaqueSelectionParticipant
```

### API-9ffd0602a689 · ndn_service_framework::OpaqueSelectionParticipant::~OpaqueSelectionParticipant

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L79)

```cpp
virtual ~OpaqueSelectionParticipant() = default;
```

### API-9d3c67221829 · ndn_service_framework::OpaqueSelectionParticipant::participantId

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L81)

```cpp
virtual std::string participantId() const = 0;
```

### API-d5f02273a7a2 · ndn_service_framework::OpaqueSelectionParticipant::participantVersion

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L82)

```cpp
virtual uint32_t participantVersion() const = 0;
```

### API-881d023cdf95 · ndn_service_framework::OpaqueSelectionParticipant::prepare

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L84)

```cpp
virtual OpaqueSelectionPrepareResult
  prepare(const AuthenticatedSelectionContext& context,
          ndn::span<const uint8_t> payload) = 0;
```

### API-65796049fc7c · ndn_service_framework::OpaqueSelectionParticipant::onCommitted

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L88)

```cpp
virtual void
  onCommitted(const GenericCommittedSelectionView& committed) = 0;
```

### API-90ae486d3b96 · ndn_service_framework::OpaqueSelectionParticipant::onAborted

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L91)

```cpp
virtual void
  onAborted(const std::string& transactionId,
            const std::string& reasonCode) = 0;
```

### API-01d874200ac2 · ndn_service_framework::GenericSelectionTxnOptions

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L96)

```cpp
struct GenericSelectionTxnOptions
```

### API-c6140f6e5f9a · ndn_service_framework::GenericSelectionTxnOptions::maxCommitBlobBytes

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L98)

```cpp
size_t maxCommitBlobBytes = 1024 * 1024;
```

### API-53b561c9adf3 · ndn_service_framework::GenericSelectionTxnOptions::maxAcceptancePayloadBytes

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L99)

```cpp
size_t maxAcceptancePayloadBytes = 64 * 1024;
```

### API-8460f08e4f6f · ndn_service_framework::GenericSelectionTxnOptions::maxSelectionPayloadBytes

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L100)

```cpp
size_t maxSelectionPayloadBytes = 1024 * 1024;
```

### API-9962386bdcc5 · ndn_service_framework::GenericSelectionTxnOptions::maxPrepareTime

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L101)

```cpp
std::chrono::milliseconds maxPrepareTime{1000};
```

### API-16b77c746203 · ndn_service_framework::GenericSelectionTxnStore

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L104)

```cpp
class GenericSelectionTxnStore
```

### API-84f8e9188c22 · ndn_service_framework::GenericSelectionTxnStore::GenericSelectionTxnStore

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L107)

```cpp
GenericSelectionTxnStore(std::string walPath,
                           ndn::Buffer storageKey,
                           std::string storageKeyEpoch,
                           GenericSelectionTxnOptions options = {});
```

### API-c9db82422332 · ndn_service_framework::GenericSelectionTxnStore::commit

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L112)

```cpp
GenericCommittedSelectionView
  commit(const AuthenticatedSelectionContext& context,
         ndn::span<const uint8_t> selectionPayload,
         OpaqueSelectionParticipant& participant,
         bool providerTokenStillLive,
         bool leaseStillLive = true,
         bool notifyParticipant = true);
```

### API-02a9c568fc41 · ndn_service_framework::GenericSelectionTxnStore::findCommitted

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L120)

```cpp
std::optional<GenericCommittedSelectionView>
  findCommitted(const std::string& transactionId) const;
```

### API-e63fcba50ce8 · ndn_service_framework::GenericSelectionTxnStore::replayCommitted

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L123)

```cpp
void replayCommitted(
      const std::map<std::string, std::shared_ptr<OpaqueSelectionParticipant>>&
          participants,
      const std::string& currentProviderBootEpoch);
```

### API-ac5ce52689ab · ndn_service_framework::GenericSelectionTxnStore::tombstone

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L128)

```cpp
bool tombstone(const std::string& transactionId, uint64_t nowUnixMs);
```

### API-205fc1f606c7 · ndn_service_framework::GenericSelectionTxnStore::size

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L130)

```cpp
size_t size() const;
```

### API-71f0747c89d1 · ndn_service_framework::GenericSelectionTxnStore::digest

public / declared-interface；[源码](../../ndn-service-framework/GenericSelectionTxnStore.hpp#L132)

```cpp
static std::string digest(ndn::span<const uint8_t> bytes);
```

## ndn-service-framework/HybridMessageCrypto.hpp

源码 SHA-256：`9920dee2217633f5bdcba7cbaeb30c829d53b95a710396836d363c00a5544d54`。

### API-936ce933848c · ndn_service_framework::HybridAeadResult

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L16)

```cpp
struct HybridAeadResult
```

### API-6fa6c9fa18d5 · ndn_service_framework::HybridAeadResult::nonce

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L18)

```cpp
ndn::Buffer nonce;
```

### API-fe0227eee8d7 · ndn_service_framework::HybridAeadResult::ciphertext

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L19)

```cpp
ndn::Buffer ciphertext;
```

### API-70d0be58ca3d · ndn_service_framework::HybridAeadResult::tag

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L20)

```cpp
ndn::Buffer tag;
```

### API-f6a85da848f5 · ndn_service_framework::HybridMessageKey

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L23)

```cpp
struct HybridMessageKey
```

### API-c46905c66809 · ndn_service_framework::HybridMessageKey::keyId

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L25)

```cpp
std::string keyId;
```

### API-c4b57cc3eba0 · ndn_service_framework::HybridMessageKey::epochId

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L26)

```cpp
std::string epochId;
```

### API-c6c1df086427 · ndn_service_framework::HybridMessageKey::keyName

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L27)

```cpp
ndn::Name keyName;
```

### API-d681535feb1b · ndn_service_framework::HybridMessageKey::key

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L28)

```cpp
ndn::Buffer key;
```

### API-67f277623b59 · ndn_service_framework::HybridMessageKey::createdAt

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L29)

```cpp
std::chrono::steady_clock::time_point createdAt;
```

### API-42c037ff81d6 · ndn_service_framework::HybridMessageKey::uses

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L30)

```cpp
size_t uses = 0;
```

### API-f1485ceb34f3 · ndn_service_framework::HybridMessageCrypto

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L33)

```cpp
class HybridMessageCrypto
```

### API-0990ba89f340 · ndn_service_framework::HybridMessageCrypto::MESSAGE_KEY_SIZE

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L36)

```cpp
static constexpr size_t MESSAGE_KEY_SIZE = 32;
```

### API-0e14216a6667 · ndn_service_framework::HybridMessageCrypto::NONCE_SIZE

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L37)

```cpp
static constexpr size_t NONCE_SIZE = 12;
```

### API-76d2d275eba4 · ndn_service_framework::HybridMessageCrypto::TAG_SIZE

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L38)

```cpp
static constexpr size_t TAG_SIZE = 16;
```

### API-acd52c630fe3 · ndn_service_framework::HybridMessageCrypto::MAX_EPOCH_USES

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L39)

```cpp
static constexpr size_t MAX_EPOCH_USES = 10000;
```

### API-55e3763edb0f · ndn_service_framework::HybridMessageCrypto::MAX_EPOCH_AGE

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L40)

```cpp
static constexpr auto MAX_EPOCH_AGE = std::chrono::seconds(60);
```

### API-202c9d200dc3 · ndn_service_framework::HybridMessageCrypto::getOrCreateSendKey

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L42)

```cpp
HybridMessageKey getOrCreateSendKey(const ndn::Name& serviceName,
                                        const ndn::Name& senderPrefix,
                                        const std::string& accessAttribute,
                                        const std::string& direction,
                                        HybridCryptoCounters& counters);
```

### API-bd40210e5b70 · ndn_service_framework::HybridMessageCrypto::getOrCreateStatusSendKey

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L48)

```cpp
HybridMessageKey getOrCreateStatusSendKey(const ndn::Name& requesterIdentity,
                                              const std::string& statusHandle,
                                              const std::string& recipientKeyId,
                                              HybridCryptoCounters& counters);
```

### API-20e0474801b7 · ndn_service_framework::HybridMessageCrypto::cacheReceiveKey

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L53)

```cpp
void cacheReceiveKey(const std::string& keyId,
                         const std::string& epochId,
                         const ndn::Buffer& key);
```

### API-76d13397b49b · ndn_service_framework::HybridMessageCrypto::cacheReceiveKey

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L58)

```cpp
void cacheReceiveKey(const ndn::Name& serviceName,
                         const std::string& keyId,
                         const std::string& epochId,
                         const ndn::Buffer& key);
```

原始接口说明：

```text
/** Cache a received MessageKey with its service scope for revocation. */
```

### API-b7af6484506c · ndn_service_framework::HybridMessageCrypto::findReceiveKey

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L63)

```cpp
bool findReceiveKey(const std::string& keyId,
                        ndn::Buffer& key,
                        HybridCryptoCounters& counters);
```

### API-b89bc4aa89a8 · ndn_service_framework::HybridMessageCrypto::markSendKeyWrapped

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L67)

```cpp
void markSendKeyWrapped(const std::string& keyId);
```

### API-b78c5305feb0 · ndn_service_framework::HybridMessageCrypto::cacheWrappedSendKey

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L68)

```cpp
void cacheWrappedSendKey(const std::string& keyId,
                             const ndn::Buffer& wrappedKey);
```

### API-495dc0f16f1b · ndn_service_framework::HybridMessageCrypto::cacheWrappedSendKey

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L71)

```cpp
void cacheWrappedSendKey(const ndn::Name& serviceName,
                             const std::string& keyId,
                             const ndn::Buffer& wrappedKey);
```

原始接口说明：

```text
/** Cache wrapped key material with its service scope for revocation. */
```

### API-d54278dea89c · ndn_service_framework::HybridMessageCrypto::getWrappedSendKey

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L74)

```cpp
bool getWrappedSendKey(const std::string& keyId,
                           ndn::Buffer& wrappedKey) const;
```

### API-59a3f334b9cd · ndn_service_framework::HybridMessageCrypto::shouldAttachWrappedKey

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L76)

```cpp
bool shouldAttachWrappedKey(const std::string& keyId) const;
```

### API-5358877312f3 · ndn_service_framework::HybridMessageCrypto::invalidateService

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L83)

```cpp
size_t invalidateService(const ndn::Name& serviceName);
```

原始接口说明：

```text
/**
     * Evict all service-scoped send/receive/wrapped key material.  Unknown
     * unscoped legacy entries are retained rather than over-invalidated.
     * Returns the number of scoped entries removed.
     */
```

### API-b776693f9faa · ndn_service_framework::makeHybridMessageKeyName

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L109)

```cpp
ndn::Name makeHybridMessageKeyName(const ndn::Name& serviceName,
                                   const ndn::Name& senderPrefix,
                                   const std::string& accessAttribute,
                                   const std::string& epochId);
```

### API-ac6d6738b3a1 · ndn_service_framework::makeHybridMessageKeyDataName

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L115)

```cpp
ndn::Name makeHybridMessageKeyDataName(const ndn::Name& serviceName,
                                       const ndn::Name& senderPrefix,
                                       const std::string& accessAttribute,
                                       const std::string& epochId);
```

原始接口说明：

```text
/** Full producer-prefixed Data name used to recover the epoch wrapping. */
```

### API-936df82e96e5 · ndn_service_framework::hybridCompactKeyId

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L121)

```cpp
std::string hybridCompactKeyId(const std::string& keyId);
```

原始接口说明：

```text
/** Return the 8-byte, hex-encoded key identifier carried by envelope v2. */
```

### API-69726a06944a · ndn_service_framework::hybridAesGcmEncrypt

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L123)

```cpp
HybridAeadResult hybridAesGcmEncrypt(const ndn::Buffer& key,
                                     ndn::span<const uint8_t> plaintext,
                                     ndn::span<const uint8_t> associatedData);
```

### API-97082f55c4f1 · ndn_service_framework::hybridAesGcmEncryptWithNonce

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L127)

```cpp
HybridAeadResult hybridAesGcmEncryptWithNonce(
                                     const ndn::Buffer& key,
                                     ndn::span<const uint8_t> nonce,
                                     ndn::span<const uint8_t> plaintext,
                                     ndn::span<const uint8_t> associatedData);
```

### API-02a0a91d1d60 · ndn_service_framework::hybridAesGcmDecrypt

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L133)

```cpp
bool hybridAesGcmDecrypt(const ndn::Buffer& key,
                         const HybridMessageEnvelope& envelope,
                         ndn::span<const uint8_t> associatedData,
                         ndn::Buffer& plaintext);
```

### API-6c412f542903 · ndn_service_framework::hybridMessageTypeForName

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L138)

```cpp
std::string hybridMessageTypeForName(const ndn::Name& name);
```

### API-8a667207ca96 · ndn_service_framework::hybridAccessAttributeForName

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L139)

```cpp
std::string hybridAccessAttributeForName(const ndn::Name& name,
                                         const ndn::Name& serviceName);
```

### API-034d624d224a · ndn_service_framework::hybridAssociatedData

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L141)

```cpp
ndn::Buffer hybridAssociatedData(const ndn::Name& messageName,
                                 const std::string& messageType,
                                 const ndn::Name& requestId,
                                 const ndn::Name& serviceName,
                                 const ndn::Name& senderPrefix,
                                 const std::string& keyId,
                                 const std::string& epochId);
```

### API-3656d7c6049c · ndn_service_framework::secureStatusAssociatedData

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L149)

```cpp
ndn::Buffer secureStatusAssociatedData(const ndn::Name& dataName,
                                       uint64_t version,
                                       const std::string& statusHandle,
                                       const ndn::Name& requesterIdentity,
                                       const ndn::Name& providerIdentity,
                                       uint64_t attempt,
                                       const std::string& keyId,
                                       const std::string& epochId);
```

### API-4feb0dcb9a29 · ndn_service_framework::generateSecureStatusKeyHex

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L158)

```cpp
std::string generateSecureStatusKeyHex();
```

### API-7cacd032c03b · ndn_service_framework::decodeSecureStatusKeyHex

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L159)

```cpp
ndn::Buffer decodeSecureStatusKeyHex(const std::string& value);
```

### API-ae18b22a87b0 · ndn_service_framework::selectionGatedHex

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L161)

```cpp
std::string selectionGatedHex(ndn::span<const uint8_t> value);
```

### API-9d8d11dd1947 · ndn_service_framework::selectionGatedUnhex

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L162)

```cpp
ndn::Buffer selectionGatedUnhex(const std::string& value);
```

### API-087f102dc712 · ndn_service_framework::selectionGatedInputAssociatedData

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L163)

```cpp
ndn::Buffer selectionGatedInputAssociatedData(
    const ndn::Name& requester, const ndn::Name& serviceName,
    const ndn::Name& requestId);
```

### API-395d5cc61a18 · ndn_service_framework::encryptSelectionGatedInput

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L166)

```cpp
std::pair<EncryptedRequestInput, ndn::Buffer> encryptSelectionGatedInput(
    const ndn::Name& requester, const ndn::Name& serviceName,
    const ndn::Name& requestId, ndn::span<const uint8_t> plaintext);
```

### API-cd21c5cf15ae · ndn_service_framework::wrapSelectionGatedInputKey

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L169)

```cpp
ndn::Buffer wrapSelectionGatedInputKey(
    const ndn::Buffer& key, ndn::span<const uint8_t> recipientPublicKey);
```

### API-0756fc315694 · ndn_service_framework::unwrapSelectionGatedInputKey

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L171)

```cpp
ndn::Buffer unwrapSelectionGatedInputKey(
    const ndn::Buffer& wrappedKey, const ndn::Name& recipientCertName,
    const ndn::security::KeyChain& keyChain);
```

### API-f73a021ce40f · ndn_service_framework::decryptSelectionGatedInput

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L174)

```cpp
bool decryptSelectionGatedInput(
    const EncryptedRequestInput& encrypted, const ndn::Buffer& key,
    const ndn::Name& requester, const ndn::Name& serviceName,
    const ndn::Name& requestId, ndn::Buffer& plaintext);
```

### API-387b0d6ac040 · ndn_service_framework::encryptRecipientAssignment

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L178)

```cpp
RecipientEncryptedAssignment encryptRecipientAssignment(
    ndn::span<const uint8_t> plaintext,
    ndn::span<const uint8_t> recipientPublicKey,
    const ndn::Name& recipient, const ndn::Name& recipientCertName,
    ndn::span<const uint8_t> associatedData);
```

### API-4835833fb4b2 · ndn_service_framework::recipientAssignmentAssociatedData

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L183)

```cpp
ndn::Buffer recipientAssignmentAssociatedData(
    const ndn::Name& requester, const ndn::Name& provider,
    const ndn::Name& serviceName, const ndn::Name& requestId,
    const std::string& reservationId, const std::string& planDigest);
```

### API-eb302eb9af59 · ndn_service_framework::decryptRecipientAssignment

public / declared-interface；[源码](../../ndn-service-framework/HybridMessageCrypto.hpp#L187)

```cpp
bool decryptRecipientAssignment(
    const RecipientEncryptedAssignment& encrypted,
    const ndn::Name& expectedRecipient,
    const ndn::Name& expectedRecipientCertName,
    const ndn::security::KeyChain& keyChain,
    ndn::span<const uint8_t> associatedData,
    ndn::Buffer& plaintext);
```

## ndn-service-framework/InvocationStream.hpp

源码 SHA-256：`47222788c5ace05054eeaead0bb36c95579bdc183322e096c0e66e698fd03291`。

### API-1fc0b7a68ceb · ndn_service_framework::tlv::(anonymous)

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L27)

```cpp
enum : uint32_t
```

### API-d34d77c56150 · ndn_service_framework::tlv::(anonymous)::StreamRequestOptionsType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L28)

```cpp
StreamRequestOptionsType = 0xF661
```

### API-1737dd9afd73 · ndn_service_framework::tlv::(anonymous)::InvocationEventMessageType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L29)

```cpp
InvocationEventMessageType = 0xF662
```

### API-1824d232284e · ndn_service_framework::tlv::(anonymous)::StreamCompletionType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L30)

```cpp
StreamCompletionType = 0xF663
```

### API-984d44ded216 · ndn_service_framework::tlv::(anonymous)::StreamGenerationIdType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L31)

```cpp
StreamGenerationIdType = 0xF664
```

### API-7c12125ce653 · ndn_service_framework::tlv::(anonymous)::StreamEpochType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L32)

```cpp
StreamEpochType = 0xF665
```

### API-b2f8394fb72e · ndn_service_framework::tlv::(anonymous)::StreamEventKeyGrantType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L33)

```cpp
StreamEventKeyGrantType = 0xF666
```

### API-ecb07034889e · ndn_service_framework::tlv::(anonymous)::StreamMaxEventsType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L34)

```cpp
StreamMaxEventsType = 0xF667
```

### API-ed4790e544ef · ndn_service_framework::tlv::(anonymous)::StreamInterestWindowType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L35)

```cpp
StreamInterestWindowType = 0xF668
```

### API-aa6171961ea5 · ndn_service_framework::tlv::(anonymous)::StreamInterestLifetimeMsType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L36)

```cpp
StreamInterestLifetimeMsType = 0xF669
```

### API-c3d01ccb9753 · ndn_service_framework::tlv::(anonymous)::StreamMaxEventRetriesType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L37)

```cpp
StreamMaxEventRetriesType = 0xF66A
```

### API-2fece245aac5 · ndn_service_framework::tlv::(anonymous)::StreamPublisherQueueType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L38)

```cpp
StreamPublisherQueueType = 0xF66B
```

### API-a958ed978f9e · ndn_service_framework::tlv::(anonymous)::StreamCallbackQueueType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L39)

```cpp
StreamCallbackQueueType = 0xF66C
```

### API-253c0f2edb7c · ndn_service_framework::tlv::(anonymous)::StreamReorderCapacityType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L40)

```cpp
StreamReorderCapacityType = 0xF66D
```

### API-63af3d6e9754 · ndn_service_framework::tlv::(anonymous)::StreamRetentionMsType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L41)

```cpp
StreamRetentionMsType = 0xF66E
```

### API-cce90997e7d1 · ndn_service_framework::tlv::(anonymous)::StreamCompletionGraceMsType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L42)

```cpp
StreamCompletionGraceMsType = 0xF66F
```

### API-b4ee86ed7bff · ndn_service_framework::tlv::(anonymous)::StreamMaxEventWireBytesType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L43)

```cpp
StreamMaxEventWireBytesType = 0xF670
```

### API-1b8d3d3556f1 · ndn_service_framework::tlv::(anonymous)::StreamAllowReplacementType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L44)

```cpp
StreamAllowReplacementType = 0xF671
```

### API-6f5cc60185ec · ndn_service_framework::tlv::(anonymous)::StreamMaxReplacementsType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L45)

```cpp
StreamMaxReplacementsType = 0xF672
```

### API-752372f0163a · ndn_service_framework::tlv::(anonymous)::StreamBindingDigestType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L46)

```cpp
StreamBindingDigestType = 0xF673
```

### API-8415228e0719 · ndn_service_framework::tlv::(anonymous)::StreamCursorType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L47)

```cpp
StreamCursorType = 0xF674
```

### API-b687926b7446 · ndn_service_framework::tlv::(anonymous)::StreamEventTypeType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L48)

```cpp
StreamEventTypeType = 0xF675
```

### API-5de078638525 · ndn_service_framework::tlv::(anonymous)::StreamPayloadDigestType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L49)

```cpp
StreamPayloadDigestType = 0xF676
```

### API-a6950d23ff7f · ndn_service_framework::tlv::(anonymous)::StreamPublishedAtUsType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L50)

```cpp
StreamPublishedAtUsType = 0xF677
```

### API-641e84bc9c3b · ndn_service_framework::tlv::(anonymous)::StreamTerminalType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L51)

```cpp
StreamTerminalType = 0xF678
```

### API-a38461e3e581 · ndn_service_framework::tlv::(anonymous)::StreamEndEventType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L52)

```cpp
StreamEndEventType = 0xF679
```

### API-fc21c72c3a83 · ndn_service_framework::tlv::(anonymous)::StreamFinalCursorType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L53)

```cpp
StreamFinalCursorType = 0xF67A
```

### API-5ffba71cea85 · ndn_service_framework::tlv::(anonymous)::StreamFinishReasonType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L54)

```cpp
StreamFinishReasonType = 0xF67B
```

### API-1d4c5395a9ce · ndn_service_framework::tlv::(anonymous)::StreamApplicationEventCountType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L55)

```cpp
StreamApplicationEventCountType = 0xF67C
```

### API-593abc77ddde · ndn_service_framework::tlv::(anonymous)::StreamGeneratedTokenCountType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L56)

```cpp
StreamGeneratedTokenCountType = 0xF67D
```

### API-e9c87634bab4 · ndn_service_framework::tlv::(anonymous)::StreamTranscriptDigestType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L57)

```cpp
StreamTranscriptDigestType = 0xF67E
```

### API-a38e8832e08c · ndn_service_framework::tlv::(anonymous)::StreamFinalResultDigestType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L58)

```cpp
StreamFinalResultDigestType = 0xF67F
```

### API-6382170554d9 · ndn_service_framework::tlv::(anonymous)::StreamEndEventNameType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L59)

```cpp
StreamEndEventNameType = 0xF680
```

### API-dfe0e86f388d · ndn_service_framework::tlv::(anonymous)::StreamInvocationModeType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L60)

```cpp
StreamInvocationModeType = 0xF681
```

### API-7bc99c2ee0df · ndn_service_framework::tlv::(anonymous)::StreamEventKeyCommitmentType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L61)

```cpp
StreamEventKeyCommitmentType = 0xF682
```

### API-bc36767f41e4 · ndn_service_framework::tlv::(anonymous)::StreamDeadlineEpochMsType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L62)

```cpp
StreamDeadlineEpochMsType = 0xF683
```

### API-f91893182d04 · ndn_service_framework::tlv::(anonymous)::StreamAttemptEpochType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L63)

```cpp
StreamAttemptEpochType = 0xF684
```

### API-26cc9d24698b · ndn_service_framework::tlv::(anonymous)::ConversationContinuationType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L64)

```cpp
ConversationContinuationType = 0xF685
```

### API-8fa44940d294 · ndn_service_framework::tlv::(anonymous)::ConversationModeType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L65)

```cpp
ConversationModeType = 0xF686
```

### API-7e9168cb89d0 · ndn_service_framework::tlv::(anonymous)::ConversationIdType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L66)

```cpp
ConversationIdType = 0xF687
```

### API-2f3f7f508274 · ndn_service_framework::tlv::(anonymous)::ConversationParentEpochType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L67)

```cpp
ConversationParentEpochType = 0xF688
```

### API-7a84a479d823 · ndn_service_framework::tlv::(anonymous)::ConversationContextEpochType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L68)

```cpp
ConversationContextEpochType = 0xF689
```

### API-15bc9d771b11 · ndn_service_framework::tlv::(anonymous)::ConversationCheckpointType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L69)

```cpp
ConversationCheckpointType = 0xF68A
```

### API-aec4232464c1 · ndn_service_framework::tlv::(anonymous)::ConversationCheckpointDigestType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L70)

```cpp
ConversationCheckpointDigestType = 0xF68B
```

### API-0d8dd9b71091 · ndn_service_framework::tlv::(anonymous)::ConversationTurnInputDigestType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L71)

```cpp
ConversationTurnInputDigestType = 0xF68C
```

### API-e781f610085d · ndn_service_framework::tlv::(anonymous)::ConversationAllowFallbackType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L72)

```cpp
ConversationAllowFallbackType = 0xF68D
```

### API-818be6067e71 · ndn_service_framework::tlv::(anonymous)::ConversationFallbackInputDigestType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L73)

```cpp
ConversationFallbackInputDigestType = 0xF68E
```

### API-8192072eea8d · ndn_service_framework::tlv::(anonymous)::ConversationRoleReceiptType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L74)

```cpp
ConversationRoleReceiptType = 0xF68F
```

### API-4bf8d72720b1 · ndn_service_framework::tlv::(anonymous)::ConversationRoleReceiptDigestType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L75)

```cpp
ConversationRoleReceiptDigestType = 0xF690
```

### API-57d3728dcebe · ndn_service_framework::tlv::(anonymous)::ConversationPlanRoleMapDigestType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L76)

```cpp
ConversationPlanRoleMapDigestType = 0xF691
```

### API-b807eccba521 · ndn_service_framework::tlv::(anonymous)::ConversationStateResidencyType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L77)

```cpp
ConversationStateResidencyType = 0xF692
```

### API-3719e1aad12f · ndn_service_framework::tlv::(anonymous)::ConversationStateReadyType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L78)

```cpp
ConversationStateReadyType = 0xF693
```

### API-b7c030b220c4 · ndn_service_framework::tlv::(anonymous)::ConversationStateReasonType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L79)

```cpp
ConversationStateReasonType = 0xF694
```

### API-ce8a189c3bda · ndn_service_framework::tlv::(anonymous)::ConversationStateBytesType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L80)

```cpp
ConversationStateBytesType = 0xF695
```

### API-c4a9b08a6218 · ndn_service_framework::tlv::(anonymous)::ConversationExpiresAtType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L81)

```cpp
ConversationExpiresAtType = 0xF696
```

### API-a85051360606 · ndn_service_framework::tlv::(anonymous)::ConversationCommitStatusType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L82)

```cpp
ConversationCommitStatusType = 0xF697
```

### API-75236edfc189 · ndn_service_framework::tlv::(anonymous)::ConversationTurnCompletionType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L83)

```cpp
ConversationTurnCompletionType = 0xF698
```

### API-2c99999c9fb4 · ndn_service_framework::tlv::(anonymous)::StreamControllerVersionType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L84)

```cpp
StreamControllerVersionType = 0xF699
```

### API-994f488008c5 · ndn_service_framework::StreamDigest

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L88)

```cpp
using StreamDigest = std::array<uint8_t, 32>;
```

原始接口说明：

```text
// namespace tlv
```

### API-4ec598c12666 · ndn_service_framework::StreamGenerationId

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L89)

```cpp
using StreamGenerationId = std::array<uint8_t, 16>;
```

### API-0a3432070918 · ndn_service_framework::StreamNonce

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L90)

```cpp
using StreamNonce = std::array<uint8_t, 12>;
```

### API-24d11b624920 · ndn_service_framework::InvocationMode

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L92)

```cpp
enum class InvocationMode : uint64_t
```

### API-6d2a411db215 · ndn_service_framework::InvocationMode::Normal

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L93)

```cpp
Normal = 0
```

### API-949db6e8cf78 · ndn_service_framework::InvocationMode::Targeted

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L94)

```cpp
Targeted = 1
```

### API-bd8d76e89a94 · ndn_service_framework::ConversationInputMode

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L104)

```cpp
enum class ConversationInputMode : uint64_t
```

### API-c268a3511c06 · ndn_service_framework::ConversationInputMode::FullContext

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L105)

```cpp
FullContext = 0
```

### API-9225be72fbab · ndn_service_framework::ConversationInputMode::AppendDelta

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L106)

```cpp
AppendDelta = 1
```

### API-11b636c640b9 · ndn_service_framework::ConversationContinuationOptions

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L109)

```cpp
struct ConversationContinuationOptions
```

### API-6bfb239f12f3 · ndn_service_framework::ConversationContinuationOptions::VERSION

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L111)

```cpp
static constexpr uint64_t VERSION = 1;
```

### API-739aef7af143 · ndn_service_framework::ConversationContinuationOptions::version

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L113)

```cpp
uint64_t version = VERSION;
```

### API-0d6857f929fd · ndn_service_framework::ConversationContinuationOptions::mode

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L114)

```cpp
ConversationInputMode mode = ConversationInputMode::FullContext;
```

### API-25a1352f7020 · ndn_service_framework::ConversationContinuationOptions::conversationId

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L115)

```cpp
std::array<uint8_t, 16> conversationId{};
```

### API-3dd413b0ffa2 · ndn_service_framework::ConversationContinuationOptions::parentContextEpoch

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L116)

```cpp
std::optional<uint64_t> parentContextEpoch;
```

### API-a57745588f08 · ndn_service_framework::ConversationContinuationOptions::parentCheckpoint

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L117)

```cpp
std::optional<ndn::Buffer> parentCheckpoint;
```

### API-81510bac1ca8 · ndn_service_framework::ConversationContinuationOptions::turnInputDigest

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L118)

```cpp
StreamDigest turnInputDigest{};
```

### API-c6f557a29461 · ndn_service_framework::ConversationContinuationOptions::allowFullPrefillFallback

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L119)

```cpp
bool allowFullPrefillFallback = false;
```

### API-cc69ba36f3a5 · ndn_service_framework::ConversationContinuationOptions::fallbackInputDigest

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L120)

```cpp
std::optional<StreamDigest> fallbackInputDigest;
```

### API-593b2814ab56 · ndn_service_framework::ConversationContinuationOptions::validate

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L122)

```cpp
void validate() const;
```

### API-d829146f6e45 · ndn_service_framework::ConversationContinuationOptions::wireEncode

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L123)

```cpp
ndn::Block wireEncode() const;
```

### API-25b72866c7ae · ndn_service_framework::ConversationContinuationOptions::wireDecode

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L124)

```cpp
bool wireDecode(const ndn::Block& wire);
```

### API-22efca5aad3e · ndn_service_framework::ConversationContinuationOptions::operator==

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L125)

```cpp
bool operator==(const ConversationContinuationOptions& other) const;
```

### API-3fec421d66af · ndn_service_framework::InvocationEventType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L128)

```cpp
enum class InvocationEventType : uint64_t
```

### API-f71d62092261 · ndn_service_framework::InvocationEventType::Application

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L129)

```cpp
Application = 1
```

### API-caffefa09a0f · ndn_service_framework::InvocationEventType::End

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L130)

```cpp
End = 2
```

### API-ecab3671f0f7 · ndn_service_framework::StreamFinishReason

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L133)

```cpp
enum class StreamFinishReason : uint64_t
```

### API-3406e9494a1c · ndn_service_framework::StreamFinishReason::Eos

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L134)

```cpp
Eos = 1
```

### API-f2324d5c30bd · ndn_service_framework::StreamFinishReason::StopSequence

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L135)

```cpp
StopSequence = 2
```

### API-5d88c2f3d3e1 · ndn_service_framework::StreamFinishReason::MaxTokens

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L136)

```cpp
MaxTokens = 3
```

### API-37a145c72a44 · ndn_service_framework::StreamFinishReason::ApplicationComplete

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L137)

```cpp
ApplicationComplete = 4
```

### API-a0969d3fafc0 · ndn_service_framework::StreamFinishReason::Deadline

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L138)

```cpp
Deadline = 5
```

### API-83e705a6d2b1 · ndn_service_framework::StreamFinishReason::Cancelled

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L139)

```cpp
Cancelled = 6
```

### API-ae3545d9fe45 · ndn_service_framework::StreamFinishReason::Failed

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L140)

```cpp
Failed = 7
```

### API-67c1071bed14 · ndn_service_framework::StreamedInvocationStatus

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L143)

```cpp
enum class StreamedInvocationStatus : uint8_t
```

### API-ec68dfdf09aa · ndn_service_framework::StreamedInvocationStatus::Created

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L144)

```cpp
Created = 0
```

### API-025b41939f39 · ndn_service_framework::StreamedInvocationStatus::Requesting

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L145)

```cpp
Requesting = 1
```

### API-301745b74972 · ndn_service_framework::StreamedInvocationStatus::Selecting

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L146)

```cpp
Selecting = 2
```

### API-ef86bc755c33 · ndn_service_framework::StreamedInvocationStatus::Streaming

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L147)

```cpp
Streaming = 3
```

### API-f0b7ae8ffa3f · ndn_service_framework::StreamedInvocationStatus::Draining

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L148)

```cpp
Draining = 4
```

### API-127013cd4e29 · ndn_service_framework::StreamedInvocationStatus::Completed

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L149)

```cpp
Completed = 5
```

### API-d2d43c0921d8 · ndn_service_framework::StreamedInvocationStatus::Failed

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L150)

```cpp
Failed = 6
```

### API-f9b0dbfa1cc7 · ndn_service_framework::StreamedInvocationStatus::Cancelled

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L151)

```cpp
Cancelled = 7
```

### API-e3d4d66af417 · ndn_service_framework::StreamedInvocationErrorCode

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L154)

```cpp
enum class StreamedInvocationErrorCode : uint8_t
```

### API-012190225dd5 · ndn_service_framework::StreamedInvocationErrorCode::StreamUnsupported

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L155)

```cpp
StreamUnsupported = 1
```

### API-525f39eb0399 · ndn_service_framework::StreamedInvocationErrorCode::InvalidOptions

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L156)

```cpp
InvalidOptions = 2
```

### API-b8cf881d29eb · ndn_service_framework::StreamedInvocationErrorCode::Unauthorized

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L157)

```cpp
Unauthorized = 3
```

### API-bfc6e9d8b1a9 · ndn_service_framework::StreamedInvocationErrorCode::AckTimeout

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L158)

```cpp
AckTimeout = 4
```

### API-4a606b1c744c · ndn_service_framework::StreamedInvocationErrorCode::SelectionFailed

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L159)

```cpp
SelectionFailed = 5
```

### API-469f58c9fe88 · ndn_service_framework::StreamedInvocationErrorCode::EventTimeout

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L160)

```cpp
EventTimeout = 6
```

### API-cfc1ad04aced · ndn_service_framework::StreamedInvocationErrorCode::EventOutsideRetention

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L161)

```cpp
EventOutsideRetention = 7
```

### API-7f6a6c77063d · ndn_service_framework::StreamedInvocationErrorCode::EventOversize

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L162)

```cpp
EventOversize = 8
```

### API-1ca01a301411 · ndn_service_framework::StreamedInvocationErrorCode::QueueDeadline

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L163)

```cpp
QueueDeadline = 9
```

### API-c72eb2ed84eb · ndn_service_framework::StreamedInvocationErrorCode::InvalidName

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L164)

```cpp
InvalidName = 10
```

### API-e7f4174531e8 · ndn_service_framework::StreamedInvocationErrorCode::InvalidSignature

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L165)

```cpp
InvalidSignature = 11
```

### API-381d7e42b0d4 · ndn_service_framework::StreamedInvocationErrorCode::DecryptionFailed

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L166)

```cpp
DecryptionFailed = 12
```

### API-c391a2e1ecc2 · ndn_service_framework::StreamedInvocationErrorCode::BindingMismatch

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L167)

```cpp
BindingMismatch = 13
```

### API-43929dcc2d47 · ndn_service_framework::StreamedInvocationErrorCode::CursorMismatch

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L168)

```cpp
CursorMismatch = 14
```

### API-6fe32cd0c32f · ndn_service_framework::StreamedInvocationErrorCode::TranscriptMismatch

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L169)

```cpp
TranscriptMismatch = 15
```

### API-d9ec067df035 · ndn_service_framework::StreamedInvocationErrorCode::TerminalMismatch

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L170)

```cpp
TerminalMismatch = 16
```

### API-30da29428bcf · ndn_service_framework::StreamedInvocationErrorCode::ProviderFailure

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L171)

```cpp
ProviderFailure = 17
```

### API-7bb04105e267 · ndn_service_framework::StreamedInvocationErrorCode::Deadline

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L172)

```cpp
Deadline = 18
```

### API-bcc3ca73009b · ndn_service_framework::StreamedInvocationErrorCode::Cancelled

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L173)

```cpp
Cancelled = 19
```

### API-0653868c921f · ndn_service_framework::StreamedInvocationErrorCode::ApplicationCallbackFailed

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L174)

```cpp
ApplicationCallbackFailed = 20
```

### API-015479bef867 · ndn_service_framework::StreamedInvocationErrorCode::ReplacementUnavailable

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L175)

```cpp
ReplacementUnavailable = 21
```

### API-caa7b63e5034 · ndn_service_framework::StreamedInvocationError

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L178)

```cpp
struct StreamedInvocationError
```

### API-44cd93bc9814 · ndn_service_framework::StreamedInvocationError::StreamedInvocationError

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L179)

```cpp
StreamedInvocationError() = default;
```

### API-5a4ed08dfac4 · ndn_service_framework::StreamedInvocationError::StreamedInvocationError

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L181)

```cpp
StreamedInvocationError(StreamedInvocationErrorCode errorCode,
                          std::string errorMessage,
                          ndn::Name invocationRequestId = {},
                          uint64_t invocationExpectedCursor = 0,
                          ndn::Name failedProviderName = {})
```

### API-4fac024bfc19 · ndn_service_framework::StreamedInvocationError::code

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L194)

```cpp
StreamedInvocationErrorCode code = StreamedInvocationErrorCode::EventTimeout;
```

### API-a16e0f0b2872 · ndn_service_framework::StreamedInvocationError::message

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L195)

```cpp
std::string message;
```

### API-32337a93f902 · ndn_service_framework::StreamedInvocationError::requestId

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L196)

```cpp
ndn::Name requestId;
```

### API-b0d694921c7c · ndn_service_framework::StreamedInvocationError::expectedCursor

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L197)

```cpp
uint64_t expectedCursor = 0;
```

### API-ee0fa09ceb1d · ndn_service_framework::StreamedInvocationError::providerName

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L201)

```cpp
ndn::Name providerName;
```

原始接口说明：

```text
/** Provider whose authenticated response/event path produced the failure.
   * Empty means that no Provider has been established and replacement must
   * not guess one from the accepted plan. */
```

### API-180d0648d239 · ndn_service_framework::StreamedInvocationMetrics

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L204)

```cpp
struct StreamedInvocationMetrics
```

### API-9fd335ed29bf · ndn_service_framework::StreamedInvocationMetrics::publishedEvents

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L205)

```cpp
uint64_t publishedEvents = 0;
```

### API-f064f3318abf · ndn_service_framework::StreamedInvocationMetrics::deliveredEvents

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L206)

```cpp
uint64_t deliveredEvents = 0;
```

### API-fd2d565567e1 · ndn_service_framework::StreamedInvocationMetrics::retryCount

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L207)

```cpp
uint64_t retryCount = 0;
```

### API-3cfa126ff08f · ndn_service_framework::StreamedInvocationMetrics::duplicateCount

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L208)

```cpp
uint64_t duplicateCount = 0;
```

### API-fbe061215002 · ndn_service_framework::StreamedInvocationSharedState

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L212)

```cpp
struct StreamedInvocationSharedState
```

### API-6e260e2aa556 · ndn_service_framework::StreamedInvocationSharedState::mutex

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L213)

```cpp
mutable std::mutex mutex;
```

### API-ce7a68c68165 · ndn_service_framework::StreamedInvocationSharedState::requestId

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L214)

```cpp
ndn::Name requestId;
```

### API-5b9303b42335 · ndn_service_framework::StreamedInvocationSharedState::status

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L215)

```cpp
StreamedInvocationStatus status = StreamedInvocationStatus::Created;
```

### API-139c05173672 · ndn_service_framework::StreamedInvocationSharedState::metrics

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L216)

```cpp
StreamedInvocationMetrics metrics;
```

### API-be9efe4a2df7 · ndn_service_framework::StreamedInvocationSharedState::cancelFence

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L220)

```cpp
std::function<void()> cancelFence;
```

原始接口说明：

```text
// The public handle can be cancelled from an application callback thread.
// Keep a synchronous fence callback in addition to the Face-owned cleanup
// callback so queued events cannot overtake cancellation.
```

### API-c49a671709bc · ndn_service_framework::StreamedInvocationSharedState::cancel

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L221)

```cpp
std::function<void()> cancel;
```

### API-c3a4e075a8b1 · ndn_service_framework::StreamedInvocationSharedState::onEvent

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L222)

```cpp
std::function<void(const ndn::Buffer&)> onEvent;
```

### API-fb7c45b83141 · ndn_service_framework::StreamedInvocationSharedState::onComplete

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L223)

```cpp
std::function<void(const ndn::Buffer&)> onComplete;
```

### API-208be1058185 · ndn_service_framework::StreamedInvocationSharedState::onError

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L224)

```cpp
std::function<void(const StreamedInvocationError&)> onError;
```

### API-67b8f9bc6f26 · ndn_service_framework::StreamedInvocationHandle

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L228)

```cpp
class StreamedInvocationHandle
```

### API-6ebdbb23c254 · ndn_service_framework::StreamedInvocationHandle::StreamedInvocationHandle

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L231)

```cpp
explicit StreamedInvocationHandle(std::shared_ptr<StreamedInvocationSharedState> state)
```

### API-1942888ca547 · ndn_service_framework::StreamedInvocationHandle::StreamedInvocationHandle

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L239)

```cpp
StreamedInvocationHandle(const StreamedInvocationHandle&) = delete;
```

### API-dadf19eb990d · ndn_service_framework::StreamedInvocationHandle::operator=

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L240)

```cpp
StreamedInvocationHandle& operator=(const StreamedInvocationHandle&) = delete;
```

### API-d5091c31ece5 · ndn_service_framework::StreamedInvocationHandle::requestId

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L242)

```cpp
const ndn::Name& requestId() const
```

### API-9f2761988257 · ndn_service_framework::StreamedInvocationHandle::status

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L244)

```cpp
StreamedInvocationStatus status() const
```

### API-3033bf524b2f · ndn_service_framework::StreamedInvocationHandle::metrics

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L250)

```cpp
StreamedInvocationMetrics metrics() const
```

### API-fcad9243e6f0 · ndn_service_framework::StreamedInvocationHandle::cancel

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L256)

```cpp
void cancel()
```

### API-f7a7013bc46b · ndn_service_framework::StreamedInvocationHandle::sharedState

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L273)

```cpp
const std::shared_ptr<StreamedInvocationSharedState>& sharedState() const
```

### API-322967393efc · ndn_service_framework::StreamedResponseWriterCore

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L282)

```cpp
class StreamedResponseWriterCore
```

### API-2bc1b3fde985 · ndn_service_framework::StreamedResponseWriterCore::PublishCallback

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L285)

```cpp
using PublishCallback = std::function<bool(const ndn::Buffer&, uint64_t&)>;
```

### API-024a7fffaaa2 · ndn_service_framework::StreamedResponseWriterCore::FinishCallback

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L286)

```cpp
using FinishCallback = std::function<bool(const ndn::Buffer&, StreamFinishReason)>;
```

### API-1479b16251fd · ndn_service_framework::StreamedResponseWriterCore::FailCallback

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L287)

```cpp
using FailCallback = std::function<bool(StreamedInvocationErrorCode,
                                          const std::string&)>;
```

### API-75be836d4569 · ndn_service_framework::StreamedResponseWriterCore::CancelledCallback

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L289)

```cpp
using CancelledCallback = std::function<bool()>;
```

### API-977efd3b5449 · ndn_service_framework::StreamedResponseWriterCore::RemainingCallback

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L290)

```cpp
using RemainingCallback = std::function<std::chrono::milliseconds()>;
```

### API-f7d124e974c6 · ndn_service_framework::StreamedResponseWriterCore::StreamedResponseWriterCore

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L292)

```cpp
StreamedResponseWriterCore(PublishCallback publish,
                             FinishCallback finish,
                             FailCallback fail,
                             CancelledCallback cancelled,
                             RemainingCallback remaining)
```

### API-0aedc3967103 · ndn_service_framework::StreamedResponseWriterCore::publish

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L302)

```cpp
bool publish(const ndn::Buffer& payload, uint64_t& cursor);
```

### API-5ea9c463760f · ndn_service_framework::StreamedResponseWriterCore::finish

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L303)

```cpp
bool finish(const ndn::Buffer& payload, StreamFinishReason reason);
```

### API-d50e917286c4 · ndn_service_framework::StreamedResponseWriterCore::fail

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L304)

```cpp
bool fail(StreamedInvocationErrorCode code, const std::string& message);
```

### API-c99c4fe6d6ea · ndn_service_framework::StreamedResponseWriterCore::isCancelled

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L305)

```cpp
bool isCancelled() const;
```

### API-0e3eb7d9417a · ndn_service_framework::StreamedResponseWriterCore::remainingDeadline

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L306)

```cpp
std::chrono::milliseconds remainingDeadline() const;
```

### API-efa9522970c1 · ndn_service_framework::StreamedResponseWriterCore::isTerminal

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L307)

```cpp
bool isTerminal() const;
```

### API-00ccf997b896 · ndn_service_framework::StreamedResponseWriterCore::invalidate

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L308)

```cpp
void invalidate();
```

### API-f4ba15e28af4 · ndn_service_framework::serializeStreamValue

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L322)

```cpp
template<typename T>
inline ndn::Buffer serializeStreamValue(const T& value)
```

### API-da120ff0b444 · ndn_service_framework::StreamedResponseWriter

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L337)

```cpp
class StreamedResponseWriter
```

### API-31df0c941115 · ndn_service_framework::StreamedResponseWriter::StreamedResponseWriter

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L340)

```cpp
explicit StreamedResponseWriter(std::shared_ptr<StreamedResponseWriterCore> core)
```

### API-e0eb94c487dc · ndn_service_framework::StreamedResponseWriter::StreamedResponseWriter

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L345)

```cpp
StreamedResponseWriter(const StreamedResponseWriter&) = delete;
```

### API-2675ae622880 · ndn_service_framework::StreamedResponseWriter::operator=

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L346)

```cpp
StreamedResponseWriter& operator=(const StreamedResponseWriter&) = delete;
```

### API-2eecc93af394 · ndn_service_framework::StreamedResponseWriter::publish

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L348)

```cpp
bool publish(const EventT& event)
```

### API-2bdd4eb83edb · ndn_service_framework::StreamedResponseWriter::finish

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L359)

```cpp
void finish(const ResponseT& response, StreamFinishReason reason)
```

### API-a83ef388a7c3 · ndn_service_framework::StreamedResponseWriter::fail

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L366)

```cpp
void fail(StreamedInvocationErrorCode code, std::string message)
```

### API-7fd410e56097 · ndn_service_framework::StreamedResponseWriter::isCancelled

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L373)

```cpp
bool isCancelled() const
```

### API-8d8b18fde58f · ndn_service_framework::StreamedResponseWriter::remainingDeadline

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L374)

```cpp
std::chrono::milliseconds remainingDeadline() const
```

### API-c016321686c7 · ndn_service_framework::StreamedResponseWriter::core

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L379)

```cpp
const std::shared_ptr<StreamedResponseWriterCore>& core() const
```

### API-360359b9d92c · ndn_service_framework::StreamRequestOptions

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L385)

```cpp
struct StreamRequestOptions
```

### API-127d32579b0d · ndn_service_framework::StreamRequestOptions::VERSION

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L387)

```cpp
static constexpr uint64_t VERSION = 1;
```

### API-8f66b41f26b5 · ndn_service_framework::StreamRequestOptions::version

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L389)

```cpp
uint64_t version = VERSION;
```

### API-ce27b8dc5e99 · ndn_service_framework::StreamRequestOptions::mode

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L390)

```cpp
InvocationMode mode = InvocationMode::Normal;
```

### API-318ac98c00e2 · ndn_service_framework::StreamRequestOptions::generationId

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L391)

```cpp
StreamGenerationId generationId{};
```

### API-37234667196b · ndn_service_framework::StreamRequestOptions::attemptEpoch

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L392)

```cpp
uint64_t attemptEpoch = 1;
```

### API-2dd945ea8b06 · ndn_service_framework::StreamRequestOptions::streamEpoch

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L393)

```cpp
uint64_t streamEpoch = 0;
```

### API-ebc0ed9d327e · ndn_service_framework::StreamRequestOptions::eventKeyCommitment

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L394)

```cpp
StreamDigest eventKeyCommitment{};
```

### API-37ca319097c8 · ndn_service_framework::StreamRequestOptions::deadlineEpochMs

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L397)

```cpp
uint64_t deadlineEpochMs = 0;
```

原始接口说明：

```text
// Absolute deadline sealed into the request so both endpoints derive the
// same StreamBindingV1 digest.
```

### API-f6e38b9d3cbf · ndn_service_framework::StreamRequestOptions::controllerVersion

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L402)

```cpp
std::optional<ControllerVersion> controllerVersion;
```

原始接口说明：

```text
// The authenticated ControllerVersion is carried once in the stream
// invocation options and then included in the StreamBinding digest.  It is
// optional for legacy unprotected streams; request-scoped/revocable paths
// must populate it before validation.
```

### API-63539d446c24 · ndn_service_framework::StreamRequestOptions::eventKeyGrant

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L406)

```cpp
std::optional<ndn::Block> eventKeyGrant;
```

原始接口说明：

```text
// Exact HybridMessageEnvelope wire. It is allowed only for an explicit
// selection-free Targeted request; combined Request validation owns that
// transport-path decision.
```

### API-205bded4f299 · ndn_service_framework::StreamRequestOptions::maxEvents

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L407)

```cpp
uint32_t maxEvents = 512;
```

### API-d86d422b7ab7 · ndn_service_framework::StreamRequestOptions::interestWindow

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L408)

```cpp
uint16_t interestWindow = 16;
```

### API-7bc61977ebf6 · ndn_service_framework::StreamRequestOptions::interestLifetimeMs

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L409)

```cpp
uint32_t interestLifetimeMs = 500;
```

### API-7db4d51f8e9a · ndn_service_framework::StreamRequestOptions::maxEventRetries

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L410)

```cpp
uint8_t maxEventRetries = 3;
```

### API-8652aa88b671 · ndn_service_framework::StreamRequestOptions::publisherQueueCapacity

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L411)

```cpp
uint16_t publisherQueueCapacity = 64;
```

### API-0aad3dd95355 · ndn_service_framework::StreamRequestOptions::callbackQueueCapacity

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L412)

```cpp
uint16_t callbackQueueCapacity = 64;
```

### API-6894809f262c · ndn_service_framework::StreamRequestOptions::reorderCapacity

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L413)

```cpp
uint16_t reorderCapacity = 64;
```

### API-bf91517250ea · ndn_service_framework::StreamRequestOptions::retentionMs

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L414)

```cpp
uint32_t retentionMs = 30000;
```

### API-4ff677cab3a9 · ndn_service_framework::StreamRequestOptions::completionGraceMs

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L415)

```cpp
uint32_t completionGraceMs = 5000;
```

### API-73348ea05167 · ndn_service_framework::StreamRequestOptions::maxEventWireBytes

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L416)

```cpp
uint32_t maxEventWireBytes = 16384;
```

### API-2d8ddc5f80c5 · ndn_service_framework::StreamRequestOptions::allowReplacement

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L417)

```cpp
bool allowReplacement = false;
```

### API-e5ce8b0320fe · ndn_service_framework::StreamRequestOptions::maxReplacements

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L418)

```cpp
uint8_t maxReplacements = 0;
```

### API-87567d6dfdaa · ndn_service_framework::StreamRequestOptions::validate

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L420)

```cpp
void validate() const;
```

### API-850f6c538101 · ndn_service_framework::StreamRequestOptions::wireEncode

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L421)

```cpp
ndn::Block wireEncode() const;
```

### API-dc6611e187a2 · ndn_service_framework::StreamRequestOptions::wireDecode

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L422)

```cpp
bool wireDecode(const ndn::Block& wire);
```

### API-decc161375d5 · ndn_service_framework::StreamRequestOptions::operator==

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L423)

```cpp
bool operator==(const StreamRequestOptions& other) const;
```

### API-0a8f6af38951 · ndn_service_framework::StreamedInvocationOptions

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L428)

```cpp
struct StreamedInvocationOptions : StreamRequestOptions
```

### API-24115433f7d5 · ndn_service_framework::StreamedInvocationOptions::StreamedInvocationOptions

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L429)

```cpp
StreamedInvocationOptions() = default;
```

### API-5c578261107f · ndn_service_framework::StreamEndEvent

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L432)

```cpp
struct StreamEndEvent
```

### API-694e58802632 · ndn_service_framework::StreamEndEvent::finalCursor

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L434)

```cpp
uint64_t finalCursor = 0;
```

### API-ffe693729ad0 · ndn_service_framework::StreamEndEvent::finishReason

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L435)

```cpp
StreamFinishReason finishReason = StreamFinishReason::Failed;
```

### API-c436b3eec5e2 · ndn_service_framework::StreamEndEvent::applicationEventCount

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L436)

```cpp
uint64_t applicationEventCount = 0;
```

### API-9bde2421c77a · ndn_service_framework::StreamEndEvent::generatedTokenCount

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L437)

```cpp
uint64_t generatedTokenCount = 0;
```

### API-032219ac8326 · ndn_service_framework::StreamEndEvent::transcriptDigest

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L438)

```cpp
StreamDigest transcriptDigest{};
```

### API-1670b12214fd · ndn_service_framework::StreamEndEvent::finalResultDigest

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L439)

```cpp
StreamDigest finalResultDigest{};
```

### API-e44b1db881a5 · ndn_service_framework::StreamEndEvent::errorInfo

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L440)

```cpp
std::string errorInfo;
```

### API-f48ca4dd130c · ndn_service_framework::StreamEndEvent::validate

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L442)

```cpp
void validate() const;
```

### API-6b85f9ea8597 · ndn_service_framework::StreamEndEvent::wireEncode

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L443)

```cpp
ndn::Block wireEncode() const;
```

### API-2d97fe54ed1b · ndn_service_framework::StreamEndEvent::wireDecode

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L444)

```cpp
bool wireDecode(const ndn::Block& wire);
```

### API-a4f823bd8829 · ndn_service_framework::StreamEndEvent::operator==

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L445)

```cpp
bool operator==(const StreamEndEvent& other) const;
```

### API-3fcdf4e93b3c · ndn_service_framework::InvocationEventMessage

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L448)

```cpp
struct InvocationEventMessage
```

### API-533e1768eaa3 · ndn_service_framework::InvocationEventMessage::VERSION

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L450)

```cpp
static constexpr uint64_t VERSION = 1;
```

### API-f8d7cd46d261 · ndn_service_framework::InvocationEventMessage::version

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L452)

```cpp
uint64_t version = VERSION;
```

### API-04737317fc0c · ndn_service_framework::InvocationEventMessage::bindingDigest

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L453)

```cpp
StreamDigest bindingDigest{};
```

### API-cb157a821d0d · ndn_service_framework::InvocationEventMessage::cursor

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L454)

```cpp
uint64_t cursor = 0;
```

### API-55e7da576bc6 · ndn_service_framework::InvocationEventMessage::eventType

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L455)

```cpp
InvocationEventType eventType = InvocationEventType::Application;
```

### API-a3b7264e2df9 · ndn_service_framework::InvocationEventMessage::payload

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L456)

```cpp
ndn::Buffer payload;
```

### API-7429257f76e0 · ndn_service_framework::InvocationEventMessage::payloadDigest

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L457)

```cpp
StreamDigest payloadDigest{};
```

### API-8e999b14bb22 · ndn_service_framework::InvocationEventMessage::publishedAtUs

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L458)

```cpp
uint64_t publishedAtUs = 0;
```

### API-88094de1ccc5 · ndn_service_framework::InvocationEventMessage::userToken

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L459)

```cpp
ndn::Buffer userToken;
```

### API-69e1a3779dc5 · ndn_service_framework::InvocationEventMessage::terminal

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L460)

```cpp
bool terminal = false;
```

### API-0b27f0eaf587 · ndn_service_framework::InvocationEventMessage::end

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L461)

```cpp
std::optional<StreamEndEvent> end;
```

### API-072eb21c955c · ndn_service_framework::InvocationEventMessage::setPayload

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L463)

```cpp
void setPayload(const ndn::Buffer& value);
```

### API-18cbed512851 · ndn_service_framework::InvocationEventMessage::validate

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L464)

```cpp
void validate() const;
```

### API-ed320966ce6f · ndn_service_framework::InvocationEventMessage::wireEncode

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L465)

```cpp
ndn::Block wireEncode() const;
```

### API-b653c5238ce5 · ndn_service_framework::InvocationEventMessage::wireDecode

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L466)

```cpp
bool wireDecode(const ndn::Block& wire);
```

### API-dfbd2566352d · ndn_service_framework::InvocationEventMessage::operator==

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L467)

```cpp
bool operator==(const InvocationEventMessage& other) const;
```

### API-b8c6ce50b60f · ndn_service_framework::StreamCompletion

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L470)

```cpp
struct StreamCompletion
```

### API-20c6e8aa6888 · ndn_service_framework::StreamCompletion::VERSION

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L472)

```cpp
static constexpr uint64_t VERSION = 1;
```

### API-df07d91afdf5 · ndn_service_framework::StreamCompletion::version

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L474)

```cpp
uint64_t version = VERSION;
```

### API-9f74ad37fe94 · ndn_service_framework::StreamCompletion::bindingDigest

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L475)

```cpp
StreamDigest bindingDigest{};
```

### API-68177661029c · ndn_service_framework::StreamCompletion::endEventName

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L476)

```cpp
ndn::Name endEventName;
```

### API-d53b2117c07c · ndn_service_framework::StreamCompletion::finalCursor

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L477)

```cpp
uint64_t finalCursor = 0;
```

### API-f2d3d03c2cd2 · ndn_service_framework::StreamCompletion::finishReason

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L478)

```cpp
StreamFinishReason finishReason = StreamFinishReason::Failed;
```

### API-3b7a2097c59d · ndn_service_framework::StreamCompletion::applicationEventCount

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L479)

```cpp
uint64_t applicationEventCount = 0;
```

### API-16040b898c13 · ndn_service_framework::StreamCompletion::transcriptDigest

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L480)

```cpp
StreamDigest transcriptDigest{};
```

### API-8977b60b237b · ndn_service_framework::StreamCompletion::finalResultDigest

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L481)

```cpp
StreamDigest finalResultDigest{};
```

### API-52b7732103ae · ndn_service_framework::StreamCompletion::validate

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L483)

```cpp
void validate() const;
```

### API-8a0901359a42 · ndn_service_framework::StreamCompletion::wireEncode

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L484)

```cpp
ndn::Block wireEncode() const;
```

### API-f4fdb555079b · ndn_service_framework::StreamCompletion::wireDecode

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L485)

```cpp
bool wireDecode(const ndn::Block& wire);
```

### API-f50030d6db47 · ndn_service_framework::StreamCompletion::operator==

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L486)

```cpp
bool operator==(const StreamCompletion& other) const;
```

### API-36a1581a5066 · ndn_service_framework::StreamTerminalState

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L489)

```cpp
enum class StreamTerminalState : uint8_t
```

### API-46a7b150e0b0 · ndn_service_framework::StreamTerminalState::None

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L490)

```cpp
None = 0
```

### API-76c00787d6f8 · ndn_service_framework::StreamTerminalState::Completed

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L491)

```cpp
Completed = 1
```

### API-6e7134a027d1 · ndn_service_framework::StreamTerminalState::Failed

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L492)

```cpp
Failed = 2
```

### API-7f8c23031a69 · ndn_service_framework::StreamTerminalState::Cancelled

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L493)

```cpp
Cancelled = 3
```

### API-6d58414b28c2 · ndn_service_framework::StreamTerminalAuthority

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L501)

```cpp
class StreamTerminalAuthority
```

### API-1f85b1a76d96 · ndn_service_framework::StreamTerminalAuthority::claim

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L504)

```cpp
bool claim(StreamTerminalState state);
```

### API-ad51f345ebb0 · ndn_service_framework::StreamTerminalAuthority::cancel

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L505)

```cpp
bool cancel();
```

### API-6923b5669ad4 · ndn_service_framework::StreamTerminalAuthority::fence

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L506)

```cpp
void fence();
```

### API-6738414c8aa6 · ndn_service_framework::StreamTerminalAuthority::isFenced

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L507)

```cpp
bool isFenced() const;
```

### API-ec0f73a7d773 · ndn_service_framework::StreamTerminalAuthority::isTerminal

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L508)

```cpp
bool isTerminal() const;
```

### API-c7e39f7d8beb · ndn_service_framework::StreamTerminalAuthority::state

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L509)

```cpp
StreamTerminalState state() const;
```

### API-68ce1755fcf3 · ndn_service_framework::StreamUserLifecycleState

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L517)

```cpp
enum class StreamUserLifecycleState : uint8_t
```

### API-7d06407d24fc · ndn_service_framework::StreamUserLifecycleState::Created

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L518)

```cpp
Created = 0
```

### API-6d9bd42ed980 · ndn_service_framework::StreamUserLifecycleState::Requesting

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L519)

```cpp
Requesting = 1
```

### API-b21c3b983843 · ndn_service_framework::StreamUserLifecycleState::Selecting

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L520)

```cpp
Selecting = 2
```

### API-e6b1d376f903 · ndn_service_framework::StreamUserLifecycleState::Streaming

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L521)

```cpp
Streaming = 3
```

### API-edb307ed708b · ndn_service_framework::StreamUserLifecycleState::Draining

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L522)

```cpp
Draining = 4
```

### API-49f335559528 · ndn_service_framework::StreamUserLifecycleState::Completed

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L523)

```cpp
Completed = 5
```

### API-eba3e5d74bd1 · ndn_service_framework::StreamUserLifecycleState::Failed

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L524)

```cpp
Failed = 6
```

### API-8dca2605bd7e · ndn_service_framework::StreamUserLifecycleState::Cancelled

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L525)

```cpp
Cancelled = 7
```

### API-59f75d39b381 · ndn_service_framework::StreamUserLifecycle

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L528)

```cpp
class StreamUserLifecycle
```

### API-e94fcd9362ac · ndn_service_framework::StreamUserLifecycle::StreamUserLifecycle

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L531)

```cpp
explicit StreamUserLifecycle(
    std::shared_ptr<StreamTerminalAuthority> authority =
      std::make_shared<StreamTerminalAuthority>());
```

### API-63308d83422d · ndn_service_framework::StreamUserLifecycle::state

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L535)

```cpp
StreamUserLifecycleState state() const;
```

### API-9ddfb5a46832 · ndn_service_framework::StreamUserLifecycle::terminalAuthority

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L536)

```cpp
const std::shared_ptr<StreamTerminalAuthority>& terminalAuthority() const;
```

### API-482f7664cb13 · ndn_service_framework::StreamUserLifecycle::isTerminal

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L537)

```cpp
bool isTerminal() const;
```

### API-719ec292f1ca · ndn_service_framework::StreamUserLifecycle::beginRequest

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L539)

```cpp
void beginRequest();
```

### API-e677eb9408e5 · ndn_service_framework::StreamUserLifecycle::beginSelection

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L540)

```cpp
void beginSelection();
```

### API-bd725b97ba5d · ndn_service_framework::StreamUserLifecycle::beginStreaming

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L541)

```cpp
void beginStreaming();
```

### API-a5c1d8665812 · ndn_service_framework::StreamUserLifecycle::beginDraining

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L542)

```cpp
void beginDraining();
```

### API-a5edcb8284d6 · ndn_service_framework::StreamUserLifecycle::complete

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L543)

```cpp
void complete();
```

### API-4a2a6a932670 · ndn_service_framework::StreamUserLifecycle::fail

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L544)

```cpp
void fail();
```

### API-febcc3f8927c · ndn_service_framework::StreamUserLifecycle::cancel

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L545)

```cpp
void cancel();
```

### API-ef221771c2ff · ndn_service_framework::StreamUserLifecycle::dispatchCallback

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L548)

```cpp
template<typename Callback, typename Value>
bool dispatchCallback(Callback&& callback, const Value& value)
```

### API-1495e38c339c · ndn_service_framework::StreamProviderLifecycleState

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L589)

```cpp
enum class StreamProviderLifecycleState : uint8_t
```

### API-1e271696424f · ndn_service_framework::StreamProviderLifecycleState::Prepared

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L590)

```cpp
Prepared = 0
```

### API-833732dd3edc · ndn_service_framework::StreamProviderLifecycleState::Active

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L591)

```cpp
Active = 1
```

### API-eec149214dec · ndn_service_framework::StreamProviderLifecycleState::Ending

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L592)

```cpp
Ending = 2
```

### API-098bf49350b5 · ndn_service_framework::StreamProviderLifecycleState::Finished

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L593)

```cpp
Finished = 3
```

### API-26caa938bf86 · ndn_service_framework::StreamProviderLifecycleState::Fenced

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L594)

```cpp
Fenced = 4
```

### API-9dbeea6df866 · ndn_service_framework::StreamProviderLifecycleState::Failed

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L595)

```cpp
Failed = 5
```

### API-5cfb7f84ec51 · ndn_service_framework::StreamProviderLifecycleState::Cancelled

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L596)

```cpp
Cancelled = 6
```

### API-0a3ce7a06ef3 · ndn_service_framework::StreamProviderLifecycle

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L599)

```cpp
class StreamProviderLifecycle
```

### API-d14712d0c6ae · ndn_service_framework::StreamProviderLifecycle::StreamProviderLifecycle

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L602)

```cpp
explicit StreamProviderLifecycle(
    std::shared_ptr<StreamTerminalAuthority> authority =
      std::make_shared<StreamTerminalAuthority>());
```

### API-55821a7f7067 · ndn_service_framework::StreamProviderLifecycle::state

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L606)

```cpp
StreamProviderLifecycleState state() const;
```

### API-84318021e9cb · ndn_service_framework::StreamProviderLifecycle::terminalAuthority

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L607)

```cpp
const std::shared_ptr<StreamTerminalAuthority>& terminalAuthority() const;
```

### API-d0a0b0f661bd · ndn_service_framework::StreamProviderLifecycle::isTerminal

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L608)

```cpp
bool isTerminal() const;
```

### API-25be2a6b7808 · ndn_service_framework::StreamProviderLifecycle::activate

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L610)

```cpp
void activate();
```

### API-bc84bb94a6db · ndn_service_framework::StreamProviderLifecycle::beginEnding

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L611)

```cpp
void beginEnding();
```

### API-8974725e7928 · ndn_service_framework::StreamProviderLifecycle::finish

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L612)

```cpp
void finish();
```

### API-6829ad8056b9 · ndn_service_framework::StreamProviderLifecycle::fail

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L613)

```cpp
void fail();
```

### API-768cce2df6fd · ndn_service_framework::StreamProviderLifecycle::cancel

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L614)

```cpp
void cancel();
```

### API-c0378c9d06cf · ndn_service_framework::StreamProviderLifecycle::fence

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L615)

```cpp
void fence();
```

### API-314150900503 · ndn_service_framework::StreamInvocationLifecycle

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L633)

```cpp
class StreamInvocationLifecycle
```

### API-1345571c068c · ndn_service_framework::StreamInvocationLifecycle::StreamInvocationLifecycle

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L636)

```cpp
explicit StreamInvocationLifecycle(bool deferredCollaboration = false)
```

### API-f37debe6f762 · ndn_service_framework::StreamInvocationLifecycle::user

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L644)

```cpp
StreamUserLifecycle& user()
```

### API-4a2e82094885 · ndn_service_framework::StreamInvocationLifecycle::user

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L645)

```cpp
const StreamUserLifecycle& user() const
```

### API-9e039efecd18 · ndn_service_framework::StreamInvocationLifecycle::provider

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L646)

```cpp
StreamProviderLifecycle& provider()
```

### API-41e5457f357f · ndn_service_framework::StreamInvocationLifecycle::provider

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L647)

```cpp
const StreamProviderLifecycle& provider() const
```

### API-1b3afc281f83 · ndn_service_framework::StreamInvocationLifecycle::terminalAuthority

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L648)

```cpp
const std::shared_ptr<StreamTerminalAuthority>& terminalAuthority() const
```

### API-583120323d8f · ndn_service_framework::StreamInvocationLifecycle::isDeferredCollaboration

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L652)

```cpp
bool isDeferredCollaboration() const
```

### API-14dc92c7dd53 · ndn_service_framework::BoundedStreamQueue

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L667)

```cpp
class BoundedStreamQueue
```

### API-ce8ba0aa26d6 · ndn_service_framework::BoundedStreamQueue::BoundedStreamQueue

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L670)

```cpp
explicit BoundedStreamQueue(size_t capacity)
```

### API-d719583d51e9 · ndn_service_framework::BoundedStreamQueue::BoundedStreamQueue

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L678)

```cpp
BoundedStreamQueue(const BoundedStreamQueue&) = delete;
```

### API-ac584edc0cbb · ndn_service_framework::BoundedStreamQueue::operator=

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L679)

```cpp
BoundedStreamQueue& operator=(const BoundedStreamQueue&) = delete;
```

### API-005488ad063b · ndn_service_framework::BoundedStreamQueue::tryPush

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L681)

```cpp
bool tryPush(T value)
```

### API-7135e851838a · ndn_service_framework::BoundedStreamQueue::waitPush

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L693)

```cpp
bool waitPush(T value, std::chrono::steady_clock::time_point deadline)
```

### API-6dc9d9647581 · ndn_service_framework::BoundedStreamQueue::tryPop

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L710)

```cpp
bool tryPop(T& value)
```

### API-4de86df20065 · ndn_service_framework::BoundedStreamQueue::waitPop

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L722)

```cpp
bool waitPop(T& value, std::chrono::steady_clock::time_point deadline)
```

### API-50d56f0ef925 · ndn_service_framework::BoundedStreamQueue::close

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L736)

```cpp
void close()
```

### API-ed72104f0456 · ndn_service_framework::BoundedStreamQueue::isClosed

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L744)

```cpp
bool isClosed() const
```

### API-2a64ff597230 · ndn_service_framework::BoundedStreamQueue::size

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L750)

```cpp
size_t size() const
```

### API-683eb0fdf0e3 · ndn_service_framework::BoundedStreamQueue::capacity

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L756)

```cpp
size_t capacity() const
```

### API-c59c4781f9da · ndn_service_framework::BoundedStreamQueue::highWaterMark

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L758)

```cpp
size_t highWaterMark() const
```

### API-7491ab7f63a6 · ndn_service_framework::StreamBinding

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L774)

```cpp
struct StreamBinding
```

### API-76fc87d02f17 · ndn_service_framework::StreamBinding::requestId

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L776)

```cpp
ndn::Name requestId;
```

### API-389ee34af2c1 · ndn_service_framework::StreamBinding::requester

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L777)

```cpp
ndn::Name requester;
```

### API-69737ac268d2 · ndn_service_framework::StreamBinding::serviceName

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L778)

```cpp
ndn::Name serviceName;
```

### API-57bdb5c41ae4 · ndn_service_framework::StreamBinding::producer

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L779)

```cpp
ndn::Name producer;
```

### API-403ef199f54c · ndn_service_framework::StreamBinding::producerBootId

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L780)

```cpp
std::string producerBootId;
```

### API-0dd76a81c4d1 · ndn_service_framework::StreamBinding::attemptEpoch

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L781)

```cpp
uint64_t attemptEpoch = 0;
```

### API-1e5e2018f589 · ndn_service_framework::StreamBinding::planDigest

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L782)

```cpp
StreamDigest planDigest{};
```

### API-bcb467fcd9e5 · ndn_service_framework::StreamBinding::generationId

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L783)

```cpp
StreamGenerationId generationId{};
```

### API-787ded8656f7 · ndn_service_framework::StreamBinding::streamEpoch

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L784)

```cpp
uint64_t streamEpoch = 0;
```

### API-e7efec0d5d10 · ndn_service_framework::StreamBinding::eventKeyCommitment

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L785)

```cpp
StreamDigest eventKeyCommitment{};
```

### API-fe0817986305 · ndn_service_framework::StreamBinding::userToken

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L786)

```cpp
ndn::Buffer userToken;
```

### API-180449efcacf · ndn_service_framework::StreamBinding::policyEpoch

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L787)

```cpp
uint64_t policyEpoch = 0;
```

### API-f521e4acd356 · ndn_service_framework::StreamBinding::controllerVersion

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L791)

```cpp
std::optional<ControllerVersion> controllerVersion;
```

原始接口说明：

```text
// If present, this is the exact Controller authority version used to
// authorize the invocation. It is included in canonicalBytes() so every
// event digest is version-bound without repeating the version per event.
```

### API-e6158bb02dc5 · ndn_service_framework::StreamBinding::deadlineEpochMs

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L792)

```cpp
uint64_t deadlineEpochMs = 0;
```

### API-241e42aa341d · ndn_service_framework::StreamBinding::validate

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L794)

```cpp
void validate() const;
```

### API-f3711b53562a · ndn_service_framework::StreamBinding::canonicalBytes

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L795)

```cpp
ndn::Buffer canonicalBytes() const;
```

### API-4824e62cf4d8 · ndn_service_framework::StreamBinding::operator==

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L796)

```cpp
bool operator==(const StreamBinding& other) const;
```

### API-7403ed92f820 · ndn_service_framework::ParsedInvocationEventName

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L799)

```cpp
struct ParsedInvocationEventName
```

### API-bb96bf63769c · ndn_service_framework::ParsedInvocationEventName::producer

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L801)

```cpp
ndn::Name producer;
```

### API-a358d83877f4 · ndn_service_framework::ParsedInvocationEventName::requester

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L802)

```cpp
ndn::Name requester;
```

### API-89a6219ef20e · ndn_service_framework::ParsedInvocationEventName::serviceName

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L803)

```cpp
ndn::Name serviceName;
```

### API-b4ce3889e9bc · ndn_service_framework::ParsedInvocationEventName::requestId

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L804)

```cpp
ndn::Name requestId;
```

### API-97629a69c7ed · ndn_service_framework::ParsedInvocationEventName::attemptEpoch

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L805)

```cpp
uint64_t attemptEpoch = 0;
```

### API-d388eabf1a41 · ndn_service_framework::ParsedInvocationEventName::planDigest

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L806)

```cpp
StreamDigest planDigest{};
```

### API-3015f9b50c8e · ndn_service_framework::ParsedInvocationEventName::generationId

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L807)

```cpp
StreamGenerationId generationId{};
```

### API-d69554498725 · ndn_service_framework::ParsedInvocationEventName::streamEpoch

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L808)

```cpp
uint64_t streamEpoch = 0;
```

### API-e893c70a8ed5 · ndn_service_framework::ParsedInvocationEventName::cursor

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L809)

```cpp
uint64_t cursor = 0;
```

### API-1eafbf0169e1 · ndn_service_framework::initialStreamTranscriptDigest

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L812)

```cpp
StreamDigest initialStreamTranscriptDigest();
```

### API-bf06a0c8c519 · ndn_service_framework::PublishedStreamEvent

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L818)

```cpp
struct PublishedStreamEvent
```

### API-9f74baee851c · ndn_service_framework::PublishedStreamEvent::name

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L820)

```cpp
ndn::Name name;
```

### API-f9380612f50c · ndn_service_framework::PublishedStreamEvent::cursor

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L821)

```cpp
uint64_t cursor = 0;
```

### API-d5079b55c84d · ndn_service_framework::PublishedStreamEvent::terminal

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L822)

```cpp
bool terminal = false;
```

### API-c7f35cd21906 · ndn_service_framework::PublishedStreamEvent::signedWire

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L823)

```cpp
ndn::Buffer signedWire;
```

### API-71b81832b23e · ndn_service_framework::ResponseMessage

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L826)

```cpp
class ResponseMessage
```

### API-76356dd8ed21 · ndn_service_framework::StreamEventPublisher

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L835)

```cpp
class StreamEventPublisher
```

### API-abc9186b9b85 · ndn_service_framework::StreamEventPublisher::PublishCallback

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L838)

```cpp
using PublishCallback = std::function<void(const PublishedStreamEvent&)>;
```

### API-5d6e34466e88 · ndn_service_framework::StreamEventPublisher::StreamEventPublisher

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L840)

```cpp
StreamEventPublisher(StreamBinding binding,
                       StreamRequestOptions options,
                       ndn::Buffer eventKey,
                       std::shared_ptr<StreamInvocationLifecycle> lifecycle,
                       ndn::KeyChain& keyChain,
                       ndn::security::SigningInfo signingInfo,
                       PublishCallback publish);
```

### API-28cba9b36a3e · ndn_service_framework::StreamEventPublisher::start

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L848)

```cpp
void start();
```

### API-97a98d5976ef · ndn_service_framework::StreamEventPublisher::publish

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L850)

```cpp
std::optional<PublishedStreamEvent>
  publish(const ndn::Buffer& payload,
          std::chrono::steady_clock::time_point deadline);
```

### API-fed653ddeaa6 · ndn_service_framework::StreamEventPublisher::finish

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L854)

```cpp
std::optional<StreamCompletion>
  finish(const ndn::Buffer& finalResult,
         StreamFinishReason reason,
         std::chrono::steady_clock::time_point deadline);
```

### API-d8be51926c29 · ndn_service_framework::StreamEventPublisher::fail

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L859)

```cpp
bool fail(const std::string& errorInfo);
```

### API-f2a25e761048 · ndn_service_framework::StreamEventPublisher::satisfy

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L862)

```cpp
std::optional<PublishedStreamEvent> satisfy(const ndn::Interest& interest);
```

原始接口说明：

```text
/** Return the exact retained Data wire, without invoking publication. */
```

### API-b0d8421ec9e9 · ndn_service_framework::StreamEventPublisher::republish

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L865)

```cpp
std::optional<PublishedStreamEvent> republish(const ndn::Name& name);
```

原始接口说明：

```text
/** Republish the exact retained Data wire for an exact-name retry. */
```

### API-ef48e05164a8 · ndn_service_framework::StreamEventPublisher::retainedCount

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L867)

```cpp
size_t retainedCount() const;
```

### API-3961a18b737f · ndn_service_framework::StreamEventPublisher::highWaterMark

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L868)

```cpp
size_t highWaterMark() const;
```

### API-1d14c0991ed0 · ndn_service_framework::StreamEventPublisher::nextCursor

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L869)

```cpp
uint64_t nextCursor() const;
```

### API-f0c8b0c29a54 · ndn_service_framework::StreamEventPublisher::transcriptDigest

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L870)

```cpp
const StreamDigest& transcriptDigest() const;
```

### API-88fec0344f6b · ndn_service_framework::StreamEventConsumer

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L915)

```cpp
class StreamEventConsumer
```

### API-44161447df5e · ndn_service_framework::StreamEventConsumer::VerifyCallback

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L918)

```cpp
using VerifyCallback = std::function<bool(const ndn::Data&)>;
```

### API-9e8a972ade23 · ndn_service_framework::StreamEventConsumer::EventCallback

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L919)

```cpp
using EventCallback = std::function<void(const InvocationEventMessage&)>;
```

### API-81bc54829725 · ndn_service_framework::StreamEventConsumer::CompletionCallback

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L920)

```cpp
using CompletionCallback = std::function<void(const ResponseMessage&)>;
```

### API-3d2b5abefe38 · ndn_service_framework::StreamEventConsumer::ErrorCallback

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L921)

```cpp
using ErrorCallback = std::function<void(const StreamedInvocationError&)>;
```

### API-aaf2fe567596 · ndn_service_framework::StreamEventConsumer::RetryCallback

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L922)

```cpp
using RetryCallback = std::function<void(const ndn::Name&)>;
```

### API-9f02e0046975 · ndn_service_framework::StreamEventConsumer::AuthorizationCallback

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L926)

```cpp
using AuthorizationCallback = std::function<bool()>;
```

原始接口说明：

```text
// Checked immediately before an event is handed to application code.  This
// closes the race between asynchronous Data validation/buffering and a
// Controller revocation update.
```

### API-af8426182c85 · ndn_service_framework::StreamEventConsumer::RetryAccountingCallback

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L930)

```cpp
using RetryAccountingCallback = std::function<void(const ndn::Name&)>;
```

原始接口说明：

```text
// Called only when the bounded retry state machine consumes retry budget.
// Initial exact-interest prefetches use RetryCallback but must not be
// reported as retransmissions.
```

### API-634f1c3697b6 · ndn_service_framework::StreamEventConsumer::StreamEventConsumer

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L932)

```cpp
StreamEventConsumer(StreamBinding binding,
                      StreamRequestOptions options,
                      ndn::Buffer eventKey,
                      std::shared_ptr<StreamInvocationLifecycle> lifecycle,
                      VerifyCallback verify,
                      EventCallback onEvent,
                      CompletionCallback onComplete,
                      ErrorCallback onError,
                      RetryCallback onRetry,
                      RetryAccountingCallback onRetryAccounting = {});
```

### API-af67ad3839a9 · ndn_service_framework::StreamEventConsumer::~StreamEventConsumer

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L943)

```cpp
~StreamEventConsumer();
```

### API-4fe2f5a8ea88 · ndn_service_framework::StreamEventConsumer::start

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L945)

```cpp
void start();
```

### API-d39374f4cde4 · ndn_service_framework::StreamEventConsumer::prefetchWindow

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L952)

```cpp
void prefetchWindow();
```

原始接口说明：

```text
/**
   * Express the initial bounded exact-name window without consuming the
   * retry budget.  The caller wires the callback to the transport after the
   * consumer is installed, so events published immediately after Selection
   * cannot be lost before the first inactivity timer fires.
   */
```

### API-40bf4533f56e · ndn_service_framework::StreamEventConsumer::setAuthorizationCallback

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L953)

```cpp
void setAuthorizationCallback(AuthorizationCallback callback);
```

### API-b878bca004f7 · ndn_service_framework::StreamEventConsumer::accept

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L954)

```cpp
bool accept(const ndn::Data& data);
```

### API-1be847a1a395 · ndn_service_framework::StreamEventConsumer::acceptResponse

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L955)

```cpp
bool acceptResponse(const ResponseMessage& response);
```

### API-6af73771d59e · ndn_service_framework::StreamEventConsumer::onInactivityTimeout

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L956)

```cpp
void onInactivityTimeout(std::chrono::steady_clock::time_point now);
```

### API-b593ad69f321 · ndn_service_framework::StreamEventConsumer::onRetryTimeout

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L957)

```cpp
void onRetryTimeout(const ndn::Name& timedOutName,
                      std::chrono::steady_clock::time_point now);
```

### API-0d82e6c040aa · ndn_service_framework::StreamEventConsumer::reject

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L959)

```cpp
void reject(StreamedInvocationErrorCode code, const std::string& message);
```

### API-46fd0f99ea56 · ndn_service_framework::StreamEventConsumer::expectedCursor

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L961)

```cpp
uint64_t expectedCursor() const;
```

### API-56fc84833821 · ndn_service_framework::StreamEventConsumer::reorderSize

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L962)

```cpp
size_t reorderSize() const;
```

### API-fc29bac93f2c · ndn_service_framework::StreamEventConsumer::retryCount

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L963)

```cpp
uint8_t retryCount() const;
```

### API-0c172f796c59 · ndn_service_framework::StreamEventConsumer::callbackHighWaterMark

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L964)

```cpp
size_t callbackHighWaterMark() const;
```

### API-96d30f9b5eda · ndn_service_framework::computeStreamSha256

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L1023)

```cpp
StreamDigest computeStreamSha256(ndn::span<const uint8_t> bytes);
```

### API-05825d6a3a30 · ndn_service_framework::computeStreamBindingDigest

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L1024)

```cpp
StreamDigest computeStreamBindingDigest(const StreamBinding& binding);
```

### API-4a87534608e1 · ndn_service_framework::computeStreamGrantBindingDigest

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L1026)

```cpp
StreamDigest computeStreamGrantBindingDigest(const StreamBinding& binding);
```

原始接口说明：

```text
/** Digest for the pre-Selection portion of a streamed key grant binding. */
```

### API-1eafbf0169e1 · ndn_service_framework::initialStreamTranscriptDigest

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L1027)

```cpp
StreamDigest initialStreamTranscriptDigest();
```

### API-44bc5ecfaa89 · ndn_service_framework::advanceStreamTranscriptDigest

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L1028)

```cpp
StreamDigest advanceStreamTranscriptDigest(
  const StreamDigest& previous, uint64_t cursor, InvocationEventType eventType,
  const StreamDigest& payloadDigest);
```

### API-0673035d7cc4 · ndn_service_framework::deriveInvocationEventNonce

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L1031)

```cpp
StreamNonce deriveInvocationEventNonce(
  const StreamDigest& eventKey, const StreamDigest& bindingDigest, uint64_t cursor);
```

### API-81721443166a · ndn_service_framework::makeInvocationEventAssociatedData

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L1033)

```cpp
ndn::Buffer makeInvocationEventAssociatedData(
  const ndn::Name& dataName, const StreamDigest& bindingDigest, uint64_t cursor);
```

### API-0b9e6005e7d4 · ndn_service_framework::isMatchingStreamCompletion

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L1035)

```cpp
bool isMatchingStreamCompletion(
  const StreamCompletion& completion, const ndn::Name& observedEndEventName,
  const InvocationEventMessage& observedEndEvent,
  ndn::span<const uint8_t> finalResponsePayload);
```

### API-b9d07499bf08 · ndn_service_framework::makeInvocationEventName

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L1040)

```cpp
ndn::Name makeInvocationEventName(const StreamBinding& binding, uint64_t cursor);
```

### API-981dbb1244de · ndn_service_framework::parseInvocationEventName

public / declared-interface；[源码](../../ndn-service-framework/InvocationStream.hpp#L1041)

```cpp
std::optional<ParsedInvocationEventName>
parseInvocationEventName(const ndn::Name& name);
```

## ndn-service-framework/LocalServiceRegistry.hpp

源码 SHA-256：`90bde23b44864bc3cdb652a88c41976b0433b6d2efe3cdc9f75e665d4ef7f801`。

### API-a7dad58a3bd0 · ndn_service_framework::LocalServiceRegistry

public / declared-interface；[源码](../../ndn-service-framework/LocalServiceRegistry.hpp#L26)

```cpp
class LocalServiceRegistry
```

### API-f105f38164d7 · ndn_service_framework::LocalServiceRegistry::LocalRequestHandler

public / declared-interface；[源码](../../ndn-service-framework/LocalServiceRegistry.hpp#L29)

```cpp
using LocalRequestHandler =
    std::function<void(const ndn::Name& requesterIdentity,
                       const ndn::Name& serviceName,
                       const RequestMessage& requestMessage,
                       ResponseMessage& responseMessage)>;
```

### API-118dac15fb30 · ndn_service_framework::LocalServiceRegistry::LegacyLocalRequestHandler

public / declared-interface；[源码](../../ndn-service-framework/LocalServiceRegistry.hpp#L35)

```cpp
using LegacyLocalRequestHandler =
    std::function<ResponseMessage(const ndn::Name& requesterIdentity,
                                  const ndn::Name& serviceName,
                                  const RequestMessage& requestMessage)>;
```

### API-3b5867c7c690 · ndn_service_framework::LocalServiceRegistry::InvocationResult

public / declared-interface；[源码](../../ndn-service-framework/LocalServiceRegistry.hpp#L41)

```cpp
struct InvocationResult
```

### API-e044ef2e91e1 · ndn_service_framework::LocalServiceRegistry::InvocationResult::success

public / declared-interface；[源码](../../ndn-service-framework/LocalServiceRegistry.hpp#L43)

```cpp
bool success = false;
```

### API-13f4be0510dc · ndn_service_framework::LocalServiceRegistry::InvocationResult::response

public / declared-interface；[源码](../../ndn-service-framework/LocalServiceRegistry.hpp#L44)

```cpp
ResponseT response;
```

### API-3477b25691ea · ndn_service_framework::LocalServiceRegistry::InvocationResult::error

public / declared-interface；[源码](../../ndn-service-framework/LocalServiceRegistry.hpp#L45)

```cpp
std::string error;
```

### API-911fd3fb94d7 · ndn_service_framework::LocalServiceRegistry::registerLocalService

public / declared-interface；[源码](../../ndn-service-framework/LocalServiceRegistry.hpp#L48)

```cpp
void
  registerLocalService(const ndn::Name& serviceName, LocalRequestHandler handler)
```

### API-9774969f497e · ndn_service_framework::LocalServiceRegistry::registerLocalService

public / declared-interface；[源码](../../ndn-service-framework/LocalServiceRegistry.hpp#L55)

```cpp
void
  registerLocalService(const ndn::Name& serviceName, LegacyLocalRequestHandler handler)
```

### API-f106bcbdf9e9 · ndn_service_framework::LocalServiceRegistry::registerLocalService

public / declared-interface；[源码](../../ndn-service-framework/LocalServiceRegistry.hpp#L71)

```cpp
template<typename RequestT, typename ResponseT>
void
  registerLocalService(const ndn::Name& serviceName,
                       std::function<void(const ndn::Name& requesterIdentity,
                                          const RequestT& request,
                                          ResponseT& response)> handler)
```

### API-75a7a473dd8a · ndn_service_framework::LocalServiceRegistry::hasService

public / declared-interface；[源码](../../ndn-service-framework/LocalServiceRegistry.hpp#L112)

```cpp
bool
  hasService(const ndn::Name& serviceName) const
```

### API-5ebad5b827eb · ndn_service_framework::LocalServiceRegistry::localInvokeRawInto

public / declared-interface；[源码](../../ndn-service-framework/LocalServiceRegistry.hpp#L119)

```cpp
bool
  localInvokeRawInto(const ndn::Name& serviceName,
                     const RequestMessage& requestMessage,
                     ResponseMessage& responseMessage,
                     const ndn::Name& requesterIdentity = ndn::Name("/local"))
```

### API-4515b86c7df7 · ndn_service_framework::LocalServiceRegistry::localInvoke

public / declared-interface；[源码](../../ndn-service-framework/LocalServiceRegistry.hpp#L141)

```cpp
template<typename RequestT, typename ResponseT>
InvocationResult<ResponseT>
  localInvoke(const ndn::Name& serviceName,
              const RequestT& request,
              const ndn::Name& requesterIdentity = ndn::Name("/local"))
```

### API-1c076dca4170 · ndn_service_framework::LocalServiceRegistry::localInvokeAsync

public / declared-interface；[源码](../../ndn-service-framework/LocalServiceRegistry.hpp#L171)

```cpp
template<typename RequestT, typename ResponseT>
std::future<InvocationResult<ResponseT>>
  localInvokeAsync(const ndn::Name& serviceName,
                   RequestT request,
                   const ndn::Name& requesterIdentity = ndn::Name("/local"))
```

### API-cf109c350f59 · ndn_service_framework::LocalServiceRegistry::localInvokeAsync

public / declared-interface；[源码](../../ndn-service-framework/LocalServiceRegistry.hpp#L185)

```cpp
template<typename RequestT, typename ResponseT>
void
  localInvokeAsync(const ndn::Name& serviceName,
                   RequestT request,
                   std::function<void(const ResponseT&)> onResponse,
                   std::function<void(const std::string&)> onError,
                   const ndn::Name& requesterIdentity = ndn::Name("/local"))
```

## ndn-service-framework/NDNSFMessages.hpp

源码 SHA-256：`cd1399f6ed1e09f6cbdb08ee6ddff5d956ec31351875291b56cb598ae3e6cfc1`。

### API-4ff2f144b28f · ndn_service_framework::tlv::(anonymous)

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L18)

```cpp
enum
```

### API-f301ddcf3dfd · ndn_service_framework::tlv::(anonymous)::RequestMessageType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L19)

```cpp
RequestMessageType = 128
```

### API-422f14f83049 · ndn_service_framework::tlv::(anonymous)::ResponseMessageType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L20)

```cpp
ResponseMessageType = 129
```

### API-22ff3a382928 · ndn_service_framework::tlv::(anonymous)::RequestAckMessageType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L21)

```cpp
RequestAckMessageType = 130
```

### API-963e734c85e8 · ndn_service_framework::tlv::(anonymous)::ServiceSelectionMessageType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L22)

```cpp
ServiceSelectionMessageType = 131
```

### API-7d9793da5347 · ndn_service_framework::tlv::(anonymous)::ServiceAccessMessageType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L23)

```cpp
ServiceAccessMessageType = 132
```

### API-7a7d5150c208 · ndn_service_framework::tlv::(anonymous)::ServiceProvisionMessageType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L24)

```cpp
ServiceProvisionMessageType = 133
```

### API-bb6465f52b97 · ndn_service_framework::tlv::(anonymous)::TokenType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L25)

```cpp
TokenType = 150
```

### API-6d8e0b2cfe31 · ndn_service_framework::tlv::(anonymous)::PayloadType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L26)

```cpp
PayloadType = 151
```

### API-ad5a6b62e1bd · ndn_service_framework::tlv::(anonymous)::StatusType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L27)

```cpp
StatusType = 152
```

### API-c02f2f8034f0 · ndn_service_framework::tlv::(anonymous)::ErrorInfoType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L28)

```cpp
ErrorInfoType = 153
```

### API-2d253ecb2335 · ndn_service_framework::tlv::(anonymous)::RequestIDType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L29)

```cpp
RequestIDType = 154
```

### API-73375b138560 · ndn_service_framework::tlv::(anonymous)::StrategyType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L30)

```cpp
StrategyType = 155
```

### API-7efa74704b0b · ndn_service_framework::tlv::(anonymous)::PermissionEntryType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L31)

```cpp
PermissionEntryType = 156
```

### API-fe08b5f164da · ndn_service_framework::tlv::(anonymous)::PermissionResponseType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L32)

```cpp
PermissionResponseType = 157
```

### API-df02cbd5d0da · ndn_service_framework::tlv::(anonymous)::ProviderNameType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L33)

```cpp
ProviderNameType = 158
```

### API-4035c1ff7c50 · ndn_service_framework::tlv::(anonymous)::ServiceNameType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L34)

```cpp
ServiceNameType = 159
```

### API-637952595484 · ndn_service_framework::tlv::(anonymous)::PermissionKindType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L35)

```cpp
PermissionKindType = 160
```

### API-ff2470ad5acd · ndn_service_framework::tlv::(anonymous)::TargetIdentityType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L36)

```cpp
TargetIdentityType = 161
```

### API-5a0d67af0ec7 · ndn_service_framework::tlv::(anonymous)::TtlType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L37)

```cpp
TtlType = 162
```

### API-d4a8416b8ea5 · ndn_service_framework::tlv::(anonymous)::VersionType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L38)

```cpp
VersionType = 163
```

### API-8f10af2fdf36 · ndn_service_framework::tlv::(anonymous)::EncryptedPermissionResponseType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L39)

```cpp
EncryptedPermissionResponseType = 164
```

### API-163d3ef63c51 · ndn_service_framework::tlv::(anonymous)::RecipientCertNameType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L40)

```cpp
RecipientCertNameType = 165
```

### API-696fb93b0181 · ndn_service_framework::tlv::(anonymous)::AlgorithmType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L41)

```cpp
AlgorithmType = 166
```

### API-e58934cf9e4f · ndn_service_framework::tlv::(anonymous)::EncryptedAesKeyType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L42)

```cpp
EncryptedAesKeyType = 167
```

### API-f1a0f983f321 · ndn_service_framework::tlv::(anonymous)::IvType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L43)

```cpp
IvType = 168
```

### API-f769330e2e27 · ndn_service_framework::tlv::(anonymous)::CipherTextType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L44)

```cpp
CipherTextType = 169
```

### API-d5484ea42489 · ndn_service_framework::tlv::(anonymous)::UserTokenType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L45)

```cpp
UserTokenType = 170
```

### API-7ee886214243 · ndn_service_framework::tlv::(anonymous)::ProviderTokenType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L46)

```cpp
ProviderTokenType = 171
```

### API-2df446672754 · ndn_service_framework::tlv::(anonymous)::HybridMessageEnvelopeType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L47)

```cpp
HybridMessageEnvelopeType = 172
```

### API-9aa3161daa0c · ndn_service_framework::tlv::(anonymous)::KeyIdType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L48)

```cpp
KeyIdType = 173
```

### API-0ad2ce6fa897 · ndn_service_framework::tlv::(anonymous)::EpochIdType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L49)

```cpp
EpochIdType = 174
```

### API-d8208d4ea11b · ndn_service_framework::tlv::(anonymous)::NonceType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L50)

```cpp
NonceType = 175
```

### API-d9afdd893cd1 · ndn_service_framework::tlv::(anonymous)::AuthTagType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L51)

```cpp
AuthTagType = 176
```

### API-75d4b0461cf8 · ndn_service_framework::tlv::(anonymous)::WrappedMessageKeyType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L52)

```cpp
WrappedMessageKeyType = 177
```

### API-8c3e13a5961c · ndn_service_framework::tlv::(anonymous)::MessageTypeType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L53)

```cpp
MessageTypeType = 178
```

### API-44f162a0f7a1 · ndn_service_framework::tlv::(anonymous)::CollaborationDataMessageType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L54)

```cpp
CollaborationDataMessageType = 179
```

### API-751071622124 · ndn_service_framework::tlv::(anonymous)::KeyScopeType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L55)

```cpp
KeyScopeType = 180
```

### API-c19e4d141f60 · ndn_service_framework::tlv::(anonymous)::TopicType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L56)

```cpp
TopicType = 181
```

### API-d61c05c15ac3 · ndn_service_framework::tlv::(anonymous)::ProducerRoleType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L57)

```cpp
ProducerRoleType = 182
```

### API-688c946d35b6 · ndn_service_framework::tlv::(anonymous)::SequenceType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L58)

```cpp
SequenceType = 183
```

### API-15134e332e9f · ndn_service_framework::tlv::(anonymous)::AssignmentPayloadType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L59)

```cpp
AssignmentPayloadType = 184
```

### API-5f984d0ccd07 · ndn_service_framework::tlv::(anonymous)::SelectionProviderEntryType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L60)

```cpp
SelectionProviderEntryType = 0xF503
```

### API-962bd125b72f · ndn_service_framework::tlv::(anonymous)::PolicyManifestType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L61)

```cpp
PolicyManifestType = 185
```

### API-e24c11737554 · ndn_service_framework::tlv::(anonymous)::ValidFromType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L62)

```cpp
ValidFromType = 186
```

### API-d86e4c7ebe4f · ndn_service_framework::tlv::(anonymous)::GracePeriodMsType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L63)

```cpp
GracePeriodMsType = 187
```

### API-bbf9f7f78cdc · ndn_service_framework::tlv::(anonymous)::RequiredKeyEpochType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L64)

```cpp
RequiredKeyEpochType = 188
```

### API-45477244f16f · ndn_service_framework::tlv::(anonymous)::RequestModeType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L65)

```cpp
RequestModeType = 189
```

### API-9e77e18470ad · ndn_service_framework::tlv::(anonymous)::ControllerVersionType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L66)

```cpp
ControllerVersionType = 0xF700
```

### API-505cb7ad705d · ndn_service_framework::tlv::(anonymous)::AllowedServiceListType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L67)

```cpp
AllowedServiceListType = 0xF501
```

### API-87dfbbaf292b · ndn_service_framework::tlv::(anonymous)::AllowedServiceType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L68)

```cpp
AllowedServiceType = 0xF502
```

### API-e43d072f7254 · ndn_service_framework::tlv::(anonymous)::DeploymentIntentType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L69)

```cpp
DeploymentIntentType = 0xF610
```

### API-fa763a3da917 · ndn_service_framework::tlv::(anonymous)::ProviderCapabilityOfferType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L70)

```cpp
ProviderCapabilityOfferType = 0xF611
```

### API-08c7249fca8d · ndn_service_framework::tlv::(anonymous)::DeploymentPlanType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L71)

```cpp
DeploymentPlanType = 0xF612
```

### API-1602e717a8ff · ndn_service_framework::tlv::(anonymous)::ProviderReadyMessageType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L72)

```cpp
ProviderReadyMessageType = 0xF613
```

### API-439dec8c2719 · ndn_service_framework::tlv::(anonymous)::ReadyAcknowledgementType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L73)

```cpp
ReadyAcknowledgementType = 0xF614
```

### API-88cb21ceb433 · ndn_service_framework::tlv::(anonymous)::ExecutionActivateMessageType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L74)

```cpp
ExecutionActivateMessageType = 0xF615
```

### API-47c6a9cda90e · ndn_service_framework::tlv::(anonymous)::SecureStatusQueryType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L75)

```cpp
SecureStatusQueryType = 0xF616
```

### API-83f5504e2c85 · ndn_service_framework::tlv::(anonymous)::SecureStatusSnapshotType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L76)

```cpp
SecureStatusSnapshotType = 0xF617
```

### API-8fca8a25cd1d · ndn_service_framework::tlv::(anonymous)::DeploymentControlFieldType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L77)

```cpp
DeploymentControlFieldType = 0xF618
```

### API-f2087fab011e · ndn_service_framework::tlv::(anonymous)::DeploymentControlFieldNameType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L78)

```cpp
DeploymentControlFieldNameType = 0xF619
```

### API-7df1ca11c2e1 · ndn_service_framework::tlv::(anonymous)::DeploymentControlFieldValueType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L79)

```cpp
DeploymentControlFieldValueType = 0xF61A
```

### API-c4c7a9bd3e79 · ndn_service_framework::tlv::(anonymous)::RequestCapabilitiesType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L80)

```cpp
RequestCapabilitiesType = 0xF61B
```

### API-20526fddfcea · ndn_service_framework::tlv::(anonymous)::EncryptedRequestInputType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L81)

```cpp
EncryptedRequestInputType = 0xF61C
```

### API-68878e04ea60 · ndn_service_framework::tlv::(anonymous)::SelectionInputKeyOfferType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L82)

```cpp
SelectionInputKeyOfferType = 0xF61D
```

### API-934b08ec2fd4 · ndn_service_framework::tlv::(anonymous)::SelectionInputKeyGrantType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L83)

```cpp
SelectionInputKeyGrantType = 0xF61E
```

### API-ebb6a22c7d86 · ndn_service_framework::tlv::(anonymous)::ReservationLeaseType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L84)

```cpp
ReservationLeaseType = 0xF61F
```

### API-cc1d82454240 · ndn_service_framework::tlv::(anonymous)::SelectionDecisionType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L85)

```cpp
SelectionDecisionType = 0xF620
```

### API-7d8ae39e392b · ndn_service_framework::tlv::(anonymous)::SelectionDecisionReceiptType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L86)

```cpp
SelectionDecisionReceiptType = 0xF621
```

### API-f6b2b8c45b9c · ndn_service_framework::tlv::(anonymous)::RecipientEncryptedAssignmentType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L87)

```cpp
RecipientEncryptedAssignmentType = 0xF622
```

### API-8aaa6d66aee0 · ndn_service_framework::tlv::(anonymous)::StageInputEvidenceType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L88)

```cpp
StageInputEvidenceType = 0xF623
```

### API-81e6371f8d74 · ndn_service_framework::tlv::(anonymous)::StageAbortType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L89)

```cpp
StageAbortType = 0xF624
```

### API-4620b28924de · ndn_service_framework::tlv::(anonymous)::SelectionDecisionTombstoneType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L90)

```cpp
SelectionDecisionTombstoneType = 0xF625
```

### API-6cf281ea62fc · ndn_service_framework::tlv::(anonymous)::AttemptType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L91)

```cpp
AttemptType = 0xF626
```

### API-c0db57ea99d8 · ndn_service_framework::tlv::(anonymous)::OpaqueAssignmentSetType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L92)

```cpp
OpaqueAssignmentSetType = 0xF627
```

### API-78374684f30d · ndn_service_framework::tlv::(anonymous)::OpaqueAssignmentItemType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L93)

```cpp
OpaqueAssignmentItemType = 0xF628
```

### API-358f3a1a30c1 · ndn_service_framework::tlv::(anonymous)::CollaborationAssignmentEnvelopeType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L94)

```cpp
CollaborationAssignmentEnvelopeType = 0xF629
```

### API-3a4c669de5ab · ndn_service_framework::tlv::(anonymous)::CollaborationRoleType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L95)

```cpp
CollaborationRoleType = 0xF62A
```

### API-5e6d1f37def8 · ndn_service_framework::tlv::(anonymous)::CollaborationArtifactType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L96)

```cpp
CollaborationArtifactType = 0xF62B
```

### API-9ece2d6c79c4 · ndn_service_framework::tlv::(anonymous)::CollaborationProvisioningType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L97)

```cpp
CollaborationProvisioningType = 0xF62C
```

### API-4a31034b8137 · ndn_service_framework::tlv::(anonymous)::CollaborationProvisioningTimeoutType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L98)

```cpp
CollaborationProvisioningTimeoutType = 0xF62D
```

### API-94432052c19c · ndn_service_framework::tlv::(anonymous)::CollaborationOpaquePayloadType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L99)

```cpp
CollaborationOpaquePayloadType = 0xF62E
```

### API-3037bd81ccc1 · ndn_service_framework::tlv::(anonymous)::CollaborationScopeKeysType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L100)

```cpp
CollaborationScopeKeysType = 0xF62F
```

### API-bd85c8762a01 · ndn_service_framework::tlv::(anonymous)::CollaborationScopeKeyType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L101)

```cpp
CollaborationScopeKeyType = 0xF630
```

### API-d43d95c86e0b · ndn_service_framework::tlv::(anonymous)::CollaborationScopeKeyNameType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L102)

```cpp
CollaborationScopeKeyNameType = 0xF631
```

### API-136b87a9d3c1 · ndn_service_framework::tlv::(anonymous)::CollaborationScopeKeyValueType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L103)

```cpp
CollaborationScopeKeyValueType = 0xF632
```

### API-7512b504d5d7 · ndn_service_framework::tlv::(anonymous)::CollaborationScopeKeyDataNamesType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L104)

```cpp
CollaborationScopeKeyDataNamesType = 0xF633
```

### API-3860392a3a26 · ndn_service_framework::tlv::(anonymous)::CollaborationScopeKeyDataNameType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L105)

```cpp
CollaborationScopeKeyDataNameType = 0xF634
```

### API-05b56101e962 · ndn_service_framework::tlv::(anonymous)::CollaborationScopeKeyDataValueType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L106)

```cpp
CollaborationScopeKeyDataValueType = 0xF635
```

### API-5ac93b4a7eac · ndn_service_framework::tlv::(anonymous)::CollaborationArtifactDataNameType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L107)

```cpp
CollaborationArtifactDataNameType = 0xF636
```

### API-bbf8193e1df8 · ndn_service_framework::tlv::(anonymous)::RequestSecurityBindingType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L112)

```cpp
RequestSecurityBindingType = 0xF810
```

原始接口说明：

```text
// Request-scoped confidentiality containers.  The concrete wire
// schema lives in RequestConfidentiality.hpp; Core messages retain
// them as opaque, validated blocks to avoid coupling the message
// layer to the crypto implementation.
```

### API-93e80ee9b23a · ndn_service_framework::tlv::(anonymous)::AeadEnvelopeType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L113)

```cpp
AeadEnvelopeType = 0xF820
```

### API-d526967e90a1 · ndn_service_framework::tlv::(anonymous)::SelectionKeyEnvelopeType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L114)

```cpp
SelectionKeyEnvelopeType = 0xF830
```

### API-e2aa51ccd5ee · ndn_service_framework::tlv::(anonymous)::EncryptionCertificateAdvertisementType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L115)

```cpp
EncryptionCertificateAdvertisementType = 0xF840
```

### API-7c7a62844c18 · ndn_service_framework::tlv::(anonymous)::CertificateNameType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L116)

```cpp
CertificateNameType = 0xF841
```

### API-c52c5f3162f2 · ndn_service_framework::tlv::(anonymous)::CertificateDigestType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L117)

```cpp
CertificateDigestType = 0xF842
```

### API-05a734621654 · ndn_service_framework::tlv::(anonymous)::CertificateValidFromType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L118)

```cpp
CertificateValidFromType = 0xF843
```

### API-231f6a2ade90 · ndn_service_framework::tlv::(anonymous)::CertificateValidUntilType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L119)

```cpp
CertificateValidUntilType = 0xF844
```

### API-9555028c6ab6 · ndn_service_framework::tlv::(anonymous)::KeyEnvelopeAlgorithmType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L120)

```cpp
KeyEnvelopeAlgorithmType = 0xF845
```

### API-4ff2f144b28f · ndn_service_framework::tlv::(anonymous)

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L124)

```cpp
enum
```

### API-a3a8a7b48a18 · ndn_service_framework::tlv::(anonymous)::FirstResponding

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L125)

```cpp
FirstResponding = 0
```

### API-53b037183251 · ndn_service_framework::tlv::(anonymous)::RandomSelection

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L126)

```cpp
RandomSelection = 1
```

### API-5851e4e3e09b · ndn_service_framework::tlv::(anonymous)::AllSelected

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L127)

```cpp
AllSelected = 2
```

### API-4ff2f144b28f · ndn_service_framework::tlv::(anonymous)

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L130)

```cpp
enum
```

### API-090c683aff87 · ndn_service_framework::tlv::(anonymous)::NormalRequest

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L131)

```cpp
NormalRequest = 0
```

### API-a0aaa2290cce · ndn_service_framework::tlv::(anonymous)::TargetedRequest

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L135)

```cpp
TargetedRequest = 1
```

原始接口说明：

```text
// Targeted invocation still uses NDNSF Request/Response, but skips the
// normal ACK/Selection phase because the requester already names the
// intended provider.
```

### API-33610a730830 · ndn_service_framework::tlv::(anonymous)::TargetedBootstrapRequest

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L136)

```cpp
TargetedBootstrapRequest = 2
```

### API-4ff2f144b28f · ndn_service_framework::tlv::(anonymous)

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L139)

```cpp
enum
```

### API-79e50754873e · ndn_service_framework::tlv::(anonymous)::UserPermission

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L140)

```cpp
UserPermission = 0
```

### API-7a26ad2928f8 · ndn_service_framework::tlv::(anonymous)::ProviderPermission

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L141)

```cpp
ProviderPermission = 1
```

### API-c53640bcd4a4 · ndn_service_framework::AbstractMessage

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L145)

```cpp
class AbstractMessage
```

### API-effb4b25ca83 · ndn_service_framework::AbstractMessage::~AbstractMessage

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L147)

```cpp
virtual ~AbstractMessage()
```

### API-4df3022754af · ndn_service_framework::AbstractMessage::WireEncode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L149)

```cpp
virtual ndn::Block WireEncode() const = 0;
```

### API-3c756dd4f910 · ndn_service_framework::AbstractMessage::WireDecode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L150)

```cpp
virtual bool WireDecode(const ndn::Block& block) = 0;
```

### API-2ba5ab5c1b1c · ndn_service_framework::AbstractMessage::Clear

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L151)

```cpp
virtual void Clear() = 0;
```

### API-e75208475d66 · ndn_service_framework::DeploymentControlMessage

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L159)

```cpp
class DeploymentControlMessage : public AbstractMessage
```

### API-73e5d70a813e · ndn_service_framework::DeploymentControlMessage::VERSION

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L162)

```cpp
static constexpr uint64_t VERSION = 1;
```

### API-27c62e6541bd · ndn_service_framework::DeploymentControlMessage::MAX_FIELDS

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L163)

```cpp
static constexpr size_t MAX_FIELDS = 64;
```

### API-2c0d8fccadc2 · ndn_service_framework::DeploymentControlMessage::MAX_FIELD_NAME

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L164)

```cpp
static constexpr size_t MAX_FIELD_NAME = 64;
```

### API-54ff183bd12c · ndn_service_framework::DeploymentControlMessage::MAX_FIELD_VALUE

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L165)

```cpp
static constexpr size_t MAX_FIELD_VALUE = 4096;
```

### API-70514ca0c04a · ndn_service_framework::DeploymentControlMessage::MAX_WIRE_SIZE

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L166)

```cpp
static constexpr size_t MAX_WIRE_SIZE = 65536;
```

### API-f042830bbde1 · ndn_service_framework::DeploymentControlMessage::DeploymentControlMessage

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L168)

```cpp
explicit DeploymentControlMessage(uint32_t messageType = tlv::DeploymentIntentType);
```

### API-599cb851f3de · ndn_service_framework::DeploymentControlMessage::setVersion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L169)

```cpp
void setVersion(uint64_t version);
```

### API-419450fc8cd0 · ndn_service_framework::DeploymentControlMessage::getVersion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L170)

```cpp
uint64_t getVersion() const;
```

### API-fe0cd8f4afcb · ndn_service_framework::DeploymentControlMessage::setField

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L171)

```cpp
void setField(const std::string& name, const std::string& value);
```

### API-3d3105a17788 · ndn_service_framework::DeploymentControlMessage::hasField

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L172)

```cpp
bool hasField(const std::string& name) const;
```

### API-6f4fe2df9285 · ndn_service_framework::DeploymentControlMessage::getField

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L173)

```cpp
const std::string& getField(const std::string& name) const;
```

### API-6e89d4842260 · ndn_service_framework::DeploymentControlMessage::getFields

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L174)

```cpp
const std::map<std::string, std::string>& getFields() const;
```

### API-ebe5ecfb33f3 · ndn_service_framework::DeploymentControlMessage::computeDigest

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L175)

```cpp
std::string computeDigest() const;
```

### API-e8ec9fc0765c · ndn_service_framework::DeploymentControlMessage::Clear

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L176)

```cpp
void Clear() override;
```

### API-7c8897a33c46 · ndn_service_framework::DeploymentControlMessage::WireEncode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L177)

```cpp
ndn::Block WireEncode() const override;
```

### API-5815885bd1c6 · ndn_service_framework::DeploymentControlMessage::WireDecode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L178)

```cpp
bool WireDecode(const ndn::Block& block) override;
```

### API-906918df6178 · ndn_service_framework::DeploymentControlMessage::getMessageType

protected / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L181)

```cpp
uint32_t getMessageType() const;
```

### API-564d9edca25a · ndn_service_framework::DeploymentIntent

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L190)

```cpp
class DeploymentIntent : public DeploymentControlMessage
```

### API-d955b3348dae · ndn_service_framework::DeploymentIntent::DeploymentIntent

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L191)

```cpp
DeploymentIntent();
```

### API-4fdba0313a78 · ndn_service_framework::ProviderCapabilityOffer

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L192)

```cpp
class ProviderCapabilityOffer : public DeploymentControlMessage
```

### API-578e13b930df · ndn_service_framework::ProviderCapabilityOffer::ProviderCapabilityOffer

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L193)

```cpp
ProviderCapabilityOffer();
```

### API-101f66f18aa3 · ndn_service_framework::DeploymentPlan

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L194)

```cpp
class DeploymentPlan : public DeploymentControlMessage
```

### API-d578238bbef2 · ndn_service_framework::DeploymentPlan::DeploymentPlan

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L195)

```cpp
DeploymentPlan();
```

### API-1d611f4a3792 · ndn_service_framework::ProviderReadyMessage

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L196)

```cpp
class ProviderReadyMessage : public DeploymentControlMessage
```

### API-5f975d239288 · ndn_service_framework::ProviderReadyMessage::ProviderReadyMessage

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L197)

```cpp
ProviderReadyMessage();
```

### API-1d1a801b32f9 · ndn_service_framework::ReadyAcknowledgement

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L198)

```cpp
class ReadyAcknowledgement : public DeploymentControlMessage
```

### API-90ca1951282a · ndn_service_framework::ReadyAcknowledgement::ReadyAcknowledgement

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L199)

```cpp
ReadyAcknowledgement();
```

### API-2678463ce886 · ndn_service_framework::ExecutionActivateMessage

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L200)

```cpp
class ExecutionActivateMessage : public DeploymentControlMessage
```

### API-b5f546661485 · ndn_service_framework::ExecutionActivateMessage::ExecutionActivateMessage

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L201)

```cpp
ExecutionActivateMessage();
```

### API-17a6b1f69141 · ndn_service_framework::SecureStatusQuery

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L202)

```cpp
class SecureStatusQuery : public DeploymentControlMessage
```

### API-19d2ede867d9 · ndn_service_framework::SecureStatusQuery::SecureStatusQuery

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L203)

```cpp
SecureStatusQuery();
```

### API-1d94fa0c8c49 · ndn_service_framework::SecureStatusSnapshot

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L204)

```cpp
class SecureStatusSnapshot : public DeploymentControlMessage
```

### API-c12a05bd1f9a · ndn_service_framework::SecureStatusSnapshot::SecureStatusSnapshot

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L205)

```cpp
SecureStatusSnapshot();
```

### API-911557d4e7e1 · ndn_service_framework::RequestCapabilities

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L206)

```cpp
class RequestCapabilities : public DeploymentControlMessage
```

### API-53b4e063ce3c · ndn_service_framework::RequestCapabilities::RequestCapabilities

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L207)

```cpp
RequestCapabilities();
```

### API-67e5144f8a8d · ndn_service_framework::EncryptedRequestInput

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L208)

```cpp
class EncryptedRequestInput : public DeploymentControlMessage
```

### API-d19c39ef4969 · ndn_service_framework::EncryptedRequestInput::EncryptedRequestInput

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L209)

```cpp
EncryptedRequestInput();
```

### API-a3c68f637531 · ndn_service_framework::SelectionInputKeyOffer

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L210)

```cpp
class SelectionInputKeyOffer : public DeploymentControlMessage
```

### API-489af1594d25 · ndn_service_framework::SelectionInputKeyOffer::SelectionInputKeyOffer

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L211)

```cpp
SelectionInputKeyOffer();
```

### API-7a3574e7bd4c · ndn_service_framework::SelectionInputKeyGrant

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L212)

```cpp
class SelectionInputKeyGrant : public DeploymentControlMessage
```

### API-582ba43eac7a · ndn_service_framework::SelectionInputKeyGrant::SelectionInputKeyGrant

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L213)

```cpp
SelectionInputKeyGrant();
```

### API-4b5fec6d147b · ndn_service_framework::ReservationLease

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L214)

```cpp
class ReservationLease : public DeploymentControlMessage
```

### API-4011004f6551 · ndn_service_framework::ReservationLease::ReservationLease

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L215)

```cpp
ReservationLease();
```

### API-b69ec21d13af · ndn_service_framework::SelectionDecision

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L216)

```cpp
class SelectionDecision : public DeploymentControlMessage
```

### API-d59ba3c80d32 · ndn_service_framework::SelectionDecision::SelectionDecision

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L217)

```cpp
SelectionDecision();
```

### API-b4ad7a3a716a · ndn_service_framework::SelectionDecisionReceipt

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L218)

```cpp
class SelectionDecisionReceipt : public DeploymentControlMessage
```

### API-edcc8338bd93 · ndn_service_framework::SelectionDecisionReceipt::SelectionDecisionReceipt

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L219)

```cpp
SelectionDecisionReceipt();
```

### API-b74a82c88e72 · ndn_service_framework::RecipientEncryptedAssignment

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L220)

```cpp
class RecipientEncryptedAssignment : public DeploymentControlMessage
```

### API-6714a28b5cae · ndn_service_framework::RecipientEncryptedAssignment::RecipientEncryptedAssignment

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L221)

```cpp
RecipientEncryptedAssignment();
```

### API-ecfb2e547dbf · ndn_service_framework::StageInputEvidence

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L222)

```cpp
class StageInputEvidence : public DeploymentControlMessage
```

### API-a508c53f5cb2 · ndn_service_framework::StageInputEvidence::StageInputEvidence

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L223)

```cpp
StageInputEvidence();
```

### API-083e6cc77b76 · ndn_service_framework::StageAbort

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L224)

```cpp
class StageAbort : public DeploymentControlMessage
```

### API-6a33d3228555 · ndn_service_framework::StageAbort::StageAbort

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L225)

```cpp
StageAbort();
```

### API-cc09276722b7 · ndn_service_framework::SelectionDecisionTombstone

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L226)

```cpp
class SelectionDecisionTombstone : public DeploymentControlMessage
```

### API-bbc001889aa4 · ndn_service_framework::SelectionDecisionTombstone::SelectionDecisionTombstone

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L227)

```cpp
SelectionDecisionTombstone();
```

### API-f3d0c7a36fc4 · ndn_service_framework::EncryptionCertificateAdvertisement

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L233)

```cpp
struct EncryptionCertificateAdvertisement : public AbstractMessage
```

### API-5abd7c262f56 · ndn_service_framework::EncryptionCertificateAdvertisement::TYPE

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L235)

```cpp
static constexpr uint32_t TYPE = tlv::EncryptionCertificateAdvertisementType;
```

### API-5b7101cadbec · ndn_service_framework::EncryptionCertificateAdvertisement::certificateName

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L237)

```cpp
ndn::Name certificateName;
```

### API-cb16091ec523 · ndn_service_framework::EncryptionCertificateAdvertisement::certificateDigest

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L238)

```cpp
std::string certificateDigest;
```

### API-9c96e620cdaf · ndn_service_framework::EncryptionCertificateAdvertisement::validFromMs

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L239)

```cpp
uint64_t validFromMs = 0;
```

### API-bbfb512072f1 · ndn_service_framework::EncryptionCertificateAdvertisement::validUntilMs

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L240)

```cpp
uint64_t validUntilMs = 0;
```

### API-2d11953bacc3 · ndn_service_framework::EncryptionCertificateAdvertisement::supportedEnvelopeAlgorithms

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L241)

```cpp
std::vector<std::string> supportedEnvelopeAlgorithms;
```

### API-3d3ddd004cc8 · ndn_service_framework::EncryptionCertificateAdvertisement::isValid

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L243)

```cpp
bool isValid(uint64_t nowMs = 0) const;
```

### API-dccb9f056027 · ndn_service_framework::EncryptionCertificateAdvertisement::supportsAlgorithm

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L244)

```cpp
bool supportsAlgorithm(const std::string& algorithm) const;
```

### API-2cf62db0deec · ndn_service_framework::EncryptionCertificateAdvertisement::Clear

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L245)

```cpp
void Clear() override;
```

### API-8c7ef7025ac9 · ndn_service_framework::EncryptionCertificateAdvertisement::WireEncode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L246)

```cpp
ndn::Block WireEncode() const override;
```

### API-18ac9b1514e3 · ndn_service_framework::EncryptionCertificateAdvertisement::WireDecode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L247)

```cpp
bool WireDecode(const ndn::Block& block) override;
```

### API-23663023e125 · ndn_service_framework::RequestMessage

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L250)

```cpp
class RequestMessage : public AbstractMessage
```

### API-340d64f7d899 · ndn_service_framework::RequestMessage::RequestMessage

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L252)

```cpp
RequestMessage();
```

### API-1a654a7a48d6 · ndn_service_framework::RequestMessage::RequestMessage

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L253)

```cpp
RequestMessage(const RequestMessage& other);
```

### API-54d785676351 · ndn_service_framework::RequestMessage::operator=

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L254)

```cpp
RequestMessage& operator=(const RequestMessage& other);
```

### API-dc2bbd0c1bdb · ndn_service_framework::RequestMessage::setTokens

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L256)

```cpp
void setTokens(const std::map<std::string, std::string>& tokens);
```

### API-a3d67a0bf3e9 · ndn_service_framework::RequestMessage::setUserToken

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L257)

```cpp
void setUserToken(const std::string& userToken);
```

### API-b8beb717f057 · ndn_service_framework::RequestMessage::setProviderToken

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L258)

```cpp
void setProviderToken(const std::string& providerToken);
```

### API-0ffabf24712f · ndn_service_framework::RequestMessage::setPayload

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L259)

```cpp
void setPayload(ndn::Buffer& payload, size_t size);
```

### API-5b5cafa22c68 · ndn_service_framework::RequestMessage::setPayloadBlock

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L260)

```cpp
void setPayloadBlock(const ndn::Block& payloadBlock);
```

### API-761ab6c327f5 · ndn_service_framework::RequestMessage::setStrategy

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L262)

```cpp
void setStrategy(size_t strategy);
```

原始接口说明：

```text
// FirstResponding = 0, RandomSelection = 1, AllSelected = 2.
```

### API-717a7817b19d · ndn_service_framework::RequestMessage::setRequestMode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L263)

```cpp
void setRequestMode(size_t requestMode);
```

### API-40a1d8ab0510 · ndn_service_framework::RequestMessage::setTargetProvider

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L264)

```cpp
void setTargetProvider(const ndn::Name& targetProvider);
```

### API-73c82cd656aa · ndn_service_framework::RequestMessage::setPolicyEpoch

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L265)

```cpp
void setPolicyEpoch(size_t policyEpoch);
```

### API-31ed14caeef2 · ndn_service_framework::RequestMessage::setControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L266)

```cpp
void setControllerVersion(const ControllerVersion& version);
```

### API-3f9a8e6618f0 · ndn_service_framework::RequestMessage::setUserEncryptionCertificate

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L267)

```cpp
void setUserEncryptionCertificate(
      const EncryptionCertificateAdvertisement& advertisement);
```

### API-4b8575d6f011 · ndn_service_framework::RequestMessage::setDeploymentIntent

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L269)

```cpp
void setDeploymentIntent(const DeploymentIntent& intent);
```

### API-945ec9cebfe3 · ndn_service_framework::RequestMessage::setRequestCapabilities

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L270)

```cpp
void setRequestCapabilities(const RequestCapabilities& capabilities);
```

### API-cc4eaf618d47 · ndn_service_framework::RequestMessage::setEncryptedRequestInput

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L271)

```cpp
void setEncryptedRequestInput(const EncryptedRequestInput& input);
```

### API-4eb1cb9b53f7 · ndn_service_framework::RequestMessage::setRequestSecurityBinding

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L272)

```cpp
void setRequestSecurityBinding(const ndn::Block& binding);
```

### API-8f18111aff0d · ndn_service_framework::RequestMessage::setStreamRequestOptions

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L273)

```cpp
void setStreamRequestOptions(const StreamRequestOptions& options);
```

### API-ebd05ece9fe9 · ndn_service_framework::RequestMessage::clearStreamRequestOptions

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L274)

```cpp
void clearStreamRequestOptions();
```

### API-4b1756a69701 · ndn_service_framework::RequestMessage::setConversationContinuation

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L275)

```cpp
void setConversationContinuation(
      const ConversationContinuationOptions& continuation);
```

### API-7466c13785d6 · ndn_service_framework::RequestMessage::clearConversationContinuation

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L277)

```cpp
void clearConversationContinuation();
```

### API-ae76b78c8101 · ndn_service_framework::RequestMessage::hasDeploymentIntent

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L278)

```cpp
bool hasDeploymentIntent() const;
```

### API-1900423c8c0d · ndn_service_framework::RequestMessage::hasRequestCapabilities

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L279)

```cpp
bool hasRequestCapabilities() const;
```

### API-30472fea493a · ndn_service_framework::RequestMessage::hasEncryptedRequestInput

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L280)

```cpp
bool hasEncryptedRequestInput() const;
```

### API-30c2b68d3f4f · ndn_service_framework::RequestMessage::hasRequestSecurityBinding

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L281)

```cpp
bool hasRequestSecurityBinding() const;
```

### API-12267eb9404e · ndn_service_framework::RequestMessage::hasUserEncryptionCertificate

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L282)

```cpp
bool hasUserEncryptionCertificate() const;
```

### API-534205386087 · ndn_service_framework::RequestMessage::hasStreamRequestOptions

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L283)

```cpp
bool hasStreamRequestOptions() const;
```

### API-31cdbd23e29d · ndn_service_framework::RequestMessage::hasConversationContinuation

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L284)

```cpp
bool hasConversationContinuation() const;
```

### API-059c94a53507 · ndn_service_framework::RequestMessage::getDeploymentIntent

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L285)

```cpp
const DeploymentIntent& getDeploymentIntent() const;
```

### API-c253ca70917d · ndn_service_framework::RequestMessage::getRequestCapabilities

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L286)

```cpp
const RequestCapabilities& getRequestCapabilities() const;
```

### API-3803f28b5ee0 · ndn_service_framework::RequestMessage::getEncryptedRequestInput

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L287)

```cpp
const EncryptedRequestInput& getEncryptedRequestInput() const;
```

### API-e81a00fb49fb · ndn_service_framework::RequestMessage::getRequestSecurityBinding

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L288)

```cpp
const ndn::Block& getRequestSecurityBinding() const;
```

### API-948096a0969b · ndn_service_framework::RequestMessage::getUserEncryptionCertificate

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L289)

```cpp
const EncryptionCertificateAdvertisement& getUserEncryptionCertificate() const;
```

### API-c50dac2adadc · ndn_service_framework::RequestMessage::getStreamRequestOptions

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L290)

```cpp
const StreamRequestOptions& getStreamRequestOptions() const;
```

### API-28b9259ca5c1 · ndn_service_framework::RequestMessage::getConversationContinuation

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L291)

```cpp
const ConversationContinuationOptions& getConversationContinuation() const;
```

### API-9d0a9f93d32b · ndn_service_framework::RequestMessage::getTokens

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L292)

```cpp
const std::map<std::string, std::string>& getTokens() const;
```

### API-18c0f3a874bb · ndn_service_framework::RequestMessage::getUserToken

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L293)

```cpp
const std::string& getUserToken() const;
```

### API-9683c7d06079 · ndn_service_framework::RequestMessage::getProviderToken

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L294)

```cpp
const std::string& getProviderToken() const;
```

### API-ada44828936c · ndn_service_framework::RequestMessage::getPayload

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L295)

```cpp
ndn::Buffer getPayload() const;
```

### API-c8afa394d1c4 · ndn_service_framework::RequestMessage::getPayloadBlock

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L296)

```cpp
const ndn::Block& getPayloadBlock() const;
```

### API-c5d9f65cc4ff · ndn_service_framework::RequestMessage::getPayloadSize

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L297)

```cpp
size_t getPayloadSize() const;
```

### API-bbb2ec663035 · ndn_service_framework::RequestMessage::getStrategy

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L298)

```cpp
size_t getStrategy() const;
```

### API-01c39174373f · ndn_service_framework::RequestMessage::getRequestMode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L299)

```cpp
size_t getRequestMode() const;
```

### API-2bae1d764de9 · ndn_service_framework::RequestMessage::getTargetProvider

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L300)

```cpp
const ndn::Name& getTargetProvider() const;
```

### API-7f74c604e6bc · ndn_service_framework::RequestMessage::getPolicyEpoch

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L301)

```cpp
size_t getPolicyEpoch() const;
```

### API-3b8d1fcfecc7 · ndn_service_framework::RequestMessage::hasControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L302)

```cpp
bool hasControllerVersion() const;
```

### API-dd33ae13a809 · ndn_service_framework::RequestMessage::getControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L303)

```cpp
const ControllerVersion& getControllerVersion() const;
```

### API-a8678eccdff1 · ndn_service_framework::RequestMessage::Clear

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L304)

```cpp
void Clear() override;
```

### API-8a33b51d019d · ndn_service_framework::RequestMessage::WireEncode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L305)

```cpp
ndn::Block WireEncode() const override;
```

### API-377c4197661c · ndn_service_framework::RequestMessage::WireDecode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L306)

```cpp
bool WireDecode(const ndn::Block& block) override;
```

### API-bc1c5189455f · ndn_service_framework::ResponseMessage

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L329)

```cpp
class ResponseMessage : public AbstractMessage
```

### API-523b4b1a7ec3 · ndn_service_framework::ResponseMessage::ResponseMessage

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L331)

```cpp
ResponseMessage();
```

### API-325e082624f1 · ndn_service_framework::ResponseMessage::ResponseMessage

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L332)

```cpp
ResponseMessage(const ResponseMessage& other);
```

### API-f83d484d109e · ndn_service_framework::ResponseMessage::operator=

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L333)

```cpp
ResponseMessage& operator=(const ResponseMessage& other);
```

### API-a0cbabda400f · ndn_service_framework::ResponseMessage::setStatus

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L335)

```cpp
void setStatus(bool status);
```

### API-49e3267f3dec · ndn_service_framework::ResponseMessage::setErrorInfo

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L336)

```cpp
void setErrorInfo(const std::string& errorInfo);
```

### API-11904bc36198 · ndn_service_framework::ResponseMessage::setTokens

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L337)

```cpp
void setTokens(const std::map<std::string, std::string>& tokens);
```

### API-24da70d22864 · ndn_service_framework::ResponseMessage::setUserToken

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L338)

```cpp
void setUserToken(const std::string& userToken);
```

### API-e167bf7b0197 · ndn_service_framework::ResponseMessage::setPayload

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L339)

```cpp
void setPayload(ndn::Buffer& payload, size_t size);
```

### API-dbcc057cc640 · ndn_service_framework::ResponseMessage::setPayloadBlock

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L340)

```cpp
void setPayloadBlock(const ndn::Block& payloadBlock);
```

### API-0a2357219723 · ndn_service_framework::ResponseMessage::setPolicyEpoch

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L341)

```cpp
void setPolicyEpoch(size_t policyEpoch);
```

### API-6e70d9073002 · ndn_service_framework::ResponseMessage::setControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L342)

```cpp
void setControllerVersion(const ControllerVersion& version);
```

### API-5e51e80dab97 · ndn_service_framework::ResponseMessage::setAeadEnvelope

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L343)

```cpp
void setAeadEnvelope(const ndn::Block& envelope);
```

### API-fc8f728c9927 · ndn_service_framework::ResponseMessage::setAuthenticatedTransportEvidence

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L344)

```cpp
void setAuthenticatedTransportEvidence(const std::string& dataName,
                                           const std::string& signerCertificate,
                                           const std::string& wireDigest);
```

### API-f2e90a30da15 · ndn_service_framework::ResponseMessage::setStreamCompletion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L347)

```cpp
void setStreamCompletion(const StreamCompletion& completion);
```

### API-217b662aaff4 · ndn_service_framework::ResponseMessage::clearStreamCompletion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L348)

```cpp
void clearStreamCompletion();
```

### API-1c9f21bd8989 · ndn_service_framework::ResponseMessage::getStatus

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L349)

```cpp
bool getStatus() const;
```

### API-d3aae96949a4 · ndn_service_framework::ResponseMessage::getErrorInfo

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L350)

```cpp
const std::string& getErrorInfo() const;
```

### API-b9941dcac494 · ndn_service_framework::ResponseMessage::getTokens

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L351)

```cpp
const std::map<std::string, std::string>& getTokens() const;
```

### API-7b16bb352c66 · ndn_service_framework::ResponseMessage::getUserToken

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L352)

```cpp
const std::string& getUserToken() const;
```

### API-859453690b3f · ndn_service_framework::ResponseMessage::getPayload

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L353)

```cpp
ndn::Buffer getPayload() const;
```

### API-bc1dd5959dc8 · ndn_service_framework::ResponseMessage::getPayloadBlock

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L354)

```cpp
const ndn::Block& getPayloadBlock() const;
```

### API-6a9e1cded6df · ndn_service_framework::ResponseMessage::getPayloadSize

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L355)

```cpp
size_t getPayloadSize() const;
```

### API-a3c8ccd7e311 · ndn_service_framework::ResponseMessage::getPolicyEpoch

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L356)

```cpp
size_t getPolicyEpoch() const;
```

### API-453a2895fa26 · ndn_service_framework::ResponseMessage::hasControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L357)

```cpp
bool hasControllerVersion() const;
```

### API-60465983f6af · ndn_service_framework::ResponseMessage::hasAeadEnvelope

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L358)

```cpp
bool hasAeadEnvelope() const;
```

### API-4fe4ebfa025a · ndn_service_framework::ResponseMessage::clearAeadEnvelope

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L359)

```cpp
void clearAeadEnvelope();
```

### API-a7bdbd20e7b1 · ndn_service_framework::ResponseMessage::getControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L360)

```cpp
const ControllerVersion& getControllerVersion() const;
```

### API-a36afb37437c · ndn_service_framework::ResponseMessage::getAeadEnvelope

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L361)

```cpp
const ndn::Block& getAeadEnvelope() const;
```

### API-6ac2993347b9 · ndn_service_framework::ResponseMessage::getDataName

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L362)

```cpp
const std::string& getDataName() const;
```

### API-f1a897db7b08 · ndn_service_framework::ResponseMessage::getSignerCertificate

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L363)

```cpp
const std::string& getSignerCertificate() const;
```

### API-629a73164ef7 · ndn_service_framework::ResponseMessage::getWireDigest

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L364)

```cpp
const std::string& getWireDigest() const;
```

### API-a7224d34e38e · ndn_service_framework::ResponseMessage::hasStreamCompletion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L365)

```cpp
bool hasStreamCompletion() const;
```

### API-421467484ae9 · ndn_service_framework::ResponseMessage::getStreamCompletion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L366)

```cpp
const StreamCompletion& getStreamCompletion() const;
```

### API-753926668e59 · ndn_service_framework::ResponseMessage::Clear

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L367)

```cpp
void Clear() override;
```

### API-1069eb606d62 · ndn_service_framework::ResponseMessage::WireEncode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L368)

```cpp
ndn::Block WireEncode() const override;
```

### API-4648222408c1 · ndn_service_framework::ResponseMessage::WireDecode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L369)

```cpp
bool WireDecode(const ndn::Block& block) override;
```

### API-5eeaf7f3d9a0 · ndn_service_framework::RequestAckMessage

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L390)

```cpp
class RequestAckMessage : public AbstractMessage
```

### API-edfdbbbabb7d · ndn_service_framework::RequestAckMessage::RequestAckMessage

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L392)

```cpp
RequestAckMessage();
```

### API-78ef9c3af9a0 · ndn_service_framework::RequestAckMessage::RequestAckMessage

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L393)

```cpp
RequestAckMessage(const RequestAckMessage& other);
```

### API-31c3d2596fd2 · ndn_service_framework::RequestAckMessage::operator=

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L394)

```cpp
RequestAckMessage& operator=(const RequestAckMessage& other);
```

### API-3ff0fca9a0e2 · ndn_service_framework::RequestAckMessage::setStatus

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L396)

```cpp
void setStatus(bool status);
```

### API-0a6bf26e57e2 · ndn_service_framework::RequestAckMessage::setMessage

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L397)

```cpp
void setMessage(const std::string& message);
```

### API-f89d5f5d8a41 · ndn_service_framework::RequestAckMessage::setUserToken

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L398)

```cpp
void setUserToken(const std::string& userToken);
```

### API-9d6bfc52ccbe · ndn_service_framework::RequestAckMessage::setProviderToken

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L399)

```cpp
void setProviderToken(const std::string& providerToken);
```

### API-c068156937de · ndn_service_framework::RequestAckMessage::setPayload

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L400)

```cpp
void setPayload(ndn::Buffer& payload, size_t size);
```

### API-77a219342a06 · ndn_service_framework::RequestAckMessage::setPayloadBlock

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L401)

```cpp
void setPayloadBlock(const ndn::Block& payloadBlock);
```

### API-da20e132291d · ndn_service_framework::RequestAckMessage::setPolicyEpoch

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L402)

```cpp
void setPolicyEpoch(size_t policyEpoch);
```

### API-bff7fd0b4667 · ndn_service_framework::RequestAckMessage::setControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L403)

```cpp
void setControllerVersion(const ControllerVersion& version);
```

### API-d850cdd30c23 · ndn_service_framework::RequestAckMessage::setProviderEncryptionCertificate

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L404)

```cpp
void setProviderEncryptionCertificate(
      const EncryptionCertificateAdvertisement& advertisement);
```

### API-1f64a5ee8c72 · ndn_service_framework::RequestAckMessage::setProviderCapabilityOffer

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L406)

```cpp
void setProviderCapabilityOffer(const ProviderCapabilityOffer& offer);
```

### API-54e33f0800c3 · ndn_service_framework::RequestAckMessage::setSelectionInputKeyOffer

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L407)

```cpp
void setSelectionInputKeyOffer(const SelectionInputKeyOffer& offer);
```

### API-dc1ac535a5bb · ndn_service_framework::RequestAckMessage::setReservationLease

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L408)

```cpp
void setReservationLease(const ReservationLease& lease);
```

### API-fe891ec1fbf3 · ndn_service_framework::RequestAckMessage::hasProviderCapabilityOffer

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L409)

```cpp
bool hasProviderCapabilityOffer() const;
```

### API-bd0cab6b3849 · ndn_service_framework::RequestAckMessage::hasSelectionInputKeyOffer

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L410)

```cpp
bool hasSelectionInputKeyOffer() const;
```

### API-bdc9782b89d4 · ndn_service_framework::RequestAckMessage::hasReservationLease

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L411)

```cpp
bool hasReservationLease() const;
```

### API-4dd23ffa0f13 · ndn_service_framework::RequestAckMessage::hasProviderEncryptionCertificate

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L412)

```cpp
bool hasProviderEncryptionCertificate() const;
```

### API-6f51928a7ea8 · ndn_service_framework::RequestAckMessage::getProviderCapabilityOffer

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L413)

```cpp
const ProviderCapabilityOffer& getProviderCapabilityOffer() const;
```

### API-0ab6f451dc70 · ndn_service_framework::RequestAckMessage::getSelectionInputKeyOffer

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L414)

```cpp
const SelectionInputKeyOffer& getSelectionInputKeyOffer() const;
```

### API-1b97d005dc90 · ndn_service_framework::RequestAckMessage::getReservationLease

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L415)

```cpp
const ReservationLease& getReservationLease() const;
```

### API-008001217963 · ndn_service_framework::RequestAckMessage::getProviderEncryptionCertificate

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L416)

```cpp
const EncryptionCertificateAdvertisement& getProviderEncryptionCertificate() const;
```

### API-7c947ca0f133 · ndn_service_framework::RequestAckMessage::getStatus

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L417)

```cpp
bool getStatus() const;
```

### API-ea46b7012abc · ndn_service_framework::RequestAckMessage::getMessage

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L418)

```cpp
const std::string& getMessage() const;
```

### API-96440d71e8ea · ndn_service_framework::RequestAckMessage::getUserToken

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L419)

```cpp
const std::string& getUserToken() const;
```

### API-a4a3e63b4aa6 · ndn_service_framework::RequestAckMessage::getProviderToken

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L420)

```cpp
const std::string& getProviderToken() const;
```

### API-f0c4c03fd37a · ndn_service_framework::RequestAckMessage::getPayload

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L421)

```cpp
ndn::Buffer getPayload() const;
```

### API-74346737e849 · ndn_service_framework::RequestAckMessage::getPayloadBlock

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L422)

```cpp
const ndn::Block& getPayloadBlock() const;
```

### API-e80db24ef2ed · ndn_service_framework::RequestAckMessage::getPayloadSize

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L423)

```cpp
size_t getPayloadSize() const;
```

### API-d3dbd38a4149 · ndn_service_framework::RequestAckMessage::getPolicyEpoch

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L424)

```cpp
size_t getPolicyEpoch() const;
```

### API-a33cacc7d676 · ndn_service_framework::RequestAckMessage::hasControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L425)

```cpp
bool hasControllerVersion() const;
```

### API-f0ae196e656e · ndn_service_framework::RequestAckMessage::getControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L426)

```cpp
const ControllerVersion& getControllerVersion() const;
```

### API-0b1fff052088 · ndn_service_framework::RequestAckMessage::Clear

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L427)

```cpp
void Clear() override;
```

### API-5d21951e7904 · ndn_service_framework::RequestAckMessage::WireEncode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L428)

```cpp
ndn::Block WireEncode() const override;
```

### API-bf80cde0e284 · ndn_service_framework::RequestAckMessage::WireDecode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L429)

```cpp
bool WireDecode(const ndn::Block& block) override;
```

### API-11c571a6eb72 · ndn_service_framework::encodeOpaqueAssignmentSet

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L452)

```cpp
ndn::Buffer
encodeOpaqueAssignmentSet(const std::vector<ndn::Buffer>& assignments);
```

原始接口说明：

```text
/** Encode multiple generic collaboration assignments for one provider.
 *
 * Core preserves item order and byte identity but never interprets an item.
 * A single assignment remains its original bytes for wire compatibility.
 */
```

### API-624cb7990cbf · ndn_service_framework::decodeOpaqueAssignmentSet

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L456)

```cpp
std::vector<ndn::Buffer>
decodeOpaqueAssignmentSet(const ndn::Buffer& payload);
```

原始接口说明：

```text
/** Decode a Core assignment set; a non-container is one opaque item. */
```

### API-181f7f48935a · ndn_service_framework::CollaborationAssignmentEnvelope

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L465)

```cpp
struct CollaborationAssignmentEnvelope
```

### API-80b2655ca9e9 · ndn_service_framework::CollaborationAssignmentEnvelope::role

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L467)

```cpp
std::string role;
```

### API-ba3358d948cb · ndn_service_framework::CollaborationAssignmentEnvelope::assignedArtifact

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L468)

```cpp
ndn::Name assignedArtifact;
```

### API-e303820ed157 · ndn_service_framework::CollaborationAssignmentEnvelope::artifactDataName

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L471)

```cpp
ndn::Name artifactDataName;
```

原始接口说明：

```text
// Content-addressed Data name for the assignment-bound canonical root.
// This is framework metadata and must survive opaque V3 payloads.
```

### API-c6a30e4f8b63 · ndn_service_framework::CollaborationAssignmentEnvelope::requiresProvisioning

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L472)

```cpp
bool requiresProvisioning = false;
```

### API-3d0718420b94 · ndn_service_framework::CollaborationAssignmentEnvelope::provisioningTimeoutMs

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L473)

```cpp
uint64_t provisioningTimeoutMs = 0;
```

### API-eb1e3cce36cf · ndn_service_framework::CollaborationAssignmentEnvelope::scopeKeys

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L474)

```cpp
std::map<std::string, ndn::Buffer> scopeKeys;
```

### API-d93f25a281ac · ndn_service_framework::CollaborationAssignmentEnvelope::scopeKeyDataNames

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L477)

```cpp
std::map<std::string, ndn::Name> scopeKeyDataNames;
```

原始接口说明：

```text
// Encrypted large-Data names from which Providers fetch request-scoped
// keys when the assignment does not carry the raw key material.
```

### API-50461a93b0a5 · ndn_service_framework::CollaborationAssignmentEnvelope::opaquePayload

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L478)

```cpp
ndn::Buffer opaquePayload;
```

### API-ce3df746200e · ndn_service_framework::encodeCollaborationAssignmentEnvelope

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L481)

```cpp
ndn::Buffer
encodeCollaborationAssignmentEnvelope(
    const CollaborationAssignmentEnvelope& assignment);
```

### API-0257390dda82 · ndn_service_framework::decodeCollaborationAssignmentEnvelope

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L485)

```cpp
bool
decodeCollaborationAssignmentEnvelope(
    const ndn::Buffer& payload,
    CollaborationAssignmentEnvelope& assignment);
```

### API-e062cbc48381 · ndn_service_framework::SelectionProviderEntry

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L490)

```cpp
struct SelectionProviderEntry
```

### API-306efc5ecf87 · ndn_service_framework::SelectionProviderEntry::providerName

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L492)

```cpp
ndn::Name providerName;
```

### API-69c05e5be183 · ndn_service_framework::SelectionProviderEntry::providerTokenHash

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L493)

```cpp
std::string providerTokenHash;
```

### API-b798b33d4ab4 · ndn_service_framework::SelectionProviderEntry::assignmentPayload

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L494)

```cpp
ndn::Buffer assignmentPayload;
```

### API-a80b41442ffd · ndn_service_framework::ServiceSelectionMessage

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L497)

```cpp
class ServiceSelectionMessage : public AbstractMessage
```

### API-e064855229fc · ndn_service_framework::ServiceSelectionMessage::ServiceSelectionMessage

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L499)

```cpp
ServiceSelectionMessage();
```

### API-6220680f3e6f · ndn_service_framework::ServiceSelectionMessage::ServiceSelectionMessage

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L500)

```cpp
ServiceSelectionMessage(const ServiceSelectionMessage& other);
```

### API-ed646a422784 · ndn_service_framework::ServiceSelectionMessage::operator=

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L501)

```cpp
ServiceSelectionMessage& operator=(const ServiceSelectionMessage& other);
```

### API-a2f0a3a96549 · ndn_service_framework::ServiceSelectionMessage::setRequestIDs

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L503)

```cpp
void setRequestIDs(const std::vector<std::string>& requestIDs);
```

### API-a9cd0261ad72 · ndn_service_framework::ServiceSelectionMessage::setProviderToken

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L504)

```cpp
void setProviderToken(const std::string& providerToken);
```

### API-188592087ce8 · ndn_service_framework::ServiceSelectionMessage::setAssignmentPayload

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L505)

```cpp
void setAssignmentPayload(const ndn::Buffer& payload);
```

### API-137f2c8d7db9 · ndn_service_framework::ServiceSelectionMessage::setPolicyEpoch

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L506)

```cpp
void setPolicyEpoch(size_t policyEpoch);
```

### API-2a34559b91fc · ndn_service_framework::ServiceSelectionMessage::setControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L507)

```cpp
void setControllerVersion(const ControllerVersion& version);
```

### API-c7848c36a3b7 · ndn_service_framework::ServiceSelectionMessage::setAttempt

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L508)

```cpp
void setAttempt(uint64_t attempt);
```

### API-7e70f5a511eb · ndn_service_framework::ServiceSelectionMessage::addProviderEntry

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L509)

```cpp
void addProviderEntry(const SelectionProviderEntry& entry);
```

### API-3aa72cb7ecfb · ndn_service_framework::ServiceSelectionMessage::setDeploymentPlan

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L510)

```cpp
void setDeploymentPlan(const DeploymentPlan& plan);
```

### API-febfa7fce5e4 · ndn_service_framework::ServiceSelectionMessage::setSelectionDecision

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L511)

```cpp
void setSelectionDecision(const SelectionDecision& decision);
```

### API-4e50439d0a0d · ndn_service_framework::ServiceSelectionMessage::setSelectionInputKeyGrant

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L512)

```cpp
void setSelectionInputKeyGrant(const SelectionInputKeyGrant& grant);
```

### API-5575854bc2c1 · ndn_service_framework::ServiceSelectionMessage::setSelectionKeyEnvelope

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L513)

```cpp
void setSelectionKeyEnvelope(const ndn::Block& envelope);
```

### API-cc1e1437f215 · ndn_service_framework::ServiceSelectionMessage::clearSelectionKeyEnvelope

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L514)

```cpp
void clearSelectionKeyEnvelope();
```

### API-77c35a520ff3 · ndn_service_framework::ServiceSelectionMessage::setStreamEventKeyGrant

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L518)

```cpp
void setStreamEventKeyGrant(const ndn::Block& grant);
```

原始接口说明：

```text
/** Set the provider-specific streamed event-key grant. The block must
     * contain exactly one HybridMessageEnvelope and is emitted only in the
     * selected Provider's Selection projection. */
```

### API-00208a882c3e · ndn_service_framework::ServiceSelectionMessage::setRecipientEncryptedAssignment

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L519)

```cpp
void setRecipientEncryptedAssignment(const RecipientEncryptedAssignment& assignment);
```

### API-3c934e3ee157 · ndn_service_framework::ServiceSelectionMessage::hasDeploymentPlan

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L520)

```cpp
bool hasDeploymentPlan() const;
```

### API-7bec0bb0a70b · ndn_service_framework::ServiceSelectionMessage::hasSelectionDecision

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L521)

```cpp
bool hasSelectionDecision() const;
```

### API-2e4d4519c8c8 · ndn_service_framework::ServiceSelectionMessage::hasSelectionInputKeyGrant

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L522)

```cpp
bool hasSelectionInputKeyGrant() const;
```

### API-c9d3f8a37b80 · ndn_service_framework::ServiceSelectionMessage::hasSelectionKeyEnvelope

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L523)

```cpp
bool hasSelectionKeyEnvelope() const;
```

### API-35cd5cd6bc3c · ndn_service_framework::ServiceSelectionMessage::hasStreamEventKeyGrant

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L524)

```cpp
bool hasStreamEventKeyGrant() const;
```

### API-0caf343f0e69 · ndn_service_framework::ServiceSelectionMessage::hasRecipientEncryptedAssignment

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L525)

```cpp
bool hasRecipientEncryptedAssignment() const;
```

### API-c5027266821b · ndn_service_framework::ServiceSelectionMessage::getDeploymentPlan

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L526)

```cpp
const DeploymentPlan& getDeploymentPlan() const;
```

### API-9658a7227bbe · ndn_service_framework::ServiceSelectionMessage::getSelectionDecision

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L527)

```cpp
const SelectionDecision& getSelectionDecision() const;
```

### API-75e7347a7b39 · ndn_service_framework::ServiceSelectionMessage::getSelectionInputKeyGrant

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L528)

```cpp
const SelectionInputKeyGrant& getSelectionInputKeyGrant() const;
```

### API-2df6e4185e06 · ndn_service_framework::ServiceSelectionMessage::getSelectionKeyEnvelope

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L529)

```cpp
const ndn::Block& getSelectionKeyEnvelope() const;
```

### API-2ed207eeaca8 · ndn_service_framework::ServiceSelectionMessage::getStreamEventKeyGrant

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L530)

```cpp
const ndn::Block& getStreamEventKeyGrant() const;
```

### API-adaff32a15c4 · ndn_service_framework::ServiceSelectionMessage::getRecipientEncryptedAssignment

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L531)

```cpp
const RecipientEncryptedAssignment& getRecipientEncryptedAssignment() const;
```

### API-e3820bc467b5 · ndn_service_framework::ServiceSelectionMessage::getRequestIDs

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L532)

```cpp
const std::vector<std::string>& getRequestIDs() const;
```

### API-ad5cb7ee07bd · ndn_service_framework::ServiceSelectionMessage::getProviderToken

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L533)

```cpp
const std::string& getProviderToken() const;
```

### API-da26433d4ea2 · ndn_service_framework::ServiceSelectionMessage::getAssignmentPayload

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L534)

```cpp
const ndn::Buffer& getAssignmentPayload() const;
```

### API-f5022c3e4026 · ndn_service_framework::ServiceSelectionMessage::getPolicyEpoch

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L535)

```cpp
size_t getPolicyEpoch() const;
```

### API-f8ee75e2abb1 · ndn_service_framework::ServiceSelectionMessage::hasControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L536)

```cpp
bool hasControllerVersion() const;
```

### API-1b80614ae745 · ndn_service_framework::ServiceSelectionMessage::getControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L537)

```cpp
const ControllerVersion& getControllerVersion() const;
```

### API-b0dea83c84fe · ndn_service_framework::ServiceSelectionMessage::getAttempt

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L538)

```cpp
uint64_t getAttempt() const;
```

### API-41eb4afbde77 · ndn_service_framework::ServiceSelectionMessage::getProviderEntries

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L539)

```cpp
const std::vector<SelectionProviderEntry>& getProviderEntries() const;
```

### API-2a05bc0a5318 · ndn_service_framework::ServiceSelectionMessage::Clear

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L540)

```cpp
void Clear() override;
```

### API-c233ed530558 · ndn_service_framework::ServiceSelectionMessage::WireEncode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L541)

```cpp
ndn::Block WireEncode() const override;
```

### API-9d2b96de07ac · ndn_service_framework::ServiceSelectionMessage::WireDecode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L542)

```cpp
bool WireDecode(const ndn::Block& block) override;
```

### API-bc2e44a4c011 · ndn_service_framework::CollaborationDataMessage

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L561)

```cpp
class CollaborationDataMessage : public AbstractMessage
```

### API-c0f653636569 · ndn_service_framework::CollaborationDataMessage::CollaborationDataMessage

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L563)

```cpp
CollaborationDataMessage();
```

### API-48068edb60bc · ndn_service_framework::CollaborationDataMessage::setKeyScope

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L565)

```cpp
void setKeyScope(const std::string& keyScope);
```

### API-3caf5b2cae45 · ndn_service_framework::CollaborationDataMessage::setTopic

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L566)

```cpp
void setTopic(const ndn::Name& topic);
```

### API-329ca7923302 · ndn_service_framework::CollaborationDataMessage::setProducerRole

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L567)

```cpp
void setProducerRole(const std::string& role);
```

### API-ac15bbbcd770 · ndn_service_framework::CollaborationDataMessage::setSequence

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L568)

```cpp
void setSequence(uint64_t sequence);
```

### API-3f49cc275dc5 · ndn_service_framework::CollaborationDataMessage::setPayload

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L569)

```cpp
void setPayload(const ndn::Buffer& payload);
```

### API-4bb13da87d38 · ndn_service_framework::CollaborationDataMessage::getKeyScope

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L571)

```cpp
const std::string& getKeyScope() const;
```

### API-897b35944429 · ndn_service_framework::CollaborationDataMessage::getTopic

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L572)

```cpp
const ndn::Name& getTopic() const;
```

### API-06f0df476ad0 · ndn_service_framework::CollaborationDataMessage::getProducerRole

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L573)

```cpp
const std::string& getProducerRole() const;
```

### API-e1ea6f11de4c · ndn_service_framework::CollaborationDataMessage::getSequence

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L574)

```cpp
uint64_t getSequence() const;
```

### API-6fc2042ce778 · ndn_service_framework::CollaborationDataMessage::getPayload

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L575)

```cpp
const ndn::Buffer& getPayload() const;
```

### API-7051a0cadf7b · ndn_service_framework::CollaborationDataMessage::Clear

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L577)

```cpp
void Clear() override;
```

### API-115c5829ea96 · ndn_service_framework::CollaborationDataMessage::WireEncode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L578)

```cpp
ndn::Block WireEncode() const override;
```

### API-6332abe8a649 · ndn_service_framework::CollaborationDataMessage::WireDecode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L579)

```cpp
bool WireDecode(const ndn::Block& block) override;
```

### API-9b2845373177 · ndn_service_framework::HybridMessageEnvelope

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L590)

```cpp
class HybridMessageEnvelope : public AbstractMessage
```

### API-462078e78b9a · ndn_service_framework::HybridMessageEnvelope::HybridMessageEnvelope

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L592)

```cpp
HybridMessageEnvelope();
```

### API-c1a22a609e78 · ndn_service_framework::HybridMessageEnvelope::setVersion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L594)

```cpp
void setVersion(size_t version);
```

### API-fcdd214c457e · ndn_service_framework::HybridMessageEnvelope::setAlgorithm

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L595)

```cpp
void setAlgorithm(const std::string& algorithm);
```

### API-007cbc2ca423 · ndn_service_framework::HybridMessageEnvelope::setKeyId

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L596)

```cpp
void setKeyId(const std::string& keyId);
```

### API-9a2c80185c37 · ndn_service_framework::HybridMessageEnvelope::setEpochId

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L597)

```cpp
void setEpochId(const std::string& epochId);
```

### API-8f20e7c75810 · ndn_service_framework::HybridMessageEnvelope::setMessageType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L598)

```cpp
void setMessageType(const std::string& messageType);
```

### API-a995affd990c · ndn_service_framework::HybridMessageEnvelope::setNonce

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L599)

```cpp
void setNonce(const ndn::Buffer& nonce);
```

### API-b11da1d4c35b · ndn_service_framework::HybridMessageEnvelope::setCipherText

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L600)

```cpp
void setCipherText(const ndn::Buffer& cipherText);
```

### API-9c92a70d8042 · ndn_service_framework::HybridMessageEnvelope::setAuthTag

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L601)

```cpp
void setAuthTag(const ndn::Buffer& authTag);
```

### API-5e43c7999757 · ndn_service_framework::HybridMessageEnvelope::setWrappedMessageKey

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L602)

```cpp
void setWrappedMessageKey(const ndn::Buffer& wrappedMessageKey);
```

### API-5a58e5825e0c · ndn_service_framework::HybridMessageEnvelope::getVersion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L604)

```cpp
size_t getVersion() const;
```

### API-f338848e2254 · ndn_service_framework::HybridMessageEnvelope::getAlgorithm

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L605)

```cpp
const std::string& getAlgorithm() const;
```

### API-b7175e22fc5a · ndn_service_framework::HybridMessageEnvelope::getKeyId

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L606)

```cpp
const std::string& getKeyId() const;
```

### API-71c7a686fda8 · ndn_service_framework::HybridMessageEnvelope::getEpochId

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L607)

```cpp
const std::string& getEpochId() const;
```

### API-0745ce74caca · ndn_service_framework::HybridMessageEnvelope::getMessageType

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L608)

```cpp
const std::string& getMessageType() const;
```

### API-636137f8e3b6 · ndn_service_framework::HybridMessageEnvelope::getNonce

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L609)

```cpp
const ndn::Buffer& getNonce() const;
```

### API-d44486d9e019 · ndn_service_framework::HybridMessageEnvelope::getCipherText

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L610)

```cpp
const ndn::Buffer& getCipherText() const;
```

### API-8aeddbd491dd · ndn_service_framework::HybridMessageEnvelope::getAuthTag

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L611)

```cpp
const ndn::Buffer& getAuthTag() const;
```

### API-e4f498067e8b · ndn_service_framework::HybridMessageEnvelope::getWrappedMessageKey

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L612)

```cpp
const ndn::Buffer& getWrappedMessageKey() const;
```

### API-a0e679281276 · ndn_service_framework::HybridMessageEnvelope::hasWrappedMessageKey

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L613)

```cpp
bool hasWrappedMessageKey() const;
```

### API-f18f3dc8b140 · ndn_service_framework::HybridMessageEnvelope::Clear

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L615)

```cpp
void Clear() override;
```

### API-a1882074c56e · ndn_service_framework::HybridMessageEnvelope::WireEncode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L616)

```cpp
ndn::Block WireEncode() const override;
```

### API-66bc0923ed2e · ndn_service_framework::HybridMessageEnvelope::WireDecode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L617)

```cpp
bool WireDecode(const ndn::Block& block) override;
```

### API-7868f6c9b82d · ndn_service_framework::PermissionEntry

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L635)

```cpp
class PermissionEntry : public AbstractMessage
```

### API-972c945782eb · ndn_service_framework::PermissionEntry::PermissionEntry

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L637)

```cpp
PermissionEntry();
```

### API-ad46d1d35113 · ndn_service_framework::PermissionEntry::setProviderName

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L639)

```cpp
void setProviderName(const std::string& providerName);
```

### API-d8b82ae58bf2 · ndn_service_framework::PermissionEntry::setServiceName

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L640)

```cpp
void setServiceName(const std::string& serviceName);
```

### API-ac1280d5ac0a · ndn_service_framework::PermissionEntry::setToken

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L641)

```cpp
void setToken(const std::string& token);
```

### API-2babce185e5c · ndn_service_framework::PermissionEntry::setTtl

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L642)

```cpp
void setTtl(size_t ttl);
```

### API-f92840f451d0 · ndn_service_framework::PermissionEntry::setVersion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L643)

```cpp
void setVersion(size_t version);
```

### API-58628cea6090 · ndn_service_framework::PermissionEntry::getProviderName

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L645)

```cpp
const std::string& getProviderName() const;
```

### API-1313539bf3c4 · ndn_service_framework::PermissionEntry::getServiceName

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L646)

```cpp
const std::string& getServiceName() const;
```

### API-56865ef59831 · ndn_service_framework::PermissionEntry::getToken

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L647)

```cpp
const std::string& getToken() const;
```

### API-68bc2b8ecfb9 · ndn_service_framework::PermissionEntry::getTtl

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L648)

```cpp
size_t getTtl() const;
```

### API-55df7d3e8d62 · ndn_service_framework::PermissionEntry::getVersion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L649)

```cpp
size_t getVersion() const;
```

### API-a927f2286b1b · ndn_service_framework::PermissionEntry::toString

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L650)

```cpp
std::string toString() const;
```

### API-2c7bc0653a08 · ndn_service_framework::PermissionEntry::Clear

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L652)

```cpp
void Clear() override;
```

### API-9014c544bc57 · ndn_service_framework::PermissionEntry::WireEncode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L653)

```cpp
ndn::Block WireEncode() const override;
```

### API-7e41d29b330a · ndn_service_framework::PermissionEntry::WireDecode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L654)

```cpp
bool WireDecode(const ndn::Block& block) override;
```

### API-53154411b7d5 · ndn_service_framework::PermissionResponse

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L665)

```cpp
class PermissionResponse : public AbstractMessage
```

### API-c5d98cb09005 · ndn_service_framework::PermissionResponse::PermissionResponse

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L667)

```cpp
PermissionResponse();
```

### API-37feaa6ebeb6 · ndn_service_framework::PermissionResponse::setTargetIdentity

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L669)

```cpp
void setTargetIdentity(const std::string& targetIdentity);
```

### API-8e6169504075 · ndn_service_framework::PermissionResponse::setPermissionKind

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L670)

```cpp
void setPermissionKind(size_t permissionKind);
```

### API-bf7f45d3f9ea · ndn_service_framework::PermissionResponse::setPolicyEpoch

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L671)

```cpp
void setPolicyEpoch(size_t policyEpoch);
```

### API-50bab4265677 · ndn_service_framework::PermissionResponse::setControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L672)

```cpp
void setControllerVersion(const ControllerVersion& version);
```

### API-98dcd751aa56 · ndn_service_framework::PermissionResponse::setEntries

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L673)

```cpp
void setEntries(const std::vector<PermissionEntry>& entries);
```

### API-653321c98e16 · ndn_service_framework::PermissionResponse::addEntry

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L674)

```cpp
void addEntry(const PermissionEntry& entry);
```

### API-24281856b2cd · ndn_service_framework::PermissionResponse::getTargetIdentity

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L676)

```cpp
const std::string& getTargetIdentity() const;
```

### API-49912174a7f9 · ndn_service_framework::PermissionResponse::getPermissionKind

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L677)

```cpp
size_t getPermissionKind() const;
```

### API-f3cba48e0d7c · ndn_service_framework::PermissionResponse::getPolicyEpoch

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L678)

```cpp
size_t getPolicyEpoch() const;
```

### API-c867684452d9 · ndn_service_framework::PermissionResponse::hasControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L679)

```cpp
bool hasControllerVersion() const;
```

### API-ad2d3387db2c · ndn_service_framework::PermissionResponse::getControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L680)

```cpp
const ControllerVersion& getControllerVersion() const;
```

### API-31ebee7c3114 · ndn_service_framework::PermissionResponse::getEntries

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L681)

```cpp
const std::vector<PermissionEntry>& getEntries() const;
```

### API-804505dc8ac6 · ndn_service_framework::PermissionResponse::toString

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L682)

```cpp
std::string toString() const;
```

### API-705e782913e8 · ndn_service_framework::PermissionResponse::Clear

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L684)

```cpp
void Clear() override;
```

### API-f0bc880d6b06 · ndn_service_framework::PermissionResponse::WireEncode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L685)

```cpp
ndn::Block WireEncode() const override;
```

### API-7cc0e3f30e71 · ndn_service_framework::PermissionResponse::WireDecode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L686)

```cpp
bool WireDecode(const ndn::Block& block) override;
```

### API-2f3e215fe55f · ndn_service_framework::PolicyManifest

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L697)

```cpp
class PolicyManifest : public AbstractMessage
```

### API-fee68ce21400 · ndn_service_framework::PolicyManifest::PolicyManifest

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L699)

```cpp
PolicyManifest();
```

### API-05d0962dc061 · ndn_service_framework::PolicyManifest::setPolicyEpoch

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L701)

```cpp
void setPolicyEpoch(size_t policyEpoch);
```

### API-fb6ef46d4f36 · ndn_service_framework::PolicyManifest::setControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L702)

```cpp
void setControllerVersion(const ControllerVersion& version);
```

### API-63c8f392fbbf · ndn_service_framework::PolicyManifest::setValidFromMs

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L703)

```cpp
void setValidFromMs(uint64_t validFromMs);
```

### API-a0e419d019a3 · ndn_service_framework::PolicyManifest::setGracePeriodMs

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L704)

```cpp
void setGracePeriodMs(uint64_t gracePeriodMs);
```

### API-562af0878d66 · ndn_service_framework::PolicyManifest::setRequiredKeyEpoch

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L705)

```cpp
void setRequiredKeyEpoch(size_t requiredKeyEpoch);
```

### API-3fc453e331d6 · ndn_service_framework::PolicyManifest::getPolicyEpoch

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L707)

```cpp
size_t getPolicyEpoch() const;
```

### API-928d3eba754a · ndn_service_framework::PolicyManifest::hasControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L708)

```cpp
bool hasControllerVersion() const;
```

### API-4c84aa830910 · ndn_service_framework::PolicyManifest::getControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L709)

```cpp
const ControllerVersion& getControllerVersion() const;
```

### API-c3db0b8b6e7a · ndn_service_framework::PolicyManifest::getValidFromMs

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L710)

```cpp
uint64_t getValidFromMs() const;
```

### API-9c579c40e340 · ndn_service_framework::PolicyManifest::getGracePeriodMs

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L711)

```cpp
uint64_t getGracePeriodMs() const;
```

### API-2c93960cad30 · ndn_service_framework::PolicyManifest::getRequiredKeyEpoch

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L712)

```cpp
size_t getRequiredKeyEpoch() const;
```

### API-1e9c4d9cb57b · ndn_service_framework::PolicyManifest::toString

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L713)

```cpp
std::string toString() const;
```

### API-09f95726b14f · ndn_service_framework::PolicyManifest::Clear

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L715)

```cpp
void Clear() override;
```

### API-a9fd26726d70 · ndn_service_framework::PolicyManifest::WireEncode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L716)

```cpp
ndn::Block WireEncode() const override;
```

### API-ef7aa836c626 · ndn_service_framework::PolicyManifest::WireDecode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L717)

```cpp
bool WireDecode(const ndn::Block& block) override;
```

### API-6acd8c142c44 · ndn_service_framework::EncryptedPermissionResponse

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L731)

```cpp
class EncryptedPermissionResponse : public AbstractMessage
```

### API-8bc2bd048ed5 · ndn_service_framework::EncryptedPermissionResponse::EncryptedPermissionResponse

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L733)

```cpp
EncryptedPermissionResponse();
```

### API-8a183ece9434 · ndn_service_framework::EncryptedPermissionResponse::setRecipientCertName

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L735)

```cpp
void setRecipientCertName(const std::string& recipientCertName);
```

### API-1fd11361134a · ndn_service_framework::EncryptedPermissionResponse::setAlgorithm

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L736)

```cpp
void setAlgorithm(const std::string& algorithm);
```

### API-e9aad738b9c2 · ndn_service_framework::EncryptedPermissionResponse::setEncryptedAesKey

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L737)

```cpp
void setEncryptedAesKey(const ndn::Buffer& encryptedAesKey);
```

### API-81162602f139 · ndn_service_framework::EncryptedPermissionResponse::setIv

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L738)

```cpp
void setIv(const ndn::Buffer& iv);
```

### API-471dbe627c0a · ndn_service_framework::EncryptedPermissionResponse::setCipherText

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L739)

```cpp
void setCipherText(const ndn::Buffer& cipherText);
```

### API-c634d5085671 · ndn_service_framework::EncryptedPermissionResponse::getRecipientCertName

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L741)

```cpp
const std::string& getRecipientCertName() const;
```

### API-89fb77fe1c30 · ndn_service_framework::EncryptedPermissionResponse::getAlgorithm

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L742)

```cpp
const std::string& getAlgorithm() const;
```

### API-820e3792376c · ndn_service_framework::EncryptedPermissionResponse::getEncryptedAesKey

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L743)

```cpp
const ndn::Buffer& getEncryptedAesKey() const;
```

### API-d18a35800006 · ndn_service_framework::EncryptedPermissionResponse::getIv

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L744)

```cpp
const ndn::Buffer& getIv() const;
```

### API-a80a1267ff0b · ndn_service_framework::EncryptedPermissionResponse::getCipherText

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L745)

```cpp
const ndn::Buffer& getCipherText() const;
```

### API-a0d3c2823725 · ndn_service_framework::EncryptedPermissionResponse::toString

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L746)

```cpp
std::string toString() const;
```

### API-38e30be0b243 · ndn_service_framework::EncryptedPermissionResponse::Clear

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L748)

```cpp
void Clear() override;
```

### API-b826fd5b6404 · ndn_service_framework::EncryptedPermissionResponse::WireEncode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L749)

```cpp
ndn::Block WireEncode() const override;
```

### API-2d2d04537b21 · ndn_service_framework::EncryptedPermissionResponse::WireDecode

public / declared-interface；[源码](../../ndn-service-framework/NDNSFMessages.hpp#L750)

```cpp
bool WireDecode(const ndn::Block& block) override;
```

## ndn-service-framework/NDNSFThreadPool.hpp

源码 SHA-256：`825dc44d7ed7e91a00599c785752790f0b3b606c99d43c0e28dde6c644a752f5`。

### API-dcd16298de1a · ndnsf_detail::IoCtx

public / declared-interface；[源码](../../ndn-service-framework/NDNSFThreadPool.hpp#L29)

```cpp
using IoCtx     = boost::asio::io_context;
```

### API-0bcf294f37b8 · ndnsf_detail::WorkGuard

public / declared-interface；[源码](../../ndn-service-framework/NDNSFThreadPool.hpp#L30)

```cpp
using WorkGuard = boost::asio::executor_work_guard<IoCtx::executor_type>;
```

### API-78c23bbf9d00 · ndnsf_detail::IoCtx

public / declared-interface；[源码](../../ndn-service-framework/NDNSFThreadPool.hpp#L32)

```cpp
using IoCtx     = boost::asio::io_service;
```

### API-25a2fc4cdbfa · ndnsf_detail::Work

public / declared-interface；[源码](../../ndn-service-framework/NDNSFThreadPool.hpp#L34)

```cpp
using Work      = IoCtx::work;
```

原始接口说明：

```text
// 旧版使用 io_service::work 保持 run() 不退出
```

### API-c136ee2712b8 · ndnsf::ThreadPool

public / declared-interface；[源码](../../ndn-service-framework/NDNSFThreadPool.hpp#L48)

```cpp
class ThreadPool
```

### API-16e5a94d9bf9 · ndnsf::ThreadPool::instance

public / declared-interface；[源码](../../ndn-service-framework/NDNSFThreadPool.hpp#L51)

```cpp
static ThreadPool& instance()
```

### API-d35fc02f57e1 · ndnsf::ThreadPool::start

public / declared-interface；[源码](../../ndn-service-framework/NDNSFThreadPool.hpp#L58)

```cpp
void start(std::size_t threadCount = std::thread::hardware_concurrency())
  //void start(std::size_t threadCount = 1)
```

原始接口说明：

```text
// 幂等：可安全多次调用；只在第一次真正启动线程池
```

### API-a461155d9967 · ndnsf::ThreadPool::post

public / declared-interface；[源码](../../ndn-service-framework/NDNSFThreadPool.hpp#L83)

```cpp
template<typename F>
void post(F&& f)
```

### API-6bc18fe725d5 · ndnsf::ThreadPool::context

public / declared-interface；[源码](../../ndn-service-framework/NDNSFThreadPool.hpp#L97)

```cpp
ndnsf_detail::IoCtx& context()
```

原始接口说明：

```text
// 如需直接访问 io_context（例如定时器等），可以用这个
```

### API-3ec8188cfd04 · ndnsf::NdnsfThreadPool

public / declared-interface；[源码](../../ndn-service-framework/NDNSFThreadPool.hpp#L126)

```cpp
using NdnsfThreadPool = ThreadPool;
```

原始接口说明：

```text
// 方便使用的别名：和之前回答的类名保持兼容
```

### API-d956c992496d · ndnsf::post

public / declared-interface；[源码](../../ndn-service-framework/NDNSFThreadPool.hpp#L132)

```cpp
template<typename F>
inline void post(F&& f)
```

## ndn-service-framework/NegativeAckReason.hpp

源码 SHA-256：`eaefab4bbfb20ca2484bdf0b2f028af532207614006521a9d104d86eab0b952c`。

### API-684e749cb101 · ndn_service_framework::negative_ack_reason::* QueueFull = "QUEUE_FULL"

public / declared-interface；[源码](../../ndn-service-framework/NegativeAckReason.hpp#L9)

```cpp
inline constexpr const char* QueueFull = "QUEUE_FULL";
```

### API-28424b784abc · ndn_service_framework::negative_ack_reason::* ProviderBusy = "PROVIDER_BUSY"

public / declared-interface；[源码](../../ndn-service-framework/NegativeAckReason.hpp#L10)

```cpp
inline constexpr const char* ProviderBusy = "PROVIDER_BUSY";
```

### API-871f23ccff3a · ndn_service_framework::negative_ack_reason::* GpuBusy = "GPU_BUSY"

public / declared-interface；[源码](../../ndn-service-framework/NegativeAckReason.hpp#L11)

```cpp
inline constexpr const char* GpuBusy = "GPU_BUSY";
```

### API-d2b1f3b0a3e8 · ndn_service_framework::negative_ack_reason::* ModelUnavailable = "MODEL_UNAVAILABLE"

public / declared-interface；[源码](../../ndn-service-framework/NegativeAckReason.hpp#L12)

```cpp
inline constexpr const char* ModelUnavailable = "MODEL_UNAVAILABLE";
```

### API-3818c3ba89d7 · ndn_service_framework::negative_ack_reason::* PermissionDenied = "PERMISSION_DENIED"

public / declared-interface；[源码](../../ndn-service-framework/NegativeAckReason.hpp#L13)

```cpp
inline constexpr const char* PermissionDenied = "PERMISSION_DENIED";
```

### API-33cf40d12ebb · ndn_service_framework::negative_ack_reason::* UnsupportedRequest = "UNSUPPORTED_REQUEST"

public / declared-interface；[源码](../../ndn-service-framework/NegativeAckReason.hpp#L14)

```cpp
inline constexpr const char* UnsupportedRequest = "UNSUPPORTED_REQUEST";
```

### API-491c1345500e · ndn_service_framework::negative_ack_reason::* InternalError = "INTERNAL_ERROR"

public / declared-interface；[源码](../../ndn-service-framework/NegativeAckReason.hpp#L15)

```cpp
inline constexpr const char* InternalError = "INTERNAL_ERROR";
```

### API-9708f2328e1d · ndn_service_framework::negative_ack_reason::* LeaseRejected = "LEASE_REJECTED"

public / declared-interface；[源码](../../ndn-service-framework/NegativeAckReason.hpp#L16)

```cpp
inline constexpr const char* LeaseRejected = "LEASE_REJECTED";
```

### API-a9f56d5857a4 · ndn_service_framework::negative_ack_reason::* LeaseExpired = "LEASE_EXPIRED"

public / declared-interface；[源码](../../ndn-service-framework/NegativeAckReason.hpp#L17)

```cpp
inline constexpr const char* LeaseExpired = "LEASE_EXPIRED";
```

### API-287bee0c082d · ndn_service_framework::negative_ack_reason::* OperationExpired = "OPERATION_EXPIRED"

public / declared-interface；[源码](../../ndn-service-framework/NegativeAckReason.hpp#L18)

```cpp
inline constexpr const char* OperationExpired = "OPERATION_EXPIRED";
```

### API-d717c33396e5 · ndn_service_framework::negative_ack_reason::isRecommended

public / declared-interface；[源码](../../ndn-service-framework/NegativeAckReason.hpp#L20)

```cpp
inline bool
isRecommended(const std::string& reason)
```

## ndn-service-framework/NetworkTelemetry.hpp

源码 SHA-256：`dcd17c622c3708d2c5f794a432bcecc8b71aa8528b02003f050416413c9470cc`。

### API-559b2e6eeac6 · ndn_service_framework::NetworkTelemetrySampleKind

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L14)

```cpp
enum class NetworkTelemetrySampleKind
```

### API-f487b4d91af8 · ndn_service_framework::NetworkTelemetrySampleKind::AckRtt

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L16)

```cpp
AckRtt
```

### API-29d9dbc59409 · ndn_service_framework::NetworkTelemetrySampleKind::ResponseRtt

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L17)

```cpp
ResponseRtt
```

### API-0ca698ef4222 · ndn_service_framework::NetworkTelemetrySampleKind::LargeDataFetch

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L18)

```cpp
LargeDataFetch
```

### API-2dfc3844d25b · ndn_service_framework::NetworkTelemetrySampleKind::ActiveProbe

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L19)

```cpp
ActiveProbe
```

### API-9a3599dc105a · ndn_service_framework::NetworkTelemetrySample

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L22)

```cpp
struct NetworkTelemetrySample
```

### API-12696de59418 · ndn_service_framework::NetworkTelemetrySample::kind

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L24)

```cpp
NetworkTelemetrySampleKind kind = NetworkTelemetrySampleKind::AckRtt;
```

### API-ed26b22325ee · ndn_service_framework::NetworkTelemetrySample::rttMs

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L25)

```cpp
double rttMs = 0.0;
```

### API-cbc25719f598 · ndn_service_framework::NetworkTelemetrySample::firstByteMs

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L26)

```cpp
double firstByteMs = 0.0;
```

### API-8d8289026fc3 · ndn_service_framework::NetworkTelemetrySample::elapsedMs

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L27)

```cpp
double elapsedMs = 0.0;
```

### API-0fc0e5f31c52 · ndn_service_framework::NetworkTelemetrySample::encodedBytes

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L28)

```cpp
size_t encodedBytes = 0;
```

### API-315ea8d51c8d · ndn_service_framework::NetworkTelemetrySample::wireBytes

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L29)

```cpp
size_t wireBytes = 0;
```

### API-7f18794c122e · ndn_service_framework::NetworkTelemetrySample::receivedSegments

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L30)

```cpp
size_t receivedSegments = 0;
```

### API-6df6983d683f · ndn_service_framework::NetworkTelemetrySample::timeoutCount

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L31)

```cpp
size_t timeoutCount = 0;
```

### API-8ac1f41cf3a7 · ndn_service_framework::NetworkTelemetrySample::nackCount

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L32)

```cpp
size_t nackCount = 0;
```

### API-c632d2947827 · ndn_service_framework::NetworkTelemetrySample::dataName

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L33)

```cpp
ndn::Name dataName;
```

### API-edc26cab5da2 · ndn_service_framework::NetworkTelemetrySnapshot

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L36)

```cpp
struct NetworkTelemetrySnapshot
```

### API-6d3e18369c83 · ndn_service_framework::NetworkTelemetrySnapshot::providerName

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L38)

```cpp
ndn::Name providerName;
```

### API-0f4eac23070c · ndn_service_framework::NetworkTelemetrySnapshot::serviceName

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L39)

```cpp
ndn::Name serviceName;
```

### API-9d2c11056adc · ndn_service_framework::NetworkTelemetrySnapshot::peerName

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L40)

```cpp
ndn::Name peerName;
```

### API-361ccaa0a580 · ndn_service_framework::NetworkTelemetrySnapshot::edgeName

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L41)

```cpp
std::string edgeName;
```

### API-89356c2915a3 · ndn_service_framework::NetworkTelemetrySnapshot::kind

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L42)

```cpp
NetworkTelemetrySampleKind kind = NetworkTelemetrySampleKind::AckRtt;
```

### API-e5d26c01200d · ndn_service_framework::NetworkTelemetrySnapshot::rttMs

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L43)

```cpp
double rttMs = 0.0;
```

### API-e24d9191febd · ndn_service_framework::NetworkTelemetrySnapshot::firstByteMs

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L44)

```cpp
double firstByteMs = 0.0;
```

### API-853b2cf63eb7 · ndn_service_framework::NetworkTelemetrySnapshot::elapsedMs

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L45)

```cpp
double elapsedMs = 0.0;
```

### API-4b313f259e08 · ndn_service_framework::NetworkTelemetrySnapshot::encodedBytes

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L46)

```cpp
size_t encodedBytes = 0;
```

### API-04528cd0096c · ndn_service_framework::NetworkTelemetrySnapshot::wireBytes

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L47)

```cpp
size_t wireBytes = 0;
```

### API-76e51835a9e0 · ndn_service_framework::NetworkTelemetrySnapshot::goodputMbps

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L48)

```cpp
double goodputMbps = 0.0;
```

### API-1fda4af7c63e · ndn_service_framework::NetworkTelemetrySnapshot::receivedSegments

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L49)

```cpp
size_t receivedSegments = 0;
```

### API-1f783b9a9db6 · ndn_service_framework::NetworkTelemetrySnapshot::timeoutCount

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L50)

```cpp
size_t timeoutCount = 0;
```

### API-3de43eb21c06 · ndn_service_framework::NetworkTelemetrySnapshot::nackCount

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L51)

```cpp
size_t nackCount = 0;
```

### API-75b127ca3cf9 · ndn_service_framework::NetworkTelemetrySnapshot::sampleCount

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L52)

```cpp
size_t sampleCount = 0;
```

### API-7e7e862d3fee · ndn_service_framework::NetworkTelemetrySnapshot::lastUpdatedMs

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L53)

```cpp
uint64_t lastUpdatedMs = 0;
```

### API-62627f44c8ad · ndn_service_framework::NetworkTelemetrySnapshot::confidence

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L54)

```cpp
double confidence = 0.0;
```

### API-c79505854de3 · ndn_service_framework::NetworkTelemetrySnapshot::stale

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L55)

```cpp
bool stale = false;
```

### API-a681d3a058d6 · ndn_service_framework::NetworkTelemetrySnapshot::dataName

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L56)

```cpp
ndn::Name dataName;
```

### API-9454709b1bc5 · ndn_service_framework::NetworkTelemetryKey

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L59)

```cpp
struct NetworkTelemetryKey
```

### API-2509d3918d79 · ndn_service_framework::NetworkTelemetryKey::providerName

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L61)

```cpp
ndn::Name providerName;
```

### API-7d5e04700a67 · ndn_service_framework::NetworkTelemetryKey::serviceName

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L62)

```cpp
ndn::Name serviceName;
```

### API-05cde736bcd7 · ndn_service_framework::NetworkTelemetryKey::peerName

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L63)

```cpp
ndn::Name peerName;
```

### API-c4c6ec2b3e83 · ndn_service_framework::NetworkTelemetryKey::edgeName

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L64)

```cpp
std::string edgeName;
```

### API-50cc64960819 · ndn_service_framework::NetworkTelemetryKey::operator<

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L66)

```cpp
bool operator<(const NetworkTelemetryKey& other) const;
```

### API-0e9083768a54 · ndn_service_framework::networkTelemetryGoodputMbps

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L69)

```cpp
double networkTelemetryGoodputMbps(size_t wireBytes, double elapsedMs);
```

### API-473854250547 · ndn_service_framework::toString

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L70)

```cpp
const char* toString(NetworkTelemetrySampleKind kind);
```

### API-4f1dc249ae70 · ndn_service_framework::NetworkTelemetryStore

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L72)

```cpp
class NetworkTelemetryStore
```

### API-551cc8765ef7 · ndn_service_framework::NetworkTelemetryStore::NetworkTelemetryStore

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L75)

```cpp
explicit NetworkTelemetryStore(
    double alpha = 0.25,
    std::chrono::milliseconds ttl = std::chrono::seconds(30));
```

### API-27265f94f021 · ndn_service_framework::NetworkTelemetryStore::update

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L79)

```cpp
void update(const NetworkTelemetryKey& key,
              const NetworkTelemetrySample& sample);
```

### API-50b5e7f84296 · ndn_service_framework::NetworkTelemetryStore::get

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L82)

```cpp
std::optional<NetworkTelemetrySnapshot>
  get(const NetworkTelemetryKey& key) const;
```

### API-09aff515630e · ndn_service_framework::NetworkTelemetryStore::updateAckRtt

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L85)

```cpp
void updateAckRtt(const ndn::Name& providerName,
                    const ndn::Name& serviceName,
                    double rttMs);
```

### API-1207c0be0f2c · ndn_service_framework::NetworkTelemetryStore::updateLargeDataFetch

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L89)

```cpp
void updateLargeDataFetch(const ndn::Name& consumerProvider,
                            const ndn::Name& producerProvider,
                            const std::string& keyScope,
                            const ndn::Name& dataName,
                            double elapsedMs,
                            double firstByteMs,
                            size_t encodedBytes,
                            size_t wireBytes,
                            size_t receivedSegments,
                            size_t timeoutCount,
                            size_t nackCount);
```

### API-9ee9692a9c82 · ndn_service_framework::NetworkTelemetryStore::getServicePath

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L101)

```cpp
std::optional<NetworkTelemetrySnapshot>
  getServicePath(const ndn::Name& providerName,
                 const ndn::Name& serviceName) const;
```

### API-3361481c4437 · ndn_service_framework::NetworkTelemetryStore::getDependencyEdge

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L105)

```cpp
std::optional<NetworkTelemetrySnapshot>
  getDependencyEdge(const ndn::Name& consumerProvider,
                    const ndn::Name& producerProvider,
                    const std::string& keyScope) const;
```

### API-f39d067a737a · ndn_service_framework::NetworkTelemetryStore::size

public / declared-interface；[源码](../../ndn-service-framework/NetworkTelemetry.hpp#L110)

```cpp
size_t size() const;
```

## ndn-service-framework/PolicyParser.hpp

源码 SHA-256：`eff6d3972b43dea4b9a85fb15325f13cf5f33da2f7559f785fd5f3889dfaa74d`。

### API-66900b98640c · ndn_service_framework::tlv::(anonymous)

public / declared-interface；[源码](../../ndn-service-framework/PolicyParser.hpp#L14)

```cpp
enum
```

### API-a3cd36ea45a8 · ndn_service_framework::tlv::(anonymous)::ServicePolicyType

public / declared-interface；[源码](../../ndn-service-framework/PolicyParser.hpp#L15)

```cpp
ServicePolicyType = 170
```

### API-74f88ab3caf0 · ndn_service_framework::tlv::(anonymous)::ForType

public / declared-interface；[源码](../../ndn-service-framework/PolicyParser.hpp#L16)

```cpp
ForType = 171
```

### API-b1dd9ee16a6d · ndn_service_framework::tlv::(anonymous)::AllowedUsersType

public / declared-interface；[源码](../../ndn-service-framework/PolicyParser.hpp#L17)

```cpp
AllowedUsersType = 173
```

### API-7eca48b769d7 · ndn_service_framework::tlv::(anonymous)::DeniedUsersType

public / declared-interface；[源码](../../ndn-service-framework/PolicyParser.hpp#L18)

```cpp
DeniedUsersType = 174
```

### API-36e3d0b1b282 · ndn_service_framework::ProviderPolicy

public / declared-interface；[源码](../../ndn-service-framework/PolicyParser.hpp#L24)

```cpp
struct ProviderPolicy
```

### API-86012aa7a8c1 · ndn_service_framework::ProviderPolicy::providerName

public / declared-interface；[源码](../../ndn-service-framework/PolicyParser.hpp#L25)

```cpp
std::string providerName;
```

### API-c08d5d18649a · ndn_service_framework::ProviderPolicy::allowedServices

public / declared-interface；[源码](../../ndn-service-framework/PolicyParser.hpp#L26)

```cpp
std::vector<std::string> allowedServices;
```

原始接口说明：

```text
// 改为以 provider 为中心
```

### API-2e4daf4de516 · ndn_service_framework::UserPolicy

public / declared-interface；[源码](../../ndn-service-framework/PolicyParser.hpp#L29)

```cpp
struct UserPolicy
```

### API-e37416ab611e · ndn_service_framework::UserPolicy::userName

public / declared-interface；[源码](../../ndn-service-framework/PolicyParser.hpp#L30)

```cpp
std::string userName;
```

### API-7c95497f5db7 · ndn_service_framework::UserPolicy::allowedServices

public / declared-interface；[源码](../../ndn-service-framework/PolicyParser.hpp#L31)

```cpp
std::vector<std::string> allowedServices;
```

### API-87f8222ff57c · ndn_service_framework::PolicyParser

public / declared-interface；[源码](../../ndn-service-framework/PolicyParser.hpp#L34)

```cpp
class PolicyParser
```

### API-ec1fb9fc71c7 · ndn_service_framework::PolicyParser::PolicyParser

public / declared-interface；[源码](../../ndn-service-framework/PolicyParser.hpp#L36)

```cpp
PolicyParser() = default;
```

### API-f825cf3ad44c · ndn_service_framework::PolicyParser::parsePolicyFile

public / declared-interface；[源码](../../ndn-service-framework/PolicyParser.hpp#L37)

```cpp
std::pair<std::vector<ProviderPolicy>, std::vector<UserPolicy>> parsePolicyFile(const std::string& filename);
```

## ndn-service-framework/PolicyRefreshCoordinator.hpp

源码 SHA-256：`8a1f7661efc03b4ac94fc6e1cde2e5f1c3d4fc2407d9d56c1c91ef1005dc3a6e`。

### API-94ed7914dcca · ndn_service_framework::PolicyRefreshCoordinator

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L20)

```cpp
class PolicyRefreshCoordinator
```

### API-6406fdf17d5f · ndn_service_framework::PolicyRefreshCoordinator::Outcome

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L23)

```cpp
enum class Outcome
```

### API-cbbb7854a731 · ndn_service_framework::PolicyRefreshCoordinator::Outcome::REJECTED_UNAUTHENTICATED_HINT

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L25)

```cpp
REJECTED_UNAUTHENTICATED_HINT
```

### API-bf8aa2df5667 · ndn_service_framework::PolicyRefreshCoordinator::Outcome::IGNORED_CURRENT

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L26)

```cpp
IGNORED_CURRENT
```

### API-a61294871ea3 · ndn_service_framework::PolicyRefreshCoordinator::Outcome::FETCH_STARTED

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L27)

```cpp
FETCH_STARTED
```

### API-6e09455a67a7 · ndn_service_framework::PolicyRefreshCoordinator::Outcome::FETCH_COALESCED

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L28)

```cpp
FETCH_COALESCED
```

### API-0d21d7dafa94 · ndn_service_framework::PolicyRefreshCoordinator::Outcome::ACCEPTED

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L29)

```cpp
ACCEPTED
```

### API-dc37ba71677b · ndn_service_framework::PolicyRefreshCoordinator::Outcome::REJECTED_STATUS

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L30)

```cpp
REJECTED_STATUS
```

### API-e8f145380cb7 · ndn_service_framework::PolicyRefreshCoordinator::Outcome::RETRY_SCHEDULED

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L31)

```cpp
RETRY_SCHEDULED
```

### API-8515dc9fa0da · ndn_service_framework::PolicyRefreshCoordinator::Outcome::EXHAUSTED

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L32)

```cpp
EXHAUSTED
```

### API-3830e0508260 · ndn_service_framework::PolicyRefreshCoordinator::FetchRequest

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L35)

```cpp
struct FetchRequest
```

### API-c7ca6c0d80ab · ndn_service_framework::PolicyRefreshCoordinator::FetchRequest::version

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L37)

```cpp
ControllerVersion version;
```

### API-f02c2d31e7ea · ndn_service_framework::PolicyRefreshCoordinator::FetchRequest::attempt

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L38)

```cpp
size_t attempt = 0;
```

### API-a13a99305427 · ndn_service_framework::PolicyRefreshCoordinator::Result

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L41)

```cpp
struct Result
```

### API-80002267feb6 · ndn_service_framework::PolicyRefreshCoordinator::Result::outcome

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L43)

```cpp
Outcome outcome = Outcome::IGNORED_CURRENT;
```

### API-67d656d52d02 · ndn_service_framework::PolicyRefreshCoordinator::Result::request

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L44)

```cpp
std::optional<FetchRequest> request;
```

### API-6de27d89a077 · ndn_service_framework::PolicyRefreshCoordinator::Result::* reason

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L45)

```cpp
const char* reason = "";
```

### API-e0ff7facc0f4 · ndn_service_framework::PolicyRefreshCoordinator::PolicyRefreshCoordinator

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L48)

```cpp
explicit PolicyRefreshCoordinator(ndn::Name serviceName,
                                    size_t maxAttempts = 3,
                                    uint64_t retryBackoffMs = 1000);
```

### API-95fcd8336698 · ndn_service_framework::PolicyRefreshCoordinator::installCurrentStatus

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L53)

```cpp
bool installCurrentStatus(const PolicyStatusData& status,
                            uint64_t nowMs,
                            bool controllerSignatureValid = true);
```

原始接口说明：

```text
/** Install the first/current signed status before processing hints. */
```

### API-dc218ab08b42 · ndn_service_framework::PolicyRefreshCoordinator::observeHint

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L58)

```cpp
Result observeHint(const ControllerVersion& hintedVersion,
                     bool authenticated,
                     uint64_t nowMs);
```

原始接口说明：

```text
/** Process a version hint only after authenticating the enclosing message. */
```

### API-5012de218e3f · ndn_service_framework::PolicyRefreshCoordinator::startScheduledRefresh

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L63)

```cpp
Result startScheduledRefresh(uint64_t nowMs, uint64_t leadMs);
```

原始接口说明：

```text
/** Start a hintless refresh when the accepted status is near expiry. */
```

### API-7848cf69fea2 · ndn_service_framework::PolicyRefreshCoordinator::completeFetch

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L66)

```cpp
Result completeFetch(const PolicyStatusData& status,
                       uint64_t nowMs,
                       bool controllerSignatureValid = true);
```

原始接口说明：

```text
/** Complete the current fetch with a Controller-signed status snapshot. */
```

### API-7f222bebc382 · ndn_service_framework::PolicyRefreshCoordinator::failFetch

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L71)

```cpp
Result failFetch(uint64_t nowMs);
```

原始接口说明：

```text
/** Record a timeout/transport failure and return a bounded retry request. */
```

### API-87c10b372ca8 · ndn_service_framework::PolicyRefreshCoordinator::retryIfDue

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L74)

```cpp
std::optional<FetchRequest> retryIfDue(uint64_t nowMs) const;
```

原始接口说明：

```text
/** Return the retry request once its bounded backoff has elapsed. */
```

### API-f9f0d9e10c66 · ndn_service_framework::PolicyRefreshCoordinator::hasCurrentStatus

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L76)

```cpp
bool hasCurrentStatus() const;
```

### API-33cb23ecaf15 · ndn_service_framework::PolicyRefreshCoordinator::currentVersion

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L77)

```cpp
const ControllerVersion& currentVersion() const;
```

### API-d679cc4dcb76 · ndn_service_framework::PolicyRefreshCoordinator::currentExpiryMs

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L78)

```cpp
uint64_t currentExpiryMs() const;
```

### API-ca96d5ccacff · ndn_service_framework::PolicyRefreshCoordinator::hasExpiredStatus

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L79)

```cpp
bool hasExpiredStatus(uint64_t nowMs) const;
```

### API-e383e0a61859 · ndn_service_framework::PolicyRefreshCoordinator::inFlight

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L80)

```cpp
bool inFlight() const;
```

### API-2d0c82fc0f8c · ndn_service_framework::PolicyRefreshCoordinator::attempt

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L81)

```cpp
size_t attempt() const;
```

### API-afc4caed3101 · ndn_service_framework::PolicyRefreshCoordinator::maxAttempts

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L82)

```cpp
size_t maxAttempts() const;
```

### API-41ad6693a2f5 · ndn_service_framework::PolicyRefreshCoordinator::highestCandidate

public / declared-interface；[源码](../../ndn-service-framework/PolicyRefreshCoordinator.hpp#L83)

```cpp
const std::optional<ControllerVersion>& highestCandidate() const;
```

## ndn-service-framework/PolicyStatus.hpp

源码 SHA-256：`78cdf89328a964d419df58903803e8358f34db631038a0ed080693e55faf09bf`。

### API-235f737d65a1 · ndn_service_framework::sameCertificateDigest

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L16)

```cpp
bool sameCertificateDigest(const std::string& left, const std::string& right);
```

原始接口说明：

```text
/** Compare certificate identities without rewriting signed or persisted status.
 * Hexadecimal case is insignificant only for complete SHA-256 digests; legacy
 * opaque identifiers retain their existing exact-match semantics.
 */
```

### API-e80e43695977 · ndn_service_framework::RevocationKind

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L18)

```cpp
enum class RevocationKind : uint8_t
```

### API-57388597148d · ndn_service_framework::RevocationKind::IDENTITY

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L20)

```cpp
IDENTITY = 1
```

### API-fdd492b16853 · ndn_service_framework::RevocationKind::CERTIFICATE

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L21)

```cpp
CERTIFICATE = 2
```

### API-2983e1329d46 · ndn_service_framework::RevocationKind::SERVICE_AUTHORIZATION

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L22)

```cpp
SERVICE_AUTHORIZATION = 3
```

### API-1520bab14a52 · ndn_service_framework::RevocationTarget

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L25)

```cpp
struct RevocationTarget
```

### API-fe92e5e3451f · ndn_service_framework::RevocationTarget::kind

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L27)

```cpp
RevocationKind kind = RevocationKind::IDENTITY;
```

### API-5996617f7ecc · ndn_service_framework::RevocationTarget::targetIdentity

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L28)

```cpp
ndn::Name targetIdentity;
```

### API-f452a34f6242 · ndn_service_framework::RevocationTarget::serviceName

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L29)

```cpp
ndn::Name serviceName;
```

### API-3785a9399d5c · ndn_service_framework::RevocationTarget::certificateDigest

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L30)

```cpp
std::string certificateDigest;
```

### API-5c04407eb7da · ndn_service_framework::RevocationTarget::authorizationAttribute

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L33)

```cpp
ndn::Name authorizationAttribute;
```

原始接口说明：

```text
// Exact NAC-ABE authorization attribute for service-scoped targets:
// /PERMISSION/<service> (User use) or /SERVICE/<service> (Provider provision).
```

### API-0d248ba17187 · ndn_service_framework::RevocationTarget::isValid

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L35)

```cpp
bool isValid() const;
```

### API-ec69843c1d2e · ndn_service_framework::RevocationTarget::wireEncode

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L36)

```cpp
ndn::Block wireEncode() const;
```

### API-e75a3fa9bc79 · ndn_service_framework::RevocationTarget::wireDecode

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L37)

```cpp
bool wireDecode(const ndn::Block& block);
```

### API-91c62a14c671 · ndn_service_framework::PolicyStatusData

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L45)

```cpp
class PolicyStatusData
```

### API-840bc2312b0b · ndn_service_framework::PolicyStatusData::TYPE

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L48)

```cpp
static constexpr uint32_t TYPE = 0xF710;
```

### API-11e0477d0729 · ndn_service_framework::PolicyStatusData::ServiceNameType

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L49)

```cpp
static constexpr uint32_t ServiceNameType = 0xF711;
```

### API-1aa9b3716211 · ndn_service_framework::PolicyStatusData::ControllerVersionType

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L50)

```cpp
static constexpr uint32_t ControllerVersionType = 0xF712;
```

### API-50e898ff6fc2 · ndn_service_framework::PolicyStatusData::ValidFromType

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L51)

```cpp
static constexpr uint32_t ValidFromType = 0xF713;
```

### API-d57ae7d8c9ee · ndn_service_framework::PolicyStatusData::ValidUntilType

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L52)

```cpp
static constexpr uint32_t ValidUntilType = 0xF714;
```

### API-c77f2efd235c · ndn_service_framework::PolicyStatusData::PolicyDigestType

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L53)

```cpp
static constexpr uint32_t PolicyDigestType = 0xF715;
```

### API-3bbae0f1bf3b · ndn_service_framework::PolicyStatusData::AbePublicParametersNameType

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L54)

```cpp
static constexpr uint32_t AbePublicParametersNameType = 0xF71E;
```

### API-695dbacfda5f · ndn_service_framework::PolicyStatusData::AbePublicParametersDigestType

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L55)

```cpp
static constexpr uint32_t AbePublicParametersDigestType = 0xF71F;
```

### API-16ab59b08669 · ndn_service_framework::PolicyStatusData::ControllerCertificateType

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L56)

```cpp
static constexpr uint32_t ControllerCertificateType = 0xF716;
```

### API-370ee946d98f · ndn_service_framework::PolicyStatusData::SignatureType

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L57)

```cpp
static constexpr uint32_t SignatureType = 0xF717;
```

### API-51bee883881a · ndn_service_framework::PolicyStatusData::RevocationTargetType

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L58)

```cpp
static constexpr uint32_t RevocationTargetType = 0xF718;
```

### API-bbac6bee7fdd · ndn_service_framework::PolicyStatusData::setServiceName

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L60)

```cpp
void setServiceName(const ndn::Name& serviceName);
```

### API-3cea56dfe554 · ndn_service_framework::PolicyStatusData::getServiceName

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L61)

```cpp
const ndn::Name& getServiceName() const;
```

### API-f8808af79ff4 · ndn_service_framework::PolicyStatusData::setControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L62)

```cpp
void setControllerVersion(const ControllerVersion& version);
```

### API-c5d885840778 · ndn_service_framework::PolicyStatusData::getControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L63)

```cpp
const ControllerVersion& getControllerVersion() const;
```

### API-1b4c8c258941 · ndn_service_framework::PolicyStatusData::setValidity

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L64)

```cpp
void setValidity(uint64_t validFromMs, uint64_t validUntilMs);
```

### API-9dd1eaa0106b · ndn_service_framework::PolicyStatusData::getValidFromMs

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L65)

```cpp
uint64_t getValidFromMs() const;
```

### API-72cb3164a0f7 · ndn_service_framework::PolicyStatusData::getValidUntilMs

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L66)

```cpp
uint64_t getValidUntilMs() const;
```

### API-011a9ae62018 · ndn_service_framework::PolicyStatusData::setPolicyDigest

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L67)

```cpp
void setPolicyDigest(const std::string& digest);
```

### API-387a03650db3 · ndn_service_framework::PolicyStatusData::getPolicyDigest

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L68)

```cpp
const std::string& getPolicyDigest() const;
```

### API-74a3792253ad · ndn_service_framework::PolicyStatusData::setAbePublicParametersName

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L70)

```cpp
void setAbePublicParametersName(const ndn::Name& name);
```

原始接口说明：

```text
/** Exact immutable NAC-ABE public-parameter Data name for this status. */
```

### API-afcd52c6efe5 · ndn_service_framework::PolicyStatusData::getAbePublicParametersName

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L71)

```cpp
const ndn::Name& getAbePublicParametersName() const;
```

### API-c924276706b4 · ndn_service_framework::PolicyStatusData::setAbePublicParametersDigest

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L73)

```cpp
void setAbePublicParametersDigest(const std::string& digest);
```

原始接口说明：

```text
/** SHA-256 digest of the canonical public-parameter Data Content. */
```

### API-1187205cd6ae · ndn_service_framework::PolicyStatusData::getAbePublicParametersDigest

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L74)

```cpp
const std::string& getAbePublicParametersDigest() const;
```

### API-02d511e705de · ndn_service_framework::PolicyStatusData::setControllerCertificate

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L75)

```cpp
void setControllerCertificate(const ndn::Name& certificate);
```

### API-82f780c8cff2 · ndn_service_framework::PolicyStatusData::getControllerCertificate

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L76)

```cpp
const ndn::Name& getControllerCertificate() const;
```

### API-5159f63ef800 · ndn_service_framework::PolicyStatusData::setSignature

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L77)

```cpp
void setSignature(const ndn::Buffer& signature);
```

### API-ef5ab2dc37f8 · ndn_service_framework::PolicyStatusData::getSignature

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L78)

```cpp
const ndn::Buffer& getSignature() const;
```

### API-e0255a70c006 · ndn_service_framework::PolicyStatusData::addRevocation

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L79)

```cpp
void addRevocation(const RevocationTarget& target);
```

### API-619cc44bb4b4 · ndn_service_framework::PolicyStatusData::getRevocations

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L80)

```cpp
const std::vector<RevocationTarget>& getRevocations() const;
```

### API-e3e2a037ed06 · ndn_service_framework::PolicyStatusData::validate

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L82)

```cpp
bool validate(uint64_t nowMs) const;
```

### API-9c4e64725e1f · ndn_service_framework::PolicyStatusData::wireEncode

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L83)

```cpp
ndn::Block wireEncode() const;
```

### API-7faecffcb8dc · ndn_service_framework::PolicyStatusData::wireDecode

public / declared-interface；[源码](../../ndn-service-framework/PolicyStatus.hpp#L84)

```cpp
bool wireDecode(const ndn::Block& block);
```

## ndn-service-framework/RequestConfidentiality.hpp

源码 SHA-256：`cd37d9dd5baa68679a7e9710f5fcce7dc5351332743513e04d98f67414092d16`。

### API-24e8e831d173 · ndn_service_framework::RequestSecurityBinding

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L18)

```cpp
struct RequestSecurityBinding
```

### API-bb224d99ba36 · ndn_service_framework::RequestSecurityBinding::TYPE

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L20)

```cpp
static constexpr uint32_t TYPE = 0xF810;
```

### API-5d02e6fe64ca · ndn_service_framework::RequestSecurityBinding::serviceName

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L22)

```cpp
ndn::Name serviceName;
```

### API-11f4e47b5d34 · ndn_service_framework::RequestSecurityBinding::requestId

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L23)

```cpp
ndn::Name requestId;
```

### API-fe921a6efdd6 · ndn_service_framework::RequestSecurityBinding::attempt

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L24)

```cpp
uint64_t attempt = 0;
```

### API-fd5c9fb261c0 · ndn_service_framework::RequestSecurityBinding::controllerVersion

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L25)

```cpp
ControllerVersion controllerVersion;
```

### API-16607e53465d · ndn_service_framework::RequestSecurityBinding::userEncryptionCertName

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L26)

```cpp
ndn::Name userEncryptionCertName;
```

### API-e9d50e263ee7 · ndn_service_framework::RequestSecurityBinding::userEncryptionCertDigest

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L27)

```cpp
std::string userEncryptionCertDigest;
```

### API-e0bd2bd17637 · ndn_service_framework::RequestSecurityBinding::providerEncryptionCertName

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L28)

```cpp
ndn::Name providerEncryptionCertName;
```

### API-094c02b9c475 · ndn_service_framework::RequestSecurityBinding::providerEncryptionCertDigest

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L29)

```cpp
std::string providerEncryptionCertDigest;
```

### API-e446e717599a · ndn_service_framework::RequestSecurityBinding::selectionDigest

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L30)

```cpp
std::string selectionDigest;
```

### API-7d8d4ee4f4a8 · ndn_service_framework::RequestSecurityBinding::inputDataName

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L31)

```cpp
ndn::Name inputDataName;
```

### API-51121bd1ef57 · ndn_service_framework::RequestSecurityBinding::segmentOrEventId

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L32)

```cpp
std::string segmentOrEventId;
```

### API-0e0c0841b9c0 · ndn_service_framework::RequestSecurityBinding::isValid

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L34)

```cpp
bool isValid() const;
```

### API-5e81519b67f1 · ndn_service_framework::RequestSecurityBinding::wireEncode

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L35)

```cpp
ndn::Block wireEncode() const;
```

### API-ee6d0814c2be · ndn_service_framework::RequestSecurityBinding::wireDecode

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L36)

```cpp
bool wireDecode(const ndn::Block& block);
```

### API-330744b69085 · ndn_service_framework::RequestSecurityBinding::canonicalAad

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L37)

```cpp
ndn::Buffer canonicalAad() const;
```

### API-cfef5beb75b1 · ndn_service_framework::RequestSecurityBinding::digestHex

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L38)

```cpp
std::string digestHex() const;
```

### API-8942451f9644 · ndn_service_framework::RequestKeyBundle

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L42)

```cpp
struct RequestKeyBundle
```

### API-6efd7d071c32 · ndn_service_framework::RequestKeyBundle::inputKey

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L44)

```cpp
ndn::Buffer inputKey;
```

### API-ee766a4b4403 · ndn_service_framework::RequestKeyBundle::responseKey

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L45)

```cpp
ndn::Buffer responseKey;
```

### API-217627cb18c0 · ndn_service_framework::RequestKeyBundle::keyId

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L46)

```cpp
std::string keyId;
```

### API-ced5aba81294 · ndn_service_framework::RequestKeyBundle::createdAtMs

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L47)

```cpp
uint64_t createdAtMs = 0;
```

### API-fff6dee3f10d · ndn_service_framework::RequestKeyBundle::expiresAtMs

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L48)

```cpp
uint64_t expiresAtMs = 0;
```

### API-4f508c4cfb5a · ndn_service_framework::RequestKeyBundle::consumed

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L49)

```cpp
bool consumed = false;
```

### API-4b5608c9b148 · ndn_service_framework::RequestKeyBundle::isValid

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L51)

```cpp
bool isValid(uint64_t nowMs) const;
```

### API-c003e2e98e0d · ndn_service_framework::RequestKeyBundle::zeroize

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L52)

```cpp
void zeroize() noexcept;
```

### API-005f56f9a13d · ndn_service_framework::SelectionKeyEnvelope

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L56)

```cpp
struct SelectionKeyEnvelope
```

### API-29382407e287 · ndn_service_framework::SelectionKeyEnvelope::TYPE

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L58)

```cpp
static constexpr uint32_t TYPE = 0xF830;
```

### API-3078a0894420 · ndn_service_framework::SelectionKeyEnvelope::binding

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L60)

```cpp
RequestSecurityBinding binding;
```

### API-54729947a7ee · ndn_service_framework::SelectionKeyEnvelope::envelopeAlgorithm

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L61)

```cpp
std::string envelopeAlgorithm = "RSA-OAEP-SHA256";
```

### API-ffbe529733ac · ndn_service_framework::SelectionKeyEnvelope::wrappedKeys

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L62)

```cpp
ndn::Buffer wrappedKeys;
```

### API-51af28dfcc7e · ndn_service_framework::SelectionKeyEnvelope::createdAtMs

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L63)

```cpp
uint64_t createdAtMs = 0;
```

### API-ed654f707313 · ndn_service_framework::SelectionKeyEnvelope::expiresAtMs

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L64)

```cpp
uint64_t expiresAtMs = 0;
```

### API-4539aee04998 · ndn_service_framework::SelectionKeyEnvelope::isValid

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L66)

```cpp
bool isValid(uint64_t nowMs) const;
```

### API-295b995c061b · ndn_service_framework::SelectionKeyEnvelope::wireEncode

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L67)

```cpp
ndn::Block wireEncode() const;
```

### API-89ed2ebcb21b · ndn_service_framework::SelectionKeyEnvelope::wireDecode

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L68)

```cpp
bool wireDecode(const ndn::Block& block);
```

### API-a4e800a3c7ed · ndn_service_framework::AeadEnvelope

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L72)

```cpp
struct AeadEnvelope
```

### API-33cc50c981af · ndn_service_framework::AeadEnvelope::TYPE

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L74)

```cpp
static constexpr uint32_t TYPE = 0xF820;
```

### API-935a641668d0 · ndn_service_framework::AeadEnvelope::algorithm

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L76)

```cpp
std::string algorithm = "AES-256-GCM";
```

### API-4dc398cf967d · ndn_service_framework::AeadEnvelope::keyId

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L77)

```cpp
std::string keyId;
```

### API-fecb2f1c7a20 · ndn_service_framework::AeadEnvelope::nonce

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L78)

```cpp
ndn::Buffer nonce;
```

### API-63f05891cc52 · ndn_service_framework::AeadEnvelope::ciphertext

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L79)

```cpp
ndn::Buffer ciphertext;
```

### API-6dbb897faedb · ndn_service_framework::AeadEnvelope::tag

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L80)

```cpp
ndn::Buffer tag;
```

### API-2e6f824ed3cd · ndn_service_framework::AeadEnvelope::aadDigest

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L81)

```cpp
ndn::Buffer aadDigest;
```

### API-4ed92a4c4624 · ndn_service_framework::AeadEnvelope::segmentOrEventId

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L82)

```cpp
std::string segmentOrEventId;
```

### API-831b3e48a6a2 · ndn_service_framework::AeadEnvelope::isValid

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L84)

```cpp
bool isValid() const;
```

### API-17ba65717140 · ndn_service_framework::AeadEnvelope::wireEncode

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L85)

```cpp
ndn::Block wireEncode() const;
```

### API-9239ebe376ab · ndn_service_framework::AeadEnvelope::wireDecode

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L86)

```cpp
bool wireDecode(const ndn::Block& block);
```

### API-75dbe1e71e85 · ndn_service_framework::RequestCryptoFailure

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L89)

```cpp
enum class RequestCryptoFailure
```

### API-c690635ffbfa · ndn_service_framework::RequestCryptoFailure::NONE

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L91)

```cpp
NONE
```

### API-f60b0b632231 · ndn_service_framework::RequestCryptoFailure::INVALID_BINDING

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L92)

```cpp
INVALID_BINDING
```

### API-ecf0d08ef78e · ndn_service_framework::RequestCryptoFailure::INVALID_KEY_BUNDLE

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L93)

```cpp
INVALID_KEY_BUNDLE
```

### API-f3081aa01e5d · ndn_service_framework::RequestCryptoFailure::EXPIRED_KEY

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L94)

```cpp
EXPIRED_KEY
```

### API-ac9886f55b6b · ndn_service_framework::RequestCryptoFailure::WRONG_KEY_ID

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L95)

```cpp
WRONG_KEY_ID
```

### API-d137b14c47cb · ndn_service_framework::RequestCryptoFailure::INVALID_ENVELOPE

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L96)

```cpp
INVALID_ENVELOPE
```

### API-998c536dce04 · ndn_service_framework::RequestCryptoFailure::AUTHENTICATION_FAILED

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L97)

```cpp
AUTHENTICATION_FAILED
```

### API-91b58097a5eb · ndn_service_framework::RequestCryptoFailure::NONCE_REUSE

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L98)

```cpp
NONCE_REUSE
```

### API-2f5f508f9d3b · ndn_service_framework::RequestCryptoFailure::WRONG_RECIPIENT

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L99)

```cpp
WRONG_RECIPIENT
```

### API-5ea714be07b7 · ndn_service_framework::requestCryptoFailureName

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L102)

```cpp
const char* requestCryptoFailureName(RequestCryptoFailure failure) noexcept;
```

### API-c8e2aebdb23d · ndn_service_framework::generateRequestKeyBundle

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L104)

```cpp
RequestKeyBundle generateRequestKeyBundle(uint64_t nowMs, uint64_t expiresAtMs);
```

### API-76d64c0b0538 · ndn_service_framework::wrapSelectionKeyEnvelope

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L106)

```cpp
SelectionKeyEnvelope wrapSelectionKeyEnvelope(
  const RequestKeyBundle& keys,
  const RequestSecurityBinding& binding,
  ndn::span<const uint8_t> recipientPublicKey,
  uint64_t nowMs);
```

### API-b2ca5d8d8b82 · ndn_service_framework::unwrapSelectionKeyEnvelope

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L112)

```cpp
bool unwrapSelectionKeyEnvelope(
  const SelectionKeyEnvelope& envelope,
  const RequestSecurityBinding& expectedBinding,
  const ndn::Name& recipientCertificateName,
  const ndn::security::KeyChain& keyChain,
  uint64_t nowMs,
  RequestKeyBundle& keys,
  RequestCryptoFailure* failure = nullptr);
```

### API-7eb04851572a · ndn_service_framework::encryptRequestContent

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L121)

```cpp
AeadEnvelope encryptRequestContent(
  const ndn::Buffer& key,
  const std::string& keyId,
  const RequestSecurityBinding& binding,
  ndn::span<const uint8_t> plaintext);
```

### API-08e0565ef7e3 · ndn_service_framework::decryptRequestContent

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L127)

```cpp
bool decryptRequestContent(
  const ndn::Buffer& key,
  const std::string& expectedKeyId,
  const RequestSecurityBinding& binding,
  const AeadEnvelope& envelope,
  ndn::Buffer& plaintext,
  RequestCryptoFailure* failure = nullptr);
```

### API-721c346489b8 · ndn_service_framework::NonceRegistry

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L136)

```cpp
class NonceRegistry
```

### API-ada5c3fe71c5 · ndn_service_framework::NonceRegistry::NONCE_SIZE

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L139)

```cpp
static constexpr size_t NONCE_SIZE = HybridMessageCrypto::NONCE_SIZE;
```

### API-af43b7a97107 · ndn_service_framework::NonceRegistry::reserve

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L141)

```cpp
bool reserve(const std::string& keyId,
               ndn::span<const uint8_t> nonce,
               std::chrono::steady_clock::time_point expiresAt);
```

### API-f4eefa3422d2 · ndn_service_framework::NonceRegistry::reserve

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L144)

```cpp
bool reserve(const std::string& keyId,
               ndn::span<const uint8_t> nonce,
               std::chrono::milliseconds lifetime);
```

### API-b951a30b943c · ndn_service_framework::NonceRegistry::reserveBatch

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L148)

```cpp
bool reserveBatch(const std::string& keyId,
                    const std::vector<ndn::Buffer>& nonces,
                    std::chrono::milliseconds lifetime);
```

原始接口说明：

```text
/** Atomically reserve a group of authenticated nonces for one key. */
```

### API-169e582d8af6 · ndn_service_framework::NonceRegistry::invalidate

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L151)

```cpp
void invalidate(const std::string& keyId);
```

### API-a3c6cab72eb5 · ndn_service_framework::NonceRegistry::clearExpired

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L152)

```cpp
void clearExpired(std::chrono::steady_clock::time_point now =
                    std::chrono::steady_clock::now());
```

### API-1a9048eada11 · ndn_service_framework::NonceRegistry::size

public / declared-interface；[源码](../../ndn-service-framework/RequestConfidentiality.hpp#L154)

```cpp
size_t size() const;
```

## ndn-service-framework/RevocationState.hpp

源码 SHA-256：`7acb6df4c4e1e7e3163f109b0cb77375b93f307e1d6cd7839a29bd0d25ad5a05`。

### API-b572e14d27d4 · ndn_service_framework::ProtectedTransition

public / declared-interface；[源码](../../ndn-service-framework/RevocationState.hpp#L11)

```cpp
enum class ProtectedTransition
```

### API-99a4b645530f · ndn_service_framework::ProtectedTransition::DISCOVERY

public / declared-interface；[源码](../../ndn-service-framework/RevocationState.hpp#L13)

```cpp
DISCOVERY
```

### API-1028d6c214a5 · ndn_service_framework::ProtectedTransition::ACK_COLLECTION

public / declared-interface；[源码](../../ndn-service-framework/RevocationState.hpp#L14)

```cpp
ACK_COLLECTION
```

### API-9a3b69aa96e6 · ndn_service_framework::ProtectedTransition::SELECTION

public / declared-interface；[源码](../../ndn-service-framework/RevocationState.hpp#L15)

```cpp
SELECTION
```

### API-dbeb1c01c8e5 · ndn_service_framework::ProtectedTransition::PROVIDER_EXECUTION

public / declared-interface；[源码](../../ndn-service-framework/RevocationState.hpp#L16)

```cpp
PROVIDER_EXECUTION
```

### API-06b84c09a5af · ndn_service_framework::ProtectedTransition::RESPONSE_DELIVERY

public / declared-interface；[源码](../../ndn-service-framework/RevocationState.hpp#L17)

```cpp
RESPONSE_DELIVERY
```

### API-963108b7fa0e · ndn_service_framework::ProtectedTransition::STREAM_EVENT

public / declared-interface；[源码](../../ndn-service-framework/RevocationState.hpp#L18)

```cpp
STREAM_EVENT
```

### API-d1d11c79acf0 · ndn_service_framework::AuthorizationSubject

public / declared-interface；[源码](../../ndn-service-framework/RevocationState.hpp#L21)

```cpp
struct AuthorizationSubject
```

### API-04fad3ad6d03 · ndn_service_framework::AuthorizationSubject::identity

public / declared-interface；[源码](../../ndn-service-framework/RevocationState.hpp#L23)

```cpp
ndn::Name identity;
```

### API-a815079cc917 · ndn_service_framework::AuthorizationSubject::certificateDigest

public / declared-interface；[源码](../../ndn-service-framework/RevocationState.hpp#L24)

```cpp
std::string certificateDigest;
```

### API-1685e6c915df · ndn_service_framework::AuthorizationSubject::serviceName

public / declared-interface；[源码](../../ndn-service-framework/RevocationState.hpp#L25)

```cpp
ndn::Name serviceName;
```

### API-90abb1ba0402 · ndn_service_framework::AuthorizationSubject::authorizationAttribute

public / declared-interface；[源码](../../ndn-service-framework/RevocationState.hpp#L26)

```cpp
ndn::Name authorizationAttribute;
```

### API-16ce80711844 · ndn_service_framework::RevocationDecision

public / declared-interface；[源码](../../ndn-service-framework/RevocationState.hpp#L29)

```cpp
struct RevocationDecision
```

### API-25b8097d4612 · ndn_service_framework::RevocationDecision::allowed

public / declared-interface；[源码](../../ndn-service-framework/RevocationState.hpp#L31)

```cpp
bool allowed = false;
```

### API-4e011131025f · ndn_service_framework::RevocationDecision::reason

public / declared-interface；[源码](../../ndn-service-framework/RevocationState.hpp#L32)

```cpp
std::string reason;
```

### API-02d467a55239 · ndn_service_framework::RevocationState

public / declared-interface；[源码](../../ndn-service-framework/RevocationState.hpp#L36)

```cpp
class RevocationState
```

### API-03488a4390c3 · ndn_service_framework::RevocationState::RevocationState

public / declared-interface；[源码](../../ndn-service-framework/RevocationState.hpp#L39)

```cpp
explicit RevocationState(ndn::Name serviceName = {});
```

### API-ff2ffd33763b · ndn_service_framework::RevocationState::acceptStatus

public / declared-interface；[源码](../../ndn-service-framework/RevocationState.hpp#L41)

```cpp
bool acceptStatus(const PolicyStatusData& status, uint64_t nowMs,
                    bool controllerSignatureValid = true);
```

### API-70f8300196a7 · ndn_service_framework::RevocationState::hasCurrentStatus

public / declared-interface；[源码](../../ndn-service-framework/RevocationState.hpp#L43)

```cpp
bool hasCurrentStatus() const;
```

### API-7a5ab9c22ca3 · ndn_service_framework::RevocationState::currentVersion

public / declared-interface；[源码](../../ndn-service-framework/RevocationState.hpp#L44)

```cpp
const ControllerVersion& currentVersion() const;
```

### API-c505d3ee1705 · ndn_service_framework::RevocationState::currentStatus

public / declared-interface；[源码](../../ndn-service-framework/RevocationState.hpp#L45)

```cpp
const PolicyStatusData& currentStatus() const;
```

### API-8585d72da56b · ndn_service_framework::RevocationState::authorize

public / declared-interface；[源码](../../ndn-service-framework/RevocationState.hpp#L47)

```cpp
RevocationDecision authorize(const AuthorizationSubject& subject,
                               ProtectedTransition transition,
                               uint64_t nowMs) const;
```

### API-9fc3806826a4 · ndn_service_framework::RevocationState::cacheInvalidated

public / declared-interface；[源码](../../ndn-service-framework/RevocationState.hpp#L51)

```cpp
bool cacheInvalidated(const std::string& family) const;
```

### API-1a8f30d37aa5 · ndn_service_framework::RevocationState::invalidatedCacheCount

public / declared-interface；[源码](../../ndn-service-framework/RevocationState.hpp#L52)

```cpp
size_t invalidatedCacheCount() const;
```

### API-58c14d5de97d · ndn_service_framework::RevocationState::clearInvalidationEvidence

public / declared-interface；[源码](../../ndn-service-framework/RevocationState.hpp#L53)

```cpp
void clearInvalidationEvidence();
```

## ndn-service-framework/RuntimeStatusStore.hpp

源码 SHA-256：`f5fcfbef7c34c597c24641632b92d063a27f4ea4e56b7364a5c3225ef0f33fc4`。

### API-b1db04fbebb0 · ndn_service_framework::RuntimeStatusStore

public / declared-interface；[源码](../../ndn-service-framework/RuntimeStatusStore.hpp#L32)

```cpp
class RuntimeStatusStore
```

### API-70ae02ccd6c4 · ndn_service_framework::RuntimeStatusStore::Record

public / declared-interface；[源码](../../ndn-service-framework/RuntimeStatusStore.hpp#L35)

```cpp
struct Record
```

### API-fb0f35520521 · ndn_service_framework::RuntimeStatusStore::Record::serviceName

public / declared-interface；[源码](../../ndn-service-framework/RuntimeStatusStore.hpp#L37)

```cpp
ndn::Name serviceName;
```

### API-c68c726c152d · ndn_service_framework::RuntimeStatusStore::Record::controllerVersion

public / declared-interface；[源码](../../ndn-service-framework/RuntimeStatusStore.hpp#L38)

```cpp
ControllerVersion controllerVersion;
```

### API-97c4a716a9e9 · ndn_service_framework::RuntimeStatusStore::Record::installTimeMs

public / declared-interface；[源码](../../ndn-service-framework/RuntimeStatusStore.hpp#L39)

```cpp
uint64_t installTimeMs = 0;
```

### API-c6243b240fcd · ndn_service_framework::RuntimeStatusStore::Record::abePublicParametersName

public / declared-interface；[源码](../../ndn-service-framework/RuntimeStatusStore.hpp#L40)

```cpp
ndn::Name abePublicParametersName;
```

### API-3bd86cf5fa96 · ndn_service_framework::RuntimeStatusStore::Record::abePublicParametersDigest

public / declared-interface；[源码](../../ndn-service-framework/RuntimeStatusStore.hpp#L41)

```cpp
std::string abePublicParametersDigest;
```

### API-168d567e031e · ndn_service_framework::RuntimeStatusStore::Record::policyStatusWire

public / declared-interface；[源码](../../ndn-service-framework/RuntimeStatusStore.hpp#L42)

```cpp
ndn::Buffer policyStatusWire;
```

### API-6dbb83c3be96 · ndn_service_framework::RuntimeStatusStore::Record::statusDataWire

public / declared-interface；[源码](../../ndn-service-framework/RuntimeStatusStore.hpp#L43)

```cpp
ndn::Buffer statusDataWire;
```

原始接口说明：

```text
// PolicyStatusData wireEncode() output
```

### API-c7a8369d9d15 · ndn_service_framework::RuntimeStatusStore::enabled

public / declared-interface；[源码](../../ndn-service-framework/RuntimeStatusStore.hpp#L47)

```cpp
static bool enabled();
```

原始接口说明：

```text
/** Whether the opt-in env gate (NDNSF_PERSIST_RUNTIME_STATE) is enabled. */
```

### API-8f0d0f161aaa · ndn_service_framework::RuntimeStatusStore::defaultStorePath

public / declared-interface；[源码](../../ndn-service-framework/RuntimeStatusStore.hpp#L54)

```cpp
static std::filesystem::path defaultStorePath(const std::string& role,
                                                const ndn::Name& identity);
```

原始接口说明：

```text
/**
   * Resolve the store path for one runtime role+identity:
   * $NDNSF_RUNTIME_STATE_DIR/<role>-<identity>.rts, default directory
   * $XDG_STATE_HOME|~/.local/state/ndnsf/runtime/.
   */
```

### API-9c85c737d51f · ndn_service_framework::RuntimeStatusStore::RuntimeStatusStore

public / declared-interface；[源码](../../ndn-service-framework/RuntimeStatusStore.hpp#L57)

```cpp
explicit RuntimeStatusStore(std::filesystem::path storePath);
```

### API-58bca9981458 · ndn_service_framework::RuntimeStatusStore::load

public / declared-interface；[源码](../../ndn-service-framework/RuntimeStatusStore.hpp#L67)

```cpp
bool load(std::vector<Record>& out) const;
```

原始接口说明：

```text
/**
   * Load all records.
   * @return false when the file is missing (cold start) or structurally
   *         corrupt/truncated (fail closed: nothing is restored and the
   *         caller starts from today's process-local behavior).  One
   *         undecodable or invalid record fails the whole load.
   * @throw std::runtime_error when the file cannot be opened at all.
   */
```

### API-2b854f4c3b69 · ndn_service_framework::RuntimeStatusStore::persist

public / declared-interface；[源码](../../ndn-service-framework/RuntimeStatusStore.hpp#L70)

```cpp
bool persist(const std::vector<Record>& records) const;
```

原始接口说明：

```text
/** Atomically replace the store with the given records (mode 0600). */
```

## ndn-service-framework/ServiceAuthorizationTable.hpp

源码 SHA-256：`e0be6a1d2ecd5776794752ea0c999680b675fcf6a6537d8e1165d43940a79e0f`。

### API-85f6b0c22685 · ndn_service_framework::ServiceAuthorizationRecord

public / declared-interface；[源码](../../ndn-service-framework/ServiceAuthorizationTable.hpp#L16)

```cpp
struct ServiceAuthorizationRecord
```

### API-14608825636f · ndn_service_framework::ServiceAuthorizationRecord::providerServiceName

public / declared-interface；[源码](../../ndn-service-framework/ServiceAuthorizationTable.hpp#L18)

```cpp
std::string providerServiceName;
```

### API-bbf7e1e9aff6 · ndn_service_framework::ServiceAuthorizationRecord::serviceName

public / declared-interface；[源码](../../ndn-service-framework/ServiceAuthorizationTable.hpp#L19)

```cpp
std::string serviceName;
```

### API-5f32ab951795 · ndn_service_framework::ServiceAuthorizationRecord::permissionKind

public / declared-interface；[源码](../../ndn-service-framework/ServiceAuthorizationTable.hpp#L20)

```cpp
size_t permissionKind = 0;
```

### API-ebb87594ca0a · ndn_service_framework::ServiceAuthorizationRecord::policyEpoch

public / declared-interface；[源码](../../ndn-service-framework/ServiceAuthorizationTable.hpp#L21)

```cpp
size_t policyEpoch = 0;
```

### API-ec62b2aa1718 · ndn_service_framework::ServiceAuthorizationRecord::isValid

public / declared-interface；[源码](../../ndn-service-framework/ServiceAuthorizationTable.hpp#L23)

```cpp
bool
  isValid() const
```

### API-0ca72dae051c · ndn_service_framework::ServiceAuthorizationTable

public / declared-interface；[源码](../../ndn-service-framework/ServiceAuthorizationTable.hpp#L33)

```cpp
class ServiceAuthorizationTable
```

### API-59da51752c45 · ndn_service_framework::ServiceAuthorizationTable::replacePermissions

public / declared-interface；[源码](../../ndn-service-framework/ServiceAuthorizationTable.hpp#L36)

```cpp
bool
  replacePermissions(size_t permissionKind,
                     size_t policyEpoch,
                     const std::vector<ServiceAuthorizationRecord>& records)
```

### API-e5fbe39185df · ndn_service_framework::ServiceAuthorizationTable::upsert

public / declared-interface；[源码](../../ndn-service-framework/ServiceAuthorizationTable.hpp#L75)

```cpp
bool
  upsert(const ServiceAuthorizationRecord& record)
```

### API-50674e01166d · ndn_service_framework::ServiceAuthorizationTable::find

public / declared-interface；[源码](../../ndn-service-framework/ServiceAuthorizationTable.hpp#L90)

```cpp
std::optional<ServiceAuthorizationRecord>
  find(const std::string& providerServiceName) const
```

### API-49159c2adf36 · ndn_service_framework::ServiceAuthorizationTable::contains

public / declared-interface；[源码](../../ndn-service-framework/ServiceAuthorizationTable.hpp#L101)

```cpp
bool
  contains(const std::string& providerServiceName,
           const std::string& serviceName,
           size_t permissionKind) const
```

### API-dba632c87d5b · ndn_service_framework::ServiceAuthorizationTable::snapshot

public / declared-interface；[源码](../../ndn-service-framework/ServiceAuthorizationTable.hpp#L111)

```cpp
std::vector<ServiceAuthorizationRecord>
  snapshot() const
```

### API-a80f341583cd · ndn_service_framework::ServiceAuthorizationTable::dumpAllowedServices

public / declared-interface；[源码](../../ndn-service-framework/ServiceAuthorizationTable.hpp#L123)

```cpp
std::vector<std::tuple<std::string, std::string, size_t>>
  dumpAllowedServices() const
```

## ndn-service-framework/ServiceContainer.hpp

源码 SHA-256：`fd9d2e0a5c42dd2823358671d191901a2c36f6d3ba0fe155eb561be82df7382e`。

### API-fbd6008915a6 · ndn_service_framework::ServiceContainer

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L37)

```cpp
class ServiceContainer
```

### API-94deaca2bd93 · ndn_service_framework::ServiceContainer::RuntimeConfig

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L40)

```cpp
struct RuntimeConfig
```

### API-bc2a86f51aad · ndn_service_framework::ServiceContainer::RuntimeConfig::containerIdentity

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L42)

```cpp
ndn::Name containerIdentity;
```

### API-f46ed0afd2ac · ndn_service_framework::ServiceContainer::RuntimeConfig::groupPrefix

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L43)

```cpp
ndn::Name groupPrefix;
```

### API-3aec968683d6 · ndn_service_framework::ServiceContainer::RuntimeConfig::controllerPrefix

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L44)

```cpp
ndn::Name controllerPrefix;
```

### API-cef4d134bff5 · ndn_service_framework::ServiceContainer::RuntimeConfig::trustSchemaPath

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L45)

```cpp
std::string trustSchemaPath;
```

### API-131f765dd1c0 · ndn_service_framework::ServiceContainer::LifecycleHook

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L48)

```cpp
struct LifecycleHook
```

### API-7014286c3026 · ndn_service_framework::ServiceContainer::LifecycleHook::start

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L50)

```cpp
std::function<void()> start;
```

### API-2427b247eae5 · ndn_service_framework::ServiceContainer::LifecycleHook::stop

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L51)

```cpp
std::function<void()> stop;
```

### API-14480d04c263 · ndn_service_framework::ServiceContainer::ServiceContainer

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L54)

```cpp
explicit
  ServiceContainer(RuntimeConfig config = RuntimeConfig{})
```

### API-e8843baf14e4 · ndn_service_framework::ServiceContainer::config

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L60)

```cpp
const RuntimeConfig&
  config() const
```

### API-205442080b40 · ndn_service_framework::ServiceContainer::localRegistry

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L66)

```cpp
LocalServiceRegistry&
  localRegistry()
```

### API-ee68261266a4 · ndn_service_framework::ServiceContainer::addLocalService

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L73)

```cpp
template <typename RequestT, typename ResponseT, typename HandlerT>
void
  addLocalService(const ndn::Name& serviceName, HandlerT&& handler)
```

### API-77bc5f8b5997 · ndn_service_framework::ServiceContainer::localRegistry

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L81)

```cpp
const LocalServiceRegistry&
  localRegistry() const
```

### API-6664bf9ed156 · ndn_service_framework::ServiceContainer::addController

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L87)

```cpp
void
  addController(const std::string& role, std::shared_ptr<ServiceController> controller)
```

### API-05e4ac6c6951 · ndn_service_framework::ServiceContainer::addController

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L103)

```cpp
void
  addController(const std::string& role, std::unique_ptr<ServiceController> controller)
```

### API-acb362c6ebd9 · ndn_service_framework::ServiceContainer::useController

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L109)

```cpp
void
  useController(const std::string& role, ServiceController& controller)
```

### API-83e0fbead798 · ndn_service_framework::ServiceContainer::hasController

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L115)

```cpp
bool
  hasController(const std::string& role) const
```

### API-29955a3af15b · ndn_service_framework::ServiceContainer::controller

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L121)

```cpp
ServiceController&
  controller(const std::string& role)
```

### API-52ebbe64fc7f · ndn_service_framework::ServiceContainer::controller

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L131)

```cpp
const ServiceController&
  controller(const std::string& role) const
```

### API-e7ec4f9bcc4e · ndn_service_framework::ServiceContainer::defaultController

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L141)

```cpp
ServiceController&
  defaultController()
```

### API-ff0a525adb88 · ndn_service_framework::ServiceContainer::addUser

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L150)

```cpp
void
  addUser(const std::string& role, std::shared_ptr<ServiceUser> user)
```

### API-25c2595ffb5d · ndn_service_framework::ServiceContainer::addUser

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L166)

```cpp
void
  addUser(const std::string& role, std::unique_ptr<ServiceUser> user)
```

### API-cc3f1cb8fa05 · ndn_service_framework::ServiceContainer::useUser

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L172)

```cpp
void
  useUser(const std::string& role, ServiceUser& user)
```

### API-03a7836b3868 · ndn_service_framework::ServiceContainer::hasUser

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L178)

```cpp
bool
  hasUser(const std::string& role) const
```

### API-bd2430bb3e18 · ndn_service_framework::ServiceContainer::user

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L184)

```cpp
ServiceUser&
  user(const std::string& role)
```

### API-17409b00e803 · ndn_service_framework::ServiceContainer::user

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L194)

```cpp
const ServiceUser&
  user(const std::string& role) const
```

### API-bb16024ecb84 · ndn_service_framework::ServiceContainer::defaultUser

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L204)

```cpp
ServiceUser&
  defaultUser()
```

### API-7aada0bf9ed4 · ndn_service_framework::ServiceContainer::addProvider

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L213)

```cpp
void
  addProvider(const std::string& role, std::shared_ptr<ServiceProvider> provider)
```

### API-2defb1ee58b2 · ndn_service_framework::ServiceContainer::addProvider

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L229)

```cpp
void
  addProvider(const std::string& role, std::unique_ptr<ServiceProvider> provider)
```

### API-99de804a6d3f · ndn_service_framework::ServiceContainer::useProvider

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L235)

```cpp
void
  useProvider(const std::string& role, ServiceProvider& provider)
```

### API-5ce3affa10c8 · ndn_service_framework::ServiceContainer::hasProvider

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L241)

```cpp
bool
  hasProvider(const std::string& role) const
```

### API-747fba2a9c63 · ndn_service_framework::ServiceContainer::provider

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L247)

```cpp
ServiceProvider&
  provider(const std::string& role)
```

### API-9a04b75e7ba5 · ndn_service_framework::ServiceContainer::provider

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L257)

```cpp
const ServiceProvider&
  provider(const std::string& role) const
```

### API-c4b18a5c4d0d · ndn_service_framework::ServiceContainer::defaultProvider

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L267)

```cpp
ServiceProvider&
  defaultProvider()
```

### API-6ccb7819d070 · ndn_service_framework::ServiceContainer::controllerRoles

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L276)

```cpp
std::vector<std::string>
  controllerRoles() const
```

### API-7aa7f4c692bf · ndn_service_framework::ServiceContainer::userRoles

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L286)

```cpp
std::vector<std::string>
  userRoles() const
```

### API-77d11644ca7f · ndn_service_framework::ServiceContainer::providerRoles

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L296)

```cpp
std::vector<std::string>
  providerRoles() const
```

### API-962bd127895c · ndn_service_framework::ServiceContainer::addLifecycleHook

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L306)

```cpp
void
  addLifecycleHook(const std::string& name, LifecycleHook hook)
```

### API-873c470ee9ae · ndn_service_framework::ServiceContainer::isStarted

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L325)

```cpp
bool
  isStarted() const
```

### API-04ad67003514 · ndn_service_framework::ServiceContainer::start

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L331)

```cpp
void
  start()
```

### API-cd596b18ee08 · ndn_service_framework::ServiceContainer::stop

public / declared-interface；[源码](../../ndn-service-framework/ServiceContainer.hpp#L365)

```cpp
void
  stop()
```

## ndn-service-framework/ServiceController.hpp

源码 SHA-256：`d6457f484c152bb6b675ac42e693b1d6f011818c41260b4224e532b0bfda7a86`。

### API-f7138fbcc116 · ndn_service_framework::ServiceControllerTestAccess

public / declared-interface；[源码](../../ndn-service-framework/ServiceController.hpp#L41)

```cpp
struct ServiceControllerTestAccess
```

### API-9e85ef81cdf4 · ndn_service_framework::ProviderPolicy

public / declared-interface；[源码](../../ndn-service-framework/ServiceController.hpp#L47)

```cpp
struct ProviderPolicy
```

### API-4df0c96ae4a9 · ndn_service_framework::ProviderPolicy::providerName

public / declared-interface；[源码](../../ndn-service-framework/ServiceController.hpp#L49)

```cpp
std::string providerName;
```

### API-09cf4a983114 · ndn_service_framework::ProviderPolicy::allowedServices

public / declared-interface；[源码](../../ndn-service-framework/ServiceController.hpp#L50)

```cpp
std::vector<std::string> allowedServices;
```

原始接口说明：

```text
// identity URI
```

### API-6d3a4b953afa · ndn_service_framework::UserPolicy

public / declared-interface；[源码](../../ndn-service-framework/ServiceController.hpp#L53)

```cpp
struct UserPolicy
```

### API-0e8964f9f88e · ndn_service_framework::UserPolicy::userName

public / declared-interface；[源码](../../ndn-service-framework/ServiceController.hpp#L55)

```cpp
std::string userName;
```

### API-f2ef043e54e2 · ndn_service_framework::UserPolicy::allowedServices

public / declared-interface；[源码](../../ndn-service-framework/ServiceController.hpp#L56)

```cpp
std::vector<std::string> allowedServices;
```

原始接口说明：

```text
// identity URI
```

### API-f735c492a96f · ndn_service_framework::PolicyParser

public / declared-interface；[源码](../../ndn-service-framework/ServiceController.hpp#L59)

```cpp
class PolicyParser
```

### API-c99ab9cb4ce0 · ndn_service_framework::PolicyParser::parsePolicyFile

public / declared-interface；[源码](../../ndn-service-framework/ServiceController.hpp#L63)

```cpp
std::pair<std::vector<ProviderPolicy>, std::vector<UserPolicy>>
  parsePolicyFile(const std::string& path);
```

原始接口说明：

```text
// returns {providerPolicies, userPolicies}
```

### API-3962ca5ceffd · ndn_service_framework::ServiceController

public / declared-interface；[源码](../../ndn-service-framework/ServiceController.hpp#L67)

```cpp
class ServiceController
```

### API-3b217f6783b8 · ndn_service_framework::ServiceController::ServiceController

public / declared-interface；[源码](../../ndn-service-framework/ServiceController.hpp#L70)

```cpp
ServiceController(ndn::Face& face,
                    ndn::security::Certificate aaCert,
                    ndn::ValidatorConfig& validator,
                    const std::string& configFilePath);
```

### API-6e2458048aa6 · ndn_service_framework::ServiceController::setControllerPrefix

public / declared-interface；[源码](../../ndn-service-framework/ServiceController.hpp#L75)

```cpp
void setControllerPrefix(const ndn::Name& prefix);
```

### API-df65fbcde4da · ndn_service_framework::ServiceController::setBootstrapTokenFile

public / declared-interface；[源码](../../ndn-service-framework/ServiceController.hpp#L76)

```cpp
void setBootstrapTokenFile(const std::string& path);
```

### API-5bafcda60ec3 · ndn_service_framework::ServiceController::cancelStart

public / declared-interface；[源码](../../ndn-service-framework/ServiceController.hpp#L77)

```cpp
void cancelStart() noexcept;
```

### API-5c5ca05622af · ndn_service_framework::ServiceController::resetStartCancellation

public / declared-interface；[源码](../../ndn-service-framework/ServiceController.hpp#L78)

```cpp
void resetStartCancellation() noexcept;
```

### API-a2c7d80f83bf · ndn_service_framework::ServiceController::start

public / declared-interface；[源码](../../ndn-service-framework/ServiceController.hpp#L83)

```cpp
void start();
```

原始接口说明：

```text
// Owns the Face event loop until registrations and an independently received,
// authenticated PUBPARAMS round trip complete (at most ten seconds). The
// probe uses ndn-cxx's configured local transport; a caller-supplied Face
// must reach that same local NFD. Cancellation or a stopped loop fails closed.
```

### API-77e8f2fed258 · ndn_service_framework::ServiceController::run

public / declared-interface；[源码](../../ndn-service-framework/ServiceController.hpp#L84)

```cpp
void run();
```

### API-4cd918345e33 · ndn_service_framework::ServiceController::getControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/ServiceController.hpp#L87)

```cpp
ControllerVersion getControllerVersion() const;
```

原始接口说明：

```text
/** Current persisted Controller authorization version. */
```

### API-a6e1ee327414 · ndn_service_framework::ServiceController::getPolicyStatus

public / declared-interface；[源码](../../ndn-service-framework/ServiceController.hpp#L91)

```cpp
PolicyStatusData getPolicyStatus(const ndn::Name& serviceName) const;
```

原始接口说明：

```text
/** Build the signed-status payload for one service.  The enclosing Data
   * packet is signed by ServiceController when published. */
```

### API-3ad7bdf59cc1 · ndn_service_framework::ServiceController::revoke

public / declared-interface；[源码](../../ndn-service-framework/ServiceController.hpp#L95)

```cpp
bool revoke(const RevocationTarget& target);
```

原始接口说明：

```text
/** Add a typed revocation, or finish its pending failed rotation on retry.
   * An already completed duplicate remains a no-op returning false. */
```

### API-d5f6b3c470a6 · ndn_service_framework::ServiceController::grant

public / declared-interface；[源码](../../ndn-service-framework/ServiceController.hpp#L103)

```cpp
bool grant(const ndn::Name& identity,
             const ndn::Name& serviceName,
             const ndn::Name& authorizationAttribute);
```

原始接口说明：

```text
/**
   * Grant one exact service authorization.  Grant-only mutations advance the
   * ControllerVersion but keep the current global ABE generation; the
   * Controller replaces only the target identity's complete policy; NAC-ABE
   * generates the replacement DKEY lazily on that identity's next fetch.
   */
```

### API-c7beb38e843f · ndn_service_framework::ServiceController::isRevoked

public / declared-interface；[源码](../../ndn-service-framework/ServiceController.hpp#L110)

```cpp
bool isRevoked(const ndn::Name& identity,
                 const ndn::Name& serviceName,
                 const std::string& certificateDigest = {},
                 const ndn::Name& authorizationAttribute = {}) const;
```

原始接口说明：

```text
/** Testable policy decision used by permission issuance.  An empty
   * authorizationAttribute is a query wildcard for legacy diagnostics; all
   * runtime authorization paths pass the exact canonical attribute. */
```

## ndn-service-framework/ServiceProvider.hpp

源码 SHA-256：`dca865f89f9c3093b9bc35d1a24c38e3f291d9263fcba4009e430582b9169752`。

### API-d30b48966880 · ndn_service_framework::RequestPayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L42)

```cpp
using RequestPayload = ndn::Buffer;
```

### API-e13f256496a9 · ndn_service_framework::ResponsePayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L43)

```cpp
using ResponsePayload = ndn::Buffer;
```

### API-ff9a274245db · ndn_service_framework::ServiceName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L44)

```cpp
using ServiceName = ndn::Name;
```

### API-0bb54cba645f · ndn_service_framework::CollaborationRole

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L45)

```cpp
using CollaborationRole = std::string;
```

### API-bba267cc036d · ndn_service_framework::KeyScope

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L46)

```cpp
using KeyScope = std::string;
```

### API-f9321c0d463a · ndn_service_framework::Topic

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L47)

```cpp
using Topic = ndn::Name;
```

### API-8c9b12e185ac · ndn_service_framework::SessionId

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L48)

```cpp
using SessionId = std::string;
```

### API-c63a255d092c · ndn_service_framework::DataV1SegmentNameFilter

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L57)

```cpp
struct DataV1SegmentNameFilter
```

### API-4edf1802c1fe · ndn_service_framework::DataV1SegmentNameFilter::predicate

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L59)

```cpp
std::function<bool(const ndn::Name&)> predicate;
```

### API-2c83f73bfd9d · ndn_service_framework::DataV1SegmentNameFilter::subscriptionReady

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L64)

```cpp
std::function<void()> subscriptionReady;
```

原始接口说明：

```text
/** Optional lifecycle observer invoked on the Face event loop after
         * the legacy SVS subscription is installed.  It does not affect
         * admission and exists so callers can coordinate a publisher without
         * timing sleeps. */
```

### API-c18b7ac419bd · ndn_service_framework::LargeDataFetchResult

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L67)

```cpp
struct LargeDataFetchResult
```

### API-949b85cc8e84 · ndn_service_framework::LargeDataFetchResult::success

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L69)

```cpp
bool success = false;
```

### API-9da60ae40b0a · ndn_service_framework::LargeDataFetchResult::plaintext

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L70)

```cpp
std::vector<uint8_t> plaintext;
```

### API-ce3c97217ba0 · ndn_service_framework::LargeDataFetchResult::errorMessage

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L71)

```cpp
std::string errorMessage;
```

### API-4333023c321f · ndn_service_framework::LargeDataResponsePublishResult

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L74)

```cpp
struct LargeDataResponsePublishResult
```

### API-eb365e8e8eef · ndn_service_framework::LargeDataResponsePublishResult::success

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L76)

```cpp
bool success = false;
```

### API-dc0000a67c58 · ndn_service_framework::LargeDataResponsePublishResult::encryptedDataName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L77)

```cpp
ndn::Name encryptedDataName;
```

### API-c103d55f12f2 · ndn_service_framework::LargeDataResponsePublishResult::objectId

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L78)

```cpp
std::string objectId;
```

### API-e179b94ba1fb · ndn_service_framework::LargeDataResponsePublishResult::digest

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L79)

```cpp
std::string digest;
```

### API-20c7b9345f68 · ndn_service_framework::LargeDataResponsePublishResult::errorMessage

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L80)

```cpp
std::string errorMessage;
```

### API-10831732544d · ndn_service_framework::LargeDataReferenceResponseResult

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L83)

```cpp
struct LargeDataReferenceResponseResult
```

### API-22b008ee63e2 · ndn_service_framework::LargeDataReferenceResponseResult::success

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L85)

```cpp
bool success = false;
```

### API-7bd3161ba605 · ndn_service_framework::LargeDataReferenceResponseResult::usedLargeDataReference

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L86)

```cpp
bool usedLargeDataReference = false;
```

### API-0a708c2031da · ndn_service_framework::LargeDataReferenceResponseResult::responseMessage

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L87)

```cpp
ndn_service_framework::ResponseMessage responseMessage;
```

### API-8f54f58568f4 · ndn_service_framework::LargeDataReferenceResponseResult::largeData

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L88)

```cpp
LargeDataResponsePublishResult largeData;
```

### API-fd8451d56b4c · ndn_service_framework::LargeDataReferenceResponseResult::errorMessage

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L89)

```cpp
std::string errorMessage;
```

### API-1c047f255f67 · ndn_service_framework::ServiceProvider

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L92)

```cpp
class ServiceProvider
```

### API-33834974022c · ndn_service_framework::ServiceProvider::ServiceKey

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L95)

```cpp
using ServiceKey = ndn::Name;
```

### API-1ff76b5498e4 · ndn_service_framework::ServiceProvider::AckDecision

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L97)

```cpp
struct AckDecision
```

### API-3340dbd38859 · ndn_service_framework::ServiceProvider::AckDecision::status

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L99)

```cpp
bool status = false;
```

### API-7047add89153 · ndn_service_framework::ServiceProvider::AckDecision::suppressAck

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L100)

```cpp
bool suppressAck = false;
```

### API-b85d51693213 · ndn_service_framework::ServiceProvider::AckDecision::message

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L101)

```cpp
std::string message;
```

### API-103b9eef1e27 · ndn_service_framework::ServiceProvider::AckDecision::payload

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L102)

```cpp
ndn::Buffer payload;
```

### API-5b1b96aaaa70 · ndn_service_framework::ServiceProvider::AckDecision::selectionInputKeyOffer

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L103)

```cpp
std::optional<SelectionInputKeyOffer> selectionInputKeyOffer;
```

### API-c23159f181ec · ndn_service_framework::ServiceProvider::AckDecision::reservationLease

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L104)

```cpp
std::optional<ReservationLease> reservationLease;
```

### API-284abac928b8 · ndn_service_framework::ServiceProvider::AckDecision::pendingStateTtlMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L107)

```cpp
uint64_t pendingStateTtlMs = 0;
```

原始接口说明：

```text
// Provider-owned local retention horizon for pending
// Request/Selection state. Zero keeps Core's bounded default.
```

### API-b0148c20b4b0 · ndn_service_framework::ServiceProvider::PeerNetworkMetric

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L110)

```cpp
struct PeerNetworkMetric
```

### API-e6d13dc91bc8 · ndn_service_framework::ServiceProvider::PeerNetworkMetric::srcPeer

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L112)

```cpp
ndn::Name srcPeer;
```

### API-969b78355738 · ndn_service_framework::ServiceProvider::PeerNetworkMetric::dstPeer

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L113)

```cpp
ndn::Name dstPeer;
```

### API-e0284386fef9 · ndn_service_framework::ServiceProvider::PeerNetworkMetric::rttMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L114)

```cpp
double rttMs = 0.0;
```

### API-8702d5a18cdc · ndn_service_framework::ServiceProvider::PeerNetworkMetric::bandwidthMbps

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L115)

```cpp
double bandwidthMbps = 0.0;
```

### API-e2d9c5677d58 · ndn_service_framework::ServiceProvider::PeerNetworkMetric::lossRate

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L116)

```cpp
double lossRate = 0.0;
```

### API-3b6185cea266 · ndn_service_framework::ServiceProvider::PeerNetworkMetric::jitterMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L117)

```cpp
double jitterMs = 0.0;
```

### API-6cc578cc1ee2 · ndn_service_framework::ServiceProvider::PeerNetworkMetric::observedAtMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L118)

```cpp
uint64_t observedAtMs = 0;
```

### API-3d53384d5ebc · ndn_service_framework::ServiceProvider::PeerNetworkMetric::confidence

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L119)

```cpp
double confidence = 1.0;
```

### API-d192f9af7306 · ndn_service_framework::ServiceProvider::GenericProviderRuntimeHint

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L122)

```cpp
struct GenericProviderRuntimeHint
```

### API-ed84f6288e50 · ndn_service_framework::ServiceProvider::GenericProviderRuntimeHint::providerName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L124)

```cpp
ndn::Name providerName;
```

### API-718cdc04f6d8 · ndn_service_framework::ServiceProvider::GenericProviderRuntimeHint::queueLength

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L125)

```cpp
uint64_t queueLength = 0;
```

### API-1525a8e8c74b · ndn_service_framework::ServiceProvider::GenericProviderRuntimeHint::estimatedQueueWaitMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L126)

```cpp
uint64_t estimatedQueueWaitMs = 0;
```

### API-e2a08fa4c78e · ndn_service_framework::ServiceProvider::GenericProviderRuntimeHint::cpuUtilization

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L127)

```cpp
double cpuUtilization = 0.0;
```

### API-085319e9b600 · ndn_service_framework::ServiceProvider::GenericProviderRuntimeHint::gpuUtilization

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L128)

```cpp
double gpuUtilization = 0.0;
```

### API-e5afc8f0d4ec · ndn_service_framework::ServiceProvider::GenericProviderRuntimeHint::freeMemoryMb

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L129)

```cpp
uint64_t freeMemoryMb = 0;
```

### API-2d5f7cb34bbe · ndn_service_framework::ServiceProvider::GenericProviderRuntimeHint::freeGpuMemoryMb

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L130)

```cpp
uint64_t freeGpuMemoryMb = 0;
```

### API-0d1fe9553579 · ndn_service_framework::ServiceProvider::GenericProviderRuntimeHint::peerMetrics

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L131)

```cpp
std::vector<PeerNetworkMetric> peerMetrics;
```

### API-56e3769db606 · ndn_service_framework::ServiceProvider::GenericAdmissionLease

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L134)

```cpp
struct GenericAdmissionLease
```

### API-f0026d66179b · ndn_service_framework::ServiceProvider::GenericAdmissionLease::leaseId

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L136)

```cpp
std::string leaseId;
```

### API-ac61bac6baa8 · ndn_service_framework::ServiceProvider::GenericAdmissionLease::providerName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L137)

```cpp
ndn::Name providerName;
```

### API-5205fad94bac · ndn_service_framework::ServiceProvider::GenericAdmissionLease::requesterName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L138)

```cpp
ndn::Name requesterName;
```

### API-d4005b6e1e28 · ndn_service_framework::ServiceProvider::GenericAdmissionLease::serviceName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L139)

```cpp
ndn::Name serviceName;
```

### API-e0d7c00312ab · ndn_service_framework::ServiceProvider::GenericAdmissionLease::expiresAtMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L140)

```cpp
uint64_t expiresAtMs = 0;
```

### API-9a946697c036 · ndn_service_framework::ServiceProvider::GenericAdmissionLease::resourceBindingProof

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L141)

```cpp
ndn::Buffer resourceBindingProof;
```

### API-2ffe900164b5 · ndn_service_framework::ServiceProvider::GenericAdmissionLease::consumed

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L142)

```cpp
bool consumed = false;
```

### API-59bd26b94cd8 · ndn_service_framework::ServiceProvider::GenericLeaseValidationResult

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L145)

```cpp
struct GenericLeaseValidationResult
```

### API-e09fb8123933 · ndn_service_framework::ServiceProvider::GenericLeaseValidationResult::status

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L147)

```cpp
bool status = false;
```

### API-219782cb2bbe · ndn_service_framework::ServiceProvider::GenericLeaseValidationResult::reasonCode

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L148)

```cpp
std::string reasonCode;
```

### API-293638c316a7 · ndn_service_framework::ServiceProvider::GenericLeaseValidationResult::leaseId

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L149)

```cpp
std::string leaseId;
```

### API-e0f5f82b1ef2 · ndn_service_framework::ServiceProvider::GenericAckMetadata

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L152)

```cpp
struct GenericAckMetadata
```

### API-dc6c7ae07976 · ndn_service_framework::ServiceProvider::GenericAckMetadata::runtimeHint

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L154)

```cpp
std::optional<GenericProviderRuntimeHint> runtimeHint;
```

### API-0a5aa0813e48 · ndn_service_framework::ServiceProvider::GenericAckMetadata::leaseOffers

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L155)

```cpp
std::vector<GenericAdmissionLease> leaseOffers;
```

### API-84d93fb44f61 · ndn_service_framework::ServiceProvider::GenericAckMetadata::servicePayloadSchema

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L156)

```cpp
std::string servicePayloadSchema;
```

### API-513446b16ab3 · ndn_service_framework::ServiceProvider::GenericAckMetadata::servicePayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L157)

```cpp
ndn::Buffer servicePayload;
```

### API-b414b078bfa1 · ndn_service_framework::ServiceProvider::DataProductReference

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L160)

```cpp
struct DataProductReference
```

### API-d9dea354eb8a · ndn_service_framework::ServiceProvider::DataProductReference::name

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L162)

```cpp
ndn::Name name;
```

### API-0660a15dc55e · ndn_service_framework::ServiceProvider::DataProductReference::producerName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L163)

```cpp
ndn::Name producerName;
```

### API-8369815f8c00 · ndn_service_framework::ServiceProvider::DataProductReference::serviceName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L164)

```cpp
ndn::Name serviceName;
```

### API-f70581ebc04f · ndn_service_framework::ServiceProvider::DataProductReference::objectClass

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L165)

```cpp
std::string objectClass;
```

### API-1d377c99585b · ndn_service_framework::ServiceProvider::DataProductReference::contentType

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L166)

```cpp
std::string contentType = "application/octet-stream";
```

### API-911ac2cffe62 · ndn_service_framework::ServiceProvider::DataProductReference::digest

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L167)

```cpp
std::string digest;
```

### API-18281a4f7907 · ndn_service_framework::ServiceProvider::DataProductReference::sizeBytes

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L168)

```cpp
uint64_t sizeBytes = 0;
```

### API-c56b8f83baaa · ndn_service_framework::ServiceProvider::DataProductReference::segmentCount

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L169)

```cpp
uint64_t segmentCount = 0;
```

### API-fdb2c9124eb0 · ndn_service_framework::ServiceProvider::DataProductReference::freshnessMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L170)

```cpp
uint64_t freshnessMs = 0;
```

### API-5c550efc53cb · ndn_service_framework::ServiceProvider::ServiceOperationStatus

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L173)

```cpp
struct ServiceOperationStatus
```

### API-55b8b24d93c5 · ndn_service_framework::ServiceProvider::ServiceOperationStatus::operationId

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L175)

```cpp
std::string operationId;
```

### API-d1793d6a2873 · ndn_service_framework::ServiceProvider::ServiceOperationStatus::operation

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L176)

```cpp
std::string operation;
```

### API-1bbb8ae8548a · ndn_service_framework::ServiceProvider::ServiceOperationStatus::serviceName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L177)

```cpp
ndn::Name serviceName;
```

### API-47c8bb227e4a · ndn_service_framework::ServiceProvider::ServiceOperationStatus::providerName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L178)

```cpp
ndn::Name providerName;
```

### API-433dc05dc5e0 · ndn_service_framework::ServiceProvider::ServiceOperationStatus::requestId

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L179)

```cpp
ndn::Name requestId;
```

### API-205d652f3362 · ndn_service_framework::ServiceProvider::ServiceOperationStatus::role

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L180)

```cpp
std::string role;
```

### API-ff8e2333bcfd · ndn_service_framework::ServiceProvider::ServiceOperationStatus::attempt

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L181)

```cpp
uint64_t attempt = 1;
```

### API-8ddc93b71e75 · ndn_service_framework::ServiceProvider::ServiceOperationStatus::epoch

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L182)

```cpp
uint64_t epoch = 1;
```

### API-2baaa7a60872 · ndn_service_framework::ServiceProvider::ServiceOperationStatus::sequence

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L183)

```cpp
uint64_t sequence = 1;
```

### API-743ae77bc118 · ndn_service_framework::ServiceProvider::ServiceOperationStatus::state

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L184)

```cpp
std::string state = "QUEUED";
```

### API-e1fa3bfb7c75 · ndn_service_framework::ServiceProvider::ServiceOperationStatus::reasonCode

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L185)

```cpp
std::string reasonCode;
```

### API-bd5053b04f85 · ndn_service_framework::ServiceProvider::ServiceOperationStatus::message

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L186)

```cpp
std::string message;
```

### API-8223522e0f92 · ndn_service_framework::ServiceProvider::ServiceOperationStatus::progressKnown

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L187)

```cpp
bool progressKnown = false;
```

### API-fe694d274a61 · ndn_service_framework::ServiceProvider::ServiceOperationStatus::progress

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L188)

```cpp
double progress = 0.0;
```

### API-531f7a99272e · ndn_service_framework::ServiceProvider::ServiceOperationStatus::resultReference

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L189)

```cpp
std::optional<DataProductReference> resultReference;
```

### API-d52cddf572aa · ndn_service_framework::ServiceProvider::ServiceOperationStatus::retryAfterMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L190)

```cpp
uint64_t retryAfterMs = 0;
```

### API-c22bd1308e13 · ndn_service_framework::ServiceProvider::ServiceOperationStatus::createdAtMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L191)

```cpp
uint64_t createdAtMs = 0;
```

### API-83e118d64c7d · ndn_service_framework::ServiceProvider::ServiceOperationStatus::updatedAtMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L192)

```cpp
uint64_t updatedAtMs = 0;
```

### API-aab621498e8e · ndn_service_framework::ServiceProvider::ServiceOperationStatus::expiresAtMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L193)

```cpp
uint64_t expiresAtMs = 0;
```

### API-c109a0b76452 · ndn_service_framework::ServiceProvider::ServiceOperationStatus::detailsSchema

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L194)

```cpp
std::string detailsSchema;
```

### API-2aa8940a2a6b · ndn_service_framework::ServiceProvider::ServiceOperationStatus::detailsPayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L195)

```cpp
ndn::Buffer detailsPayload;
```

### API-83df62446c29 · ndn_service_framework::ServiceProvider::ProviderCapabilityHint

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L198)

```cpp
struct ProviderCapabilityHint
```

### API-e28be5bcd389 · ndn_service_framework::ServiceProvider::ProviderCapabilityHint::providerName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L200)

```cpp
ndn::Name providerName;
```

### API-29d41fd25868 · ndn_service_framework::ServiceProvider::ProviderCapabilityHint::serviceName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L201)

```cpp
ndn::Name serviceName;
```

### API-80ef9318d7ed · ndn_service_framework::ServiceProvider::ProviderCapabilityHint::ready

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L202)

```cpp
bool ready = true;
```

### API-52987151adcd · ndn_service_framework::ServiceProvider::ProviderCapabilityHint::drainState

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L203)

```cpp
std::string drainState = "ACTIVE";
```

### API-a3fe70efd848 · ndn_service_framework::ServiceProvider::ProviderCapabilityHint::reasonCode

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L204)

```cpp
std::string reasonCode;
```

### API-732ddb1e169e · ndn_service_framework::ServiceProvider::ProviderCapabilityHint::message

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L205)

```cpp
std::string message;
```

### API-f391b6591d7f · ndn_service_framework::ServiceProvider::ProviderCapabilityHint::runtimeHint

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L206)

```cpp
std::optional<GenericProviderRuntimeHint> runtimeHint;
```

### API-a20dfc66260f · ndn_service_framework::ServiceProvider::ProviderCapabilityHint::leaseOffers

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L207)

```cpp
std::vector<GenericAdmissionLease> leaseOffers;
```

### API-0e5d9cb06169 · ndn_service_framework::ServiceProvider::ProviderCapabilityHint::operationStatus

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L208)

```cpp
std::optional<ServiceOperationStatus> operationStatus;
```

### API-b0b9b738373f · ndn_service_framework::ServiceProvider::ProviderCapabilityHint::servicePayloadSchema

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L209)

```cpp
std::string servicePayloadSchema;
```

### API-659bc6405349 · ndn_service_framework::ServiceProvider::ProviderCapabilityHint::servicePayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L210)

```cpp
ndn::Buffer servicePayload;
```

### API-88252393a09e · ndn_service_framework::ServiceProvider::ProviderCapabilityHint::readyForNewRequest

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L212)

```cpp
bool readyForNewRequest() const;
```

### API-e9908683269c · ndn_service_framework::ServiceProvider::ProviderAdmissionLeaseTable

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L215)

```cpp
class ProviderAdmissionLeaseTable
```

### API-d6c5cfa9ae74 · ndn_service_framework::ServiceProvider::ProviderAdmissionLeaseTable::grant

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L218)

```cpp
void grant(GenericAdmissionLease lease);
```

### API-a64c2ce80165 · ndn_service_framework::ServiceProvider::ProviderAdmissionLeaseTable::consume

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L219)

```cpp
GenericLeaseValidationResult consume(
                    const std::string& leaseId,
                    const ndn::Name& requesterName,
                    const ndn::Name& providerName,
                    const ndn::Name& serviceName,
                    const ndn::Buffer& resourceBindingProof,
                    uint64_t nowMs);
```

### API-e2144a65e5c0 · ndn_service_framework::ServiceProvider::ProviderAdmissionLeaseTable::size

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L226)

```cpp
size_t size() const;
```

### API-2c34162490e9 · ndn_service_framework::ServiceProvider::makeGenericAdmissionLeaseAckPayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L233)

```cpp
static ndn::Buffer makeGenericAdmissionLeaseAckPayload(
                const GenericAdmissionLease& lease,
                const ndn::Buffer& servicePayload = ndn::Buffer());
```

### API-743e07f4ba0d · ndn_service_framework::ServiceProvider::makeGenericAckMetadataPayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L236)

```cpp
static ndn::Buffer makeGenericAckMetadataPayload(
                const GenericAckMetadata& metadata);
```

### API-080cd7afb0fa · ndn_service_framework::ServiceProvider::parseGenericAckMetadataPayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L238)

```cpp
static GenericAckMetadata parseGenericAckMetadataPayload(
                const ndn::Buffer& payload);
```

### API-01a0ed166cc3 · ndn_service_framework::ServiceProvider::makePeerNetworkMetricPayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L240)

```cpp
static ndn::Buffer makePeerNetworkMetricPayload(
                const PeerNetworkMetric& metric);
```

### API-7e70a9a43bda · ndn_service_framework::ServiceProvider::parsePeerNetworkMetricPayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L242)

```cpp
static std::optional<PeerNetworkMetric> parsePeerNetworkMetricPayload(
                const ndn::Buffer& payload);
```

### API-618393892d7d · ndn_service_framework::ServiceProvider::makeDataProductReferencePayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L244)

```cpp
static ndn::Buffer makeDataProductReferencePayload(
                const DataProductReference& reference);
```

### API-20d7e5bb6baf · ndn_service_framework::ServiceProvider::parseDataProductReferencePayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L246)

```cpp
static std::optional<DataProductReference> parseDataProductReferencePayload(
                const ndn::Buffer& payload);
```

### API-518a5539ce35 · ndn_service_framework::ServiceProvider::makeServiceOperationStatusPayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L248)

```cpp
static ndn::Buffer makeServiceOperationStatusPayload(
                const ServiceOperationStatus& status);
```

### API-50794245c81f · ndn_service_framework::ServiceProvider::parseServiceOperationStatusPayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L250)

```cpp
static std::optional<ServiceOperationStatus> parseServiceOperationStatusPayload(
                const ndn::Buffer& payload);
```

### API-2a6ad4bc18d0 · ndn_service_framework::ServiceProvider::makeProviderCapabilityHintPayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L252)

```cpp
static ndn::Buffer makeProviderCapabilityHintPayload(
                const ProviderCapabilityHint& hint);
```

### API-f3c0c2499501 · ndn_service_framework::ServiceProvider::parseProviderCapabilityHintPayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L254)

```cpp
static std::optional<ProviderCapabilityHint> parseProviderCapabilityHintPayload(
                const ndn::Buffer& payload);
```

### API-9d613e350dfe · ndn_service_framework::ServiceProvider::GenericAdmissionLeaseValidationRequest

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L257)

```cpp
struct GenericAdmissionLeaseValidationRequest
```

### API-27e78b4be976 · ndn_service_framework::ServiceProvider::GenericAdmissionLeaseValidationRequest::requesterName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L259)

```cpp
ndn::Name requesterName;
```

### API-0686a0708ada · ndn_service_framework::ServiceProvider::GenericAdmissionLeaseValidationRequest::providerName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L260)

```cpp
ndn::Name providerName;
```

### API-5ff2b21ed1b5 · ndn_service_framework::ServiceProvider::GenericAdmissionLeaseValidationRequest::serviceName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L261)

```cpp
ndn::Name serviceName;
```

### API-77ad3bfb69a1 · ndn_service_framework::ServiceProvider::GenericAdmissionLeaseValidationRequest::requestId

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L262)

```cpp
ndn::Name requestId;
```

### API-e2d47e84d3e7 · ndn_service_framework::ServiceProvider::GenericAdmissionLeaseValidationRequest::requestMessage

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L263)

```cpp
RequestMessage requestMessage;
```

### API-7d75044143cf · ndn_service_framework::ServiceProvider::GenericAdmissionLeaseValidationRequest::selectionMessage

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L264)

```cpp
ServiceSelectionMessage selectionMessage;
```

### API-f755eddc3d3f · ndn_service_framework::ServiceProvider::GenericAdmissionLeaseValidationRequest::assignmentPayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L265)

```cpp
ndn::Buffer assignmentPayload;
```

### API-599b7f5dd51d · ndn_service_framework::ServiceProvider::AckStrategyHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L268)

```cpp
using AckStrategyHandler =
                std::function<AckDecision(const RequestMessage&)>;
```

### API-07dd2303ce5d · ndn_service_framework::ServiceProvider::GenericAdmissionLeaseValidator

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L271)

```cpp
using GenericAdmissionLeaseValidator =
                std::function<GenericLeaseValidationResult(
                    const GenericAdmissionLeaseValidationRequest&)>;
```

### API-0b46c80238f2 · ndn_service_framework::ServiceProvider::LegacyAckStrategyHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L275)

```cpp
using LegacyAckStrategyHandler =
                std::function<std::pair<bool, ndn::Block>(const RequestAckMessage&)>;
```

### API-598b119cc8a7 · ndn_service_framework::ServiceProvider::SimpleAckStrategyHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L278)

```cpp
using SimpleAckStrategyHandler =
                std::function<bool(const RequestMessage&)>;
```

### API-b59ac53b3def · ndn_service_framework::ServiceProvider::RequestHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L281)

```cpp
using RequestHandler =
                std::function<ResponseMessage(const ndn::Name& requesterIdentity,
                                              const ndn::Name& providerName,
                                              const ndn::Name& serviceName,
                                              const ndn::Name& requestId,
                                              const RequestMessage& requestMessage)>;
```

### API-70edab4a4925 · ndn_service_framework::ServiceProvider::StreamingHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L288)

```cpp
using StreamingHandler =
                std::function<void(const ndn::Name& requesterIdentity,
                                   const ndn::Name& providerName,
                                   const ndn::Name& serviceName,
                                   const ndn::Name& requestId,
                                   const RequestMessage& requestMessage,
                                   StreamedResponseWriter<ndn::Buffer, ndn::Buffer>& writer)>;
```

### API-aeb686c8e90f · ndn_service_framework::ServiceProvider::DeploymentPrepareHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L300)

```cpp
using DeploymentPrepareHandler = std::function<ProviderReadyMessage(
                const ndn::Name& requesterIdentity,
                const ndn::Name& providerIdentity,
                const ndn::Name& serviceName,
                const ndn::Name& requestId,
                const RequestMessage& request,
                const DeploymentPlan& plan,
                const std::string& selectionDigest)>;
```

原始接口说明：

```text
/** Application-owned model preparation hook for the generic
             * selection-gated deployment protocol. The Core invokes it only
             * after a valid Selection carrying DeploymentPlan. Returning a
             * ProviderReadyMessage does not authorize handler execution. */
```

### API-c5f32f647af4 · ndn_service_framework::ServiceProvider::ProviderReadyPublisher

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L308)

```cpp
using ProviderReadyPublisher = std::function<void(
                const ndn::Name& requesterIdentity,
                const ProviderReadyMessage& ready)>;
```

### API-7ab9d59592ba · ndn_service_framework::ServiceProvider::R1SelectionDecisionHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L313)

```cpp
using R1SelectionDecisionHandler =
                std::function<SelectionDecisionReceipt(const SelectionDecision&)>;
```

原始接口说明：

```text
/** Application-owned reservation transition. Core authenticates
             * and fences the exact-target decision before invoking it. */
```

### API-178d62a2748b · ndn_service_framework::ServiceProvider::R1ReservationTerminalHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L315)

```cpp
using R1ReservationTerminalHandler =
                std::function<void(const std::string& reservationId,
                                   const std::string& cause)>;
```

### API-61fbd5719e6f · ndn_service_framework::ServiceProvider::setDeploymentPrepareHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L319)

```cpp
void setDeploymentPrepareHandler(DeploymentPrepareHandler handler);
```

### API-daabf1ba5a5f · ndn_service_framework::ServiceProvider::setProviderReadyPublisher

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L320)

```cpp
void setProviderReadyPublisher(ProviderReadyPublisher publisher);
```

### API-87b397209e52 · ndn_service_framework::ServiceProvider::setR1SelectionDecisionHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L321)

```cpp
void setR1SelectionDecisionHandler(
                const ndn::Name& serviceName,
                R1SelectionDecisionHandler handler);
```

### API-d3caa5945319 · ndn_service_framework::ServiceProvider::setR1ReservationTerminalHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L324)

```cpp
void setR1ReservationTerminalHandler(
                const ndn::Name& serviceName,
                R1ReservationTerminalHandler handler);
```

### API-665cd00696f0 · ndn_service_framework::ServiceProvider::setGenericSelectionTxnStore

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L327)

```cpp
void setGenericSelectionTxnStore(
                std::shared_ptr<GenericSelectionTxnStore> store);
```

### API-25ef7db11552 · ndn_service_framework::ServiceProvider::registerOpaqueSelectionParticipant

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L329)

```cpp
void registerOpaqueSelectionParticipant(
                const ndn::Name& serviceName,
                std::shared_ptr<OpaqueSelectionParticipant> participant);
```

### API-6c446accd4b9 · ndn_service_framework::ServiceProvider::acceptExecutionActivate

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L332)

```cpp
bool acceptExecutionActivate(const ExecutionActivateMessage& activation,
                                         std::string* rejectionReason = nullptr);
```

### API-79c267e38770 · ndn_service_framework::ServiceProvider::SimpleRequestHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L335)

```cpp
using SimpleRequestHandler =
                std::function<ResponseMessage(const RequestMessage& requestMessage)>;
```

### API-9dfd1678f6f2 · ndn_service_framework::ServiceProvider::ServiceMode

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L338)

```cpp
enum class ServiceMode
```

### API-16230ab3f669 · ndn_service_framework::ServiceProvider::ServiceMode::Normal

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L340)

```cpp
Normal
```

### API-bbe9fa0fee6d · ndn_service_framework::ServiceProvider::ServiceMode::Targeted

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L343)

```cpp
Targeted
```

原始接口说明：

```text
// Targeted services accept Request->Response invocation from
// a requester that already names this provider as the target.
```

### API-6ad843811658 · ndn_service_framework::ServiceProvider::ServiceInvocationMode

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L346)

```cpp
enum class ServiceInvocationMode
```

### API-5d8ba1d23fa4 · ndn_service_framework::ServiceProvider::ServiceInvocationMode::NormalOnly

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L348)

```cpp
NormalOnly
```

### API-5d4e58a8c664 · ndn_service_framework::ServiceProvider::ServiceInvocationMode::TargetedOnly

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L349)

```cpp
TargetedOnly
```

### API-b0b02bf1a133 · ndn_service_framework::ServiceProvider::ServiceInvocationMode::NormalAndTargeted

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L350)

```cpp
NormalAndTargeted
```

### API-01c16748f935 · ndn_service_framework::ServiceProvider::CollaborationAssignment

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L353)

```cpp
struct CollaborationAssignment
```

### API-c3a5ab4d248d · ndn_service_framework::ServiceProvider::CollaborationAssignment::role

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L355)

```cpp
CollaborationRole role;
```

### API-d9f6546e7025 · ndn_service_framework::ServiceProvider::CollaborationAssignment::service

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L356)

```cpp
ServiceName service;
```

### API-f854b9af5123 · ndn_service_framework::ServiceProvider::CollaborationAssignment::assignedArtifact

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L357)

```cpp
ndn::Name assignedArtifact;
```

### API-c960ed622753 · ndn_service_framework::ServiceProvider::CollaborationAssignment::artifactDataName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L358)

```cpp
ndn::Name artifactDataName;
```

### API-1d554d2af18e · ndn_service_framework::ServiceProvider::CollaborationAssignment::requiresProvisioning

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L359)

```cpp
bool requiresProvisioning = false;
```

### API-a64dc0b6ca4a · ndn_service_framework::ServiceProvider::CollaborationAssignment::provisioningTimeoutMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L360)

```cpp
int provisioningTimeoutMs = 0;
```

### API-7f104569556f · ndn_service_framework::ServiceProvider::CollaborationAssignment::assignmentPayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L361)

```cpp
ndn::Buffer assignmentPayload;
```

### API-c5640323a547 · ndn_service_framework::ServiceProvider::CollaborationAssignment::scopeKeys

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L362)

```cpp
std::map<KeyScope, ndn::Buffer> scopeKeys;
```

### API-8953b46afbf1 · ndn_service_framework::ServiceProvider::CollaborationAssignment::scopeKeyDataNames

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L363)

```cpp
std::map<KeyScope, ndn::Name> scopeKeyDataNames;
```

### API-741a010b6201 · ndn_service_framework::ServiceProvider::CollaborationAssignment::roleProviders

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L364)

```cpp
std::map<CollaborationRole, ndn::Name> roleProviders;
```

### API-5a10e135387a · ndn_service_framework::ServiceProvider::CollaborationAssignment::artifactPayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L365)

```cpp
ndn::Buffer artifactPayload;
```

### API-5a1d0ae21f51 · ndn_service_framework::ServiceProvider::CollaborationAssignment::selectionDigest

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L366)

```cpp
std::string selectionDigest;
```

### API-890222be5acf · ndn_service_framework::ServiceProvider::CollaborationData

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L369)

```cpp
struct CollaborationData
```

### API-b9db44e62184 · ndn_service_framework::ServiceProvider::CollaborationData::sessionId

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L371)

```cpp
SessionId sessionId;
```

### API-0e0119d93a02 · ndn_service_framework::ServiceProvider::CollaborationData::keyScope

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L372)

```cpp
KeyScope keyScope;
```

### API-32d83eacd2dc · ndn_service_framework::ServiceProvider::CollaborationData::topic

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L373)

```cpp
Topic topic;
```

### API-3b9ef6446efa · ndn_service_framework::ServiceProvider::CollaborationData::producer

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L374)

```cpp
ndn::Name producer;
```

### API-5f61cf3f57e5 · ndn_service_framework::ServiceProvider::CollaborationData::producerRole

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L375)

```cpp
CollaborationRole producerRole;
```

### API-7578e2559d21 · ndn_service_framework::ServiceProvider::CollaborationData::sequence

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L376)

```cpp
uint64_t sequence = 0;
```

### API-096db9afb2e9 · ndn_service_framework::ServiceProvider::CollaborationData::payload

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L377)

```cpp
ndn::Buffer payload;
```

### API-7b910a6cf76e · ndn_service_framework::ServiceProvider::CollaborationContext

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L380)

```cpp
class CollaborationContext
```

### API-d6b7fd598179 · ndn_service_framework::ServiceProvider::CollaborationContext::CollaborationContext

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L383)

```cpp
CollaborationContext(ServiceProvider& provider,
                                     ndn::Name requesterName,
                                     ndn::Name requestId,
                                     RequestMessage requestMessage,
                                     CollaborationAssignment assignment);
```

### API-83e364f2acf2 · ndn_service_framework::ServiceProvider::CollaborationContext::sessionId

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L389)

```cpp
SessionId sessionId() const;
```

### API-14b4581c7643 · ndn_service_framework::ServiceProvider::CollaborationContext::requesterName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L390)

```cpp
ndn::Name requesterName() const;
```

### API-c2f9f38fd3db · ndn_service_framework::ServiceProvider::CollaborationContext::role

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L391)

```cpp
CollaborationRole role() const;
```

### API-d5874b564cd8 · ndn_service_framework::ServiceProvider::CollaborationContext::localProvider

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L392)

```cpp
ndn::Name localProvider() const;
```

### API-7ac537ba9844 · ndn_service_framework::ServiceProvider::CollaborationContext::assignment

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L393)

```cpp
const CollaborationAssignment& assignment() const;
```

### API-950845589f40 · ndn_service_framework::ServiceProvider::CollaborationContext::hasArtifact

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L395)

```cpp
bool hasArtifact(const ndn::Name& artifactName) const;
```

### API-4c67b6f9c634 · ndn_service_framework::ServiceProvider::CollaborationContext::fetchArtifact

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L396)

```cpp
bool fetchArtifact(const ndn::Name& artifactName, int timeoutMs);
```

### API-411e3d9b70f2 · ndn_service_framework::ServiceProvider::CollaborationContext::getArtifact

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L397)

```cpp
std::optional<ndn::Buffer> getArtifact(const ndn::Name& artifactName) const;
```

### API-3803173cdf7c · ndn_service_framework::ServiceProvider::CollaborationContext::fetchEncryptedLargeData

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L398)

```cpp
std::optional<ndn::Buffer> fetchEncryptedLargeData(
                    const ndn::Name& dataName,
                    const ndn::Name& serviceName = ndn::Name());
```

### API-7a9e8ccd41c0 · ndn_service_framework::ServiceProvider::CollaborationContext::fail

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L401)

```cpp
void fail(const std::string& reason);
```

### API-0b50bd859960 · ndn_service_framework::ServiceProvider::CollaborationContext::publish

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L403)

```cpp
void publish(KeyScope keyScope,
                             Topic topic,
                             const ndn::Buffer& payload);
```

### API-cafe328c439c · ndn_service_framework::ServiceProvider::CollaborationContext::publishLarge

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L417)

```cpp
ndn::Name publishLarge(KeyScope keyScope,
                                       Topic topic,
                                       const ndn::Buffer& payload,
                                       size_t maxSegmentSize = 7000,
                                       int freshnessMs = 60000);
```

原始接口说明：

```text
/**
                 * Publish one exact-name segmented object for collaboration.
                 *
                 * Use this family for large static or planned objects such as
                 * files, model artifacts, catalog snapshots, recordings, and
                 * DI tensor bundles. Consumers retrieve the returned name with
                 * fetchLarge(), which uses segmented Data / SegmentFetcher-style
                 * exact-name retrieval. Do not use StreamChunk for these objects
                 * unless an application explicitly needs a metadata-envelope
                 * experiment.
                 */
```

### API-cc998001fc90 · ndn_service_framework::ServiceProvider::CollaborationContext::publishLargeNamed

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L429)

```cpp
ndn::Name publishLargeNamed(KeyScope keyScope,
                                            const ndn::Name& dataName,
                                            const ndn::Buffer& payload,
                                            size_t maxSegmentSize = 7000,
                                            int freshnessMs = 60000);
```

原始接口说明：

```text
/**
                 * Publish one segmented object under a caller-chosen exact name.
                 *
                 * This is the preferred collaboration primitive when a plan or
                 * manifest already assigns the Data name, as in DI activation
                 * exchange. It is separate from the continuous stream substrate.
                 */
```

### API-ec7fc0c3358b · ndn_service_framework::ServiceProvider::CollaborationContext::fetchLarge

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L441)

```cpp
std::optional<ndn::Buffer> fetchLarge(const ndn::Name& dataName,
                                                      KeyScope keyScope,
                                                      int timeoutMs);
```

原始接口说明：

```text
/**
                 * Fetch one exact-name segmented collaboration object.
                 *
                 * This is the large-object counterpart to publishLarge() and
                 * publishLargeNamed(). It is intended for exact-name object
                 * retrieval, not continuous stream consumption.
                 */
```

### API-1c23b0b638a5 · ndn_service_framework::ServiceProvider::CollaborationContext::fetchLarge

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L444)

```cpp
std::optional<ndn::Buffer> fetchLarge(const ndn::Name& dataName,
                                                      KeyScope keyScope,
                                                      int timeoutMs,
                                                      std::size_t expectedSegments);
```

### API-223b85ff9b9d · ndn_service_framework::ServiceProvider::CollaborationContext::publishDataV1Segments

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L455)

```cpp
bool publishDataV1Segments(
                    KeyScope keyScope,
                    const std::vector<std::pair<ndn::Name, ndn::Buffer>>& segments,
                    int freshnessMs = 60000);
```

原始接口说明：

```text
/**
                 * Publish request-scoped NDNSF_DATA_V1 segments through the
                 * SVSPubSub data path.  Each pair contains the complete
                 * segment Data name and the already-authenticated segment
                 * wire bytes; the transport must not wrap them in a second
                 * large-object envelope.
                 */
```

### API-56623a5f6614 · ndn_service_framework::ServiceProvider::CollaborationContext::fetchDataV1Segments

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L468)

```cpp
std::optional<std::vector<ndn::Buffer>> fetchDataV1Segments(
                    KeyScope keyScope,
                    const ndn::Name& producerPrefix,
                    std::uint64_t operationIndex,
                    const std::string& producerRank,
                    const std::string& tensorDigest,
                    std::size_t expectedSegments,
                    std::size_t maxSegments,
                    int timeoutMs,
                    std::function<std::size_t(const ndn::Buffer&)>
                        segmentCountDecoder = {},
                    DataV1SegmentNameFilter nameFilter = {});
```

原始接口说明：

```text
/**
                 * Fetch request-scoped NDNSF_DATA_V1 segment wires from a
                 * Provider's SVS publication stream.  The returned vector is
                 * ordered by segment number and contains no plaintext.
                 * ``expectedSegments == 0`` enables bounded in-subscription
                 * discovery: the caller-supplied decoder reads segment zero's
                 * authenticated manifest count, after which this same fetch
                 * waits for the exact complete segment set.
                 */
```

### API-6c2535c79faa · ndn_service_framework::ServiceProvider::CollaborationContext::publishSignedExactData

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L483)

```cpp
bool publishSignedExactData(
                    KeyScope keyScope,
                    const std::vector<std::pair<ndn::Name, ndn::Buffer>>& objects,
                    int freshnessMs = 60000);
```

原始接口说明：

```text
/** Publish only the exact signed Data names authorized by a
                 * sealed V3 mayPublish contract. No SVS notification or
                 * process-local sequence is introduced. */
```

### API-821efa7f0fd1 · ndn_service_framework::ServiceProvider::CollaborationContext::fetchSignedExactData

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L491)

```cpp
std::optional<ndn::Buffer> fetchSignedExactData(
                    KeyScope keyScope,
                    const ndn::Name& dataName,
                    const ndn::Name& expectedProducer,
                    int timeoutMs,
                    std::function<bool()> shouldCancel = {});
```

原始接口说明：

```text
/** Express and, on timeout/Nack, re-express the same exact
                 * Interest name until the bounded deadline. The returned
                 * content is released only after trust-schema and expected
                 * producer-identity validation. */
```

### API-a9702dd252b6 · ndn_service_framework::ServiceProvider::CollaborationContext::subscribe

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L497)

```cpp
void subscribe(KeyScope keyScope,
                               Topic topicPrefix,
                               std::function<void(const CollaborationData&)> onData);
```

### API-2c0d005f35d0 · ndn_service_framework::ServiceProvider::CollaborationContext::subscribe

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L500)

```cpp
void subscribe(KeyScope keyScope,
                               Topic topicPrefix,
                               std::function<void(CollaborationContext&,
                                                  const CollaborationData&)> onData);
```

### API-0ab753bffde0 · ndn_service_framework::ServiceProvider::CollaborationContext::allowData

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L513)

```cpp
void allowData(KeyScope keyScope, Topic topicPrefix);
```

原始接口说明：

```text
/**
                 * Allow encrypted collaboration Data for this request only
                 * when its scope and topic match the supplied binding.
                 *
                 * This is useful for applications that consume through
                 * waitOne()/waitFor(): the receive filter is installed before
                 * any Data is decrypted, so unrelated role traffic is dropped
                 * without attempting authentication with the wrong scope key.
                 */
```

### API-ffd5356bbf09 · ndn_service_framework::ServiceProvider::CollaborationContext::waitOne

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L514)

```cpp
std::optional<CollaborationData> waitOne(KeyScope keyScope,
                                                         Topic topicPrefix,
                                                         int timeoutMs);
```

### API-3a50c682da55 · ndn_service_framework::ServiceProvider::CollaborationContext::waitFor

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L517)

```cpp
std::vector<CollaborationData> waitFor(KeyScope keyScope,
                                                       Topic topicPrefix,
                                                       size_t minCount,
                                                       int timeoutMs);
```

### API-10ce666958e9 · ndn_service_framework::ServiceProvider::CollaborationContext::reportOperationStatus

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L521)

```cpp
void reportOperationStatus(ServiceOperationStatus status);
```

### API-208502f7163d · ndn_service_framework::ServiceProvider::CollaborationContext::publishFinalResponse

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L522)

```cpp
void publishFinalResponse(const ndn::Buffer& payload);
```

### API-fd0dd35571e5 · ndn_service_framework::ServiceProvider::CollaborationContext::isStreamed

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L528)

```cpp
bool isStreamed() const;
```

原始接口说明：

```text
/** Streamed DI bridge over the already-selected collaboration.
                 * These methods are valid only when the original Request carried
                 * StreamRequestOptions and Core attached its publisher after
                 * Selection; they never allocate another request or plan. */
```

### API-c6a70d5d1250 · ndn_service_framework::ServiceProvider::CollaborationContext::publishStreamEvent

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L529)

```cpp
uint64_t publishStreamEvent(const ndn::Buffer& payload);
```

### API-961196d5f2f9 · ndn_service_framework::ServiceProvider::CollaborationContext::finishStream

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L530)

```cpp
bool finishStream(const ndn::Buffer& payload,
                                  StreamFinishReason reason);
```

### API-07cda833f107 · ndn_service_framework::ServiceProvider::CollaborationContext::failStream

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L532)

```cpp
bool failStream(StreamedInvocationErrorCode code,
                                const std::string& message);
```

### API-e4dd065df0fd · ndn_service_framework::ServiceProvider::CollaborationContext::completeRole

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L541)

```cpp
bool completeRole();
```

原始接口说明：

```text
/**
                 * Mark this selected collaboration role complete without
                 * publishing a user-facing Response.  This is required for
                 * non-final streamed DI roles: only the final role owns End
                 * and the terminal Response, while every other role must
                 * still release its provider-side pending request/lease.
                 */
```

### API-0334bb398005 · ndn_service_framework::ServiceProvider::CollaborationContext::streamCancelled

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L542)

```cpp
bool streamCancelled() const;
```

### API-5aaa6dc862d1 · ndn_service_framework::ServiceProvider::CollaborationContext::streamRemainingDeadline

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L543)

```cpp
std::chrono::milliseconds streamRemainingDeadline() const;
```

### API-f185e2e3d618 · ndn_service_framework::ServiceProvider::CollaborationHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L555)

```cpp
using CollaborationHandler =
                std::function<void(CollaborationContext& ctx,
                                   const RequestMessage& initialRequest)>;
```

### API-d0487406beff · ndn_service_framework::ServiceProvider::LocalPublicationHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L561)

```cpp
using LocalPublicationHandler =
                std::function<void(const ndn::Name& messageName,
                                   const ndn::Buffer& wire)>;
```

原始接口说明：

```text
// Test-only publication boundary for LocalMockTag integration
// fixtures. Production instances publish through SVSPubSub.
```

### API-fb897b30d2d5 · ndn_service_framework::ServiceProvider::StreamPublicationInterceptorForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceProvider.hpp#L572)

```cpp
using StreamPublicationInterceptorForTest =
                std::function<bool(const ndn::Data& data)>;
```

原始接口说明：

```text
/**
             * Test-only hook at the real streamed-event publication boundary.
             * Returning false suppresses the SVS publication after the exact
             * signed Data has been retained in the Provider IMS.  This lets
             * integration tests exercise the production exact-Interest retry
             * path without intercepting DummyFace traffic.
             */
```

### API-d86d01299940 · ndn_service_framework::ServiceProvider::StreamRetentionInterceptorForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceProvider.hpp#L576)

```cpp
using StreamRetentionInterceptorForTest =
                std::function<bool(const ndn::Data& data)>;
```

原始接口说明：

```text
/** Test-only hook that can suppress retention before publication. */
```

### API-1ec7359057d4 · ndn_service_framework::ServiceProvider::StreamRetentionExpiryObserverForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceProvider.hpp#L580)

```cpp
using StreamRetentionExpiryObserverForTest =
                std::function<void(const ndn::Name& eventName)>;
```

原始接口说明：

```text
/** Test-only observer fired after a retained event is evicted. */
```

### API-b4da87745e7d · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleState

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L583)

```cpp
enum class ProviderRequestLifecycleState
```

### API-3b2c8e983326 · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleState::REQUEST_OBSERVED

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L585)

```cpp
REQUEST_OBSERVED
```

### API-6885cb2775d6 · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleState::ACK_ADMISSION_CHECKED

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L586)

```cpp
ACK_ADMISSION_CHECKED
```

### API-dcde1fd9943c · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleState::ACK_SUPPRESSED_OVERLOAD

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L587)

```cpp
ACK_SUPPRESSED_OVERLOAD
```

### API-7e5ce03f5baa · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleState::ACK_PUBLISHED

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L588)

```cpp
ACK_PUBLISHED
```

### API-ce5f9803c72a · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleState::SELECTION_RECEIVED

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L589)

```cpp
SELECTION_RECEIVED
```

### API-00ee5ebf2c24 · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleState::EXECUTION_STARTED

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L590)

```cpp
EXECUTION_STARTED
```

### API-6f5158f924d2 · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleState::EXECUTION_DONE

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L591)

```cpp
EXECUTION_DONE
```

### API-511b7dc29b47 · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleState::RESPONSE_PUBLISHED

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L592)

```cpp
RESPONSE_PUBLISHED
```

### API-b8ded0e9c64d · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleState::PROVIDER_REQUEST_EXPIRED

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L593)

```cpp
PROVIDER_REQUEST_EXPIRED
```

### API-60403e50e217 · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleStatus

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L596)

```cpp
struct ProviderRequestLifecycleStatus
```

### API-4bb4304b5e61 · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleStatus::requestId

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L598)

```cpp
ndn::Name requestId;
```

### API-364272f99517 · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleStatus::serviceName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L599)

```cpp
ndn::Name serviceName;
```

### API-abf5611325ef · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleStatus::providerName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L600)

```cpp
ndn::Name providerName;
```

### API-202c97cab04d · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleStatus::state

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L601)

```cpp
ProviderRequestLifecycleState state =
                    ProviderRequestLifecycleState::REQUEST_OBSERVED;
```

### API-c6ee796a2d11 · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleStatus::requestObservedTimestampUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L603)

```cpp
uint64_t requestObservedTimestampUs = 0;
```

### API-1f488bd6635c · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleStatus::ackAdmissionDecisionTimestampUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L604)

```cpp
uint64_t ackAdmissionDecisionTimestampUs = 0;
```

### API-2a6ddebbd834 · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleStatus::ackPublishedOrSuppressedTimestampUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L605)

```cpp
uint64_t ackPublishedOrSuppressedTimestampUs = 0;
```

### API-f4668346eaa1 · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleStatus::suppressionReason

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L606)

```cpp
std::string suppressionReason;
```

### API-654479366bd6 · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleStatus::providerPendingCountAtDecision

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L607)

```cpp
size_t providerPendingCountAtDecision = 0;
```

### API-dfe44583d33b · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleStatus::eventLoopLagUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L608)

```cpp
uint64_t eventLoopLagUs = 0;
```

### API-10706ebe9994 · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleStatus::selectionLagUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L609)

```cpp
uint64_t selectionLagUs = 0;
```

### API-673f32b0672c · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleStatus::selectionReceivedTimestampUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L610)

```cpp
uint64_t selectionReceivedTimestampUs = 0;
```

### API-0356ecd20748 · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleStatus::executionStartTimestampUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L611)

```cpp
uint64_t executionStartTimestampUs = 0;
```

### API-167c5acc5c22 · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleStatus::executionDoneTimestampUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L612)

```cpp
uint64_t executionDoneTimestampUs = 0;
```

### API-7ea68d5ebce8 · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleStatus::responsePublishedTimestampUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L613)

```cpp
uint64_t responsePublishedTimestampUs = 0;
```

### API-9eeefbec74ea · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleStatus::finalStatus

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L614)

```cpp
std::string finalStatus;
```

### API-51aef1c39cab · ndn_service_framework::ServiceProvider::ProviderRequestLifecycleCallback

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L617)

```cpp
using ProviderRequestLifecycleCallback =
                std::function<void(const ProviderRequestLifecycleStatus&)>;
```

### API-fbc519b20a27 · ndn_service_framework::ServiceProvider::LocalMockTag

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L620)

```cpp
struct LocalMockTag
```

### API-ce1b48a6d21f · ndn_service_framework::ServiceProvider::ServiceProvider

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L624)

```cpp
ServiceProvider(ndn::Face& face, ndn::Name group_prefix, ndn::security::Certificate identityCert, ndn::security::Certificate attrAuthorityCertificate,std::string trustSchemaPath);
```

### API-e39bddff5420 · ndn_service_framework::ServiceProvider::ServiceProvider

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L625)

```cpp
ServiceProvider(ndn::Face& face,
                            ndn::Name group_prefix,
                            ndn::security::Certificate encryptionCert,
                            ndn::security::Certificate signingCert,
                            ndn::security::Certificate attrAuthorityCertificate,
                            std::string trustSchemaPath);
```

### API-5fdb45f688cf · ndn_service_framework::ServiceProvider::ServiceProvider

public / test-helper；[源码](../../ndn-service-framework/ServiceProvider.hpp#L631)

```cpp
ServiceProvider(LocalMockTag,
                            ndn::Face& face,
                            ndn::Name group_prefix,
                            ndn::security::Certificate identityCert,
                            ndn::security::Certificate attrAuthorityCertificate,
                            std::string trustSchemaPath);
```

### API-627ba4315a41 · ndn_service_framework::ServiceProvider::ServiceProvider

public / test-helper；[源码](../../ndn-service-framework/ServiceProvider.hpp#L637)

```cpp
ServiceProvider(LocalMockTag,
                            ndn::Face& face,
                            ndn::Name group_prefix,
                            ndn::security::Certificate encryptionCert,
                            ndn::security::Certificate signingCert,
                            ndn::security::Certificate attrAuthorityCertificate,
                            std::string trustSchemaPath);
```

### API-3d23890199d6 · ndn_service_framework::ServiceProvider::~ServiceProvider

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L644)

```cpp
virtual ~ServiceProvider();
```

### API-636a142d4a9e · ndn_service_framework::ServiceProvider::init

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L646)

```cpp
void init();
```

### API-0fa2aff03b6c · ndn_service_framework::ServiceProvider::attachLocalMockPubSubForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceProvider.hpp#L657)

```cpp
void attachLocalMockPubSubForTest(
                std::shared_ptr<ndn::svs::SVSPubSub> pubSub);
```

原始接口说明：

```text
/**
             * Install an SVSPubSub instance on a LocalMock provider.
             *
             * This is intentionally restricted to the LocalMock construction
             * path so in-process integration tests can exercise the same
             * production collaboration transport without entering NAC-ABE
             * bootstrap. Normal providers create their SVS instance during
             * construction and must not call this hook.
             */
```

### API-146ced26b638 · ndn_service_framework::ServiceProvider::installLocalMockDataIngressForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceProvider.hpp#L668)

```cpp
void installLocalMockDataIngressForTest();
```

原始接口说明：

```text
/**
             * Install only the LocalMock exact-Data/IMS interest filters.
             *
             * This keeps the LocalMock publication callback path intact while
             * allowing a test to fetch Data objects retained in the Provider
             * IMS.  It intentionally does not attach an SVSPubSub instance or
             * change publication semantics.
             */
```

### API-c6aa1cbd14e9 · ndn_service_framework::ServiceProvider::cacheHybridReceiveKeyForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceProvider.hpp#L676)

```cpp
void cacheHybridReceiveKeyForTest(const std::string& keyId,
                                              const std::string& epochId,
                                              const ndn::Buffer& key);
```

原始接口说明：

```text
/**
             * Seed a receive key for a LocalMock integration test.  This keeps
             * the test on the real HybridMessageEnvelope ingress while
             * intentionally omitting the controller/NAC bootstrap from the
             * transport gate.
             */
```

### API-15f750d9fff2 · ndn_service_framework::ServiceProvider::prepareHybridSendKeyForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceProvider.hpp#L686)

```cpp
HybridMessageKey prepareHybridSendKeyForTest(
                const ndn::Name& serviceName,
                const std::string& messageType);
```

原始接口说明：

```text
/**
             * Prime one LocalMock outbound epoch and return the matching key to
             * the integration fixture.  The fixture installs it in the peer's
             * receive cache, modelling a completed NAC-ABE MessageKey exchange
             * while retaining the real encrypted ACK/Response wire path.
             */
```

### API-9a8269ba68e1 · ndn_service_framework::ServiceProvider::markHybridResponseKeyWrappedForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceProvider.hpp#L697)

```cpp
void markHybridResponseKeyWrappedForTest(
                const ndn::Name& serviceName);
```

原始接口说明：

```text
/**
             * Mark the LocalMock response epoch as already wrapped.  The
             * in-process transport gate has no controller/NAC-ABE producer,
             * so this lets it verify the real encrypted Response publication
             * without silently replacing the post-Selection path with a
             * plaintext callback.
             */
```

### API-e73b451fd915 · ndn_service_framework::ServiceProvider::useSigningKeyChainForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceProvider.hpp#L703)

```cpp
void useSigningKeyChainForTest(ndn::KeyChain& keyChain);
```

原始接口说明：

```text
/** Bind LocalMock signing to the fixture KeyChain that owns the
             * supplied certificate and private key. Production providers keep
             * using their process KeyChain. */
```

### API-d32b7fdd69e3 · ndn_service_framework::ServiceProvider::isNacConsumerReadyForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceProvider.hpp#L707)

```cpp
bool isNacConsumerReadyForTest();
```

原始接口说明：

```text
/** Return whether the active LocalMock NAC-ABE Consumer has
             * obtained its DKEY. */
```

### API-5df29a8b6169 · ndn_service_framework::ServiceProvider::setStreamPublicationInterceptorForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceProvider.hpp#L710)

```cpp
void setStreamPublicationInterceptorForTest(
                StreamPublicationInterceptorForTest interceptor);
```

原始接口说明：

```text
/** Install or clear the LocalMock streamed-event publication hook. */
```

### API-d2a786ccc4ed · ndn_service_framework::ServiceProvider::setStreamRetentionInterceptorForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceProvider.hpp#L712)

```cpp
void setStreamRetentionInterceptorForTest(
                StreamRetentionInterceptorForTest interceptor);
```

### API-40d42e2c80b2 · ndn_service_framework::ServiceProvider::setStreamRetentionExpiryObserverForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceProvider.hpp#L714)

```cpp
void setStreamRetentionExpiryObserverForTest(
                StreamRetentionExpiryObserverForTest observer);
```

### API-2538c1e16987 · ndn_service_framework::ServiceProvider::publishStreamPacketForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceProvider.hpp#L720)

```cpp
void publishStreamPacketForTest(const ndn::Data& data);
```

原始接口说明：

```text
/** Re-publish one already signed streamed Data packet through the
             * production SVS endpoint.  This is test-only fault-controller
             * plumbing used to inject duplicate and reordered publications;
             * it does not re-sign or modify the packet. */
```

### API-5ac1c9d9ae7e · ndn_service_framework::ServiceProvider::streamPublisherHighWaterMarkForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceProvider.hpp#L721)

```cpp
size_t streamPublisherHighWaterMarkForTest(
                const ndn::Name& requesterName,
                const ndn::Name& serviceName,
                const ndn::Name& requestId);
```

### API-ce3dcebc7a71 · ndn_service_framework::ServiceProvider::getName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L726)

```cpp
ndn::Name getName();
```

### API-3f9449d524db · ndn_service_framework::ServiceProvider::getSigningKeyName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L731)

```cpp
ndn::Name getSigningKeyName() const;
```

原始接口说明：

```text
/** Public names of the certificate used for Provider-signed Data.
             * These expose no private key material and let an external
             * application signer select the exact Provider signing key. */
```

### API-e3de334d06fa · ndn_service_framework::ServiceProvider::getSigningCertificateName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L732)

```cpp
ndn::Name getSigningCertificateName() const;
```

### API-e784390ed60d · ndn_service_framework::ServiceProvider::createLiveStream

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L735)

```cpp
std::shared_ptr<LiveStreamPublisher>
            createLiveStream(const LiveStreamDefinition& definition);
```

原始接口说明：

```text
/** Create the sole Core-owned semantic-name live-stream publisher. */
```

### API-dddfa2f74d6c · ndn_service_framework::ServiceProvider::createStream

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L739)

```cpp
std::shared_ptr<StreamPublisher>
            createStream(const StreamConfig& config);
```

原始接口说明：

```text
/** Create the additive high-level stream facade. */
```

### API-45cb8bdafd65 · ndn_service_framework::ServiceProvider::fetchPermissionsFromController

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L742)

```cpp
void fetchPermissionsFromController(const ndn::Name& controllerPrefix);
```

### API-f1e11f19bc51 · ndn_service_framework::ServiceProvider::refreshProviderPermissionsAfterAdvance

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L748)

```cpp
void refreshProviderPermissionsAfterAdvance(
                const ndn::Name& serviceName);
```

原始接口说明：

```text
/** Re-fetch the identity-wide ProviderPermission renewal after an
             * already-installed policy status advanced to a newer
             * ControllerVersion (a revocation discovered through the
             * scheduled refresh).  Cold-bootstrap first installs skip this:
             * the enclosing bootstrap already fetched permissions. */
```

### API-0266b035080d · ndn_service_framework::ServiceProvider::applyPermissionResponse

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L750)

```cpp
void applyPermissionResponse(const PermissionResponse& response);
```

### API-213547b17772 · ndn_service_framework::ServiceProvider::installControllerStatus

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L753)

```cpp
bool installControllerStatus(const PolicyStatusData& status,
                                         bool controllerSignatureValid = true);
```

原始接口说明：

```text
/** Install a Controller-signed policy status after the enclosing
             * Data has passed the configured trust validator. */
```

### API-2012f4274296 · ndn_service_framework::ServiceProvider::persistAcceptedControllerStatus

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L757)

```cpp
void persistAcceptedControllerStatus(
                const ndn::Data& validatedData,
                const PolicyStatusData& status);
```

原始接口说明：

```text
/** Persist an accepted Controller status Data (opt-in durable
             * mode, FR-039); no-op when the store is disabled. */
```

### API-12e2ce6ae87a · ndn_service_framework::ServiceProvider::restorePersistedRuntimeStatuses

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L765)

```cpp
void restorePersistedRuntimeStatuses();
```

原始接口说明：

```text
/** Restore persisted per-service statuses after startup (opt-in
             * durable mode, FR-039): unexpired, never-superseded records are
             * re-verified against the configured trust anchor and seeded as
             * authority; each seed schedules a bounded online confirmation
             * refresh whose Controller answer is authority. */
```

### API-59e47caec038 · ndn_service_framework::ServiceProvider::getControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L766)

```cpp
std::optional<ControllerVersion> getControllerVersion() const;
```

### API-46c8af736b00 · ndn_service_framework::ServiceProvider::getControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L767)

```cpp
std::optional<ControllerVersion> getControllerVersion(
                const ndn::Name& serviceName) const;
```

### API-f8407c1adb09 · ndn_service_framework::ServiceProvider::hasProviderPermissionForService

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L775)

```cpp
bool hasProviderPermissionForService(const ndn::Name& serviceName) const;
```

原始接口说明：

```text
/**
             * Return whether the current provider authorization table contains
             * permission for the requested service.  This is a runtime
             * readiness query, not an authorization bypass; request handling
             * still performs its own permission check.
             */
```

### API-b8d7d269d60d · ndn_service_framework::ServiceProvider::getCurrentPolicyEpoch

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L776)

```cpp
size_t getCurrentPolicyEpoch() const;
```

### API-7d5890dd18ca · ndn_service_framework::ServiceProvider::getCurrentPolicyEpoch

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L777)

```cpp
size_t getCurrentPolicyEpoch(const ndn::Name& serviceName) const;
```

### API-c6dea62e6c08 · ndn_service_framework::ServiceProvider::handlePermissionResponseData

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L778)

```cpp
static bool handlePermissionResponseData(const ndn::Data& data,
                                                     const ndn::Name& identity,
                                                     ndn::KeyChain& keyChain,
                                                     ServiceAuthorizationTable& permissionTable);
```

### API-c2929804e2e4 · ndn_service_framework::ServiceProvider::getPendingRequestCountForTesting

public / test-helper；[源码](../../ndn-service-framework/ServiceProvider.hpp#L783)

```cpp
size_t getPendingRequestCountForTesting() const;
```

### API-439a67325b0d · ndn_service_framework::ServiceProvider::getSelectedOutstandingRequestCountForTesting

public / test-helper；[源码](../../ndn-service-framework/ServiceProvider.hpp#L784)

```cpp
size_t getSelectedOutstandingRequestCountForTesting() const;
```

### API-51c75a6654a2 · ndn_service_framework::ServiceProvider::getPendingProviderTokenCountForTesting

public / test-helper；[源码](../../ndn-service-framework/ServiceProvider.hpp#L785)

```cpp
size_t getPendingProviderTokenCountForTesting() const;
```

### API-ef92f681a693 · ndn_service_framework::ServiceProvider::getCleanupInvocationCountForTesting

public / test-helper；[源码](../../ndn-service-framework/ServiceProvider.hpp#L786)

```cpp
size_t getCleanupInvocationCountForTesting() const;
```

### API-95d045339c43 · ndn_service_framework::ServiceProvider::getTokenConsumeCountForTesting

public / test-helper；[源码](../../ndn-service-framework/ServiceProvider.hpp#L787)

```cpp
size_t getTokenConsumeCountForTesting() const;
```

### API-d78b692dfe66 · ndn_service_framework::ServiceProvider::setPendingRequestTimeoutGrace

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L788)

```cpp
void setPendingRequestTimeoutGrace(ndn::time::milliseconds grace);
```

### API-d36d3f04fde5 · ndn_service_framework::ServiceProvider::setPerformanceMode

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L789)

```cpp
void setPerformanceMode(bool enabled);
```

### API-112e5882a420 · ndn_service_framework::ServiceProvider::setHandlerThreads

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L790)

```cpp
void setHandlerThreads(size_t n);
```

### API-7f08525e103a · ndn_service_framework::ServiceProvider::getHandlerThreads

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L791)

```cpp
size_t getHandlerThreads() const;
```

### API-85d41d867877 · ndn_service_framework::ServiceProvider::getHandlerQueueDepth

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L792)

```cpp
size_t getHandlerQueueDepth() const;
```

### API-2051397730e9 · ndn_service_framework::ServiceProvider::setAckThreads

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L793)

```cpp
void setAckThreads(size_t n);
```

### API-c86f7a89aa26 · ndn_service_framework::ServiceProvider::getAckThreads

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L794)

```cpp
size_t getAckThreads() const;
```

### API-fc4bbc5ea043 · ndn_service_framework::ServiceProvider::getAckQueueDepth

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L795)

```cpp
size_t getAckQueueDepth() const;
```

### API-252f54bca3cd · ndn_service_framework::ServiceProvider::setUseTokens

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L796)

```cpp
void setUseTokens(bool enabled);
```

### API-e2dec08131c2 · ndn_service_framework::ServiceProvider::getUseTokens

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L797)

```cpp
bool getUseTokens() const;
```

### API-479aa59bf057 · ndn_service_framework::ServiceProvider::getHybridCryptoCounters

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L798)

```cpp
HybridCryptoCounters& getHybridCryptoCounters();
```

### API-feda0ee15189 · ndn_service_framework::ServiceProvider::setTimelineTrace

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L799)

```cpp
void setTimelineTrace(bool enabled);
```

### API-3e01b8d0cd73 · ndn_service_framework::ServiceProvider::setAdaptiveAckAdmission

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L800)

```cpp
void setAdaptiveAckAdmission(bool enabled);
```

### API-88831dde40d8 · ndn_service_framework::ServiceProvider::setProviderAckMaxPending

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L801)

```cpp
void setProviderAckMaxPending(size_t maxPending);
```

### API-cdab42063071 · ndn_service_framework::ServiceProvider::setProviderAckMaxEventLoopLag

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L802)

```cpp
void setProviderAckMaxEventLoopLag(ndn::time::milliseconds maxLag);
```

### API-cc1c094c8a74 · ndn_service_framework::ServiceProvider::setProviderAckMaxSelectionLag

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L803)

```cpp
void setProviderAckMaxSelectionLag(ndn::time::milliseconds maxLag);
```

### API-4e036980604d · ndn_service_framework::ServiceProvider::setProviderRequestLifecycleCallback

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L804)

```cpp
void setProviderRequestLifecycleCallback(
                ProviderRequestLifecycleCallback callback);
```

### API-6fb01034d8fc · ndn_service_framework::ServiceProvider::getProviderRequestStatus

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L806)

```cpp
std::optional<ProviderRequestLifecycleStatus>
            getProviderRequestStatus(const ndn::Name& requestId) const;
```

### API-0d47cf3ba1ad · ndn_service_framework::ServiceProvider::getActiveProviderRequestStatuses

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L808)

```cpp
std::vector<ProviderRequestLifecycleStatus>
            getActiveProviderRequestStatuses() const;
```

### API-cd4f1165dbe0 · ndn_service_framework::ServiceProvider::getSelectionExecutionStatus

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L810)

```cpp
std::optional<SelectionExecutionStatus>
            getSelectionExecutionStatus(const std::string& selectionDigest) const;
```

### API-bb6badb86888 · ndn_service_framework::ServiceProvider::getProviderAdmissionCounters

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L812)

```cpp
std::map<std::string, uint64_t> getProviderAdmissionCounters() const;
```

### API-5e04301e8c00 · ndn_service_framework::ServiceProvider::providerRequestLifecycleStateToString

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L813)

```cpp
static const char* providerRequestLifecycleStateToString(
                ProviderRequestLifecycleState state);
```

### API-cea2e1d1f4c3 · ndn_service_framework::ServiceProvider::publishServiceInfo

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L816)

```cpp
void publishServiceInfo(const ndn::Name& serviceName,
                                    int serviceLifetimeSeconds,
                                    std::map<std::string, std::string> serviceMetaInfo = {});
```

### API-97517e61aeaa · ndn_service_framework::ServiceProvider::updateNdnsdMeta

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L821)

```cpp
void updateNdnsdMeta(const std::string& key, const std::string& value);
```

原始接口说明：

```text
/// Update one key in the internal NDNSD meta dict (thread-safe).
```

### API-29cba35a8dbb · ndn_service_framework::ServiceProvider::setNdnsdMeta

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L824)

```cpp
void setNdnsdMeta(const std::map<std::string, std::string>& meta);
```

原始接口说明：

```text
/// Replace the entire internal NDNSD meta dict (thread-safe).
```

### API-c6af420209f4 · ndn_service_framework::ServiceProvider::startNdnsdPeriodicPublish

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L828)

```cpp
void startNdnsdPeriodicPublish(int intervalSeconds);
```

原始接口说明：

```text
/// Start periodic NDNSD heartbeat for all registered services.
/// Meta is read from the internal dict (updated via updateNdnsdMeta).
```

### API-3e88adaee0bc · ndn_service_framework::ServiceProvider::ServiceRegistration

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L840)

```cpp
class ServiceRegistration
```

### API-c3a3fd9da1b0 · ndn_service_framework::ServiceProvider::addScopedService

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L856)

```cpp
ServiceRegistration addScopedService(const ndn::Name& serviceName,
                                                 AckStrategyHandler ackHandler,
                                                 RequestHandler requestHandler,
                                                 ServiceInvocationMode invocationMode);
```

原始接口说明：

```text
/**
             * Register a service under exclusive scoped ownership: the
             * returned handle's generation is bound to every authenticated
             * Request this service accepts, so closing the handle stops late
             * ACK completion, Selection dispatch and queued execution from
             * publishing results for that generation.  The name must not be
             * occupied by any other registration (legacy or active scoped);
             * closed generations are drained before the check.  All legacy
             * addService/addCollaborationHandler signatures and behavior are
             * unchanged; legacy adds refuse to overwrite an active scoped
             * registration.  Like the legacy registration APIs, these may be
             * called only on the Face event thread or before the event loop
             * starts.
             */
```

### API-23e6ea05d4d6 · ndn_service_framework::ServiceProvider::addScopedService

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L860)

```cpp
ServiceRegistration addScopedService(const ndn::Name& serviceName,
                                                 AckStrategyHandler ackHandler,
                                                 RequestHandler requestHandler,
                                                 ServiceMode mode);
```

### API-33314e8f5bbb · ndn_service_framework::ServiceProvider::addScopedCollaborationHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L869)

```cpp
ServiceRegistration addScopedCollaborationHandler(
                const ndn::Name& serviceName,
                std::vector<CollaborationRole> allowedRoles,
                AckStrategyHandler ackHandler,
                CollaborationHandler handler);
```

原始接口说明：

```text
/**
             * Scoped variant of addCollaborationHandler with the same
             * generation binding and exclusivity rules as addScopedService.
             */
```

### API-5bf1f47bce59 · ndn_service_framework::ServiceProvider::OnRequest

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L875)

```cpp
void OnRequest(const ndn::svs::SVSPubSub::SubscriptionData &subscription);
```

### API-2c74fd2b18f6 · ndn_service_framework::ServiceProvider::addService

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L880)

```cpp
void addService(const ndn::Name& serviceName,
                            AckStrategyHandler ackHandler,
                            RequestHandler requestHandler);
```

原始接口说明：

```text
// After receiving service selection message, this function is called to consumeRequest.
// Generic dynamic providers can rely on this safe default; legacy generated providers
// may still override it for service-specific dispatch.
```

### API-ce6ad183c0ae · ndn_service_framework::ServiceProvider::addStreamingHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L888)

```cpp
void addStreamingHandler(const ndn::Name& serviceName,
                                     StreamingHandler handler);
```

原始接口说明：

```text
/** Register a generic streamed handler. The handler receives the
             * same authenticated Request/Selection context as unary dispatch;
             * the Core owns cursor, AEAD, signed Data publication and the
             * terminal Response. */
```

### API-f4246c574451 · ndn_service_framework::ServiceProvider::addStreamingHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L892)

```cpp
template<typename RequestT, typename EventT, typename ResponseT>
void addStreamingHandler(
                const ndn::Name& serviceName,
                std::function<void(const RequestT&,
                                   StreamedResponseWriter<EventT, ResponseT>&)> handler)
```

### API-db1a937c4083 · ndn_service_framework::ServiceProvider::addService

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L919)

```cpp
void addService(const ndn::Name& serviceName,
                            AckStrategyHandler ackHandler,
                            RequestHandler requestHandler,
                            ServiceMode mode);
```

### API-d26296f9148f · ndn_service_framework::ServiceProvider::addService

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L924)

```cpp
void addService(const ndn::Name& serviceName,
                            AckStrategyHandler ackHandler,
                            RequestHandler requestHandler,
                            ServiceInvocationMode invocationMode);
```

### API-7b0bf829c735 · ndn_service_framework::ServiceProvider::addService

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L929)

```cpp
void addService(const ndn::Name& serviceName,
                            LegacyAckStrategyHandler ackHandler,
                            RequestHandler requestHandler);
```

### API-ecfe6d2a4627 · ndn_service_framework::ServiceProvider::addService

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L933)

```cpp
void addService(const ndn::Name& serviceName,
                            RequestHandler requestHandler);
```

### API-9e97fc596ffd · ndn_service_framework::ServiceProvider::addService

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L936)

```cpp
void addService(const ndn::Name& serviceName,
                            AckStrategyHandler ackHandler,
                            SimpleRequestHandler requestHandler);
```

### API-bf6079c17933 · ndn_service_framework::ServiceProvider::addService

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L940)

```cpp
void addService(const ndn::Name& serviceName,
                            AckStrategyHandler ackHandler,
                            SimpleRequestHandler requestHandler,
                            ServiceInvocationMode invocationMode);
```

### API-f3ad4bab8cf5 · ndn_service_framework::ServiceProvider::addTargetedService

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L945)

```cpp
void addTargetedService(const ndn::Name& serviceName,
                                    RequestHandler requestHandler);
```

### API-395d98cbbd61 · ndn_service_framework::ServiceProvider::addTargetedService

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L948)

```cpp
void addTargetedService(const ndn::Name& serviceName,
                                    SimpleRequestHandler requestHandler);
```

### API-10f7220f7bc0 · ndn_service_framework::ServiceProvider::addService

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L951)

```cpp
void addService(const ndn::Name& serviceName,
                            LegacyAckStrategyHandler ackHandler,
                            SimpleRequestHandler requestHandler);
```

### API-2dc04fe0801d · ndn_service_framework::ServiceProvider::addService

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L955)

```cpp
void addService(const ndn::Name& serviceName,
                            SimpleAckStrategyHandler ackHandler,
                            RequestHandler requestHandler);
```

### API-967ffe7a4dbe · ndn_service_framework::ServiceProvider::addService

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L959)

```cpp
void addService(const ndn::Name& serviceName,
                            SimpleAckStrategyHandler ackHandler,
                            SimpleRequestHandler requestHandler);
```

### API-b29e85449c8b · ndn_service_framework::ServiceProvider::addService

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L963)

```cpp
void addService(const ndn::Name& serviceName,
                            SimpleAckStrategyHandler ackHandler,
                            SimpleRequestHandler requestHandler,
                            ServiceInvocationMode invocationMode);
```

### API-31fb9e95454b · ndn_service_framework::ServiceProvider::addCollaborationHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L968)

```cpp
void addCollaborationHandler(const ndn::Name& serviceName,
                                         AckStrategyHandler ackHandler,
                                         CollaborationHandler handler);
```

### API-defd0b71d2e8 · ndn_service_framework::ServiceProvider::addCollaborationHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L972)

```cpp
void addCollaborationHandler(const ndn::Name& serviceName,
                                         std::vector<CollaborationRole> allowedRoles,
                                         AckStrategyHandler ackHandler,
                                         CollaborationHandler handler);
```

### API-b1331efa596b · ndn_service_framework::ServiceProvider::addCollaborationHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L977)

```cpp
void addCollaborationHandler(const ndn::Name& serviceName,
                                         CollaborationHandler handler);
```

### API-8d4b04abb862 · ndn_service_framework::ServiceProvider::addCollaborationHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L980)

```cpp
void addCollaborationHandler(const ndn::Name& serviceName,
                                         std::vector<CollaborationRole> allowedRoles,
                                         CollaborationHandler handler);
```

### API-2b56b8632469 · ndn_service_framework::ServiceProvider::setAckStrategyHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L984)

```cpp
void setAckStrategyHandler(const ndn::Name& serviceName,
                                       AckStrategyHandler ackHandler);
```

### API-7f02e58f28aa · ndn_service_framework::ServiceProvider::setLocalPublicationHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L987)

```cpp
void setLocalPublicationHandler(LocalPublicationHandler handler);
```

### API-4e7ad019e025 · ndn_service_framework::ServiceProvider::setLegacyAckStrategyHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L989)

```cpp
void setLegacyAckStrategyHandler(const ndn::Name& serviceName,
                                             LegacyAckStrategyHandler ackHandler);
```

### API-183a987e9062 · ndn_service_framework::ServiceProvider::setSelectionStatusQueryable

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L992)

```cpp
void setSelectionStatusQueryable(const ndn::Name& serviceName,
                                              bool enabled = true);
```

### API-9878963a17f0 · ndn_service_framework::ServiceProvider::reportSelectionOperationStatus

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L998)

```cpp
void reportSelectionOperationStatus(
                const std::string& selectionDigest,
                ServiceOperationStatus status);
```

原始接口说明：

```text
/** Attach the latest bounded member snapshot before a
             * CollaborationContext exists. The selection digest is the
             * responsibility binding; it is not readiness authority. */
```

### API-08874a7bd8dc · ndn_service_framework::ServiceProvider::setGenericAdmissionLeaseValidator

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1001)

```cpp
void setGenericAdmissionLeaseValidator(
                const ndn::Name& serviceName,
                GenericAdmissionLeaseValidator validator,
                bool required = true);
```

### API-f73e53ed6563 · ndn_service_framework::ServiceProvider::setGenericAdmissionLeaseRequired

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1005)

```cpp
void setGenericAdmissionLeaseRequired(const ndn::Name& serviceName,
                                                  bool required = true);
```

### API-18e4f9c73cfa · ndn_service_framework::ServiceProvider::grantGenericAdmissionLease

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1007)

```cpp
void grantGenericAdmissionLease(GenericAdmissionLease lease);
```

### API-99a811f6f19a · ndn_service_framework::ServiceProvider::addHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1010)

```cpp
template<typename RequestT, typename ResponseT>
void addHandler(const ndn::Name& serviceName,
                            std::function<void(const ndn::Name& requesterIdentity,
                                               const RequestT& request,
                                               ResponseT& response)> handler)
```

### API-1695ffb726c0 · ndn_service_framework::ServiceProvider::hasService

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1051)

```cpp
bool hasService(const ndn::Name& serviceName) const;
```

### API-f316970f2dd8 · ndn_service_framework::ServiceProvider::fetchAndDecryptLargeData

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1053)

```cpp
LargeDataFetchResult fetchAndDecryptLargeData(
                const ndn::Name& encryptedDataName,
                const std::string& serviceName);
```

### API-95516b8e845c · ndn_service_framework::ServiceProvider::resolveLargeDataReferencePayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1057)

```cpp
LargeDataFetchResult resolveLargeDataReferencePayload(
                const ndn::Buffer& payload,
                const std::string& serviceName);
```

### API-72ed2bc30851 · ndn_service_framework::ServiceProvider::makeRequestScopedResponseWithLargeDataOptimization

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1065)

```cpp
LargeDataReferenceResponseResult makeRequestScopedResponseWithLargeDataOptimization(
                const ndn::Name& requesterName,
                const ndn::Name& providerName,
                const ndn::Name& serviceName,
                const ndn::Name& requestId,
                ResponseMessage response,
                const RequestKeyBundle& keys,
                RequestSecurityBinding binding,
                size_t thresholdBytes = 0,
                ndn::time::milliseconds freshness = ndn::DEFAULT_FRESHNESS_PERIOD);
```

原始接口说明：

```text
// Request-scoped large responses cannot use the legacy
// service-wide HybridMessageCrypto carrier.  Each segment is an
// independently authenticated AeadEnvelope bound to the same
// invocation and a unique segment identity.
```

### API-62e1060571d6 · ndn_service_framework::ServiceProvider::dispatchRequest

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1076)

```cpp
ResponseMessage dispatchRequest(const ndn::Name& requesterIdentity,
                                            const ndn::Name& providerName,
                                            const ndn::Name& serviceName,
                                            const ndn::Name& requestId,
                                            const RequestMessage& requestMessage) const;
```

### API-5842147058bb · ndn_service_framework::ServiceProvider::handleDecryptedRequestByName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1082)

```cpp
ResponseMessage handleDecryptedRequestByName(const ndn::Name& requestName,
                                                         const RequestMessage& requestMessage) const;
```

### API-73855595bb88 · ndn_service_framework::ServiceProvider::handleDecryptedRequestByName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1085)

```cpp
ResponseMessage handleDecryptedRequestByName(const ndn::Name& requestName,
                                                         const ndn::Block& requestBlock) const;
```

### API-9efc8aff0382 · ndn_service_framework::ServiceProvider::OnRequestDecryptionSuccessCallbackV2

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1088)

```cpp
void OnRequestDecryptionSuccessCallbackV2(const ndn::Name& requesterIdentity,
                                                       const ndn::Name& serviceName,
                                                       const ndn::Name& requestId,
                                                       const ndn::Buffer& buffer);
```

### API-dd06b8fe796c · ndn_service_framework::ServiceProvider::OnRequestDecryptionErrorCallback

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1093)

```cpp
void OnRequestDecryptionErrorCallback(const ndn::Name& requesterIdentity,
                                                  const ndn::Name& serviceName,
                                                  const ndn::Name& requestId,
                                                  const std::string& error);
```

### API-d0973caab0bb · ndn_service_framework::ServiceProvider::processNDNSDServiceInfoCallback

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1099)

```cpp
void processNDNSDServiceInfoCallback(const ndnsd::discovery::Details& callback);
```

原始接口说明：

```text
// ndnsd serviceinfo discovery callback
```

### API-edad70e6ecad · ndn_service_framework::ServiceProvider::onPermissionResponseData

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1101)

```cpp
void onPermissionResponseData(const ndn::Interest& interest,
                                           const ndn::Data& data);
```

### API-46be81968559 · ndn_service_framework::ServiceProvider::onPermissionResponseTimeout

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1103)

```cpp
void onPermissionResponseTimeout(const ndn::Interest& interest,
                                             int attempt = 1);
```

### API-5c4b6b629ff6 · ndn_service_framework::ServiceProvider::fetchPolicyManifestFromController

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1105)

```cpp
void fetchPolicyManifestFromController(const ndn::Name& controllerPrefix,
                                                   int attempt = 1);
```

### API-134fa8f4cd07 · ndn_service_framework::ServiceProvider::onPolicyManifestData

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1107)

```cpp
void onPolicyManifestData(const ndn::Interest& interest,
                                      const ndn::Data& data);
```

### API-67ea06304aab · ndn_service_framework::ServiceProvider::onPolicyManifestTimeout

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1109)

```cpp
void onPolicyManifestTimeout(const ndn::Interest& interest,
                                         int attempt = 1);
```

### API-59fa08838d0f · ndn_service_framework::ServiceProvider::fetchPolicyStatusFromController

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1111)

```cpp
void fetchPolicyStatusFromController(const ndn::Name& controllerPrefix,
                                                 const ndn::Name& serviceName,
                                                 int attempt = 1,
                                                 std::optional<ControllerVersion> expectedVersion = std::nullopt);
```

### API-bc7d535f828c · ndn_service_framework::ServiceProvider::onPolicyStatusData

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1115)

```cpp
void onPolicyStatusData(const ndn::Interest& interest,
                                    const ndn::Data& data,
                                    int attempt = 1);
```

### API-26bc88b44bf4 · ndn_service_framework::ServiceProvider::onPolicyStatusTimeout

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1118)

```cpp
void onPolicyStatusTimeout(const ndn::Interest& interest,
                                       int attempt = 1);
```

### API-c9ea6ff970ae · ndn_service_framework::ServiceProvider::scheduleControllerStatusRefresh

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1120)

```cpp
void scheduleControllerStatusRefresh(
                const ndn::Name& serviceName,
                const PolicyStatusData& status);
```

### API-58d9c5682c33 · ndn_service_framework::ServiceProvider::scheduleDeferredDkeyRefreshRetry

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1127)

```cpp
void scheduleDeferredDkeyRefreshRetry(const ndn::Name& serviceName);
```

原始接口说明：

```text
// Bounded DKEY re-arm retries for a LocalMock whose immediate
// refresh was deferred (no fixture-owned Consumer).  The retry
// fires only while the Face is pumped and stops as soon as the
// Consumer is ready or the attempt bound is exhausted.
```

### API-4d38f57cdfc4 · ndn_service_framework::ServiceProvider::isAcceptablePolicyEpoch

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1128)

```cpp
bool isAcceptablePolicyEpoch(size_t messageEpoch) const;
```

### API-e4629f6ec171 · ndn_service_framework::ServiceProvider::isAcceptablePolicyEpoch

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1129)

```cpp
bool isAcceptablePolicyEpoch(const ndn::Name& serviceName,
                                         size_t messageEpoch) const;
```

### API-f77c208372af · ndn_service_framework::ServiceProvider::isAcceptableControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1131)

```cpp
bool isAcceptableControllerVersion(
                const std::optional<ControllerVersion>& messageVersion) const;
```

### API-12effb6738be · ndn_service_framework::ServiceProvider::isAcceptableControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1133)

```cpp
bool isAcceptableControllerVersion(
                const ndn::Name& serviceName,
                const std::optional<ControllerVersion>& messageVersion) const;
```

### API-1dcc5d19761d · ndn_service_framework::ServiceProvider::maybeRefreshControllerVersionHint

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1136)

```cpp
void maybeRefreshControllerVersionHint(
                const ndn::Name& serviceName,
                const std::optional<ControllerVersion>& messageVersion) const;
```

### API-fbbec2ad0788 · ndn_service_framework::ServiceProvider::authorizeControllerTransition

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1143)

```cpp
__attribute__((noinline))
            #endif
            bool authorizeControllerTransition(
                const ndn::Name& serviceName,
                ProtectedTransition transition) const;
```

### API-98ad0cd6a8ef · ndn_service_framework::ServiceProvider::adoptControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1148)

```cpp
void adoptControllerVersion(const ControllerVersion& version);
```

### API-402503e0f5ef · ndn_service_framework::ServiceProvider::invalidateControllerScopedCaches

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1154)

```cpp
void invalidateControllerScopedCaches(
                const ndn::Name& serviceName,
                const ControllerVersion& version,
                bool abeGenerationChanged = true,
                const PolicyStatusData* status = nullptr,
                bool grantOnlyDkeyRefresh = false);
```

原始接口说明：

```text
/**
             * Evict per-service authorization material after an authenticated
             * ControllerVersion change.  Replay tombstones remain bounded and
             * are not removed merely because a newer policy arrived.
             */
```

### API-76eb67f4d8ba · ndn_service_framework::ServiceProvider::refreshNacDkeyForControllerStatus

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1172)

```cpp
void refreshNacDkeyForControllerStatus(
                const ndn::Name& serviceName,
                const ControllerVersion& version,
                bool abeGenerationChanged,
                bool grantOnlyDkeyRefresh);
```

原始接口说明：

```text
/**
             * Run the DKEY-only refresh leg of a Controller-status install
             * (grant-only target-policy replacement, FR-017/SC-022).  Called
             * from invalidateControllerScopedCaches on a version-advance
             * install and directly from installControllerStatus when an
             * equal-version install consumes a pending grant-only refresh
             * that arrived after the version-advance install already ran
             * (reverse order: the status channel installed the version before
             * the PermissionResponse recorded the grant).  A LocalMock
             * without its fixture-owned Consumer defers to the explicit
             * bootstrap path via scheduleDeferredDkeyRefreshRetry.
             */
```

### API-65ca9a008ce1 · ndn_service_framework::ServiceProvider::replyFromIMS

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1179)

```cpp
bool replyFromIMS(const ndn::Interest &interest);
```

### API-56dddf3d86fd · ndn_service_framework::ServiceProvider::rememberPendingImsInterest

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1180)

```cpp
void rememberPendingImsInterest(const ndn::Interest& interest);
```

### API-eac7c82bea8e · ndn_service_framework::ServiceProvider::insertDataIntoIMS

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1181)

```cpp
void insertDataIntoIMS(const ndn::Data& data);
```

### API-13d0944a5adb · ndn_service_framework::ServiceProvider::insertDataIntoIMS

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1182)

```cpp
void insertDataIntoIMS(const ndn::Data& data,
                                   const ndn::time::milliseconds& freshness);
```

### API-b7c16e8da36d · ndn_service_framework::ServiceProvider::satisfyPendingImsInterestsLocked

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1184)

```cpp
void satisfyPendingImsInterestsLocked(const ndn::Data& insertedData);
```

### API-263c8d46dffd · ndn_service_framework::ServiceProvider::satisfyPendingImsInterestsLocked

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1185)

```cpp
void satisfyPendingImsInterestsLocked();
```

### API-a6ddfa0f1c45 · ndn_service_framework::ServiceProvider::pruneExpiredPendingImsInterestsLocked

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1186)

```cpp
void pruneExpiredPendingImsInterestsLocked();
```

### API-d72f5a00851e · ndn_service_framework::ServiceProvider::onPrefixRegisterFailure

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1188)

```cpp
void onPrefixRegisterFailure(const ndn::Name& prefix, const std::string& reason);
```

### API-3c19df8c9d1b · ndn_service_framework::ServiceProvider::onInterest

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1190)

```cpp
void onInterest(const ndn::InterestFilter &, const ndn::Interest &interest);
```

### API-7d3167e87f3b · ndn_service_framework::ServiceProvider::handleExecutionActivateInterest

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1191)

```cpp
bool handleExecutionActivateInterest(const ndn::Interest& interest);
```

### API-38bc9fc53d11 · ndn_service_framework::ServiceProvider::publishProviderReady

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1192)

```cpp
void publishProviderReady(const ndn::Name& requesterIdentity,
                                      const ProviderReadyMessage& ready,
                                      const std::string& statusHandle,
                                      int attempt = 0);
```

### API-3a8f7722f091 · ndn_service_framework::ServiceProvider::getProviderBootEpoch

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1199)

```cpp
std::string getProviderBootEpoch() const
```

原始接口说明：

```text
/** Stable process-incarnation fence exposed to opaque application
             * participants. It is an identity binding, not a secret. */
```

### API-ee7357741d83 · ndn_service_framework::ServiceProvider::serveDataWithIMS

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1204)

```cpp
void serveDataWithIMS(ndn::nacabe::SPtrVector<ndn::Data>& contentData, ndn::nacabe::SPtrVector<ndn::Data>& ckData);
```

### API-d73e86f7f792 · ndn_service_framework::ServiceProvider::PublishRequestAckMessageV2

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1206)

```cpp
void PublishRequestAckMessageV2(const ndn::Name& requesterIdentity,
                                            const ndn::Name& serviceName,
                                            const ndn::Name& requestId,
                                            bool status,
                                            const std::string& msg,
                                            const ndn::Buffer& payload = ndn::Buffer(),
                                            const std::string& userToken = "",
                                            const std::string& providerToken = "",
                                            const RequestMessage* sourceRequest = nullptr,
                                            const AckDecision* ackDecision = nullptr);
```

### API-288a73a6eedd · ndn_service_framework::ServiceProvider::onServiceSelectionMessage

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1217)

```cpp
void onServiceSelectionMessage(const ndn::svs::SVSPubSub::SubscriptionData &subscription);
```

### API-031e52165fd9 · ndn_service_framework::ServiceProvider::handleServiceSelectionMessage

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1218)

```cpp
void handleServiceSelectionMessage(const ndn::svs::SVSPubSub::SubscriptionData& subscription,
                                               bool checkFreshness);
```

### API-aeac3b1ae7a2 · ndn_service_framework::ServiceProvider::prefetchSelectionMessageV2

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1220)

```cpp
void prefetchSelectionMessageV2(const ndn::Name& requesterIdentity,
                                            const ndn::Name& serviceName,
                                            const ndn::Name& requestId);
```

### API-d57cbb677b34 · ndn_service_framework::ServiceProvider::PublishMessage

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1224)

```cpp
void PublishMessage(const ndn::Name& messageName, const ndn::Name &messageNameWithoutPrefix, AbstractMessage& message);
```

### API-f4882855a67f · ndn_service_framework::ServiceProvider::publishHybridMessage

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1225)

```cpp
void publishHybridMessage(const ndn::Name& messageName,
                                      const ndn::Name& messageNameWithoutPrefix,
                                      AbstractMessage& message);
```

### API-486ced5b2db0 · ndn_service_framework::ServiceProvider::publishHybridEncodedMessage

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1228)

```cpp
void publishHybridEncodedMessage(const ndn::Name& messageName,
                                             ndn::Buffer plaintext);
```

### API-92478876642b · ndn_service_framework::ServiceProvider::decryptHybridMessage

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1230)

```cpp
bool decryptHybridMessage(const ndn::Name& messageName,
                                      const ndn::Block& envelopeBlock,
                                      std::function<void(const ndn::Buffer&)> onSuccess,
                                      std::function<void(const std::string&)> onError);
```

### API-075ccc6b9422 · ndn_service_framework::ServiceProvider::OnServiceSelectionMessageDecryptionSuccessCallbackV2

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1235)

```cpp
void OnServiceSelectionMessageDecryptionSuccessCallbackV2(const ndn::Name& requesterName,
                                                                          const ndn::Name& providerName,
                                                                          const ndn::Name& serviceName,
                                                                          const ndn::Name& msgId,
                                                                          const ndn::Buffer& buffer);
```

### API-f765d181e2ad · ndn_service_framework::ServiceProvider::OnServiceSelectionMessageDecryptionErrorCallback

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1241)

```cpp
void OnServiceSelectionMessageDecryptionErrorCallback(
                const ndn::Name& requesterName,
                const ndn::Name& providerName,
                const ndn::Name& serviceName,
                const ndn::Name& msgId,
                const std::string& reason);
```

### API-c912a55f3fb3 · ndn_service_framework::ServiceProvider::registerNDNSFMessages

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1249)

```cpp
void registerNDNSFMessages();
```

原始接口说明：

```text
// Register NDNSF Messages in the ndn-svs
```

### API-842f103f9a6f · ndn_service_framework::ServiceProvider::registerServiceInfo

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1253)

```cpp
virtual void registerServiceInfo();
```

原始接口说明：

```text
// Register service info using ndnsd(). Generic dynamic providers may use the no-op
// default; legacy generated providers may still override it.
```

### API-c2bd7a3f9535 · ndn_service_framework::ServiceProvider::isFresh

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1255)

```cpp
bool isFresh(const ndn::svs::SVSPubSub::SubscriptionData &subscription);
```

### API-7462c8035226 · ndn_service_framework::ServiceProvider::onMissingData

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1258)

```cpp
void
            onMissingData(const std::vector<ndn::svs::MissingDataInfo> &);
```

### API-c4065d4a0587 · ndn_service_framework::ServiceProvider::RegistrationState

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1271)

```cpp
struct RegistrationState
```

### API-44600f49e5dd · ndn_service_framework::ServiceProvider::RegistrationState::RegistrationState

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1273)

```cpp
RegistrationState(ndn::Name name, uint64_t gen)
```

### API-d8d0ec15abc7 · ndn_service_framework::ServiceProvider::RegistrationState::serviceName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1276)

```cpp
ndn::Name serviceName;
```

### API-1589c75f4289 · ndn_service_framework::ServiceProvider::RegistrationState::generation

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1277)

```cpp
uint64_t generation = 0;
```

### API-96863bebf41d · ndn_service_framework::ServiceProvider::RegistrationState::closed

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1278)

```cpp
std::atomic<bool> closed{false};
```

### API-0375561134d9 · ndn_service_framework::ServiceProvider::RegistrationControl

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1281)

```cpp
struct RegistrationControl
```

### API-db0361bed0b0 · ndn_service_framework::ServiceProvider::RegistrationControl::mutex

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1285)

```cpp
std::mutex mutex;
```

原始接口说明：

```text
// Serializes ownership hand-off between the Face event thread
// (registration and cleanup) and the Provider destructor.
```

### API-ae0636ddfccc · ndn_service_framework::ServiceProvider::RegistrationControl::* owner

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1286)

```cpp
ServiceProvider* owner = nullptr;
```

### API-3e88adaee0bc · ndn_service_framework::ServiceProvider::ServiceRegistration

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1292)

```cpp
class ServiceRegistration
```

### API-efb9110501eb · ndn_service_framework::ServiceProvider::ServiceRegistration::ServiceRegistration

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1295)

```cpp
ServiceRegistration() noexcept = default;
```

### API-e454035bfb35 · ndn_service_framework::ServiceProvider::ServiceRegistration::ServiceRegistration

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1296)

```cpp
ServiceRegistration(ServiceRegistration&& other) noexcept;
```

### API-82860a8731a0 · ndn_service_framework::ServiceProvider::ServiceRegistration::operator=

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1297)

```cpp
ServiceRegistration& operator=(ServiceRegistration&& other) noexcept;
```

### API-cf5638749d93 · ndn_service_framework::ServiceProvider::ServiceRegistration::ServiceRegistration

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1298)

```cpp
ServiceRegistration(const ServiceRegistration&) = delete;
```

### API-5f364d33d436 · ndn_service_framework::ServiceProvider::ServiceRegistration::operator=

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1299)

```cpp
ServiceRegistration& operator=(const ServiceRegistration&) = delete;
```

### API-ee96c9126468 · ndn_service_framework::ServiceProvider::ServiceRegistration::~ServiceRegistration

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1300)

```cpp
~ServiceRegistration();
```

### API-862a92089c03 · ndn_service_framework::ServiceProvider::ServiceRegistration::close

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1302)

```cpp
void close() noexcept;
```

### API-1dddddcd6a39 · ndn_service_framework::ServiceProvider::ServiceRegistration::closed

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1303)

```cpp
bool closed() const noexcept;
```

### API-922ae8c53766 · ndn_service_framework::ServiceProvider::ServiceRegistration::valid

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1304)

```cpp
bool valid() const noexcept;
```

### API-18ed137aaede · ndn_service_framework::ServiceProvider::ServiceRegistration::generation

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1305)

```cpp
uint64_t generation() const noexcept;
```

### API-7c07afca0097 · ndn_service_framework::ServiceProvider::ServiceRegistration::serviceName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1306)

```cpp
ndn::Name serviceName() const;
```

### API-599401d26556 · ndn_service_framework::ServiceProvider::RegisteredService

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1318)

```cpp
struct RegisteredService
```

### API-ef091ca23577 · ndn_service_framework::ServiceProvider::RegisteredService::ackHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1320)

```cpp
AckStrategyHandler ackHandler;
```

### API-39160a416528 · ndn_service_framework::ServiceProvider::RegisteredService::requestHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1321)

```cpp
RequestHandler requestHandler;
```

### API-1f4782edee49 · ndn_service_framework::ServiceProvider::RegisteredService::targetedRequestHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1322)

```cpp
RequestHandler targetedRequestHandler;
```

### API-93a4bf1fcd63 · ndn_service_framework::ServiceProvider::RegisteredService::streamingHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1323)

```cpp
StreamingHandler streamingHandler;
```

### API-802562d45c35 · ndn_service_framework::ServiceProvider::RegisteredService::mode

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1324)

```cpp
ServiceMode mode = ServiceMode::Normal;
```

### API-4c68df2cc9ff · ndn_service_framework::ServiceProvider::RegisteredService::selectionStatusQueryable

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1325)

```cpp
bool selectionStatusQueryable = false;
```

### API-d1cc6fa59fa0 · ndn_service_framework::ServiceProvider::RegisteredService::genericAdmissionLeaseRequired

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1326)

```cpp
bool genericAdmissionLeaseRequired = false;
```

### API-ea6b65383541 · ndn_service_framework::ServiceProvider::RegisteredService::genericAdmissionLeaseValidator

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1327)

```cpp
GenericAdmissionLeaseValidator genericAdmissionLeaseValidator;
```

### API-20e46bab00e9 · ndn_service_framework::ServiceProvider::RegisteredService::registrationState

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1332)

```cpp
std::shared_ptr<RegistrationState> registrationState;
```

原始接口说明：

```text
// Non-null exactly for entries owned by an active or closed
// scoped registration.  A closed state kept in the map means
// cleanup has not been drained yet; legacy registration may
// take the entry over by clearing the pointer.
```

### API-8d9ad37510cc · ndn_service_framework::ServiceProvider::RegisteredCollaborationService

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1335)

```cpp
struct RegisteredCollaborationService
```

### API-296946e4fc86 · ndn_service_framework::ServiceProvider::RegisteredCollaborationService::ackHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1337)

```cpp
AckStrategyHandler ackHandler;
```

### API-165a20b51883 · ndn_service_framework::ServiceProvider::RegisteredCollaborationService::handler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1338)

```cpp
CollaborationHandler handler;
```

### API-a60e696dba58 · ndn_service_framework::ServiceProvider::RegisteredCollaborationService::allowedRoles

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1339)

```cpp
std::vector<CollaborationRole> allowedRoles;
```

### API-5239f78b9673 · ndn_service_framework::ServiceProvider::RegisteredCollaborationService::registrationState

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1341)

```cpp
std::shared_ptr<RegistrationState> registrationState;
```

原始接口说明：

```text
// Same ownership rules as RegisteredService::registrationState.
```

### API-932339d9b076 · ndn_service_framework::ServiceProvider::allocateRegistrationGeneration

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1349)

```cpp
uint64_t allocateRegistrationGeneration();
```

原始接口说明：

```text
// Face-serialized helpers for scoped registration.  These run on
// the Face event thread (registration calls or posted cleanup
// closures), never concurrently with each other; the destructor
// additionally serializes against them with
// m_registrationControl->mutex.
```

### API-e972422983f2 · ndn_service_framework::ServiceProvider::allowLegacyServiceTakeover

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1355)

```cpp
bool allowLegacyServiceTakeover(const ndn::Name& serviceName);
```

原始接口说明：

```text
// Legacy-registration guard: refuses (returns false) when the
// service name in m_services is owned by a live scoped
// registration, and clears a closed parked state (legacy
// takeover) when the name is free.  Callers must hold
// m_registrationControl->mutex.
```

### API-8b4c085b5c8c · ndn_service_framework::ServiceProvider::serviceEntryBusy

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1359)

```cpp
static bool serviceEntryBusy(const RegisteredService& entry);
```

原始接口说明：

```text
// True when the m_services record already carries registration
// content (scoped state, legacy handlers, stream sentinels, the
// collaboration shell flag, or admission-lease settings).
```

### API-f1bdc6e2c3c0 · ndn_service_framework::ServiceProvider::drainClosedRegistrations

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1364)

```cpp
void drainClosedRegistrations(
                std::vector<RegisteredService>& retiredServices,
                std::vector<RegisteredCollaborationService>& retiredCollaborations);
```

原始接口说明：

```text
// Removes every map entry that parks a closed registration state
// and appends the retired handler owners to the out-vectors, so
// callers destroy them only after the control lock is released.
// Callers must hold m_registrationControl->mutex.
```

### API-d23b39554507 · ndn_service_framework::ServiceProvider::detachClosedRegistration

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1370)

```cpp
void detachClosedRegistration(
                const std::shared_ptr<RegistrationState>& state,
                RegisteredService& retiredService,
                RegisteredCollaborationService& retiredCollaboration);
```

原始接口说明：

```text
// Removes map entries still owned by the given closed state and
// moves their handler owners into the out-parameters, so callers
// destroy them only after releasing any held lock.
```

### API-677ff27cd99d · ndn_service_framework::ServiceProvider::closeAllRegistrationStates

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1374)

```cpp
void closeAllRegistrationStates();
```

### API-111b4d5b3b0d · ndn_service_framework::ServiceProvider::PendingEncryptedCollaborationData

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1376)

```cpp
struct PendingEncryptedCollaborationData
```

### API-52cdf45c2a8c · ndn_service_framework::ServiceProvider::PendingEncryptedCollaborationData::dataName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1378)

```cpp
ndn::Name dataName;
```

### API-65ed815f3331 · ndn_service_framework::ServiceProvider::PendingEncryptedCollaborationData::requestId

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1379)

```cpp
ndn::Name requestId;
```

### API-8a4e89490cfe · ndn_service_framework::ServiceProvider::PendingEncryptedCollaborationData::producer

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1380)

```cpp
ndn::Name producer;
```

### API-491c036e0cb0 · ndn_service_framework::ServiceProvider::PendingEncryptedCollaborationData::message

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1381)

```cpp
CollaborationDataMessage message;
```

### API-ab5cb6333b09 · ndn_service_framework::ServiceProvider::CollaborationSubscription

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1384)

```cpp
struct CollaborationSubscription
```

### API-6b50df5100a4 · ndn_service_framework::ServiceProvider::CollaborationSubscription::requestId

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1386)

```cpp
ndn::Name requestId;
```

### API-1780a4152db2 · ndn_service_framework::ServiceProvider::CollaborationSubscription::requesterName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1387)

```cpp
ndn::Name requesterName;
```

### API-e93ad80a8d05 · ndn_service_framework::ServiceProvider::CollaborationSubscription::keyScope

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1388)

```cpp
KeyScope keyScope;
```

### API-1b0f28906a23 · ndn_service_framework::ServiceProvider::CollaborationSubscription::topicPrefix

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1389)

```cpp
Topic topicPrefix;
```

### API-9152a069823a · ndn_service_framework::ServiceProvider::CollaborationSubscription::requestMessage

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1390)

```cpp
RequestMessage requestMessage;
```

### API-9bf00fd40443 · ndn_service_framework::ServiceProvider::CollaborationSubscription::assignment

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1391)

```cpp
CollaborationAssignment assignment;
```

### API-dcce64d5dcf3 · ndn_service_framework::ServiceProvider::CollaborationSubscription::onData

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1392)

```cpp
std::function<void(const CollaborationData&)> onData;
```

### API-340edbdf97b9 · ndn_service_framework::ServiceProvider::CollaborationSubscription::onContextData

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1393)

```cpp
std::function<void(CollaborationContext&,
                                   const CollaborationData&)> onContextData;
```

### API-a23a0cc3d3e5 · ndn_service_framework::ServiceProvider::CollaborationSubscription::receiveFilterOnly

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1395)

```cpp
bool receiveFilterOnly = false;
```

### API-f1e5894f209e · ndn_service_framework::ServiceProvider::TargetedProviderTokenState

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1398)

```cpp
struct TargetedProviderTokenState
```

### API-80bfd74e9735 · ndn_service_framework::ServiceProvider::TargetedProviderTokenState::requesterIdentity

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1400)

```cpp
ndn::Name requesterIdentity;
```

### API-a87ce996e626 · ndn_service_framework::ServiceProvider::TargetedProviderTokenState::serviceName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1401)

```cpp
ndn::Name serviceName;
```

### API-71e235e53772 · ndn_service_framework::ServiceProvider::TargetedProviderTokenState::userToken

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1402)

```cpp
std::string userToken;
```

### API-4aacefd49201 · ndn_service_framework::ServiceProvider::makeErrorResponse

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1405)

```cpp
static ResponseMessage makeErrorResponse(const std::string& errorInfo);
```

### API-c4a7c0428da9 · ndn_service_framework::ServiceProvider::makeDefaultAckDecision

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1407)

```cpp
static AckDecision makeDefaultAckDecision();
```

### API-73b305e9d09f · ndn_service_framework::ServiceProvider::schedulePendingRequestCleanup

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1409)

```cpp
void schedulePendingRequestCleanup(const ndn::Name& pendingKey,
                                               ndn::time::milliseconds ttl = ndn::time::seconds(30),
                                               bool authoritative = false);
```

### API-f782769947eb · ndn_service_framework::ServiceProvider::CollaborationWorkFence

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1413)

```cpp
struct CollaborationWorkFence
```

### API-6e6b7eca40fb · ndn_service_framework::ServiceProvider::CollaborationWorkFence::deadline

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1415)

```cpp
std::chrono::steady_clock::time_point deadline;
```

### API-6e0835fe6e89 · ndn_service_framework::ServiceProvider::CollaborationWorkFence::current

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1416)

```cpp
std::function<bool()> current;
```

### API-4a8150d45aa7 · ndn_service_framework::ServiceProvider::makeCollaborationWorkFence

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1418)

```cpp
CollaborationWorkFence makeCollaborationWorkFence(
                const ndn::Name& requesterName, const ndn::Name& requestId,
                const ndn::Name& serviceName,
                std::optional<ControllerVersion> version,
                std::shared_ptr<RegistrationState> registrationState = nullptr);
```

### API-1e0772d52d1c · ndn_service_framework::ServiceProvider::fetchAndDecryptLargeDataUntil

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1423)

```cpp
LargeDataFetchResult fetchAndDecryptLargeDataUntil(
                const ndn::Name& encryptedDataName, const std::string& serviceName,
                std::chrono::steady_clock::time_point deadline,
                std::function<bool()> current = {});
```

### API-2bacec394dcb · ndn_service_framework::ServiceProvider::cleanupPendingRequestState

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1428)

```cpp
void cleanupPendingRequestState(const ndn::Name& pendingKey,
                                            bool preserveReplayTombstone = false);
```

### API-cab7bbd74966 · ndn_service_framework::ServiceProvider::fencePendingRegistrationExecution

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1437)

```cpp
std::string fencePendingRegistrationExecution(
                const ndn::Name& pendingKey,
                const std::shared_ptr<RegistrationState>& entryState);
```

原始接口说明：

```text
// spec182: execution-side generation fence.  Under
// m_pendingRequestMutex, binds pendingKey to the registration
// state of the entry the dispatcher chose unless the request was
// already bound to a different (earlier) generation.  Returns an
// empty string when execution may proceed, else the refusal
// reason.  Callers pass a null state only for legacy entries,
// which always pass.
```

### API-6dfada20b2f0 · ndn_service_framework::ServiceProvider::gateInlineRequestExecution

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1448)

```cpp
bool gateInlineRequestExecution(
                const ndn::Name& requesterName,
                const ndn::Name& providerName,
                const ndn::Name& serviceName,
                const ndn::Name& requestId,
                const RequestMessage& requestMessage,
                std::string selectionDigest,
                std::shared_ptr<RegistrationState>& registrationState);
```

原始接口说明：

```text
// spec182: inline (pool-0) execution fence.  Resolves the current
// m_services entry for serviceName and refuses (publishing the
// failure on the caller's behalf) when a scoped registration
// retired or the pending binding names an earlier generation.
// On success, *registrationState carries the state to forward to
// finishRequestExecutionOnEventLoop (null for legacy entries).
// Face-thread only.  Returns true when inline execution may
// proceed.
```

### API-0f72b70a5c2e · ndn_service_framework::ServiceProvider::expirePendingRequestState

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1457)

```cpp
bool expirePendingRequestState(const ndn::Name& pendingKey);
```

### API-3bfcd488f7fb · ndn_service_framework::ServiceProvider::shouldSuppressAdaptiveAck

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1459)

```cpp
bool shouldSuppressAdaptiveAck(const ndn::Name& requesterIdentity,
                                           const ndn::Name& serviceName,
                                           const ndn::Name& requestId);
```

### API-a97027124f22 · ndn_service_framework::ServiceProvider::updateProviderRequestLifecycleState

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1462)

```cpp
void updateProviderRequestLifecycleState(
                const ndn::Name& requestId,
                const ndn::Name& serviceName,
                ProviderRequestLifecycleState state,
                const std::string& suppressionReason = "",
                const std::string& finalStatus = "");
```

### API-61826440cbcf · ndn_service_framework::ServiceProvider::attachStreamLifecycle

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1470)

```cpp
std::shared_ptr<StreamInvocationLifecycle>
            attachStreamLifecycle(const ndn::Name& pendingKey);
```

原始接口说明：

```text
/** Attach streamed provider state to an existing pending request. */
```

### API-5216bbc1c80b · ndn_service_framework::ServiceProvider::getStreamLifecycle

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1473)

```cpp
std::shared_ptr<StreamInvocationLifecycle>
            getStreamLifecycle(const ndn::Name& pendingKey) const;
```

### API-ef159610e769 · ndn_service_framework::ServiceProvider::updateSelectionExecutionStatus

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1475)

```cpp
void updateSelectionExecutionStatus(
                const std::string& selectionDigest,
                SelectionExecutionState state,
                const ndn::Name& providerName,
                const ndn::Name& serviceName,
                const ndn::Name& requestId,
                const std::string& message = "",
                const ndn::Name& responseName = ndn::Name());
```

### API-9a68cedff369 · ndn_service_framework::ServiceProvider::replySelectionExecutionStatus

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1483)

```cpp
bool replySelectionExecutionStatus(const ndn::Interest& interest);
```

### API-2bf864a5e507 · ndn_service_framework::ServiceProvider::encodeSelectionExecutionStatus

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1484)

```cpp
static std::string encodeSelectionExecutionStatus(
                const SelectionExecutionStatus& status);
```

### API-9c4ee9ce1caa · ndn_service_framework::ServiceProvider::makeUnknownSelectionExecutionStatus

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1486)

```cpp
static SelectionExecutionStatus makeUnknownSelectionExecutionStatus(
                const ndn::Name& providerName,
                const std::string& selectionDigest);
```

### API-f94cad8c3026 · ndn_service_framework::ServiceProvider::dispatchAckDecisionAsync

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1492)

```cpp
bool dispatchAckDecisionAsync(
                const ndn::Name& requesterIdentity,
                const ndn::Name& serviceName,
                const ndn::Name& requestId,
                RequestMessage requestMessage,
                AckStrategyHandler ackHandler,
                std::shared_ptr<RegistrationState> registrationState = nullptr);
```

原始接口说明：

```text
// registrationState (spec182): non-null when the ack decision was
// taken against a scoped registration; the worker fences against
// it and forwards it to finishAckDecisionOnEventLoop.
```

### API-0b75b0df3923 · ndn_service_framework::ServiceProvider::finishAckDecisionOnEventLoop

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1504)

```cpp
void finishAckDecisionOnEventLoop(
                const ndn::Name& requesterIdentity,
                const ndn::Name& serviceName,
                const ndn::Name& requestId,
                RequestMessage requestMessage,
                AckDecision decision,
                std::shared_ptr<RegistrationState> registrationState = nullptr);
```

原始接口说明：

```text
// registrationState (spec182): the scoped registration the
// pending acceptance was bound to; when it closed or was
// superseded before the decision finished, the positive decision
// degrades to the negative path (no pending store, no positive
// ACK).  Null for legacy acceptances.
```

### API-a2550f12d7d0 · ndn_service_framework::ServiceProvider::validateGenericAdmissionLeaseForSelection

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1511)

```cpp
GenericLeaseValidationResult validateGenericAdmissionLeaseForSelection(
                const ndn::Name& requesterName,
                const ndn::Name& providerName,
                const ndn::Name& serviceName,
                const ndn::Name& requestId,
                const RequestMessage& requestMessage,
                const ServiceSelectionMessage& selectionMessage,
                const ndn::Buffer& assignmentPayload);
```

### API-0e9638ed5a67 · ndn_service_framework::ServiceProvider::finishDecodedRequestOnEventLoop

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1519)

```cpp
void finishDecodedRequestOnEventLoop(
                const ndn::Name& requesterIdentity,
                const ndn::Name& serviceName,
                const ndn::Name& requestId,
                RequestMessage requestMessage);
```

### API-5ce5cb310353 · ndn_service_framework::ServiceProvider::finishTargetedRequestOnEventLoop

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1524)

```cpp
bool finishTargetedRequestOnEventLoop(
                const ndn::Name& requesterIdentity,
                const ndn::Name& serviceName,
                const ndn::Name& requestId,
                RequestMessage requestMessage);
```

### API-2d6d06b7da5e · ndn_service_framework::ServiceProvider::consumeTargetedProviderToken

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1529)

```cpp
bool consumeTargetedProviderToken(const ndn::Name& requesterIdentity,
                                              const ndn::Name& serviceName,
                                              const RequestMessage& requestMessage,
                                              std::string& error) const;
```

### API-02b7f5bc26de · ndn_service_framework::ServiceProvider::attachTargetedTokenBatch

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1533)

```cpp
void attachTargetedTokenBatch(const ndn::Name& requesterIdentity,
                                          const ndn::Name& serviceName,
                                          const RequestMessage& requestMessage,
                                          ResponseMessage& response) const;
```

### API-672267e1211d · ndn_service_framework::ServiceProvider::dispatchRequestExecutionAsync

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1541)

```cpp
bool dispatchRequestExecutionAsync(
                const ndn::Name& requesterName,
                const ndn::Name& providerName,
                const ndn::Name& serviceName,
                const ndn::Name& requestId,
                RequestMessage requestMessage,
                std::string selectionDigest = "",
                std::shared_ptr<RegistrationState>* registrationStateOut = nullptr);
```

原始接口说明：

```text
// registrationStateOut (spec182): when non-null and the dispatch
// resolved a scoped registration entry, receives the entry's
// registration state (legacy entries leave it null).  Inline
// fallback callers use it to fence a pool-0 dispatch.
```

### API-e158ebcd790b · ndn_service_framework::ServiceProvider::initializeStreamPublisher

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1549)

```cpp
bool initializeStreamPublisher(
                const ndn::Name& requesterName,
                const ndn::Name& providerName,
                const ndn::Name& serviceName,
                const ndn::Name& requestId,
                const RequestMessage& requestMessage,
                const ServiceSelectionMessage& selectionMessage,
                const std::string& selectionDigest);
```

### API-d120bddd3926 · ndn_service_framework::ServiceProvider::publishStreamEventOnFaceEventLoop

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1557)

```cpp
void publishStreamEventOnFaceEventLoop(
                PublishedStreamEvent event,
                uint64_t retentionMs);
```

### API-d40ee2710d13 · ndn_service_framework::ServiceProvider::onStreamEvent

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1560)

```cpp
void onStreamEvent(const ndn::svs::SVSPubSub::SubscriptionData& subscription);
```

### API-1c7a0c296a9b · ndn_service_framework::ServiceProvider::dispatchCollaborationExecutionAsync

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1561)

```cpp
bool dispatchCollaborationExecutionAsync(
                const ndn::Name& requesterName,
                const ndn::Name& providerName,
                const ndn::Name& serviceName,
                const ndn::Name& requestId,
                RequestMessage requestMessage,
                CollaborationAssignment assignment,
                std::string selectionDigest = "");
```

### API-27a871d54edb · ndn_service_framework::ServiceProvider::prepareCollaborationAssignmentAsync

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1572)

```cpp
void prepareCollaborationAssignmentAsync(
                const ndn::Name& requesterName,
                const ndn::Name& requestId,
                CollaborationAssignment assignment,
                std::function<void(bool, std::string,
                                   CollaborationAssignment)> onReady,
                std::shared_ptr<RegistrationState> registrationState = nullptr);
```

原始接口说明：

```text
// registrationState (spec182): the scoped collaboration
// registration the request was accepted against; bound under
// m_collaborationMutex next to m_collaborationServiceNamesByRequest.
```

### API-23c721b42c2b · ndn_service_framework::ServiceProvider::finishRequestExecutionOnEventLoop

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1583)

```cpp
void finishRequestExecutionOnEventLoop(
                const ndn::Name& requesterName,
                const ndn::Name& providerName,
                const ndn::Name& serviceName,
                const ndn::Name& requestId,
                const RequestMessage& requestMessage,
                ResponseMessage response,
                std::string selectionDigest = "",
                std::shared_ptr<RegistrationState> registrationState = nullptr);
```

原始接口说明：

```text
// registrationState (spec182): non-null when the executing
// request was bound to a scoped registration; a positive response
// whose registration closed meanwhile degrades to an error (the
// retired registration must never commit work in its name).
```

### API-56afe706b6da · ndn_service_framework::ServiceProvider::fetchRequestScopedInputAndDispatch

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1592)

```cpp
void fetchRequestScopedInputAndDispatch(
                const ndn::Name& requesterName,
                const ndn::Name& providerName,
                const ndn::Name& serviceName,
                const ndn::Name& requestId,
                RequestMessage requestMessage,
                const ServiceSelectionMessage& selectionMessage,
                const std::string& selectionDigest);
```

### API-2d6d6a0712c2 · ndn_service_framework::ServiceProvider::publishExecutionFailureOnEventLoop

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1600)

```cpp
void publishExecutionFailureOnEventLoop(
                const ndn::Name& requesterName,
                const ndn::Name& providerName,
                const ndn::Name& serviceName,
                const ndn::Name& requestId,
                const RequestMessage& requestMessage,
                const std::string& error,
                std::string selectionDigest = "");
```

### API-1904728801e2 · ndn_service_framework::ServiceProvider::completeCollaborationRoleOnEventLoop

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1608)

```cpp
void completeCollaborationRoleOnEventLoop(
                const ndn::Name& requesterName,
                const ndn::Name& providerName,
                const ndn::Name& serviceName,
                const ndn::Name& requestId,
                std::string selectionDigest = "");
```

### API-a922ea3b371e · ndn_service_framework::ServiceProvider::publishCollaborationData

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1614)

```cpp
void publishCollaborationData(const ndn::Name& requesterName,
                                          const ndn::Name& requestId,
                                          const std::string& producerRole,
                                          const std::string& keyScope,
                                          const ndn::Name& topic,
                                          const ndn::Buffer& payload);
```

### API-ad762997629f · ndn_service_framework::ServiceProvider::publishCollaborationLargeData

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1620)

```cpp
ndn::Name publishCollaborationLargeData(
                const ndn::Name& requesterName,
                const ndn::Name& requestId,
                const std::string& producerRole,
                const std::string& keyScope,
                const ndn::Name& topic,
                const ndn::Buffer& payload,
                size_t maxSegmentSize,
                int freshnessMs);
```

### API-8fea9e8fc5b9 · ndn_service_framework::ServiceProvider::publishCollaborationLargeDataNamed

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1629)

```cpp
ndn::Name publishCollaborationLargeDataNamed(
                const ndn::Name& requestId,
                const std::string& keyScope,
                const ndn::Name& dataName,
                const ndn::Buffer& payload,
                size_t maxSegmentSize,
                int freshnessMs);
```

### API-922529a44a17 · ndn_service_framework::ServiceProvider::fetchCollaborationLargeData

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1636)

```cpp
std::optional<ndn::Buffer> fetchCollaborationLargeData(
                const ndn::Name& requestId,
                const std::string& keyScope,
                const ndn::Name& dataName,
                int timeoutMs,
                std::size_t expectedSegments = 0);
```

### API-4796be8289a4 · ndn_service_framework::ServiceProvider::publishCollaborationDataV1Segments

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1642)

```cpp
bool publishCollaborationDataV1Segments(
                const ndn::Name& requestId,
                const std::string& keyScope,
                const std::vector<std::pair<ndn::Name, ndn::Buffer>>& segments,
                int freshnessMs);
```

### API-2688b430f544 · ndn_service_framework::ServiceProvider::fetchCollaborationDataV1Segments

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1647)

```cpp
std::optional<std::vector<ndn::Buffer>> fetchCollaborationDataV1Segments(
                const ndn::Name& requestId,
                const std::string& keyScope,
                const ndn::Name& producerPrefix,
                std::uint64_t operationIndex,
                const std::string& producerRank,
                const std::string& tensorDigest,
                std::size_t expectedSegments,
                std::size_t maxSegments,
                int timeoutMs,
                std::function<std::size_t(const ndn::Buffer&)>
                    segmentCountDecoder = {},
                DataV1SegmentNameFilter nameFilter = {});
```

### API-3f73f418ea50 · ndn_service_framework::ServiceProvider::publishCollaborationSignedExactData

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1660)

```cpp
bool publishCollaborationSignedExactData(
                const ndn::Name& requestId,
                const std::string& keyScope,
                const std::vector<std::pair<ndn::Name, ndn::Buffer>>& objects,
                int freshnessMs);
```

### API-e015c9f48e5f · ndn_service_framework::ServiceProvider::fetchCollaborationSignedExactData

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1665)

```cpp
std::optional<ndn::Buffer> fetchCollaborationSignedExactData(
                const ndn::Name& requestId,
                const std::string& keyScope,
                const ndn::Name& dataName,
                const ndn::Name& expectedProducer,
                int timeoutMs,
                std::function<bool()> shouldCancel = {});
```

### API-679c637f45aa · ndn_service_framework::ServiceProvider::publishCollaborationFinalResponse

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1672)

```cpp
void publishCollaborationFinalResponse(
                const ndn::Name& requesterName,
                const ndn::Name& serviceName,
                const ndn::Name& requestId,
                const RequestMessage& requestMessage,
                const ndn::Buffer& payload,
                const std::string& selectionDigest);
```

### API-02499acc82b6 · ndn_service_framework::ServiceProvider::onCollaborationDataMessage

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1679)

```cpp
void onCollaborationDataMessage(
                const ndn::svs::SVSPubSub::SubscriptionData& subscription);
```

### API-510ddd43018a · ndn_service_framework::ServiceProvider::deliverCollaborationData

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1681)

```cpp
void deliverCollaborationData(const CollaborationData& data);
```

### API-f7feb4359e3b · ndn_service_framework::ServiceProvider::addCollaborationSubscription

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1682)

```cpp
void addCollaborationSubscription(
                const ndn::Name& requestId,
                KeyScope keyScope,
                Topic topicPrefix,
                std::function<void(const CollaborationData&)> onData);
```

### API-27ca45502581 · ndn_service_framework::ServiceProvider::addCollaborationSubscription

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1687)

```cpp
void addCollaborationSubscription(
                const ndn::Name& requesterName,
                const ndn::Name& requestId,
                RequestMessage requestMessage,
                CollaborationAssignment assignment,
                KeyScope keyScope,
                Topic topicPrefix,
                std::function<void(CollaborationContext&,
                                   const CollaborationData&)> onData);
```

### API-ceb046c1cbf5 · ndn_service_framework::ServiceProvider::addCollaborationReceiveFilter

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1696)

```cpp
void addCollaborationReceiveFilter(const ndn::Name& requestId,
                                                KeyScope keyScope,
                                                Topic topicPrefix);
```

### API-543e38ad6090 · ndn_service_framework::ServiceProvider::decryptCollaborationDataOrQueue

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1699)

```cpp
void decryptCollaborationDataOrQueue(
                const ndn::Name& dataName,
                const ndn::Name& requestId,
                const ndn::Name& producer,
                const CollaborationDataMessage& message);
```

### API-1c7f79508c23 · ndn_service_framework::ServiceProvider::maybeFetchCollaborationScopeKey

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1704)

```cpp
bool maybeFetchCollaborationScopeKey(
                const ndn::Name& requestId,
                const KeyScope& keyScope);
```

### API-7ce24fd422fc · ndn_service_framework::ServiceProvider::waitForCollaborationData

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1707)

```cpp
std::vector<CollaborationData> waitForCollaborationData(
                const ndn::Name& requestId,
                const std::string& keyScope,
                const ndn::Name& topicPrefix,
                size_t minCount,
                int timeoutMs);
```

### API-3571588ed81c · ndn_service_framework::ServiceProvider::parseCollaborationAssignment

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1713)

```cpp
static CollaborationAssignment parseCollaborationAssignment(
                const ndn::Name& serviceName,
                const ndn::Buffer& payload);
```

### API-fb6a06f0c37a · ndn_service_framework::ServiceProvider::activeNacConsumer

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1716)

```cpp
ndn::nacabe::Consumer& activeNacConsumer()
```

### API-ccf6d2d21c37 · ndn_service_framework::ServiceProvider::activeNacProducer

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1721)

```cpp
ndn::nacabe::CacheProducer& activeNacProducer()
```

### API-a698349e341b · ndn_service_framework::ServiceProvider::& m_face

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1725)

```cpp
ndn::Face& m_face;
```

### API-82663c61581e · ndn_service_framework::ServiceProvider::m_scheduler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1726)

```cpp
ndn::Scheduler m_scheduler;
```

### API-1323b542a015 · ndn_service_framework::ServiceProvider::identity

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1727)

```cpp
ndn::Name identity;
```

### API-b6d355d93612 · ndn_service_framework::ServiceProvider::m_keyChain

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1728)

```cpp
ndn::KeyChain m_keyChain;
```

### API-c885186c4a3d · ndn_service_framework::ServiceProvider::* m_testSigningKeyChain

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1729)

```cpp
ndn::KeyChain* m_testSigningKeyChain = nullptr;
```

### API-c74ad22fd157 · ndn_service_framework::ServiceProvider::m_svsps

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1730)

```cpp
std::shared_ptr<ndn::svs::SVSPubSub> m_svsps;
```

### API-d1b4850ff110 · ndn_service_framework::ServiceProvider::m_localPublicationHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1731)

```cpp
LocalPublicationHandler m_localPublicationHandler;
```

### API-21cc036f7b41 · ndn_service_framework::ServiceProvider::m_streamPublicationInterceptorMutex

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1732)

```cpp
mutable std::mutex m_streamPublicationInterceptorMutex;
```

### API-624297ea67c9 · ndn_service_framework::ServiceProvider::m_streamPublicationInterceptorForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1733)

```cpp
StreamPublicationInterceptorForTest m_streamPublicationInterceptorForTest;
```

### API-6caf5478c38b · ndn_service_framework::ServiceProvider::m_streamRetentionInterceptorForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1734)

```cpp
StreamRetentionInterceptorForTest m_streamRetentionInterceptorForTest;
```

### API-cdba0f1b1ee5 · ndn_service_framework::ServiceProvider::m_streamRetentionExpiryObserverForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1735)

```cpp
StreamRetentionExpiryObserverForTest m_streamRetentionExpiryObserverForTest;
```

### API-46fbca9d353a · ndn_service_framework::ServiceProvider::validator

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1736)

```cpp
std::shared_ptr<MessageValidator> validator;
```

### API-697b8ee6b738 · ndn_service_framework::ServiceProvider::m_serviceNames

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1737)

```cpp
std::vector<std::string> m_serviceNames;
```

### API-81ffaa9fe0e3 · ndn_service_framework::ServiceProvider::nac_validator

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1740)

```cpp
ndn::ValidatorConfig nac_validator{m_face};
```

原始接口说明：

```text
//ndn::security::Validator nac_validator;
```

### API-5f1154b70250 · ndn_service_framework::ServiceProvider::identityCert

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1741)

```cpp
ndn::security::Certificate identityCert;
```

### API-1ee2ee51e46e · ndn_service_framework::ServiceProvider::signingCert

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1742)

```cpp
ndn::security::Certificate signingCert;
```

### API-fa739f438e88 · ndn_service_framework::ServiceProvider::attrAuthorityCertificate

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1743)

```cpp
ndn::security::Certificate attrAuthorityCertificate;
```

### API-81fbb89e7c43 · ndn_service_framework::ServiceProvider::nacConsumer

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1744)

```cpp
ndn::nacabe::Consumer nacConsumer;
```

### API-8ad35472965a · ndn_service_framework::ServiceProvider::m_testNacConsumer

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1745)

```cpp
std::unique_ptr<ndn::nacabe::Consumer> m_testNacConsumer;
```

### API-683592a3d78e · ndn_service_framework::ServiceProvider::m_isLocalMock

public / test-helper；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1749)

```cpp
bool m_isLocalMock = false;
```

原始接口说明：

```text
// LocalMock fixtures do not model the production Controller/AA
// bootstrap lifecycle.  Keep their refresh path fail-closed until
// the fixture explicitly completes its bootstrap.
```

### API-c9c3f3730d06 · ndn_service_framework::ServiceProvider::m_networkTelemetry

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1751)

```cpp
NetworkTelemetryStore m_networkTelemetry;
```

原始接口说明：

```text
//ndn::nacabe::Producer nacProducer;
```

### API-abf35d1d5f62 · ndn_service_framework::ServiceProvider::nacProducer

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1752)

```cpp
ndn::nacabe::CacheProducer nacProducer;
```

### API-e87a401f831d · ndn_service_framework::ServiceProvider::m_testNacProducer

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1756)

```cpp
std::unique_ptr<ndn::nacabe::CacheProducer> m_testNacProducer;
```

原始接口说明：

```text
// LocalMock may sign with a fixture-owned in-memory KeyChain.
// NAC-ABE Producer stores its KeyChain by reference, so changing
// only the direct signing pointer is insufficient.
```

### API-b30bd4c55bbb · ndn_service_framework::ServiceProvider::m_signingInfo

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1757)

```cpp
ndn::security::SigningInfo m_signingInfo;
```

### API-4a96a630c08c · ndn_service_framework::ServiceProvider::m_timelineTrace

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1761)

```cpp
bool m_timelineTrace = timelineTraceEnvEnabled();
```

原始接口说明：

```text
// Environment-driven tracing must be resolved before the native
// Python wrapper starts the Face loop; Python does not call the
// C++ setter used by the standalone examples.
```

### API-2354c93b190b · ndn_service_framework::ServiceProvider::m_currentPolicyEpoch

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1762)

```cpp
size_t m_currentPolicyEpoch = 0;
```

### API-12b6cbbda4fc · ndn_service_framework::ServiceProvider::m_controllerVersionMutex

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1763)

```cpp
mutable std::mutex m_controllerVersionMutex;
```

### API-26f9ac862707 · ndn_service_framework::ServiceProvider::m_controllerVersion

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1764)

```cpp
std::optional<ControllerVersion> m_controllerVersion;
```

### API-24eaae4c846c · ndn_service_framework::ServiceProvider::m_revocationStates

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1765)

```cpp
std::map<std::string, RevocationState> m_revocationStates;
```

### API-b258f5e6ce1f · ndn_service_framework::ServiceProvider::m_policyRefreshCoordinators

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1766)

```cpp
std::map<std::string, PolicyRefreshCoordinator>
                m_policyRefreshCoordinators;
```

### API-df4a1e5ef657 · ndn_service_framework::ServiceProvider::m_controllerPrefix

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1768)

```cpp
ndn::Name m_controllerPrefix;
```

### API-e2cdf6ac66f4 · ndn_service_framework::ServiceProvider::m_policyStatusFetchInFlight

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1769)

```cpp
std::set<std::string> m_policyStatusFetchInFlight;
```

### API-9314b31070fd · ndn_service_framework::ServiceProvider::m_policyStatusRefreshScheduled

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1770)

```cpp
std::map<std::string, ControllerVersion>
                m_policyStatusRefreshScheduled;
```

### API-554bcece10de · ndn_service_framework::ServiceProvider::m_nacDkeyRefreshPendingServices

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1777)

```cpp
std::set<std::string> m_nacDkeyRefreshPendingServices;
```

原始接口说明：

```text
// A grant-only PermissionResponse can add a service while the
// ABE generation remains unchanged. Defer one identity-scoped
// DKEY replacement for the affected service until its signed
// PolicyStatus is installed; keep this per-service so an update
// for S cannot trigger a refresh while installing T's status.
```

### API-62c6a27d3c7e · ndn_service_framework::ServiceProvider::m_lastDkeyRefreshWave

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1781)

```cpp
std::optional<ControllerVersion> m_lastDkeyRefreshWave;
```

原始接口说明：

```text
// ControllerVersion of the last permission wave for which a
// DKEY refresh was requested; same-wave service status installs
// collapse into that single identity-wide replacement fetch.
```

### API-280930a317b5 · ndn_service_framework::ServiceProvider::m_runtimeStatusStore

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1785)

```cpp
std::unique_ptr<RuntimeStatusStore> m_runtimeStatusStore;
```

原始接口说明：

```text
// Opt-in durable runtime status (NDNSF_PERSIST_RUNTIME_STATE):
// accepted per-service statuses are persisted and restored across
// process restart.  DKEY material is never stored.
```

### API-a358bab753a2 · ndn_service_framework::ServiceProvider::m_persistedRuntimeStatuses

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1786)

```cpp
std::map<std::string, RuntimeStatusStore::Record>
                m_persistedRuntimeStatuses;
```

### API-1b460a38a1c3 · ndn_service_framework::ServiceProvider::m_processStartedAtUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1790)

```cpp
const uint64_t m_processStartedAtUs = static_cast<uint64_t>(
                std::chrono::duration_cast<std::chrono::microseconds>(
                    std::chrono::system_clock::now().time_since_epoch()).count());
```

原始接口说明：

```text
// Process-incarnation fence used by deployment capability and
// readiness contracts. It is never an authorization secret.
```

### API-599b389fd603 · ndn_service_framework::ServiceProvider::m_requiredKeyEpoch

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1793)

```cpp
size_t m_requiredKeyEpoch = 0;
```

### API-dfe4e578eca5 · ndn_service_framework::ServiceProvider::m_policyGracePeriodMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1794)

```cpp
uint64_t m_policyGracePeriodMs = 0;
```

### API-6b887d738514 · ndn_service_framework::ServiceProvider::m_hybridMessageCrypto

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1795)

```cpp
HybridMessageCrypto m_hybridMessageCrypto;
```

### API-b23bf61b7bc9 · ndn_service_framework::ServiceProvider::m_hybridCryptoCounters

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1796)

```cpp
HybridCryptoCounters m_hybridCryptoCounters;
```

### API-5ab8229bbd6f · ndn_service_framework::ServiceProvider::m_cryptoProduceQueue

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1797)

```cpp
SerializedWorkerQueue m_cryptoProduceQueue{"ServiceProvider NAC-ABE produce"};
```

### API-49278d3e6662 · ndn_service_framework::ServiceProvider::m_handlerPool

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1798)

```cpp
BoundedWorkerPool m_handlerPool{"ServiceProvider application handlers"};
```

### API-89f7255140ef · ndn_service_framework::ServiceProvider::m_ackPool

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1799)

```cpp
BoundedWorkerPool m_ackPool{"ServiceProvider ACK handlers"};
```

### API-0f7f0cf5cf39 · ndn_service_framework::ServiceProvider::m_fetchStopping

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1801)

```cpp
std::shared_ptr<std::atomic<bool>> m_fetchStopping =
                std::make_shared<std::atomic<bool>>(false);
```

原始接口说明：

```text
// Shared with queued Face callbacks; inspect before dereferencing this.
```

### API-98c0a978d4a0 · ndn_service_framework::ServiceProvider::m_fetchPool

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1803)

```cpp
BoundedWorkerPool m_fetchPool{"ServiceProvider collaboration fetches"};
```

### API-f5751759608a · ndn_service_framework::ServiceProvider::chanllengeRecords

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1806)

```cpp
std::map<ndn::Name,std::pair<std::string, ndn::Name>> chanllengeRecords;
```

原始接口说明：

```text
// ChanllengeID->(Token->RequestNameWithoutRequestID)
```

### API-4e4ce2b2a167 · ndn_service_framework::ServiceProvider::authorizedRequestPrefixSet

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1808)

```cpp
std::set<ndn::Name> authorizedRequestPrefixSet;
```

原始接口说明：

```text
// RequestPrefix is a Request Name Without RequestID
```

### API-f9bc51790989 · ndn_service_framework::ServiceProvider::unauthorizedRequestMap

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1810)

```cpp
std::map<ndn::Name,ndn::Name> unauthorizedRequestMap;
```

原始接口说明：

```text
// Requests that are authorized request -> requestPrefix
```

### API-9fc71187a405 · ndn_service_framework::ServiceProvider::pendingRequests

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1816)

```cpp
std::map<ndn::Name,std::shared_ptr<RequestMessage>> pendingRequests;
```

原始接口说明：

```text
/*
                pending requests waiting for Service Selection Message;
                (/<requesterName>/<serviceName>/<requestID> -> RequestMessage)
            */
```

### API-eadc1b11c430 · ndn_service_framework::ServiceProvider::m_pendingRegistrationStates

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1822)

```cpp
std::map<ndn::Name, std::shared_ptr<RegistrationState>>
                m_pendingRegistrationStates;
```

原始接口说明：

```text
// spec182: registration-generation binding per pending request.
// Mirrors pendingRequests (same key space, same lock
// m_pendingRequestMutex, same lifetime); non-null only when the
// request was accepted against a scoped registration (legacy
// acceptances leave no binding).
```

### API-f584ee5adaf8 · ndn_service_framework::ServiceProvider::pendingProviderTokens

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1824)

```cpp
std::map<ndn::Name,std::string> pendingProviderTokens;
```

### API-7ef96b9763dd · ndn_service_framework::ServiceProvider::m_streamLifecycles

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1827)

```cpp
std::map<ndn::Name, std::shared_ptr<StreamInvocationLifecycle>>
                m_streamLifecycles;
```

原始接口说明：

```text
// Keyed by the existing requester/service/request-id pending key.
// Stream attachment never creates a second request identity.
```

### API-5272268517a3 · ndn_service_framework::ServiceProvider::m_streamPublishers

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1829)

```cpp
std::map<ndn::Name, std::shared_ptr<StreamEventPublisher>>
                m_streamPublishers;
```

### API-cd0c8192c399 · ndn_service_framework::ServiceProvider::m_streamBindings

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1831)

```cpp
std::map<ndn::Name, StreamBinding> m_streamBindings;
```

### API-6a12aa118e5f · ndn_service_framework::ServiceProvider::pendingReservationLeases

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1832)

```cpp
std::map<ndn::Name, ReservationLease> pendingReservationLeases;
```

### API-5efae5a1b30c · ndn_service_framework::ServiceProvider::m_recentProviderRequests

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1833)

```cpp
std::set<ndn::Name> m_recentProviderRequests;
```

### API-3f7b72dae8de · ndn_service_framework::ServiceProvider::m_selectedProviderRequests

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1834)

```cpp
std::set<ndn::Name> m_selectedProviderRequests;
```

### API-9b8d3d4b046c · ndn_service_framework::ServiceProvider::m_selectionDecryptsInFlight

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1835)

```cpp
std::set<ndn::Name> m_selectionDecryptsInFlight;
```

### API-63acba59e3bb · ndn_service_framework::ServiceProvider::R1AcceptedSelectionDecision

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1836)

```cpp
struct R1AcceptedSelectionDecision
```

### API-8deed03fc24d · ndn_service_framework::ServiceProvider::R1AcceptedSelectionDecision::decisionDigest

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1838)

```cpp
std::string decisionDigest;
```

### API-66d0469d1703 · ndn_service_framework::ServiceProvider::R1AcceptedSelectionDecision::providerTokenHash

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1839)

```cpp
std::string providerTokenHash;
```

### API-b6c3cd7de2e5 · ndn_service_framework::ServiceProvider::R1AcceptedSelectionDecision::decision

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1840)

```cpp
std::string decision;
```

### API-1015e1d4ae11 · ndn_service_framework::ServiceProvider::R1AcceptedSelectionDecision::receiptWire

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1841)

```cpp
ndn::Buffer receiptWire;
```

### API-20197963d495 · ndn_service_framework::ServiceProvider::R1AcceptedSelectionDecision::retainUntilMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1842)

```cpp
uint64_t retainUntilMs = 0;
```

### API-f09477c6329d · ndn_service_framework::ServiceProvider::m_r1AcceptedSelectionDecisions

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1847)

```cpp
std::map<std::string, R1AcceptedSelectionDecision>
                m_r1AcceptedSelectionDecisions;
```

原始接口说明：

```text
// First authenticated decision for a reservation is immutable.
// Retaining the token proof permits exact duplicate decisions to
// be acknowledged after their pending request has been consumed.
```

### API-60f20d59175c · ndn_service_framework::ServiceProvider::m_pendingRequestTokenHashes

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1849)

```cpp
std::map<ndn::Name, std::string> m_pendingRequestTokenHashes;
```

### API-fe0acab2f31d · ndn_service_framework::ServiceProvider::m_selectedProviderTokenHashes

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1850)

```cpp
std::map<ndn::Name, std::string> m_selectedProviderTokenHashes;
```

### API-2a277c8d5027 · ndn_service_framework::ServiceProvider::m_recentProviderRequestTokenHashes

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1851)

```cpp
std::set<std::string> m_recentProviderRequestTokenHashes;
```

### API-d762195a6ce0 · ndn_service_framework::ServiceProvider::m_consumedProviderTokenHashes

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1852)

```cpp
std::set<std::string> m_consumedProviderTokenHashes;
```

### API-3f040bdf0a12 · ndn_service_framework::ServiceProvider::RequestScopedInvocationState

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1853)

```cpp
struct RequestScopedInvocationState
```

### API-809e397d4a57 · ndn_service_framework::ServiceProvider::RequestScopedInvocationState::keys

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1855)

```cpp
RequestKeyBundle keys;
```

### API-a17b05381bf5 · ndn_service_framework::ServiceProvider::RequestScopedInvocationState::binding

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1856)

```cpp
RequestSecurityBinding binding;
```

### API-45ced86cd15e · ndn_service_framework::ServiceProvider::m_requestScopedInvocations

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1862)

```cpp
std::map<ndn::Name, RequestScopedInvocationState>
                m_requestScopedInvocations;
```

原始接口说明：

```text
// One state entry is created only after a Selection envelope is
// authenticated and consumed.  It is retained until the
// terminal Response is published so the response key cannot be
// reconstructed from a service-wide ABE key.
```

### API-eba0479d0c22 · ndn_service_framework::ServiceProvider::m_requestScopedNonceRegistry

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1864)

```cpp
NonceRegistry m_requestScopedNonceRegistry;
```

### API-b54f6201fd77 · ndn_service_framework::ServiceProvider::PreparedDeploymentExecution

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1865)

```cpp
struct PreparedDeploymentExecution
```

### API-5ac4d3a8e41f · ndn_service_framework::ServiceProvider::PreparedDeploymentExecution::requesterName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1867)

```cpp
ndn::Name requesterName;
```

### API-e03f6adf2442 · ndn_service_framework::ServiceProvider::PreparedDeploymentExecution::providerName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1868)

```cpp
ndn::Name providerName;
```

### API-2e097280c2d8 · ndn_service_framework::ServiceProvider::PreparedDeploymentExecution::serviceName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1869)

```cpp
ndn::Name serviceName;
```

### API-a9d2415d826c · ndn_service_framework::ServiceProvider::PreparedDeploymentExecution::requestId

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1870)

```cpp
ndn::Name requestId;
```

### API-90115091b544 · ndn_service_framework::ServiceProvider::PreparedDeploymentExecution::request

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1871)

```cpp
RequestMessage request;
```

### API-ca7c0f7587e5 · ndn_service_framework::ServiceProvider::PreparedDeploymentExecution::plan

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1872)

```cpp
DeploymentPlan plan;
```

### API-61fd58894128 · ndn_service_framework::ServiceProvider::PreparedDeploymentExecution::ready

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1873)

```cpp
ProviderReadyMessage ready;
```

### API-e65002ec8424 · ndn_service_framework::ServiceProvider::PreparedDeploymentExecution::selectionDigest

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1874)

```cpp
std::string selectionDigest;
```

### API-307cbecab851 · ndn_service_framework::ServiceProvider::PreparedDeploymentExecution::activationDigest

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1875)

```cpp
std::string activationDigest;
```

### API-bf157a0acfbf · ndn_service_framework::ServiceProvider::PreparedDeploymentExecution::activated

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1876)

```cpp
bool activated = false;
```

### API-4e0d84f4058b · ndn_service_framework::ServiceProvider::m_deploymentPrepareHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1878)

```cpp
DeploymentPrepareHandler m_deploymentPrepareHandler;
```

### API-d79974f8e1f6 · ndn_service_framework::ServiceProvider::m_providerReadyPublisher

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1879)

```cpp
ProviderReadyPublisher m_providerReadyPublisher;
```

### API-dd2871953a4c · ndn_service_framework::ServiceProvider::m_r1SelectionDecisionHandlers

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1880)

```cpp
std::map<ndn::Name, R1SelectionDecisionHandler>
                m_r1SelectionDecisionHandlers;
```

### API-826e0a9d8a78 · ndn_service_framework::ServiceProvider::m_genericSelectionTxnStore

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1882)

```cpp
std::shared_ptr<GenericSelectionTxnStore>
                m_genericSelectionTxnStore;
```

### API-d157ad5fe2e7 · ndn_service_framework::ServiceProvider::m_opaqueSelectionParticipants

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1884)

```cpp
std::map<ndn::Name, std::shared_ptr<OpaqueSelectionParticipant>>
                m_opaqueSelectionParticipants;
```

### API-5408f83d9d05 · ndn_service_framework::ServiceProvider::m_r1ReservationTerminalHandlers

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1886)

```cpp
std::map<ndn::Name, R1ReservationTerminalHandler>
                m_r1ReservationTerminalHandlers;
```

### API-b8263d8ef0d2 · ndn_service_framework::ServiceProvider::m_r1ReservationByRequest

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1888)

```cpp
std::map<ndn::Name, std::string> m_r1ReservationByRequest;
```

### API-51d8a8d4ee95 · ndn_service_framework::ServiceProvider::m_preparedDeployments

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1889)

```cpp
std::map<std::string, PreparedDeploymentExecution> m_preparedDeployments;
```

### API-add41ad49704 · ndn_service_framework::ServiceProvider::m_targetedProviderTokens

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1890)

```cpp
mutable std::map<std::string, TargetedProviderTokenState>
                m_targetedProviderTokens;
```

### API-9e1080bacdb9 · ndn_service_framework::ServiceProvider::m_consumedTargetedProviderTokenHashes

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1892)

```cpp
mutable std::set<std::string> m_consumedTargetedProviderTokenHashes;
```

### API-41ea3a51ef3c · ndn_service_framework::ServiceProvider::m_pendingRequestMutex

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1893)

```cpp
mutable std::mutex m_pendingRequestMutex;
```

### API-ae6000c6a2ee · ndn_service_framework::ServiceProvider::m_collaborationServices

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1894)

```cpp
std::map<ndn::Name, RegisteredCollaborationService> m_collaborationServices;
```

### API-fb92b428febb · ndn_service_framework::ServiceProvider::m_collaborationDataByRequest

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1895)

```cpp
std::map<ndn::Name, std::vector<CollaborationData>> m_collaborationDataByRequest;
```

### API-98edc2ac7b40 · ndn_service_framework::ServiceProvider::m_collaborationScopeKeysByRequest

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1896)

```cpp
std::map<ndn::Name, std::map<KeyScope, ndn::Buffer>> m_collaborationScopeKeysByRequest;
```

### API-de86cd2993f6 · ndn_service_framework::ServiceProvider::m_collaborationScopeKeyDataNamesByRequest

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1897)

```cpp
std::map<ndn::Name, std::map<KeyScope, ndn::Name>> m_collaborationScopeKeyDataNamesByRequest;
```

### API-94e2306fd620 · ndn_service_framework::ServiceProvider::m_collaborationServiceNamesByRequest

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1898)

```cpp
std::map<ndn::Name, ndn::Name> m_collaborationServiceNamesByRequest;
```

### API-fa5b00d936d2 · ndn_service_framework::ServiceProvider::m_collaborationRegistrationStates

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1904)

```cpp
std::map<ndn::Name, std::shared_ptr<RegistrationState>>
                m_collaborationRegistrationStates;
```

原始接口说明：

```text
// spec182: registration-generation binding per collaboration
// request.  Mirrors m_collaborationServiceNamesByRequest (same
// requestId key space, same lock m_collaborationMutex, same
// lifetime); non-null only when the collaboration request was
// accepted against a scoped collaboration registration.
```

### API-ddfc9ff6daec · ndn_service_framework::ServiceProvider::m_collaborationScopeKeyFetchesInFlight

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1906)

```cpp
std::set<std::string> m_collaborationScopeKeyFetchesInFlight;
```

### API-a942b5d1a61f · ndn_service_framework::ServiceProvider::m_pendingEncryptedCollaborationData

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1907)

```cpp
std::map<ndn::Name, std::vector<PendingEncryptedCollaborationData>> m_pendingEncryptedCollaborationData;
```

### API-dc46d182d1c8 · ndn_service_framework::ServiceProvider::m_collaborationArtifacts

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1908)

```cpp
std::map<std::string, ndn::Buffer> m_collaborationArtifacts;
```

### API-78b0e064a5f7 · ndn_service_framework::ServiceProvider::m_collaborationSubscriptions

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1909)

```cpp
std::vector<CollaborationSubscription> m_collaborationSubscriptions;
```

### API-d5524848de8f · ndn_service_framework::ServiceProvider::m_collaborationMutex

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1910)

```cpp
std::mutex m_collaborationMutex;
```

### API-1a2e12883725 · ndn_service_framework::ServiceProvider::m_collaborationCv

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1911)

```cpp
std::condition_variable m_collaborationCv;
```

### API-b895aa0f34a9 · ndn_service_framework::ServiceProvider::m_collaborationSequence

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1912)

```cpp
std::atomic<uint64_t> m_collaborationSequence{0};
```

### API-e4797fbbaecb · ndn_service_framework::ServiceProvider::m_selectedOutstandingRequests

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1913)

```cpp
std::atomic<size_t> m_selectedOutstandingRequests{0};
```

### API-4622029069b4 · ndn_service_framework::ServiceProvider::m_cleanupInvocationCount

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1914)

```cpp
size_t m_cleanupInvocationCount = 0;
```

### API-3b44894a82bf · ndn_service_framework::ServiceProvider::m_tokenConsumeCount

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1915)

```cpp
size_t m_tokenConsumeCount = 0;
```

### API-6100f8c74690 · ndn_service_framework::ServiceProvider::m_pendingRequestTimeoutGrace

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1916)

```cpp
ndn::time::milliseconds m_pendingRequestTimeoutGrace{1000};
```

### API-88aa8dec28cb · ndn_service_framework::ServiceProvider::m_performanceMode

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1917)

```cpp
bool m_performanceMode = false;
```

### API-c2ba295d7acf · ndn_service_framework::ServiceProvider::m_useTokens

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1918)

```cpp
bool m_useTokens = true;
```

### API-ea7f5617011c · ndn_service_framework::ServiceProvider::m_adaptiveAckAdmission

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1919)

```cpp
bool m_adaptiveAckAdmission = false;
```

### API-568c9f0cf2fe · ndn_service_framework::ServiceProvider::m_providerAckMaxPending

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1920)

```cpp
size_t m_providerAckMaxPending = 0;
```

### API-31005f93e288 · ndn_service_framework::ServiceProvider::m_providerAckMaxEventLoopLag

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1921)

```cpp
ndn::time::milliseconds m_providerAckMaxEventLoopLag{0};
```

### API-9b50528558ca · ndn_service_framework::ServiceProvider::m_providerAckMaxSelectionLag

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1922)

```cpp
ndn::time::milliseconds m_providerAckMaxSelectionLag{0};
```

### API-0bc9fd5ebdaa · ndn_service_framework::ServiceProvider::m_pendingCleanupDeadlineMutex

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1923)

```cpp
mutable std::mutex m_pendingCleanupDeadlineMutex;
```

### API-43b8361f909a · ndn_service_framework::ServiceProvider::m_pendingCleanupDeadlines

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1924)

```cpp
std::map<ndn::Name, std::chrono::steady_clock::time_point>
                m_pendingCleanupDeadlines;
```

### API-b1e232914602 · ndn_service_framework::ServiceProvider::m_pendingCleanupExpiryUnixMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1926)

```cpp
std::map<ndn::Name, uint64_t> m_pendingCleanupExpiryUnixMs;
```

### API-fd3021ca6842 · ndn_service_framework::ServiceProvider::m_authoritativePendingCleanupDeadlines

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1927)

```cpp
std::set<ndn::Name> m_authoritativePendingCleanupDeadlines;
```

### API-2cd81553bdb6 · ndn_service_framework::ServiceProvider::m_providerRequestLifecycleStatuses

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1928)

```cpp
std::map<ndn::Name, ProviderRequestLifecycleStatus>
                m_providerRequestLifecycleStatuses;
```

### API-d1448250efda · ndn_service_framework::ServiceProvider::m_selectionExecutionStatuses

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1930)

```cpp
std::map<std::string, SelectionExecutionStatus>
                m_selectionExecutionStatuses;
```

### API-6a8ce5f63811 · ndn_service_framework::ServiceProvider::m_providerRequestLifecycleCallback

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1932)

```cpp
ProviderRequestLifecycleCallback m_providerRequestLifecycleCallback;
```

### API-755c3d80ecd4 · ndn_service_framework::ServiceProvider::m_providerAdmissionCounters

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1933)

```cpp
std::map<std::string, uint64_t> m_providerAdmissionCounters;
```

### API-1b873421ea01 · ndn_service_framework::ServiceProvider::m_genericAdmissionLeases

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1934)

```cpp
ProviderAdmissionLeaseTable m_genericAdmissionLeases;
```

### API-beb7330d60bc · ndn_service_framework::ServiceProvider::random

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1936)

```cpp
ndn::random::RandomNumberEngine random;
```

### API-1c0ca675a660 · ndn_service_framework::ServiceProvider::m_IMS

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1938)

```cpp
ndn::InMemoryStorageFifo m_IMS;
```

### API-7d4c38ab5b2b · ndn_service_framework::ServiceProvider::_cache_mutex

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1939)

```cpp
std::mutex _cache_mutex;
```

### API-4bbbfff90da1 · ndn_service_framework::ServiceProvider::PendingImsInterest

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1940)

```cpp
struct PendingImsInterest
```

### API-3b6ef13301cb · ndn_service_framework::ServiceProvider::PendingImsInterest::interest

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1942)

```cpp
ndn::Interest interest;
```

### API-9ac237ccd980 · ndn_service_framework::ServiceProvider::PendingImsInterest::requestedAt

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1943)

```cpp
ndn::time::steady_clock::time_point requestedAt;
```

### API-8a2e6f15dbb2 · ndn_service_framework::ServiceProvider::PendingImsInterest::expiresAt

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1944)

```cpp
ndn::time::steady_clock::time_point expiresAt;
```

### API-bcb4c68daeaa · ndn_service_framework::ServiceProvider::m_pendingImsInterestsByName

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1946)

```cpp
std::map<ndn::Name, std::deque<PendingImsInterest>> m_pendingImsInterestsByName;
```

### API-0a15463aae37 · ndn_service_framework::ServiceProvider::m_pendingPrefixImsInterests

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1947)

```cpp
std::vector<PendingImsInterest> m_pendingPrefixImsInterests;
```

### API-8bbb40b705cd · ndn_service_framework::ServiceProvider::m_pendingImsInsertionOrder

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1948)

```cpp
std::deque<ndn::Name> m_pendingImsInsertionOrder;
```

### API-f3a032033b03 · ndn_service_framework::ServiceProvider::m_pendingImsInterestCount

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1949)

```cpp
size_t m_pendingImsInterestCount = 0;
```

### API-03165f8d6a50 · ndn_service_framework::ServiceProvider::m_ServiceDiscovery

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1951)

```cpp
OptionalServiceDiscovery m_ServiceDiscovery;
```

### API-d8cedc19a00a · ndn_service_framework::ServiceProvider::m_ndnsdMeta

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1952)

```cpp
std::map<std::string, std::string> m_ndnsdMeta;
```

### API-1f197842f9d6 · ndn_service_framework::ServiceProvider::m_ndnsdMetaMutex

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1953)

```cpp
mutable std::mutex m_ndnsdMetaMutex;
```

### API-80499de488eb · ndn_service_framework::ServiceProvider::m_ndnsdScheduler

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1954)

```cpp
std::unique_ptr<ndn::Scheduler> m_ndnsdScheduler;
```

### API-630daeae9763 · ndn_service_framework::ServiceProvider::m_ndnsdHeartbeatEvent

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1955)

```cpp
ndn::scheduler::ScopedEventId m_ndnsdHeartbeatEvent;
```

### API-bc496e281665 · ndn_service_framework::ServiceProvider::m_ndnsdHeartbeatIntervalSeconds

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1956)

```cpp
int m_ndnsdHeartbeatIntervalSeconds = 0;
```

### API-8a5af24f1060 · ndn_service_framework::ServiceProvider::m_authorizations

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1958)

```cpp
ServiceAuthorizationTable m_authorizations;
```

### API-4c4deef14d2e · ndn_service_framework::ServiceProvider::m_configManager

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1960)

```cpp
ConfigManager m_configManager;
```

### API-8dff57fda8e9 · ndn_service_framework::ServiceProvider::m_sessionIDMap

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1962)

```cpp
std::map<ndn::Name, int> m_sessionIDMap;
```

### API-a89e6d5c1dc4 · ndn_service_framework::ServiceProvider::svs_mutex

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1964)

```cpp
std::mutex svs_mutex;
```

### API-485fb045be04 · ndn_service_framework::ServiceProvider::m_registrationControl

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1971)

```cpp
std::shared_ptr<RegistrationControl> m_registrationControl;
```

原始接口说明：

```text
// spec182 scoped registration bookkeeping.  All registration-path
// code runs on the Face event thread, so m_registrationGeneration
// needs no extra lock; m_registrationControl->mutex serializes
// only against the Provider destructor.  m_registrationControl is
// null only if construction never completed.
```

### API-3f9a9dc32cd2 · ndn_service_framework::ServiceProvider::m_registrationGeneration

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1972)

```cpp
uint64_t m_registrationGeneration = 0;
```

### API-523e086d4e3b · ndn_service_framework::ServiceProvider::m_services

public / declared-interface；[源码](../../ndn-service-framework/ServiceProvider.hpp#L1974)

```cpp
std::map<ServiceKey, RegisteredService> m_services;
```

## ndn-service-framework/ServiceUser.hpp

源码 SHA-256：`ef797725a923b2058faa9f48d68cbc4591275154b78c995588c069080396a5c2`。

### API-1da6177a0572 · ndn_service_framework::AckInfo

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L40)

```cpp
struct AckInfo
```

### API-8f7a4465fdbc · ndn_service_framework::AckInfo::providerName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L41)

```cpp
ndn::Name providerName;
```

### API-88a6233147a6 · ndn_service_framework::AckInfo::serviceName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L42)

```cpp
ndn::Name serviceName;
```

### API-31570211469d · ndn_service_framework::AckInfo::requestID

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L43)

```cpp
ndn::Name requestID;
```

### API-4c4148ecc5a3 · ndn_service_framework::AckAuthenticationEvidence

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L56)

```cpp
struct AckAuthenticationEvidence
```

### API-cc4701b411d5 · ndn_service_framework::AckAuthenticationEvidence::signerIdentity

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L58)

```cpp
std::string signerIdentity;
```

### API-c9f35c068f5e · ndn_service_framework::AckAuthenticationEvidence::signerKeyLocator

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L59)

```cpp
std::string signerKeyLocator;
```

### API-f686344fd629 · ndn_service_framework::AckAuthenticationEvidence::wireDigest

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L60)

```cpp
std::string wireDigest;
```

### API-b7a4fddd8b6b · ndn_service_framework::AckAuthenticationEvidence::trustSchemaValidated

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L63)

```cpp
bool trustSchemaValidated = false;
```

原始接口说明：

```text
// True only for an ACK delivered through the validated ServiceUser
// subscription path. Direct/unit fixtures leave this false.
```

### API-6cf209d301f0 · ndn_service_framework::AckSelectionCandidate

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L66)

```cpp
struct AckSelectionCandidate
```

### API-d957343d9347 · ndn_service_framework::AckSelectionCandidate::providerName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L68)

```cpp
ndn::Name providerName;
```

### API-f614e1681e23 · ndn_service_framework::AckSelectionCandidate::serviceName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L69)

```cpp
ndn::Name serviceName;
```

### API-306bf0f04565 · ndn_service_framework::AckSelectionCandidate::requestId

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L70)

```cpp
ndn::Name requestId;
```

### API-4aee427f399e · ndn_service_framework::AckSelectionCandidate::ack

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L71)

```cpp
ndn_service_framework::RequestAckMessage ack;
```

### API-d0a5d84c4475 · ndn_service_framework::AckSelectionCandidate::telemetry

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L72)

```cpp
std::optional<ndn_service_framework::NetworkTelemetrySnapshot> telemetry;
```

### API-7e410496f216 · ndn_service_framework::AckSelectionCandidate::authenticationEvidence

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L73)

```cpp
AckAuthenticationEvidence authenticationEvidence{};
```

### API-574c42882eb8 · ndn_service_framework::ProviderId

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L76)

```cpp
using ProviderId = ndn::Name;
```

### API-68447c5f6bf7 · ndn_service_framework::ServiceName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L77)

```cpp
using ServiceName = ndn::Name;
```

### API-644692a3425c · ndn_service_framework::RequestId

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L78)

```cpp
using RequestId = ndn::Name;
```

### API-4c91c36362c1 · ndn_service_framework::RequestPayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L79)

```cpp
using RequestPayload = ndn::Buffer;
```

### API-55751a75759b · ndn_service_framework::ResponsePayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L80)

```cpp
using ResponsePayload = ndn::Buffer;
```

### API-a50de800cc56 · ndn_service_framework::AckCandidate

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L81)

```cpp
using AckCandidate = AckSelectionCandidate;
```

### API-503f70a3b02b · ndn_service_framework::CollaborationRole

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L82)

```cpp
using CollaborationRole = std::string;
```

### API-bd95795f316f · ndn_service_framework::KeyScope

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L83)

```cpp
using KeyScope = std::string;
```

### API-4af12b9fd3dc · ndn_service_framework::AckSelectionPolicy

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L85)

```cpp
class AckSelectionPolicy
```

### API-63f4e3ee065f · ndn_service_framework::AckSelectionPolicy::select

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L88)

```cpp
virtual std::vector<ProviderId>
        select(const std::vector<AckCandidate>& candidates) const = 0;
```

### API-13ed1f35f87f · ndn_service_framework::AckSelectionPolicy::requestStrategy

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L91)

```cpp
virtual size_t
        requestStrategy() const
```

### API-c446b07af7d5 · ndn_service_framework::AckSelectionPolicy::~AckSelectionPolicy

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L97)

```cpp
virtual ~AckSelectionPolicy() = default;
```

### API-99ab4baaaaec · ndn_service_framework::strategy::FirstResponding

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L102)

```cpp
extern const std::shared_ptr<const AckSelectionPolicy> FirstResponding;
```

### API-6e6e1cf538f6 · ndn_service_framework::strategy::RandomSelection

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L103)

```cpp
extern const std::shared_ptr<const AckSelectionPolicy> RandomSelection;
```

### API-e097d849f282 · ndn_service_framework::strategy::AllSelected

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L104)

```cpp
extern const std::shared_ptr<const AckSelectionPolicy> AllSelected;
```

### API-78b8bdf2f907 · ndn_service_framework::CollaborationRoleSpec

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L107)

```cpp
struct CollaborationRoleSpec
```

### API-99411eedf8b1 · ndn_service_framework::CollaborationRoleSpec::role

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L109)

```cpp
CollaborationRole role;
```

### API-c2a8c0dae878 · ndn_service_framework::CollaborationRoleSpec::service

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L110)

```cpp
ServiceName service;
```

### API-c810ceb0840b · ndn_service_framework::CollaborationRoleSpec::requiredArtifact

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L111)

```cpp
ndn::Name requiredArtifact;
```

### API-e6eaac2c5ce2 · ndn_service_framework::CollaborationRoleSpec::allowDynamicProvisioning

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L112)

```cpp
bool allowDynamicProvisioning = false;
```

### API-9358500cf605 · ndn_service_framework::CollaborationRoleSpec::provisioningTimeoutMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L113)

```cpp
int provisioningTimeoutMs = 30000;
```

### API-21433141803f · ndn_service_framework::CollaborationRoleSpec::appRequirement

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L114)

```cpp
ndn::Buffer appRequirement;
```

### API-85d983e506e5 · ndn_service_framework::CollaborationRoleSpec::assignmentPayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L117)

```cpp
ndn::Buffer assignmentPayload;
```

原始接口说明：

```text
// Optional exact participant payload chosen after ACK closure.  This is
// generic opaque application data; NDNSF does not parse its schema.
```

### API-08c28dcfd45a · ndn_service_framework::CollaborationRoleSpec::minProviders

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L118)

```cpp
size_t minProviders = 1;
```

### API-dfe6ed353515 · ndn_service_framework::CollaborationRoleSpec::maxProviders

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L119)

```cpp
size_t maxProviders = 1;
```

### API-197e9155254c · ndn_service_framework::CollaborationRoleSpec::terminalResponseOwner

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L123)

```cpp
bool terminalResponseOwner = false;
```

原始接口说明：

```text
// A streamed collaboration has exactly one user-facing terminal
// owner.  Other selected roles may publish internal data and must
// complete without claiming the shared End/Response lifecycle.
```

### API-18f2d660a684 · ndn_service_framework::CollaborationKeyScope

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L126)

```cpp
struct CollaborationKeyScope
```

### API-6f60c5e9be60 · ndn_service_framework::CollaborationKeyScope::name

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L128)

```cpp
KeyScope name;
```

### API-7e65624fee52 · ndn_service_framework::CollaborationKeyScope::roles

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L129)

```cpp
std::vector<CollaborationRole> roles;
```

### API-5f014e9f546a · ndn_service_framework::CollaborationDependency

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L132)

```cpp
struct CollaborationDependency
```

### API-4c3ee813bae3 · ndn_service_framework::CollaborationDependency::producers

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L134)

```cpp
std::vector<CollaborationRole> producers;
```

### API-e27d8b179301 · ndn_service_framework::CollaborationDependency::consumers

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L135)

```cpp
std::vector<CollaborationRole> consumers;
```

### API-b3b3fdc43f9f · ndn_service_framework::CollaborationDependency::keyScope

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L136)

```cpp
KeyScope keyScope;
```

### API-a07da240ef65 · ndn_service_framework::CollaborationDependency::topicPrefix

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L137)

```cpp
ndn::Name topicPrefix;
```

### API-bce6ff39aaf8 · ndn_service_framework::CollaborationDependency::required

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L138)

```cpp
bool required = true;
```

### API-c65c77bc21cd · ndn_service_framework::SelectedParticipant

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L141)

```cpp
struct SelectedParticipant
```

### API-e7f280d85180 · ndn_service_framework::SelectedParticipant::role

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L143)

```cpp
CollaborationRole role;
```

### API-78ddf1d337c8 · ndn_service_framework::SelectedParticipant::service

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L144)

```cpp
ServiceName service;
```

### API-957578f710e8 · ndn_service_framework::SelectedParticipant::provider

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L145)

```cpp
ProviderId provider;
```

### API-bc309e8ced6e · ndn_service_framework::SelectedParticipant::assignedArtifact

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L146)

```cpp
ndn::Name assignedArtifact;
```

### API-bb51fceb20e2 · ndn_service_framework::SelectedParticipant::requiresProvisioning

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L147)

```cpp
bool requiresProvisioning = false;
```

### API-bebef90e71dd · ndn_service_framework::SelectedParticipant::provisioningTimeoutMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L148)

```cpp
int provisioningTimeoutMs = 0;
```

### API-0bac62dbe15b · ndn_service_framework::SelectedParticipant::assignmentPayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L149)

```cpp
ndn::Buffer assignmentPayload;
```

### API-1b4594306741 · ndn_service_framework::SelectedParticipant::ack

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L150)

```cpp
AckCandidate ack;
```

### API-36cb0e4610de · ndn_service_framework::SelectedParticipant::artifactDataName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L153)

```cpp
ndn::Name artifactDataName;
```

原始接口说明：

```text
// Assignment-bound canonical root Data name. Keep this at the end so
// existing aggregate initializers remain source-compatible.
```

### API-9073a1f97916 · ndn_service_framework::ParticipantSelectionPolicy

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L156)

```cpp
class ParticipantSelectionPolicy
```

### API-9f0a4caff0b4 · ndn_service_framework::ParticipantSelectionPolicy::select

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L159)

```cpp
virtual std::vector<SelectedParticipant>
        select(const std::vector<AckCandidate>& candidates,
               const std::vector<CollaborationRoleSpec>& roles) const = 0;
```

### API-08d20c38b468 · ndn_service_framework::ParticipantSelectionPolicy::~ParticipantSelectionPolicy

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L163)

```cpp
virtual ~ParticipantSelectionPolicy() = default;
```

### API-8efdcffdcf72 · ndn_service_framework::CollaborationPlan

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L166)

```cpp
struct CollaborationPlan
```

### API-5c650c6cf71a · ndn_service_framework::CollaborationPlan::ackCollectionTimeMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L168)

```cpp
int ackCollectionTimeMs = 200;
```

### API-618c489081c4 · ndn_service_framework::CollaborationPlan::timeoutMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L169)

```cpp
int timeoutMs = 5000;
```

### API-b5e69e28f2ed · ndn_service_framework::CollaborationPlan::roles

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L170)

```cpp
std::vector<CollaborationRoleSpec> roles;
```

### API-39e57a672a42 · ndn_service_framework::CollaborationPlan::keyScopes

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L171)

```cpp
std::vector<CollaborationKeyScope> keyScopes;
```

### API-164973cf2345 · ndn_service_framework::CollaborationPlan::dependencies

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L172)

```cpp
std::vector<CollaborationDependency> dependencies;
```

### API-ecfd390e86a2 · ndn_service_framework::CollaborationPlan::sharedAssignmentMetadata

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L175)

```cpp
ndn::Buffer sharedAssignmentMetadata;
```

原始接口说明：

```text
// Generic metadata shared by selected participants and transported
// separately from each exact opaque assignment.
```

### API-87ca16a06ec3 · ndn_service_framework::CollaborationPlan::participantSelector

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L176)

```cpp
std::shared_ptr<const ParticipantSelectionPolicy> participantSelector;
```

### API-0a0193928ecf · ndn_service_framework::VerifiedCollaborationData

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L182)

```cpp
struct VerifiedCollaborationData
```

### API-509fcf355770 · ndn_service_framework::VerifiedCollaborationData::dataName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L184)

```cpp
ndn::Name dataName;
```

### API-c5322c14578e · ndn_service_framework::VerifiedCollaborationData::requestId

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L185)

```cpp
ndn::Name requestId;
```

### API-bd78f54214d1 · ndn_service_framework::VerifiedCollaborationData::keyScope

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L186)

```cpp
KeyScope keyScope;
```

### API-52f7362191a3 · ndn_service_framework::VerifiedCollaborationData::topic

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L187)

```cpp
ndn::Name topic;
```

### API-54120ead20db · ndn_service_framework::VerifiedCollaborationData::producer

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L188)

```cpp
ndn::Name producer;
```

### API-525be5351b61 · ndn_service_framework::VerifiedCollaborationData::producerRole

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L189)

```cpp
CollaborationRole producerRole;
```

### API-9a67a49e37e3 · ndn_service_framework::VerifiedCollaborationData::sequence

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L190)

```cpp
uint64_t sequence = 0;
```

### API-a3a9fa7f90ae · ndn_service_framework::VerifiedCollaborationData::payload

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L191)

```cpp
ndn::Buffer payload;
```

### API-e8727da40455 · ndn_service_framework::VerifiedCollaborationData::signerCertificate

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L192)

```cpp
std::string signerCertificate;
```

### API-5862f46cc040 · ndn_service_framework::VerifiedCollaborationData::wireDigest

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L193)

```cpp
std::string wireDigest;
```

### API-32e3c334287b · ndn_service_framework::CollaborationAckClosure

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L196)

```cpp
struct CollaborationAckClosure
```

### API-43bbb164ce0b · ndn_service_framework::CollaborationAckClosure::requestId

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L198)

```cpp
RequestId requestId;
```

### API-eae479996c26 · ndn_service_framework::CollaborationAckClosure::candidates

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L199)

```cpp
std::vector<AckCandidate> candidates;
```

### API-0903f74d0216 · ndn_service_framework::CollaborationAckClosure::digest

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L200)

```cpp
std::string digest;
```

### API-33e933f05017 · ndn_service_framework::CollaborationAckClosure::closedAtUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L201)

```cpp
uint64_t closedAtUs = 0;
```

### API-15569891c0d7 · ndn_service_framework::CollaborationAckClosure::requestDeadlineUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L202)

```cpp
uint64_t requestDeadlineUs = 0;
```

### API-9af5ec11c34d · ndn_service_framework::CollaborationAckClosedHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L205)

```cpp
using CollaborationAckClosedHandler =
        std::function<void(const CollaborationAckClosure&)>;
```

### API-abd89ba5fa85 · ndn_service_framework::CollaborationAckCoverageHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L212)

```cpp
using CollaborationAckCoverageHandler =
        std::function<bool(const std::vector<AckCandidate>&)>;
```

原始接口说明：

```text
// Optional, application-owned ACK coverage hook.  The hook is evaluated
// only after a candidate has passed the normal ACK authentication and
// replay checks.  Returning true closes the ACK window early; it does not
// select providers, create a plan, or bypass ACK_CLOSED immutability.
```

### API-6ee9c701b6f5 · ndn_service_framework::PreparedServiceRequest

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L215)

```cpp
struct PreparedServiceRequest
```

### API-4b81eed0fab1 · ndn_service_framework::PreparedServiceRequest::serviceName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L217)

```cpp
ndn::Name serviceName;
```

### API-128e7f2445ca · ndn_service_framework::PreparedServiceRequest::requestId

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L218)

```cpp
ndn::Name requestId;
```

### API-ec6b89713a11 · ndn_service_framework::PreparedServiceRequest::used

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L219)

```cpp
mutable bool used = false;
```

### API-0e9bd80ed86a · ndn_service_framework::LargeDataPublishResult

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L222)

```cpp
struct LargeDataPublishResult
```

### API-6dd7970e6d9c · ndn_service_framework::LargeDataPublishResult::success

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L224)

```cpp
bool success = false;
```

### API-7e355df50571 · ndn_service_framework::LargeDataPublishResult::encryptedDataName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L225)

```cpp
ndn::Name encryptedDataName;
```

### API-8988266544f4 · ndn_service_framework::LargeDataPublishResult::objectId

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L226)

```cpp
std::string objectId;
```

### API-4ef2b899cf55 · ndn_service_framework::LargeDataPublishResult::plaintextSize

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L227)

```cpp
size_t plaintextSize = 0;
```

### API-fe7c10de97ab · ndn_service_framework::LargeDataPublishResult::contentDigest

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L228)

```cpp
std::string contentDigest;
```

### API-b534333b1fe5 · ndn_service_framework::LargeDataPublishResult::manifestDigest

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L229)

```cpp
std::string manifestDigest;
```

### API-7bc58be3c420 · ndn_service_framework::LargeDataPublishResult::authorizationScope

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L230)

```cpp
std::string authorizationScope;
```

### API-66036f2faf90 · ndn_service_framework::LargeDataPublishResult::protectionEpoch

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L231)

```cpp
std::string protectionEpoch;
```

### API-8c5af3b80c04 · ndn_service_framework::LargeDataPublishResult::encrypted

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L232)

```cpp
bool encrypted = true;
```

### API-c22ebf94a96f · ndn_service_framework::LargeDataPublishResult::errorMessage

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L233)

```cpp
std::string errorMessage;
```

### API-e8fffa0768e5 · ndn_service_framework::LargeDataReferenceRequestResult

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L236)

```cpp
struct LargeDataReferenceRequestResult
```

### API-ebf72332a343 · ndn_service_framework::LargeDataReferenceRequestResult::success

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L238)

```cpp
bool success = false;
```

### API-63f50cec9aa5 · ndn_service_framework::LargeDataReferenceRequestResult::usedLargeDataReference

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L239)

```cpp
bool usedLargeDataReference = false;
```

### API-ee67dbd4986b · ndn_service_framework::LargeDataReferenceRequestResult::requestMessage

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L240)

```cpp
ndn_service_framework::RequestMessage requestMessage;
```

### API-91093509d9f6 · ndn_service_framework::LargeDataReferenceRequestResult::largeData

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L241)

```cpp
LargeDataPublishResult largeData;
```

### API-1b0f2826ce03 · ndn_service_framework::LargeDataReferenceRequestResult::errorMessage

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L242)

```cpp
std::string errorMessage;
```

### API-e562105669c6 · ndn_service_framework::Timeout_Callback

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L245)

```cpp
using Timeout_Callback = std::function<void(const std::string & reason)>;
```

### API-0731bb0e18ca · ndn_service_framework::ServiceUser

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L247)

```cpp
class ServiceUser
```

### API-66e0a0005266 · ndn_service_framework::ServiceUser::postToIo

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L254)

```cpp
void postToIo(std::function<void()> task) const;
```

原始接口说明：

```text
/** Queue work on this user's existing Face I/O context.
             * Never invokes inline or starts an event loop. The application
             * keeps the Face alive and running; the task must retain its
             * own owners and contain its asynchronous exceptions. */
```

### API-0342779f279f · ndn_service_framework::ServiceUser::isOnIoThread

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L258)

```cpp
bool isOnIoThread() const;
```

原始接口说明：

```text
/** True while this thread executes the existing Face context.
             * Blocking application waits must be rejected on that thread. */
```

### API-110eae5a0e96 · ndn_service_framework::ServiceUser::AcksHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L260)

```cpp
using AcksHandler =
                std::function<std::vector<ndn_service_framework::RequestAckMessage>(
                    const std::vector<ndn_service_framework::RequestAckMessage>&)>;
```

### API-a425be413cf0 · ndn_service_framework::ServiceUser::AckCandidatesHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L264)

```cpp
using AckCandidatesHandler =
                std::function<std::vector<ndn_service_framework::AckSelectionCandidate>(
                    const std::vector<ndn_service_framework::AckSelectionCandidate>&)>;
```

### API-d9b8774000f4 · ndn_service_framework::ServiceUser::AckSelectionStrategy

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L268)

```cpp
enum class AckSelectionStrategy
```

### API-f286992ebf31 · ndn_service_framework::ServiceUser::AckSelectionStrategy::FirstRespondingSelection

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L270)

```cpp
FirstRespondingSelection
```

### API-62dd7ddd96a5 · ndn_service_framework::ServiceUser::AckSelectionStrategy::RandomSelection

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L271)

```cpp
RandomSelection
```

### API-cc2a8eb0a5c5 · ndn_service_framework::ServiceUser::AckSelectionStrategy::AllSelected

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L272)

```cpp
AllSelected
```

### API-f08ea3ea3f6b · ndn_service_framework::ServiceUser::AckSelectionStrategy::CustomSelectionStrategy

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L273)

```cpp
CustomSelectionStrategy
```

### API-06343fb680b2 · ndn_service_framework::ServiceUser::ResponseHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L276)

```cpp
using ResponseHandler =
                std::function<void(const ndn_service_framework::ResponseMessage&)>;
```

### API-1407ce2e6874 · ndn_service_framework::ServiceUser::TimeoutHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L279)

```cpp
using TimeoutHandler =
                std::function<void(const ndn::Name&)>;
```

### API-5e0f185844a5 · ndn_service_framework::ServiceUser::SelectionStatusOptions

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L282)

```cpp
struct SelectionStatusOptions
```

### API-01eadd0ee13c · ndn_service_framework::ServiceUser::SelectionStatusOptions::SelectionStatusOptions

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L284)

```cpp
explicit SelectionStatusOptions(bool enabled = true,
                                                int queryIntervalMs = 1000,
                                                int queryTimeoutMs = 500)
```

### API-d649daba175d · ndn_service_framework::ServiceUser::SelectionStatusOptions::enabled

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L293)

```cpp
bool enabled;
```

### API-b2802f5dcae4 · ndn_service_framework::ServiceUser::SelectionStatusOptions::queryIntervalMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L294)

```cpp
int queryIntervalMs;
```

### API-90ef12c10044 · ndn_service_framework::ServiceUser::SelectionStatusOptions::queryTimeoutMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L295)

```cpp
int queryTimeoutMs;
```

### API-177171c74e9c · ndn_service_framework::ServiceUser::SelectionStatusHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L298)

```cpp
using SelectionStatusHandler =
                std::function<void(const SelectionExecutionStatus&)>;
```

### API-cc0854bddd09 · ndn_service_framework::ServiceUser::SelectionStatusTimeoutHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L301)

```cpp
using SelectionStatusTimeoutHandler =
                std::function<void(const ndn::Name& requestId,
                                   const std::vector<SelectionExecutionStatus>& statuses)>;
```

### API-3c8258fb8daa · ndn_service_framework::ServiceUser::RequestLifecycleState

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L305)

```cpp
enum class RequestLifecycleState
```

### API-2191cc0c6c96 · ndn_service_framework::ServiceUser::RequestLifecycleState::QUEUED_LOCAL

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L307)

```cpp
QUEUED_LOCAL
```

### API-82df9e698921 · ndn_service_framework::ServiceUser::RequestLifecycleState::ADMISSION_DELAYED

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L308)

```cpp
ADMISSION_DELAYED
```

### API-08ea02671f25 · ndn_service_framework::ServiceUser::RequestLifecycleState::ADMITTED

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L309)

```cpp
ADMITTED
```

### API-a85940784922 · ndn_service_framework::ServiceUser::RequestLifecycleState::REQUEST_PUBLISHED

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L310)

```cpp
REQUEST_PUBLISHED
```

### API-baa8f53dfd2d · ndn_service_framework::ServiceUser::RequestLifecycleState::ACK_MATCHED

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L311)

```cpp
ACK_MATCHED
```

### API-9a2979ca32f4 · ndn_service_framework::ServiceUser::RequestLifecycleState::PROVIDER_SELECTED

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L312)

```cpp
PROVIDER_SELECTED
```

### API-0e892a8d63b0 · ndn_service_framework::ServiceUser::RequestLifecycleState::SELECTION_PUBLISHED

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L313)

```cpp
SELECTION_PUBLISHED
```

### API-b6aa54d185c7 · ndn_service_framework::ServiceUser::RequestLifecycleState::RESPONSE_OBSERVED

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L314)

```cpp
RESPONSE_OBSERVED
```

### API-b69be178178a · ndn_service_framework::ServiceUser::RequestLifecycleState::RESPONSE_DECRYPTED

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L315)

```cpp
RESPONSE_DECRYPTED
```

### API-c5034b865cfc · ndn_service_framework::ServiceUser::RequestLifecycleState::CALLBACK_FIRED

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L316)

```cpp
CALLBACK_FIRED
```

### API-768d965007ff · ndn_service_framework::ServiceUser::RequestLifecycleState::COMPLETED

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L317)

```cpp
COMPLETED
```

### API-b494d092357f · ndn_service_framework::ServiceUser::RequestLifecycleState::ADMISSION_REJECTED

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L318)

```cpp
ADMISSION_REJECTED
```

### API-cd07d5decf8f · ndn_service_framework::ServiceUser::RequestLifecycleState::TIMED_OUT

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L319)

```cpp
TIMED_OUT
```

### API-9dd3cd496d63 · ndn_service_framework::ServiceUser::RequestLifecycleState::CANCELLED_OR_DROPPED

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L320)

```cpp
CANCELLED_OR_DROPPED
```

### API-2a4fc2caabe2 · ndn_service_framework::ServiceUser::RequestLifecycleStatus

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L323)

```cpp
struct RequestLifecycleStatus
```

### API-e40269bb787c · ndn_service_framework::ServiceUser::RequestLifecycleStatus::applicationTaskId

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L325)

```cpp
std::string applicationTaskId;
```

### API-e0687e3551c9 · ndn_service_framework::ServiceUser::RequestLifecycleStatus::requestId

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L326)

```cpp
ndn::Name requestId;
```

### API-13edaf60e497 · ndn_service_framework::ServiceUser::RequestLifecycleStatus::serviceName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L327)

```cpp
ndn::Name serviceName;
```

### API-d33c2da9d5f1 · ndn_service_framework::ServiceUser::RequestLifecycleStatus::state

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L328)

```cpp
RequestLifecycleState state = RequestLifecycleState::QUEUED_LOCAL;
```

### API-c0438878ba08 · ndn_service_framework::ServiceUser::RequestLifecycleStatus::selectedProviderName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L329)

```cpp
ndn::Name selectedProviderName;
```

### API-3877a3bace27 · ndn_service_framework::ServiceUser::RequestLifecycleStatus::enqueueTimestampUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L330)

```cpp
uint64_t enqueueTimestampUs = 0;
```

### API-0fdd56664a8c · ndn_service_framework::ServiceUser::RequestLifecycleStatus::admissionTimestampUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L331)

```cpp
uint64_t admissionTimestampUs = 0;
```

### API-116ac93f4e2c · ndn_service_framework::ServiceUser::RequestLifecycleStatus::publishTimestampUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L332)

```cpp
uint64_t publishTimestampUs = 0;
```

### API-3f1f24cbef27 · ndn_service_framework::ServiceUser::RequestLifecycleStatus::ackMatchedTimestampUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L333)

```cpp
uint64_t ackMatchedTimestampUs = 0;
```

### API-492aafc12d73 · ndn_service_framework::ServiceUser::RequestLifecycleStatus::providerSelectionTimestampUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L334)

```cpp
uint64_t providerSelectionTimestampUs = 0;
```

### API-f96e770b02cc · ndn_service_framework::ServiceUser::RequestLifecycleStatus::selectionPublishTimestampUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L335)

```cpp
uint64_t selectionPublishTimestampUs = 0;
```

### API-5db2ae0ef815 · ndn_service_framework::ServiceUser::RequestLifecycleStatus::responseObservedTimestampUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L336)

```cpp
uint64_t responseObservedTimestampUs = 0;
```

### API-0537e93601c6 · ndn_service_framework::ServiceUser::RequestLifecycleStatus::responseDecryptedTimestampUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L337)

```cpp
uint64_t responseDecryptedTimestampUs = 0;
```

### API-bb3aa7a06081 · ndn_service_framework::ServiceUser::RequestLifecycleStatus::callbackTimestampUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L338)

```cpp
uint64_t callbackTimestampUs = 0;
```

### API-0b05cf7fd0e3 · ndn_service_framework::ServiceUser::RequestLifecycleStatus::completionTimestampUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L339)

```cpp
uint64_t completionTimestampUs = 0;
```

### API-4e72ab6ac44e · ndn_service_framework::ServiceUser::RequestLifecycleStatus::timeoutTimestampUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L340)

```cpp
uint64_t timeoutTimestampUs = 0;
```

### API-2bb4a69f8688 · ndn_service_framework::ServiceUser::RequestLifecycleStatus::queuedDurationMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L341)

```cpp
double queuedDurationMs = 0.0;
```

### API-0b771716e5f0 · ndn_service_framework::ServiceUser::RequestLifecycleStatus::inflightDurationMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L342)

```cpp
double inflightDurationMs = 0.0;
```

### API-a453911f55cf · ndn_service_framework::ServiceUser::RequestLifecycleStatus::endToEndLatencyMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L343)

```cpp
double endToEndLatencyMs = 0.0;
```

### API-46d758106201 · ndn_service_framework::ServiceUser::RequestLifecycleStatus::delayedByAdmissionControl

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L344)

```cpp
bool delayedByAdmissionControl = false;
```

### API-4adbeb4f40fe · ndn_service_framework::ServiceUser::RequestLifecycleStatus::finalCleanupReason

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L345)

```cpp
std::string finalCleanupReason;
```

### API-0f5ffd22783b · ndn_service_framework::ServiceUser::RequestLifecycleCallback

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L348)

```cpp
using RequestLifecycleCallback =
                std::function<void(const RequestLifecycleStatus&)>;
```

### API-1a1d457f29af · ndn_service_framework::ServiceUser::AdmissionControlStatus

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L351)

```cpp
struct AdmissionControlStatus
```

### API-477aac580781 · ndn_service_framework::ServiceUser::AdmissionControlStatus::requestId

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L353)

```cpp
ndn::Name requestId;
```

### API-4dca4f174189 · ndn_service_framework::ServiceUser::AdmissionControlStatus::queueDepth

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L354)

```cpp
size_t queueDepth = 0;
```

### API-fe527eaecc2d · ndn_service_framework::ServiceUser::AdmissionControlStatus::softQueueLimit

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L355)

```cpp
size_t softQueueLimit = 0;
```

### API-d2b5b21f4aba · ndn_service_framework::ServiceUser::AdmissionControlStatus::hardQueueLimit

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L356)

```cpp
size_t hardQueueLimit = 0;
```

### API-bd414abf8208 · ndn_service_framework::ServiceUser::AdmissionControlStatus::remainingHardSlots

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L357)

```cpp
size_t remainingHardSlots = 0;
```

### API-ffb953715a8d · ndn_service_framework::ServiceUser::AdmissionControlStatus::reason

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L358)

```cpp
std::string reason;
```

### API-5fcd40abbe17 · ndn_service_framework::ServiceUser::ResponseRetryOptions

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L361)

```cpp
struct ResponseRetryOptions
```

### API-4b97a902bc42 · ndn_service_framework::ServiceUser::ResponseRetryOptions::enabled

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L363)

```cpp
bool enabled = false;
```

### API-21c7afe909f2 · ndn_service_framework::ServiceUser::ResponseRetryOptions::attemptTimeoutMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L364)

```cpp
int attemptTimeoutMs = 1000;
```

### API-f616e4bad265 · ndn_service_framework::ServiceUser::ResponseRetryOptions::maxAttempts

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L366)

```cpp
size_t maxAttempts = 4;
```

原始接口说明：

```text
/** Maximum selections, including the initial Provider. */
```

### API-e758ad02f94e · ndn_service_framework::ServiceUser::AdmissionControlWarningHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L369)

```cpp
using AdmissionControlWarningHandler =
                std::function<void(const AdmissionControlStatus&)>;
```

### API-0b08dfe4d213 · ndn_service_framework::ServiceUser::AdmissionControlRejectHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L372)

```cpp
using AdmissionControlRejectHandler =
                std::function<void(const AdmissionControlStatus&)>;
```

### API-fb06d97ce511 · ndn_service_framework::ServiceUser::RequestPublisher

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L375)

```cpp
using RequestPublisher =
                std::function<void(const ndn::Name& requestId,
                                   const ndn::Name& requestName,
                                   const std::vector<ndn::Name>& providers,
                                   const ndn::Name& serviceName,
                                   const ndn_service_framework::RequestMessage& requestMessage,
                                   size_t strategy)>;
```

### API-988f344f4e25 · ndn_service_framework::ServiceUser::LocalPublicationHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L385)

```cpp
using LocalPublicationHandler =
                std::function<void(const ndn::Name& messageName,
                                   const ndn::Buffer& wire)>;
```

原始接口说明：

```text
// Test-only publication boundary for LocalMockTag integration
// fixtures. Production instances publish through SVSPubSub.
```

### API-97e6f6fe625e · ndn_service_framework::ServiceUser::LocalMockTag

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L389)

```cpp
struct LocalMockTag
```

### API-a912e57055a3 · ndn_service_framework::ServiceUser::ServiceUser

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L393)

```cpp
ServiceUser(ndn::Face& face,ndn::Name group_prefix, ndn::security::Certificate identityCert,ndn::security::Certificate attrAuthorityCertificate,std::string trustSchemaPath);
```

### API-5ac63eb88500 · ndn_service_framework::ServiceUser::ServiceUser

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L394)

```cpp
ServiceUser(ndn::Face& face,
                        ndn::Name group_prefix,
                        ndn::security::Certificate encryptionCert,
                        ndn::security::Certificate signingCert,
                        ndn::security::Certificate attrAuthorityCertificate,
                        std::string trustSchemaPath);
```

### API-95ec09f8ec7e · ndn_service_framework::ServiceUser::ServiceUser

public / test-helper；[源码](../../ndn-service-framework/ServiceUser.hpp#L400)

```cpp
ServiceUser(LocalMockTag,
                        ndn::Face& face,
                        ndn::Name group_prefix,
                        ndn::security::Certificate identityCert,
                        ndn::security::Certificate attrAuthorityCertificate,
                        std::string trustSchemaPath);
```

### API-d6cddf557395 · ndn_service_framework::ServiceUser::ServiceUser

public / test-helper；[源码](../../ndn-service-framework/ServiceUser.hpp#L406)

```cpp
ServiceUser(LocalMockTag,
                        ndn::Face& face,
                        ndn::Name group_prefix,
                        ndn::security::Certificate encryptionCert,
                        ndn::security::Certificate signingCert,
                        ndn::security::Certificate attrAuthorityCertificate,
                        std::string trustSchemaPath);
```

### API-d22945d1e1a9 · ndn_service_framework::ServiceUser::~ServiceUser

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L414)

```cpp
virtual ~ServiceUser();
```

### API-e45c59f5f194 · ndn_service_framework::ServiceUser::init

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L415)

```cpp
void init();
```

### API-74ac68718c71 · ndn_service_framework::ServiceUser::attachLocalMockPubSubForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceUser.hpp#L424)

```cpp
void attachLocalMockPubSubForTest(
                std::shared_ptr<ndn::svs::SVSPubSub> pubSub);
```

原始接口说明：

```text
/**
             * Install an SVSPubSub instance on a LocalMock user.
             *
             * This test-only hook lets the in-process integration fixture run
             * the same SVS publication and subscription path as production
             * without performing the controller/NAC bootstrap.
             */
```

### API-98376dee365b · ndn_service_framework::ServiceUser::useSigningKeyChainForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceUser.hpp#L429)

```cpp
void useSigningKeyChainForTest(ndn::KeyChain& keyChain);
```

原始接口说明：

```text
/** Bind all LocalMock signing, including NAC-ABE wrapping, to the
             * fixture-owned KeyChain that contains the supplied certificate. */
```

### API-7dcfee7062e3 · ndn_service_framework::ServiceUser::isNacConsumerReadyForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceUser.hpp#L435)

```cpp
bool isNacConsumerReadyForTest();
```

原始接口说明：

```text
/** Return whether the active LocalMock NAC-ABE Consumer has
             * obtained its DKEY.  Integration bootstrap uses this as a hard
             * readiness condition instead of inferring readiness from public
             * parameter traffic alone. */
```

### API-4d712b34832a · ndn_service_framework::ServiceUser::cacheHybridReceiveKeyForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceUser.hpp#L438)

```cpp
void cacheHybridReceiveKeyForTest(const std::string& keyId,
                                              const std::string& epochId,
                                              const ndn::Buffer& key);
```

原始接口说明：

```text
/** Seed a receive key for a LocalMock ingress test. */
```

### API-8bd6ab6219ef · ndn_service_framework::ServiceUser::prepareHybridSendKeyForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceUser.hpp#L445)

```cpp
HybridMessageKey prepareHybridSendKeyForTest(
                const ndn::Name& serviceName,
                const std::string& messageType);
```

原始接口说明：

```text
/** Prepare a deterministic LocalMock outbound key and mark its
             * wrapped-key state as already bootstrapped.  This is test-only
             * plumbing; production authorization still uses NAC-ABE. */
```

### API-9e3f1a5874c2 · ndn_service_framework::ServiceUser::cacheDataForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceUser.hpp#L450)

```cpp
void cacheDataForTest(
                const ndn::Data& data,
                ndn::time::milliseconds freshness = ndn::DEFAULT_FRESHNESS_PERIOD);
```

原始接口说明：

```text
/** Cache a pre-built Data packet for LocalMock fetcher tests. */
```

### API-ab05f5dfc7f8 · ndn_service_framework::ServiceUser::cancelStreamRequestForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceUser.hpp#L456)

```cpp
void cancelStreamRequestForTest(const ndn::Name& requestId);
```

原始接口说明：

```text
/** Trigger the real streamed cancellation path from LocalMock
             * integration fixtures that start through deferred collaboration. */
```

### API-d456eb947180 · ndn_service_framework::ServiceUser::cancelStreamRequest

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L459)

```cpp
void cancelStreamRequest(const ndn::Name& requestId);
```

原始接口说明：

```text
/** Cancel callback delivery and terminal acceptance for one active
             * streamed request, including deferred collaboration requests. */
```

### API-9a7442abbc1e · ndn_service_framework::ServiceUser::getStreamMetricsForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceUser.hpp#L460)

```cpp
StreamedInvocationMetrics getStreamMetricsForTest(
                const ndn::Name& requestId) const;
```

### API-39cac6fc3339 · ndn_service_framework::ServiceUser::hasStreamStateForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceUser.hpp#L462)

```cpp
bool hasStreamStateForTest(const ndn::Name& requestId) const;
```

### API-9d346c360551 · ndn_service_framework::ServiceUser::streamCallbackQueueHighWaterMarkForTest

public / test-helper；[源码](../../ndn-service-framework/ServiceUser.hpp#L463)

```cpp
size_t streamCallbackQueueHighWaterMarkForTest(
                const ndn::Name& requestId) const;
```

### API-7dcf3a18fda6 · ndn_service_framework::ServiceUser::getName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L466)

```cpp
ndn::Name getName();
```

### API-cb4383650299 · ndn_service_framework::ServiceUser::openLiveStream

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L469)

```cpp
std::shared_ptr<LiveStreamConsumerHandle>
            openLiveStream(const LiveStreamDescriptor& descriptor,
                           LiveStreamOpenOptions options);
```

原始接口说明：

```text
/** Open a validated semantic-name live stream on this user's Face. */
```

### API-2619677ba51d · ndn_service_framework::ServiceUser::subscribeStream

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L474)

```cpp
std::shared_ptr<PredictiveStreamSubscriber>
            subscribeStream(const PredictiveStreamDescriptor& descriptor,
                            StreamSubscriptionOptions options);
```

原始接口说明：

```text
/** Open and start the predictive high-level stream subscription. */
```

### API-9040da67e128 · ndn_service_framework::ServiceUser::fetchPermissionsFromController

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L478)

```cpp
void fetchPermissionsFromController(const ndn::Name& controllerPrefix);
```

### API-f8c12279aea3 · ndn_service_framework::ServiceUser::applyPermissionResponse

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L479)

```cpp
void applyPermissionResponse(const PermissionResponse& response);
```

### API-7a605821e212 · ndn_service_framework::ServiceUser::installControllerStatus

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L482)

```cpp
bool installControllerStatus(const PolicyStatusData& status,
                                         bool controllerSignatureValid = true);
```

原始接口说明：

```text
/** Install a Controller-signed policy status after the enclosing
             * Data has passed the configured trust validator. */
```

### API-e6f6f184bd0e · ndn_service_framework::ServiceUser::persistAcceptedControllerStatus

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L486)

```cpp
void persistAcceptedControllerStatus(
                const ndn::Data& validatedData,
                const PolicyStatusData& status);
```

原始接口说明：

```text
/** Persist an accepted Controller status Data (opt-in durable
             * mode, FR-039); no-op when the store is disabled. */
```

### API-f098e7e18447 · ndn_service_framework::ServiceUser::restorePersistedRuntimeStatuses

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L494)

```cpp
void restorePersistedRuntimeStatuses();
```

原始接口说明：

```text
/** Restore persisted per-service statuses after startup (opt-in
             * durable mode, FR-039): unexpired, never-superseded records are
             * re-verified against the configured trust anchor and seeded as
             * authority; each seed schedules a bounded online confirmation
             * refresh whose Controller answer is authority. */
```

### API-438fea11dbfe · ndn_service_framework::ServiceUser::getControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L495)

```cpp
std::optional<ControllerVersion> getControllerVersion() const;
```

### API-dd4b28380e03 · ndn_service_framework::ServiceUser::getControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L496)

```cpp
std::optional<ControllerVersion> getControllerVersion(
                const ndn::Name& serviceName) const;
```

### API-88ae4c77c7c4 · ndn_service_framework::ServiceUser::getCurrentPolicyEpoch

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L498)

```cpp
size_t getCurrentPolicyEpoch() const;
```

### API-84e0c0ecbc30 · ndn_service_framework::ServiceUser::getCurrentPolicyEpoch

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L499)

```cpp
size_t getCurrentPolicyEpoch(const ndn::Name& serviceName) const;
```

### API-68a709378c04 · ndn_service_framework::ServiceUser::getAllowedServices

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L500)

```cpp
std::vector<std::tuple<std::string, std::string, size_t>>
            getAllowedServices() const;
```

### API-18aa6ea62344 · ndn_service_framework::ServiceUser::getNdnsdReceivedDetails

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L503)

```cpp
std::map<std::string, ndnsd::discovery::Details>
            getNdnsdReceivedDetails() const;
```

原始接口说明：

```text
/// Return received NDNSD service details keyed by provider identity.
```

### API-4d5b745be8b9 · ndn_service_framework::ServiceUser::handlePermissionResponseData

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L505)

```cpp
static bool handlePermissionResponseData(const ndn::Data& data,
                                                     const ndn::Name& identity,
                                                     ndn::KeyChain& keyChain,
                                                     ServiceAuthorizationTable& permissionTable);
```

### API-d6f00640ce6b · ndn_service_framework::ServiceUser::setRequestPublisher

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L509)

```cpp
void setRequestPublisher(RequestPublisher publisher);
```

### API-9fe44b5ec94b · ndn_service_framework::ServiceUser::setLocalPublicationHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L510)

```cpp
void setLocalPublicationHandler(LocalPublicationHandler handler);
```

### API-23fb7c9fabfe · ndn_service_framework::ServiceUser::makeGenericAdmissionLeaseSelectionPayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L511)

```cpp
static ndn::Buffer makeGenericAdmissionLeaseSelectionPayload(
                const std::string& leaseId,
                const ndn::Buffer& resourceBindingProof = ndn::Buffer());
```

### API-5886684a7094 · ndn_service_framework::ServiceUser::setSelectionAssignmentPayloadForRequest

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L514)

```cpp
bool setSelectionAssignmentPayloadForRequest(
                const ndn::Name& requestId,
                const ndn::Name& providerName,
                const ndn::Buffer& assignmentPayload);
```

### API-eb6ca0577d31 · ndn_service_framework::ServiceUser::setRequestLifecycleCallback

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L518)

```cpp
void setRequestLifecycleCallback(RequestLifecycleCallback callback);
```

### API-e6ce28e6083d · ndn_service_framework::ServiceUser::setAdmissionControlWarningHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L519)

```cpp
void setAdmissionControlWarningHandler(AdmissionControlWarningHandler handler);
```

### API-f4d7093795b8 · ndn_service_framework::ServiceUser::setAdmissionControlRejectHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L520)

```cpp
void setAdmissionControlRejectHandler(AdmissionControlRejectHandler handler);
```

### API-c27ab275c7c1 · ndn_service_framework::ServiceUser::getRequestStatus

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L521)

```cpp
std::optional<RequestLifecycleStatus>
            getRequestStatus(const ndn::Name& requestId) const;
```

### API-6ef9cdbd5325 · ndn_service_framework::ServiceUser::getActiveRequestStatuses

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L523)

```cpp
std::vector<RequestLifecycleStatus> getActiveRequestStatuses() const;
```

### API-9cfc4eb4c026 · ndn_service_framework::ServiceUser::requestLifecycleStateToString

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L524)

```cpp
static const char* requestLifecycleStateToString(RequestLifecycleState state);
```

### API-c17dcf371aa0 · ndn_service_framework::ServiceUser::getPendingCallCount

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L525)

```cpp
size_t getPendingCallCount() const;
```

### API-ca10a646c305 · ndn_service_framework::ServiceUser::setPendingCallTimeoutGrace

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L526)

```cpp
void setPendingCallTimeoutGrace(ndn::time::milliseconds grace);
```

### API-b003ce033edd · ndn_service_framework::ServiceUser::setResponseRetryOptions

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L527)

```cpp
void setResponseRetryOptions(ResponseRetryOptions options);
```

### API-9317432ffb9c · ndn_service_framework::ServiceUser::getResponseRetryOptions

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L528)

```cpp
ResponseRetryOptions getResponseRetryOptions() const;
```

### API-ce250534c2c1 · ndn_service_framework::ServiceUser::setPerformanceMode

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L529)

```cpp
void setPerformanceMode(bool enabled);
```

### API-e9a18fe4cf1c · ndn_service_framework::ServiceUser::setHandlerThreads

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L530)

```cpp
void setHandlerThreads(size_t n);
```

### API-df431f9288c1 · ndn_service_framework::ServiceUser::getHandlerThreads

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L531)

```cpp
size_t getHandlerThreads() const;
```

### API-747d66c00b29 · ndn_service_framework::ServiceUser::getHandlerQueueDepth

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L532)

```cpp
size_t getHandlerQueueDepth() const;
```

### API-a9caed5fe994 · ndn_service_framework::ServiceUser::setAckProcessingThreads

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L533)

```cpp
void setAckProcessingThreads(size_t n);
```

### API-599982292ec7 · ndn_service_framework::ServiceUser::getAckProcessingThreads

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L534)

```cpp
size_t getAckProcessingThreads() const;
```

### API-f4d875fb2a6c · ndn_service_framework::ServiceUser::getAckProcessingQueueDepth

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L535)

```cpp
size_t getAckProcessingQueueDepth() const;
```

### API-1820caa0ffcb · ndn_service_framework::ServiceUser::setUseTokens

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L536)

```cpp
void setUseTokens(bool enabled);
```

### API-41b5e88474ac · ndn_service_framework::ServiceUser::getUseTokens

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L537)

```cpp
bool getUseTokens() const;
```

### API-b2b04bc8048f · ndn_service_framework::ServiceUser::getHybridCryptoCounters

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L538)

```cpp
HybridCryptoCounters& getHybridCryptoCounters();
```

### API-8586016c0b28 · ndn_service_framework::ServiceUser::setTimelineTrace

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L539)

```cpp
void setTimelineTrace(bool enabled);
```

### API-c1db43fd7176 · ndn_service_framework::ServiceUser::RuntimeDiagnostics

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L540)

```cpp
struct RuntimeDiagnostics
```

### API-91f5c0107357 · ndn_service_framework::ServiceUser::RuntimeDiagnostics::callbackSkippedNoPending

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L542)

```cpp
uint64_t callbackSkippedNoPending = 0;
```

### API-a2b2d0b6e23d · ndn_service_framework::ServiceUser::RuntimeDiagnostics::callbackSkippedTimeout

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L543)

```cpp
uint64_t callbackSkippedTimeout = 0;
```

### API-4be0e3cb7205 · ndn_service_framework::ServiceUser::RuntimeDiagnostics::responseAfterPendingTimeout

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L544)

```cpp
uint64_t responseAfterPendingTimeout = 0;
```

### API-d644550f65d3 · ndn_service_framework::ServiceUser::RuntimeDiagnostics::ackLatenciesMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L545)

```cpp
std::vector<double> ackLatenciesMs;
```

### API-b383d3039d73 · ndn_service_framework::ServiceUser::consumeRuntimeDiagnostics

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L547)

```cpp
RuntimeDiagnostics consumeRuntimeDiagnostics();
```

### API-10f832c2cdbc · ndn_service_framework::ServiceUser::waitForVerifiedCollaborationData

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L553)

```cpp
std::vector<VerifiedCollaborationData>
            waitForVerifiedCollaborationData(const RequestId& requestId,
                                             const KeyScope& keyScope,
                                             const ndn::Name& topicPrefix,
                                             size_t minCount,
                                             int timeoutMs,
                                             bool consume = true);
```

原始接口说明：

```text
/** Wait off the Face/io_context thread for validated collaboration
             * records from the selected Providers. Records are retained until
             * explicitly consumed so a terminal Response cannot race receipt
             * delivery. */
```

### API-69fb5b1187df · ndn_service_framework::ServiceUser::clearVerifiedCollaborationData

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L566)

```cpp
void clearVerifiedCollaborationData(const RequestId& requestId,
                                                const KeyScope& keyScope);
```

原始接口说明：

```text
/** Release verified collaboration records and the request-scope
             * key after a multi-step conversation transaction has finished.
             * Waiting with consume=true removes matching records but keeps the
             * key alive so a later authenticated control/ack can still use
             * the same request scope. */
```

### API-56610990f594 · ndn_service_framework::ServiceUser::AdaptiveAdmissionOptions

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L569)

```cpp
struct AdaptiveAdmissionOptions
```

### API-80ab4c65f157 · ndn_service_framework::ServiceUser::AdaptiveAdmissionOptions::enabled

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L571)

```cpp
bool enabled = true;
```

### API-24dd43daa795 · ndn_service_framework::ServiceUser::AdaptiveAdmissionOptions::minWindow

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L572)

```cpp
size_t minWindow = 1;
```

### API-ae8080eefdfd · ndn_service_framework::ServiceUser::AdaptiveAdmissionOptions::maxWindow

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L573)

```cpp
size_t maxWindow = 512;
```

### API-26f1c58b2e56 · ndn_service_framework::ServiceUser::AdaptiveAdmissionOptions::initialWindow

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L574)

```cpp
size_t initialWindow = 16;
```

### API-a655948e3efd · ndn_service_framework::ServiceUser::AdaptiveAdmissionOptions::hardInflightLimit

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L575)

```cpp
size_t hardInflightLimit = 512;
```

### API-d4385a891d8f · ndn_service_framework::ServiceUser::AdaptiveAdmissionOptions::aiStep

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L576)

```cpp
size_t aiStep = 4;
```

### API-eb1367292022 · ndn_service_framework::ServiceUser::AdaptiveAdmissionOptions::mdFactor

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L577)

```cpp
double mdFactor = 0.85;
```

### API-6901bfb60243 · ndn_service_framework::ServiceUser::AdaptiveAdmissionOptions::severeMdFactor

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L578)

```cpp
double severeMdFactor = 0.5;
```

### API-20d3697181cd · ndn_service_framework::ServiceUser::AdaptiveAdmissionOptions::controlIntervalMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L579)

```cpp
int controlIntervalMs = 500;
```

### API-c14d6c769037 · ndn_service_framework::ServiceUser::AdaptiveAdmissionOptions::targetLatencyMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L580)

```cpp
int targetLatencyMs = 350;
```

### API-03b24a2904f2 · ndn_service_framework::ServiceUser::AdaptiveAdmissionOptions::hardTargetLatencyMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L581)

```cpp
int hardTargetLatencyMs = 500;
```

### API-2cf3722c8edd · ndn_service_framework::ServiceUser::AdaptiveAdmissionOptions::softQueueLimit

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L582)

```cpp
size_t softQueueLimit = 0;
```

### API-72cdb60cdeeb · ndn_service_framework::ServiceUser::AdaptiveAdmissionOptions::hardQueueLimit

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L583)

```cpp
size_t hardQueueLimit = 0;
```

### API-9d133a530fb3 · ndn_service_framework::ServiceUser::AdaptiveAdmissionOptions::rateRecommendationEnabled

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L584)

```cpp
bool rateRecommendationEnabled = true;
```

### API-5845243a9003 · ndn_service_framework::ServiceUser::AdaptiveAdmissionOptions::initialRecommendedRateRps

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L585)

```cpp
double initialRecommendedRateRps = 0.0;
```

### API-dd7870cce4b0 · ndn_service_framework::ServiceUser::AdaptiveAdmissionOptions::minRecommendedRateRps

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L586)

```cpp
double minRecommendedRateRps = 1.0;
```

### API-a2e04e93ceaf · ndn_service_framework::ServiceUser::AdaptiveAdmissionOptions::maxRecommendedRateRps

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L587)

```cpp
double maxRecommendedRateRps = 0.0;
```

### API-8275a2df13a4 · ndn_service_framework::ServiceUser::setAdaptiveAdmissionControl

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L589)

```cpp
void setAdaptiveAdmissionControl(const AdaptiveAdmissionOptions& options);
```

### API-e3ac568794f9 · ndn_service_framework::ServiceUser::getAdaptiveAdmissionOptions

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L590)

```cpp
AdaptiveAdmissionOptions getAdaptiveAdmissionOptions() const;
```

### API-12365af618cf · ndn_service_framework::ServiceUser::getAdaptiveAdmissionWindow

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L591)

```cpp
size_t getAdaptiveAdmissionWindow() const;
```

### API-28aeef09291c · ndn_service_framework::ServiceUser::getAdaptiveAdmissionInflight

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L592)

```cpp
size_t getAdaptiveAdmissionInflight() const;
```

### API-d6445c6139e0 · ndn_service_framework::ServiceUser::getAdaptiveAdmissionQueueDepth

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L593)

```cpp
size_t getAdaptiveAdmissionQueueDepth() const;
```

### API-cf43f1578fdc · ndn_service_framework::ServiceUser::getAdaptiveAdmissionRecommendedRateRps

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L594)

```cpp
double getAdaptiveAdmissionRecommendedRateRps() const;
```

### API-876759dcfcab · ndn_service_framework::ServiceUser::recordAdaptiveAdmissionBackpressure

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L595)

```cpp
void recordAdaptiveAdmissionBackpressure();
```

### API-5c50b53030e1 · ndn_service_framework::ServiceUser::makeAckSelectionHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L597)

```cpp
static AckCandidatesHandler makeAckSelectionHandler(
                AckSelectionStrategy strategy);
```

### API-f14aabbb8d51 · ndn_service_framework::ServiceUser::selectFirstRespondingAck

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L600)

```cpp
static std::vector<ndn_service_framework::AckSelectionCandidate>
            selectFirstRespondingAck(
                const std::vector<ndn_service_framework::AckSelectionCandidate>& candidates);
```

### API-0c93cf2d8d48 · ndn_service_framework::ServiceUser::selectRandomAck

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L604)

```cpp
static std::vector<ndn_service_framework::AckSelectionCandidate>
            selectRandomAck(
                const std::vector<ndn_service_framework::AckSelectionCandidate>& candidates);
```

### API-bf0c8e10a164 · ndn_service_framework::ServiceUser::selectAllResponderAcks

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L608)

```cpp
static std::vector<ndn_service_framework::AckSelectionCandidate>
            selectAllResponderAcks(
                const std::vector<ndn_service_framework::AckSelectionCandidate>& candidates);
```

### API-15c424171b44 · ndn_service_framework::ServiceUser::PublishRequestV2

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L612)

```cpp
void PublishRequestV2(const std::vector<ndn::Name>& serviceProviderNames,
                                  const ndn::Name& serviceName,
                                  const ndn::Name& requestId,
                                  const ndn::Buffer& payload,
                                  const size_t& strategy=ndn_service_framework::tlv::FirstResponding);
```

### API-c4424e99b06e · ndn_service_framework::ServiceUser::prepareServiceRequest

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L618)

```cpp
PreparedServiceRequest prepareServiceRequest(const std::string& serviceName);
```

### API-0ce2c8a00b75 · ndn_service_framework::ServiceUser::publishEncryptedLargeData

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L620)

```cpp
LargeDataPublishResult publishEncryptedLargeData(
                const PreparedServiceRequest& ctx,
                const std::vector<uint8_t>& plaintext,
                const std::string& objectLabel = "",
                ndn::time::milliseconds freshness = ndn::DEFAULT_FRESHNESS_PERIOD);
```

### API-9e90db18a209 · ndn_service_framework::ServiceUser::SignedAppDataHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L626)

```cpp
using SignedAppDataHandler = std::function<void(const ndn::Data&)>;
```

### API-e21a3ff89e6b · ndn_service_framework::ServiceUser::SignedAppDataFailureHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L627)

```cpp
using SignedAppDataFailureHandler =
                std::function<void(const ndn::Name&, const std::string&)>;
```

### API-160499aab055 · ndn_service_framework::ServiceUser::publishSignedAppData

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L637)

```cpp
ndn::Name publishSignedAppData(
                const ndn::Name& dataName,
                const ndn::Buffer& payload,
                ndn::time::milliseconds freshness = ndn::DEFAULT_FRESHNESS_PERIOD);
```

原始接口说明：

```text
/** Publish exact-name APP data signed by this ServiceUser identity.
             *
             * This is a small transport primitive for versioned APP records.
             * It does not define a new NDNSF message or invocation mode. The
             * name must remain below /<identity>/NDNSF/DI so another
             * application cannot use this user as an arbitrary Data signer.
             */
```

### API-9abc485ebd0b · ndn_service_framework::ServiceUser::publishCollaborationData

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L648)

```cpp
bool publishCollaborationData(
                const ndn::Name& targetProvider,
                const ndn::Name& requestId,
                const KeyScope& keyScope,
                const ndn::Name& topic,
                const ndn::Buffer& payload);
```

原始接口说明：

```text
/** Publish one requester-authenticated collaboration control record.
             * The payload is encrypted with the request scope key and the
             * outer SVS Data is signed by this User identity. It is intended
             * for request-scoped Provider commit/rollback controls, not for
             * application data or a new invocation mode.
             */
```

### API-ef2281d2fe2d · ndn_service_framework::ServiceUser::fetchSignedAppData

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L660)

```cpp
void fetchSignedAppData(
                const ndn::Name& dataName,
                const ndn::Name& expectedSigner,
                int timeoutMs,
                SignedAppDataHandler onData,
                SignedAppDataFailureHandler onFailure);
```

原始接口说明：

```text
/** Fetch and validate one exact-name APP record.
             *
             * Validation uses the configured trust schema and additionally
             * requires the Data KeyLocator to belong to expectedSigner.
             */
```

### API-d7a9e5056883 · ndn_service_framework::ServiceUser::makeRequestWithLargeDataOptimization

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L667)

```cpp
LargeDataReferenceRequestResult makeRequestWithLargeDataOptimization(
                const PreparedServiceRequest& ctx,
                const std::vector<uint8_t>& payload,
                const std::string& objectLabel = "",
                const std::string& objectType = "",
                size_t thresholdBytes = 1024,
                ndn::time::milliseconds freshness = ndn::DEFAULT_FRESHNESS_PERIOD);
```

### API-c677e3df31ec · ndn_service_framework::ServiceUser::RequestService

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L675)

```cpp
ndn::Name RequestService(const PreparedServiceRequest& ctx,
                                 ndn_service_framework::RequestMessage requestMessage,
                                 int timeoutMs,
                                 TimeoutHandler onTimeout,
                                 ResponseHandler onResponseHandler,
                                 size_t strategy = ndn_service_framework::tlv::FirstResponding);
```

### API-3d96c89ed2a8 · ndn_service_framework::ServiceUser::RequestService

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L682)

```cpp
ndn::Name RequestService(const PreparedServiceRequest& ctx,
                                 const std::vector<ndn::Name>& providers,
                                 ndn_service_framework::RequestMessage requestMessage,
                                 int timeoutMs,
                                 TimeoutHandler onTimeout,
                                 ResponseHandler onResponseHandler,
                                 size_t strategy = ndn_service_framework::tlv::FirstResponding);
```

### API-a123bee560b9 · ndn_service_framework::ServiceUser::RequestService

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L690)

```cpp
ndn::Name RequestService(const std::vector<ndn::Name>& providers,
                                 const ndn::Name& serviceName,
                                 ndn_service_framework::RequestMessage requestMessage,
                                 int timeoutMs,
                                 TimeoutHandler onTimeout,
                                 ResponseHandler onResponseHandler,
                                 size_t strategy = ndn_service_framework::tlv::FirstResponding);
```

### API-96309fee7a50 · ndn_service_framework::ServiceUser::RequestServiceTracked

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L698)

```cpp
ndn::Name RequestServiceTracked(
                                 const std::vector<ndn::Name>& providers,
                                 const ndn::Name& serviceName,
                                 ndn_service_framework::RequestMessage requestMessage,
                                 int timeoutMs,
                                 SelectionStatusTimeoutHandler onTimeout,
                                 ResponseHandler onResponseHandler,
                                 size_t strategy = ndn_service_framework::tlv::FirstResponding,
                                 SelectionStatusOptions statusOptions = SelectionStatusOptions());
```

### API-0904c56617b8 · ndn_service_framework::ServiceUser::QuerySelectionStatus

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L708)

```cpp
void QuerySelectionStatus(const ndn::Name& providerName,
                                      const ndn::Name& serviceName,
                                      const std::string& selectionDigest,
                                      SelectionStatusHandler onStatus,
                                      TimeoutHandler onTimeout,
                                      int timeoutMs = 500);
```

### API-5913a3259254 · ndn_service_framework::ServiceUser::GetCollaborationStatusSnapshot

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L715)

```cpp
std::vector<SelectionExecutionStatus>
            GetCollaborationStatusSnapshot(const ndn::Name& requestId) const;
```

### API-1009e77682c2 · ndn_service_framework::ServiceUser::RequestServiceTargeted

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L718)

```cpp
ndn::Name RequestServiceTargeted(const ndn::Name& provider,
                                 const ndn::Name& serviceName,
                                 ndn_service_framework::RequestMessage requestMessage,
                                 int timeoutMs,
                                 TimeoutHandler onTimeout,
                                 ResponseHandler onResponseHandler);
```

### API-09fc656cb649 · ndn_service_framework::ServiceUser::RequestService

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L725)

```cpp
ndn::Name RequestService(const ndn::Name& serviceName,
                                 ndn_service_framework::RequestMessage requestMessage,
                                 int timeoutMs,
                                 TimeoutHandler onTimeout,
                                 ResponseHandler onResponseHandler,
                                 size_t strategy = ndn_service_framework::tlv::FirstResponding);
```

### API-842e1076ab9c · ndn_service_framework::ServiceUser::RequestService

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L732)

```cpp
ndn::Name RequestService(const ndn::Name& serviceName,
                                 ndn_service_framework::RequestMessage requestMessage,
                                 int ackTimeoutMs,
                                 AcksHandler onAcksHandler,
                                 int timeoutMs,
                                 TimeoutHandler onTimeout,
                                 ResponseHandler onResponseHandler);
```

### API-19440859f4fe · ndn_service_framework::ServiceUser::RequestService

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L740)

```cpp
ndn::Name RequestService(const ndn::Name& serviceName,
                                 ndn_service_framework::RequestMessage requestMessage,
                                 int ackTimeoutMs,
                                 AckCandidatesHandler onAcksHandler,
                                 int timeoutMs,
                                 TimeoutHandler onTimeout,
                                 ResponseHandler onResponseHandler);
```

### API-99f816ae87bb · ndn_service_framework::ServiceUser::RequestService

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L748)

```cpp
ndn::Name RequestService(const std::vector<ndn::Name>& providers,
                                 const ndn::Name& serviceName,
                                 ndn_service_framework::RequestMessage requestMessage,
                                 int ackTimeoutMs,
                                 AckCandidatesHandler onAcksHandler,
                                 int timeoutMs,
                                 TimeoutHandler onTimeout,
                                 ResponseHandler onResponseHandler,
                                 size_t requestStrategy = ndn_service_framework::tlv::FirstResponding,
                                 const RequestId& requestId = RequestId());
```

### API-0187d1aee6c3 · ndn_service_framework::ServiceUser::RequestService

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L759)

```cpp
ndn::Name RequestService(const std::vector<ndn::Name>& providers,
                                 const ndn::Name& serviceName,
                                 ndn_service_framework::RequestMessage requestMessage,
                                 int ackTimeoutMs,
                                 AckSelectionStrategy selectionStrategy,
                                 int timeoutMs,
                                 TimeoutHandler onTimeout,
                                 ResponseHandler onResponseHandler,
                                 const RequestId& requestId = RequestId());
```

### API-224b39843407 · ndn_service_framework::ServiceUser::RequestService

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L769)

```cpp
ndn::Name RequestService(const ServiceName& service,
                                     const RequestPayload& request,
                                     int ackCollectionTimeMs,
                                     std::shared_ptr<const AckSelectionPolicy> selectionPolicy,
                                     int timeoutMs,
                                     ResponseHandler onResponse,
                                     TimeoutHandler onTimeout,
                                     const RequestId& requestId = RequestId());
```

### API-9e9c476a768e · ndn_service_framework::ServiceUser::RequestServiceStreaming

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L779)

```cpp
template<typename RequestT, typename EventT, typename ResponseT>
std::shared_ptr<StreamedInvocationHandle<EventT, ResponseT>>
            RequestServiceStreaming(
                const ndn::Name& serviceName,
                const RequestT& request,
                StreamedInvocationOptions options,
                std::function<void(const EventT&)> onEvent,
                std::function<void(const ResponseT&)> onComplete,
                std::function<void(const StreamedInvocationError&)> onError,
                size_t strategy = ndn_service_framework::tlv::FirstResponding)
```

### API-d37543fe7d2f · ndn_service_framework::ServiceUser::RequestServiceStreaming

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L822)

```cpp
template<typename RequestT, typename EventT, typename ResponseT>
std::shared_ptr<StreamedInvocationHandle<EventT, ResponseT>>
            RequestServiceStreaming(
                const ndn::Name& provider,
                const ndn::Name& serviceName,
                const RequestT& request,
                StreamedInvocationOptions options,
                std::function<void(const EventT&)> onEvent,
                std::function<void(const ResponseT&)> onComplete,
                std::function<void(const StreamedInvocationError&)> onError)
```

### API-a7927024cd2d · ndn_service_framework::ServiceUser::RequestCollaboration

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L851)

```cpp
ndn::Name RequestCollaboration(const ServiceName& service,
                                           const RequestPayload& initialRequest,
                                           CollaborationPlan plan,
                                           ResponseHandler onFinalResponse,
                                           TimeoutHandler onTimeout,
                                           const RequestId& requestId = RequestId());
```

### API-c9045b5b03e6 · ndn_service_framework::ServiceUser::BeginCollaboration

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L858)

```cpp
ndn::Name BeginCollaboration(const ServiceName& service,
                                         const RequestPayload& initialRequest,
                                         int ackCollectionTimeMs,
                                         int timeoutMs,
                                         CollaborationAckClosedHandler onAckClosed,
                                         ResponseHandler onFinalResponse,
                                         TimeoutHandler onTimeout,
                                         const RequestId& requestId = RequestId());
```

### API-a36a8977cca7 · ndn_service_framework::ServiceUser::BeginCollaboration

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L867)

```cpp
ndn::Name BeginCollaboration(const ServiceName& service,
                                         const RequestPayload& initialRequest,
                                         int ackCollectionTimeMs,
                                         int timeoutMs,
                                         CollaborationAckClosedHandler onAckClosed,
                                         ResponseHandler onFinalResponse,
                                         TimeoutHandler onTimeout,
                                         const RequestId& requestId,
                                         CollaborationAckCoverageHandler onAckCoverage,
                                         const RequestCapabilities& requestCapabilities =
                                             RequestCapabilities(),
                                         const std::optional<StreamRequestOptions>&
                                             streamOptions = std::nullopt,
                                         std::function<void(const ndn::Buffer&)>
                                             onStreamEvent = {},
                                         std::function<void(const ndn::Buffer&)>
                                             onStreamComplete = {},
                                         std::function<void(const StreamedInvocationError&)>
                                             onStreamError = {});
```

### API-3ab0d3d87607 · ndn_service_framework::ServiceUser::CommitCollaborationPlan

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L887)

```cpp
bool CommitCollaborationPlan(const RequestId& requestId,
                                         const std::string& ackClosedDigest,
                                         CollaborationPlan plan);
```

### API-633ec82f4942 · ndn_service_framework::ServiceUser::RequestService

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L892)

```cpp
template<typename RequestT, typename ResponseT>
ndn::Name RequestService(const ServiceName& service,
                                     const RequestT& request,
                                     int ackCollectionTimeMs,
                                     std::shared_ptr<const AckSelectionPolicy> selectionPolicy,
                                     int timeoutMs,
                                     std::function<void(const ResponseT&)> onResponse,
                                     std::function<void(const RequestId&)> onTimeout)
```

### API-0e69e1dfb16b · ndn_service_framework::ServiceUser::RequestService

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L931)

```cpp
template<typename RequestT, typename ResponseT>
ndn::Name RequestService(const std::vector<ndn::Name>& providers,
                                     const ndn::Name& serviceName,
                                     const RequestT& request,
                                     std::function<void(const ResponseT&)> onResponse,
                                     std::function<void()> onTimeout,
                                     int timeoutMs,
                                     size_t strategy = ndn_service_framework::tlv::FirstResponding)
```

### API-efa9e29aae17 · ndn_service_framework::ServiceUser::RequestServiceTargeted

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L977)

```cpp
template<typename RequestT, typename ResponseT>
ndn::Name RequestServiceTargeted(const ndn::Name& provider,
                                           const ndn::Name& serviceName,
                                           const RequestT& request,
                                           std::function<void(const ResponseT&)> onResponse,
                                           std::function<void()> onTimeout,
                                           int timeoutMs)
```

### API-c9ae0f32ad81 · ndn_service_framework::ServiceUser::handleResponse

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1020)

```cpp
void handleResponse(const ndn::Name& requestId,
                                const ndn::Name& providerName,
                                const ndn_service_framework::ResponseMessage& responseMessage);
```

### API-828aa81a1971 · ndn_service_framework::ServiceUser::handleDecryptedResponse

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1024)

```cpp
bool handleDecryptedResponse(const ndn::Name& requestId,
                                         const ndn::Name& providerName,
                                         const ndn_service_framework::ResponseMessage& responseMessage);
```

### API-4a1ba83da401 · ndn_service_framework::ServiceUser::handleDecryptedResponse

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1033)

```cpp
bool handleDecryptedResponse(const ndn::Name& requestId,
                                         const ndn::Name& providerName,
                                         const ndn_service_framework::ResponseMessage& responseMessage,
                                         bool resolvedLargeResponse);
```

原始接口说明：

```text
// resolvedLargeResponse=true marks a Response whose payload was
// reconstructed by resolveLargeResponseReferencePayload; that
// path already authenticated every segment against the
// invocation binding, nonce registry, and reference digest, so
// the envelope decrypt step is skipped here.
```

### API-9ea6e276850d · ndn_service_framework::ServiceUser::handleDecryptedResponse

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1038)

```cpp
bool handleDecryptedResponse(const ndn::Name& requestId,
                                         const ndn_service_framework::ResponseMessage& responseMessage);
```

### API-9ea8d03ae1a4 · ndn_service_framework::ServiceUser::handleDecryptedResponse

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1041)

```cpp
bool handleDecryptedResponse(const ndn::Name& requestId,
                                         const ndn::Block& responseBlock);
```

### API-89ed88bc8601 · ndn_service_framework::ServiceUser::handleDecryptedResponseByName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1044)

```cpp
bool handleDecryptedResponseByName(const ndn::Name& responseName,
                                               const ndn_service_framework::ResponseMessage& responseMessage);
```

### API-c095b78ebaff · ndn_service_framework::ServiceUser::handleDecryptedResponseByName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1047)

```cpp
bool handleDecryptedResponseByName(const ndn::Name& responseName,
                                               const ndn::Block& responseBlock);
```

### API-ca47e4123ba0 · ndn_service_framework::ServiceUser::resolveLargeResponseReferencePayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1050)

```cpp
std::optional<ResponseMessage>
            resolveLargeResponseReferencePayload(const ResponseMessage& responseMessage,
                                                 const ndn::Name& responseName,
                                                 const ndn::Name& serviceName,
                                                 std::string& errorMessage);
```

### API-ffcdf14a6fa0 · ndn_service_framework::ServiceUser::handleRequestAckByName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1056)

```cpp
bool handleRequestAckByName(const ndn::Name& ackName,
                                        const ndn_service_framework::RequestAckMessage& ackMessage,
                                        AckAuthenticationEvidence authenticationEvidence = {});
```

### API-c9b8ee5b662c · ndn_service_framework::ServiceUser::handleRequestAckByName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1060)

```cpp
bool handleRequestAckByName(const ndn::Name& ackName,
                                        const ndn::Block& ackBlock);
```

### API-334f9010e1f6 · ndn_service_framework::ServiceUser::dispatchDecryptedResponseByName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1062)

```cpp
void dispatchDecryptedResponseByName(const ndn::Name& responseName,
                                                 const ndn::Name& requestId,
                                                 const ndn::Buffer& buffer,
                                                 const std::string& dataName = {},
                                                 const std::string& signerCertificate = {},
                                                 const std::string& wireDigest = {});
```

### API-3988b41ce599 · ndn_service_framework::ServiceUser::finishDecryptedResponseByName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1068)

```cpp
void finishDecryptedResponseByName(const ndn::Name& responseName,
                                               const ndn::Name& requestId,
                                               ndn_service_framework::ResponseMessage responseMessage);
```

### API-a4c9df664571 · ndn_service_framework::ServiceUser::finishRequestAckOnEventLoop

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1071)

```cpp
void finishRequestAckOnEventLoop(const ndn::Name& providerName,
                                             const ndn::Name& ServiceName,
                                             const ndn::Name& requestID,
                                             ndn_service_framework::RequestAckMessage AckMessage,
                                             AckAuthenticationEvidence authenticationEvidence = {});
```

### API-ae527a32a956 · ndn_service_framework::ServiceUser::OnResponse

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1077)

```cpp
virtual void OnResponse(const ndn::svs::SVSPubSub::SubscriptionData &subscription);
```

### API-c9635ab486b2 · ndn_service_framework::ServiceUser::OnCollaborationData

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1078)

```cpp
void OnCollaborationData(
                const ndn::svs::SVSPubSub::SubscriptionData& subscription);
```

### API-211636f4e6df · ndn_service_framework::ServiceUser::processNDNSDServiceInfoCallback

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1082)

```cpp
void processNDNSDServiceInfoCallback(const ndnsd::discovery::Details& callback);
```

原始接口说明：

```text
// ndnsd serviceinfo discovery callback
```

### API-0c13b18c8c90 · ndn_service_framework::ServiceUser::onPermissionResponseData

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1084)

```cpp
void onPermissionResponseData(const ndn::Interest& interest,
                                           const ndn::Data& data);
```

### API-daedb9326ec9 · ndn_service_framework::ServiceUser::onPermissionResponseTimeout

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1086)

```cpp
void onPermissionResponseTimeout(const ndn::Interest& interest,
                                             int attempt = 1);
```

### API-fb334897a679 · ndn_service_framework::ServiceUser::fetchPolicyManifestFromController

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1088)

```cpp
void fetchPolicyManifestFromController(const ndn::Name& controllerPrefix,
                                                   int attempt = 1);
```

### API-5c25b8bd2177 · ndn_service_framework::ServiceUser::onPolicyManifestData

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1090)

```cpp
void onPolicyManifestData(const ndn::Interest& interest,
                                      const ndn::Data& data);
```

### API-327147448d17 · ndn_service_framework::ServiceUser::onPolicyManifestTimeout

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1092)

```cpp
void onPolicyManifestTimeout(const ndn::Interest& interest,
                                         int attempt = 1);
```

### API-3e92132744eb · ndn_service_framework::ServiceUser::fetchPolicyStatusFromController

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1094)

```cpp
void fetchPolicyStatusFromController(const ndn::Name& controllerPrefix,
                                                 const ndn::Name& serviceName,
                                                 int attempt = 1,
                                                 std::optional<ControllerVersion> expectedVersion = std::nullopt);
```

### API-b5997741cf50 · ndn_service_framework::ServiceUser::onPolicyStatusData

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1098)

```cpp
void onPolicyStatusData(const ndn::Interest& interest,
                                    const ndn::Data& data,
                                    int attempt = 1);
```

### API-b43eca6f5846 · ndn_service_framework::ServiceUser::onPolicyStatusTimeout

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1101)

```cpp
void onPolicyStatusTimeout(const ndn::Interest& interest,
                                       int attempt = 1);
```

### API-63dc80f1db1a · ndn_service_framework::ServiceUser::scheduleControllerStatusRefresh

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1103)

```cpp
void scheduleControllerStatusRefresh(
                const ndn::Name& serviceName,
                const PolicyStatusData& status);
```

### API-2e0717822d44 · ndn_service_framework::ServiceUser::scheduleDeferredDkeyRefreshRetry

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1110)

```cpp
void scheduleDeferredDkeyRefreshRetry(const ndn::Name& serviceName);
```

原始接口说明：

```text
// Bounded DKEY re-arm retries for a LocalMock whose immediate
// refresh was deferred (no fixture-owned Consumer).  The retry
// fires only while the Face is pumped and stops as soon as the
// Consumer is ready or the attempt bound is exhausted.
```

### API-06ec85e8e6d8 · ndn_service_framework::ServiceUser::isAcceptablePolicyEpoch

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1111)

```cpp
bool isAcceptablePolicyEpoch(size_t messageEpoch) const;
```

### API-44d767ca2443 · ndn_service_framework::ServiceUser::isAcceptablePolicyEpoch

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1112)

```cpp
bool isAcceptablePolicyEpoch(const ndn::Name& serviceName,
                                         size_t messageEpoch) const;
```

### API-ed23d83484c5 · ndn_service_framework::ServiceUser::isAcceptableControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1114)

```cpp
bool isAcceptableControllerVersion(
                const std::optional<ControllerVersion>& messageVersion) const;
```

### API-db76576d031f · ndn_service_framework::ServiceUser::isAcceptableControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1116)

```cpp
bool isAcceptableControllerVersion(
                const ndn::Name& serviceName,
                const std::optional<ControllerVersion>& messageVersion) const;
```

### API-d7a720ade7db · ndn_service_framework::ServiceUser::maybeRefreshControllerVersionHint

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1119)

```cpp
void maybeRefreshControllerVersionHint(
                const ndn::Name& serviceName,
                const std::optional<ControllerVersion>& messageVersion);
```

### API-e7f43da03b88 · ndn_service_framework::ServiceUser::authorizeControllerTransition

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1127)

```cpp
__attribute__((noinline))
            #endif
            bool authorizeControllerTransition(
                const ndn::Name& serviceName,
                ProtectedTransition transition) const;
```

### API-5f0665ec0e4f · ndn_service_framework::ServiceUser::adoptControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1132)

```cpp
void adoptControllerVersion(const ControllerVersion& version);
```

### API-27c05278318d · ndn_service_framework::ServiceUser::invalidateControllerScopedCaches

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1141)

```cpp
void invalidateControllerScopedCaches(
                const ndn::Name& serviceName,
                const ControllerVersion& version,
                bool abeGenerationChanged = true,
                const PolicyStatusData* status = nullptr,
                bool grantOnlyDkeyRefresh = false);
```

原始接口说明：

```text
/**
             * Evict per-service authorization material after an authenticated
             * ControllerVersion change.  This is deliberately separate from
             * RevocationState's evidence set: the latter records which cache
             * families were affected, while this hook removes live User-side
             * token, binding, nonce, stream, collaboration, and incomplete
             * request state.
             */
```

### API-740f7165dd02 · ndn_service_framework::ServiceUser::refreshNacDkeyForControllerStatus

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1159)

```cpp
void refreshNacDkeyForControllerStatus(
                const ndn::Name& serviceName,
                const ControllerVersion& version,
                bool abeGenerationChanged,
                bool grantOnlyDkeyRefresh);
```

原始接口说明：

```text
/**
             * Run the DKEY-only refresh leg of a Controller-status install
             * (grant-only target-policy replacement, FR-017/SC-022).  Called
             * from invalidateControllerScopedCaches on a version-advance
             * install and directly from installControllerStatus when an
             * equal-version install consumes a pending grant-only refresh
             * that arrived after the version-advance install already ran
             * (reverse order: the status channel installed the version before
             * the PermissionResponse recorded the grant).  A LocalMock
             * without its fixture-owned Consumer defers to the explicit
             * bootstrap path via scheduleDeferredDkeyRefreshRetry.
             */
```

### API-189ab2271fd4 · ndn_service_framework::ServiceUser::OnRequestAck

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1165)

```cpp
void OnRequestAck(const ndn::svs::SVSPubSub::SubscriptionData &subscription);
```

### API-58cb7ad27e00 · ndn_service_framework::ServiceUser::OnRequestAckDecryptionSuccessCallback

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1167)

```cpp
void OnRequestAckDecryptionSuccessCallback(const ndn::Name& providerName,
                                                       const ndn::Name& serviceName,
                                                       const ndn::Name& requestID,
                                                       const ndn::Buffer& buffer,
                                                       AckAuthenticationEvidence authenticationEvidence = {});
```

### API-bbb79a8c26d4 · ndn_service_framework::ServiceUser::OnRequestAckDecryptionErrorCallback

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1173)

```cpp
void OnRequestAckDecryptionErrorCallback(const ndn::Name& providerName,
                                                     const ndn::Name& serviceName,
                                                     const ndn::Name& requestID,
                                                     const std::string& error);
```

### API-b5e9a2edcda8 · ndn_service_framework::ServiceUser::PublishServiceSelectionMessageV2

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1178)

```cpp
void PublishServiceSelectionMessageV2(const ndn::Name& providerName,
                                                     const ndn::Name& serviceName,
                                                     const ndn::Name& requestId);
```

### API-5de9e066fe28 · ndn_service_framework::ServiceUser::OnResponseDecryptionErrorCallback

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1182)

```cpp
void OnResponseDecryptionErrorCallback(const ndn::Name& providerName,
                                                   const ndn::Name& serviceName,
                                                   const ndn::Name& requestID,
                                                   const std::string& error);
```

### API-8506002ed424 · ndn_service_framework::ServiceUser::replyFromIMS

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1187)

```cpp
bool replyFromIMS(const ndn::Interest &interest);
```

### API-bf325a4ba4f1 · ndn_service_framework::ServiceUser::onPrefixRegisterFailure

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1189)

```cpp
void onPrefixRegisterFailure(const ndn::Name& prefix, const std::string& reason);
```

### API-a696bb966cab · ndn_service_framework::ServiceUser::onInterest

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1191)

```cpp
void onInterest(const ndn::InterestFilter &, const ndn::Interest &interest);
```

### API-79262cd74613 · ndn_service_framework::ServiceUser::serveDataWithIMS

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1193)

```cpp
void serveDataWithIMS(ndn::nacabe::SPtrVector<ndn::Data>& contentData, ndn::nacabe::SPtrVector<ndn::Data>& ckData);
```

### API-056d03a201c5 · ndn_service_framework::ServiceUser::PublishMessage

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1195)

```cpp
void PublishMessage(const ndn::Name& messageName, const ndn::Name &messageNameWithoutPrefix, AbstractMessage& message);
```

### API-358c2d8e17d6 · ndn_service_framework::ServiceUser::publishHybridMessage

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1196)

```cpp
void publishHybridMessage(const ndn::Name& messageName,
                                      const ndn::Name& messageNameWithoutPrefix,
                                      AbstractMessage& message);
```

### API-826015885a98 · ndn_service_framework::ServiceUser::publishHybridEncodedMessage

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1199)

```cpp
void publishHybridEncodedMessage(const ndn::Name& messageName,
                                             ndn::Buffer plaintext);
```

### API-30a5417563c7 · ndn_service_framework::ServiceUser::decryptHybridMessage

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1201)

```cpp
bool decryptHybridMessage(const ndn::Name& messageName,
                                      const ndn::Block& envelopeBlock,
                                      std::function<void(const ndn::Buffer&)> onSuccess,
                                      std::function<void(const std::string&)> onError);
```

### API-f94792b18a33 · ndn_service_framework::ServiceUser::registerNDNSFMessages

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1207)

```cpp
void registerNDNSFMessages();
```

原始接口说明：

```text
// Register NDNSF Messages in the ndn-svs
```

### API-1503be689f5e · ndn_service_framework::ServiceUser::requestForServiceInfo

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1210)

```cpp
void requestForServiceInfo();
```

原始接口说明：

```text
// search for service info using ndnsd();
```

### API-54985b7e62bd · ndn_service_framework::ServiceUser::isFresh

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1212)

```cpp
bool isFresh(const ndn::svs::SVSPubSub::SubscriptionData &subscription);
```

### API-01c300d565e0 · ndn_service_framework::ServiceUser::registerIdentityPrefixWithRetry

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1217)

```cpp
void registerIdentityPrefixWithRetry(size_t attempts = 0);
```

### API-e7a8200cd255 · ndn_service_framework::ServiceUser::onMissingData

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1219)

```cpp
void
            onMissingData(const std::vector<ndn::svs::MissingDataInfo> &);
```

### API-001a7f560c96 · ndn_service_framework::ServiceUser::StoredAck

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1223)

```cpp
struct StoredAck
```

### API-cd86d1d15995 · ndn_service_framework::ServiceUser::StoredAck::providerName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1225)

```cpp
ndn::Name providerName;
```

### API-eecad3b0d527 · ndn_service_framework::ServiceUser::StoredAck::serviceName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1226)

```cpp
ndn::Name serviceName;
```

### API-4899b0e61388 · ndn_service_framework::ServiceUser::StoredAck::requestId

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1227)

```cpp
ndn::Name requestId;
```

### API-a1c567a2abbe · ndn_service_framework::ServiceUser::StoredAck::message

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1228)

```cpp
ndn_service_framework::RequestAckMessage message;
```

### API-ba0bd49d868a · ndn_service_framework::ServiceUser::StoredAck::authenticationEvidence

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1229)

```cpp
AckAuthenticationEvidence authenticationEvidence{};
```

### API-046da228b4fd · ndn_service_framework::ServiceUser::PendingCall

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1232)

```cpp
struct PendingCall
```

### API-6387bbddee0d · ndn_service_framework::ServiceUser::PublishCompactServiceSelectionMessageV2

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1234)

```cpp
void PublishCompactServiceSelectionMessageV2(const std::vector<StoredAck>& selectedAcks);
```

### API-035c1ee9eea3 · ndn_service_framework::ServiceUser::usesR1ReservationSelection

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1235)

```cpp
bool usesR1ReservationSelection(const PendingCall& pendingCall) const;
```

### API-ef0735061691 · ndn_service_framework::ServiceUser::PublishR1SelectionDecision

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1236)

```cpp
void PublishR1SelectionDecision(const StoredAck& ack, bool selected);
```

### API-884193dc17c8 · ndn_service_framework::ServiceUser::closeR1ReservationDecisions

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1237)

```cpp
void closeR1ReservationDecisions(PendingCall& pendingCall);
```

### API-e87c4d4d3399 · ndn_service_framework::ServiceUser::pollR1DecisionReceipt

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1238)

```cpp
void pollR1DecisionReceipt(const ndn::Name& requestId,
                                       const std::string& reservationId);
```

### API-780b7da689c5 · ndn_service_framework::ServiceUser::retryR1Decision

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1240)

```cpp
void retryR1Decision(const ndn::Name& requestId,
                                 const std::string& reservationId);
```

### API-f942d136aedb · ndn_service_framework::ServiceUser::makeAckSelectionCandidate

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1242)

```cpp
ndn_service_framework::AckSelectionCandidate
            makeAckSelectionCandidate(const StoredAck& storedAck) const;
```

### API-046da228b4fd · ndn_service_framework::ServiceUser::PendingCall

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1245)

```cpp
struct PendingCall
```

### API-2312564f53a2 · ndn_service_framework::ServiceUser::PendingCall::R1DecisionDelivery

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1247)

```cpp
struct R1DecisionDelivery
```

### API-7bf3a2d59c6b · ndn_service_framework::ServiceUser::PendingCall::R1DecisionDelivery::providerName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1249)

```cpp
ndn::Name providerName;
```

### API-49a116267aea · ndn_service_framework::ServiceUser::PendingCall::R1DecisionDelivery::serviceName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1250)

```cpp
ndn::Name serviceName;
```

### API-4ce35d6b77e3 · ndn_service_framework::ServiceUser::PendingCall::R1DecisionDelivery::messageName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1251)

```cpp
ndn::Name messageName;
```

### API-3adbcdf6e177 · ndn_service_framework::ServiceUser::PendingCall::R1DecisionDelivery::messageSuffix

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1252)

```cpp
ndn::Name messageSuffix;
```

### API-a6638f21bf56 · ndn_service_framework::ServiceUser::PendingCall::R1DecisionDelivery::message

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1253)

```cpp
ServiceSelectionMessage message;
```

### API-119fc6dda1b8 · ndn_service_framework::ServiceUser::PendingCall::R1DecisionDelivery::selectionDigest

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1254)

```cpp
std::string selectionDigest;
```

### API-48f3c0ea7988 · ndn_service_framework::ServiceUser::PendingCall::R1DecisionDelivery::decisionDigest

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1255)

```cpp
std::string decisionDigest;
```

### API-c8cd1cb898a4 · ndn_service_framework::ServiceUser::PendingCall::R1DecisionDelivery::expiresAtMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1256)

```cpp
uint64_t expiresAtMs = 0;
```

### API-84c7e6d2d8f0 · ndn_service_framework::ServiceUser::PendingCall::R1DecisionDelivery::transmissions

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1257)

```cpp
size_t transmissions = 0;
```

### API-dc0745b1ce00 · ndn_service_framework::ServiceUser::PendingCall::R1DecisionDelivery::receiptAccepted

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1258)

```cpp
bool receiptAccepted = false;
```

### API-0056498194b1 · ndn_service_framework::ServiceUser::PendingCall::providers

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1260)

```cpp
std::vector<ndn::Name> providers;
```

### API-95e1905d578a · ndn_service_framework::ServiceUser::PendingCall::serviceName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1261)

```cpp
ndn::Name serviceName;
```

### API-533da8a3c0c3 · ndn_service_framework::ServiceUser::PendingCall::requestName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1262)

```cpp
ndn::Name requestName;
```

### API-ec165135578e · ndn_service_framework::ServiceUser::PendingCall::requestNameWithoutPrefix

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1263)

```cpp
ndn::Name requestNameWithoutPrefix;
```

### API-c967cef4dd9d · ndn_service_framework::ServiceUser::PendingCall::requestMessage

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1264)

```cpp
ndn_service_framework::RequestMessage requestMessage;
```

### API-358d35a79449 · ndn_service_framework::ServiceUser::PendingCall::strategy

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1265)

```cpp
size_t strategy = ndn_service_framework::tlv::FirstResponding;
```

### API-2317e1b19049 · ndn_service_framework::ServiceUser::PendingCall::timeoutMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1266)

```cpp
int timeoutMs = 0;
```

### API-8526b8ed89b2 · ndn_service_framework::ServiceUser::PendingCall::ackTimeoutMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1267)

```cpp
int ackTimeoutMs = 0;
```

### API-c2881f1b09b3 · ndn_service_framework::ServiceUser::PendingCall::createdAtUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1268)

```cpp
uint64_t createdAtUs = 0;
```

### API-fd262e1aa405 · ndn_service_framework::ServiceUser::PendingCall::publishedAtUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1269)

```cpp
uint64_t publishedAtUs = 0;
```

### API-95acfe9e4759 · ndn_service_framework::ServiceUser::PendingCall::firstAckAtUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1270)

```cpp
uint64_t firstAckAtUs = 0;
```

### API-387d54843fdf · ndn_service_framework::ServiceUser::PendingCall::ackWindowDeadlineUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1271)

```cpp
uint64_t ackWindowDeadlineUs = 0;
```

### API-bf3e33d7dd68 · ndn_service_framework::ServiceUser::PendingCall::ackSelectionAtUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1272)

```cpp
uint64_t ackSelectionAtUs = 0;
```

### API-9f7714860df4 · ndn_service_framework::ServiceUser::PendingCall::ackSelectionCompletedAtUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1273)

```cpp
uint64_t ackSelectionCompletedAtUs = 0;
```

### API-f3398b90f11f · ndn_service_framework::ServiceUser::PendingCall::selectionScheduledAtUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1274)

```cpp
uint64_t selectionScheduledAtUs = 0;
```

### API-9cd8a4e5b89c · ndn_service_framework::ServiceUser::PendingCall::selectionPublishedAtUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1275)

```cpp
uint64_t selectionPublishedAtUs = 0;
```

### API-f4ca85044a90 · ndn_service_framework::ServiceUser::PendingCall::responseObservedAtUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1276)

```cpp
uint64_t responseObservedAtUs = 0;
```

### API-d613dc0ae3cf · ndn_service_framework::ServiceUser::PendingCall::responseDecryptedAtUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1277)

```cpp
uint64_t responseDecryptedAtUs = 0;
```

### API-ab6047d0c88e · ndn_service_framework::ServiceUser::PendingCall::responseValidatedAtUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1278)

```cpp
uint64_t responseValidatedAtUs = 0;
```

### API-01c240f41f69 · ndn_service_framework::ServiceUser::PendingCall::requestDeadlineUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1279)

```cpp
uint64_t requestDeadlineUs = 0;
```

### API-8c4456844fdd · ndn_service_framework::ServiceUser::PendingCall::acksHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1280)

```cpp
AcksHandler acksHandler;
```

### API-0bf1c28fb48d · ndn_service_framework::ServiceUser::PendingCall::ackCandidatesHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1281)

```cpp
AckCandidatesHandler ackCandidatesHandler;
```

### API-c8313aeef265 · ndn_service_framework::ServiceUser::PendingCall::timeoutHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1282)

```cpp
TimeoutHandler timeoutHandler;
```

### API-e72b314f13e5 · ndn_service_framework::ServiceUser::PendingCall::responseHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1283)

```cpp
ResponseHandler responseHandler;
```

### API-1ecc1ff2feeb · ndn_service_framework::ServiceUser::PendingCall::hasResponse

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1284)

```cpp
bool hasResponse = false;
```

### API-c78c593c86cc · ndn_service_framework::ServiceUser::PendingCall::admissionPublished

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1285)

```cpp
bool admissionPublished = false;
```

### API-76a65b6ad6ed · ndn_service_framework::ServiceUser::PendingCall::admissionReleased

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1286)

```cpp
bool admissionReleased = false;
```

### API-ae25a7d8bf0f · ndn_service_framework::ServiceUser::PendingCall::ackTimeoutScheduled

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1287)

```cpp
bool ackTimeoutScheduled = false;
```

### API-17ebc21e4170 · ndn_service_framework::ServiceUser::PendingCall::requestTimeoutScheduled

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1288)

```cpp
bool requestTimeoutScheduled = false;
```

### API-779757975605 · ndn_service_framework::ServiceUser::PendingCall::scheduleAckTimeoutAfterPublish

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1289)

```cpp
bool scheduleAckTimeoutAfterPublish = false;
```

### API-0b3f9938d1b5 · ndn_service_framework::ServiceUser::PendingCall::scheduleImmediateAckTimeoutAfterPublish

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1290)

```cpp
bool scheduleImmediateAckTimeoutAfterPublish = false;
```

### API-af6e68c713da · ndn_service_framework::ServiceUser::PendingCall::ackWindowExpired

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1291)

```cpp
bool ackWindowExpired = false;
```

### API-7cef8041306c · ndn_service_framework::ServiceUser::PendingCall::providerSelected

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1292)

```cpp
bool providerSelected = false;
```

### API-1f7b8476ab4e · ndn_service_framework::ServiceUser::PendingCall::targetedMode

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1293)

```cpp
bool targetedMode = false;
```

### API-3ea6aec4e8ae · ndn_service_framework::ServiceUser::PendingCall::timedOut

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1294)

```cpp
bool timedOut = false;
```

### API-f786d9e1d1da · ndn_service_framework::ServiceUser::PendingCall::timeoutGraceActive

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1295)

```cpp
bool timeoutGraceActive = false;
```

### API-bd7e64e04031 · ndn_service_framework::ServiceUser::PendingCall::requestTimeoutEvent

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1296)

```cpp
ndn::scheduler::EventId requestTimeoutEvent;
```

### API-3234a69fdd2f · ndn_service_framework::ServiceUser::PendingCall::responseAttemptTimeoutEvent

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1297)

```cpp
ndn::scheduler::EventId responseAttemptTimeoutEvent;
```

### API-f352a30a56e4 · ndn_service_framework::ServiceUser::PendingCall::responseRetryEnabled

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1298)

```cpp
bool responseRetryEnabled = false;
```

### API-1798d0bf9a59 · ndn_service_framework::ServiceUser::PendingCall::responseRetryTimerArmed

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1299)

```cpp
bool responseRetryTimerArmed = false;
```

### API-14c6400c84dc · ndn_service_framework::ServiceUser::PendingCall::responseAttemptTimeoutMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1300)

```cpp
int responseAttemptTimeoutMs = 0;
```

### API-fd24e51e5d99 · ndn_service_framework::ServiceUser::PendingCall::responseMaxAttempts

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1301)

```cpp
size_t responseMaxAttempts = 1;
```

### API-89347a46bc0e · ndn_service_framework::ServiceUser::PendingCall::responseAttemptStartedAtUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1302)

```cpp
uint64_t responseAttemptStartedAtUs = 0;
```

### API-47b001c47d46 · ndn_service_framework::ServiceUser::PendingCall::responseAttemptProviders

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1303)

```cpp
std::vector<ndn::Name> responseAttemptProviders;
```

### API-ced835a9f46d · ndn_service_framework::ServiceUser::PendingCall::ackDecryptsInFlight

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1304)

```cpp
size_t ackDecryptsInFlight = 0;
```

### API-c795b9420d9b · ndn_service_framework::ServiceUser::PendingCall::ackSelectionDeferrals

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1305)

```cpp
size_t ackSelectionDeferrals = 0;
```

### API-f375c02b1614 · ndn_service_framework::ServiceUser::PendingCall::learnedAckProviderCountAtPublish

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1306)

```cpp
size_t learnedAckProviderCountAtPublish = 0;
```

### API-7ea434d63615 · ndn_service_framework::ServiceUser::PendingCall::requestAcks

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1307)

```cpp
std::vector<StoredAck> requestAcks;
```

### API-835b9e3fbd0e · ndn_service_framework::ServiceUser::PendingCall::customSelectedAcks

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1308)

```cpp
std::vector<StoredAck> customSelectedAcks;
```

### API-dd01d96387d8 · ndn_service_framework::ServiceUser::PendingCall::successfulAckProviders

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1309)

```cpp
std::vector<ndn::Name> successfulAckProviders;
```

### API-d56b28183df4 · ndn_service_framework::ServiceUser::PendingCall::negativeAckProviders

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1310)

```cpp
std::vector<ndn::Name> negativeAckProviders;
```

### API-1e310a75774d · ndn_service_framework::ServiceUser::PendingCall::selectionPublishedProviders

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1311)

```cpp
std::vector<ndn::Name> selectionPublishedProviders;
```

### API-e5939e149789 · ndn_service_framework::ServiceUser::PendingCall::expectedResponseProviders

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1312)

```cpp
std::vector<ndn::Name> expectedResponseProviders;
```

### API-6e3bff28885e · ndn_service_framework::ServiceUser::PendingCall::responseProviders

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1313)

```cpp
std::vector<ndn::Name> responseProviders;
```

### API-ac469cdd5c19 · ndn_service_framework::ServiceUser::PendingCall::responseDecryptProvidersInFlight

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1314)

```cpp
std::vector<ndn::Name> responseDecryptProvidersInFlight;
```

### API-427c7815b6d3 · ndn_service_framework::ServiceUser::PendingCall::largeResponseReferenceProvidersInFlight

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1315)

```cpp
std::vector<ndn::Name> largeResponseReferenceProvidersInFlight;
```

### API-6de5cfecce14 · ndn_service_framework::ServiceUser::PendingCall::selectedProvider

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1316)

```cpp
ndn::Name selectedProvider;
```

### API-026ad9bef8cc · ndn_service_framework::ServiceUser::PendingCall::providerTokens

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1317)

```cpp
std::map<std::string, std::string> providerTokens;
```

### API-4eb9f7dd148a · ndn_service_framework::ServiceUser::PendingCall::selectionGatedInputKey

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1318)

```cpp
ndn::Buffer selectionGatedInputKey;
```

### API-e16c64ecdfc6 · ndn_service_framework::ServiceUser::PendingCall::requestScopedPlaintext

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1324)

```cpp
ndn::Buffer requestScopedPlaintext;
```

原始接口说明：

```text
// Request-scoped confidentiality keeps the application input
// out of the discovery Request.  The plaintext is retained
// only until the selected Provider's exact-name Data packet
// has been published, then the key bundle is retained only
// for response decryption.
```

### API-51cab1946277 · ndn_service_framework::ServiceUser::PendingCall::requestScopedKeys

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1325)

```cpp
std::optional<RequestKeyBundle> requestScopedKeys;
```

### API-c5c1cf6e70c1 · ndn_service_framework::ServiceUser::PendingCall::requestScopedBinding

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1326)

```cpp
std::optional<RequestSecurityBinding> requestScopedBinding;
```

### API-410648907dee · ndn_service_framework::ServiceUser::PendingCall::requestScopedInputDataName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1327)

```cpp
ndn::Name requestScopedInputDataName;
```

### API-7f407112726c · ndn_service_framework::ServiceUser::PendingCall::requestScopedConfidentiality

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1328)

```cpp
bool requestScopedConfidentiality = false;
```

### API-09d11f58b249 · ndn_service_framework::ServiceUser::PendingCall::negativeAckReasons

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1329)

```cpp
std::map<std::string, std::string> negativeAckReasons;
```

### API-a4f2192e08ad · ndn_service_framework::ServiceUser::PendingCall::isCollaboration

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1330)

```cpp
bool isCollaboration = false;
```

### API-705d4154a9c5 · ndn_service_framework::ServiceUser::PendingCall::collaborationDeferred

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1331)

```cpp
bool collaborationDeferred = false;
```

### API-00f7f226eca2 · ndn_service_framework::ServiceUser::PendingCall::collaborationAcksClosed

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1332)

```cpp
bool collaborationAcksClosed = false;
```

### API-eb70bcfbd149 · ndn_service_framework::ServiceUser::PendingCall::collaborationPlanCommitted

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1333)

```cpp
bool collaborationPlanCommitted = false;
```

### API-a4d893b7bd69 · ndn_service_framework::ServiceUser::PendingCall::collaborationPlan

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1334)

```cpp
CollaborationPlan collaborationPlan;
```

### API-b607cb83e1ac · ndn_service_framework::ServiceUser::PendingCall::collaborationAckClosedHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1335)

```cpp
CollaborationAckClosedHandler collaborationAckClosedHandler;
```

### API-02d3df9cc637 · ndn_service_framework::ServiceUser::PendingCall::collaborationAckCoverageHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1336)

```cpp
CollaborationAckCoverageHandler collaborationAckCoverageHandler;
```

### API-be96f0329361 · ndn_service_framework::ServiceUser::PendingCall::collaborationClosedAcks

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1337)

```cpp
std::vector<StoredAck> collaborationClosedAcks;
```

### API-1baaa89e042b · ndn_service_framework::ServiceUser::PendingCall::collaborationCommittedParticipants

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1338)

```cpp
std::vector<SelectedParticipant> collaborationCommittedParticipants;
```

### API-e149ce0fb45b · ndn_service_framework::ServiceUser::PendingCall::collaborationAckClosedDigest

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1339)

```cpp
std::string collaborationAckClosedDigest;
```

### API-43076c47022e · ndn_service_framework::ServiceUser::PendingCall::collaborationCommittedPlanDigest

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1340)

```cpp
std::string collaborationCommittedPlanDigest;
```

### API-a00d9dcbf314 · ndn_service_framework::ServiceUser::PendingCall::collaborationAcksClosedAtUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1341)

```cpp
uint64_t collaborationAcksClosedAtUs = 0;
```

### API-171514b7a752 · ndn_service_framework::ServiceUser::PendingCall::collaborationAssignments

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1342)

```cpp
std::map<std::string, ndn::Buffer> collaborationAssignments;
```

### API-60e4498ddf11 · ndn_service_framework::ServiceUser::PendingCall::collaborationScopeKeys

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1347)

```cpp
std::map<std::string, ndn::Buffer> collaborationScopeKeys;
```

原始接口说明：

```text
// One generated key per committed collaboration dependency
// scope.  The keys are carried only in the framework-owned
// assignment envelope and are reused for an idempotent
// Selection retransmission.
```

### API-28d73eaff71c · ndn_service_framework::ServiceUser::PendingCall::selectionAssignmentPayloads

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1348)

```cpp
std::map<std::string, ndn::Buffer> selectionAssignmentPayloads;
```

### API-950deded55f9 · ndn_service_framework::ServiceUser::PendingCall::trackSelectionStatus

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1349)

```cpp
bool trackSelectionStatus = false;
```

### API-b46be9db36b8 · ndn_service_framework::ServiceUser::PendingCall::selectionStatusOptions

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1350)

```cpp
SelectionStatusOptions selectionStatusOptions;
```

### API-042869ba8769 · ndn_service_framework::ServiceUser::PendingCall::statusTimeoutHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1351)

```cpp
SelectionStatusTimeoutHandler statusTimeoutHandler;
```

### API-559502f2dd29 · ndn_service_framework::ServiceUser::PendingCall::selectionDigestsByProvider

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1352)

```cpp
std::map<std::string, std::string> selectionDigestsByProvider;
```

### API-65a47c8cf46b · ndn_service_framework::ServiceUser::PendingCall::selectionStatusesByProvider

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1353)

```cpp
std::map<std::string, SelectionExecutionStatus> selectionStatusesByProvider;
```

### API-b5a409892a64 · ndn_service_framework::ServiceUser::PendingCall::r1DecisionDeliveries

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1354)

```cpp
std::map<std::string, R1DecisionDelivery> r1DecisionDeliveries;
```

### API-a7328cd52175 · ndn_service_framework::ServiceUser::PendingCall::deploymentPlan

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1355)

```cpp
std::optional<DeploymentPlan> deploymentPlan;
```

### API-efd2af9c9138 · ndn_service_framework::ServiceUser::PendingCall::deploymentReadyByMember

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1356)

```cpp
std::map<std::string, ProviderReadyMessage> deploymentReadyByMember;
```

### API-84f8689f1958 · ndn_service_framework::ServiceUser::PendingCall::deploymentActivationSent

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1357)

```cpp
bool deploymentActivationSent = false;
```

### API-8548b5d7e3c1 · ndn_service_framework::ServiceUser::PendingCall::streamLifecycle

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1360)

```cpp
std::shared_ptr<StreamInvocationLifecycle> streamLifecycle;
```

原始接口说明：

```text
// Populated only by the streamed API. Unary and legacy
// Targeted calls intentionally allocate no stream owner.
```

### API-af99eca92c8a · ndn_service_framework::ServiceUser::PendingCall::streamOptions

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1361)

```cpp
std::optional<StreamRequestOptions> streamOptions;
```

### API-2cea8667d6d2 · ndn_service_framework::ServiceUser::PendingCall::streamEventKey

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1362)

```cpp
ndn::Buffer streamEventKey;
```

### API-7d79063d0991 · ndn_service_framework::ServiceUser::TargetedTokenPair

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1365)

```cpp
struct TargetedTokenPair
```

### API-f32c8772b14d · ndn_service_framework::ServiceUser::TargetedTokenPair::providerToken

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1367)

```cpp
std::string providerToken;
```

### API-ba4bb272daa2 · ndn_service_framework::ServiceUser::TargetedTokenPair::userToken

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1368)

```cpp
std::string userToken;
```

### API-8cd4f4e179f5 · ndn_service_framework::ServiceUser::TargetedTokenPoolControl

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1371)

```cpp
struct TargetedTokenPoolControl
```

### API-ffd0a5b23226 · ndn_service_framework::ServiceUser::TargetedTokenPoolControl::nextBatch

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1373)

```cpp
size_t nextBatch = 0;
```

### API-459670c861fc · ndn_service_framework::ServiceUser::TargetedTokenPoolControl::capacity

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1374)

```cpp
size_t capacity = 0;
```

### API-c28ed5bb3144 · ndn_service_framework::ServiceUser::TargetedTokenPoolControl::consumedSinceStore

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1375)

```cpp
size_t consumedSinceStore = 0;
```

### API-48d57ed0c662 · ndn_service_framework::ServiceUser::TargetedTokenPoolControl::lastStoredAtUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1376)

```cpp
uint64_t lastStoredAtUs = 0;
```

### API-614cd6ef8046 · ndn_service_framework::ServiceUser::TargetedTokenPoolControl::refillStartedAtUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1377)

```cpp
uint64_t refillStartedAtUs = 0;
```

### API-bffe7a508221 · ndn_service_framework::ServiceUser::TargetedTokenPoolControl::observed

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1378)

```cpp
bool observed = false;
```

### API-e6c25a22a863 · ndn_service_framework::ServiceUser::TargetedTokenPoolControl::refillInFlight

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1379)

```cpp
bool refillInFlight = false;
```

### API-dc2726915a44 · ndn_service_framework::ServiceUser::TargetedStreamOffer

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1382)

```cpp
struct TargetedStreamOffer
```

### API-8578bfe2f8df · ndn_service_framework::ServiceUser::TargetedStreamOffer::providerBootEpoch

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1384)

```cpp
std::string providerBootEpoch;
```

### API-d96f1fbf6282 · ndn_service_framework::ServiceUser::TargetedStreamOffer::recipientCertName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1385)

```cpp
std::string recipientCertName;
```

### API-74816ab13152 · ndn_service_framework::ServiceUser::TargetedStreamOffer::recipientCertDigest

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1386)

```cpp
std::string recipientCertDigest;
```

### API-e51d98535d6c · ndn_service_framework::ServiceUser::TargetedStreamOffer::recipientPublicKey

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1387)

```cpp
std::string recipientPublicKey;
```

### API-d6bee9857798 · ndn_service_framework::ServiceUser::attachStreamLifecycle

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1395)

```cpp
std::shared_ptr<StreamInvocationLifecycle>
            attachStreamLifecycle(const ndn::Name& requestId,
                                  bool deferredCollaboration);
```

原始接口说明：

```text
/**
             * Attach streamed delivery to an already-created request. The
             * public API and local fixtures use this same ownership rule;
             * attachment cannot manufacture a second request ID or Request.
             */
```

### API-7fbaf2f986a5 · ndn_service_framework::ServiceUser::getStreamLifecycle

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1399)

```cpp
std::shared_ptr<StreamInvocationLifecycle>
            getStreamLifecycle(const ndn::Name& requestId) const;
```

### API-ac71ff49a8d1 · ndn_service_framework::ServiceUser::StreamBytesCallback

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1401)

```cpp
using StreamBytesCallback = std::function<void(const ndn::Buffer&)>;
```

### API-55b3502c0edb · ndn_service_framework::ServiceUser::requestServiceStreamingBytes

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1402)

```cpp
std::shared_ptr<StreamedInvocationSharedState>
            requestServiceStreamingBytes(
                const ndn::Name& serviceName,
                const ndn::Name& targetProvider,
                ndn::Buffer requestPayload,
                StreamedInvocationOptions options,
                StreamBytesCallback onEvent,
                StreamBytesCallback onComplete,
                std::function<void(const StreamedInvocationError&)> onError,
                size_t strategy);
```

### API-b5593af2d60c · ndn_service_framework::ServiceUser::onStreamEvent

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1412)

```cpp
void onStreamEvent(const ndn::svs::SVSPubSub::SubscriptionData& subscription);
```

### API-f54ed6c2f88d · ndn_service_framework::ServiceUser::initializeStreamConsumer

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1413)

```cpp
bool initializeStreamConsumer(const ndn::Name& providerName,
                                          const ndn::Name& serviceName,
                                          const ndn::Name& requestId,
                                          const std::string& selectionDigest);
```

### API-c7bcf58fe30b · ndn_service_framework::ServiceUser::armStreamInactivityTimer

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1417)

```cpp
void armStreamInactivityTimer(const ndn::Name& requestId,
                                          uint64_t interestLifetimeMs);
```

### API-28085e2da3e1 · ndn_service_framework::ServiceUser::disarmStreamInactivityTimer

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1419)

```cpp
void disarmStreamInactivityTimer(const ndn::Name& requestId);
```

### API-53a20d2f43e7 · ndn_service_framework::ServiceUser::PendingCallTraceRecord

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1420)

```cpp
struct PendingCallTraceRecord
```

### API-6402e82431a4 · ndn_service_framework::ServiceUser::PendingCallTraceRecord::createdAtUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1422)

```cpp
uint64_t createdAtUs = 0;
```

### API-f6c83afe9d91 · ndn_service_framework::ServiceUser::PendingCallTraceRecord::erasedAtUs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1423)

```cpp
uint64_t erasedAtUs = 0;
```

### API-cca07feed715 · ndn_service_framework::ServiceUser::PendingCallTraceRecord::timedOut

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1424)

```cpp
bool timedOut = false;
```

### API-08237ec65268 · ndn_service_framework::ServiceUser::PendingCallTraceRecord::completed

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1425)

```cpp
bool completed = false;
```

### API-3bb8c5e4cd8a · ndn_service_framework::ServiceUser::PendingCallTraceRecord::matchedAck

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1426)

```cpp
bool matchedAck = false;
```

### API-332650d8b9bc · ndn_service_framework::ServiceUser::PendingCallTraceRecord::requestName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1427)

```cpp
ndn::Name requestName;
```

### API-c8a71d52e933 · ndn_service_framework::ServiceUser::makeRequestId

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1430)

```cpp
static ndn::Name makeRequestId();
```

### API-f94e3fd013cd · ndn_service_framework::ServiceUser::sanitizeLargeDataObjectId

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1432)

```cpp
static std::string sanitizeLargeDataObjectId(const std::string& objectLabel);
```

### API-971ee7d67ffe · ndn_service_framework::ServiceUser::shouldTrackAckDecrypt

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1434)

```cpp
static bool shouldTrackAckDecrypt(const PendingCall& pendingCall);
```

### API-fec5a2c78a5c · ndn_service_framework::ServiceUser::requiresRequestScopedConfidentiality

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1436)

```cpp
static bool requiresRequestScopedConfidentiality(
                const ndn_service_framework::RequestMessage& requestMessage);
```

### API-2a0be76137f6 · ndn_service_framework::ServiceUser::prepareRequestScopedRequest

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1446)

```cpp
bool prepareRequestScopedRequest(
                ndn_service_framework::RequestMessage& requestMessage,
                const ndn::Name& serviceName,
                const ndn::Name& requestId,
                ndn::Buffer& plaintext) const;
```

原始接口说明：

```text
/**
             * Apply the request-scoped confidentiality default for configured
             * Controller runtimes and move the application payload out of the
             * discovery Request.  Controller-free LocalMock callers and
             * explicit compatibility/feature modes retain their existing
             * behavior.  The returned plaintext is held only until the
             * selected Provider's exact-name Input Data is published.
             */
```

### API-a4753af70941 · ndn_service_framework::ServiceUser::prepareRequestControllerVersion

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1457)

```cpp
bool prepareRequestControllerVersion(
                ndn_service_framework::RequestMessage& requestMessage,
                const ndn::Name& serviceName,
                const ndn::Name& requestId);
```

原始接口说明：

```text
/**
             * Apply the current service-scoped ControllerVersion to a request
             * before any raw-payload overload publishes it.  The typed and
             * prepared paths already pass through startRequestServiceWithRequestId;
             * legacy payload overloads must share the same authority check.
             */
```

### API-04d36fcf105f · ndn_service_framework::ServiceUser::makeRequestScopedInputDataName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1461)

```cpp
ndn::Name makeRequestScopedInputDataName(
                const ndn::Name& serviceName,
                const ndn::Name& requestId,
                uint64_t attempt) const;
```

### API-2d1e47ba6b1c · ndn_service_framework::ServiceUser::evaluateAckSelection

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1466)

```cpp
bool evaluateAckSelection(const ndn::Name& requestId);
```

### API-f9e8e24260df · ndn_service_framework::ServiceUser::handleAckCollectionTimeout

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1468)

```cpp
bool handleAckCollectionTimeout(const ndn::Name& requestId);
```

### API-ee4d0a9d5bc1 · ndn_service_framework::ServiceUser::closeDeferredCollaborationAcks

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1470)

```cpp
bool closeDeferredCollaborationAcks(const ndn::Name& requestId,
                                                PendingCall& pendingCall);
```

### API-d46fafdc5754 · ndn_service_framework::ServiceUser::selectLateAckAfterAckTimeout

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1473)

```cpp
bool selectLateAckAfterAckTimeout(PendingCall& pendingCall,
                                              const StoredAck& storedAck);
```

### API-0f1880792ad9 · ndn_service_framework::ServiceUser::evaluateCustomAckSelection

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1476)

```cpp
bool evaluateCustomAckSelection(PendingCall& pendingCall);
```

### API-df8d924f663d · ndn_service_framework::ServiceUser::externalizeLargeCollaborationAssignment

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1478)

```cpp
ndn::Buffer externalizeLargeCollaborationAssignment(
                const ndn::Name& providerName,
                const ndn::Name& serviceName,
                const ndn::Name& requestId,
                const ndn::Buffer& assignmentPayload);
```

### API-f9f9a5535040 · ndn_service_framework::ServiceUser::evaluateBuiltInAckSelection

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1484)

```cpp
bool evaluateBuiltInAckSelection(PendingCall& pendingCall);
```

### API-6fd7beccda7f · ndn_service_framework::ServiceUser::recordNegativeAck

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1485)

```cpp
void recordNegativeAck(PendingCall& pendingCall,
                                   const ndn::Name& requestId,
                                   const ndn::Name& providerName,
                                   const ndn_service_framework::RequestAckMessage& ackMessage);
```

### API-48f8ea735f96 · ndn_service_framework::ServiceUser::maybeEarlyStopAllKnownProvidersNegative

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1489)

```cpp
bool maybeEarlyStopAllKnownProvidersNegative(const ndn::Name& requestId);
```

### API-368ce5ce0f65 · ndn_service_framework::ServiceUser::hasReachedLatePipelineStage

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1490)

```cpp
bool hasReachedLatePipelineStage(const PendingCall& pendingCall) const;
```

### API-e01eddc6844e · ndn_service_framework::ServiceUser::recordObservedAckProvider

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1491)

```cpp
void recordObservedAckProvider(const ndn::Name& serviceName,
                                           const ndn::Name& providerName,
                                           uint64_t timestampUs);
```

### API-529f86a704ee · ndn_service_framework::ServiceUser::getRecentAckProviderCount

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1494)

```cpp
size_t getRecentAckProviderCount(const ndn::Name& serviceName,
                                             uint64_t nowUs);
```

### API-1370828a801c · ndn_service_framework::ServiceUser::collaborationAckRoleCoverageSatisfied

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1496)

```cpp
bool collaborationAckRoleCoverageSatisfied(const ndn::Name& requestId,
                                                       const PendingCall& pendingCall) const;
```

### API-c3c47ed4d531 · ndn_service_framework::ServiceUser::scheduleRequestTimeout

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1498)

```cpp
void scheduleRequestTimeout(const ndn::Name& requestId, int timeoutMs);
```

### API-3745e2dd38a6 · ndn_service_framework::ServiceUser::finalizeTimedOutPendingCall

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1499)

```cpp
void finalizeTimedOutPendingCall(const ndn::Name& requestId);
```

### API-9e86b24e713e · ndn_service_framework::ServiceUser::scheduleSelectionStatusQuery

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1500)

```cpp
void scheduleSelectionStatusQuery(const ndn::Name& requestId,
                                              const ndn::Name& providerName,
                                              const std::string& selectionDigest);
```

### API-7eea77b32640 · ndn_service_framework::ServiceUser::querySelectionStatusForTimeoutDiagnostics

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1503)

```cpp
void querySelectionStatusForTimeoutDiagnostics(const ndn::Name& requestId,
                                                           const PendingCall& pendingCall);
```

### API-c7e9f9a0a713 · ndn_service_framework::ServiceUser::parseSelectionExecutionStatusPayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1505)

```cpp
static SelectionExecutionStatus parseSelectionExecutionStatusPayload(
                const ndn::Data& data,
                const ndn::Name& providerName,
                const std::string& selectionDigest);
```

### API-672f152af72a · ndn_service_framework::ServiceUser::admitOrQueuePendingCall

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1509)

```cpp
void admitOrQueuePendingCall(const ndn::Name& requestId,
                                         bool scheduleAckTimeout,
                                         bool scheduleImmediateAckTimeout);
```

### API-1523810cb6ee · ndn_service_framework::ServiceUser::getEffectiveAdaptiveAdmissionQueueLimits

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1512)

```cpp
std::pair<size_t, size_t>
            getEffectiveAdaptiveAdmissionQueueLimits(size_t activeLimit) const;
```

### API-1fe6600a2bed · ndn_service_framework::ServiceUser::makeAdmissionControlStatus

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1514)

```cpp
AdmissionControlStatus makeAdmissionControlStatus(const ndn::Name& requestId,
                                                              size_t queueDepth,
                                                              const char* reason,
                                                              size_t softQueueLimit = 0,
                                                              size_t hardQueueLimit = 0) const;
```

### API-8d07afa65c6b · ndn_service_framework::ServiceUser::notifyAdmissionControlWarning

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1519)

```cpp
void notifyAdmissionControlWarning(const ndn::Name& requestId,
                                               size_t queueDepth,
                                               const char* reason,
                                               size_t softQueueLimit = 0,
                                               size_t hardQueueLimit = 0);
```

### API-bfe33a2fb521 · ndn_service_framework::ServiceUser::rejectPendingCallByAdmission

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1524)

```cpp
void rejectPendingCallByAdmission(const ndn::Name& requestId,
                                              const char* reason,
                                              size_t softQueueLimit = 0,
                                              size_t hardQueueLimit = 0);
```

### API-0794e9e0bc8b · ndn_service_framework::ServiceUser::publishAdmittedPendingCall

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1528)

```cpp
void publishAdmittedPendingCall(const ndn::Name& requestId);
```

### API-98f56c3a2be3 · ndn_service_framework::ServiceUser::drainAdaptiveAdmissionQueue

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1529)

```cpp
void drainAdaptiveAdmissionQueue();
```

### API-199d5f169d0c · ndn_service_framework::ServiceUser::scheduleAdaptiveAdmissionControl

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1530)

```cpp
void scheduleAdaptiveAdmissionControl();
```

### API-7802313f96be · ndn_service_framework::ServiceUser::controlAdaptiveAdmissionWindow

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1531)

```cpp
void controlAdaptiveAdmissionWindow();
```

### API-b818e8f4b915 · ndn_service_framework::ServiceUser::getEffectiveAdaptiveAdmissionWindow

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1532)

```cpp
size_t getEffectiveAdaptiveAdmissionWindow() const;
```

### API-8774c4a81a99 · ndn_service_framework::ServiceUser::releaseAdaptiveAdmissionSlot

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1533)

```cpp
void releaseAdaptiveAdmissionSlot(const ndn::Name& requestId,
                                               PendingCall& pendingCall,
                                               const char* reason,
                                              uint64_t terminalTimestampUs);
```

### API-d0a3828d435a · ndn_service_framework::ServiceUser::containsName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1538)

```cpp
static bool containsName(const std::vector<ndn::Name>& names,
                                     const ndn::Name& name);
```

### API-1fbe99359b61 · ndn_service_framework::ServiceUser::addUniqueName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1541)

```cpp
static void addUniqueName(std::vector<ndn::Name>& names,
                                      const ndn::Name& name);
```

### API-0fe74a0ce9f7 · ndn_service_framework::ServiceUser::removeName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1543)

```cpp
static void removeName(std::vector<ndn::Name>& names,
                                   const ndn::Name& name);
```

### API-593ccfccd1a6 · ndn_service_framework::ServiceUser::selectRandomProvider

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1546)

```cpp
static ndn::Name selectRandomProvider(const std::vector<ndn::Name>& providers);
```

### API-2da59090d747 · ndn_service_framework::ServiceUser::hasUserPermissionForProvider

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1548)

```cpp
bool hasUserPermissionForProvider(const ndn::Name& providerName,
                                              const ndn::Name& serviceName) const;
```

### API-60b3ee60724a · ndn_service_framework::ServiceUser::hasUserPermissionForRequest

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1550)

```cpp
bool hasUserPermissionForRequest(
                const std::vector<ndn::Name>& providers,
                const ndn::Name& serviceName) const;
```

### API-54b2296a56c4 · ndn_service_framework::ServiceUser::makeTargetedTokenPoolKey

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1553)

```cpp
static std::string makeTargetedTokenPoolKey(
                const ndn::Name& providerName,
                const ndn::Name& serviceName);
```

### API-fe731758c629 · ndn_service_framework::ServiceUser::popTargetedTokenPair

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1556)

```cpp
bool popTargetedTokenPair(const ndn::Name& providerName,
                                      const ndn::Name& serviceName,
                                      TargetedTokenPair& pair);
```

### API-a86212539260 · ndn_service_framework::ServiceUser::storeTargetedTokenPairs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1559)

```cpp
void storeTargetedTokenPairs(const ndn::Name& providerName,
                                         const ndn::Name& serviceName,
                                         const ndn_service_framework::ResponseMessage& responseMessage);
```

### API-e5fbd57f31f6 · ndn_service_framework::ServiceUser::getTargetedTokenBatchHint

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1562)

```cpp
size_t getTargetedTokenBatchHint(const ndn::Name& providerName,
                                             const ndn::Name& serviceName);
```

### API-97e188fc1de1 · ndn_service_framework::ServiceUser::markTargetedTokenRefillInFlight

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1564)

```cpp
bool markTargetedTokenRefillInFlight(const ndn::Name& providerName,
                                                 const ndn::Name& serviceName,
                                                 size_t requestedBatch);
```

### API-d1567f8f970c · ndn_service_framework::ServiceUser::clearTargetedTokenRefill

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1567)

```cpp
void clearTargetedTokenRefill(const ndn::Name& providerName,
                                          const ndn::Name& serviceName);
```

### API-2d80707fc0a5 · ndn_service_framework::ServiceUser::maybeRefillTargetedTokenPool

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1569)

```cpp
void maybeRefillTargetedTokenPool(const ndn::Name& providerName,
                                              const ndn::Name& serviceName);
```

### API-73fd89632c47 · ndn_service_framework::ServiceUser::findStoredAck

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1572)

```cpp
static const StoredAck* findStoredAck(
                const PendingCall& pendingCall,
                const ndn_service_framework::RequestAckMessage& ackMessage);
```

### API-68bfaf23ccff · ndn_service_framework::ServiceUser::startRequestServiceWithRequestId

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1576)

```cpp
ndn::Name startRequestServiceWithRequestId(const ndn::Name& requestId,
                                                  const std::vector<ndn::Name>& providers,
                                                  const ndn::Name& serviceName,
                                                  ndn_service_framework::RequestMessage requestMessage,
                                                  int timeoutMs,
                                                  TimeoutHandler onTimeout,
                                                  ResponseHandler onResponseHandler,
                                                  size_t strategy,
                                                  bool trackSelectionStatus = false,
                                                  SelectionStatusTimeoutHandler statusTimeoutHandler = {},
                                                  SelectionStatusOptions statusOptions = SelectionStatusOptions());
```

### API-cf5b018cc6c6 · ndn_service_framework::ServiceUser::prepareSelectionGatedInput

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1587)

```cpp
ndn::Buffer prepareSelectionGatedInput(
                ndn_service_framework::RequestMessage& requestMessage,
                const ndn::Name& serviceName,
                const ndn::Name& requestId);
```

### API-c5e49e77bff8 · ndn_service_framework::ServiceUser::cleanupPendingCallState

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1592)

```cpp
void cleanupPendingCallState(const ndn::Name& requestId);
```

### API-293d008705b6 · ndn_service_framework::ServiceUser::handleProviderReadyInterest

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1593)

```cpp
bool handleProviderReadyInterest(const ndn::Interest& interest);
```

### API-3e6999918cb2 · ndn_service_framework::ServiceUser::maybeActivateReadyDeployment

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1594)

```cpp
void maybeActivateReadyDeployment(const ndn::Name& requestId,
                                              PendingCall& pendingCall);
```

### API-facb9a63b90d · ndn_service_framework::ServiceUser::publishExecutionActivate

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1596)

```cpp
void publishExecutionActivate(const ndn::Name& provider,
                                          const std::string& controlHandle,
                                          const ExecutionActivateMessage& activation,
                                          int attempt = 0);
```

### API-deb4b79b686f · ndn_service_framework::ServiceUser::logRequestPendingCreated

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1600)

```cpp
void logRequestPendingCreated(const ndn::Name& requestId,
                                          const PendingCall& pendingCall);
```

### API-5d38672a68c3 · ndn_service_framework::ServiceUser::erasePendingCallWithTrace

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1602)

```cpp
void erasePendingCallWithTrace(const ndn::Name& requestId,
                                           std::map<ndn::Name, PendingCall>::iterator pendingCall,
                                           const char* reason);
```

### API-9683cb541d2b · ndn_service_framework::ServiceUser::logAckMatchAttempt

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1605)

```cpp
void logAckMatchAttempt(const ndn::Name& requestId,
                                    const ndn::Name& ackName,
                                    const ndn::Name& providerName,
                                    uint64_t ackReceiveUs,
                                    const char* phase);
```

### API-a8a608e3a643 · ndn_service_framework::ServiceUser::logAckNoPending

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1610)

```cpp
void logAckNoPending(const ndn::Name& requestId,
                                 const ndn::Name& ackName,
                                 const ndn::Name& providerName,
                                 uint64_t ackReceiveUs);
```

### API-dc04a1029638 · ndn_service_framework::ServiceUser::updateRequestLifecycleState

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1614)

```cpp
void updateRequestLifecycleState(const ndn::Name& requestId,
                                             RequestLifecycleState state,
                                             const char* cleanupReason = nullptr);
```

### API-8001383bccd9 · ndn_service_framework::ServiceUser::samplePendingCallKeys

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1617)

```cpp
std::string samplePendingCallKeys(size_t limit = 5) const;
```

### API-d1694fe1f37a · ndn_service_framework::ServiceUser::dispatchResponseHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1618)

```cpp
void dispatchResponseHandler(ResponseHandler responseHandler,
                                         const ndn::Name& requestId,
                                         ResponseMessage responseMessage);
```

### API-6d4d8cdcc6a7 · ndn_service_framework::ServiceUser::scheduleResponseAttemptTimeout

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1621)

```cpp
void scheduleResponseAttemptTimeout(const ndn::Name& requestId,
                                                const ndn::Name& providerName);
```

### API-be1faa33cb25 · ndn_service_framework::ServiceUser::retryResponseWithNextProvider

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1623)

```cpp
bool retryResponseWithNextProvider(const ndn::Name& requestId,
                                               const char* trigger);
```

### API-44a82c42f8db · ndn_service_framework::ServiceUser::activeNacConsumer

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1626)

```cpp
ndn::nacabe::Consumer& activeNacConsumer()
```

### API-79230bc56187 · ndn_service_framework::ServiceUser::activeNacProducer

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1631)

```cpp
ndn::nacabe::CacheProducer& activeNacProducer()
```

### API-64a4f6aa2239 · ndn_service_framework::ServiceUser::& m_face

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1636)

```cpp
ndn::Face& m_face;
```

### API-c7c857876899 · ndn_service_framework::ServiceUser::m_scheduler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1637)

```cpp
ndn::Scheduler m_scheduler;
```

### API-0adb9180a382 · ndn_service_framework::ServiceUser::m_identityRegistration

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1639)

```cpp
std::shared_ptr<ndn::ScopedRegisteredPrefixHandle> m_identityRegistration;
```

原始接口说明：

```text
// Retry callbacks retain only a weak reference to this route owner.
```

### API-7e340a3afdfa · ndn_service_framework::ServiceUser::identity

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1640)

```cpp
ndn::Name identity;
```

### API-f6572efd9449 · ndn_service_framework::ServiceUser::m_keyChain

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1641)

```cpp
ndn::KeyChain m_keyChain;
```

### API-01087fbd13b9 · ndn_service_framework::ServiceUser::* m_testSigningKeyChain

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1642)

```cpp
ndn::KeyChain* m_testSigningKeyChain = nullptr;
```

### API-39484288b53f · ndn_service_framework::ServiceUser::m_svsps

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1643)

```cpp
std::shared_ptr<ndn::svs::SVSPubSub> m_svsps;
```

### API-f2f75286a556 · ndn_service_framework::ServiceUser::validator

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1644)

```cpp
std::shared_ptr<MessageValidator> validator;
```

### API-fa01d3bfd8c7 · ndn_service_framework::ServiceUser::m_serviceNames

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1645)

```cpp
std::vector<std::string> m_serviceNames;
```

### API-fe2c1ada83f5 · ndn_service_framework::ServiceUser::nac_validator

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1648)

```cpp
ndn::ValidatorConfig nac_validator{m_face};
```

原始接口说明：

```text
//ndn::security::Validator nac_validator;
```

### API-73f76d0eb5b8 · ndn_service_framework::ServiceUser::identityCert

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1649)

```cpp
ndn::security::Certificate identityCert;
```

### API-704147065e27 · ndn_service_framework::ServiceUser::signingCert

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1650)

```cpp
ndn::security::Certificate signingCert;
```

### API-1ba8b6d625c2 · ndn_service_framework::ServiceUser::attrAuthorityCertificate

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1651)

```cpp
ndn::security::Certificate attrAuthorityCertificate;
```

### API-59220350f259 · ndn_service_framework::ServiceUser::nacConsumer

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1653)

```cpp
ndn::nacabe::Consumer nacConsumer;
```

### API-8fa98345ae10 · ndn_service_framework::ServiceUser::m_testNacConsumer

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1654)

```cpp
std::unique_ptr<ndn::nacabe::Consumer> m_testNacConsumer;
```

### API-611591472c46 · ndn_service_framework::ServiceUser::m_isLocalMock

public / test-helper；[源码](../../ndn-service-framework/ServiceUser.hpp#L1658)

```cpp
bool m_isLocalMock = false;
```

原始接口说明：

```text
// LocalMock fixtures do not model the production Controller/AA
// bootstrap lifecycle.  Keep their refresh path fail-closed until
// the fixture explicitly completes its bootstrap.
```

### API-0c36638fbe7c · ndn_service_framework::ServiceUser::nacProducer

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1660)

```cpp
ndn::nacabe::CacheProducer nacProducer;
```

原始接口说明：

```text
//ndn::nacabe::Producer nacProducer;
```

### API-f9bb52318bf2 · ndn_service_framework::ServiceUser::m_testNacProducer

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1661)

```cpp
std::unique_ptr<ndn::nacabe::CacheProducer> m_testNacProducer;
```

### API-083dfd9a5f2a · ndn_service_framework::ServiceUser::m_signingInfo

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1662)

```cpp
ndn::security::SigningInfo m_signingInfo;
```

### API-6d6a86f27d5c · ndn_service_framework::ServiceUser::m_useTokens

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1663)

```cpp
bool m_useTokens = true;
```

### API-d03435f26d44 · ndn_service_framework::ServiceUser::m_timelineTrace

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1666)

```cpp
bool m_timelineTrace = timelineTraceEnvEnabled();
```

原始接口说明：

```text
// Allow Python/experiment launchers to enable the existing
// ndn-cxx TimelineTrace logger without a wrapper-only API seam.
```

### API-2ab078f1b726 · ndn_service_framework::ServiceUser::m_currentPolicyEpoch

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1667)

```cpp
size_t m_currentPolicyEpoch = 0;
```

### API-83aebaba4f5b · ndn_service_framework::ServiceUser::m_requiredKeyEpoch

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1668)

```cpp
size_t m_requiredKeyEpoch = 0;
```

### API-d6e49ace0598 · ndn_service_framework::ServiceUser::m_controllerVersionMutex

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1669)

```cpp
mutable std::mutex m_controllerVersionMutex;
```

### API-6e7d84bf37e7 · ndn_service_framework::ServiceUser::m_controllerVersion

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1670)

```cpp
std::optional<ControllerVersion> m_controllerVersion;
```

### API-80d88c7cc3fe · ndn_service_framework::ServiceUser::m_revocationStates

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1671)

```cpp
std::map<std::string, RevocationState> m_revocationStates;
```

### API-4f25c8f26a0f · ndn_service_framework::ServiceUser::m_policyRefreshCoordinators

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1672)

```cpp
std::map<std::string, PolicyRefreshCoordinator>
                m_policyRefreshCoordinators;
```

### API-7e6d32c30304 · ndn_service_framework::ServiceUser::m_controllerPrefix

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1674)

```cpp
ndn::Name m_controllerPrefix;
```

### API-68d120c92aa6 · ndn_service_framework::ServiceUser::m_policyStatusFetchInFlight

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1675)

```cpp
std::set<std::string> m_policyStatusFetchInFlight;
```

### API-4cc875534957 · ndn_service_framework::ServiceUser::m_policyStatusRefreshScheduled

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1676)

```cpp
std::map<std::string, ControllerVersion>
                m_policyStatusRefreshScheduled;
```

### API-6761b3b4e359 · ndn_service_framework::ServiceUser::m_nacDkeyRefreshPendingServices

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1683)

```cpp
std::set<std::string> m_nacDkeyRefreshPendingServices;
```

原始接口说明：

```text
// A grant-only PermissionResponse can add a service while the
// ABE generation remains unchanged. Defer one identity-scoped
// DKEY replacement for the affected service until its signed
// PolicyStatus is installed; keep this per-service so an update
// for S cannot trigger a refresh while installing T's status.
```

### API-e108651a6d90 · ndn_service_framework::ServiceUser::m_lastDkeyRefreshWave

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1687)

```cpp
std::optional<ControllerVersion> m_lastDkeyRefreshWave;
```

原始接口说明：

```text
// ControllerVersion of the last permission wave for which a
// DKEY refresh was requested; same-wave service status installs
// collapse into that single identity-wide replacement fetch.
```

### API-015f9875677b · ndn_service_framework::ServiceUser::m_runtimeStatusStore

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1691)

```cpp
std::unique_ptr<RuntimeStatusStore> m_runtimeStatusStore;
```

原始接口说明：

```text
// Opt-in durable runtime status (NDNSF_PERSIST_RUNTIME_STATE):
// accepted per-service statuses are persisted and restored across
// process restart.  DKEY material is never stored.
```

### API-33865a2ccd49 · ndn_service_framework::ServiceUser::m_persistedRuntimeStatuses

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1692)

```cpp
std::map<std::string, RuntimeStatusStore::Record>
                m_persistedRuntimeStatuses;
```

### API-a1a19d6fa058 · ndn_service_framework::ServiceUser::m_policyGracePeriodMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1694)

```cpp
uint64_t m_policyGracePeriodMs = 0;
```

### API-b77b60f430d3 · ndn_service_framework::ServiceUser::m_hybridMessageCrypto

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1695)

```cpp
HybridMessageCrypto m_hybridMessageCrypto;
```

### API-83e0a6b62839 · ndn_service_framework::ServiceUser::m_hybridCryptoCounters

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1696)

```cpp
HybridCryptoCounters m_hybridCryptoCounters;
```

### API-cf891da3c6a3 · ndn_service_framework::ServiceUser::m_requestScopedNonceRegistry

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1697)

```cpp
NonceRegistry m_requestScopedNonceRegistry;
```

### API-0b949906e95b · ndn_service_framework::ServiceUser::m_cryptoProduceQueue

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1698)

```cpp
SerializedWorkerQueue m_cryptoProduceQueue{"ServiceUser NAC-ABE produce"};
```

### API-a64b79665371 · ndn_service_framework::ServiceUser::m_handlerPool

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1699)

```cpp
BoundedWorkerPool m_handlerPool{"ServiceUser response callbacks"};
```

### API-f6813cf0f7a8 · ndn_service_framework::ServiceUser::m_ackProcessingPool

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1700)

```cpp
BoundedWorkerPool m_ackProcessingPool{"ServiceUser ACK processing"};
```

### API-37b0f630002c · ndn_service_framework::ServiceUser::m_IMS

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1702)

```cpp
ndn::InMemoryStorageFifo m_IMS;
```

### API-61a9fb53ea1c · ndn_service_framework::ServiceUser::_cache_mutex

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1703)

```cpp
std::mutex _cache_mutex;
```

### API-0eabc0eab13b · ndn_service_framework::ServiceUser::m_ServiceDiscovery

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1705)

```cpp
OptionalServiceDiscovery m_ServiceDiscovery;
```

### API-60afc6394c21 · ndn_service_framework::ServiceUser::m_authorizations

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1706)

```cpp
ServiceAuthorizationTable m_authorizations;
```

### API-0a627ee12533 · ndn_service_framework::ServiceUser::m_strategyMap

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1708)

```cpp
std::map<ndn::Name, size_t> m_strategyMap;
```

### API-f008aeadef12 · ndn_service_framework::ServiceUser::m_AckInfoMap

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1711)

```cpp
std::map<ndn::Name, std::vector<AckInfo>> m_AckInfoMap;
```

原始接口说明：

```text
// a map used for load balancing requestID
```

### API-748ac086f43f · ndn_service_framework::ServiceUser::m_configManager

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1713)

```cpp
ConfigManager m_configManager;
```

### API-6e23a954b0cd · ndn_service_framework::ServiceUser::m_sessionIDMap

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1715)

```cpp
std::map<ndn::Name, int> m_sessionIDMap;
```

### API-bd56d68a6265 · ndn_service_framework::ServiceUser::svs_mutex

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1717)

```cpp
std::mutex svs_mutex;
```

### API-e2bc80a3ad2c · ndn_service_framework::ServiceUser::m_pendingCalls

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1719)

```cpp
std::map<ndn::Name, PendingCall> m_pendingCalls;
```

### API-c7edb879ed2c · ndn_service_framework::ServiceUser::m_verifiedCollaborationMutex

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1720)

```cpp
std::mutex m_verifiedCollaborationMutex;
```

### API-10ea00745e47 · ndn_service_framework::ServiceUser::m_verifiedCollaborationCv

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1721)

```cpp
std::condition_variable m_verifiedCollaborationCv;
```

### API-eeab16ca9a39 · ndn_service_framework::ServiceUser::m_userCollaborationScopeKeys

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1722)

```cpp
std::map<ndn::Name, std::map<KeyScope, ndn::Buffer>>
                m_userCollaborationScopeKeys;
```

### API-101460588366 · ndn_service_framework::ServiceUser::m_verifiedCollaborationData

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1724)

```cpp
std::map<ndn::Name, std::vector<VerifiedCollaborationData>>
                m_verifiedCollaborationData;
```

### API-9f848c53c589 · ndn_service_framework::ServiceUser::m_collaborationSequence

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1726)

```cpp
std::atomic<uint64_t> m_collaborationSequence{0};
```

### API-778d9cd69638 · ndn_service_framework::ServiceUser::m_streamConsumers

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1727)

```cpp
std::map<ndn::Name, std::shared_ptr<StreamEventConsumer>> m_streamConsumers;
```

### API-52398f58551e · ndn_service_framework::ServiceUser::m_streamStates

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1728)

```cpp
std::map<ndn::Name, std::shared_ptr<StreamedInvocationSharedState>> m_streamStates;
```

### API-f75bf2be4fac · ndn_service_framework::ServiceUser::m_streamInactivityMutex

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1729)

```cpp
std::mutex m_streamInactivityMutex;
```

### API-110690ed20e1 · ndn_service_framework::ServiceUser::m_streamInactivityEpochs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1730)

```cpp
std::map<ndn::Name, uint64_t> m_streamInactivityEpochs;
```

### API-a430da7beb60 · ndn_service_framework::ServiceUser::m_streamEventKeysPending

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1731)

```cpp
std::map<ndn::Name, ndn::Buffer> m_streamEventKeysPending;
```

### API-189fd0f97e49 · ndn_service_framework::ServiceUser::m_targetedTokenPoolsMutex

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1732)

```cpp
std::mutex m_targetedTokenPoolsMutex;
```

### API-e82a36e054ee · ndn_service_framework::ServiceUser::m_targetedTokenPools

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1733)

```cpp
std::map<std::string, std::deque<TargetedTokenPair>> m_targetedTokenPools;
```

### API-4a6f21bcea56 · ndn_service_framework::ServiceUser::m_targetedTokenPoolControls

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1734)

```cpp
std::map<std::string, TargetedTokenPoolControl>
                m_targetedTokenPoolControls;
```

### API-481172a7a551 · ndn_service_framework::ServiceUser::m_targetedStreamOffers

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1736)

```cpp
std::map<std::string, TargetedStreamOffer> m_targetedStreamOffers;
```

### API-03ffff65b652 · ndn_service_framework::ServiceUser::m_recentAckProvidersByService

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1737)

```cpp
std::map<ndn::Name, std::map<std::string, uint64_t>>
                m_recentAckProvidersByService;
```

### API-21402ac03f28 · ndn_service_framework::ServiceUser::m_pendingCallTraceHistory

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1739)

```cpp
std::map<ndn::Name, PendingCallTraceRecord> m_pendingCallTraceHistory;
```

### API-658bc5fb913a · ndn_service_framework::ServiceUser::m_requestLifecycleStatuses

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1740)

```cpp
std::map<ndn::Name, RequestLifecycleStatus> m_requestLifecycleStatuses;
```

### API-e1635f8d7b53 · ndn_service_framework::ServiceUser::m_requestLifecycleCallback

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1741)

```cpp
RequestLifecycleCallback m_requestLifecycleCallback;
```

### API-3defb8966a75 · ndn_service_framework::ServiceUser::m_admissionControlWarningHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1742)

```cpp
AdmissionControlWarningHandler m_admissionControlWarningHandler;
```

### API-0ca5ddd65d6a · ndn_service_framework::ServiceUser::m_admissionControlRejectHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1743)

```cpp
AdmissionControlRejectHandler m_admissionControlRejectHandler;
```

### API-1ced81974539 · ndn_service_framework::ServiceUser::m_requestPublisher

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1744)

```cpp
RequestPublisher m_requestPublisher;
```

### API-ea1ab92a19a3 · ndn_service_framework::ServiceUser::m_localPublicationHandler

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1745)

```cpp
LocalPublicationHandler m_localPublicationHandler;
```

### API-84ced0cbc441 · ndn_service_framework::ServiceUser::m_pendingCallTimeoutGrace

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1746)

```cpp
ndn::time::milliseconds m_pendingCallTimeoutGrace{500};
```

### API-23103b8a0c33 · ndn_service_framework::ServiceUser::m_responseRetryOptions

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1747)

```cpp
ResponseRetryOptions m_responseRetryOptions;
```

### API-ead02acf70d4 · ndn_service_framework::ServiceUser::m_performanceMode

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1748)

```cpp
bool m_performanceMode = false;
```

### API-551b62c2dd9f · ndn_service_framework::ServiceUser::m_runtimeDiagnostics

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1751)

```cpp
mutable RuntimeDiagnostics m_runtimeDiagnostics;
```

原始接口说明：

```text
// const request-preparation paths record compatibility counters;
// keep the diagnostics mutable so those observations are retained.
```

### API-5f86db79b462 · ndn_service_framework::ServiceUser::m_networkTelemetry

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1752)

```cpp
NetworkTelemetryStore m_networkTelemetry;
```

### API-e11366325474 · ndn_service_framework::ServiceUser::m_adaptiveAdmissionOptions

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1753)

```cpp
AdaptiveAdmissionOptions m_adaptiveAdmissionOptions;
```

### API-8466bd677055 · ndn_service_framework::ServiceUser::m_adaptiveAdmissionWindow

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1754)

```cpp
size_t m_adaptiveAdmissionWindow = 16;
```

### API-1574d9af6fb5 · ndn_service_framework::ServiceUser::m_adaptiveAdmissionSlowStartThreshold

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1755)

```cpp
size_t m_adaptiveAdmissionSlowStartThreshold = 512;
```

### API-52405e26da59 · ndn_service_framework::ServiceUser::m_adaptiveAdmissionInflight

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1756)

```cpp
size_t m_adaptiveAdmissionInflight = 0;
```

### API-13a763ea0489 · ndn_service_framework::ServiceUser::m_adaptiveAdmissionControlScheduled

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1757)

```cpp
bool m_adaptiveAdmissionControlScheduled = false;
```

### API-39f0c0db0071 · ndn_service_framework::ServiceUser::m_adaptiveAdmissionIntervalSuccesses

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1758)

```cpp
uint64_t m_adaptiveAdmissionIntervalSuccesses = 0;
```

### API-30d68b19b8c7 · ndn_service_framework::ServiceUser::m_adaptiveAdmissionIntervalTimeouts

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1759)

```cpp
uint64_t m_adaptiveAdmissionIntervalTimeouts = 0;
```

### API-df88682d30aa · ndn_service_framework::ServiceUser::m_adaptiveAdmissionIntervalBackpressure

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1760)

```cpp
uint64_t m_adaptiveAdmissionIntervalBackpressure = 0;
```

### API-4068045ce9d1 · ndn_service_framework::ServiceUser::m_adaptiveAdmissionIntervalQueueWarnings

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1761)

```cpp
uint64_t m_adaptiveAdmissionIntervalQueueWarnings = 0;
```

### API-79243a936a04 · ndn_service_framework::ServiceUser::m_adaptiveAdmissionIntervalLatencySumMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1762)

```cpp
double m_adaptiveAdmissionIntervalLatencySumMs = 0.0;
```

### API-347923943e97 · ndn_service_framework::ServiceUser::m_adaptiveAdmissionIntervalLatencyCount

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1763)

```cpp
uint64_t m_adaptiveAdmissionIntervalLatencyCount = 0;
```

### API-e61911c32498 · ndn_service_framework::ServiceUser::m_adaptiveAdmissionIntervalLatenciesMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1764)

```cpp
std::vector<double> m_adaptiveAdmissionIntervalLatenciesMs;
```

### API-36d077012e87 · ndn_service_framework::ServiceUser::m_adaptiveAdmissionBaselineLatencyMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1765)

```cpp
double m_adaptiveAdmissionBaselineLatencyMs = 0.0;
```

### API-65cc555b5784 · ndn_service_framework::ServiceUser::m_adaptiveAdmissionPreviousQueueDelayMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1766)

```cpp
double m_adaptiveAdmissionPreviousQueueDelayMs = 0.0;
```

### API-50a8245f87b0 · ndn_service_framework::ServiceUser::m_adaptiveAdmissionPreviousAverageLatencyMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1767)

```cpp
double m_adaptiveAdmissionPreviousAverageLatencyMs = 0.0;
```

### API-b77631453f00 · ndn_service_framework::ServiceUser::m_adaptiveAdmissionPreviousP95LatencyMs

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1768)

```cpp
double m_adaptiveAdmissionPreviousP95LatencyMs = 0.0;
```

### API-3b3e7e6027a0 · ndn_service_framework::ServiceUser::m_adaptiveAdmissionCompletionRateEmaRps

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1769)

```cpp
double m_adaptiveAdmissionCompletionRateEmaRps = 0.0;
```

### API-de9e4daed687 · ndn_service_framework::ServiceUser::m_adaptiveAdmissionRecommendedRateRps

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1770)

```cpp
double m_adaptiveAdmissionRecommendedRateRps = 0.0;
```

### API-52f265f4b255 · ndn_service_framework::ServiceUser::m_adaptiveAdmissionLatencyRisingIntervals

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1771)

```cpp
size_t m_adaptiveAdmissionLatencyRisingIntervals = 0;
```

### API-3696e0497e49 · ndn_service_framework::ServiceUser::m_adaptiveAdmissionAverageLatencyRisingIntervals

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1772)

```cpp
size_t m_adaptiveAdmissionAverageLatencyRisingIntervals = 0;
```

### API-73bb980483ca · ndn_service_framework::ServiceUser::m_adaptiveAdmissionRecoveryIntervals

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1773)

```cpp
size_t m_adaptiveAdmissionRecoveryIntervals = 0;
```

### API-d4cae7293627 · ndn_service_framework::ServiceUser::m_adaptiveAdmissionSuccessfulControlIntervals

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1774)

```cpp
size_t m_adaptiveAdmissionSuccessfulControlIntervals = 0;
```

### API-bd9334981877 · ndn_service_framework::ServiceUser::m_adaptiveAdmissionQueueDelayOverTargetIntervals

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1775)

```cpp
size_t m_adaptiveAdmissionQueueDelayOverTargetIntervals = 0;
```

### API-93cb3af40891 · ndn_service_framework::ServiceUser::m_adaptiveAdmissionIntervalCongested

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1776)

```cpp
bool m_adaptiveAdmissionIntervalCongested = false;
```

### API-2df6a7ebcabd · ndn_service_framework::ServiceUser::m_adaptiveAdmissionIntervalSevere

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1777)

```cpp
bool m_adaptiveAdmissionIntervalSevere = false;
```

### API-874dae6f514f · ndn_service_framework::ServiceUser::m_adaptiveAdmissionQueue

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1778)

```cpp
std::deque<ndn::Name> m_adaptiveAdmissionQueue;
```

### API-72ee2b984a73 · ndnsf::ProviderId

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1784)

```cpp
using ProviderId = ndn_service_framework::ProviderId;
```

### API-cd1b12e83847 · ndnsf::ServiceName

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1785)

```cpp
using ServiceName = ndn_service_framework::ServiceName;
```

### API-e0b258b758d9 · ndnsf::RequestId

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1786)

```cpp
using RequestId = ndn_service_framework::RequestId;
```

### API-4642dc7737ec · ndnsf::RequestPayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1787)

```cpp
using RequestPayload = ndn_service_framework::RequestPayload;
```

### API-28f5378048ec · ndnsf::ResponsePayload

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1788)

```cpp
using ResponsePayload = ndn_service_framework::ResponsePayload;
```

### API-4cf896187af2 · ndnsf::AckCandidate

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1789)

```cpp
using AckCandidate = ndn_service_framework::AckCandidate;
```

### API-f715b66f185d · ndnsf::AckSelectionPolicy

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1790)

```cpp
using AckSelectionPolicy = ndn_service_framework::AckSelectionPolicy;
```

### API-aae4275c7907 · ndnsf::strategy::FirstResponding

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1794)

```cpp
extern const std::shared_ptr<const ndn_service_framework::AckSelectionPolicy>
            FirstResponding;
```

### API-f95460d715ff · ndnsf::strategy::RandomSelection

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1796)

```cpp
extern const std::shared_ptr<const ndn_service_framework::AckSelectionPolicy>
            RandomSelection;
```

### API-95cd02696316 · ndnsf::strategy::AllSelected

public / declared-interface；[源码](../../ndn-service-framework/ServiceUser.hpp#L1798)

```cpp
extern const std::shared_ptr<const ndn_service_framework::AckSelectionPolicy>
            AllSelected;
```

## ndn-service-framework/Stream.hpp

源码 SHA-256：`326d98d68459db9817265528e9c56bcf266f3b3681571f149af546c182b58b21`。

### API-f2931c8ae364 · ndn_service_framework::stream_tlv::(anonymous)

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L23)

```cpp
enum
```

### API-b15221a04061 · ndn_service_framework::stream_tlv::(anonymous)::StreamInfoType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L24)

```cpp
StreamInfoType = 0xF610
```

### API-34e5270e0370 · ndn_service_framework::stream_tlv::(anonymous)::StreamChunkType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L25)

```cpp
StreamChunkType = 0xF611
```

### API-4d9803d86e24 · ndn_service_framework::stream_tlv::(anonymous)::StreamFecInfoType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L26)

```cpp
StreamFecInfoType = 0xF612
```

### API-7e1317c0aaa1 · ndn_service_framework::stream_tlv::(anonymous)::StreamIdType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L27)

```cpp
StreamIdType = 0xF613
```

### API-66e19e032f52 · ndn_service_framework::stream_tlv::(anonymous)::StreamSessionEpochType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L28)

```cpp
StreamSessionEpochType = 0xF614
```

### API-62145ea5c977 · ndn_service_framework::stream_tlv::(anonymous)::StreamPrefixType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L29)

```cpp
StreamPrefixType = 0xF615
```

### API-51636e65e55d · ndn_service_framework::stream_tlv::(anonymous)::StreamSequenceType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L30)

```cpp
StreamSequenceType = 0xF616
```

### API-ef7f9cb419f8 · ndn_service_framework::stream_tlv::(anonymous)::StreamContentTypeType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L31)

```cpp
StreamContentTypeType = 0xF617
```

### API-aa58c7add632 · ndn_service_framework::stream_tlv::(anonymous)::StreamFreshnessMsType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L32)

```cpp
StreamFreshnessMsType = 0xF618
```

### API-ecd63ff6cc02 · ndn_service_framework::stream_tlv::(anonymous)::StreamMaxPayloadBytesType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L33)

```cpp
StreamMaxPayloadBytesType = 0xF619
```

### API-f0e9c8b67721 · ndn_service_framework::stream_tlv::(anonymous)::StreamWindowType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L34)

```cpp
StreamWindowType = 0xF61A
```

### API-f8d78a9c67cb · ndn_service_framework::stream_tlv::(anonymous)::StreamLookaheadType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L35)

```cpp
StreamLookaheadType = 0xF61B
```

### API-e93fb03b5476 · ndn_service_framework::stream_tlv::(anonymous)::StreamInterestLifetimeMsType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L36)

```cpp
StreamInterestLifetimeMsType = 0xF61C
```

### API-c6d234249b1a · ndn_service_framework::stream_tlv::(anonymous)::StreamMissingTimeoutMsType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L37)

```cpp
StreamMissingTimeoutMsType = 0xF61D
```

### API-ed8495b7a827 · ndn_service_framework::stream_tlv::(anonymous)::StreamReliabilityType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L38)

```cpp
StreamReliabilityType = 0xF61E
```

### API-c1ada74c6fe6 · ndn_service_framework::stream_tlv::(anonymous)::StreamCreatedMsType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L39)

```cpp
StreamCreatedMsType = 0xF61F
```

### API-57219750e604 · ndn_service_framework::stream_tlv::(anonymous)::StreamCaptureMsType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L40)

```cpp
StreamCaptureMsType = 0xF620
```

### API-b4bef7fcec7a · ndn_service_framework::stream_tlv::(anonymous)::StreamArrivalMsType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L41)

```cpp
StreamArrivalMsType = 0xF621
```

### API-27fe2e08d0d1 · ndn_service_framework::stream_tlv::(anonymous)::StreamDeadlineMsType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L42)

```cpp
StreamDeadlineMsType = 0xF622
```

### API-a42a9e028541 · ndn_service_framework::stream_tlv::(anonymous)::StreamKeyChunkType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L43)

```cpp
StreamKeyChunkType = 0xF623
```

### API-25e849065923 · ndn_service_framework::stream_tlv::(anonymous)::StreamFrameIdType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L44)

```cpp
StreamFrameIdType = 0xF624
```

### API-43da642c3738 · ndn_service_framework::stream_tlv::(anonymous)::StreamFrameFirstSeqType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L45)

```cpp
StreamFrameFirstSeqType = 0xF625
```

### API-37b687a8da04 · ndn_service_framework::stream_tlv::(anonymous)::StreamFrameLastSeqType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L46)

```cpp
StreamFrameLastSeqType = 0xF626
```

### API-90e5080df20b · ndn_service_framework::stream_tlv::(anonymous)::StreamSegmentIndexType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L47)

```cpp
StreamSegmentIndexType = 0xF627
```

### API-c3463ad64db0 · ndn_service_framework::stream_tlv::(anonymous)::StreamSegmentCountType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L48)

```cpp
StreamSegmentCountType = 0xF628
```

### API-8c6bad8af585 · ndn_service_framework::stream_tlv::(anonymous)::StreamFecSchemeType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L49)

```cpp
StreamFecSchemeType = 0xF629
```

### API-878175a61828 · ndn_service_framework::stream_tlv::(anonymous)::StreamFecDataShardsType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L50)

```cpp
StreamFecDataShardsType = 0xF62A
```

### API-fffa303c416a · ndn_service_framework::stream_tlv::(anonymous)::StreamFecParityShardsType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L51)

```cpp
StreamFecParityShardsType = 0xF62B
```

### API-f9e197daa198 · ndn_service_framework::stream_tlv::(anonymous)::StreamFecSymbolIndexType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L52)

```cpp
StreamFecSymbolIndexType = 0xF62C
```

### API-aa8e65a616ba · ndn_service_framework::stream_tlv::(anonymous)::StreamFecSymbolCountType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L53)

```cpp
StreamFecSymbolCountType = 0xF62D
```

### API-eeaa6580a787 · ndn_service_framework::stream_tlv::(anonymous)::StreamFecDataLengthType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L54)

```cpp
StreamFecDataLengthType = 0xF62E
```

### API-14c379d45987 · ndn_service_framework::stream_tlv::(anonymous)::StreamFecSourceBlockIdType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L55)

```cpp
StreamFecSourceBlockIdType = 0xF62F
```

### API-7bd54a708c64 · ndn_service_framework::stream_tlv::(anonymous)::StreamFecRepairSymbolType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L56)

```cpp
StreamFecRepairSymbolType = 0xF630
```

### API-0310fb5d9477 · ndn_service_framework::stream_tlv::(anonymous)::StreamMetadataType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L57)

```cpp
StreamMetadataType = 0xF631
```

### API-ed1c95167646 · ndn_service_framework::stream_tlv::(anonymous)::StreamPayloadType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L58)

```cpp
StreamPayloadType = 0xF632
```

### API-6f1132c041d1 · ndn_service_framework::stream_tlv::(anonymous)::StreamNameMapBlockType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L59)

```cpp
StreamNameMapBlockType = 0xF633
```

### API-43b04b937f07 · ndn_service_framework::stream_tlv::(anonymous)::StreamContractVersionType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L60)

```cpp
StreamContractVersionType = 0xF634
```

### API-16d6cd5bd33c · ndn_service_framework::stream_tlv::(anonymous)::StreamMappingVersionType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L61)

```cpp
StreamMappingVersionType = 0xF635
```

### API-3c3d9f59fa43 · ndn_service_framework::stream_tlv::(anonymous)::StreamMapBlockNumberType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L62)

```cpp
StreamMapBlockNumberType = 0xF636
```

### API-110a0a721c51 · ndn_service_framework::stream_tlv::(anonymous)::StreamMapBlockCapacityType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L63)

```cpp
StreamMapBlockCapacityType = 0xF637
```

### API-6169ef3b34cb · ndn_service_framework::stream_tlv::(anonymous)::StreamMapFirstCursorType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L64)

```cpp
StreamMapFirstCursorType = 0xF638
```

### API-7584aeea4eb7 · ndn_service_framework::stream_tlv::(anonymous)::StreamPreviousContentDigestType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L65)

```cpp
StreamPreviousContentDigestType = 0xF639
```

### API-2dc91e01ca8e · ndn_service_framework::stream_tlv::(anonymous)::StreamNameMapEntryType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L66)

```cpp
StreamNameMapEntryType = 0xF63A
```

### API-baf0809dab31 · ndn_service_framework::stream_tlv::(anonymous)::StreamNameMapTombstoneType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L67)

```cpp
StreamNameMapTombstoneType = 0xF63B
```

### API-33c6ceb3ceac · ndn_service_framework::stream_tlv::(anonymous)::LiveStreamFecRepairType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L68)

```cpp
LiveStreamFecRepairType = 0xF63C
```

### API-6d26472cf71e · ndn_service_framework::stream_tlv::(anonymous)::LiveStreamFecGroupIdType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L69)

```cpp
LiveStreamFecGroupIdType = 0xF63D
```

### API-c5c4159e5cdd · ndn_service_framework::stream_tlv::(anonymous)::LiveStreamFecCreatedMsType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L70)

```cpp
LiveStreamFecCreatedMsType = 0xF63E
```

### API-25ed70b2d547 · ndn_service_framework::stream_tlv::(anonymous)::LiveStreamFecExpiresMsType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L71)

```cpp
LiveStreamFecExpiresMsType = 0xF63F
```

### API-2185c0019376 · ndn_service_framework::stream_tlv::(anonymous)::LiveStreamFecSourceType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L72)

```cpp
LiveStreamFecSourceType = 0xF640
```

### API-8a8a626c8240 · ndn_service_framework::stream_tlv::(anonymous)::LiveStreamFecSourceNameType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L73)

```cpp
LiveStreamFecSourceNameType = 0xF641
```

### API-1ba907dcf985 · ndn_service_framework::stream_tlv::(anonymous)::LiveStreamFecSourceCursorType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L74)

```cpp
LiveStreamFecSourceCursorType = 0xF642
```

### API-a933d1b1358c · ndn_service_framework::stream_tlv::(anonymous)::LiveStreamFecSourceLengthType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L75)

```cpp
LiveStreamFecSourceLengthType = 0xF643
```

### API-c7e4bb593028 · ndn_service_framework::stream_tlv::(anonymous)::LiveStreamFecSourceDigestType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L76)

```cpp
LiveStreamFecSourceDigestType = 0xF644
```

### API-22987e8fff12 · ndn_service_framework::stream_tlv::(anonymous)::LiveStreamFecRepairNameType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L77)

```cpp
LiveStreamFecRepairNameType = 0xF645
```

### API-62516251f97a · ndn_service_framework::stream_tlv::(anonymous)::LiveStreamFecRepairCursorType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L78)

```cpp
LiveStreamFecRepairCursorType = 0xF646
```

### API-e380fac4b1d1 · ndn_service_framework::stream_tlv::(anonymous)::LiveStreamFecCodedBytesType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L79)

```cpp
LiveStreamFecCodedBytesType = 0xF647
```

### API-3b33b1dce5e4 · ndn_service_framework::stream_tlv::(anonymous)::StreamGroupIdType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L80)

```cpp
StreamGroupIdType = 0xF648
```

### API-f9914bcb5b01 · ndn_service_framework::stream_tlv::(anonymous)::StreamSampleClassType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L81)

```cpp
StreamSampleClassType = 0xF649
```

### API-9ecae5568da5 · ndn_service_framework::stream_tlv::(anonymous)::StreamGroupItemIndexType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L82)

```cpp
StreamGroupItemIndexType = 0xF64A
```

### API-f3eb81a1a82f · ndn_service_framework::stream_tlv::(anonymous)::StreamPredictedSourceItemsType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L83)

```cpp
StreamPredictedSourceItemsType = 0xF64B
```

### API-602cf936a750 · ndn_service_framework::stream_tlv::(anonymous)::StreamPredictedRepairItemsType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L84)

```cpp
StreamPredictedRepairItemsType = 0xF64C
```

### API-4333278bfc9a · ndn_service_framework::stream_tlv::(anonymous)::LiveStreamSampleEnvelopeType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L85)

```cpp
LiveStreamSampleEnvelopeType = 0xF64D
```

### API-d8923528bff8 · ndn_service_framework::stream_tlv::(anonymous)::StreamActualSourceItemsType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L86)

```cpp
StreamActualSourceItemsType = 0xF64E
```

### API-d28b4225b1c6 · ndn_service_framework::stream_tlv::(anonymous)::StreamItemKindType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L87)

```cpp
StreamItemKindType = 0xF64F
```

### API-b0ad68c9944c · ndn_service_framework::stream_tlv::(anonymous)::LiveStreamFecSchemeType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L88)

```cpp
LiveStreamFecSchemeType = 0xF650
```

### API-41e1fd0110d1 · ndn_service_framework::stream_tlv::(anonymous)::LiveStreamFecRecoveryCapacityType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L89)

```cpp
LiveStreamFecRecoveryCapacityType = 0xF651
```

### API-1466b715aeee · ndn_service_framework::stream_tlv::(anonymous)::LiveStreamFecRepairIndexType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L90)

```cpp
LiveStreamFecRepairIndexType = 0xF652
```

### API-0d39dba61a8f · ndn_service_framework::stream_tlv::(anonymous)::PredictiveStreamGroupCommitType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L91)

```cpp
PredictiveStreamGroupCommitType = 0xF653
```

### API-bbe382d7c181 · ndn_service_framework::stream_tlv::(anonymous)::PredictiveStreamFrontierType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L92)

```cpp
PredictiveStreamFrontierType = 0xF654
```

### API-d3f33ec47244 · ndn_service_framework::stream_tlv::(anonymous)::PredictiveStreamSourceType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L93)

```cpp
PredictiveStreamSourceType = 0xF655
```

### API-4a9178153bf0 · ndn_service_framework::stream_tlv::(anonymous)::PredictiveStreamSourceWireLengthType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L94)

```cpp
PredictiveStreamSourceWireLengthType = 0xF656
```

### API-99397afd6d49 · ndn_service_framework::stream_tlv::(anonymous)::PredictiveStreamSourceWireDigestType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L95)

```cpp
PredictiveStreamSourceWireDigestType = 0xF657
```

### API-7d0536ba5261 · ndn_service_framework::stream_tlv::(anonymous)::PredictiveStreamRepairNameType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L96)

```cpp
PredictiveStreamRepairNameType = 0xF658
```

### API-d422a705d4fb · ndn_service_framework::stream_tlv::(anonymous)::PredictiveStreamLatestCommittedGroupType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L97)

```cpp
PredictiveStreamLatestCommittedGroupType = 0xF659
```

### API-cde19eb45b5f · ndn_service_framework::stream_tlv::(anonymous)::PredictiveStreamGroupCommitNameType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L98)

```cpp
PredictiveStreamGroupCommitNameType = 0xF65A
```

### API-692e2cf37bf2 · ndn_service_framework::stream_tlv::(anonymous)::PredictiveStreamInitialSampleIdType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L99)

```cpp
PredictiveStreamInitialSampleIdType = 0xF65B
```

### API-5a861869b478 · ndn_service_framework::stream_tlv::(anonymous)::PredictiveStreamOldestRetainedSampleIdType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L100)

```cpp
PredictiveStreamOldestRetainedSampleIdType = 0xF65C
```

### API-4eb5444af9c4 · ndn_service_framework::stream_tlv::(anonymous)::PredictiveStreamLatestProducedSampleIdType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L101)

```cpp
PredictiveStreamLatestProducedSampleIdType = 0xF65D
```

### API-edb6aec4fdf5 · ndn_service_framework::stream_tlv::(anonymous)::PredictiveStreamNextExpectedSampleIdType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L102)

```cpp
PredictiveStreamNextExpectedSampleIdType = 0xF65E
```

### API-9dead2deb649 · ndn_service_framework::stream_tlv::(anonymous)::PredictiveStreamGroupFirstCursorType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L103)

```cpp
PredictiveStreamGroupFirstCursorType = 0xF65F
```

### API-488b627b6b22 · ndn_service_framework::stream_tlv::(anonymous)::PredictiveStreamGroupLastCursorType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L104)

```cpp
PredictiveStreamGroupLastCursorType = 0xF660
```

### API-87802afa5d12 · ndn_service_framework::streamNowMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L108)

```cpp
uint64_t streamNowMs();
```

原始接口说明：

```text
// namespace stream_tlv
```

### API-9bb6e701bbd3 · ndn_service_framework::StreamFecInfo

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L110)

```cpp
struct StreamFecInfo
```

### API-3bc9e12fe30d · ndn_service_framework::StreamFecInfo::scheme

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L112)

```cpp
std::string scheme;
```

### API-11c0169adf09 · ndn_service_framework::StreamFecInfo::dataShards

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L113)

```cpp
uint64_t dataShards = 0;
```

### API-28d25fda3629 · ndn_service_framework::StreamFecInfo::parityShards

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L114)

```cpp
uint64_t parityShards = 0;
```

### API-a7381ecb8b91 · ndn_service_framework::StreamFecInfo::symbolIndex

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L115)

```cpp
uint64_t symbolIndex = 0;
```

### API-28d7abd20f19 · ndn_service_framework::StreamFecInfo::symbolCount

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L116)

```cpp
uint64_t symbolCount = 0;
```

### API-bf9e10cbfdf7 · ndn_service_framework::StreamFecInfo::dataLengths

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L117)

```cpp
std::vector<uint64_t> dataLengths;
```

### API-5bd885db8bb5 · ndn_service_framework::StreamFecInfo::sourceBlockId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L118)

```cpp
std::string sourceBlockId;
```

### API-856f47c12373 · ndn_service_framework::StreamFecInfo::repairSymbol

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L119)

```cpp
bool repairSymbol = false;
```

### API-fb707bf42328 · ndn_service_framework::StreamFecInfo::metadata

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L120)

```cpp
std::map<std::string, std::string> metadata;
```

### API-b4ddc0813d63 · ndn_service_framework::StreamFecInfo::enabled

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L122)

```cpp
bool enabled() const;
```

### API-bf229478b21f · ndn_service_framework::StreamFecInfo::wireEncode

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L123)

```cpp
ndn::Block wireEncode() const;
```

### API-c9cc2bdb896a · ndn_service_framework::StreamFecInfo::wireDecode

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L124)

```cpp
bool wireDecode(const ndn::Block& block);
```

### API-032aa1b2e51d · ndn_service_framework::StreamInfo

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L140)

```cpp
struct StreamInfo
```

### API-24678adb9952 · ndn_service_framework::StreamInfo::streamId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L142)

```cpp
std::string streamId;
```

### API-e1c65d57eca1 · ndn_service_framework::StreamInfo::sessionEpoch

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L143)

```cpp
uint64_t sessionEpoch = 0;
```

### API-a5ffb0ff9dfe · ndn_service_framework::StreamInfo::streamPrefix

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L144)

```cpp
ndn::Name streamPrefix;
```

### API-6ebc74f061da · ndn_service_framework::StreamInfo::nextSeq

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L145)

```cpp
uint64_t nextSeq = 0;
```

### API-ddd0bafac7df · ndn_service_framework::StreamInfo::contentType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L146)

```cpp
std::string contentType = "application/octet-stream";
```

### API-8cf01252433a · ndn_service_framework::StreamInfo::freshnessMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L147)

```cpp
uint64_t freshnessMs = 80;
```

### API-0ea6706d43c0 · ndn_service_framework::StreamInfo::maxPayloadBytes

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L148)

```cpp
uint64_t maxPayloadBytes = 3600;
```

### API-31aca37484de · ndn_service_framework::StreamInfo::window

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L149)

```cpp
uint64_t window = 32;
```

### API-04ab67988dae · ndn_service_framework::StreamInfo::lookahead

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L150)

```cpp
uint64_t lookahead = 8;
```

### API-04122e6039d3 · ndn_service_framework::StreamInfo::interestLifetimeMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L151)

```cpp
uint64_t interestLifetimeMs = 500;
```

### API-b6ee28fc6db1 · ndn_service_framework::StreamInfo::missingTimeoutMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L152)

```cpp
uint64_t missingTimeoutMs = 300;
```

### API-7bddda523639 · ndn_service_framework::StreamInfo::reliability

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L153)

```cpp
std::string reliability = "best-effort";
```

### API-a7d93c082e1e · ndn_service_framework::StreamInfo::createdMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L154)

```cpp
uint64_t createdMs = 0;
```

### API-0cbe2581bbc2 · ndn_service_framework::StreamInfo::metadata

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L155)

```cpp
std::map<std::string, std::string> metadata;
```

### API-994580296635 · ndn_service_framework::StreamInfo::chunkName

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L157)

```cpp
ndn::Name chunkName(uint64_t seq) const;
```

### API-e41c2bc630c4 · ndn_service_framework::StreamInfo::wireEncode

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L158)

```cpp
ndn::Block wireEncode() const;
```

### API-bf817962c934 · ndn_service_framework::StreamInfo::wireDecode

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L159)

```cpp
bool wireDecode(const ndn::Block& block);
```

### API-306194754069 · ndn_service_framework::StreamChunk

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L170)

```cpp
struct StreamChunk
```

### API-d946f16459a9 · ndn_service_framework::StreamChunk::streamId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L172)

```cpp
std::string streamId;
```

### API-9748ddafd3be · ndn_service_framework::StreamChunk::sessionEpoch

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L173)

```cpp
uint64_t sessionEpoch = 0;
```

### API-009fd3f53976 · ndn_service_framework::StreamChunk::seq

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L174)

```cpp
uint64_t seq = 0;
```

### API-e672fc88bd59 · ndn_service_framework::StreamChunk::payload

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L175)

```cpp
std::vector<uint8_t> payload;
```

### API-f82c1dc9b597 · ndn_service_framework::StreamChunk::contentType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L176)

```cpp
std::string contentType = "application/octet-stream";
```

### API-dd6da2067b1a · ndn_service_framework::StreamChunk::captureMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L177)

```cpp
uint64_t captureMs = 0;
```

### API-73a8347b8a51 · ndn_service_framework::StreamChunk::arrivalMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L178)

```cpp
uint64_t arrivalMs = 0;
```

### API-7b701cb6a3bb · ndn_service_framework::StreamChunk::deadlineMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L179)

```cpp
uint64_t deadlineMs = 0;
```

### API-91f5ca2b013b · ndn_service_framework::StreamChunk::keyChunk

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L180)

```cpp
bool keyChunk = false;
```

### API-f62604eb1ec3 · ndn_service_framework::StreamChunk::frameId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L181)

```cpp
uint64_t frameId = 0;
```

### API-ba7a27c647c8 · ndn_service_framework::StreamChunk::frameFirstSeq

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L182)

```cpp
uint64_t frameFirstSeq = 0;
```

### API-5367a0f842be · ndn_service_framework::StreamChunk::frameLastSeq

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L183)

```cpp
uint64_t frameLastSeq = 0;
```

### API-ae2946830cfc · ndn_service_framework::StreamChunk::segmentIndex

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L184)

```cpp
uint64_t segmentIndex = 0;
```

### API-4f280141f257 · ndn_service_framework::StreamChunk::segmentCount

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L185)

```cpp
uint64_t segmentCount = 1;
```

### API-f0ade59d0308 · ndn_service_framework::StreamChunk::fec

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L186)

```cpp
std::optional<StreamFecInfo> fec;
```

### API-0a1ef71c89cd · ndn_service_framework::StreamChunk::metadata

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L187)

```cpp
std::map<std::string, std::string> metadata;
```

### API-02845ccf3072 · ndn_service_framework::StreamChunk::wireEncode

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L189)

```cpp
ndn::Block wireEncode() const;
```

### API-85294aeed48e · ndn_service_framework::StreamChunk::wireDecode

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L190)

```cpp
bool wireDecode(const ndn::Block& block);
```

### API-1e84c0067897 · ndn_service_framework::StreamCursor

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L193)

```cpp
using StreamCursor = uint64_t;
```

### API-e24931fe5ee7 · ndn_service_framework::StreamContentDigest

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L194)

```cpp
using StreamContentDigest = std::array<uint8_t, 32>;
```

### API-e9be17eecbc4 · ndn_service_framework::computeStreamContentDigest

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L196)

```cpp
StreamContentDigest
computeStreamContentDigest(ndn::span<const uint8_t> bytes);
```

### API-1af137bac550 · ndn_service_framework::STREAM_NAME_MAP_CONTRACT_VERSION_V1 = 1

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L199)

```cpp
inline constexpr uint64_t STREAM_NAME_MAP_CONTRACT_VERSION_V1 = 1;
```

### API-0c6ae761d012 · ndn_service_framework::STREAM_NAME_MAP_CONTRACT_VERSION_V2 = 2

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L200)

```cpp
inline constexpr uint64_t STREAM_NAME_MAP_CONTRACT_VERSION_V2 = 2;
```

### API-734890a4a096 · ndn_service_framework::STREAM_NAME_MAP_CONTRACT_VERSION =   STREAM_NAME_MAP_CONTRACT_VERSION_V1

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L201)

```cpp
inline constexpr uint64_t STREAM_NAME_MAP_CONTRACT_VERSION =
  STREAM_NAME_MAP_CONTRACT_VERSION_V1;
```

### API-df07e2aa9dda · ndn_service_framework::STREAM_NAME_MAP_MAX_BLOCK_CAPACITY = 4096

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L203)

```cpp
inline constexpr uint64_t STREAM_NAME_MAP_MAX_BLOCK_CAPACITY = 4096;
```

### API-24bfb3e79d27 · ndn_service_framework::STREAM_NAME_MAP_MAX_RESOLVER_BLOCKS = 4096

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L204)

```cpp
inline constexpr size_t STREAM_NAME_MAP_MAX_RESOLVER_BLOCKS = 4096;
```

### API-8c889596492e · ndn_service_framework::STREAM_NAME_MAP_MAX_REVERSE_ENTRIES = 1048576

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L205)

```cpp
inline constexpr size_t STREAM_NAME_MAP_MAX_REVERSE_ENTRIES = 1048576;
```

### API-c811abb5f5ec · ndn_service_framework::StreamNameMapEntry

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L214)

```cpp
struct StreamNameMapEntry
```

### API-d26f9a1e6a34 · ndn_service_framework::StreamNameMapEntry::originalName

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L216)

```cpp
ndn::Name originalName;
```

### API-dd605c93990f · ndn_service_framework::StreamNameMapEntry::tombstone

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L217)

```cpp
bool tombstone = false;
```

### API-048ae073fcac · ndn_service_framework::StreamNameMapEntry::groupId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L218)

```cpp
std::string groupId;
```

### API-87118b7d2c9b · ndn_service_framework::StreamNameMapEntry::sampleClass

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L219)

```cpp
std::string sampleClass;
```

### API-283b5e20f0b0 · ndn_service_framework::StreamNameMapEntry::groupItemIndex

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L220)

```cpp
uint64_t groupItemIndex = 0;
```

### API-0a9c7ee70b7b · ndn_service_framework::StreamNameMapEntry::predictedSourceItems

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L221)

```cpp
uint64_t predictedSourceItems = 0;
```

### API-d827b98b9b5d · ndn_service_framework::StreamNameMapEntry::predictedRepairItems

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L222)

```cpp
uint64_t predictedRepairItems = 0;
```

### API-69d2a0f42830 · ndn_service_framework::StreamNameMapEntry::fromName

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L224)

```cpp
static StreamNameMapEntry fromName(const ndn::Name& name);
```

### API-6b947dabb7fa · ndn_service_framework::StreamNameMapEntry::fromGroupedName

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L225)

```cpp
static StreamNameMapEntry fromGroupedName(const ndn::Name& name,
                                            std::string groupId,
                                            std::string sampleClass,
                                            uint64_t groupItemIndex,
                                            uint64_t predictedSourceItems,
                                            uint64_t predictedRepairItems);
```

### API-53bd37861541 · ndn_service_framework::StreamNameMapEntry::makeTombstone

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L231)

```cpp
static StreamNameMapEntry makeTombstone();
```

### API-854ecb6efb63 · ndn_service_framework::StreamNameMapEntry::isTombstone

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L232)

```cpp
bool isTombstone() const;
```

### API-6ff9826e856e · ndn_service_framework::StreamNameMapEntry::hasGroupBinding

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L233)

```cpp
bool hasGroupBinding() const;
```

### API-d61c59874177 · ndn_service_framework::StreamNameMapEntry::predictedGroupItems

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L234)

```cpp
uint64_t predictedGroupItems() const;
```

### API-d3cf67d52ebf · ndn_service_framework::StreamNameMapBlock

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L244)

```cpp
struct StreamNameMapBlock
```

### API-695d82ee2818 · ndn_service_framework::StreamNameMapBlock::contractVersion

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L246)

```cpp
uint64_t contractVersion = STREAM_NAME_MAP_CONTRACT_VERSION;
```

### API-47b757741715 · ndn_service_framework::StreamNameMapBlock::streamId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L247)

```cpp
std::string streamId;
```

### API-2805d764a730 · ndn_service_framework::StreamNameMapBlock::sessionEpoch

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L248)

```cpp
uint64_t sessionEpoch = 0;
```

### API-486529f149f3 · ndn_service_framework::StreamNameMapBlock::mappingVersion

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L249)

```cpp
uint64_t mappingVersion = 0;
```

### API-4f635b2d90da · ndn_service_framework::StreamNameMapBlock::blockNumber

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L250)

```cpp
uint64_t blockNumber = 0;
```

### API-b8add758ac17 · ndn_service_framework::StreamNameMapBlock::blockCapacity

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L251)

```cpp
uint64_t blockCapacity = 0;
```

### API-03e7f28bfa40 · ndn_service_framework::StreamNameMapBlock::firstCursor

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L252)

```cpp
StreamCursor firstCursor = 0;
```

### API-10c935c085cc · ndn_service_framework::StreamNameMapBlock::previousContentDigest

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L253)

```cpp
std::optional<StreamContentDigest> previousContentDigest;
```

### API-6da9fee969cb · ndn_service_framework::StreamNameMapBlock::entries

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L254)

```cpp
std::vector<StreamNameMapEntry> entries;
```

### API-f124b44a14b3 · ndn_service_framework::StreamNameMapBlock::validate

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L256)

```cpp
std::optional<std::string> validate() const;
```

### API-ae5b4a3c3804 · ndn_service_framework::StreamNameMapBlock::wireEncode

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L257)

```cpp
ndn::Block wireEncode() const;
```

### API-d19a7d8c20c3 · ndn_service_framework::StreamNameMapBlock::wireDecode

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L258)

```cpp
bool wireDecode(const ndn::Block& block);
```

### API-7d1cbb766202 · ndn_service_framework::StreamNameMapBlock::canonicalContent

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L259)

```cpp
ndn::Block canonicalContent() const;
```

### API-a245f1674d79 · ndn_service_framework::StreamNameMapBlock::contentDigest

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L260)

```cpp
StreamContentDigest contentDigest() const;
```

### API-afa3ca164493 · ndn_service_framework::StreamNameMapBlock::fitsSignedWireBudget

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L261)

```cpp
bool fitsSignedWireBudget(size_t signedEnvelopeOverhead,
                            size_t configuredWireCap) const;
```

### API-6418ad67e9ee · ndn_service_framework::StreamNameMapBlock::lastCursor

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L263)

```cpp
StreamCursor lastCursor() const;
```

### API-537055f78f15 · ndn_service_framework::makeStreamNameMapRoot

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L267)

```cpp
ndn::Name makeStreamNameMapRoot(const ndn::Name& provider,
                                const std::string& streamId);
```

原始接口说明：

```text
/** Base name before the typed mapping Version and SequenceNum components. */
```

### API-7c9b8fcacb7c · ndn_service_framework::makeStreamNameMapBlockName

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L271)

```cpp
ndn::Name makeStreamNameMapBlockName(const ndn::Name& mappingRoot,
                                     uint64_t mappingVersion,
                                     uint64_t blockNumber);
```

原始接口说明：

```text
/** Exact Mapping Data name: root / Version(mappingVersion) / SequenceNum(block). */
```

### API-1e6a72eb3dad · ndn_service_framework::StreamCursorFrontiers

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L275)

```cpp
struct StreamCursorFrontiers
```

### API-4f5d50a390a9 · ndn_service_framework::StreamCursorFrontiers::oldestRetained

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L277)

```cpp
StreamCursor oldestRetained = 0;
```

### API-8e67527be4db · ndn_service_framework::StreamCursorFrontiers::latestJoin

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L278)

```cpp
StreamCursor latestJoin = 0;
```

### API-97def79be8f5 · ndn_service_framework::StreamCursorFrontiers::latestProduced

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L279)

```cpp
StreamCursor latestProduced = 0;
```

### API-cc14e7130767 · ndn_service_framework::StreamCursorFrontiers::mappingCommittedThrough

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L280)

```cpp
StreamCursor mappingCommittedThrough = 0;
```

### API-2d4cec981c5f · ndn_service_framework::StreamCursorFrontiers::nextReserved

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L281)

```cpp
StreamCursor nextReserved = 0;
```

### API-2c42191d2928 · ndn_service_framework::StreamCursorFrontiers::validate

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L283)

```cpp
std::optional<std::string> validate(uint64_t blockCapacity,
                                      uint64_t checkpointBlock) const;
```

### API-791ecaa777ae · ndn_service_framework::StreamNameMapCheckpoint

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L287)

```cpp
struct StreamNameMapCheckpoint
```

### API-b1fa5a3c28d2 · ndn_service_framework::StreamNameMapCheckpoint::frontiers

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L289)

```cpp
StreamCursorFrontiers frontiers;
```

### API-fdb608f3be1a · ndn_service_framework::StreamNameMapCheckpoint::blockNumber

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L290)

```cpp
uint64_t blockNumber = 0;
```

### API-bdb830344323 · ndn_service_framework::StreamNameMapCheckpoint::contentDigest

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L291)

```cpp
StreamContentDigest contentDigest{};
```

### API-4e7297ca38f0 · ndn_service_framework::StreamNameMapResolverConfig

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L294)

```cpp
struct StreamNameMapResolverConfig
```

### API-11977b48c97a · ndn_service_framework::StreamNameMapResolverConfig::contractVersion

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L296)

```cpp
uint64_t contractVersion = STREAM_NAME_MAP_CONTRACT_VERSION;
```

### API-c285dc3e28cf · ndn_service_framework::StreamNameMapResolverConfig::streamId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L297)

```cpp
std::string streamId;
```

### API-57ffa744e43d · ndn_service_framework::StreamNameMapResolverConfig::sessionEpoch

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L298)

```cpp
uint64_t sessionEpoch = 0;
```

### API-f24d53cea0ee · ndn_service_framework::StreamNameMapResolverConfig::mappingVersion

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L299)

```cpp
uint64_t mappingVersion = 0;
```

### API-7236311a5370 · ndn_service_framework::StreamNameMapResolverConfig::blockCapacity

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L300)

```cpp
uint64_t blockCapacity = 0;
```

### API-d4f5094d3ebc · ndn_service_framework::StreamNameMapResolverConfig::expectedProvider

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L301)

```cpp
ndn::Name expectedProvider;
```

### API-ed8a2789b5b9 · ndn_service_framework::StreamNameMapResolverConfig::mappingRoot

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L302)

```cpp
ndn::Name mappingRoot;
```

### API-3ab353ab706d · ndn_service_framework::StreamNameMapResolverConfig::payloadPrefix

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L303)

```cpp
ndn::Name payloadPrefix;
```

### API-a2a0be0b5841 · ndn_service_framework::StreamNameMapResolverConfig::signedWireCap

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L304)

```cpp
size_t signedWireCap = ndn::MAX_NDN_PACKET_SIZE;
```

### API-b937813cdd44 · ndn_service_framework::StreamNameMapResolverConfig::maxVerifiedBlocks

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L305)

```cpp
size_t maxVerifiedBlocks = 32;
```

### API-af45cbf9ab28 · ndn_service_framework::StreamNameMapResolverConfig::maxQuarantineBlocks

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L306)

```cpp
size_t maxQuarantineBlocks = 8;
```

### API-96c407f9c0ef · ndn_service_framework::StreamNameMapResolverConfig::maxReverseEntries

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L307)

```cpp
size_t maxReverseEntries = 65536;
```

### API-fa5a8610472a · ndn_service_framework::StreamNameMapResolverConfig::maxOriginalNameWireBytes

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L308)

```cpp
size_t maxOriginalNameWireBytes = 4096;
```

### API-4da3a87157b9 · ndn_service_framework::VerifiedStreamNameMapData

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L318)

```cpp
struct VerifiedStreamNameMapData
```

### API-a4ea8113837c · ndn_service_framework::VerifiedStreamNameMapData::dataName

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L320)

```cpp
ndn::Name dataName;
```

### API-65021ffe9dd6 · ndn_service_framework::VerifiedStreamNameMapData::verifiedProvider

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L321)

```cpp
ndn::Name verifiedProvider;
```

### API-01c4780b0df5 · ndn_service_framework::VerifiedStreamNameMapData::contentType

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L322)

```cpp
uint32_t contentType = ndn::tlv::ContentType_Blob;
```

### API-015723f63e2f · ndn_service_framework::VerifiedStreamNameMapData::hasFinalBlock

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L323)

```cpp
bool hasFinalBlock = false;
```

### API-1fcc1a28bd2f · ndn_service_framework::VerifiedStreamNameMapData::signedWireSize

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L324)

```cpp
size_t signedWireSize = 0;
```

### API-be8e4b94d5fc · ndn_service_framework::VerifiedStreamNameMapData::content

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L325)

```cpp
ndn::Block content;
```

### API-18886da83a07 · ndn_service_framework::VerifiedStreamNameMapData::receivedMonotonicMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L326)

```cpp
uint64_t receivedMonotonicMs = 0;
```

### API-e74c88d27914 · ndn_service_framework::VerifiedStreamNameMapData::requiredBeforeMonotonicMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L327)

```cpp
uint64_t requiredBeforeMonotonicMs = 0;
```

### API-924ebf310d88 · ndn_service_framework::StreamNameMapAdmissionDisposition

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L330)

```cpp
enum class StreamNameMapAdmissionDisposition
```

### API-116ee0bb3486 · ndn_service_framework::StreamNameMapAdmissionDisposition::Admitted

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L332)

```cpp
Admitted
```

### API-bd0d22bf2fe8 · ndn_service_framework::StreamNameMapAdmissionDisposition::Duplicate

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L333)

```cpp
Duplicate
```

### API-52d8048de535 · ndn_service_framework::StreamNameMapAdmissionDisposition::Quarantined

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L334)

```cpp
Quarantined
```

### API-ced5fd3cabbe · ndn_service_framework::StreamNameMapAdmissionDisposition::Rejected

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L335)

```cpp
Rejected
```

### API-e16127e150f2 · ndn_service_framework::StreamNameMapAdmissionDisposition::FatalSession

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L336)

```cpp
FatalSession
```

### API-8f3cb63c083a · ndn_service_framework::toString

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L339)

```cpp
const char* toString(StreamNameMapAdmissionDisposition disposition);
```

### API-d042750c1c18 · ndn_service_framework::StreamNameMapTiming

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L341)

```cpp
enum class StreamNameMapTiming
```

### API-372d4bb8a697 · ndn_service_framework::StreamNameMapTiming::Unclassified

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L343)

```cpp
Unclassified
```

### API-c884f1484a79 · ndn_service_framework::StreamNameMapTiming::Ahead

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L344)

```cpp
Ahead
```

### API-3f192791bf50 · ndn_service_framework::StreamNameMapTiming::Late

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L345)

```cpp
Late
```

### API-1f6f412a8ea6 · ndn_service_framework::toString

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L348)

```cpp
const char* toString(StreamNameMapTiming timing);
```

### API-47e112ead7aa · ndn_service_framework::StreamNameMapAdmissionResult

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L350)

```cpp
struct StreamNameMapAdmissionResult
```

### API-16649ff1a913 · ndn_service_framework::StreamNameMapAdmissionResult::disposition

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L352)

```cpp
StreamNameMapAdmissionDisposition disposition =
    StreamNameMapAdmissionDisposition::Rejected;
```

### API-66fa5952ddb2 · ndn_service_framework::StreamNameMapAdmissionResult::timing

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L354)

```cpp
StreamNameMapTiming timing = StreamNameMapTiming::Unclassified;
```

### API-25981148f39a · ndn_service_framework::StreamNameMapAdmissionResult::reason

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L355)

```cpp
std::string reason;
```

### API-92858d5392ef · ndn_service_framework::StreamNameMapAdmissionResult::stateChanged

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L356)

```cpp
bool stateChanged = false;
```

### API-8bab62e8969e · ndn_service_framework::StreamNameMapAdmissionResult::mappingCommittedThrough

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L357)

```cpp
StreamCursor mappingCommittedThrough = 0;
```

### API-d4b05ed3370a · ndn_service_framework::StreamNameMapAdmissionResult::accepted

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L359)

```cpp
bool accepted() const;
```

### API-e107a8fd6b90 · ndn_service_framework::StreamNameMapAdmissionResult::fatal

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L360)

```cpp
bool fatal() const;
```

### API-544b7bdb00d0 · ndn_service_framework::StreamNameMapResolution

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L363)

```cpp
struct StreamNameMapResolution
```

### API-1b8041497577 · ndn_service_framework::StreamNameMapResolution::cursor

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L365)

```cpp
StreamCursor cursor = 0;
```

### API-f4aafcf61d36 · ndn_service_framework::StreamNameMapResolution::originalName

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L366)

```cpp
ndn::Name originalName;
```

### API-59aa0cc1390b · ndn_service_framework::StreamNameMapResolution::tombstone

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L367)

```cpp
bool tombstone = false;
```

### API-1d0eadd9927b · ndn_service_framework::StreamNameMapResolution::terminalUnproduced

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L368)

```cpp
bool terminalUnproduced = false;
```

### API-817d7f165936 · ndn_service_framework::StreamNameMapResolution::timing

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L369)

```cpp
StreamNameMapTiming timing = StreamNameMapTiming::Unclassified;
```

### API-42b790fd6b57 · ndn_service_framework::StreamNameMapResolution::groupId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L370)

```cpp
std::string groupId;
```

### API-25e055d00d1c · ndn_service_framework::StreamNameMapResolution::sampleClass

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L371)

```cpp
std::string sampleClass;
```

### API-ef327f9d7847 · ndn_service_framework::StreamNameMapResolution::groupItemIndex

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L372)

```cpp
uint64_t groupItemIndex = 0;
```

### API-ccbaa3204d99 · ndn_service_framework::StreamNameMapResolution::predictedSourceItems

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L373)

```cpp
uint64_t predictedSourceItems = 0;
```

### API-9d96119ddedb · ndn_service_framework::StreamNameMapResolution::predictedRepairItems

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L374)

```cpp
uint64_t predictedRepairItems = 0;
```

### API-6de05b7428eb · ndn_service_framework::StreamNameMapResolution::schedulable

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L376)

```cpp
bool schedulable() const;
```

### API-af13910b17cf · ndn_service_framework::StreamNameMapResolution::hasGroupBinding

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L377)

```cpp
bool hasGroupBinding() const;
```

### API-ea8f94e0111b · ndn_service_framework::StreamNameMapResolution::predictedGroupItems

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L378)

```cpp
uint64_t predictedGroupItems() const;
```

### API-fe5df180e510 · ndn_service_framework::StreamNameResolverState

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L382)

```cpp
class StreamNameResolverState
```

### API-4756bb0c4cd1 · ndn_service_framework::StreamNameResolverState::reset

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L385)

```cpp
void reset(const StreamNameMapResolverConfig& config,
             const StreamNameMapCheckpoint& checkpoint);
```

### API-960f5d5bd225 · ndn_service_framework::StreamNameResolverState::admitVerifiedBlock

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L388)

```cpp
StreamNameMapAdmissionResult
  admitVerifiedBlock(const VerifiedStreamNameMapData& input);
```

### API-07de761c435a · ndn_service_framework::StreamNameResolverState::refreshCheckpoint

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L392)

```cpp
StreamNameMapAdmissionResult
  refreshCheckpoint(const StreamNameMapCheckpoint& checkpoint);
```

原始接口说明：

```text
/** Apply one application-verified descriptor/checkpoint snapshot atomically. */
```

### API-0533665f0218 · ndn_service_framework::StreamNameResolverState::lookup

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L395)

```cpp
std::optional<StreamNameMapResolution> lookup(StreamCursor cursor) const;
```

### API-6a3950575dcd · ndn_service_framework::StreamNameResolverState::resolve

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L396)

```cpp
std::optional<ndn::Name> resolve(StreamCursor cursor) const;
```

### API-7a2e6493b08a · ndn_service_framework::StreamNameResolverState::reverseResolve

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L397)

```cpp
std::optional<StreamCursor> reverseResolve(const ndn::Name& originalName) const;
```

### API-5180e6bf591b · ndn_service_framework::StreamNameResolverState::markTerminalUnproduced

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L398)

```cpp
bool markTerminalUnproduced(StreamCursor cursor);
```

### API-d1653c12ba6a · ndn_service_framework::StreamNameResolverState::evictLocalBlock

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L401)

```cpp
bool evictLocalBlock(uint64_t blockNumber);
```

原始接口说明：

```text
/** Local cache eviction never advances the Provider retention frontier. */
```

### API-b3d776a872a9 · ndn_service_framework::StreamNameResolverState::frontiers

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L403)

```cpp
StreamCursorFrontiers frontiers() const;
```

### API-d23954b6cdfd · ndn_service_framework::StreamNameResolverState::checkpoint

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L404)

```cpp
StreamNameMapCheckpoint checkpoint() const;
```

### API-41b8d6d439e8 · ndn_service_framework::StreamNameResolverState::faulted

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L405)

```cpp
bool faulted() const;
```

### API-e5f2d47ff543 · ndn_service_framework::StreamNameResolverState::verifiedBlockCount

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L406)

```cpp
size_t verifiedBlockCount() const;
```

### API-535fbaa70996 · ndn_service_framework::StreamNameResolverState::quarantinedBlockCount

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L407)

```cpp
size_t quarantinedBlockCount() const;
```

### API-e6355d90b5b0 · ndn_service_framework::StreamNameResolverState::bindingCount

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L408)

```cpp
size_t bindingCount() const;
```

### API-85e2bfbe4b21 · ndn_service_framework::StreamNameResolverState::diagnostics

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L409)

```cpp
std::map<std::string, uint64_t> diagnostics() const;
```

### API-c0194f1cd5c5 · ndn_service_framework::LiveStreamFecScheme

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L458)

```cpp
enum class LiveStreamFecScheme
```

### API-ed95a6ff6648 · ndn_service_framework::LiveStreamFecScheme::None

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L460)

```cpp
None
```

### API-4b62d2164713 · ndn_service_framework::LiveStreamFecScheme::XorOneRepair

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L461)

```cpp
XorOneRepair
```

### API-3e3b8440820c · ndn_service_framework::LiveStreamFecScheme::Gf256TwoRepair

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L462)

```cpp
Gf256TwoRepair
```

### API-4168c885ec19 · ndn_service_framework::toString

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L465)

```cpp
const char* toString(LiveStreamFecScheme scheme);
```

### API-2920b0b64257 · ndn_service_framework::LiveStreamFecOptions

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L468)

```cpp
struct LiveStreamFecOptions
```

### API-5c192bd7638b · ndn_service_framework::LiveStreamFecOptions::scheme

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L470)

```cpp
LiveStreamFecScheme scheme = LiveStreamFecScheme::None;
```

### API-afae42eebc26 · ndn_service_framework::LiveStreamFecOptions::maxSourceItems

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L471)

```cpp
size_t maxSourceItems = 0;
```

### API-745144e6cc4e · ndn_service_framework::LiveStreamFecOptions::maxSourceBytes

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L472)

```cpp
size_t maxSourceBytes = 0;
```

### API-acea2acffd9b · ndn_service_framework::LiveStreamFecOptions::recoveryBudgetMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L473)

```cpp
uint64_t recoveryBudgetMs = 0;
```

### API-876486f372ce · ndn_service_framework::LiveStreamFecOptions::repairSymbols

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L474)

```cpp
size_t repairSymbols = 0;
```

### API-7e04ebfd2fa7 · ndn_service_framework::LiveStreamFecOptions::none

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L476)

```cpp
static LiveStreamFecOptions none();
```

### API-b5931aa3e616 · ndn_service_framework::LiveStreamFecOptions::xorOneRepair

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L477)

```cpp
static LiveStreamFecOptions xorOneRepair(size_t maxSourceItems,
                                           size_t maxSourceBytes,
                                           uint64_t recoveryBudgetMs = 500);
```

### API-25c398447b0e · ndn_service_framework::LiveStreamFecOptions::gf256TwoRepair

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L480)

```cpp
static LiveStreamFecOptions gf256TwoRepair(size_t maxSourceItems,
                                             size_t maxSourceBytes,
                                             uint64_t recoveryBudgetMs = 500);
```

### API-08b0c63ce2d2 · ndn_service_framework::LiveStreamFecOptions::enabled

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L483)

```cpp
bool enabled() const;
```

### API-db0ae5497061 · ndn_service_framework::LiveStreamFecOptions::repairItemCount

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L484)

```cpp
size_t repairItemCount() const;
```

### API-7eb1130e0322 · ndn_service_framework::LiveStreamFecOptions::recoveryCapacity

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L485)

```cpp
size_t recoveryCapacity() const;
```

### API-432369f4937f · ndn_service_framework::LiveStreamFecOptions::validate

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L486)

```cpp
std::optional<std::string> validate() const;
```

### API-e464b62ef1ad · ndn_service_framework::SampleClassProfile

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L489)

```cpp
struct SampleClassProfile
```

### API-941606e631d4 · ndn_service_framework::SampleClassProfile::classId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L491)

```cpp
std::string classId;
```

### API-24fee49b260f · ndn_service_framework::SampleClassProfile::seedSourceItems

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L492)

```cpp
size_t seedSourceItems = 1;
```

### API-5a860b56a26b · ndn_service_framework::SampleClassProfile::hardMaxSourceItems

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L493)

```cpp
size_t hardMaxSourceItems = 64;
```

### API-594b392e2dc1 · ndn_service_framework::SampleClassProfile::historyCapacity

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L494)

```cpp
size_t historyCapacity = 32;
```

### API-c72d2191794e · ndn_service_framework::SampleClassProfile::safetyMarginItems

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L495)

```cpp
size_t safetyMarginItems = 1;
```

### API-018c792d6f4a · ndn_service_framework::SampleClassProfile::bounded

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L497)

```cpp
static SampleClassProfile bounded(std::string classId,
                                    size_t seedSourceItems,
                                    size_t hardMaxSourceItems,
                                    size_t historyCapacity = 32,
                                    size_t safetyMarginItems = 1);
```

### API-2eae207f35bf · ndn_service_framework::SampleClassProfile::validate

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L502)

```cpp
std::optional<std::string> validate() const;
```

### API-079717caab3b · ndn_service_framework::SampleClassPredictionStatus

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L505)

```cpp
struct SampleClassPredictionStatus
```

### API-f339111e701a · ndn_service_framework::SampleClassPredictionStatus::classId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L507)

```cpp
std::string classId;
```

### API-bb468acc39a5 · ndn_service_framework::SampleClassPredictionStatus::prediction

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L508)

```cpp
size_t prediction = 0;
```

### API-ad2ccd659284 · ndn_service_framework::SampleClassPredictionStatus::observations

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L509)

```cpp
size_t observations = 0;
```

### API-a8d3aa987f13 · ndn_service_framework::SampleClassPredictionStatus::underpredictions

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L510)

```cpp
uint64_t underpredictions = 0;
```

### API-4c9990ac435a · ndn_service_framework::SampleClassPredictionStatus::underpredictedItems

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L511)

```cpp
uint64_t underpredictedItems = 0;
```

### API-630387b22840 · ndn_service_framework::SampleClassPredictionStatus::overpredictions

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L512)

```cpp
uint64_t overpredictions = 0;
```

### API-2440754eb1af · ndn_service_framework::SampleClassPredictionStatus::overpredictedItems

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L513)

```cpp
uint64_t overpredictedItems = 0;
```

### API-a8d754150445 · ndn_service_framework::LiveStreamSamplePredictor

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L517)

```cpp
class LiveStreamSamplePredictor
```

### API-924156e2a460 · ndn_service_framework::LiveStreamSamplePredictor::LiveStreamSamplePredictor

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L520)

```cpp
explicit LiveStreamSamplePredictor(std::vector<SampleClassProfile> profiles = {});
```

### API-fa3fac13b264 · ndn_service_framework::LiveStreamSamplePredictor::reset

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L521)

```cpp
void reset(std::vector<SampleClassProfile> profiles);
```

### API-6ec52666ff28 · ndn_service_framework::LiveStreamSamplePredictor::predict

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L522)

```cpp
size_t predict(const std::string& classId) const;
```

### API-feab37f60c9b · ndn_service_framework::LiveStreamSamplePredictor::observe

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L523)

```cpp
bool observe(const std::string& classId, size_t actualSourceItems);
```

### API-8f5c5e782c66 · ndn_service_framework::LiveStreamSamplePredictor::status

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L524)

```cpp
std::optional<SampleClassPredictionStatus> status(const std::string& classId) const;
```

### API-55fce83bd3cf · ndn_service_framework::LiveStreamSamplePredictor::statuses

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L525)

```cpp
std::map<std::string, SampleClassPredictionStatus> statuses() const;
```

### API-3a723b32eff7 · ndn_service_framework::LiveStreamDefinition

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L541)

```cpp
struct LiveStreamDefinition
```

### API-4c9bc0a8e498 · ndn_service_framework::LiveStreamDefinition::contractVersion

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L543)

```cpp
uint64_t contractVersion = STREAM_NAME_MAP_CONTRACT_VERSION_V1;
```

### API-5fa798cdff96 · ndn_service_framework::LiveStreamDefinition::streamId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L544)

```cpp
std::string streamId;
```

### API-18afc5e81a63 · ndn_service_framework::LiveStreamDefinition::provider

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L545)

```cpp
ndn::Name provider;
```

### API-ff445b79d993 · ndn_service_framework::LiveStreamDefinition::semanticDataPrefix

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L546)

```cpp
ndn::Name semanticDataPrefix;
```

### API-dbf39108e892 · ndn_service_framework::LiveStreamDefinition::sessionEpoch

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L547)

```cpp
uint64_t sessionEpoch = 0;
```

### API-b17d948ae929 · ndn_service_framework::LiveStreamDefinition::mappingVersion

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L548)

```cpp
uint64_t mappingVersion = 0;
```

### API-35ed26078e86 · ndn_service_framework::LiveStreamDefinition::mappingBlockCapacity

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L549)

```cpp
size_t mappingBlockCapacity = 16;
```

### API-0688d6553aa4 · ndn_service_framework::LiveStreamDefinition::mappingAheadBlocks

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L550)

```cpp
size_t mappingAheadBlocks = 4;
```

### API-2e5a198e71a0 · ndn_service_framework::LiveStreamDefinition::retainedItems

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L551)

```cpp
size_t retainedItems = 600;
```

### API-6aa7ad01ebb2 · ndn_service_framework::LiveStreamDefinition::maxNameReservations

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L552)

```cpp
size_t maxNameReservations = 65536;
```

### API-99e5f14bac6d · ndn_service_framework::LiveStreamDefinition::maxPendingInterests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L553)

```cpp
size_t maxPendingInterests = 256;
```

### API-9f54e4c48ca9 · ndn_service_framework::LiveStreamDefinition::signedWireCap

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L554)

```cpp
size_t signedWireCap = ndn::MAX_NDN_PACKET_SIZE;
```

### API-f9946628a04d · ndn_service_framework::LiveStreamDefinition::samplePeriodMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L555)

```cpp
double samplePeriodMs = 0.0;
```

### API-a074594c0fea · ndn_service_framework::LiveStreamDefinition::sampleClasses

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L556)

```cpp
std::vector<SampleClassProfile> sampleClasses;
```

### API-64dffd04cf85 · ndn_service_framework::LiveStreamDefinition::fec

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L557)

```cpp
LiveStreamFecOptions fec = LiveStreamFecOptions::none();
```

### API-c80d3b506e16 · ndn_service_framework::LiveStreamDefinition::validate

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L559)

```cpp
std::optional<std::string> validate() const;
```

### API-f4563f181bd9 · ndn_service_framework::LiveStreamDefinition::mappingRoot

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L560)

```cpp
ndn::Name mappingRoot() const;
```

### API-f74613fcbe0f · ndn_service_framework::computeLiveStreamMappingLead

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L568)

```cpp
size_t
computeLiveStreamMappingLead(double rttMs, double productionPeriodMs,
                             double jitterMs, size_t minimumItems,
                             size_t maximumItems);
```

原始接口说明：

```text
/**
 * Convert measured path/production timing into a bounded names-only Mapping lead.
 * The returned item count covers one RTT, the observed jitter margin, and one
 * additional production interval. Media bytes are never part of this horizon.
 */
```

### API-cccee4a1f826 · ndn_service_framework::LiveStreamItemReservation

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L574)

```cpp
struct LiveStreamItemReservation
```

### API-438c4e32938b · ndn_service_framework::LiveStreamItemReservation::cursor

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L576)

```cpp
StreamCursor cursor = 0;
```

### API-06039156f777 · ndn_service_framework::LiveStreamItemReservation::originalName

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L577)

```cpp
ndn::Name originalName;
```

### API-cd402c887d54 · ndn_service_framework::LiveStreamItemReservation::sessionEpoch

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L578)

```cpp
uint64_t sessionEpoch = 0;
```

### API-e66e71c2231d · ndn_service_framework::LiveStreamItemReservation::mappingVersion

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L579)

```cpp
uint64_t mappingVersion = 0;
```

### API-ce8fe37d6de0 · ndn_service_framework::LiveStreamItemReservation::belongsTo

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L581)

```cpp
bool belongsTo(const LiveStreamDefinition& definition) const;
```

### API-d8826739b5fe · ndn_service_framework::LiveStreamGroupReservation

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L584)

```cpp
struct LiveStreamGroupReservation
```

### API-4a3ef9f3d1c5 · ndn_service_framework::LiveStreamGroupReservation::groupId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L586)

```cpp
std::string groupId;
```

### API-8150d9cb9603 · ndn_service_framework::LiveStreamGroupReservation::sources

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L587)

```cpp
std::vector<LiveStreamItemReservation> sources;
```

### API-170f705b7f24 · ndn_service_framework::LiveStreamGroupReservation::repairs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L588)

```cpp
std::vector<LiveStreamItemReservation> repairs;
```

### API-74bf357d49d6 · ndn_service_framework::LiveStreamGroupReservation::validate

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L590)

```cpp
std::optional<std::string> validate(const LiveStreamDefinition& definition) const;
```

### API-a105df7ef304 · ndn_service_framework::LiveStreamItemKind

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L593)

```cpp
enum class LiveStreamItemKind
```

### API-a0f9f01b9e16 · ndn_service_framework::LiveStreamItemKind::Source

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L595)

```cpp
Source
```

### API-dbf906d88c9e · ndn_service_framework::LiveStreamItemKind::Repair

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L596)

```cpp
Repair
```

### API-bb217c5ea17d · ndn_service_framework::LiveStreamSampleEnvelope

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L600)

```cpp
struct LiveStreamSampleEnvelope
```

### API-a8cf45edc09b · ndn_service_framework::LiveStreamSampleEnvelope::groupId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L602)

```cpp
std::string groupId;
```

### API-203ba8a599ea · ndn_service_framework::LiveStreamSampleEnvelope::sampleClass

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L603)

```cpp
std::string sampleClass;
```

### API-e8efe5dbc650 · ndn_service_framework::LiveStreamSampleEnvelope::groupItemIndex

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L604)

```cpp
uint64_t groupItemIndex = 0;
```

### API-e7197cb0062d · ndn_service_framework::LiveStreamSampleEnvelope::actualSourceItems

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L605)

```cpp
uint64_t actualSourceItems = 0;
```

### API-64d144315e68 · ndn_service_framework::LiveStreamSampleEnvelope::itemKind

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L606)

```cpp
LiveStreamItemKind itemKind = LiveStreamItemKind::Source;
```

### API-c410b3e85e35 · ndn_service_framework::LiveStreamSampleEnvelope::opaqueContent

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L607)

```cpp
std::vector<uint8_t> opaqueContent;
```

### API-5aa881db8c30 · ndn_service_framework::LiveStreamSampleEnvelope::validate

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L609)

```cpp
std::optional<std::string> validate() const;
```

### API-350e68e22653 · ndn_service_framework::LiveStreamSampleEnvelope::wireEncode

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L610)

```cpp
ndn::Block wireEncode() const;
```

### API-a78b40f88999 · ndn_service_framework::LiveStreamSampleEnvelope::wireDecode

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L611)

```cpp
bool wireDecode(const ndn::Block& block);
```

### API-af6115e7da4a · ndn_service_framework::LiveStreamSampleReservation

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L614)

```cpp
struct LiveStreamSampleReservation
```

### API-3054365f4c1a · ndn_service_framework::LiveStreamSampleReservation::sampleId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L616)

```cpp
uint64_t sampleId = 0;
```

### API-0e0b76dd32b1 · ndn_service_framework::LiveStreamSampleReservation::sampleClass

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L617)

```cpp
std::string sampleClass;
```

### API-e27fb978353e · ndn_service_framework::LiveStreamSampleReservation::predictedSourceItems

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L618)

```cpp
size_t predictedSourceItems = 0;
```

### API-447e39404c8b · ndn_service_framework::LiveStreamSampleReservation::group

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L619)

```cpp
LiveStreamGroupReservation group;
```

### API-d1ea686e7656 · ndn_service_framework::LiveStreamSampleReservation::validate

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L621)

```cpp
std::optional<std::string> validate(const LiveStreamDefinition& definition) const;
```

### API-c1d0e5c4b07a · ndn_service_framework::LiveStreamFecRepair

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L625)

```cpp
struct LiveStreamFecRepair
```

### API-3e7067958d3c · ndn_service_framework::LiveStreamFecRepair::scheme

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L627)

```cpp
LiveStreamFecScheme scheme = LiveStreamFecScheme::XorOneRepair;
```

### API-26817b4af3e6 · ndn_service_framework::LiveStreamFecRepair::recoveryCapacity

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L628)

```cpp
uint64_t recoveryCapacity = 1;
```

### API-bf42b2dd1e1f · ndn_service_framework::LiveStreamFecRepair::repairIndex

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L629)

```cpp
uint64_t repairIndex = 0;
```

### API-fdedf1d3298d · ndn_service_framework::LiveStreamFecRepair::streamId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L630)

```cpp
std::string streamId;
```

### API-8920c5e9943f · ndn_service_framework::LiveStreamFecRepair::sessionEpoch

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L631)

```cpp
uint64_t sessionEpoch = 0;
```

### API-c8ad954b34ad · ndn_service_framework::LiveStreamFecRepair::mappingVersion

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L632)

```cpp
uint64_t mappingVersion = 0;
```

### API-ebd259a3c28d · ndn_service_framework::LiveStreamFecRepair::groupId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L633)

```cpp
std::string groupId;
```

### API-7fffc2d19302 · ndn_service_framework::LiveStreamFecRepair::createdMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L634)

```cpp
uint64_t createdMs = 0;
```

### API-fee7f5ef30b4 · ndn_service_framework::LiveStreamFecRepair::expiresMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L635)

```cpp
uint64_t expiresMs = 0;
```

### API-76afc8e61790 · ndn_service_framework::LiveStreamFecRepair::sourceNames

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L636)

```cpp
std::vector<ndn::Name> sourceNames;
```

### API-dcc5d45721e5 · ndn_service_framework::LiveStreamFecRepair::sourceCursors

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L637)

```cpp
std::vector<StreamCursor> sourceCursors;
```

### API-72e394f4ee87 · ndn_service_framework::LiveStreamFecRepair::sourceLengths

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L638)

```cpp
std::vector<uint64_t> sourceLengths;
```

### API-e185383e6702 · ndn_service_framework::LiveStreamFecRepair::sourceDigests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L639)

```cpp
std::vector<StreamContentDigest> sourceDigests;
```

### API-5801b3456413 · ndn_service_framework::LiveStreamFecRepair::repairName

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L640)

```cpp
ndn::Name repairName;
```

### API-32ab12d56401 · ndn_service_framework::LiveStreamFecRepair::repairCursor

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L641)

```cpp
StreamCursor repairCursor = 0;
```

### API-b27addad2fd3 · ndn_service_framework::LiveStreamFecRepair::codedBytes

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L642)

```cpp
std::vector<uint8_t> codedBytes;
```

### API-616be7f89556 · ndn_service_framework::LiveStreamFecRepair::validate

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L644)

```cpp
std::string validate(const LiveStreamDefinition& definition) const;
```

### API-07fb184e8add · ndn_service_framework::LiveStreamFecRepair::wireEncode

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L645)

```cpp
ndn::Block wireEncode() const;
```

### API-3f67cb723008 · ndn_service_framework::LiveStreamFecRepair::wireDecode

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L646)

```cpp
bool wireDecode(const ndn::Block& block);
```

### API-b0aa5389c207 · ndn_service_framework::makeLiveStreamXorRepair

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L649)

```cpp
LiveStreamFecRepair
makeLiveStreamXorRepair(const LiveStreamDefinition& definition,
                        const std::string& groupId,
                        const std::vector<LiveStreamItemReservation>& sources,
                        const LiveStreamItemReservation& repair,
                        const std::vector<std::vector<uint8_t>>& opaqueSources,
                        uint64_t createdMs,
                        uint64_t expiresMs);
```

### API-b01e9755e398 · ndn_service_framework::makeLiveStreamRepairSymbols

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L658)

```cpp
std::vector<LiveStreamFecRepair>
makeLiveStreamRepairSymbols(
  const LiveStreamDefinition& definition,
  const std::string& groupId,
  const std::vector<LiveStreamItemReservation>& sources,
  const std::vector<LiveStreamItemReservation>& repairs,
  const std::vector<std::vector<uint8_t>>& opaqueSources,
  uint64_t createdMs,
  uint64_t expiresMs);
```

### API-e424d48c0522 · ndn_service_framework::recoverLiveStreamXorSource

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L668)

```cpp
std::optional<std::vector<uint8_t>>
recoverLiveStreamXorSource(
  const LiveStreamDefinition& definition,
  const LiveStreamFecRepair& repair,
  const std::vector<std::optional<std::vector<uint8_t>>>& opaqueSources,
  size_t missingIndex,
  uint64_t nowMs);
```

### API-35aa7388cd07 · ndn_service_framework::recoverLiveStreamSources

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L676)

```cpp
std::optional<std::vector<std::optional<std::vector<uint8_t>>>>
recoverLiveStreamSources(
  const LiveStreamDefinition& definition,
  const std::vector<LiveStreamFecRepair>& repairs,
  const std::vector<std::optional<std::vector<uint8_t>>>& opaqueSources,
  uint64_t nowMs);
```

### API-8f01cd0affb0 · ndn_service_framework::LiveStreamReadiness

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L683)

```cpp
struct LiveStreamReadiness
```

### API-db41d9435824 · ndn_service_framework::LiveStreamReadiness::measuredSamplePeriodMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L685)

```cpp
double measuredSamplePeriodMs = 0.0;
```

### API-aa4803a63df2 · ndn_service_framework::LiveStreamReadiness::safeJoinCursor

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L686)

```cpp
StreamCursor safeJoinCursor = 0;
```

### API-9fd5ef09e38f · ndn_service_framework::LiveStreamDescriptor

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L689)

```cpp
struct LiveStreamDescriptor
```

### API-68f917c6d5df · ndn_service_framework::LiveStreamDescriptor::definition

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L691)

```cpp
LiveStreamDefinition definition;
```

### API-bc67c2bf544a · ndn_service_framework::LiveStreamDescriptor::checkpoint

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L692)

```cpp
StreamNameMapCheckpoint checkpoint;
```

### API-0c3bda4cb732 · ndn_service_framework::LiveStreamDescriptor::measuredSamplePeriodMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L693)

```cpp
double measuredSamplePeriodMs = 0.0;
```

### API-02287af127c5 · ndn_service_framework::LiveStreamDescriptor::safeJoinCursor

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L694)

```cpp
StreamCursor safeJoinCursor = 0;
```

### API-87f692431a99 · ndn_service_framework::LiveStreamDescriptor::validate

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L696)

```cpp
std::optional<std::string> validate() const;
```

### API-44809c2f9fdd · ndn_service_framework::LiveStreamLifecycleState

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L699)

```cpp
enum class LiveStreamLifecycleState
```

### API-f4db824f7627 · ndn_service_framework::LiveStreamLifecycleState::Preparing

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L701)

```cpp
Preparing
```

### API-902d96b8d1df · ndn_service_framework::LiveStreamLifecycleState::Active

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L702)

```cpp
Active
```

### API-88ccd6764a31 · ndn_service_framework::LiveStreamLifecycleState::Stopped

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L703)

```cpp
Stopped
```

### API-68fb98e04b04 · ndn_service_framework::LiveStreamLifecycleState::Failed

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L704)

```cpp
Failed
```

### API-368dcf575361 · ndn_service_framework::LiveStreamItemProvenance

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L707)

```cpp
enum class LiveStreamItemProvenance
```

### API-a436b93bd69f · ndn_service_framework::LiveStreamItemProvenance::SignedData

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L709)

```cpp
SignedData
```

### API-89dd3d327643 · ndn_service_framework::LiveStreamItemProvenance::FecRecovered

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L710)

```cpp
FecRecovered
```

### API-2b801c07174d · ndn_service_framework::toString

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L713)

```cpp
const char* toString(LiveStreamLifecycleState state);
```

### API-0ccdce0a352f · ndn_service_framework::toString

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L714)

```cpp
const char* toString(LiveStreamItemProvenance provenance);
```

### API-62c043a4ec6f · ndn_service_framework::PredictiveStreamCheckpoint

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L718)

```cpp
struct PredictiveStreamCheckpoint
```

### API-292782965dad · ndn_service_framework::PredictiveStreamCheckpoint::initialSampleId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L720)

```cpp
uint64_t initialSampleId = 0;
```

### API-a5d28c2dbd2d · ndn_service_framework::PredictiveStreamCheckpoint::oldestRetainedSampleId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L721)

```cpp
uint64_t oldestRetainedSampleId = 0;
```

### API-e5a9f887dde5 · ndn_service_framework::PredictiveStreamCheckpoint::latestProducedSampleId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L722)

```cpp
uint64_t latestProducedSampleId = 0;
```

### API-6a38da5c3db8 · ndn_service_framework::PredictiveStreamCheckpoint::nextExpectedSampleId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L723)

```cpp
uint64_t nextExpectedSampleId = 0;
```

### API-bb63c5edc382 · ndn_service_framework::PredictiveStreamCheckpoint::validate

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L725)

```cpp
std::optional<std::string> validate() const;
```

### API-ccaf95dba028 · ndn_service_framework::makePredictiveFrontierName

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L728)

```cpp
ndn::Name
makePredictiveFrontierName(const ndn::Name& mappingRoot);
```

### API-427707061a56 · ndn_service_framework::makePredictiveDataName

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L731)

```cpp
ndn::Name
makePredictiveDataName(const ndn::Name& mappingRoot,
                       uint64_t mappingVersion,
                       uint64_t sequence);
```

### API-1b4653aa3bf3 · ndn_service_framework::makePredictiveDataName

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L736)

```cpp
ndn::Name
makePredictiveDataName(const LiveStreamDefinition& definition,
                       uint64_t sequence);
```

### API-0326275fefc0 · ndn_service_framework::makePredictiveGroupName

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L740)

```cpp
ndn::Name
makePredictiveGroupName(const LiveStreamDefinition& definition,
                        uint64_t groupId);
```

### API-d40f87ce33de · ndn_service_framework::makePredictiveRepairName

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L744)

```cpp
ndn::Name
makePredictiveRepairName(const LiveStreamDefinition& definition,
                         uint64_t groupId, uint64_t repairIndex);
```

### API-5636489f1620 · ndn_service_framework::PredictiveStreamGroupCommit

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L748)

```cpp
struct PredictiveStreamGroupCommit
```

### API-122b02b64e43 · ndn_service_framework::PredictiveStreamGroupCommit::contractVersion

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L750)

```cpp
uint64_t contractVersion = 1;
```

### API-4647c732ea68 · ndn_service_framework::PredictiveStreamGroupCommit::streamId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L751)

```cpp
std::string streamId;
```

### API-74478a93acfa · ndn_service_framework::PredictiveStreamGroupCommit::sessionEpoch

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L752)

```cpp
uint64_t sessionEpoch = 0;
```

### API-951b913c13a0 · ndn_service_framework::PredictiveStreamGroupCommit::mappingVersion

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L753)

```cpp
uint64_t mappingVersion = 0;
```

### API-09d985f2d085 · ndn_service_framework::PredictiveStreamGroupCommit::groupId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L754)

```cpp
uint64_t groupId = 0;
```

### API-de05d818918f · ndn_service_framework::PredictiveStreamGroupCommit::createdMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L755)

```cpp
uint64_t createdMs = 0;
```

### API-d381307e67fc · ndn_service_framework::PredictiveStreamGroupCommit::expiresMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L756)

```cpp
uint64_t expiresMs = 0;
```

### API-705e2495484c · ndn_service_framework::PredictiveStreamGroupCommit::sourceNames

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L757)

```cpp
std::vector<ndn::Name> sourceNames;
```

### API-6850940ffbb8 · ndn_service_framework::PredictiveStreamGroupCommit::sourceWireLengths

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L758)

```cpp
std::vector<uint64_t> sourceWireLengths;
```

### API-f28fcf907fdb · ndn_service_framework::PredictiveStreamGroupCommit::sourceWireDigests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L759)

```cpp
std::vector<std::array<uint8_t, 32>> sourceWireDigests;
```

### API-6f9ae68a7e1c · ndn_service_framework::PredictiveStreamGroupCommit::repairNames

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L760)

```cpp
std::vector<ndn::Name> repairNames;
```

### API-422e2747072c · ndn_service_framework::PredictiveStreamGroupCommit::recoveryCapacity

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L761)

```cpp
uint64_t recoveryCapacity = 0;
```

### API-dfde83455d68 · ndn_service_framework::PredictiveStreamGroupCommit::validate

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L763)

```cpp
std::optional<std::string>
  validate(const LiveStreamDefinition& definition) const;
```

### API-7c978e994808 · ndn_service_framework::PredictiveStreamGroupCommit::wireEncode

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L765)

```cpp
ndn::Block wireEncode() const;
```

### API-6fcaa42389ae · ndn_service_framework::PredictiveStreamGroupCommit::wireDecode

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L766)

```cpp
bool wireDecode(const ndn::Block& block);
```

### API-4148bcd7ea54 · ndn_service_framework::PredictiveStreamDescriptor

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L769)

```cpp
struct PredictiveStreamDescriptor
```

### API-0cdbc7778118 · ndn_service_framework::PredictiveStreamDescriptor::definition

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L771)

```cpp
LiveStreamDefinition definition;
```

### API-2aefde06728f · ndn_service_framework::PredictiveStreamDescriptor::checkpoint

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L772)

```cpp
PredictiveStreamCheckpoint checkpoint;
```

### API-83175a6ba275 · ndn_service_framework::PredictiveStreamDescriptor::frontierName

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L773)

```cpp
ndn::Name frontierName;
```

### API-20be81380bbc · ndn_service_framework::PredictiveStreamDescriptor::measuredSamplePeriodMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L774)

```cpp
double measuredSamplePeriodMs = 0.0;
```

### API-95745303b22f · ndn_service_framework::PredictiveStreamDescriptor::validate

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L776)

```cpp
std::optional<std::string> validate() const;
```

### API-a92dbdc5c470 · ndn_service_framework::PredictiveStreamDescriptor::isPredictive

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L777)

```cpp
bool isPredictive() const;
```

### API-fbacb1e1123d · ndn_service_framework::PredictiveStreamFrontier

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L780)

```cpp
struct PredictiveStreamFrontier
```

### API-453120ef1f27 · ndn_service_framework::PredictiveStreamFrontier::contractVersion

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L782)

```cpp
uint64_t contractVersion = 2;
```

### API-1041739d395e · ndn_service_framework::PredictiveStreamFrontier::streamId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L783)

```cpp
std::string streamId;
```

### API-2baf70e88f9d · ndn_service_framework::PredictiveStreamFrontier::sessionEpoch

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L784)

```cpp
uint64_t sessionEpoch = 0;
```

### API-aad5607f1e2a · ndn_service_framework::PredictiveStreamFrontier::mappingVersion

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L785)

```cpp
uint64_t mappingVersion = 0;
```

### API-cdf864508277 · ndn_service_framework::PredictiveStreamFrontier::checkpoint

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L786)

```cpp
PredictiveStreamCheckpoint checkpoint;
```

### API-418547e8ad66 · ndn_service_framework::PredictiveStreamFrontier::latestCommittedGroupId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L787)

```cpp
std::optional<uint64_t> latestCommittedGroupId;
```

### API-5e81fed907ab · ndn_service_framework::PredictiveStreamFrontier::retainedGroupCommitNames

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L788)

```cpp
std::vector<ndn::Name> retainedGroupCommitNames;
```

### API-b1c4b4aacbf8 · ndn_service_framework::PredictiveStreamFrontier::retainedGroupFirstCursors

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L789)

```cpp
std::vector<uint64_t> retainedGroupFirstCursors;
```

### API-a734fa216b57 · ndn_service_framework::PredictiveStreamFrontier::retainedGroupLastCursors

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L790)

```cpp
std::vector<uint64_t> retainedGroupLastCursors;
```

### API-80599d97c461 · ndn_service_framework::PredictiveStreamFrontier::validate

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L792)

```cpp
std::optional<std::string>
  validate(const LiveStreamDefinition& definition) const;
```

### API-899db0b1289e · ndn_service_framework::PredictiveStreamFrontier::wireEncode

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L794)

```cpp
ndn::Block wireEncode() const;
```

### API-e5836335e598 · ndn_service_framework::PredictiveStreamFrontier::wireDecode

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L795)

```cpp
bool wireDecode(const ndn::Block& block);
```

### API-0861dfc3b8ac · ndn_service_framework::VerifiedLiveStreamItem

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L798)

```cpp
struct VerifiedLiveStreamItem
```

### API-fb513fc81418 · ndn_service_framework::VerifiedLiveStreamItem::cursor

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L800)

```cpp
StreamCursor cursor = 0;
```

### API-99ab3543736e · ndn_service_framework::VerifiedLiveStreamItem::originalName

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L801)

```cpp
ndn::Name originalName;
```

### API-301614b0c661 · ndn_service_framework::VerifiedLiveStreamItem::verifiedProvider

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L802)

```cpp
ndn::Name verifiedProvider;
```

### API-383d93ed3451 · ndn_service_framework::VerifiedLiveStreamItem::content

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L803)

```cpp
std::vector<uint8_t> content;
```

### API-abd5aee1f724 · ndn_service_framework::VerifiedLiveStreamItem::provenance

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L804)

```cpp
LiveStreamItemProvenance provenance = LiveStreamItemProvenance::SignedData;
```

### API-ee7486257e9b · ndn_service_framework::VerifiedLiveStreamItem::receivedMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L805)

```cpp
uint64_t receivedMs = 0;
```

### API-245145080067 · ndn_service_framework::LiveStreamItemAdmission

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L808)

```cpp
struct LiveStreamItemAdmission
```

### API-2d614c1bb495 · ndn_service_framework::LiveStreamItemAdmission::accepted

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L810)

```cpp
bool accepted = false;
```

### API-631bfea047c2 · ndn_service_framework::LiveStreamItemAdmission::reason

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L811)

```cpp
std::string reason;
```

### API-3764aea8a6de · ndn_service_framework::LiveStreamItemAdmission::acceptItem

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L813)

```cpp
static LiveStreamItemAdmission acceptItem();
```

### API-c7f6b15696c9 · ndn_service_framework::LiveStreamItemAdmission::rejectItem

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L814)

```cpp
static LiveStreamItemAdmission rejectItem(std::string reason);
```

### API-311fec75d8e4 · ndn_service_framework::LiveStreamSampleObservation

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L817)

```cpp
struct LiveStreamSampleObservation
```

### API-75448dd865d1 · ndn_service_framework::LiveStreamSampleObservation::sampleId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L819)

```cpp
uint64_t sampleId = 0;
```

### API-b34d9875f490 · ndn_service_framework::LiveStreamSampleObservation::arrivalMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L820)

```cpp
uint64_t arrivalMs = 0;
```

### API-aa660ccc2b1a · ndn_service_framework::LiveStreamSampleObservation::retrievalDelayMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L821)

```cpp
double retrievalDelayMs = 0.0;
```

### API-35601aecc22f · ndn_service_framework::LiveStreamSampleObservation::itemCount

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L822)

```cpp
uint64_t itemCount = 1;
```

### API-ae443df86c49 · ndn_service_framework::StreamFetchDecision

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L825)

```cpp
struct StreamFetchDecision
```

### API-1c2c6d8276f4 · ndn_service_framework::StreamAdaptiveFetcherState

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L826)

```cpp
class StreamAdaptiveFetcherState
```

### API-ec41e8b38077 · ndn_service_framework::LiveStreamStatus

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L828)

```cpp
struct LiveStreamStatus
```

### API-f391e4682e04 · ndn_service_framework::LiveStreamStatus::state

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L830)

```cpp
LiveStreamLifecycleState state = LiveStreamLifecycleState::Preparing;
```

### API-62f9c423f61b · ndn_service_framework::LiveStreamStatus::frontiers

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L831)

```cpp
StreamCursorFrontiers frontiers;
```

### API-757382afd401 · ndn_service_framework::LiveStreamStatus::retainedItems

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L832)

```cpp
size_t retainedItems = 0;
```

### API-95f0ebd9180b · ndn_service_framework::LiveStreamStatus::pendingInterests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L833)

```cpp
size_t pendingInterests = 0;
```

### API-bdb19c70416d · ndn_service_framework::LiveStreamStatus::mappingBlocks

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L834)

```cpp
size_t mappingBlocks = 0;
```

### API-77188c6017be · ndn_service_framework::LiveStreamStatus::inFlight

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L835)

```cpp
size_t inFlight = 0;
```

### API-35385b448134 · ndn_service_framework::LiveStreamStatus::delivered

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L836)

```cpp
uint64_t delivered = 0;
```

### API-6d136790119c · ndn_service_framework::LiveStreamStatus::rejected

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L837)

```cpp
uint64_t rejected = 0;
```

### API-f55c68d51301 · ndn_service_framework::LiveStreamStatus::recovered

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L838)

```cpp
uint64_t recovered = 0;
```

### API-ca8cad680bac · ndn_service_framework::LiveStreamStatus::timeouts

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L839)

```cpp
uint64_t timeouts = 0;
```

### API-d73ecd830971 · ndn_service_framework::LiveStreamStatus::nacks

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L840)

```cpp
uint64_t nacks = 0;
```

### API-335303bedc35 · ndn_service_framework::LiveStreamStatus::retryAttempts

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L841)

```cpp
uint64_t retryAttempts = 0;
```

### API-8589b690a04f · ndn_service_framework::LiveStreamStatus::lateArrivals

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L842)

```cpp
uint64_t lateArrivals = 0;
```

### API-e4f2c799132b · ndn_service_framework::LiveStreamStatus::deadlineSkips

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L843)

```cpp
uint64_t deadlineSkips = 0;
```

### API-62adeea8b903 · ndn_service_framework::LiveStreamStatus::retryExhaustions

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L844)

```cpp
uint64_t retryExhaustions = 0;
```

### API-2e121436dccf · ndn_service_framework::LiveStreamStatus::mappingInterests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L845)

```cpp
uint64_t mappingInterests = 0;
```

### API-e5bb3907f62e · ndn_service_framework::LiveStreamStatus::mappingDataResponses

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L849)

```cpp
uint64_t mappingDataResponses = 0;
```

原始接口说明：

```text
// Validated Mapping Data responses, and the subset that advanced resolver
// state. Their ratio distinguishes useful Mapping information from duplicate
// control Data without inspecting application payloads.
```

### API-a75e831b6089 · ndn_service_framework::LiveStreamStatus::mappingNewDataResponses

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L850)

```cpp
uint64_t mappingNewDataResponses = 0;
```

### API-52318f46e831 · ndn_service_framework::LiveStreamStatus::payloadInterests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L851)

```cpp
uint64_t payloadInterests = 0;
```

### API-014fc30eb4ee · ndn_service_framework::LiveStreamStatus::initialPayloadInterests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L852)

```cpp
uint64_t initialPayloadInterests = 0;
```

### API-e4b5f44d406b · ndn_service_framework::LiveStreamStatus::retryPayloadInterests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L853)

```cpp
uint64_t retryPayloadInterests = 0;
```

### API-00903ef1f7cf · ndn_service_framework::LiveStreamStatus::payloadSourceInterests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L854)

```cpp
uint64_t payloadSourceInterests = 0;
```

### API-06fee76d9d3f · ndn_service_framework::LiveStreamStatus::initialPayloadSourceInterests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L855)

```cpp
uint64_t initialPayloadSourceInterests = 0;
```

### API-06d73c20c552 · ndn_service_framework::LiveStreamStatus::retryPayloadSourceInterests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L856)

```cpp
uint64_t retryPayloadSourceInterests = 0;
```

### API-d182a62155a1 · ndn_service_framework::LiveStreamStatus::payloadRepairInterests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L857)

```cpp
uint64_t payloadRepairInterests = 0;
```

### API-f059284c25a6 · ndn_service_framework::LiveStreamStatus::initialPayloadRepairInterests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L858)

```cpp
uint64_t initialPayloadRepairInterests = 0;
```

### API-705a5c0ddfd6 · ndn_service_framework::LiveStreamStatus::retryPayloadRepairInterests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L859)

```cpp
uint64_t retryPayloadRepairInterests = 0;
```

### API-ffab9c852f86 · ndn_service_framework::LiveStreamStatus::payloadUnclassifiedInterests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L860)

```cpp
uint64_t payloadUnclassifiedInterests = 0;
```

### API-9195faf2f6e1 · ndn_service_framework::LiveStreamStatus::payloadSourceDataAdmissions

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L866)

```cpp
uint64_t payloadSourceDataAdmissions = 0;
```

原始接口说明：

```text
// Generic Payload-Interest utility accounting. A valid source Data response
// becomes application-useful only after the APP admits it. A valid repair
// Data response remains protection-only unless a successful recovery
// consumes that exact repair symbol. Every other terminal attempt is
// nonproductive; an unresolved remainder is exposed instead of guessed.
```

### API-bdf5a0b4740d · ndn_service_framework::LiveStreamStatus::payloadRepairDataResponses

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L867)

```cpp
uint64_t payloadRepairDataResponses = 0;
```

### API-32af6c79c8fd · ndn_service_framework::LiveStreamStatus::payloadRepairDataConsumed

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L868)

```cpp
uint64_t payloadRepairDataConsumed = 0;
```

### API-ab7670b2d958 · ndn_service_framework::LiveStreamStatus::payloadApplicationUsefulInterests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L869)

```cpp
uint64_t payloadApplicationUsefulInterests = 0;
```

### API-59cba0eba093 · ndn_service_framework::LiveStreamStatus::payloadProtectionOnlyInterests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L870)

```cpp
uint64_t payloadProtectionOnlyInterests = 0;
```

### API-bdf27d529793 · ndn_service_framework::LiveStreamStatus::payloadNonproductiveInterests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L871)

```cpp
uint64_t payloadNonproductiveInterests = 0;
```

### API-c6c66ecf3313 · ndn_service_framework::LiveStreamStatus::payloadUnresolvedInterests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L872)

```cpp
uint64_t payloadUnresolvedInterests = 0;
```

### API-abad187b338b · ndn_service_framework::LiveStreamStatus::futurePayloadInterests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L876)

```cpp
uint64_t futurePayloadInterests = 0;
```

原始接口说明：

```text
// Consumer-side count of Interests beyond the immutable join checkpoint.
// Provider future-interest/future-hit counters are authoritative for whether
// an Interest actually arrived before payload materialization.
```

### API-9f418d2e3ea0 · ndn_service_framework::LiveStreamStatus::initialFuturePayloadInterests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L877)

```cpp
uint64_t initialFuturePayloadInterests = 0;
```

### API-0df5955b0924 · ndn_service_framework::LiveStreamStatus::retryFuturePayloadInterests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L878)

```cpp
uint64_t retryFuturePayloadInterests = 0;
```

### API-877c8754159f · ndn_service_framework::LiveStreamStatus::futureCursorHorizon

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L879)

```cpp
uint64_t futureCursorHorizon = 0;
```

### API-7d6af52f4e55 · ndn_service_framework::LiveStreamStatus::retrySuccesses

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L880)

```cpp
uint64_t retrySuccesses = 0;
```

### API-0024e6205b74 · ndn_service_framework::LiveStreamStatus::retrySuppressions

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L881)

```cpp
uint64_t retrySuppressions = 0;
```

### API-e529b4736f55 · ndn_service_framework::LiveStreamStatus::retrySuppressionReasons

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L882)

```cpp
std::map<std::string, uint64_t> retrySuppressionReasons;
```

### API-070edca492a1 · ndn_service_framework::LiveStreamStatus::declaredRecoveryCapacity

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L883)

```cpp
uint64_t declaredRecoveryCapacity = 0;
```

### API-234c61a5d698 · ndn_service_framework::LiveStreamStatus::recoveryEligibleSources

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L884)

```cpp
uint64_t recoveryEligibleSources = 0;
```

### API-ae3bd233d4fe · ndn_service_framework::LiveStreamStatus::terminalMissingSources

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L885)

```cpp
uint64_t terminalMissingSources = 0;
```

### API-4f2b6a2e1528 · ndn_service_framework::LiveStreamStatus::recoverableGroups

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L886)

```cpp
uint64_t recoverableGroups = 0;
```

### API-e6ecd1261015 · ndn_service_framework::LiveStreamStatus::recoveredGroups

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L887)

```cpp
uint64_t recoveredGroups = 0;
```

### API-b0b71041f89e · ndn_service_framework::LiveStreamStatus::recoveryAttempts

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L888)

```cpp
uint64_t recoveryAttempts = 0;
```

### API-d8be074d1481 · ndn_service_framework::LiveStreamStatus::recoveryExhaustions

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L889)

```cpp
uint64_t recoveryExhaustions = 0;
```

### API-b10aa55f69c0 · ndn_service_framework::LiveStreamStatus::recoveryControlInterests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L892)

```cpp
uint64_t recoveryControlInterests = 0;
```

原始接口说明：

```text
// Predictive repair metadata is a separate control class. It is not the
// Mapping-first name-discovery path and must not inflate mappingInterests.
```

### API-73f55c9700f8 · ndn_service_framework::LiveStreamStatus::recoveryFrontierInterests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L893)

```cpp
uint64_t recoveryFrontierInterests = 0;
```

### API-3c2cbd4a7d86 · ndn_service_framework::LiveStreamStatus::recoveryGroupInterests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L894)

```cpp
uint64_t recoveryGroupInterests = 0;
```

### API-f8a2d69cb84f · ndn_service_framework::LiveStreamStatus::recoveryCoalescedWaiters

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L895)

```cpp
uint64_t recoveryCoalescedWaiters = 0;
```

### API-2367326d5461 · ndn_service_framework::LiveStreamStatus::recoveryMetadataCacheHits

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L896)

```cpp
uint64_t recoveryMetadataCacheHits = 0;
```

### API-c74ed40f0db8 · ndn_service_framework::LiveStreamStatus::nextDeliverCursor

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L897)

```cpp
uint64_t nextDeliverCursor = 0;
```

### API-aea6648401bb · ndn_service_framework::LiveStreamStatus::readyQueueDepth

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L898)

```cpp
uint64_t readyQueueDepth = 0;
```

### API-30cd2c399e65 · ndn_service_framework::LiveStreamStatus::oldestReadyCursor

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L899)

```cpp
uint64_t oldestReadyCursor = 0;
```

### API-fce9fcf35f28 · ndn_service_framework::LiveStreamStatus::terminalGapQueueDepth

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L900)

```cpp
uint64_t terminalGapQueueDepth = 0;
```

### API-54cf1b1dd485 · ndn_service_framework::LiveStreamStatus::drainWakeCount

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L901)

```cpp
uint64_t drainWakeCount = 0;
```

### API-81aa2c31847d · ndn_service_framework::LiveStreamStatus::staleReadyDrops

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L902)

```cpp
uint64_t staleReadyDrops = 0;
```

### API-1f151a1f1557 · ndn_service_framework::LiveStreamStatus::terminalGapSuperseded

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L903)

```cpp
uint64_t terminalGapSuperseded = 0;
```

### API-3a7381f0c3b2 · ndn_service_framework::LiveStreamStatus::mappingBytes

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L904)

```cpp
uint64_t mappingBytes = 0;
```

### API-df3b00dde141 · ndn_service_framework::LiveStreamStatus::providerFutureInterests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L905)

```cpp
uint64_t providerFutureInterests = 0;
```

### API-aa5e8f3509ce · ndn_service_framework::LiveStreamStatus::providerFutureHits

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L906)

```cpp
uint64_t providerFutureHits = 0;
```

### API-a4d40ff5a249 · ndn_service_framework::LiveStreamStatus::providerInitialFutureInterests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L907)

```cpp
uint64_t providerInitialFutureInterests = 0;
```

### API-dac391f8f8b4 · ndn_service_framework::LiveStreamStatus::providerInitialFutureHits

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L908)

```cpp
uint64_t providerInitialFutureHits = 0;
```

### API-3c1ac8fdc22f · ndn_service_framework::LiveStreamStatus::providerRetryFutureInterests

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L909)

```cpp
uint64_t providerRetryFutureInterests = 0;
```

### API-9469cd99c657 · ndn_service_framework::LiveStreamStatus::providerRetryFutureHits

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L910)

```cpp
uint64_t providerRetryFutureHits = 0;
```

### API-011e0b7862d3 · ndn_service_framework::LiveStreamStatus::sampleClassPredictions

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L911)

```cpp
std::map<std::string, SampleClassPredictionStatus> sampleClassPredictions;
```

### API-98ebb83ab6fc · ndn_service_framework::LiveStreamStatus::reason

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L912)

```cpp
std::string reason;
```

### API-28c5b5836916 · ndn_service_framework::LiveStreamStatus::fetchDecision

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L913)

```cpp
std::shared_ptr<StreamFetchDecision> fetchDecision;
```

### API-50be405d5aac · ndn_service_framework::PublishedLiveStreamPacketKind

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L916)

```cpp
enum class PublishedLiveStreamPacketKind
```

### API-d4865def774f · ndn_service_framework::PublishedLiveStreamPacketKind::Mapping

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L918)

```cpp
Mapping
```

### API-09229363af17 · ndn_service_framework::PublishedLiveStreamPacketKind::Source

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L919)

```cpp
Source
```

### API-6c1504748fb6 · ndn_service_framework::PublishedLiveStreamPacketKind::Repair

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L920)

```cpp
Repair
```

### API-41bdd2cd8007 · ndn_service_framework::PublishedLiveStreamPacket

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L923)

```cpp
struct PublishedLiveStreamPacket
```

### API-ca318cfa38af · ndn_service_framework::PublishedLiveStreamPacket::kind

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L925)

```cpp
PublishedLiveStreamPacketKind kind = PublishedLiveStreamPacketKind::Source;
```

### API-897fb14ad2a4 · ndn_service_framework::PublishedLiveStreamPacket::streamId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L926)

```cpp
std::string streamId;
```

### API-194074226768 · ndn_service_framework::PublishedLiveStreamPacket::sessionEpoch

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L927)

```cpp
uint64_t sessionEpoch = 0;
```

### API-255fe0d4af11 · ndn_service_framework::PublishedLiveStreamPacket::mappingVersion

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L928)

```cpp
uint64_t mappingVersion = 0;
```

### API-935fe219cea6 · ndn_service_framework::PublishedLiveStreamPacket::cursor

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L929)

```cpp
std::optional<StreamCursor> cursor;
```

### API-dad6f50a164f · ndn_service_framework::PublishedLiveStreamPacket::dataName

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L930)

```cpp
ndn::Name dataName;
```

### API-4a641a7ef38b · ndn_service_framework::PublishedLiveStreamPacket::provider

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L931)

```cpp
ndn::Name provider;
```

### API-154e73ebd69b · ndn_service_framework::PublishedLiveStreamPacket::signedDataWire

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L932)

```cpp
ndn::Buffer signedDataWire;
```

### API-96057969afa2 · ndn_service_framework::PublishedLiveStreamPacket::wireDigest

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L933)

```cpp
StreamContentDigest wireDigest{};
```

### API-4f1024c21df9 · ndn_service_framework::PublishedLiveStreamPacket::materializedMonotonicUs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L934)

```cpp
uint64_t materializedMonotonicUs = 0;
```

### API-ce821c061e1a · ndn_service_framework::PublishedPacketFeedOptions

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L937)

```cpp
struct PublishedPacketFeedOptions
```

### API-cb23bd09a6af · ndn_service_framework::PublishedPacketFeedOptions::fromCursor

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L939)

```cpp
StreamCursor fromCursor = 0;
```

### API-94b4830c5db3 · ndn_service_framework::PublishedPacketFeedOptions::maxQueuedPackets

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L940)

```cpp
size_t maxQueuedPackets = 1024;
```

### API-1ebb093acd05 · ndn_service_framework::PublishedPacketFeedOptions::maxQueuedBytes

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L941)

```cpp
size_t maxQueuedBytes = 8 * 1024 * 1024;
```

### API-dc2301ed3822 · ndn_service_framework::PublishedPacketFeedStatus

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L944)

```cpp
struct PublishedPacketFeedStatus
```

### API-199e59ea7156 · ndn_service_framework::PublishedPacketFeedStatus::queuedPackets

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L946)

```cpp
size_t queuedPackets = 0;
```

### API-25dfc394ae0e · ndn_service_framework::PublishedPacketFeedStatus::queuedBytes

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L947)

```cpp
size_t queuedBytes = 0;
```

### API-87a57bb40c46 · ndn_service_framework::PublishedPacketFeedStatus::droppedPackets

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L948)

```cpp
uint64_t droppedPackets = 0;
```

### API-21d5af23f4c9 · ndn_service_framework::PublishedPacketFeedStatus::firstDroppedCursor

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L949)

```cpp
std::optional<StreamCursor> firstDroppedCursor;
```

### API-af0f711bed8b · ndn_service_framework::PublishedPacketFeedStatus::lastDroppedCursor

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L950)

```cpp
std::optional<StreamCursor> lastDroppedCursor;
```

### API-143250501196 · ndn_service_framework::PublishedPacketFeedStatus::closed

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L951)

```cpp
bool closed = false;
```

### API-8b68586817e8 · ndn_service_framework::PublishedPacketFeed

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L954)

```cpp
class PublishedPacketFeed
```

### API-80748e74a947 · ndn_service_framework::PublishedPacketFeed::takeAvailable

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L957)

```cpp
std::vector<PublishedLiveStreamPacket> takeAvailable(size_t maxItems);
```

### API-9fe91840ddec · ndn_service_framework::PublishedPacketFeed::status

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L958)

```cpp
PublishedPacketFeedStatus status() const;
```

### API-14ba3c837d97 · ndn_service_framework::PublishedPacketFeed::close

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L959)

```cpp
void close();
```

### API-4eacc5e9561f · ndn_service_framework::StoredSignedPacketProducer

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L981)

```cpp
class StoredSignedPacketProducer
```

### API-37ddbe121642 · ndn_service_framework::StoredSignedPacketProducer::StoredSignedPacketProducer

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L984)

```cpp
StoredSignedPacketProducer(ndn::Face& face, ndn::Name routePrefix,
                             const std::vector<ndn::Buffer>& signedPacketWires);
```

### API-797b1ae6057f · ndn_service_framework::StoredSignedPacketProducer::~StoredSignedPacketProducer

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L986)

```cpp
~StoredSignedPacketProducer();
```

### API-649aa3253514 · ndn_service_framework::StoredSignedPacketProducer::start

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L988)

```cpp
void start();
```

### API-75c5bc493bf7 · ndn_service_framework::StoredSignedPacketProducer::stop

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L989)

```cpp
void stop();
```

### API-a6233028235d · ndn_service_framework::StoredSignedPacketProducer::packetCount

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L990)

```cpp
size_t packetCount() const;
```

### API-9659b60dc16e · ndn_service_framework::LiveStreamStart

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1004)

```cpp
enum class LiveStreamStart
```

### API-45352d2cb246 · ndn_service_framework::LiveStreamStart::Beginning

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1006)

```cpp
Beginning
```

### API-46fdff9f1214 · ndn_service_framework::LiveStreamStart::Latest

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1007)

```cpp
Latest
```

### API-f6242d9f5bc1 · ndn_service_framework::LiveStreamPrefetchPolicy

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1010)

```cpp
enum class LiveStreamPrefetchPolicy
```

### API-8dafcd1eb6f6 · ndn_service_framework::LiveStreamPrefetchPolicy::MappedPressure

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1012)

```cpp
MappedPressure
```

### API-32c40ee3172e · ndn_service_framework::LiveStreamPrefetchPolicy::MappedLiveFutureOn

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1013)

```cpp
MappedLiveFutureOn
```

### API-c6c255c03472 · ndn_service_framework::LiveStreamPrefetchPolicy::MappedLiveFutureOff

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1014)

```cpp
MappedLiveFutureOff
```

### API-a5ac7a9580de · ndn_service_framework::LiveStreamPrefetchPolicy::AdaptiveSampleAtomic

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1015)

```cpp
AdaptiveSampleAtomic
```

### API-6f4b9d6699df · ndn_service_framework::toString

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1018)

```cpp
const char*
toString(LiveStreamPrefetchPolicy policy);
```

### API-976ea8f9e9b8 · ndn_service_framework::LiveStreamOpenOptions

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1021)

```cpp
struct LiveStreamOpenOptions
```

### API-a943bd2df129 · ndn_service_framework::LiveStreamOpenOptions::start

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1023)

```cpp
LiveStreamStart start = LiveStreamStart::Latest;
```

### API-e0fb31942970 · ndn_service_framework::LiveStreamOpenOptions::prefetchPolicy

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1024)

```cpp
LiveStreamPrefetchPolicy prefetchPolicy = LiveStreamPrefetchPolicy::MappedPressure;
```

### API-310e8860dbb2 · ndn_service_framework::LiveStreamOpenOptions::aggregateInterestLimit

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1025)

```cpp
size_t aggregateInterestLimit = 64;
```

### API-8525fd2a8f25 · ndn_service_framework::LiveStreamOpenOptions::enableFecRecovery

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1026)

```cpp
bool enableFecRecovery = false;
```

### API-37374a9d47bb · ndn_service_framework::LiveStreamOpenOptions::interestLifetimeMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1027)

```cpp
uint64_t interestLifetimeMs = 500;
```

### API-1645b963ee6e · ndn_service_framework::LiveStreamOpenOptions::onItem

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1028)

```cpp
std::function<LiveStreamItemAdmission(const VerifiedLiveStreamItem&)> onItem;
```

### API-a39d0468aead · ndn_service_framework::LiveStreamOpenOptions::onStatus

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1029)

```cpp
std::function<void(const LiveStreamStatus&)> onStatus;
```

### API-38689e9b52ac · ndn_service_framework::LiveStreamPublisher

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1033)

```cpp
class LiveStreamPublisher : public std::enable_shared_from_this<LiveStreamPublisher>
```

### API-3de8e520cdfc · ndn_service_framework::LiveStreamPublisher::LiveStreamPublisher

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1036)

```cpp
LiveStreamPublisher(LiveStreamDefinition definition,
                      ndn::Face& face,
                      ndn::KeyChain& keyChain,
                      ndn::security::SigningInfo signingInfo);
```

### API-81aa475567a9 · ndn_service_framework::LiveStreamPublisher::~LiveStreamPublisher

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1040)

```cpp
~LiveStreamPublisher();
```

### API-5ce09323025a · ndn_service_framework::LiveStreamPublisher::start

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1042)

```cpp
void start();
```

### API-6254bfb3ae53 · ndn_service_framework::LiveStreamPublisher::startPredictive

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1044)

```cpp
void startPredictive();
```

原始接口说明：

```text
/** Register the single predictable-name route used by the high-level API. */
```

### API-33911ea0d6bb · ndn_service_framework::LiveStreamPublisher::waitUntilReady

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1051)

```cpp
void waitUntilReady(std::chrono::milliseconds timeout);
```

原始接口说明：

```text
/**
   * Wait until both existing routes are registered.
   *
   * This lifecycle-only primitive must be called off the Face I/O thread.
   * It does not fetch Data or change stream algorithms.
   */
```

### API-c09c780c4a4d · ndn_service_framework::LiveStreamPublisher::activatePredictive

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1053)

```cpp
void activatePredictive(double measuredSamplePeriodMs);
```

原始接口说明：

```text
/** Activate after the predictive route is registered; no Mapping bootstrap. */
```

### API-5f3da1f988a6 · ndn_service_framework::LiveStreamPublisher::publishSignedData

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1058)

```cpp
bool publishSignedData(const std::shared_ptr<ndn::Data>& signedData);
```

原始接口说明：

```text
/**
   * Retain and serve the exact App-signed wire without wrapping or re-signing.
   * @return true when newly admitted; false for a byte-identical duplicate.
   */
```

### API-b6a69b9f7efa · ndn_service_framework::LiveStreamPublisher::commitPredictiveGroup

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1060)

```cpp
PredictiveStreamFrontier commitPredictiveGroup(
    uint64_t groupId,
    const std::vector<std::shared_ptr<ndn::Data>>& signedSources);
```

原始接口说明：

```text
/** Atomically publish Core-owned repair/group/frontier Data for one group. */
```

### API-c4863bf545a3 · ndn_service_framework::LiveStreamPublisher::predictiveFrontier

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1063)

```cpp
PredictiveStreamFrontier predictiveFrontier() const;
```

### API-15c051953cad · ndn_service_framework::LiveStreamPublisher::reserveAhead

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1064)

```cpp
LiveStreamItemReservation reserveAhead(const ndn::Name& originalName);
```

### API-4a0051dcbf90 · ndn_service_framework::LiveStreamPublisher::reserveAhead

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1065)

```cpp
std::vector<LiveStreamItemReservation>
  reserveAhead(const std::vector<ndn::Name>& originalNames);
```

### API-f177133dd1ec · ndn_service_framework::LiveStreamPublisher::reserveGroup

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1067)

```cpp
LiveStreamGroupReservation reserveGroup(const std::string& groupId,
                                           const std::vector<ndn::Name>& sourceNames,
                                           const std::vector<ndn::Name>& repairNames);
```

### API-25adf696a8ed · ndn_service_framework::LiveStreamPublisher::announceSample

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1070)

```cpp
LiveStreamSampleReservation announceSample(
    uint64_t sampleId,
    const std::string& sampleClass,
    const std::function<ndn::Name(size_t, LiveStreamItemKind)>& nameFactory);
```

### API-b7ea3e3e99c6 · ndn_service_framework::LiveStreamPublisher::prepareSampleExtent

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1075)

```cpp
std::vector<LiveStreamItemReservation>
  prepareSampleExtent(const LiveStreamSampleReservation& reservation,
                      size_t actualSourceItems);
```

原始接口说明：

```text
/** Resolve an authenticated APP-known real extent before name-bound protection. */
```

### API-d85153e9610c · ndn_service_framework::LiveStreamPublisher::publish

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1078)

```cpp
void publish(const LiveStreamItemReservation& reservation,
               const std::vector<uint8_t>& opaqueContent);
```

### API-e8c0a4a60f43 · ndn_service_framework::LiveStreamPublisher::publishGroup

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1080)

```cpp
void publishGroup(const LiveStreamGroupReservation& reservation,
                    const std::vector<std::vector<uint8_t>>& opaqueSources);
```

### API-9810154f5561 · ndn_service_framework::LiveStreamPublisher::publishSample

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1082)

```cpp
void publishSample(const LiveStreamSampleReservation& reservation,
                     const std::vector<std::vector<uint8_t>>& opaqueSources);
```

### API-bac5278607ab · ndn_service_framework::LiveStreamPublisher::activate

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1084)

```cpp
LiveStreamDescriptor activate(const LiveStreamReadiness& readiness);
```

### API-b435901c8922 · ndn_service_framework::LiveStreamPublisher::openPublishedPacketFeed

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1085)

```cpp
std::shared_ptr<PublishedPacketFeed>
  openPublishedPacketFeed(const PublishedPacketFeedOptions& options);
```

### API-13f3830170dc · ndn_service_framework::LiveStreamPublisher::status

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1087)

```cpp
LiveStreamStatus status() const;
```

### API-eb8ba02cfd5f · ndn_service_framework::LiveStreamPublisher::stop

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1088)

```cpp
void stop();
```

### API-bcb8c928e07e · ndn_service_framework::LiveStreamConsumerHandle

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1180)

```cpp
class LiveStreamConsumerHandle : public std::enable_shared_from_this<LiveStreamConsumerHandle>
```

### API-a3173e00b005 · ndn_service_framework::LiveStreamConsumerHandle::LiveStreamConsumerHandle

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1183)

```cpp
LiveStreamConsumerHandle(LiveStreamDescriptor descriptor,
                           LiveStreamOpenOptions options,
                           ndn::Face& face,
                           std::shared_ptr<MessageValidator> validator);
```

### API-1039850ee459 · ndn_service_framework::LiveStreamConsumerHandle::~LiveStreamConsumerHandle

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1187)

```cpp
~LiveStreamConsumerHandle();
```

### API-dea4c2ff11fd · ndn_service_framework::LiveStreamConsumerHandle::start

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1189)

```cpp
void start();
```

### API-110b34093f64 · ndn_service_framework::LiveStreamConsumerHandle::observeAcceptedSample

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1190)

```cpp
bool observeAcceptedSample(const LiveStreamSampleObservation& observation);
```

### API-dde88f94dac6 · ndn_service_framework::LiveStreamConsumerHandle::status

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1191)

```cpp
LiveStreamStatus status() const;
```

### API-90e88544eacf · ndn_service_framework::LiveStreamConsumerHandle::stop

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1192)

```cpp
void stop();
```

### API-6d7309d444f2 · ndn_service_framework::StreamMetrics

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1309)

```cpp
struct StreamMetrics
```

### API-6b96d39ed0b4 · ndn_service_framework::StreamMetrics::produced

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1311)

```cpp
uint64_t produced = 0;
```

### API-40f44b841eab · ndn_service_framework::StreamMetrics::evicted

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1312)

```cpp
uint64_t evicted = 0;
```

### API-c49c97985a29 · ndn_service_framework::StreamMetrics::received

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1313)

```cpp
uint64_t received = 0;
```

### API-c420fabfb048 · ndn_service_framework::StreamMetrics::emitted

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1314)

```cpp
uint64_t emitted = 0;
```

### API-4682ede84fb9 · ndn_service_framework::StreamMetrics::duplicates

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1315)

```cpp
uint64_t duplicates = 0;
```

### API-080e8f75f996 · ndn_service_framework::StreamMetrics::stale

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1316)

```cpp
uint64_t stale = 0;
```

### API-78beebd5d429 · ndn_service_framework::StreamMetrics::gaps

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1317)

```cpp
uint64_t gaps = 0;
```

### API-1f3907038df3 · ndn_service_framework::StreamMetrics::timeouts

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1318)

```cpp
uint64_t timeouts = 0;
```

### API-baca29165830 · ndn_service_framework::StreamMetrics::nacks

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1319)

```cpp
uint64_t nacks = 0;
```

### API-587f3ec04201 · ndn_service_framework::StreamMetrics::overflows

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1320)

```cpp
uint64_t overflows = 0;
```

### API-b4ba885acdba · ndn_service_framework::StreamMetrics::maxPending

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1321)

```cpp
uint64_t maxPending = 0;
```

### API-ee37592bd831 · ndn_service_framework::StreamMetrics::bytesProduced

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1322)

```cpp
uint64_t bytesProduced = 0;
```

### API-c06736664178 · ndn_service_framework::StreamMetrics::bytesReceived

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1323)

```cpp
uint64_t bytesReceived = 0;
```

### API-21bc0a1c36fd · ndn_service_framework::StreamProducerBuffer

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1326)

```cpp
class StreamProducerBuffer
```

### API-2d07cb0d0f26 · ndn_service_framework::StreamProducerBuffer::StreamProducerBuffer

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1329)

```cpp
explicit StreamProducerBuffer(size_t maxChunks = 600);
```

### API-3a8e31c35c35 · ndn_service_framework::StreamProducerBuffer::put

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1331)

```cpp
void put(const StreamChunk& chunk);
```

### API-2cba17d169d6 · ndn_service_framework::StreamProducerBuffer::get

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1332)

```cpp
std::optional<StreamChunk> get(uint64_t seq) const;
```

### API-979832f5515a · ndn_service_framework::StreamProducerBuffer::getEncoded

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1333)

```cpp
std::optional<ndn::Block> getEncoded(uint64_t seq) const;
```

### API-9b58a7aa3e49 · ndn_service_framework::StreamProducerBuffer::sequences

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1334)

```cpp
std::vector<uint64_t> sequences() const;
```

### API-c9a35c124480 · ndn_service_framework::StreamProducerBuffer::size

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1335)

```cpp
size_t size() const;
```

### API-b50f467b7241 · ndn_service_framework::StreamProducerBuffer::metrics

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1336)

```cpp
StreamMetrics metrics() const;
```

### API-56c59f65116e · ndn_service_framework::StreamConsumerReorderBuffer

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1346)

```cpp
class StreamConsumerReorderBuffer
```

### API-7e8aec196d78 · ndn_service_framework::StreamConsumerReorderBuffer::StreamConsumerReorderBuffer

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1349)

```cpp
StreamConsumerReorderBuffer(std::string streamId,
                              uint64_t sessionEpoch,
                              uint64_t nextSeq = 0,
                              size_t maxPending = 512,
                              size_t history = 1024);
```

### API-0f17755136f5 · ndn_service_framework::StreamConsumerReorderBuffer::reset

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1355)

```cpp
void reset(std::string streamId, uint64_t sessionEpoch, uint64_t nextSeq = 0);
```

### API-a2ad61b80780 · ndn_service_framework::StreamConsumerReorderBuffer::push

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1356)

```cpp
std::vector<StreamChunk> push(const StreamChunk& chunk);
```

### API-09e0da368f49 · ndn_service_framework::StreamConsumerReorderBuffer::missingSequences

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1357)

```cpp
std::vector<uint64_t> missingSequences(size_t limit = 32) const;
```

### API-e78d36b06623 · ndn_service_framework::StreamConsumerReorderBuffer::pendingSequences

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1358)

```cpp
std::vector<uint64_t> pendingSequences(size_t limit = 0) const;
```

### API-8dace6aa49f7 · ndn_service_framework::StreamConsumerReorderBuffer::drainReady

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1359)

```cpp
std::vector<StreamChunk> drainReady();
```

### API-781062af8977 · ndn_service_framework::StreamConsumerReorderBuffer::skipTo

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1360)

```cpp
void skipTo(uint64_t seq);
```

### API-bf0d51bac75c · ndn_service_framework::StreamConsumerReorderBuffer::nextSeq

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1361)

```cpp
uint64_t nextSeq() const;
```

### API-4408a300e156 · ndn_service_framework::StreamConsumerReorderBuffer::pendingCount

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1362)

```cpp
size_t pendingCount() const;
```

### API-a80c26cf83b8 · ndn_service_framework::StreamConsumerReorderBuffer::pendingBytes

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1363)

```cpp
size_t pendingBytes() const;
```

### API-b5dec8cd062f · ndn_service_framework::StreamConsumerReorderBuffer::metrics

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1364)

```cpp
StreamMetrics metrics() const;
```

### API-8550ad5f6c9f · ndn_service_framework::StreamPrefetchPhase

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1384)

```cpp
enum class StreamPrefetchPhase
```

### API-08f338a45b8d · ndn_service_framework::StreamPrefetchPhase::Inactive

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1386)

```cpp
Inactive
```

### API-a3ae790e666b · ndn_service_framework::StreamPrefetchPhase::Chasing

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1387)

```cpp
Chasing
```

### API-2406db93a487 · ndn_service_framework::StreamPrefetchPhase::Adjusting

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1388)

```cpp
Adjusting
```

### API-359a5852d996 · ndn_service_framework::StreamPrefetchPhase::Fetching

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1389)

```cpp
Fetching
```

### API-672c824e325e · ndn_service_framework::StreamPrefetchPhase::Recovering

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1390)

```cpp
Recovering
```

### API-56812e6a93b1 · ndn_service_framework::StreamPrefetchPhase::Stopped

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1391)

```cpp
Stopped
```

### API-a7f0cc0a78e6 · ndn_service_framework::toString

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1394)

```cpp
const char*
toString(StreamPrefetchPhase phase);
```

### API-ae443df86c49 · ndn_service_framework::StreamFetchDecision

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1397)

```cpp
struct StreamFetchDecision
```

### API-4398fda56f3c · ndn_service_framework::StreamFetchDecision::window

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1399)

```cpp
uint64_t window = 0;
```

### API-daa76018a2c1 · ndn_service_framework::StreamFetchDecision::lookahead

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1400)

```cpp
uint64_t lookahead = 0;
```

### API-ba6b4c307734 · ndn_service_framework::StreamFetchDecision::interestLifetimeMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1401)

```cpp
uint64_t interestLifetimeMs = 0;
```

### API-72c87d7d22ef · ndn_service_framework::StreamFetchDecision::missingTimeoutMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1402)

```cpp
uint64_t missingTimeoutMs = 0;
```

### API-a147d7d12757 · ndn_service_framework::StreamFetchDecision::sampleDemand

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1403)

```cpp
uint64_t sampleDemand = 0;
```

### API-7b43da4b4b84 · ndn_service_framework::StreamFetchDecision::packetDemand

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1404)

```cpp
uint64_t packetDemand = 0;
```

### API-a0ce3695718f · ndn_service_framework::StreamFetchDecision::holdMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1405)

```cpp
uint64_t holdMs = 0;
```

### API-c4c21041edcb · ndn_service_framework::StreamFetchDecision::recoveryCheckpointMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1406)

```cpp
uint64_t recoveryCheckpointMs = 0;
```

### API-ffff15cca1ce · ndn_service_framework::StreamFetchDecision::remainingRecoveryBudgetMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1407)

```cpp
uint64_t remainingRecoveryBudgetMs = 0;
```

### API-92a41f9edc4f · ndn_service_framework::StreamFetchDecision::mappingBeginBlock

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1408)

```cpp
uint64_t mappingBeginBlock = 0;
```

### API-1c3211f324f8 · ndn_service_framework::StreamFetchDecision::mappingEndBlock

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1409)

```cpp
uint64_t mappingEndBlock = 0;
```

### API-2bfec6e2b037 · ndn_service_framework::StreamFetchDecision::payloadBeginCursor

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1410)

```cpp
uint64_t payloadBeginCursor = 0;
```

### API-8918baca0544 · ndn_service_framework::StreamFetchDecision::payloadEndCursor

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1411)

```cpp
uint64_t payloadEndCursor = 0;
```

### API-7e56e33e05ad · ndn_service_framework::StreamFetchDecision::aggregateInFlightLimit

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1412)

```cpp
uint64_t aggregateInFlightLimit = 0;
```

### API-0a27ae81af6c · ndn_service_framework::StreamFetchDecision::mappingBudget

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1413)

```cpp
uint64_t mappingBudget = 0;
```

### API-66688b2f0dff · ndn_service_framework::StreamFetchDecision::payloadBudget

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1414)

```cpp
uint64_t payloadBudget = 0;
```

### API-a67d8e30993b · ndn_service_framework::StreamFetchDecision::retransmissionBudget

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1415)

```cpp
uint64_t retransmissionBudget = 0;
```

### API-b72526a9444b · ndn_service_framework::StreamFetchDecision::futureWaitCount

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1416)

```cpp
uint64_t futureWaitCount = 0;
```

### API-c2568da2d78b · ndn_service_framework::StreamFetchDecision::terminalUnproducedAdvice

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1417)

```cpp
uint64_t terminalUnproducedAdvice = 0;
```

### API-afaea4282e9c · ndn_service_framework::StreamFetchDecision::laterCursorAdvice

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1418)

```cpp
uint64_t laterCursorAdvice = 0;
```

### API-9681b39930b6 · ndn_service_framework::StreamFetchDecision::atomicExpansions

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1419)

```cpp
uint64_t atomicExpansions = 0;
```

### API-edb181dc3cbd · ndn_service_framework::StreamFetchDecision::atomicDeferrals

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1420)

```cpp
uint64_t atomicDeferrals = 0;
```

### API-2fefd0593ee9 · ndn_service_framework::StreamFetchDecision::pressure

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1421)

```cpp
double pressure = 0.0;
```

### API-7fb9d9ff018e · ndn_service_framework::StreamFetchDecision::liveEdgeConfidence

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1422)

```cpp
double liveEdgeConfidence = 0.0;
```

### API-531f5d5dda31 · ndn_service_framework::StreamFetchDecision::mappingReady

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1423)

```cpp
bool mappingReady = false;
```

### API-5e0bc919e8fe · ndn_service_framework::StreamFetchDecision::futureWait

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1424)

```cpp
bool futureWait = false;
```

### API-a36e0113ddbc · ndn_service_framework::StreamFetchDecision::congestionHold

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1425)

```cpp
bool congestionHold = false;
```

### API-8d2abf558fd7 · ndn_service_framework::StreamFetchDecision::retransmissionEligible

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1426)

```cpp
bool retransmissionEligible = false;
```

### API-9eb9b21dbef6 · ndn_service_framework::StreamFetchDecision::phase

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1427)

```cpp
StreamPrefetchPhase phase = StreamPrefetchPhase::Inactive;
```

### API-ac856af47086 · ndn_service_framework::StreamFetchDecision::policyMode

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1428)

```cpp
std::string policyMode = "pressure-only";
```

### API-7bd7d6a15fbc · ndn_service_framework::StreamFetchDecision::detectorProfile

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1429)

```cpp
std::string detectorProfile = "none";
```

### API-58f8a66e5009 · ndn_service_framework::StreamFetchDecision::mappingWaitReason

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1430)

```cpp
std::string mappingWaitReason = "inactive";
```

### API-4fb73646e5b9 · ndn_service_framework::StreamFetchDecision::capacityReason

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1431)

```cpp
std::string capacityReason;
```

### API-cee2abe50080 · ndn_service_framework::StreamFetchDecision::reason

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1432)

```cpp
std::string reason = "stable";
```

### API-ea4d2cb9a981 · ndn_service_framework::StreamHealthState

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1435)

```cpp
enum class StreamHealthState
```

### API-bfd785be6abb · ndn_service_framework::StreamHealthState::Active

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1437)

```cpp
Active
```

### API-82c8f38c043a · ndn_service_framework::StreamHealthState::Degraded

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1438)

```cpp
Degraded
```

### API-ebbfd855b305 · ndn_service_framework::StreamHealthState::Congested

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1439)

```cpp
Congested
```

### API-b888d3356a07 · ndn_service_framework::StreamHealthState::Stale

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1440)

```cpp
Stale
```

### API-0c3643897a33 · ndn_service_framework::StreamHealthState::Stopped

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1441)

```cpp
Stopped
```

### API-6b0c3c672f79 · ndn_service_framework::StreamHealth

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1444)

```cpp
struct StreamHealth
```

### API-8baebd9a9d5d · ndn_service_framework::StreamHealth::streamId

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1446)

```cpp
std::string streamId;
```

### API-dceeaf38383c · ndn_service_framework::StreamHealth::sessionEpoch

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1447)

```cpp
uint64_t sessionEpoch = 0;
```

### API-5f03a14cf98d · ndn_service_framework::StreamHealth::state

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1448)

```cpp
StreamHealthState state = StreamHealthState::Active;
```

### API-5393050d7311 · ndn_service_framework::StreamHealth::nextSeq

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1449)

```cpp
uint64_t nextSeq = 0;
```

### API-31f48ff9d4ef · ndn_service_framework::StreamHealth::lastChunkMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1450)

```cpp
uint64_t lastChunkMs = 0;
```

### API-2063d18c4a8c · ndn_service_framework::StreamHealth::updatedMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1451)

```cpp
uint64_t updatedMs = 0;
```

### API-7db5f2b30cc9 · ndn_service_framework::StreamHealth::metrics

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1452)

```cpp
StreamMetrics metrics;
```

### API-79b68ff385c3 · ndn_service_framework::StreamHealth::fetchDecision

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1453)

```cpp
StreamFetchDecision fetchDecision;
```

### API-f4d7f60afe58 · ndn_service_framework::StreamHealth::reason

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1454)

```cpp
std::string reason;
```

### API-87bdee51d9ad · ndn_service_framework::StreamHealth::metadata

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1455)

```cpp
std::map<std::string, std::string> metadata;
```

### API-70ca1b205086 · ndn_service_framework::StreamHealth::fromStream

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1457)

```cpp
static StreamHealth fromStream(const StreamInfo& info,
                                 const StreamMetrics& metrics,
                                 const std::optional<StreamFetchDecision>& fetchDecision = std::nullopt,
                                 uint64_t nextSeq = 0,
                                 uint64_t lastChunkMs = 0,
                                 bool stopped = false,
                                 uint64_t staleAfterMs = 3000,
                                 uint64_t nowMs = 0);
```

### API-b28773333fec · ndn_service_framework::toString

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1467)

```cpp
const char*
toString(StreamHealthState state);
```

### API-1c2c6d8276f4 · ndn_service_framework::StreamAdaptiveFetcherState

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1470)

```cpp
class StreamAdaptiveFetcherState
```

### API-d1e0bb260d49 · ndn_service_framework::StreamAdaptiveFetcherState::rttMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1473)

```cpp
double rttMs = 100.0;
```

### API-0ecdc2ff5299 · ndn_service_framework::StreamAdaptiveFetcherState::timeoutPressure

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1474)

```cpp
double timeoutPressure = 0.0;
```

### API-8450c48854f5 · ndn_service_framework::StreamAdaptiveFetcherState::nackPressure

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1475)

```cpp
double nackPressure = 0.0;
```

### API-3f63499ad80c · ndn_service_framework::StreamAdaptiveFetcherState::duplicatePressure

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1476)

```cpp
double duplicatePressure = 0.0;
```

### API-5e8b543aad07 · ndn_service_framework::StreamAdaptiveFetcherState::backlogPressure

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1477)

```cpp
double backlogPressure = 0.0;
```

### API-835b7602e8eb · ndn_service_framework::StreamAdaptiveFetcherState::minWindow

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1478)

```cpp
uint64_t minWindow = 4;
```

### API-0aebdf80649f · ndn_service_framework::StreamAdaptiveFetcherState::baseWindow

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1479)

```cpp
uint64_t baseWindow = 32;
```

### API-5b6c537ab726 · ndn_service_framework::StreamAdaptiveFetcherState::maxWindow

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1480)

```cpp
uint64_t maxWindow = 256;
```

### API-beb858d0cc65 · ndn_service_framework::StreamAdaptiveFetcherState::minLookahead

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1481)

```cpp
uint64_t minLookahead = 2;
```

### API-966f610e0eb4 · ndn_service_framework::StreamAdaptiveFetcherState::baseLookahead

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1482)

```cpp
uint64_t baseLookahead = 8;
```

### API-c1c3b6926827 · ndn_service_framework::StreamAdaptiveFetcherState::maxLookahead

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1483)

```cpp
uint64_t maxLookahead = 128;
```

### API-d9fbaeaa1db8 · ndn_service_framework::StreamAdaptiveFetcherState::minInterestLifetimeMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1484)

```cpp
uint64_t minInterestLifetimeMs = 100;
```

### API-065576b3fec1 · ndn_service_framework::StreamAdaptiveFetcherState::maxInterestLifetimeMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1485)

```cpp
uint64_t maxInterestLifetimeMs = 2000;
```

### API-e46a35930a61 · ndn_service_framework::StreamAdaptiveFetcherState::minMissingTimeoutMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1486)

```cpp
uint64_t minMissingTimeoutMs = 80;
```

### API-ef5c4a9af106 · ndn_service_framework::StreamAdaptiveFetcherState::maxMissingTimeoutMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1487)

```cpp
uint64_t maxMissingTimeoutMs = 1500;
```

### API-e6fbee14ec3a · ndn_service_framework::StreamAdaptiveFetcherState::liveEdgeChangeThreshold

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1488)

```cpp
double liveEdgeChangeThreshold = 0.10;
```

### API-19575d334106 · ndn_service_framework::StreamAdaptiveFetcherState::liveEdgePeriodSimilarity

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1489)

```cpp
double liveEdgePeriodSimilarity = 0.95;
```

### API-92b6706010bc · ndn_service_framework::StreamAdaptiveFetcherState::liveEdgeWindow

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1490)

```cpp
uint64_t liveEdgeWindow = 30;
```

### API-8c8188b9de87 · ndn_service_framework::StreamAdaptiveFetcherState::liveEdgeStableRequired

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1491)

```cpp
uint64_t liveEdgeStableRequired = 4;
```

### API-f3ae24ebdc54 · ndn_service_framework::StreamAdaptiveFetcherState::detectionPeriodMs

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1492)

```cpp
uint64_t detectionPeriodMs = 1000;
```

### API-f5a9c61733d3 · ndn_service_framework::StreamAdaptiveFetcherState::recoveryReservePackets

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1493)

```cpp
uint64_t recoveryReservePackets = 1;
```

### API-a2289b0b1869 · ndn_service_framework::StreamAdaptiveFetcherState::aggregateInFlightLimit

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1494)

```cpp
uint64_t aggregateInFlightLimit = 64;
```

### API-a75babbfa06b · ndn_service_framework::StreamAdaptiveFetcherState::mappingReserve

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1495)

```cpp
uint64_t mappingReserve = 4;
```

### API-1b9bd7d7c693 · ndn_service_framework::StreamAdaptiveFetcherState::retransmissionReserve

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1496)

```cpp
uint64_t retransmissionReserve = 1;
```

### API-edb4b83c7403 · ndn_service_framework::StreamAdaptiveFetcherState::mappingBlockCapacity

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1497)

```cpp
uint64_t mappingBlockCapacity = 16;
```

### API-b343a3db8ff5 · ndn_service_framework::StreamAdaptiveFetcherState::chaseMultiplier

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1498)

```cpp
double chaseMultiplier = 2.0;
```

### API-aaec6a88fd18 · ndn_service_framework::StreamAdaptiveFetcherState::adjustMultiplier

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1499)

```cpp
double adjustMultiplier = 0.75;
```

### API-7ed438e32561 · ndn_service_framework::StreamAdaptiveFetcherState::congestionDecreaseMultiplier

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1500)

```cpp
double congestionDecreaseMultiplier = 0.5;
```

### API-b3578e2d4ee9 · ndn_service_framework::StreamAdaptiveFetcherState::detectorProfile

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1501)

```cpp
std::string detectorProfile = "ndnsf-conservative-seed";
```

### API-2ca06319497d · ndn_service_framework::StreamAdaptiveFetcherState::observeRtt

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1503)

```cpp
void observeRtt(double sampleMs, double alpha = 0.25);
```

### API-a3e263583273 · ndn_service_framework::StreamAdaptiveFetcherState::observePayloadDelay

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1512)

```cpp
void observePayloadDelay(double sampleMs, bool aheadOfJoinCheckpoint);
```

原始接口说明：

```text
/**
   * Observe Interest-expression to Data-reception delay for one payload.
   *
   * An ahead-of-join-checkpoint Interest may include producer generation wait
   * (DRD' = DRD + dgen). During live-edge search that upper bound may correct
   * an RTT estimate downward, but cannot raise it. Once Fetching is stable,
   * generation wait is expected to be minimal and normal adaptation resumes.
   */
```

### API-2a91d200472f · ndn_service_framework::StreamAdaptiveFetcherState::recordTimeout

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1513)

```cpp
void recordTimeout();
```

### API-7cec002fc82a · ndn_service_framework::StreamAdaptiveFetcherState::recordTimeout

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1514)

```cpp
void recordTimeout(uint64_t cursor, bool knownProduced, bool wasFuture);
```

### API-d05ba5c1bc9a · ndn_service_framework::StreamAdaptiveFetcherState::recordNack

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1515)

```cpp
void recordNack();
```

### API-e76281b9cf6f · ndn_service_framework::StreamAdaptiveFetcherState::recordNack

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1516)

```cpp
void recordNack(uint64_t cursor, const std::string& reason);
```

### API-ad54ecca61b3 · ndn_service_framework::StreamAdaptiveFetcherState::recordCongestionMark

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1517)

```cpp
void recordCongestionMark(uint64_t cursor, uint64_t mark);
```

### API-600425762bb3 · ndn_service_framework::StreamAdaptiveFetcherState::recordDuplicate

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1518)

```cpp
void recordDuplicate();
```

### API-de575a38ee16 · ndn_service_framework::StreamAdaptiveFetcherState::setBacklogPressure

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1519)

```cpp
void setBacklogPressure(double pressure);
```

### API-d689e91b97ff · ndn_service_framework::StreamAdaptiveFetcherState::decay

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1520)

```cpp
void decay(double factor = 0.85);
```

### API-85887ec22e3d · ndn_service_framework::StreamAdaptiveFetcherState::resetLive

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1521)

```cpp
void resetLive(uint64_t sessionEpoch, uint64_t nextSeq,
                 double samplePeriodMs, uint64_t nowMs = 0);
```

### API-8626ac355418 · ndn_service_framework::StreamAdaptiveFetcherState::configureMappedLive

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1523)

```cpp
void configureMappedLive(uint64_t aggregateLimit,
                           uint64_t mapReserve,
                           uint64_t retransmitReserve,
                           uint64_t blockCapacity,
                           std::string profile);
```

### API-5cdfe43c015a · ndn_service_framework::StreamAdaptiveFetcherState::resetMappedLive

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1528)

```cpp
void resetMappedLive(uint64_t sessionEpoch,
                       uint64_t nextCursor,
                       double samplePeriodMs,
                       uint64_t latestProducedCursor,
                       uint64_t mappingCommittedThroughCursor,
                       uint64_t nextReservedCursor,
                       uint64_t nowMs = 0);
```

### API-b3d54eca9f5c · ndn_service_framework::StreamAdaptiveFetcherState::updateMappingFrontier

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1535)

```cpp
void updateMappingFrontier(uint64_t mappingCommittedThroughCursor,
                             uint64_t nextReservedCursor);
```

### API-85d0c04fbfb0 · ndn_service_framework::StreamAdaptiveFetcherState::advanceNextCursor

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1537)

```cpp
void advanceNextCursor(uint64_t nextCursor);
```

### API-5d048e9cfa30 · ndn_service_framework::StreamAdaptiveFetcherState::setMappedLivePolicyEnabled

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1538)

```cpp
void setMappedLivePolicyEnabled(bool enabled);
```

### API-052cf4a4a2e4 · ndn_service_framework::StreamAdaptiveFetcherState::setPredictedSampleGroups

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1540)

```cpp
void setPredictedSampleGroups(std::vector<uint64_t> groupItems);
```

原始接口说明：

```text
/** Replace the bounded lookahead with signed Mapping v2 group extents. */
```

### API-44d6d4c34037 · ndn_service_framework::StreamAdaptiveFetcherState::setInFlight

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1541)

```cpp
void setInFlight(uint64_t mapping,
                   uint64_t payload,
                   uint64_t retransmission);
```

### API-5e1a18a49393 · ndn_service_framework::StreamAdaptiveFetcherState::observeAcceptedSample

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1544)

```cpp
bool observeAcceptedSample(uint64_t sessionEpoch, uint64_t sampleId,
                             uint64_t arrivalMs, double retrievalDelayMs,
                             uint64_t segmentCount = 1,
                             bool knownProduced = true);
```

### API-740b4a40c2ed · ndn_service_framework::StreamAdaptiveFetcherState::observeSampleExtent

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1548)

```cpp
void observeSampleExtent(uint64_t predictedCount, uint64_t actualCount);
```

### API-68b41ba80f11 · ndn_service_framework::StreamAdaptiveFetcherState::beginRecovery

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1549)

```cpp
void beginRecovery(uint64_t nowMs, uint64_t playoutDeadlineMs);
```

### API-1113470d1b38 · ndn_service_framework::StreamAdaptiveFetcherState::recordRecovery

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1550)

```cpp
void recordRecovery(bool completed);
```

### API-43255f027e45 · ndn_service_framework::StreamAdaptiveFetcherState::recordInvalidObservation

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1551)

```cpp
void recordInvalidObservation();
```

### API-e0ab3b039ab2 · ndn_service_framework::StreamAdaptiveFetcherState::stopLive

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1552)

```cpp
void stopLive();
```

### API-5194f6a2dc30 · ndn_service_framework::StreamAdaptiveFetcherState::phase

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1553)

```cpp
StreamPrefetchPhase phase() const;
```

### API-8864e7a829ec · ndn_service_framework::StreamAdaptiveFetcherState::invalidObservations

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1554)

```cpp
uint64_t invalidObservations() const;
```

### API-b6ae8929d02a · ndn_service_framework::StreamAdaptiveFetcherState::decide

public / declared-interface；[源码](../../ndn-service-framework/Stream.hpp#L1555)

```cpp
StreamFetchDecision decide(uint64_t nowMs = 0,
                             uint64_t playoutDeadlineMs = 0) const;
```

## ndn-service-framework/StreamFacade.hpp

源码 SHA-256：`e1fad4708bc24508eb38a3b730839c073e2c3ca58863b2a28a7177887e18d1dc`。

### API-5dac8ce1648b · ndn_service_framework::ServiceProvider

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L15)

```cpp
class ServiceProvider
```

### API-a48d1669d7a8 · ndn_service_framework::StreamAdvancedOptions

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L17)

```cpp
struct StreamAdvancedOptions
```

### API-18f2299143c8 · ndn_service_framework::StreamAdvancedOptions::mappingBlockCapacity

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L19)

```cpp
size_t mappingBlockCapacity = 16;
```

### API-136cd21efd32 · ndn_service_framework::StreamAdvancedOptions::mappingAheadBlocks

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L20)

```cpp
size_t mappingAheadBlocks = 4;
```

### API-c03034d863b5 · ndn_service_framework::StreamAdvancedOptions::retainedItems

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L21)

```cpp
size_t retainedItems = 600;
```

### API-1eee74321add · ndn_service_framework::StreamAdvancedOptions::maxNameReservations

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L22)

```cpp
size_t maxNameReservations = 65536;
```

### API-e170870e858e · ndn_service_framework::StreamAdvancedOptions::maxPendingInterests

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L23)

```cpp
size_t maxPendingInterests = 256;
```

### API-a5647bc411d0 · ndn_service_framework::StreamAdvancedOptions::signedWireCap

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L24)

```cpp
size_t signedWireCap = ndn::MAX_NDN_PACKET_SIZE;
```

### API-0b8bd80393a8 · ndn_service_framework::StreamAdvancedOptions::startupTimeoutMs

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L25)

```cpp
uint64_t startupTimeoutMs = 1000;
```

### API-e60280282773 · ndn_service_framework::StreamConfig

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L28)

```cpp
struct StreamConfig
```

### API-e6f3aed9b00f · ndn_service_framework::StreamConfig::streamId

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L30)

```cpp
std::string streamId;
```

### API-eafb26228695 · ndn_service_framework::StreamConfig::dataPrefix

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L31)

```cpp
ndn::Name dataPrefix;
```

### API-2b047da2cb3a · ndn_service_framework::StreamConfig::samplePeriodMs

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L32)

```cpp
double samplePeriodMs = 0.0;
```

### API-95a76d89e4b8 · ndn_service_framework::StreamConfig::sampleClasses

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L33)

```cpp
std::vector<SampleClassProfile> sampleClasses;
```

### API-519efd0376a1 · ndn_service_framework::StreamConfig::fec

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L34)

```cpp
LiveStreamFecOptions fec = LiveStreamFecOptions::none();
```

### API-353bcfff20cd · ndn_service_framework::StreamConfig::sessionEpoch

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L35)

```cpp
std::optional<uint64_t> sessionEpoch;
```

### API-fb5d6ae02eb2 · ndn_service_framework::StreamConfig::advanced

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L36)

```cpp
StreamAdvancedOptions advanced;
```

### API-a113f6eef388 · ndn_service_framework::StreamPublisher

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L39)

```cpp
class StreamPublisher
```

### API-6fc16d8cbe76 · ndn_service_framework::StreamPublisher::~StreamPublisher

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L42)

```cpp
~StreamPublisher();
```

### API-c4812a7dbff9 · ndn_service_framework::StreamPublisher::start

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L44)

```cpp
PredictiveStreamDescriptor start();
```

### API-68706878d1a8 · ndn_service_framework::StreamPublisher::push

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L46)

```cpp
void push(std::shared_ptr<ndn::Data> signedData);
```

### API-68ec21120e39 · ndn_service_framework::StreamPublisher::flush

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L48)

```cpp
void flush();
```

### API-31d163061f7b · ndn_service_framework::StreamPublisher::status

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L51)

```cpp
LiveStreamStatus status() const;
```

原始接口说明：

```text
// ── Common ──
```

### API-91340729ae8a · ndn_service_framework::StreamPublisher::stop

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L52)

```cpp
void stop();
```

### API-e1b30eba7e79 · ndn_service_framework::StreamSubscriptionOptions

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L94)

```cpp
struct StreamSubscriptionOptions
```

### API-53dab1cd5b22 · ndn_service_framework::StreamSubscriptionOptions::start

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L96)

```cpp
LiveStreamStart start = LiveStreamStart::Latest;
```

### API-29840aac5ccb · ndn_service_framework::StreamSubscriptionOptions::prefetchPolicy

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L97)

```cpp
std::optional<LiveStreamPrefetchPolicy> prefetchPolicy;
```

### API-9ab23469adc2 · ndn_service_framework::StreamSubscriptionOptions::aggregateInterestLimit

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L98)

```cpp
size_t aggregateInterestLimit = 64;
```

### API-12940c84e302 · ndn_service_framework::StreamSubscriptionOptions::enableFecRecovery

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L99)

```cpp
bool enableFecRecovery = true;
```

### API-060d3bb63fec · ndn_service_framework::StreamSubscriptionOptions::requireFullDelivery

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L100)

```cpp
bool requireFullDelivery = false;
```

### API-b8d295be356c · ndn_service_framework::StreamSubscriptionOptions::interestLifetimeMs

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L101)

```cpp
uint64_t interestLifetimeMs = 500;
```

### API-0793600a9c87 · ndn_service_framework::StreamSubscriptionOptions::onItem

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L102)

```cpp
std::function<LiveStreamItemAdmission(const VerifiedLiveStreamItem&)> onItem;
```

### API-c190ca507011 · ndn_service_framework::StreamSubscriptionOptions::onStatus

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L103)

```cpp
std::function<void(const LiveStreamStatus&)> onStatus;
```

### API-ac228d7cfcd0 · ndn_service_framework::PredictiveStreamSubscriber

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L106)

```cpp
class PredictiveStreamSubscriber
  : public std::enable_shared_from_this<PredictiveStreamSubscriber>
```

### API-745e3e675f75 · ndn_service_framework::PredictiveStreamSubscriber::ItemCallback

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L110)

```cpp
using ItemCallback =
    std::function<LiveStreamItemAdmission(const VerifiedLiveStreamItem&)>;
```

### API-d9f884f6dc45 · ndn_service_framework::PredictiveStreamSubscriber::StatusCallback

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L112)

```cpp
using StatusCallback =
    std::function<void(const LiveStreamStatus&)>;
```

### API-59b8429f782a · ndn_service_framework::PredictiveStreamSubscriber::PredictiveStreamSubscriber

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L115)

```cpp
PredictiveStreamSubscriber(ndn::Face& face,
                              std::shared_ptr<MessageValidator> validator,
                              PredictiveStreamDescriptor descriptor,
                              StreamSubscriptionOptions options);
```

### API-ddbc2b8e9950 · ndn_service_framework::PredictiveStreamSubscriber::~PredictiveStreamSubscriber

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L119)

```cpp
~PredictiveStreamSubscriber();
```

### API-7d8a2ef8f5bd · ndn_service_framework::PredictiveStreamSubscriber::start

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L121)

```cpp
void start();
```

### API-9f3c2d4533ba · ndn_service_framework::PredictiveStreamSubscriber::status

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L122)

```cpp
LiveStreamStatus status() const;
```

### API-340f98ad9ba7 · ndn_service_framework::PredictiveStreamSubscriber::stop

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L123)

```cpp
void stop();
```

### API-31c8e32a663a · ndn_service_framework::detail::computePredictiveFutureCursorHorizon

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L261)

```cpp
uint64_t
computePredictiveFutureCursorHorizon(uint64_t lookahead,
                                     uint64_t aggregateWindow);
```

### API-0962a1025efd · ndn_service_framework::detail::makeLiveStreamOpenOptions

public / declared-interface；[源码](../../ndn-service-framework/StreamFacade.hpp#L265)

```cpp
LiveStreamOpenOptions
makeLiveStreamOpenOptions(const LiveStreamDescriptor& descriptor,
                          StreamSubscriptionOptions options);
```

## ndn-service-framework/TimelineTrace.hpp

源码 SHA-256：`c320167ef449add0fa653c803453496b22aa54c53a59abe934571371b03ad3f1`。

### API-472016455888 · ndn_service_framework::TimelineFields

public / declared-interface；[源码](../../ndn-service-framework/TimelineTrace.hpp#L12)

```cpp
using TimelineFields = std::initializer_list<std::pair<std::string, std::string>>;
```

### API-c1ed5583a8be · ndn_service_framework::timelineSteadyMicroseconds

public / declared-interface；[源码](../../ndn-service-framework/TimelineTrace.hpp#L14)

```cpp
uint64_t timelineSteadyMicroseconds();
```

### API-c4624a1f0946 · ndn_service_framework::timelineTraceEnvEnabled

public / declared-interface；[源码](../../ndn-service-framework/TimelineTrace.hpp#L16)

```cpp
bool timelineTraceEnvEnabled();
```

### API-c6ae47bc028e · ndn_service_framework::logTimelineTrace

public / declared-interface；[源码](../../ndn-service-framework/TimelineTrace.hpp#L18)

```cpp
void logTimelineTrace(const std::string& role,
                      const std::string& event,
                      const ndn::Name& requestId,
                      TimelineFields fields = {});
```

### API-31d087c48ce1 · ndn_service_framework::logStreamTimelineTrace

public / declared-interface；[源码](../../ndn-service-framework/TimelineTrace.hpp#L23)

```cpp
void logStreamTimelineTrace(const std::string& role,
                            const std::string& event,
                            const std::string& streamId,
                            uint64_t sessionEpoch,
                            uint64_t cursor,
                            TimelineFields fields = {});
```

### API-298da84016ee · ndn_service_framework::hybridCryptoTimingEnvEnabled

public / declared-interface；[源码](../../ndn-service-framework/TimelineTrace.hpp#L30)

```cpp
bool hybridCryptoTimingEnvEnabled();
```

### API-7ceef89ae32f · ndn_service_framework::logHybridCryptoTiming

public / declared-interface；[源码](../../ndn-service-framework/TimelineTrace.hpp#L32)

```cpp
void logHybridCryptoTiming(const std::string& role,
                           const std::string& event,
                           const ndn::Name& requestId,
                           TimelineFields fields = {});
```

### API-c5045fbdf396 · ndn_service_framework::controlTimingEnvEnabled

public / declared-interface；[源码](../../ndn-service-framework/TimelineTrace.hpp#L37)

```cpp
bool controlTimingEnvEnabled();
```

### API-df3b71bf67b1 · ndn_service_framework::logControlTiming

public / declared-interface；[源码](../../ndn-service-framework/TimelineTrace.hpp#L39)

```cpp
void logControlTiming(const std::string& role,
                      const std::string& event,
                      const ndn::Name& requestId,
                      TimelineFields fields = {});
```

## ndn-service-framework/common.hpp

源码 SHA-256：`831e18aa8e8a0892343299ff872d3fe639b4f739e335f931b4c23a2b7010d48d`。

### API-dd44cb42f54f · ndn_service_framework::getCertificateKeyType

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L65)

```cpp
ndn::KeyType
    getCertificateKeyType(const ndn::security::Certificate& cert);
```

### API-19c292f074fb · ndn_service_framework::getEcdsaSigningCertificateOrFallback

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L68)

```cpp
ndn::security::Certificate
    getEcdsaSigningCertificateOrFallback(ndn::KeyChain& keyChain,
                                         const ndn::security::Certificate& fallbackCert);
```

### API-78a01ff714c5 · ndn_service_framework::getRsaEncryptionCertificateOrThrow

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L72)

```cpp
ndn::security::Certificate
    getRsaEncryptionCertificateOrThrow(ndn::KeyChain& keyChain,
                                       const ndn::security::Certificate& identityHintCert);
```

### API-6f42d249aca1 · ndn_service_framework::getRsaEncryptionCertificateOrThrow

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L76)

```cpp
ndn::security::Certificate
    getRsaEncryptionCertificateOrThrow(const ndn::security::Certificate& identityHintCert);
```

### API-300015301fb9 · ndn_service_framework::makeEcdsaPreferredSigningInfo

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L79)

```cpp
ndn::security::SigningInfo
    makeEcdsaPreferredSigningInfo(ndn::KeyChain& keyChain,
                                  const ndn::Name& identityName);
```

### API-1e07e88b3f6d · ndn_service_framework::signDataEcdsaPreferred

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L83)

```cpp
void
    signDataEcdsaPreferred(ndn::KeyChain& keyChain,
                           ndn::Data& data,
                           const ndn::Name& identityName);
```

### API-68fd798e27cf · ndn_service_framework::configureSvsPubSubOptionsFromEnvironment

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L89)

```cpp
void
    configureSvsPubSubOptionsFromEnvironment(ndn::svs::SVSPubSubOptions& options);
```

原始接口说明：

```text
/** Apply validated NDNSF environment overrides to SVS PubSub options. */
```

### API-992344a088a3 · ndn_service_framework::SelectionExecutionState

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L92)

```cpp
enum class SelectionExecutionState
```

### API-15014d215bad · ndn_service_framework::SelectionExecutionState::Unknown

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L94)

```cpp
Unknown
```

### API-5ede2d1971bb · ndn_service_framework::SelectionExecutionState::Received

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L95)

```cpp
Received
```

### API-a98f920148ca · ndn_service_framework::SelectionExecutionState::Queued

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L96)

```cpp
Queued
```

### API-2809ca5cded4 · ndn_service_framework::SelectionExecutionState::Running

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L97)

```cpp
Running
```

### API-7a33e9c4d9aa · ndn_service_framework::SelectionExecutionState::Completed

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L98)

```cpp
Completed
```

### API-608bd62c46de · ndn_service_framework::SelectionExecutionState::Failed

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L99)

```cpp
Failed
```

### API-43037015b7e2 · ndn_service_framework::SelectionExecutionState::Rejected

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L100)

```cpp
Rejected
```

### API-151ab273c108 · ndn_service_framework::SelectionExecutionState::Expired

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L101)

```cpp
Expired
```

### API-8666a834e4da · ndn_service_framework::SelectionExecutionState::Cancelled

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L102)

```cpp
Cancelled
```

### API-49ac79013bdc · ndn_service_framework::CollaborationMemberStatus

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L105)

```cpp
struct CollaborationMemberStatus
```

### API-af920a86cc09 · ndn_service_framework::CollaborationMemberStatus::providerName

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L107)

```cpp
ndn::Name providerName;
```

### API-10b8df9ca7f2 · ndn_service_framework::CollaborationMemberStatus::serviceName

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L108)

```cpp
ndn::Name serviceName;
```

### API-a6c885a8ae3c · ndn_service_framework::CollaborationMemberStatus::requestId

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L109)

```cpp
ndn::Name requestId;
```

### API-4b8149df17f9 · ndn_service_framework::CollaborationMemberStatus::selectionDigest

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L110)

```cpp
std::string selectionDigest;
```

### API-b78e8fb681ac · ndn_service_framework::CollaborationMemberStatus::role

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L111)

```cpp
std::string role;
```

### API-6e61d179393d · ndn_service_framework::CollaborationMemberStatus::operationId

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L112)

```cpp
std::string operationId;
```

### API-b0408feee187 · ndn_service_framework::CollaborationMemberStatus::operation

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L113)

```cpp
std::string operation;
```

### API-f853a03caaec · ndn_service_framework::CollaborationMemberStatus::state

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L114)

```cpp
std::string state = "QUEUED";
```

### API-8079d9db093c · ndn_service_framework::CollaborationMemberStatus::reasonCode

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L115)

```cpp
std::string reasonCode;
```

### API-fa723b86294b · ndn_service_framework::CollaborationMemberStatus::message

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L116)

```cpp
std::string message;
```

### API-25e6f997ec5c · ndn_service_framework::CollaborationMemberStatus::attempt

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L117)

```cpp
uint64_t attempt = 1;
```

### API-ab421f681c3c · ndn_service_framework::CollaborationMemberStatus::epoch

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L118)

```cpp
uint64_t epoch = 1;
```

### API-689bd1362cc1 · ndn_service_framework::CollaborationMemberStatus::sequence

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L119)

```cpp
uint64_t sequence = 1;
```

### API-675f9ac1330b · ndn_service_framework::CollaborationMemberStatus::progressKnown

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L120)

```cpp
bool progressKnown = false;
```

### API-42d93a9924b9 · ndn_service_framework::CollaborationMemberStatus::progress

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L121)

```cpp
double progress = 0.0;
```

### API-adfbe8c48bfd · ndn_service_framework::CollaborationMemberStatus::createdAtMs

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L122)

```cpp
uint64_t createdAtMs = 0;
```

### API-6cc6127e0d57 · ndn_service_framework::CollaborationMemberStatus::updatedAtMs

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L123)

```cpp
uint64_t updatedAtMs = 0;
```

### API-aac76a40a88d · ndn_service_framework::CollaborationMemberStatus::expiresAtMs

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L124)

```cpp
uint64_t expiresAtMs = 0;
```

### API-d52be0e185c6 · ndn_service_framework::CollaborationMemberStatus::detailsSchema

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L125)

```cpp
std::string detailsSchema;
```

### API-13920c2744ff · ndn_service_framework::CollaborationMemberStatus::detailsPayload

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L126)

```cpp
ndn::Buffer detailsPayload;
```

### API-879d5dec1533 · ndn_service_framework::SelectionExecutionStatus

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L129)

```cpp
struct SelectionExecutionStatus
```

### API-d0919f65b1e2 · ndn_service_framework::SelectionExecutionStatus::providerName

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L131)

```cpp
ndn::Name providerName;
```

### API-307fc623b26d · ndn_service_framework::SelectionExecutionStatus::serviceName

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L132)

```cpp
ndn::Name serviceName;
```

### API-1c53c73b6667 · ndn_service_framework::SelectionExecutionStatus::requestId

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L133)

```cpp
ndn::Name requestId;
```

### API-cb8d43eb64a0 · ndn_service_framework::SelectionExecutionStatus::selectionDigest

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L134)

```cpp
std::string selectionDigest;
```

### API-a137d1484e2b · ndn_service_framework::SelectionExecutionStatus::state

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L135)

```cpp
SelectionExecutionState state = SelectionExecutionState::Unknown;
```

### API-80bf8b13f104 · ndn_service_framework::SelectionExecutionStatus::message

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L136)

```cpp
std::string message;
```

### API-69b88593bca0 · ndn_service_framework::SelectionExecutionStatus::responseName

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L137)

```cpp
ndn::Name responseName;
```

### API-6ba6467d7f92 · ndn_service_framework::SelectionExecutionStatus::receivedAtUs

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L138)

```cpp
uint64_t receivedAtUs = 0;
```

### API-85265badc93d · ndn_service_framework::SelectionExecutionStatus::queuedAtUs

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L139)

```cpp
uint64_t queuedAtUs = 0;
```

### API-09152b91a58f · ndn_service_framework::SelectionExecutionStatus::runningAtUs

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L140)

```cpp
uint64_t runningAtUs = 0;
```

### API-de57d2f581b1 · ndn_service_framework::SelectionExecutionStatus::completedAtUs

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L141)

```cpp
uint64_t completedAtUs = 0;
```

### API-aa264b89a5ec · ndn_service_framework::SelectionExecutionStatus::updatedAtUs

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L142)

```cpp
uint64_t updatedAtUs = 0;
```

### API-6a3db16273b4 · ndn_service_framework::SelectionExecutionStatus::decisionReceipt

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L146)

```cpp
ndn::Buffer decisionReceipt;
```

原始接口说明：

```text
// Canonical SelectionDecisionReceipt wire bytes. The enclosing exact-
// name status Data is Provider-signed, so the receipt is authenticated
// without introducing unsolicited push traffic.
```

### API-ba4d5692c75b · ndn_service_framework::SelectionExecutionStatus::memberStatuses

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L147)

```cpp
std::vector<CollaborationMemberStatus> memberStatuses;
```

### API-e141f9a9a455 · ndn_service_framework::selectionExecutionStateToString

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L150)

```cpp
const char*
    selectionExecutionStateToString(SelectionExecutionState state);
```

### API-c7db57f4a12a · ndn_service_framework::selectionExecutionStateFromString

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L153)

```cpp
SelectionExecutionState
    selectionExecutionStateFromString(const std::string& state);
```

### API-eb8f149418f8 · ndn_service_framework::HybridCryptoCounters

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L156)

```cpp
struct HybridCryptoCounters
```

### API-51f64cda6506 · ndn_service_framework::HybridCryptoCounters::hybrid_key_epoch_created

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L158)

```cpp
std::atomic<uint64_t> hybrid_key_epoch_created{0};
```

### API-dbd6291a013d · ndn_service_framework::HybridCryptoCounters::hybrid_key_rotation_age

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L159)

```cpp
std::atomic<uint64_t> hybrid_key_rotation_age{0};
```

### API-6d601450ac2d · ndn_service_framework::HybridCryptoCounters::hybrid_key_rotation_uses

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L160)

```cpp
std::atomic<uint64_t> hybrid_key_rotation_uses{0};
```

### API-c97ff8083e41 · ndn_service_framework::HybridCryptoCounters::nac_abe_key_wrap_count

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L161)

```cpp
std::atomic<uint64_t> nac_abe_key_wrap_count{0};
```

### API-31b09fe7a2f1 · ndn_service_framework::HybridCryptoCounters::nac_abe_key_unwrap_count

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L162)

```cpp
std::atomic<uint64_t> nac_abe_key_unwrap_count{0};
```

### API-8d5c9681aa9d · ndn_service_framework::HybridCryptoCounters::symmetric_encrypt_count

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L163)

```cpp
std::atomic<uint64_t> symmetric_encrypt_count{0};
```

### API-411b3719a0a6 · ndn_service_framework::HybridCryptoCounters::symmetric_decrypt_count

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L164)

```cpp
std::atomic<uint64_t> symmetric_decrypt_count{0};
```

### API-e13188fbde24 · ndn_service_framework::HybridCryptoCounters::key_cache_hit_count

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L165)

```cpp
std::atomic<uint64_t> key_cache_hit_count{0};
```

### API-29312b68ac96 · ndn_service_framework::HybridCryptoCounters::key_cache_miss_count

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L166)

```cpp
std::atomic<uint64_t> key_cache_miss_count{0};
```

### API-db20f8521983 · ndn_service_framework::HybridCryptoCounters::auth_decrypt_failure_count

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L167)

```cpp
std::atomic<uint64_t> auth_decrypt_failure_count{0};
```

### API-a5dca70ca517 · ndn_service_framework::HybridCryptoCounters::user_token_symmetric_encrypt_count

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L168)

```cpp
std::atomic<uint64_t> user_token_symmetric_encrypt_count{0};
```

### API-914b8f144c0e · ndn_service_framework::HybridCryptoCounters::user_token_symmetric_decrypt_count

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L169)

```cpp
std::atomic<uint64_t> user_token_symmetric_decrypt_count{0};
```

### API-b5ddb785506d · ndn_service_framework::HybridCryptoCounters::provider_token_symmetric_encrypt_count

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L170)

```cpp
std::atomic<uint64_t> provider_token_symmetric_encrypt_count{0};
```

### API-1662c6843d1c · ndn_service_framework::HybridCryptoCounters::provider_token_symmetric_decrypt_count

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L171)

```cpp
std::atomic<uint64_t> provider_token_symmetric_decrypt_count{0};
```

### API-b9c08be9a330 · ndn_service_framework::HybridCryptoCounters::legacy_token_nac_abe_encrypt_count

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L172)

```cpp
std::atomic<uint64_t> legacy_token_nac_abe_encrypt_count{0};
```

### API-4e03f5a58038 · ndn_service_framework::HybridCryptoCounters::legacy_token_nac_abe_decrypt_count

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L173)

```cpp
std::atomic<uint64_t> legacy_token_nac_abe_decrypt_count{0};
```

### API-687a67d1379f · ndn_service_framework::SerializedWorkerQueue

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L176)

```cpp
class SerializedWorkerQueue
```

### API-5b3c0162de7f · ndn_service_framework::SerializedWorkerQueue::SerializedWorkerQueue

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L179)

```cpp
explicit SerializedWorkerQueue(std::string name, size_t maxQueueSize = 1024)
```

### API-4c2f248e3365 · ndn_service_framework::SerializedWorkerQueue::~SerializedWorkerQueue

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L186)

```cpp
~SerializedWorkerQueue()
```

### API-612b7822ba83 · ndn_service_framework::SerializedWorkerQueue::SerializedWorkerQueue

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L191)

```cpp
SerializedWorkerQueue(const SerializedWorkerQueue&) = delete;
```

### API-c3f821e69c18 · ndn_service_framework::SerializedWorkerQueue::operator=

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L192)

```cpp
SerializedWorkerQueue& operator=(const SerializedWorkerQueue&) = delete;
```

### API-618a8d3e6d66 · ndn_service_framework::SerializedWorkerQueue::post

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L194)

```cpp
bool
        post(std::function<void()> task)
```

### API-bfa1c1a87108 · ndn_service_framework::SerializedWorkerQueue::shutdown

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L212)

```cpp
void
        shutdown()
```

### API-77cd3cc9d227 · ndn_service_framework::BoundedWorkerPool

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L269)

```cpp
class BoundedWorkerPool
```

### API-1e343f0473eb · ndn_service_framework::BoundedWorkerPool::BoundedWorkerPool

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L272)

```cpp
explicit BoundedWorkerPool(std::string name, size_t maxQueueSize = 1024)
```

### API-5b29fd4a27c1 · ndn_service_framework::BoundedWorkerPool::~BoundedWorkerPool

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L278)

```cpp
~BoundedWorkerPool()
```

### API-ffdc33fba80d · ndn_service_framework::BoundedWorkerPool::BoundedWorkerPool

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L283)

```cpp
BoundedWorkerPool(const BoundedWorkerPool&) = delete;
```

### API-607a83f8c4c3 · ndn_service_framework::BoundedWorkerPool::operator=

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L284)

```cpp
BoundedWorkerPool& operator=(const BoundedWorkerPool&) = delete;
```

### API-8471f6045343 · ndn_service_framework::BoundedWorkerPool::setThreadCount

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L286)

```cpp
void
        setThreadCount(size_t threadCount)
```

### API-8c0e115f150b · ndn_service_framework::BoundedWorkerPool::getThreadCount

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L304)

```cpp
size_t
        getThreadCount() const
```

### API-b5f586c428be · ndn_service_framework::BoundedWorkerPool::getQueueSize

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L311)

```cpp
size_t
        getQueueSize() const
```

### API-5b4a0c4937b1 · ndn_service_framework::BoundedWorkerPool::post

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L318)

```cpp
bool
        post(std::function<void()> task)
```

### API-685eaf4bdd68 · ndn_service_framework::BoundedWorkerPool::shutdown

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L339)

```cpp
void
        shutdown()
```

### API-135f82a7e566 · ndn_service_framework::OptionalServiceDiscovery

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L404)

```cpp
class OptionalServiceDiscovery
```

### API-646144c46267 · ndn_service_framework::OptionalServiceDiscovery::Details

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L407)

```cpp
using Details = ndnsd::discovery::Details;
```

### API-3cccbbbdcc1a · ndn_service_framework::OptionalServiceDiscovery::Callback

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L408)

```cpp
using Callback = std::function<void(const Details&)>;
```

### API-c8ba46c3703b · ndn_service_framework::OptionalServiceDiscovery::OptionalServiceDiscovery

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L410)

```cpp
OptionalServiceDiscovery() = default;
```

### API-fbedeca7f1c5 · ndn_service_framework::OptionalServiceDiscovery::OptionalServiceDiscovery

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L412)

```cpp
OptionalServiceDiscovery(const ndn::Name& groupPrefix,
                                 const ndn::Name& identity,
                                 ndn::Face& face,
                                 ndn::KeyChain& keyChain,
                                 Callback callback)
```

### API-9ececed0fc09 · ndn_service_framework::OptionalServiceDiscovery::enable

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L421)

```cpp
void enable(const ndn::Name& groupPrefix,
                    const ndn::Name& identity,
                    ndn::Face& face,
                    ndn::KeyChain& keyChain,
                    Callback callback)
```

### API-73720608095b · ndn_service_framework::OptionalServiceDiscovery::isEnabled

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L431)

```cpp
bool isEnabled() const
```

### API-865600d961e3 · ndn_service_framework::OptionalServiceDiscovery::publishServiceDetail

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L436)

```cpp
void publishServiceDetail(const Details& details)
```

### API-e9d806051904 · ndn_service_framework::OptionalServiceDiscovery::getReceivedServiceDetails

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L443)

```cpp
std::map<std::string, Details>
        getReceivedServiceDetails() const
```

### API-5d71a587dc45 · ndn_service_framework::MessageValidator

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L456)

```cpp
class MessageValidator : public ndn::svs::BaseValidator,
                             public std::enable_shared_from_this<MessageValidator>
```

### API-2bdf8f8fa8d4 · ndn_service_framework::MessageValidator::MessageValidator

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L460)

```cpp
MessageValidator(std::string trustSchemaPath,
                         std::optional<ndn::Name> svsGroupPrefix = std::nullopt,
                         ndn::Face* callbackFace = nullptr)
```

### API-ac9b66c57e28 · ndn_service_framework::MessageValidator::getFailureCountForTesting

public / test-helper；[源码](../../ndn-service-framework/common.hpp#L481)

```cpp
size_t
        getFailureCountForTesting() const
```

### API-749fe6a90ae9 · ndn_service_framework::MessageValidator::getLocalCertificateCacheSizeForTesting

public / test-helper；[源码](../../ndn-service-framework/common.hpp#L487)

```cpp
size_t
        getLocalCertificateCacheSizeForTesting() const
```

### API-dd5073d4eaa4 · ndn_service_framework::MessageValidator::getConfiguredValidatorForSegmentFetcher

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L503)

```cpp
ndn::security::Validator&
        getConfiguredValidatorForSegmentFetcher() noexcept
```

原始接口说明：

```text
/**
         * @brief Return the configured ndn-cxx validator used by the normal
         *        trust-schema validation path.
         *
         * SegmentFetcher accepts ndn::security::Validator rather than the
         * ndn-svs BaseValidator interface implemented by MessageValidator.
         * Exposing this reference keeps segmented retrieval on the same
         * configured trust schema instead of falling back to ValidatorNull.
         */
```

### API-399211f371cc · ndn_service_framework::MessageValidator::validateWithConfiguredTrustSchema

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L519)

```cpp
void
        validateWithConfiguredTrustSchema(
          const ndn::Data& data,
          const ndn::security::DataValidationSuccessCallback& successCb,
          const ndn::security::DataValidationFailureCallback& failureCb)
```

原始接口说明：

```text
/**
         * @brief Validate Data through the configured trust schema only.
         *
         * The normal SVS validator path has a local-certificate fast path for
         * application Data.  Controller PolicyStatus is authority material,
         * so it must not be accepted merely because a matching certificate is
         * present in a local PIB.  This entry point deliberately bypasses the
         * local signature shortcut and invokes ValidatorConfig, including its
         * configured trust anchor and name rules.
         */
```

### API-2efa9b194f65 · ndn_service_framework::MessageValidator::validate

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L549)

```cpp
void
        validate(const ndn::Data &data,
                 const ndn::security::DataValidationSuccessCallback &successCb,
                 const ndn::security::DataValidationFailureCallback &failureCb) override
```

原始接口说明：

```text
/**
         * @brief Asynchronously validate @p data
         *
         * @note @p successCb and @p failureCb must not be nullptr
         */
```

### API-13e90d01b03b · ndn_service_framework::MessageValidator::validateData

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L557)

```cpp
void
        validateData(
          const ndn::Data& data,
          const ndn::security::DataValidationSuccessCallback& successCb,
          const ndn::security::DataValidationFailureCallback& failureCb)
```

### API-e55c13c91b2e · ndn_service_framework::MessageValidator::validateDataOnFace

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L589)

```cpp
void
        validateDataOnFace(
          const ndn::Data& data,
          ndn::security::DataValidationSuccessCallback successCb,
          ndn::security::DataValidationFailureCallback failureCb)
```

### API-c3f930ff6b43 · ndn_service_framework::MessageValidator::validate

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L691)

```cpp
void
        validate(const ndn::Interest &interest,
                 const ndn::security::InterestValidationSuccessCallback &successCb,
                 const ndn::security::InterestValidationFailureCallback &failureCb) override
```

原始接口说明：

```text
/**
         * @brief Asynchronously validate @p interest
         *
         * @note @p successCb and @p failureCb must not be nullptr
         */
```

### API-54bb08da57b3 · ndn_service_framework::CommandInterestSigner

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L949)

```cpp
class CommandInterestSigner : public ndn::svs::BaseSigner
```

### API-7d17e2ca3fea · ndn_service_framework::CommandInterestSigner::CommandInterestSigner

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L952)

```cpp
explicit CommandInterestSigner(ndn::security::KeyChain &keyChain)
```

### API-72b67bd4b16b · ndn_service_framework::CommandInterestSigner::sign

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L958)

```cpp
void
        sign(ndn::Interest &interest) const override
```

### API-55140c1e1320 · ndn_service_framework::CommandInterestSigner::sign

public / declared-interface；[源码](../../ndn-service-framework/common.hpp#L967)

```cpp
void
        sign(ndn::Data &data) const override
```

## ndn-service-framework/nac-abe-config.hpp

源码 SHA-256：`bd856b209bef623753a1da2b015e257c48faeca82dd86ec577d178aaff166545`。

## ndn-service-framework/utils.hpp

源码 SHA-256：`59e5eec6db7096c99fac3f30822a0af1808565c22552687cf592258f1b54fd8c`。

### API-21f9815fdd57 · ndn_service_framework::PolicyStatusName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L25)

```cpp
struct PolicyStatusName
```

### API-64fb6fc88bf8 · ndn_service_framework::PolicyStatusName::serviceName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L27)

```cpp
ndn::Name serviceName;
```

### API-e07d563e2ebf · ndn_service_framework::PolicyStatusName::version

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L28)

```cpp
std::optional<ControllerVersion> version;
```

### API-f3af33b5f94c · ndn_service_framework::makePolicyStatusName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L31)

```cpp
ndn::Name makePolicyStatusName(const ndn::Name& controllerPrefix,
                                   const ndn::Name& serviceName,
                                   const ControllerVersion& version);
```

### API-671e63304879 · ndn_service_framework::parsePolicyStatusName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L34)

```cpp
std::optional<PolicyStatusName>
    parsePolicyStatusName(const ndn::Name& controllerPrefix,
                          const ndn::Name& name);
```

### API-b54eaac0b634 · ndn_service_framework::RequestNameV2

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L38)

```cpp
struct RequestNameV2
```

### API-8ede977e1c35 · ndn_service_framework::RequestNameV2::requesterName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L40)

```cpp
ndn::Name requesterName;
```

### API-320aa7ae6409 · ndn_service_framework::RequestNameV2::serviceName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L41)

```cpp
ndn::Name serviceName;
```

### API-9cf300eea986 · ndn_service_framework::RequestNameV2::requestId

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L42)

```cpp
ndn::Name requestId;
```

### API-98b7583433f3 · ndn_service_framework::ResponseNameV2

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L45)

```cpp
struct ResponseNameV2
```

### API-5ebf4c386379 · ndn_service_framework::ResponseNameV2::providerName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L47)

```cpp
ndn::Name providerName;
```

### API-37d9415547e1 · ndn_service_framework::ResponseNameV2::requesterName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L48)

```cpp
ndn::Name requesterName;
```

### API-e9e5f73a18b3 · ndn_service_framework::ResponseNameV2::serviceName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L49)

```cpp
ndn::Name serviceName;
```

### API-da37325389b2 · ndn_service_framework::ResponseNameV2::requestId

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L50)

```cpp
ndn::Name requestId;
```

### API-7f69605a94a6 · ndn_service_framework::RequestAckNameV2

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L53)

```cpp
struct RequestAckNameV2
```

### API-22d203885643 · ndn_service_framework::RequestAckNameV2::providerName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L55)

```cpp
ndn::Name providerName;
```

### API-473a646bc1b8 · ndn_service_framework::RequestAckNameV2::requesterName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L56)

```cpp
ndn::Name requesterName;
```

### API-4b25e191ccc2 · ndn_service_framework::RequestAckNameV2::serviceName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L57)

```cpp
ndn::Name serviceName;
```

### API-8b8db5ab334e · ndn_service_framework::RequestAckNameV2::requestId

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L58)

```cpp
ndn::Name requestId;
```

### API-ece437890a55 · ndn_service_framework::ServiceSelectionNameV2

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L61)

```cpp
struct ServiceSelectionNameV2
```

### API-f9f0d7709554 · ndn_service_framework::ServiceSelectionNameV2::requesterName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L63)

```cpp
ndn::Name requesterName;
```

### API-71dcefba7ab4 · ndn_service_framework::ServiceSelectionNameV2::providerName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L64)

```cpp
ndn::Name providerName;
```

### API-2edbada314de · ndn_service_framework::ServiceSelectionNameV2::serviceName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L65)

```cpp
ndn::Name serviceName;
```

### API-6406ca25fdd6 · ndn_service_framework::ServiceSelectionNameV2::requestId

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L66)

```cpp
ndn::Name requestId;
```

### API-df16e63752e1 · ndn_service_framework::ServiceSelectionDecisionNameV2

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L69)

```cpp
struct ServiceSelectionDecisionNameV2
```

### API-372967ea3638 · ndn_service_framework::ServiceSelectionDecisionNameV2::requesterName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L71)

```cpp
ndn::Name requesterName;
```

### API-95d28aaa1058 · ndn_service_framework::ServiceSelectionDecisionNameV2::providerName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L72)

```cpp
ndn::Name providerName;
```

### API-311fbf75c469 · ndn_service_framework::ServiceSelectionDecisionNameV2::serviceName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L73)

```cpp
ndn::Name serviceName;
```

### API-8409d101b904 · ndn_service_framework::ServiceSelectionDecisionNameV2::requestId

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L74)

```cpp
ndn::Name requestId;
```

### API-ad917344297d · ndn_service_framework::ServiceSelectionDecisionNameV2::attempt

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L75)

```cpp
uint64_t attempt = 0;
```

### API-63b72a32f3a7 · ndn_service_framework::CompactServiceSelectionNameV2

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L78)

```cpp
struct CompactServiceSelectionNameV2
```

### API-2904fea8e7e4 · ndn_service_framework::CompactServiceSelectionNameV2::requesterName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L80)

```cpp
ndn::Name requesterName;
```

### API-e1071a4c5f7b · ndn_service_framework::CompactServiceSelectionNameV2::serviceName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L81)

```cpp
ndn::Name serviceName;
```

### API-e87f93e1cdc1 · ndn_service_framework::CompactServiceSelectionNameV2::requestId

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L82)

```cpp
ndn::Name requestId;
```

### API-b06611783506 · ndn_service_framework::SelectionStatusQueryName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L85)

```cpp
struct SelectionStatusQueryName
```

### API-aefff56b5750 · ndn_service_framework::SelectionStatusQueryName::providerName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L87)

```cpp
ndn::Name providerName;
```

### API-06602f3c7123 · ndn_service_framework::SelectionStatusQueryName::serviceName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L88)

```cpp
ndn::Name serviceName;
```

### API-1bb8f7ecd8a0 · ndn_service_framework::SelectionStatusQueryName::selectionDigest

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L89)

```cpp
std::string selectionDigest;
```

### API-4ef9a291e622 · ndn_service_framework::SecureSelectionStatusName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L92)

```cpp
struct SecureSelectionStatusName
```

### API-0005807dc34e · ndn_service_framework::SecureSelectionStatusName::providerName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L94)

```cpp
ndn::Name providerName;
```

### API-3a62e75421a9 · ndn_service_framework::SecureSelectionStatusName::version

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L95)

```cpp
uint64_t version = 0;
```

### API-8fe0019a8397 · ndn_service_framework::SecureSelectionStatusName::statusHandle

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L96)

```cpp
std::string statusHandle;
```

### API-40b97b4f3b31 · ndn_service_framework::ProviderReadyName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L99)

```cpp
struct ProviderReadyName
```

### API-84720198e9d3 · ndn_service_framework::ProviderReadyName::requesterName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L101)

```cpp
ndn::Name requesterName;
```

### API-fd0c2c0a9d05 · ndn_service_framework::ProviderReadyName::version

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L102)

```cpp
uint64_t version = 0;
```

### API-d9a8cf160c1b · ndn_service_framework::ProviderReadyName::controlHandle

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L103)

```cpp
std::string controlHandle;
```

### API-f0507dd02e11 · ndn_service_framework::ExecutionActivateName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L106)

```cpp
struct ExecutionActivateName
```

### API-dff0f2248ea0 · ndn_service_framework::ExecutionActivateName::providerName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L108)

```cpp
ndn::Name providerName;
```

### API-10ba7660e685 · ndn_service_framework::ExecutionActivateName::version

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L109)

```cpp
uint64_t version = 0;
```

### API-b6ef3d7ec9d9 · ndn_service_framework::ExecutionActivateName::controlHandle

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L110)

```cpp
std::string controlHandle;
```

### API-db6b6a904eab · ndn_service_framework::CollaborationDataName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L113)

```cpp
struct CollaborationDataName
```

### API-4543cb83bd74 · ndn_service_framework::CollaborationDataName::producerName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L115)

```cpp
ndn::Name producerName;
```

### API-ec7036218860 · ndn_service_framework::CollaborationDataName::requesterName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L116)

```cpp
ndn::Name requesterName;
```

### API-56ef81eb671e · ndn_service_framework::CollaborationDataName::requestId

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L117)

```cpp
ndn::Name requestId;
```

### API-38b36c473baf · ndn_service_framework::CollaborationDataName::keyScope

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L118)

```cpp
std::string keyScope;
```

### API-f63598055bb4 · ndn_service_framework::CollaborationDataName::topic

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L119)

```cpp
ndn::Name topic;
```

### API-2eb6542c2edd · ndn_service_framework::CollaborationDataName::sequence

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L120)

```cpp
uint64_t sequence = 0;
```

### API-72dca16f16cf · ndn_service_framework::LargeDataReference

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L123)

```cpp
struct LargeDataReference
```

### API-6fe4041ba37f · ndn_service_framework::LargeDataReference::dataName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L125)

```cpp
ndn::Name dataName;
```

### API-d914aa4f471e · ndn_service_framework::LargeDataReference::objectType

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L126)

```cpp
std::string objectType;
```

### API-8d7e3a237e88 · ndn_service_framework::LargeDataReference::objectId

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L127)

```cpp
std::string objectId;
```

### API-0ea309e4e19d · ndn_service_framework::LargeDataReference::keyScope

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L132)

```cpp
std::string keyScope;
```

原始接口说明：

```text
// Empty preserves the legacy service-wide MessageKey path.  The
// request-scoped response path sets this to "request" so the User
// resolves the object with the invocation's K_response instead of a
// service-wide ABE key.
```

### API-809ff373c241 · ndn_service_framework::LargeDataReference::plaintextSize

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L133)

```cpp
size_t plaintextSize = 0;
```

### API-1bff9628e826 · ndn_service_framework::LargeDataReference::encrypted

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L134)

```cpp
bool encrypted = true;
```

### API-ecc8bded9ba2 · ndn_service_framework::LargeDataReference::digest

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L135)

```cpp
std::string digest;
```

### API-5cb75d4307b1 · ndn_service_framework::makeRequestNameV2

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L152)

```cpp
ndn::Name makeRequestNameV2(const ndn::Name& requesterName,
                                const ndn::Name& serviceName,
                                const ndn::Name& requestId);
```

原始接口说明：

```text
// V2 wire names use one unified serviceName endpoint path.
// NAC-ABE attributes follow message direction: REQUEST/SELECTION use
// /SERVICE/<serviceName>; RESPONSE/ACK use /PERMISSION/<serviceName>.
// Request:
//   /<requester>/NDNSF/REQUEST/<serviceName...>/<requestId>
// Response:
//   /<provider>/NDNSF/RESPONSE/<requester-uri-component>/<serviceName...>/<requestId>
// ACK:
//   /<provider>/NDNSF/ACK/<requester-uri-component>/<serviceName...>/<requestId>
// Selection:
//   /<requester>/NDNSF/SELECTION/<serviceName...>/<requestId>
// Selected providers are carried in the SelectionMessage payload entries.
// The provider-specific selection-name helpers remain parseable for old
// data, but new V2 publishing uses the unified selection name above.
```

### API-b5b726a9d0d0 · ndn_service_framework::makeRequestNameWithoutPrefixV2

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L155)

```cpp
ndn::Name makeRequestNameWithoutPrefixV2(const ndn::Name& serviceName,
                                             const ndn::Name& requestId);
```

### API-6fdc5c21dc93 · ndn_service_framework::parseRequestNameV2

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L157)

```cpp
std::optional<RequestNameV2> parseRequestNameV2(const ndn::Name& requestName);
```

### API-9840eba3df98 · ndn_service_framework::makeResponseNameV2

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L159)

```cpp
ndn::Name makeResponseNameV2(const ndn::Name& providerName,
                                 const ndn::Name& requesterName,
                                 const ndn::Name& serviceName,
                                 const ndn::Name& requestId);
```

### API-3f6efdf65efc · ndn_service_framework::makeResponseNameWithoutPrefixV2

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L163)

```cpp
ndn::Name makeResponseNameWithoutPrefixV2(const ndn::Name& requesterName,
                                              const ndn::Name& serviceName,
                                              const ndn::Name& requestId);
```

### API-1cd4afe1043f · ndn_service_framework::stripTrailingResponseSegments

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L169)

```cpp
ndn::Name stripTrailingResponseSegments(const ndn::Name& responseName);
```

原始接口说明：

```text
// SegmentFetcher may append version/segment components to a response Data
// name. Logical response matching and hybrid-message AAD use the
// unsegmented V2 name.
```

### API-38b97ddebbef · ndn_service_framework::parseResponseNameV2

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L170)

```cpp
std::optional<ResponseNameV2> parseResponseNameV2(const ndn::Name& responseName);
```

### API-16f31992a05d · ndn_service_framework::makeRequestAckNameV2

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L172)

```cpp
ndn::Name makeRequestAckNameV2(const ndn::Name& providerName,
                                   const ndn::Name& requesterName,
                                   const ndn::Name& serviceName,
                                   const ndn::Name& requestId);
```

### API-66acbc694560 · ndn_service_framework::makeRequestAckNameWithoutPrefixV2

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L176)

```cpp
ndn::Name makeRequestAckNameWithoutPrefixV2(const ndn::Name& requesterName,
                                                const ndn::Name& serviceName,
                                                const ndn::Name& requestId);
```

### API-69ff954f1297 · ndn_service_framework::parseRequestAckNameV2

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L179)

```cpp
std::optional<RequestAckNameV2> parseRequestAckNameV2(const ndn::Name& requestAckName);
```

### API-88ff25d9903b · ndn_service_framework::makeServiceSelectionNameV2

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L181)

```cpp
ndn::Name makeServiceSelectionNameV2(const ndn::Name& requesterName,
                                            const ndn::Name& providerName,
                                            const ndn::Name& serviceName,
                                            const ndn::Name& requestId);
```

### API-385c9aa409ec · ndn_service_framework::makeServiceSelectionNameWithoutPrefixV2

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L185)

```cpp
ndn::Name makeServiceSelectionNameWithoutPrefixV2(const ndn::Name& providerName,
                                                         const ndn::Name& serviceName,
                                                         const ndn::Name& requestId);
```

### API-bef2f6e3960d · ndn_service_framework::parseServiceSelectionNameV2

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L188)

```cpp
std::optional<ServiceSelectionNameV2>
    parseServiceSelectionNameV2(const ndn::Name& serviceSelectionName);
```

### API-b3d77b5bc86c · ndn_service_framework::makeServiceSelectionDecisionNameV2

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L190)

```cpp
ndn::Name makeServiceSelectionDecisionNameV2(const ndn::Name& requesterName,
                                                 const ndn::Name& providerName,
                                                 const ndn::Name& serviceName,
                                                 const ndn::Name& requestId,
                                                 uint64_t attempt);
```

### API-ab40586c90d6 · ndn_service_framework::makeServiceSelectionDecisionNameWithoutPrefixV2

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L195)

```cpp
ndn::Name makeServiceSelectionDecisionNameWithoutPrefixV2(
                                                 const ndn::Name& providerName,
                                                 const ndn::Name& serviceName,
                                                 const ndn::Name& requestId,
                                                 uint64_t attempt);
```

### API-533dee72063c · ndn_service_framework::parseServiceSelectionDecisionNameV2

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L200)

```cpp
std::optional<ServiceSelectionDecisionNameV2>
    parseServiceSelectionDecisionNameV2(const ndn::Name& name);
```

### API-1ffbb3570e47 · ndn_service_framework::makeCompactServiceSelectionNameV2

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L202)

```cpp
ndn::Name makeCompactServiceSelectionNameV2(const ndn::Name& requesterName,
                                                const ndn::Name& serviceName,
                                                const ndn::Name& requestId);
```

### API-e53905ff44a3 · ndn_service_framework::makeCompactServiceSelectionNameWithoutPrefixV2

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L205)

```cpp
ndn::Name makeCompactServiceSelectionNameWithoutPrefixV2(const ndn::Name& serviceName,
                                                             const ndn::Name& requestId);
```

### API-fb2fce0aa812 · ndn_service_framework::parseCompactServiceSelectionNameV2

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L207)

```cpp
std::optional<CompactServiceSelectionNameV2>
    parseCompactServiceSelectionNameV2(const ndn::Name& serviceSelectionName);
```

### API-1833840e6ef2 · ndn_service_framework::computeSelectionProviderTokenProofHash

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L209)

```cpp
std::string computeSelectionProviderTokenProofHash(const ndn::Name& requesterName,
                                                       const ndn::Name& providerName,
                                                       const ndn::Name& serviceName,
                                                       const std::string& providerToken);
```

### API-01c54a265bb8 · ndn_service_framework::makeSelectionStatusQueryName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L214)

```cpp
ndn::Name makeSelectionStatusQueryName(const ndn::Name& providerName,
                                           const ndn::Name& serviceName,
                                           const std::string& selectionDigest);
```

### API-c6e881e54133 · ndn_service_framework::parseSelectionStatusQueryName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L217)

```cpp
std::optional<SelectionStatusQueryName>
    parseSelectionStatusQueryName(const ndn::Name& statusQueryName);
```

### API-7e1feb8867fe · ndn_service_framework::computeSelectionDigest

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L219)

```cpp
std::string computeSelectionDigest(const ServiceSelectionMessage& message);
```

### API-fbe2ea4a3f2f · ndn_service_framework::computeSelectionDigestWithoutKeyEnvelope

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L225)

```cpp
std::string computeSelectionDigestWithoutKeyEnvelope(
        const ServiceSelectionMessage& message);
```

原始接口说明：

```text
/** Compute the pre-envelope Selection digest used by request-scoped key
     * bindings.  The RSA envelope is excluded to avoid a circular digest;
     * the final Selection wire digest remains available from
     * computeSelectionDigest(). */
```

### API-a9aaa21180a2 · ndn_service_framework::makeOpaqueControlHandle

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L228)

```cpp
std::string makeOpaqueControlHandle(size_t bytes = 24);
```

### API-fdb14258a5d9 · ndn_service_framework::isValidOpaqueControlHandle

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L229)

```cpp
bool isValidOpaqueControlHandle(const std::string& handle);
```

### API-c490bfc5576b · ndn_service_framework::makeSecureSelectionStatusName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L230)

```cpp
ndn::Name makeSecureSelectionStatusName(const ndn::Name& providerName,
                                            uint64_t version,
                                            const std::string& statusHandle);
```

### API-a3125456c436 · ndn_service_framework::parseSecureSelectionStatusName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L233)

```cpp
std::optional<SecureSelectionStatusName>
    parseSecureSelectionStatusName(const ndn::Name& name);
```

### API-d3d21490d3f9 · ndn_service_framework::makeProviderReadyName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L235)

```cpp
ndn::Name makeProviderReadyName(const ndn::Name& requesterName,
                                    uint64_t version,
                                    const std::string& controlHandle);
```

### API-cb7fa70f1cd0 · ndn_service_framework::parseProviderReadyName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L238)

```cpp
std::optional<ProviderReadyName> parseProviderReadyName(const ndn::Name& name);
```

### API-d3e29152a447 · ndn_service_framework::makeExecutionActivateName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L239)

```cpp
ndn::Name makeExecutionActivateName(const ndn::Name& providerName,
                                        uint64_t version,
                                        const std::string& controlHandle);
```

### API-7ef076a90e61 · ndn_service_framework::parseExecutionActivateName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L242)

```cpp
std::optional<ExecutionActivateName> parseExecutionActivateName(const ndn::Name& name);
```

### API-3fcf347366ca · ndn_service_framework::makeCollaborationDataName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L244)

```cpp
ndn::Name makeCollaborationDataName(const ndn::Name& producerName,
                                        const ndn::Name& requesterName,
                                        const ndn::Name& requestId,
                                        const std::string& keyScope,
                                        const ndn::Name& topic,
                                        uint64_t sequence);
```

### API-e468ce279ae3 · ndn_service_framework::parseCollaborationDataName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L250)

```cpp
std::optional<CollaborationDataName>
    parseCollaborationDataName(const ndn::Name& collaborationDataName);
```

### API-9f0585c744fd · ndn_service_framework::encodeLargeDataReferencePayload

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L253)

```cpp
ndn::Buffer encodeLargeDataReferencePayload(const LargeDataReference& reference);
```

### API-c84978b39adf · ndn_service_framework::parseLargeDataReferencePayload

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L254)

```cpp
std::optional<LargeDataReference> parseLargeDataReferencePayload(const ndn::Buffer& payload);
```

### API-af4c372f60d5 · ndn_service_framework::isLargeDataReferencePayload

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L255)

```cpp
bool isLargeDataReferencePayload(const ndn::Buffer& payload);
```

### API-8709dd1c5f7a · ndn_service_framework::registerInterestFilterWithRetry

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L262)

```cpp
void registerInterestFilterWithRetry(
        ndn::Face& face,
        const ndn::Name& prefix,
        std::function<void(const ndn::InterestFilter&, const ndn::Interest&)> onInterest,
        std::function<void(const ndn::Name&)> onSuccess,
        std::function<void(const ndn::Name&, const std::string&)> onFailure,
        size_t attempts,
        std::chrono::milliseconds delayBetweenAttempts);
```

原始接口说明：

```text
// Register one Interest filter with a bounded retry on transient RIB
// authorization rejections.  NFD's offline command authenticator can
// miss the freshly created signer certificate while the node PIB is
// briefly locked by a concurrent keychain operation; a short bounded
// retry avoids turning that race into a fatal startup failure.
```

### API-d0fd890274a6 · ndn_service_framework::CombineSegmentsIntoBuffer

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L273)

```cpp
std::shared_ptr<ndn::Buffer> CombineSegmentsIntoBuffer(ndn::nacabe::SPtrVector<ndn::Data> segments);
```

原始接口说明：

```text
// /muas/drone1/NDNSF/TOKEN/ObjectDetection/YOLOv8/0
// <provider> <service> <function> <seqNum>
```

### API-5debd4c7f01e · ndn_service_framework::NameToRegexString

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L275)

```cpp
std::string NameToRegexString(ndn::Name& name);
```

### API-b437a32734af · ndn_service_framework::RandomString

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L277)

```cpp
std::string RandomString(const int len);
```

### API-8c8bb30785c4 · ndn_service_framework::GetAttributesByName

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L279)

```cpp
std::optional<std::vector<std::string>> GetAttributesByName(const ndn::Name& name);
```

### API-724503b5e594 · ndn_service_framework::blockToSpan

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L282)

```cpp
ndn::span<const uint8_t> blockToSpan(const ndn::Block& block);
```

原始接口说明：

```text
// ndn::Block to ndn::span<const uint8_t>
```

### API-b39361bb85e0 · ndn_service_framework::ConcatenateString

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L285)

```cpp
std::string ConcatenateString(const std::vector<std::string>& strs);
```

原始接口说明：

```text
// concatnate std::vector<std::string> using " and "
```

### API-a556ce8e8670 · ndn_service_framework::mergeDataContents

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L288)

```cpp
std::vector<uint8_t> mergeDataContents(const std::vector<std::shared_ptr<ndn::Data>>& dataPackets);
```

原始接口说明：

```text
// Function to merge content values of multiple ndn::Data objects into a uint8_t array
```

### API-9fc2062cbeb4 · ndn_service_framework::encryptPermissionResponseForCertificate

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L292)

```cpp
EncryptedPermissionResponse
    encryptPermissionResponseForCertificate(const PermissionResponse& response,
                                            const ndn::security::Certificate& recipientCert);
```

原始接口说明：

```text
// PermissionResponse-only encryption helpers. These do not use NAC-ABE.
// Intended algorithm: RSA-wrapped AES-CBC.
```

### API-0ff254455223 · ndn_service_framework::decryptPermissionResponseWithKeyChain

public / declared-interface；[源码](../../ndn-service-framework/utils.hpp#L296)

```cpp
PermissionResponse
    decryptPermissionResponseWithKeyChain(const EncryptedPermissionResponse& encryptedResponse,
                                          const ndn::security::KeyChain& keyChain);
```

## pythonWrapper/ndnsf/__init__.py

源码 SHA-256：`60110c2159ed701b88c6df7da8dbf6adc6b3f545c24d033ff9d3565506c9103a`。

显式导出：`DeploymentIntent`, `ProviderCapabilityOffer`, `DeploymentPlan`, `ProviderReadyMessage`, `ReadyAcknowledgement`, `ExecutionActivateMessage`, `SecureStatusQuery`, `SecureStatusSnapshot`, `EncryptedRequestInput`, `SelectionInputKeyOffer`, `SelectionInputKeyGrant`, `ReservationLease`, `SelectionDecision`, `SelectionDecisionReceipt`, `RecipientEncryptedAssignment`, `StageInputEvidence`, `StageAbort`, `SelectionDecisionTombstone`, `make_opaque_control_handle`, `is_valid_opaque_control_handle`, `AckCompatibilityCounters`, `AckCompatibilityMode`, `AckMetadataDecodeError`, `ApplicationConfig`, `AckCandidate`, `AckDecision`, `AllowedService`, `CollaborationAssignment`, `CollaborationAckClosed`, `CollaborationContext`, `CollaborationData`, `VerifiedCollaborationData`, `CollaborationDependency`, `CollaborationRole`, `CollaborationInvocation`, `ControllerConfig`, `AdaptiveSegmentFetchResult`, `DataPacket`, `DataProductReference`, `DRAIN_ACTIVE`, `DRAIN_DRAINING`, `DRAIN_MAINTENANCE`, `DRAIN_OFFLINE`, `DRAIN_PROVISIONING`, `DRAIN_READY`, `DRAIN_UNAVAILABLE`, `ExecutionLeaseBinding`, `ExecutionLeaseCounters`, `ExecutionLeaseResult`, `ExecutionLeaseState`, `GenericExecutionLease`, `LargeDataReference`, `LargeDataPublishResult`, `SignedAppDataResult`, `NEGATIVE_ACK_REASON_GPU_BUSY`, `NEGATIVE_ACK_REASON_INTERNAL_ERROR`, `NEGATIVE_ACK_REASON_MODEL_UNAVAILABLE`, `NEGATIVE_ACK_REASON_PERMISSION_DENIED`, `NEGATIVE_ACK_REASON_PROVIDER_BUSY`, `NEGATIVE_ACK_REASON_QUEUE_FULL`, `NEGATIVE_ACK_REASON_UNSUPPORTED_REQUEST`, `NDNSFSession`, `NdnProcess`, `NdnRuntime`, `ProcessResult`, `ProviderConfig`, `CollaborationSelectionStatus`, `CollaborationDeadlineExceeded`, `DeadlineMonitor`, `DeadlineTerminal`, `ProviderCapabilityHint`, `ProviderCapabilityAckDecodeResult`, `ProviderExecutionLeaseTable`, `ProgressDecision`, `ProgressObservation`, `NON_READY_DRAIN_STATES`, `RECOMMENDED_NEGATIVE_ACK_REASONS`, `RECOMMENDED_REJECTION_REASONS`, `RejectionReason`, `RuntimeHint`, `SegmentHintRange`, `SegmentedObjectProducer`, `ServiceController`, `ServiceDiscoveryRecord`, `ServiceDiscoverySnapshot`, `ServiceOperationState`, `ServiceOperationStatus`, `ServiceProvider`, `ProviderSigningMetadata`, `ServiceResponse`, `StreamedInvocation`, `StreamedInvocationError`, `StreamedInvocationMetrics`, `StreamedInvocationOptions`, `ServiceUser`, `StoredDataProducer`, `decode_data_packet`, `decode_provider_capability_ack`, `STREAM_CHUNK_MAGIC`, `STREAM_NAME_MAP_CONTENT_TYPE_MANIFEST`, `STREAM_NAME_MAP_CONTRACT_VERSION`, `STREAM_NAME_MAP_CONTRACT_VERSION_V1`, `STREAM_NAME_MAP_CONTRACT_VERSION_V2`, `SampleClassProfile`, `SampleClassPredictionStatus`, `LiveStreamSamplePredictor`, `LiveStreamSampleReservation`, `StreamAdaptiveFetcherState`, `StreamAdvancedOptions`, `StreamChunk`, `StreamConfig`, `StreamConsumerReorderBuffer`, `StreamCursorFrontiers`, `StreamFecInfo`, `StreamFetchDecision`, `StreamHealth`, `StreamHealthState`, `StreamInfo`, `StreamMetrics`, `StreamNameMapAdmissionResult`, `StreamNameMapBlock`, `StreamNameMapCheckpoint`, `StreamNameMapEntry`, `StreamNameMapResolution`, `StreamNameMapResolverConfig`, `StreamNameResolver`, `StreamProducerBuffer`, `StreamPublisher`, `StreamSubscriptionOptions`, `PredictiveStreamCheckpoint`, `PredictiveStreamDescriptor`, `PredictiveStreamSubscriber`, `LiveStreamConsumerHandle`, `LiveStreamDefinition`, `LiveStreamDescriptor`, `LiveStreamFecOptions`, `LiveStreamGroupReservation`, `LiveStreamItemAdmission`, `LiveStreamItemReservation`, `LiveStreamPublisher`, `PublishedLiveStreamPacket`, `PublishedPacketFeed`, `VerifiedLiveStreamItem`, `UserConfig`, `READY_DRAIN_STATES`, `decode_stream_chunk`, `encode_provider_capability_ack`, `encode_large_data_reference_payload`, `encode_stream_chunk`, `make_stream_name_map_block_name`, `make_stream_name_map_root`, `fetch_segmented_object`, `fetch_segmented_object_with_segment_hints`, `fetch_known_segmented_object_with_segment_hints`, `fetch_adaptive_segmented_data_packets`, `fetch_segmented_data_packets`, `fetch_exact_data_packet`, `is_recommended_rejection_reason`, `make_segmented_data_packets`, `make_signed_data`, `make_predictive_data_name`, `normalize_drain_state`, `parse_large_data_reference_payload`, `provider_ready_for_new_request`, `stream_now_ms`。

## pythonWrapper/ndnsf/api.py

源码 SHA-256：`a3a8fbbacf7c05c51034f2b0dccefcac3d9e22a979fcb0c512fa0240bb35fc6b`。

### API-430e8331f4a6 · ApplicationConfig

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L20)

```python
class ApplicationConfig:
```

原始接口说明：

```text
Generic NDNSF application process description.
```

### API-fcff5cfcea94 · ApplicationConfig.name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L23)

```python
name: str
```

### API-b68db5fcdf19 · ApplicationConfig.binary

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L24)

```python
binary: str
```

### API-5cd7781a267a · ApplicationConfig.args

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L25)

```python
args: tuple[str, ...] = ()
```

### API-69d96fa84a7a · ApplicationConfig.env

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L26)

```python
env: Mapping[str, str] = field(default_factory=dict)
```

### API-38551fa525d9 · ControllerConfig

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L30)

```python
class ControllerConfig:
```

原始接口说明：

```text
ServiceController process configuration.
```

### API-e66414b9da19 · ControllerConfig.policy_file

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L33)

```python
policy_file: str
```

### API-4639136e5bc2 · ControllerConfig.name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L34)

```python
name: str = "controller"
```

### API-6dabdfbe2153 · ControllerConfig.binary

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L35)

```python
binary: str = "App_ServiceController"
```

### API-8ae15a07467a · ControllerConfig.bootstrap_token_file

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L36)

```python
bootstrap_token_file: str = ""
```

### API-cf33d4f839e8 · ControllerConfig.extra_args

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L37)

```python
extra_args: tuple[str, ...] = ()
```

### API-f494eb5fe329 · ControllerConfig.env

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L38)

```python
env: Mapping[str, str] = field(default_factory=dict)
```

### API-dd760a708641 · ControllerConfig.as_application

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L40)

```python
def as_application(self) -> ApplicationConfig:
```

### API-e29a5a9d0a34 · ProviderConfig

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L54)

```python
class ProviderConfig:
```

原始接口说明：

```text
Provider process configuration.

The optional service and role fields are metadata for Python orchestration
and logs. Concrete command-line arguments remain application-defined.
```

### API-372d6702ad89 · ProviderConfig.name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L61)

```python
name: str
```

### API-6f7932766d32 · ProviderConfig.binary

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L62)

```python
binary: str
```

### API-68071705a4b3 · ProviderConfig.args

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L63)

```python
args: tuple[str, ...] = ()
```

### API-d8dacd077d96 · ProviderConfig.service

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L64)

```python
service: str = ""
```

### API-5012f564afd6 · ProviderConfig.role

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L65)

```python
role: str = ""
```

### API-faeafd0f65ca · ProviderConfig.bootstrap_token

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L66)

```python
bootstrap_token: str = ""
```

### API-fd325f24a549 · ProviderConfig.env

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L67)

```python
env: Mapping[str, str] = field(default_factory=dict)
```

### API-d557228ad44b · ProviderConfig.as_application

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L69)

```python
def as_application(self) -> ApplicationConfig:
```

### API-3c48087d4f6b · UserConfig

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L77)

```python
class UserConfig:
```

原始接口说明：

```text
User/client process configuration.
```

### API-172a61083854 · UserConfig.name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L80)

```python
name: str
```

### API-a2be150faf45 · UserConfig.binary

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L81)

```python
binary: str
```

### API-7381874a539f · UserConfig.args

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L82)

```python
args: tuple[str, ...] = ()
```

### API-f95b58dbb406 · UserConfig.service

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L83)

```python
service: str = ""
```

### API-ece1c621b9d0 · UserConfig.bootstrap_token

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L84)

```python
bootstrap_token: str = ""
```

### API-245884570a25 · UserConfig.env

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L85)

```python
env: Mapping[str, str] = field(default_factory=dict)
```

### API-45551dde0e48 · UserConfig.as_application

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L87)

```python
def as_application(self) -> ApplicationConfig:
```

### API-685ebbef24b3 · NDNSFSession

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L94)

```python
class NDNSFSession:
```

原始接口说明：

```text
High-level reusable Python orchestration API for NDNSF applications.
```

### API-32da50e55975 · NDNSFSession.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L97)

```python
def __init__(
        self,
        repo_root: Optional[Path] = None,
        log_dir: Optional[Path] = None,
        *,
        binary_dir: Optional[Path] = None,
        cwd: Optional[Path] = None,
        library_dirs: Optional[Iterable[Path]] = None,
    ) -> None:
```

### API-7554d737fc25 · NDNSFSession.repo_root

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L115)

```python
def repo_root(self) -> Optional[Path]:
```

### API-a8eff627fed2 · NDNSFSession.command_for

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L118)

```python
def command_for(self, app: ApplicationConfig) -> list[str]:
```

### API-5b95fa9ed0fe · NDNSFSession.commands_for

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L124)

```python
def commands_for(self, apps: Iterable[ApplicationConfig]) -> list[list[str]]:
```

### API-e2050721552e · NDNSFSession.configure_svs_group

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L127)

```python
def configure_svs_group(
        self,
        sync_prefix: str,
        strategy: str = "/localhost/nfd/strategy/multicast",
    ) -> None:
```

### API-5ded5e0289fa · NDNSFSession.start_application

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L134)

```python
def start_application(self, app: ApplicationConfig) -> NdnProcess:
```

### API-814adbc997db · NDNSFSession.start_controller

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L142)

```python
def start_controller(self, config: ControllerConfig) -> NdnProcess:
```

### API-ef28ce80a828 · NDNSFSession.start_provider

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L145)

```python
def start_provider(self, config: ProviderConfig) -> NdnProcess:
```

### API-9b4a4aaa4ccb · NDNSFSession.start_user

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L148)

```python
def start_user(self, config: UserConfig) -> NdnProcess:
```

### API-842069478c09 · NDNSFSession.run_application

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L151)

```python
def run_application(
        self,
        app: ApplicationConfig,
        timeout: Optional[float] = None,
    ) -> ProcessResult:
```

### API-0a80d0cf8a25 · NDNSFSession.run_user

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L167)

```python
def run_user(self, config: UserConfig, timeout: Optional[float] = None) -> ProcessResult:
```

### API-0f4fb36a8141 · NDNSFSession.stop_all

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/api.py#L170)

```python
def stop_all(self) -> list[ProcessResult]:
```

## pythonWrapper/ndnsf/deployment_control.py

源码 SHA-256：`0c167f8470fb9fc56fe4c3c47af64ae84a80fe698d765dc865d0d163a748b4cf`。

显式导出：`DeploymentIntent`, `ProviderCapabilityOffer`, `DeploymentPlan`, `ProviderReadyMessage`, `ReadyAcknowledgement`, `ExecutionActivateMessage`, `SecureStatusQuery`, `SecureStatusSnapshot`, `EncryptedRequestInput`, `SelectionInputKeyOffer`, `SelectionInputKeyGrant`, `ReservationLease`, `SelectionDecision`, `SelectionDecisionReceipt`, `RecipientEncryptedAssignment`, `StageInputEvidence`, `StageAbort`, `SelectionDecisionTombstone`, `make_opaque_control_handle`, `is_valid_opaque_control_handle`。

### API-dd074130545b · make_opaque_control_handle

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/deployment_control.py#L27)

```python
def make_opaque_control_handle(random_bytes: int = 24) -> str:
```

### API-464a66112bb7 · is_valid_opaque_control_handle

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/deployment_control.py#L31)

```python
def is_valid_opaque_control_handle(handle: str) -> bool:
```

## pythonWrapper/ndnsf/metrics.py

源码 SHA-256：`4d6d949f2c2e82e1bb370d5127671b67812f2b217fe0c7c020c1dc39615e309a`。

### API-8224c6646baf · NdnMetrics

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/metrics.py#L87)

```python
class NdnMetrics:
```

### API-460a3fd26493 · NdnMetrics.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/metrics.py#L88)

```python
def __init__(self):
```

### API-4fa37db30998 · NdnMetrics.dumps

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/metrics.py#L123)

```python
def dumps(self) -> str:
```

### API-78fda0a52a66 · start_metrics_server

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/metrics.py#L162)

```python
def start_metrics_server(metrics: NdnMetrics, port: int = 9090,
                         bind: str = "127.0.0.1") -> None:
```

原始接口说明：

```text
Start a lightweight HTTP server serving /metrics in a daemon thread.
```

## pythonWrapper/ndnsf/ndnsd_health.py

源码 SHA-256：`7408ebaf8bec76c1596d4bfa6a046d94e16e5b45dc6b12f6c85aed731eab9a97`。

### API-e87b7c8b9f61 · NdnsdProviderState

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/ndnsd_health.py#L28)

```python
class NdnsdProviderState:
```

### API-c0aa57e9f532 · NdnsdProviderState.provider

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/ndnsd_health.py#L29)

```python
provider: str = ""
```

### API-b4a1654b3814 · NdnsdProviderState.service_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/ndnsd_health.py#L30)

```python
service_name: str = ""
```

### API-fae653cb432f · NdnsdProviderState.service_lifetime_s

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/ndnsd_health.py#L31)

```python
service_lifetime_s: int = 30
```

### API-bddbfb9398c3 · NdnsdProviderState.publish_timestamp_s

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/ndnsd_health.py#L32)

```python
publish_timestamp_s: int = 0
```

### API-8d684d2ed42b · NdnsdProviderState.meta

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/ndnsd_health.py#L33)

```python
meta: dict[str, str] = field(default_factory=dict)
```

### API-d1f5f104fb18 · NdnsdProviderState.age_s

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/ndnsd_health.py#L36)

```python
def age_s(self) -> float:
```

### API-57a5d945298b · NdnsdProviderState.is_fresh

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/ndnsd_health.py#L40)

```python
def is_fresh(self) -> bool:
```

### API-dbf169fd6196 · NdnsdProviderState.freshness_ratio

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/ndnsd_health.py#L44)

```python
def freshness_ratio(self) -> float:
```

原始接口说明：

```text
1.0 = just published; 0.0 = at or past TTL.
```

### API-51970bdcb653 · NdnsdProviderState.meta_int

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/ndnsd_health.py#L50)

```python
def meta_int(self, key: str, default: int = 0) -> int:
```

### API-b37091da2534 · NdnsdProviderState.meta_str

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/ndnsd_health.py#L56)

```python
def meta_str(self, key: str, default: str = "") -> str:
```

### API-9bb846fce276 · NdnsdHealthTracker

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/ndnsd_health.py#L60)

```python
class NdnsdHealthTracker:
```

原始接口说明：

```text
Tracks provider health from NDNSD service discovery updates.

Providers are scored on freshness, capacity, and stability.
A circuit breaker opens when a provider misses its TTL.
```

### API-ee3a4532df0c · NdnsdHealthTracker.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/ndnsd_health.py#L67)

```python
def __init__(self,
                 *,
                 stale_threshold_ratio: float = 0.8,
                 circuit_open_after_missed: int = 2,
                 half_open_probe_after_s: float = 5.0):
```

### API-d6082b1eb316 · NdnsdHealthTracker.update_from_ndnsd

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/ndnsd_health.py#L84)

```python
def update_from_ndnsd(self, services: list[dict[str, Any]]) -> None:
```

原始接口说明：

```text
Ingest NDNSD service details from ``ServiceUser.get_ndnsd_services()``.
```

### API-f4f132fe5428 · NdnsdHealthTracker.health_score

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/ndnsd_health.py#L129)

```python
def health_score(self, provider: str) -> float:
```

原始接口说明：

```text
Return 0.0 (worst) to 1.0 (best) health score for a provider.

Unknown providers (never seen on NDNSD) default to 1.0 — they may
simply not have NDNSD enabled. Only providers that have published
and then went stale get degraded scores.
```

### API-a5f796e85c06 · NdnsdHealthTracker.circuit_breaker_state

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/ndnsd_health.py#L153)

```python
def circuit_breaker_state(self, provider: str) -> str:
```

原始接口说明：

```text
Return CLOSED, OPEN, or HALF_OPEN for a provider.

Unknown providers default to CLOSED (they may not have NDNSD enabled).
```

### API-ba67763abb68 · NdnsdHealthTracker.is_available

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/ndnsd_health.py#L164)

```python
def is_available(self, provider: str) -> bool:
```

原始接口说明：

```text
Return True if the provider should receive requests.
```

### API-264ecfb3e77d · NdnsdHealthTracker.provider_capacity

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/ndnsd_health.py#L173)

```python
def provider_capacity(self, provider: str) -> dict[str, Any]:
```

原始接口说明：

```text
Return observed capacity from NDNSD meta info.
```

### API-5fe67710289f · NdnsdHealthTracker.all_providers

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/ndnsd_health.py#L190)

```python
def all_providers(self) -> list[str]:
```

### API-94b60b9bf01e · NdnsdHealthTracker.snapshot

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/ndnsd_health.py#L193)

```python
def snapshot(self) -> dict[str, Any]:
```

## pythonWrapper/ndnsf/progress_deadline.py

源码 SHA-256：`c0deef2af779cc230e84ae99a73ffe1252302f25b4c07520bcfaee2d7b1c115e`。

### API-972050c81865 · DeadlineTerminal

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L10)

```python
class DeadlineTerminal(str, Enum):
```

### API-5fa07d2a661b · ProgressObservation

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L19)

```python
class ProgressObservation:
```

### API-7222fce1d2a7 · ProgressObservation.request_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L20)

```python
request_id: str
```

### API-cf7f3d3c12d9 · ProgressObservation.operation_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L21)

```python
operation_id: str
```

### API-625d30512b42 · ProgressObservation.provider

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L22)

```python
provider: str
```

### API-9e0ec967d740 · ProgressObservation.role

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L23)

```python
role: str
```

### API-e299eb31d95e · ProgressObservation.attempt

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L24)

```python
attempt: int
```

### API-713d3ce3ac8c · ProgressObservation.epoch

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L25)

```python
epoch: int
```

### API-b2efc211f401 · ProgressObservation.sequence

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L26)

```python
sequence: int
```

### API-6b7b1b7ef0f7 · ProgressObservation.phase

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L27)

```python
phase: str
```

### API-be749bcf0461 · ProgressObservation.completed_work

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L28)

```python
completed_work: int
```

### API-feb1b4400ca2 · ProgressObservation.total_work

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L29)

```python
total_work: Optional[int]
```

### API-41d1493daead · ProgressObservation.authenticated

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L30)

```python
authenticated: bool
```

### API-7eb0153af8a0 · ProgressObservation.observed_at

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L31)

```python
observed_at: Optional[float] = None
```

### API-fae661fd08ec · ProgressObservation.checkpoint

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L32)

```python
checkpoint: str = ""
```

### API-d5359a245316 · ProgressDecision

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L36)

```python
class ProgressDecision:
```

### API-0e9652e1d2cc · ProgressDecision.admitted

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L37)

```python
admitted: bool
```

### API-588dc091d9d3 · ProgressDecision.renewed

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L38)

```python
renewed: bool
```

### API-a7cef60d1601 · ProgressDecision.reason

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L39)

```python
reason: str
```

### API-76a25d18ee42 · ProgressDecision.idle_deadline

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L40)

```python
idle_deadline: float
```

### API-c741453a75a6 · ProgressDecision.hard_deadline

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L41)

```python
hard_deadline: float
```

### API-c78d67358846 · ProgressDecision.last_checkpoint

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L42)

```python
last_checkpoint: str = ""
```

### API-d234f8c43e5e · DeadlineMonitor

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L45)

```python
class DeadlineMonitor:
```

原始接口说明：

```text
State machine whose clock and identity are fixed at construction.
```

### API-ba6dc8409407 · DeadlineMonitor.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L48)

```python
def __init__(
        self,
        *,
        request_id: str,
        operation_id: str,
        provider: str,
        role: str,
        attempt: int,
        idle_budget: float,
        hard_budget: float,
        clock: Callable[[], float],
        phase_order: tuple[str, ...] = (),
    ) -> None:
```

### API-8a243e1822dc · DeadlineMonitor.poll

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L99)

```python
def poll(self) -> Optional[DeadlineTerminal]:
```

### API-12c6449381ae · DeadlineMonitor.finish

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L102)

```python
def finish(
        self, terminal: DeadlineTerminal, *, now: Optional[float] = None
    ) -> bool:
```

### API-7fc72d84cb94 · DeadlineMonitor.admit

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L130)

```python
def admit(self, observation: ProgressObservation) -> ProgressDecision:
```

### API-4f1151c7f705 · DeadlineMonitor.last_checkpoint

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L207)

```python
def last_checkpoint(self) -> str:
```

### API-208e6afc9463 · DeadlineMonitor.terminal_evidence

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/progress_deadline.py#L210)

```python
def terminal_evidence(self) -> dict[str, object]:
```

原始接口说明：

```text
Return bounded operator evidence for a terminal deadline outcome.
```

## pythonWrapper/ndnsf/runtime.py

源码 SHA-256：`4f68e59df9f9b1570864a5bcdf35f2bacd0a04b01ebae021f8acaf691ebb30d6`。

### API-93a5f2e09dee · ProcessResult

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime.py#L16)

```python
class ProcessResult:
```

原始接口说明：

```text
Captured result of a completed NDNSF process.
```

### API-4d2e579a965e · ProcessResult.name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime.py#L19)

```python
name: str
```

### API-89bedb7ac8e2 · ProcessResult.returncode

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime.py#L20)

```python
returncode: int
```

### API-bbe328ddec57 · ProcessResult.stdout

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime.py#L21)

```python
stdout: str
```

### API-2e174a974bf8 · ProcessResult.stderr

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime.py#L22)

```python
stderr: str
```

### API-b54b7b77db3a · NdnProcess

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime.py#L25)

```python
class NdnProcess:
```

原始接口说明：

```text
Small lifecycle wrapper around a C++ NDNSF process.
```

### API-3ac67f030ebc · NdnProcess.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime.py#L28)

```python
def __init__(
        self,
        name: str,
        command: Sequence[str],
        *,
        cwd: Path,
        env: Optional[Mapping[str, str]] = None,
        stdout_path: Optional[Path] = None,
        stderr_path: Optional[Path] = None,
    ) -> None:
```

### API-5148d9f5dbaf · NdnProcess.pid

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime.py#L49)

```python
def pid(self) -> Optional[int]:
```

### API-0fd4d61536bf · NdnProcess.start

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime.py#L52)

```python
def start(self) -> "NdnProcess":
```

### API-f600ecb3899b · NdnProcess.wait

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime.py#L78)

```python
def wait(self, timeout: Optional[float] = None) -> ProcessResult:
```

### API-27eae6087161 · NdnProcess.terminate

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime.py#L85)

```python
def terminate(self, timeout: float = 5.0) -> ProcessResult:
```

### API-44b3e949f77b · NdnRuntime

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime.py#L105)

```python
class NdnRuntime:
```

原始接口说明：

```text
Factory and lifecycle manager for NDNSF example binaries.
```

### API-0e660323cfc9 · NdnRuntime.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime.py#L108)

```python
def __init__(
        self,
        repo_root: Optional[Path] = None,
        log_dir: Optional[Path] = None,
        *,
        binary_dir: Optional[Path] = None,
        cwd: Optional[Path] = None,
        library_dirs: Optional[Iterable[Path]] = None,
    ) -> None:
```

### API-95276aaeba13 · NdnRuntime.binary

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime.py#L125)

```python
def binary(self, name: str) -> Path:
```

### API-ea311f93ec89 · NdnRuntime.make_process

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime.py#L176)

```python
def make_process(
        self,
        name: str,
        binary: str,
        args: Iterable[str] = (),
        *,
        env: Optional[Mapping[str, str]] = None,
    ) -> NdnProcess:
```

### API-1003ff401c35 · NdnRuntime.start_process

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime.py#L209)

```python
def start_process(
        self,
        name: str,
        binary: str,
        args: Iterable[str] = (),
        *,
        env: Optional[Mapping[str, str]] = None,
    ) -> NdnProcess:
```

### API-492ff0bb57ec · NdnRuntime.start_controller

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime.py#L221)

```python
def start_controller(
        self,
        *,
        policy_file: str,
        bootstrap_token_file: str = "",
        extra_args: Iterable[str] = (),
        env: Optional[Mapping[str, str]] = None,
    ) -> NdnProcess:
```

### API-fb6aacaa6e56 · NdnRuntime.configure_local_strategy

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime.py#L235)

```python
def configure_local_strategy(
        self,
        prefix: str,
        strategy: str = "/localhost/nfd/strategy/multicast",
    ) -> None:
```

### API-88f4d5ea0eab · NdnRuntime.stop_all

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime.py#L247)

```python
def stop_all(self) -> list[ProcessResult]:
```

### API-8bea885c66e2 · wait_for_startup

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime.py#L261)

```python
def wait_for_startup(seconds: float) -> None:
```

原始接口说明：

```text
Wait for controller/provider permission discovery and SVS subscription.
```

## pythonWrapper/ndnsf/runtime_telemetry.py

源码 SHA-256：`f5ee9f3950bbad5bdb6c2dcf2c4e78636e9c493ddcd053825e485b01d92ff999`。

### API-e4c062ef68f4 · TokenBucket

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L40)

```python
class TokenBucket:
```

原始接口说明：

```text
Token-bucket rate limiter (thread-safe).

Usage::

    limiter = TokenBucket(rate_per_second=10.0, burst=20)
    if limiter.consume():
        ...  # request allowed
    else:
        ...  # rate-limited
```

### API-fc5cff1e8876 · TokenBucket.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L52)

```python
def __init__(self, rate_per_second: float, burst: int = 0):
```

### API-fe4476efbedc · TokenBucket.consumed

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L62)

```python
def consumed(self) -> int:
```

### API-bc0cc77b491a · TokenBucket.dropped

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L66)

```python
def dropped(self) -> int:
```

### API-7834c6fe4f79 · TokenBucket.consume

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L69)

```python
def consume(self, tokens: float = 1.0) -> bool:
```

### API-782229f7f78f · TokenBucket.reset

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L85)

```python
def reset(self) -> None:
```

### API-c777627a281f · now_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L98)

```python
def now_ms() -> int:
```

### API-8f7a89897e4f · to_plain

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L102)

```python
def to_plain(value: Any) -> Any:
```

### API-baf977f86944 · stable_json

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L116)

```python
def stable_json(payload: Any) -> str:
```

### API-ae3ee123f8e3 · stable_digest

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L120)

```python
def stable_digest(payload: Any, *, length: int = 16) -> str:
```

### API-0e4c6a6ad277 · write_json

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L124)

```python
def write_json(path: str | Path, payload: Any) -> None:
```

### API-0d39257d6f3f · read_json

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L131)

```python
def read_json(path: str | Path) -> dict[str, Any]:
```

### API-3c03d14d79dd · encode_ack_metadata

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L152)

```python
def encode_ack_metadata(fields: dict[str, Any]) -> bytes:
```

原始接口说明：

```text
Encode typed metadata as legacy-compatible ``key=value;`` fields.
```

### API-31ee7e306162 · parse_ack_metadata

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L178)

```python
def parse_ack_metadata(payload: bytes | str) -> dict[str, Any]:
```

### API-964b6ae5879d · AckCompatibilityMode

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L208)

```python
class AckCompatibilityMode(str, Enum):
```

### API-256b55819263 · AckMetadataDecodeError

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L213)

```python
class AckMetadataDecodeError(ValueError):
```

原始接口说明：

```text
Typed ACK metadata is absent, malformed, or has an unknown schema.
```

### API-c737378b2b45 · AckCompatibilityCounters

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L217)

```python
class AckCompatibilityCounters:
```

原始接口说明：

```text
Thread-safe process-local counters for the bounded legacy reader epoch.
```

### API-50deb42b4c33 · AckCompatibilityCounters.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L220)

```python
def __init__(self) -> None:
```

### API-af04bebb2c85 · AckCompatibilityCounters.reset

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L224)

```python
def reset(self) -> None:
```

### API-1ac1673b54cd · AckCompatibilityCounters.increment

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L236)

```python
def increment(self, key: str, *, fields: Iterable[str] = ()) -> None:
```

### API-29cca9fd74b7 · AckCompatibilityCounters.snapshot

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L243)

```python
def snapshot(self) -> dict[str, Any]:
```

### API-8edfbe770192 · ProviderCapabilityAckDecodeResult

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L252)

```python
class ProviderCapabilityAckDecodeResult:
```

### API-cabe8eba9b8d · ProviderCapabilityAckDecodeResult.hint

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L253)

```python
hint: "ProviderCapabilityHint"
```

### API-a525b0e09123 · ProviderCapabilityAckDecodeResult.source

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L254)

```python
source: str
```

### API-e0a9d2d17049 · ProviderCapabilityAckDecodeResult.fields

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L255)

```python
fields: dict[str, Any] = field(default_factory=dict)
```

### API-89bf4b6a4f08 · ProviderCapabilityAckDecodeResult.conflicting_fields

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L256)

```python
conflicting_fields: tuple[str, ...] = ()
```

### API-b3b4c2cd5469 · ProviderCapabilityAckDecodeResult.counters

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L257)

```python
counters: dict[str, Any] = field(default_factory=dict)
```

### API-2d343e47aff6 · AdmissionLeaseStatus

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L351)

```python
class AdmissionLeaseStatus(str, Enum):
```

### API-5bc4993cf2f6 · RejectionReason

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L360)

```python
class RejectionReason(str, Enum):
```

原始接口说明：

```text
Core reason-code vocabulary for negative ACKs and admission decisions.
```

### API-1ea111e0691e · RECOMMENDED_REJECTION_REASONS

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L375)

```python
RECOMMENDED_REJECTION_REASONS: tuple[str, ...] = tuple(item.value for item in RejectionReason)
```

### API-7dde9fcdfc95 · is_recommended_rejection_reason

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L378)

```python
def is_recommended_rejection_reason(reason: str) -> bool:
```

### API-7c9b4a7f8e58 · RESIDENCY_READY_COST_MS

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L386)

```python
RESIDENCY_READY_COST_MS: dict[str, float] = {
    "GPU_LOADED": 0.0,
    "CPU_RESIDENT": 8.0,
    "DISK_RESIDENT": 35.0,
    "REPO_AVAILABLE": 120.0,
    "MISSING": 1_000_000.0,
}
```

### API-2d40a8b863f8 · DeploymentStatus

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L400)

```python
class DeploymentStatus(str, Enum):
```

### API-7690292111b9 · ServiceOperationState

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L409)

```python
class ServiceOperationState(str, Enum):
```

原始接口说明：

```text
Reusable lifecycle states for long-running service operations.
```

### API-d7706fe046c6 · TERMINAL_SERVICE_OPERATION_STATES

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L421)

```python
TERMINAL_SERVICE_OPERATION_STATES: tuple[ServiceOperationState, ...] = (
    ServiceOperationState.DONE,
    ServiceOperationState.FAILED,
    ServiceOperationState.CANCELED,
    ServiceOperationState.EXPIRED,
)
```

### API-8a8b4f661224 · ServiceOperationStatus

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L430)

```python
class ServiceOperationStatus:
```

原始接口说明：

```text
App-neutral status envelope for long-running service work.

Repo insert/fetch operations, UAV missions/recordings, and DI provisioning or
execution may all expose this envelope while keeping their domain details in
``metadata`` or in an app-specific result reference.
```

### API-a1bcc1f76fb3 · ServiceOperationStatus.operation_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L438)

```python
operation_id: str
```

### API-c98fd9bf36d6 · ServiceOperationStatus.operation

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L439)

```python
operation: str
```

### API-5817863eb4cb · ServiceOperationStatus.service_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L440)

```python
service_name: str = ""
```

### API-c19eb6df3d6f · ServiceOperationStatus.provider_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L441)

```python
provider_name: str = ""
```

### API-5a790325512d · ServiceOperationStatus.request_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L442)

```python
request_id: str = ""
```

### API-ad2955410f60 · ServiceOperationStatus.role

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L443)

```python
role: str = ""
```

### API-55d666202280 · ServiceOperationStatus.attempt

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L444)

```python
attempt: int = 1
```

### API-e4b4310324e8 · ServiceOperationStatus.epoch

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L445)

```python
epoch: int = 1
```

### API-884bb3b44b63 · ServiceOperationStatus.sequence

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L446)

```python
sequence: int = 1
```

### API-f10ea3a3cbdb · ServiceOperationStatus.state

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L447)

```python
state: ServiceOperationState = ServiceOperationState.QUEUED
```

### API-9d8dda53b023 · ServiceOperationStatus.reason_code

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L448)

```python
reason_code: str = ""
```

### API-99886537fbd2 · ServiceOperationStatus.message

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L449)

```python
message: str = ""
```

### API-a275101d70dc · ServiceOperationStatus.progress_known

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L450)

```python
progress_known: bool = False
```

### API-ea06717643b6 · ServiceOperationStatus.progress

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L451)

```python
progress: float = 0.0
```

### API-ca61a9b45cca · ServiceOperationStatus.result_reference

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L452)

```python
result_reference: dict[str, Any] = field(default_factory=dict)
```

### API-89496a7eb621 · ServiceOperationStatus.retry_after_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L453)

```python
retry_after_ms: int = 0
```

### API-2640b311cb4c · ServiceOperationStatus.created_at_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L454)

```python
created_at_ms: int = field(default_factory=now_ms)
```

### API-30c85b42df48 · ServiceOperationStatus.updated_at_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L455)

```python
updated_at_ms: int = field(default_factory=now_ms)
```

### API-b299abea6814 · ServiceOperationStatus.expires_at_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L456)

```python
expires_at_ms: int = 0
```

### API-1637e92c9b3a · ServiceOperationStatus.details_schema

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L457)

```python
details_schema: str = ""
```

### API-24cbedf8650a · ServiceOperationStatus.details_payload

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L458)

```python
details_payload: bytes = b""
```

### API-f6d1e9fdacda · ServiceOperationStatus.metadata

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L459)

```python
metadata: dict[str, Any] = field(default_factory=dict)
```

### API-99def65c9df1 · ServiceOperationStatus.from_dict

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L474)

```python
def from_dict(cls, payload: dict[str, Any]) -> "ServiceOperationStatus":
```

### API-050f2185c5d3 · ServiceOperationStatus.terminal

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L511)

```python
def terminal(self) -> bool:
```

### API-b9e653bf4e0e · ServiceOperationStatus.is_fresh

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L514)

```python
def is_fresh(self, *, now_ms_value: int | None = None) -> bool:
```

### API-f76e10d5695f · CollaborationSelectionStatus

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L520)

```python
class CollaborationSelectionStatus:
```

原始接口说明：

```text
Validated signed SELECTION-STATUS snapshot with bounded members.
```

### API-fb67353af25a · CollaborationSelectionStatus.provider_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L523)

```python
provider_name: str
```

### API-4a83ce51d558 · CollaborationSelectionStatus.service_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L524)

```python
service_name: str
```

### API-95be09f2e980 · CollaborationSelectionStatus.request_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L525)

```python
request_id: str
```

### API-e417d2b365f9 · CollaborationSelectionStatus.selection_digest

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L526)

```python
selection_digest: str
```

### API-e6e4d2fa5ea5 · CollaborationSelectionStatus.state

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L527)

```python
state: str
```

### API-46970bf56d15 · CollaborationSelectionStatus.message

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L528)

```python
message: str = ""
```

### API-a68e595d47df · CollaborationSelectionStatus.response_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L529)

```python
response_name: str = ""
```

### API-a59d4659d00b · CollaborationSelectionStatus.updated_at_us

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L530)

```python
updated_at_us: int = 0
```

### API-1feb5e5472cc · CollaborationSelectionStatus.member_statuses

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L531)

```python
member_statuses: tuple[ServiceOperationStatus, ...] = ()
```

### API-0213cb480eaa · CollaborationSelectionStatus.from_dict

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L550)

```python
def from_dict(cls, payload: dict[str, Any]) -> "CollaborationSelectionStatus":
```

### API-506bf4892fdb · PlacementConstraint

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L566)

```python
class PlacementConstraint:
```

原始接口说明：

```text
Per-role resource constraints for deployment placement.

Used by the placement algorithm to filter provider candidates.
All fields are advisory minimums; the provider is the final authority
via admission control and lease validation.
```

### API-fd8b7a3fde42 · PlacementConstraint.role_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L573)

```python
role_id: str = ""
```

### API-3195502ac6fe · PlacementConstraint.min_gpu_memory_mb

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L574)

```python
min_gpu_memory_mb: float = 0.0
```

### API-4bdb5c44be7b · PlacementConstraint.min_cpu_memory_mb

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L575)

```python
min_cpu_memory_mb: float = 0.0
```

### API-2762ffebda82 · PlacementConstraint.required_backend

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L576)

```python
required_backend: str = ""
```

### API-99f155ff4321 · PlacementConstraint.anti_affinity

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L577)

```python
anti_affinity: tuple[str, ...] = ()
```

### API-8da6db8af729 · PlacementConstraint.affinity

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L578)

```python
affinity: tuple[str, ...] = ()
```

### API-4fac538e8b4b · PlacementConstraint.min_replicas

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L579)

```python
min_replicas: int = 1
```

### API-f436f85d3c5d · PlacementConstraint.max_replicas

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L580)

```python
max_replicas: int = 1
```

### API-695045de122f · PlacementConstraint.preload_priority

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L581)

```python
preload_priority: int = 0
```

### API-c86cabb19d1e · PlacementConstraint.from_dict

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L584)

```python
def from_dict(cls, payload: dict[str, Any]) -> "PlacementConstraint":
```

### API-a55c7e56e6ab · PeerNetworkMetric

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L606)

```python
class PeerNetworkMetric:
```

### API-7606cc09566b · PeerNetworkMetric.src_peer

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L607)

```python
src_peer: str
```

### API-8132be8c82b5 · PeerNetworkMetric.dst_peer

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L608)

```python
dst_peer: str
```

### API-3b3c9e1e7d22 · PeerNetworkMetric.rtt_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L609)

```python
rtt_ms: float = 0.0
```

### API-ebce9094a025 · PeerNetworkMetric.bandwidth_mbps

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L610)

```python
bandwidth_mbps: float = 0.0
```

### API-ae326b12e6d4 · PeerNetworkMetric.loss_rate

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L611)

```python
loss_rate: float = 0.0
```

### API-15d0b3c66947 · PeerNetworkMetric.jitter_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L612)

```python
jitter_ms: float = 0.0
```

### API-d70feea6a25a · PeerNetworkMetric.bytes_sampled

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L613)

```python
bytes_sampled: int = 0
```

### API-7ab30b5823ce · PeerNetworkMetric.updated_at_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L614)

```python
updated_at_ms: int = field(default_factory=now_ms)
```

### API-ff92c6176337 · PeerNetworkMetric.confidence

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L615)

```python
confidence: float = 1.0
```

### API-271b7616a9c7 · PeerNetworkMetric.from_dict

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L628)

```python
def from_dict(cls, payload: dict[str, Any]) -> "PeerNetworkMetric":
```

### API-c86242c67c9c · GenericProviderRuntimeHint

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L645)

```python
class GenericProviderRuntimeHint:
```

### API-669806e9e174 · GenericProviderRuntimeHint.provider_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L646)

```python
provider_name: str
```

### API-43266afbd880 · GenericProviderRuntimeHint.timestamp_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L647)

```python
timestamp_ms: int = field(default_factory=now_ms)
```

### API-c0c015c72e20 · GenericProviderRuntimeHint.active_work_count

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L648)

```python
active_work_count: int = 0
```

### API-4a41477f7dc2 · GenericProviderRuntimeHint.queue_length

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L649)

```python
queue_length: int = 0
```

### API-49b700385ab6 · GenericProviderRuntimeHint.estimated_queue_wait_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L650)

```python
estimated_queue_wait_ms: float = 0.0
```

### API-733953adfea8 · GenericProviderRuntimeHint.capacity_hints

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L651)

```python
capacity_hints: dict[str, Any] = field(default_factory=dict)
```

### API-c6b479ed0994 · GenericProviderRuntimeHint.peer_metrics

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L652)

```python
peer_metrics: tuple[PeerNetworkMetric, ...] = ()
```

### API-a68055d445a3 · GenericProviderRuntimeHint.confidence

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L653)

```python
confidence: float = 1.0
```

### API-4f22ecf3da2e · GenericProviderRuntimeHint.from_dict

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L664)

```python
def from_dict(cls, payload: dict[str, Any]) -> "GenericProviderRuntimeHint":
```

### API-99eb93f558ff · ProviderCapabilityHint

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L689)

```python
class ProviderCapabilityHint:
```

原始接口说明：

```text
Reusable provider ACK/discovery envelope.

The envelope says whether a provider can currently accept a service request
and carries core runtime/lease/status fields. Domain-specific details stay
in ``service_payload`` under a declared ``service_payload_schema``.
```

### API-b96bca5ce51f · ProviderCapabilityHint.provider_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L697)

```python
provider_name: str
```

### API-effe92e4dd3a · ProviderCapabilityHint.service_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L698)

```python
service_name: str
```

### API-b56bdb70194f · ProviderCapabilityHint.ready

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L699)

```python
ready: bool = True
```

### API-8c975d71fdaf · ProviderCapabilityHint.drain_state

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L700)

```python
drain_state: str = "ACTIVE"
```

### API-b4d8ebc40262 · ProviderCapabilityHint.reason_code

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L701)

```python
reason_code: str = ""
```

### API-67e4910b3173 · ProviderCapabilityHint.message

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L702)

```python
message: str = ""
```

### API-b8fb5d0fefe1 · ProviderCapabilityHint.runtime_hint

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L703)

```python
runtime_hint: GenericProviderRuntimeHint | None = None
```

### API-bbed93a8d8d2 · ProviderCapabilityHint.lease_offers

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L704)

```python
lease_offers: tuple["GenericAdmissionLease", ...] = ()
```

### API-6885a0d8f22f · ProviderCapabilityHint.operation_status

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L705)

```python
operation_status: ServiceOperationStatus | None = None
```

### API-89ebc4c8f7dd · ProviderCapabilityHint.service_payload_schema

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L706)

```python
service_payload_schema: str = ""
```

### API-52457edbe0dc · ProviderCapabilityHint.service_payload

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L707)

```python
service_payload: dict[str, Any] = field(default_factory=dict)
```

### API-ff7e48a97cb6 · ProviderCapabilityHint.timestamp_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L708)

```python
timestamp_ms: int = field(default_factory=now_ms)
```

### API-c089e1c78dfc · ProviderCapabilityHint.expires_at_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L709)

```python
expires_at_ms: int = 0
```

### API-581fa0da7001 · ProviderCapabilityHint.schema

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L710)

```python
schema: str = PROVIDER_CAPABILITY_SCHEMA_V2
```

### API-a231c8147497 · ProviderCapabilityHint.from_dict

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L719)

```python
def from_dict(cls, payload: dict[str, Any]) -> "ProviderCapabilityHint":
```

### API-937b46163f0d · ProviderCapabilityHint.is_fresh

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L753)

```python
def is_fresh(self, *, now_ms_value: int | None = None) -> bool:
```

### API-6e7f13c39d3a · ProviderCapabilityHint.ready_for_new_request

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L758)

```python
def ready_for_new_request(self) -> bool:
```

### API-0cbc1d9bdba8 · ProviderCapabilityHint.to_ack_fields

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L761)

```python
def to_ack_fields(self) -> dict[str, Any]:
```

### API-6774dba9a639 · ProviderCapabilityHint.from_ack_fields

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L765)

```python
def from_ack_fields(cls, fields: dict[str, Any]) -> "ProviderCapabilityHint":
```

### API-2c307c61ddcc · encode_provider_capability_ack

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L772)

```python
def encode_provider_capability_ack(hint: ProviderCapabilityHint) -> bytes:
```

### API-04b2fb9c4b52 · decode_provider_capability_ack

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L779)

```python
def decode_provider_capability_ack(
        payload: bytes | str,
        *,
        mode: AckCompatibilityMode | str | None = None,
        counters: AckCompatibilityCounters | None = None,
        provider_name: str = "",
        service_name: str = "") -> ProviderCapabilityAckDecodeResult:
```

### API-01af7070801d · DataProductReference

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L846)

```python
class DataProductReference:
```

原始接口说明：

```text
App-neutral reference to a produced named data object or manifest.
```

### API-9e3d097d3f0c · DataProductReference.object_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L849)

```python
object_name: str
```

### API-b150c2d9e351 · DataProductReference.object_class

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L850)

```python
object_class: str = ""
```

### API-3f62c7678906 · DataProductReference.content_type

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L851)

```python
content_type: str = "application/octet-stream"
```

### API-77ba54c98e6a · DataProductReference.producer_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L852)

```python
producer_name: str = ""
```

### API-e130afbeeb7e · DataProductReference.version

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L853)

```python
version: str = ""
```

### API-1052fa0304c6 · DataProductReference.session_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L854)

```python
session_id: str = ""
```

### API-08b8a828b635 · DataProductReference.large_data_reference

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L855)

```python
large_data_reference: dict[str, Any] = field(default_factory=dict)
```

### API-c25e9c93d102 · DataProductReference.repo_manifest

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L856)

```python
repo_manifest: dict[str, Any] = field(default_factory=dict)
```

### API-1ea994a2775b · DataProductReference.encryption_info

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L857)

```python
encryption_info: dict[str, Any] = field(default_factory=dict)
```

### API-1eabe0d26663 · DataProductReference.size_bytes

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L858)

```python
size_bytes: int = 0
```

### API-89cfb78854d9 · DataProductReference.digest

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L859)

```python
digest: str = ""
```

### API-088651e1760f · DataProductReference.ttl_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L860)

```python
ttl_ms: int = 0
```

### API-15937904a533 · DataProductReference.metadata

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L861)

```python
metadata: dict[str, Any] = field(default_factory=dict)
```

### API-c5e68e497315 · DataProductReference.schema

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L862)

```python
schema: str = "ndnsf-data-product-reference-v1"
```

### API-55601eb6eabc · DataProductReference.from_dict

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L871)

```python
def from_dict(cls, payload: dict[str, Any]) -> "DataProductReference":
```

### API-e9df7a16d1b2 · GenericAdmissionLease

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L893)

```python
class GenericAdmissionLease:
```

### API-9b310962080a · GenericAdmissionLease.lease_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L894)

```python
lease_id: str
```

### API-f839640805da · GenericAdmissionLease.request_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L895)

```python
request_id: str
```

### API-5f134cc78f8d · GenericAdmissionLease.service_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L896)

```python
service_name: str
```

### API-ee2dbb25acf9 · GenericAdmissionLease.provider_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L897)

```python
provider_name: str
```

### API-4e3484457e4b · GenericAdmissionLease.status

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L898)

```python
status: AdmissionLeaseStatus = AdmissionLeaseStatus.GRANTED
```

### API-dc5ac9ec6211 · GenericAdmissionLease.reason_code

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L899)

```python
reason_code: str = ""
```

### API-ae988bf49859 · GenericAdmissionLease.estimated_start_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L900)

```python
estimated_start_ms: int = 0
```

### API-7a39e5264912 · GenericAdmissionLease.estimated_finish_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L901)

```python
estimated_finish_ms: int = 0
```

### API-fb07f554d3ba · GenericAdmissionLease.expires_at_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L902)

```python
expires_at_ms: int = 0
```

### API-a33c712a8bb3 · GenericAdmissionLease.resource_binding_schema

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L903)

```python
resource_binding_schema: str = ""
```

### API-c4f7aa3a1b21 · GenericAdmissionLease.resource_binding

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L904)

```python
resource_binding: dict[str, Any] = field(default_factory=dict)
```

### API-d3b76e33ec35 · GenericAdmissionLease.consumed

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L905)

```python
consumed: bool = False
```

### API-06ec5b98b099 · GenericAdmissionLease.from_dict

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L918)

```python
def from_dict(cls, payload: dict[str, Any]) -> "GenericAdmissionLease":
```

### API-c847f96a8221 · GenericAdmissionLease.is_valid

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L942)

```python
def is_valid(self, *, now_ms_value: int | None = None) -> bool:
```

### API-25d747a01946 · GenericAdmissionLease.binding_digest

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L950)

```python
def binding_digest(self) -> str:
```

### API-a26b5975562d · GenericLeaseValidationResult

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L958)

```python
class GenericLeaseValidationResult:
```

### API-b3f7794e954e · GenericLeaseValidationResult.status

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L959)

```python
status: bool
```

### API-1ffc9c5c808d · GenericLeaseValidationResult.reason_code

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L960)

```python
reason_code: str = ""
```

### API-cf11876ed6d8 · GenericLeaseValidationResult.lease_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L961)

```python
lease_id: str = ""
```

### API-694a7bf44c6d · GenericLeaseValidationResult.request_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L962)

```python
request_id: str = ""
```

### API-b68e1f7b881f · GenericLeaseValidationResult.service_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L963)

```python
service_name: str = ""
```

### API-da1c9959d7cc · GenericLeaseValidationResult.provider_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L964)

```python
provider_name: str = ""
```

### API-c67f2fa08d7d · GenericAckMetadata

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L968)

```python
class GenericAckMetadata:
```

### API-773f5e43e018 · GenericAckMetadata.provider_runtime_hint

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L969)

```python
provider_runtime_hint: GenericProviderRuntimeHint
```

### API-a0b52be0e1b0 · GenericAckMetadata.lease_offers

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L970)

```python
lease_offers: tuple[GenericAdmissionLease, ...] = ()
```

### API-c6949f3775e8 · GenericAckMetadata.service_payload_schema

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L971)

```python
service_payload_schema: str = ""
```

### API-33b4f2833c20 · GenericAckMetadata.service_payload

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L972)

```python
service_payload: dict[str, Any] = field(default_factory=dict)
```

### API-f46e95964c1e · GenericAckMetadata.metric_digest

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L973)

```python
metric_digest: str = ""
```

### API-8bcb2eee1d08 · GenericAckMetadata.notes

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L974)

```python
notes: str = ""
```

### API-4936b4958c9b · GenericAckMetadata.schema

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L975)

```python
schema: str = "ndnsf-ack-metadata-v1"
```

### API-63e54b9c1afc · GenericAckMetadata.from_dict

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L978)

```python
def from_dict(cls, payload: dict[str, Any]) -> "GenericAckMetadata":
```

### API-79ccb59fedb5 · GenericAckMetadata.to_ack_fields

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L999)

```python
def to_ack_fields(self) -> dict[str, Any]:
```

### API-fbc7a5ef28f4 · GenericAckMetadata.from_ack_fields

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L1003)

```python
def from_ack_fields(cls, fields: dict[str, Any]) -> "GenericAckMetadata":
```

### API-bf8c64fedb03 · GenericAckMetadata.from_provider_capability_hint

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L1010)

```python
def from_provider_capability_hint(cls,
                                      hint: ProviderCapabilityHint) -> "GenericAckMetadata":
```

### API-849a7a285ab1 · ProviderAdmissionLeaseTable

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L1041)

```python
class ProviderAdmissionLeaseTable:
```

### API-93e26c8e0168 · ProviderAdmissionLeaseTable.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L1042)

```python
def __init__(self) -> None:
```

### API-00acbe267473 · ProviderAdmissionLeaseTable.grant

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L1052)

```python
def grant(self, lease: GenericAdmissionLease) -> GenericAdmissionLease:
```

### API-8a80410783fa · ProviderAdmissionLeaseTable.release

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L1060)

```python
def release(self, lease_id: str) -> GenericLeaseValidationResult:
```

### API-42f27b4777ad · ProviderAdmissionLeaseTable.consume

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L1074)

```python
def consume(self, *,
                lease_id: str,
                request_id: str,
                service_name: str,
                provider_name: str,
                resource_binding: dict[str, Any] | None = None,
                now_ms_value: int | None = None) -> GenericLeaseValidationResult:
```

### API-d1d3b93e07fa · ProviderAdmissionLeaseTable.counters

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L1106)

```python
def counters(self) -> dict[str, int]:
```

### API-e6510764c9a9 · probe_peer_rtt

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L1131)

```python
def probe_peer_rtt(provider: Any, peer_name: str, service: str,
                   *, sizes: tuple[int, ...] = (1024, 10240, 102400, 512000),
                   timeout_ms: int = 2000) -> dict[str, Any]:
```

原始接口说明：

```text
Probe a peer provider for RTT and bandwidth using small Data packets.

Returns a dict with rtt_ms, bandwidth_mbps, loss_rate suitable for
constructing a PeerNetworkMetric.
```

### API-b2f9adaf13f4 · score_normalized

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L1170)

```python
def score_normalized(value: float, worst: float, best: float = 0.0, invert: bool = True) -> float:
```

原始接口说明：

```text
Normalize a value to 0-100. If invert=True, lower is better (returns 100-best).
```

### API-3c32e145b79d · build_network_matrix_from_ndnsd

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L1178)

```python
def build_network_matrix_from_ndnsd(ndnsd_services: list[dict[str, Any]]) -> ProviderNetworkMatrix:
```

原始接口说明：

```text
Build a ProviderNetworkMatrix from NDNSD-published peer metrics.
```

### API-ad061448fc39 · ProviderNetworkMatrix

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L1200)

```python
class ProviderNetworkMatrix:
```

### API-3b0b9ee3712a · ProviderNetworkMatrix.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L1201)

```python
def __init__(self,
                 metrics: Iterable[PeerNetworkMetric] = (),
                 *,
                 default_rtt_ms: float = 50.0,
                 default_bandwidth_mbps: float = 100.0,
                 stale_after_ms: int = 30000,
                 stale_penalty_ms: float = 25.0,
                 unknown_penalty_ms: float = 100.0):
```

### API-ee953fd2f369 · ProviderNetworkMatrix.from_dict

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L1217)

```python
def from_dict(cls, payload: dict[str, Any]) -> "ProviderNetworkMatrix":
```

### API-3f201865ce70 · ProviderNetworkMatrix.metric

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L1237)

```python
def metric(self, src_peer: str, dst_peer: str) -> PeerNetworkMetric | None:
```

### API-07eadcf3f07e · ProviderNetworkMatrix.transfer_cost_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L1240)

```python
def transfer_cost_ms(self, src_peer: str, dst_peer: str, bytes_count: int,
                         *, now_ms_value: int | None = None) -> tuple[float, dict[str, Any]]:
```

### API-a71f7c1918f4 · ProviderNetworkMatrix.rank_transfer_candidates

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L1277)

```python
def rank_transfer_candidates(self, src_peer: str, dst_peers: Iterable[str],
                                 bytes_count: int,
                                 *, now_ms_value: int | None = None) -> list[dict[str, Any]]:
```

### API-7583adf4bd49 · ProviderNetworkMatrix.best_transfer_target

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/runtime_telemetry.py#L1293)

```python
def best_transfer_target(self, src_peer: str, dst_peers: Iterable[str],
                             bytes_count: int,
                             *, now_ms_value: int | None = None) -> dict[str, Any] | None:
```

## pythonWrapper/ndnsf/service.py

源码 SHA-256：`5b8f4a999001da97d8eb5faa075f8a63456c5fc726aae9558c9094fd0c75a99a`。

### API-cdf1f41211e2 · CollaborationDeadlineExceeded

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L34)

```python
class CollaborationDeadlineExceeded(TimeoutError):
```

原始接口说明：

```text
Distinct terminal reason for a collaboration status watch.
```

### API-1bef4506ecbc · CollaborationDeadlineExceeded.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L37)

```python
def __init__(self, reason: str):
```

### API-192ed631b76a · default_large_data_interest_lifetime_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L85)

```python
def default_large_data_interest_lifetime_ms() -> int:
```

原始接口说明：

```text
InterestLifetime for segmented large-object fetches.

DI dependency prefetch can intentionally issue Interests before upstream
providers publish the corresponding Data. Keep this long enough for normal
distributed inference runs so the Interest stays pending instead of being
re-expressed every second.
```

### API-15312f7d047a · ServiceResponse

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L98)

```python
class ServiceResponse:
```

### API-64f129f8c3b7 · ServiceResponse.status

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L99)

```python
status: bool
```

### API-a492bac1ef12 · ServiceResponse.payload

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L100)

```python
payload: bytes = b""
```

### API-6a31fba34d61 · ServiceResponse.error

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L101)

```python
error: str = ""
```

### API-27e099bd4e74 · ServiceResponse.request_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L102)

```python
request_id: str = ""
```

### API-8383c5a3d18c · ServiceResponse.data_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L103)

```python
data_name: str = ""
```

### API-6bf0f5f2ae7e · ServiceResponse.signer_certificate

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L104)

```python
signer_certificate: str = ""
```

### API-39dc47579f48 · ServiceResponse.wire_digest

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L105)

```python
wire_digest: str = ""
```

### API-97f082062c45 · StreamedInvocationOptions

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L109)

```python
class StreamedInvocationOptions:
```

原始接口说明：

```text
Bounded request-scoped stream options shared with the C++ wire API.

``max_events`` counts every cursor, including the authenticated End event;
therefore an application that may emit N payload events must reserve at
least N + 1 cursor slots.
```

### API-8b4357b9cf64 · StreamedInvocationOptions.mode

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L117)

```python
mode: str = "Normal"
```

### API-189f7c8e008e · StreamedInvocationOptions.max_events

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L118)

```python
max_events: int = 512
```

### API-3ea110b3f145 · StreamedInvocationOptions.interest_window

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L119)

```python
interest_window: int = 16
```

### API-7b4c00327ec6 · StreamedInvocationOptions.interest_lifetime_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L120)

```python
interest_lifetime_ms: int = 500
```

### API-ba7f8a1c0bd2 · StreamedInvocationOptions.max_event_retries

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L121)

```python
max_event_retries: int = 3
```

### API-11cef42fe910 · StreamedInvocationOptions.publisher_queue_capacity

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L122)

```python
publisher_queue_capacity: int = 64
```

### API-cc9ef4e19d03 · StreamedInvocationOptions.callback_queue_capacity

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L123)

```python
callback_queue_capacity: int = 64
```

### API-62731ad32661 · StreamedInvocationOptions.reorder_capacity

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L124)

```python
reorder_capacity: int = 64
```

### API-26bdc3444d8b · StreamedInvocationOptions.retention_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L125)

```python
retention_ms: int = 30000
```

### API-62546ea8b6f1 · StreamedInvocationOptions.completion_grace_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L126)

```python
completion_grace_ms: int = 5000
```

### API-3315e4817a51 · StreamedInvocationOptions.max_event_wire_bytes

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L127)

```python
max_event_wire_bytes: int = 16384
```

### API-af291329b71e · StreamedInvocationOptions.allow_replacement

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L128)

```python
allow_replacement: bool = False
```

### API-61562ab484f4 · StreamedInvocationOptions.max_replacements

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L129)

```python
max_replacements: int = 0
```

### API-6065150b69cc · StreamedInvocationOptions.attempt_epoch

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L133)

```python
attempt_epoch: int = 1
```

### API-1356147c0b7f · StreamedInvocationOptions.generation_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L134)

```python
generation_id: str = ""
```

### API-636157680de4 · StreamedInvocationOptions.stream_epoch

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L135)

```python
stream_epoch: int = 0
```

### API-e1a789b7188a · StreamedInvocationOptions.as_dict

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L137)

```python
def as_dict(self) -> dict[str, object]:
```

### API-241db6c02688 · StreamedInvocationMetrics

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L159)

```python
class StreamedInvocationMetrics:
```

原始接口说明：

```text
Bounded Core counters for one request-scoped streamed invocation.
```

### API-aa8900a429d5 · StreamedInvocationMetrics.published_events

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L162)

```python
published_events: int = 0
```

### API-bf62a8394d56 · StreamedInvocationMetrics.delivered_events

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L163)

```python
delivered_events: int = 0
```

### API-e47210034fec · StreamedInvocationMetrics.retry_count

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L164)

```python
retry_count: int = 0
```

### API-e70cbcdf3e0d · StreamedInvocationMetrics.duplicate_count

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L165)

```python
duplicate_count: int = 0
```

### API-3ad1c1da574f · StreamedInvocationError

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L168)

```python
class StreamedInvocationError(RuntimeError):
```

原始接口说明：

```text
Terminal streamed-invocation failure reported by the Core runtime.
```

### API-d8dfdb56200e · StreamedInvocationError.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L171)

```python
def __init__(
        self, *, code: int, message: str, request_id: str = "",
        expected_cursor: int = 0, provider_name: str = "",
    ) -> None:
```

### API-f13024c70371 · StreamedInvocationError.from_native

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L183)

```python
def from_native(cls, value: Mapping[str, Any]) -> "StreamedInvocationError":
```

### API-fb90b0ee2d13 · StreamedInvocation

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L348)

```python
class StreamedInvocation:
```

原始接口说明：

```text
One Python view of a Core-owned streamed invocation lifecycle.
```

### API-a142a94b5256 · StreamedInvocation.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L351)

```python
def __init__(self, native_handle, delivery: _StreamedInvocationDelivery) -> None:
```

### API-210001c78cc6 · StreamedInvocation.request_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L360)

```python
def request_id(self) -> str:
```

### API-2dc3e62e7c68 · StreamedInvocation.status

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L364)

```python
def status(self):
```

### API-f722fd5a1cc7 · StreamedInvocation.metrics

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L368)

```python
def metrics(self) -> StreamedInvocationMetrics:
```

### API-859a94407745 · StreamedInvocation.result

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L380)

```python
async def result(self):
```

### API-914f40c256d2 · StreamedInvocation.cancel

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L383)

```python
async def cancel(self) -> None:
```

### API-bf34e4d1a5a2 · ProviderSigningMetadata

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L393)

```python
class ProviderSigningMetadata:
```

原始接口说明：

```text
Public Provider signing names; no private key material is exposed.
```

### API-b6881f152d86 · ProviderSigningMetadata.provider_identity

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L396)

```python
provider_identity: str
```

### API-7f061c6382a8 · ProviderSigningMetadata.signing_key_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L397)

```python
signing_key_name: str
```

### API-5ace30aa185f · ProviderSigningMetadata.signing_certificate_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L398)

```python
signing_certificate_name: str
```

### API-4409056d0945 · AckDecision

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L402)

```python
class AckDecision:
```

### API-45a03066a82d · AckDecision.status

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L403)

```python
status: bool = True
```

### API-457421d0e5bd · AckDecision.payload

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L404)

```python
payload: bytes = b""
```

### API-1100b95d971a · AckDecision.message

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L405)

```python
message: str = "ok"
```

### API-06cb08cbc5da · AckDecision.suppress

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L406)

```python
suppress: bool = False
```

### API-782620e3c59b · AckDecision.reservation_lease

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L407)

```python
reservation_lease: Mapping[str, str] = field(default_factory=dict)
```

### API-1015aec70e37 · AckDecision.selection_input_key_offer

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L408)

```python
selection_input_key_offer: Mapping[str, str] = field(default_factory=dict)
```

### API-374be71300bd · AckDecision.pending_state_ttl_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L409)

```python
pending_state_ttl_ms: int = 0
```

### API-3c06cdbd3f61 · AckCandidate

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L413)

```python
class AckCandidate:
```

### API-4b281f1b5fc7 · AckCandidate.provider_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L414)

```python
provider_name: str
```

### API-ae78ea8e85a7 · AckCandidate.service_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L415)

```python
service_name: str
```

### API-39f00c8fde9e · AckCandidate.request_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L416)

```python
request_id: str
```

### API-adfce905874d · AckCandidate.status

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L417)

```python
status: bool
```

### API-cd24d4c48734 · AckCandidate.message

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L418)

```python
message: str = ""
```

### API-17af40c8adfc · AckCandidate.payload

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L419)

```python
payload: bytes = b""
```

### API-cc77c989dbc7 · AckCandidate.telemetry

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L420)

```python
telemetry: Optional[Mapping[str, Any]] = None
```

### API-97c7ff6b8cce · AckCandidate.selection_input_key_offer

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L421)

```python
selection_input_key_offer: Mapping[str, str] = field(default_factory=dict)
```

### API-eab4b6e69cf8 · AckCandidate.signer_identity

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L425)

```python
signer_identity: str = ""
```

### API-a9b459242e4c · AckCandidate.signer_key_locator

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L426)

```python
signer_key_locator: str = ""
```

### API-3888268dc059 · AckCandidate.validated_wire_digest

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L427)

```python
validated_wire_digest: str = ""
```

### API-103ba8f0958d · AckCandidate.trust_schema_validated

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L428)

```python
trust_schema_validated: bool = False
```

### API-1d20aceb176d · CollaborationAckClosed

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L432)

```python
class CollaborationAckClosed:
```

### API-4c5487ba0afa · CollaborationAckClosed.request_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L433)

```python
request_id: str
```

### API-f946ccc73f48 · CollaborationAckClosed.candidates

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L434)

```python
candidates: tuple[AckCandidate, ...]
```

### API-5f28a22f756e · CollaborationAckClosed.digest

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L435)

```python
digest: str
```

### API-8875e716da20 · CollaborationAckClosed.closed_at_us

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L436)

```python
closed_at_us: int
```

### API-9ac76f68e6ce · CollaborationAckClosed.request_deadline_us

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L437)

```python
request_deadline_us: int
```

### API-a99a2e342ab3 · AllowedService

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L448)

```python
class AllowedService:
```

原始接口说明：

```text
A service permission entry visible to a Python NDNSF user.

provider_service is the full permission namespace, typically
/<provider>/<service>. service is the unified service name applications pass
to request_service(), such as /HELLO. policy_epoch identifies the
controller policy snapshot that authorized this record.
```

### API-d68c209b8c39 · AllowedService.provider_service

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L457)

```python
provider_service: str
```

### API-55883c1cec61 · AllowedService.service

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L458)

```python
service: str
```

### API-9fa2c024134f · AllowedService.policy_epoch

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L459)

```python
policy_epoch: int = 0
```

### API-033c83f0a16a · LargeDataPublishResult

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L463)

```python
class LargeDataPublishResult:
```

### API-7f8314098174 · LargeDataPublishResult.success

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L464)

```python
success: bool
```

### API-ffbcec0dd9bd · LargeDataPublishResult.encrypted_data_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L465)

```python
encrypted_data_name: str = ""
```

### API-d31f5c7a1ae2 · LargeDataPublishResult.object_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L466)

```python
object_id: str = ""
```

### API-4411fd9011a2 · LargeDataPublishResult.plaintext_size

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L467)

```python
plaintext_size: int = 0
```

### API-47f91ca53021 · LargeDataPublishResult.content_digest

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L468)

```python
content_digest: str = ""
```

### API-e59ffea7729e · LargeDataPublishResult.manifest_digest

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L469)

```python
manifest_digest: str = ""
```

### API-75fc5d9165e7 · LargeDataPublishResult.authorization_scope

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L470)

```python
authorization_scope: str = ""
```

### API-2144aa20ff1c · LargeDataPublishResult.protection_epoch

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L471)

```python
protection_epoch: str = ""
```

### API-5c471768bd96 · LargeDataPublishResult.encrypted

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L472)

```python
encrypted: bool = True
```

### API-21856b493450 · LargeDataPublishResult.error

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L473)

```python
error: str = ""
```

### API-79abf9f8c46a · SignedAppDataResult

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L477)

```python
class SignedAppDataResult:
```

原始接口说明：

```text
Result of exact-name, trust-schema-validated APP Data I/O.
```

### API-2251ecb0fb73 · SignedAppDataResult.success

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L480)

```python
success: bool
```

### API-ad13be3c1483 · SignedAppDataResult.data_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L481)

```python
data_name: str = ""
```

### API-ae1dfdf518df · SignedAppDataResult.signer_certificate

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L482)

```python
signer_certificate: str = ""
```

### API-71432f111af5 · SignedAppDataResult.payload

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L483)

```python
payload: bytes = b""
```

### API-4c350653de4f · SignedAppDataResult.error

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L484)

```python
error: str = ""
```

### API-88f3f321d2e1 · LargeDataReference

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L488)

```python
class LargeDataReference:
```

### API-a735de04019c · LargeDataReference.data_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L489)

```python
data_name: str
```

### API-4630165e3e1e · LargeDataReference.object_type

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L490)

```python
object_type: str = ""
```

### API-64c8fe81e4c3 · LargeDataReference.object_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L491)

```python
object_id: str = ""
```

### API-5e7bc1797082 · LargeDataReference.plaintext_size

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L492)

```python
plaintext_size: int = 0
```

### API-4396c61859f3 · LargeDataReference.encrypted

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L493)

```python
encrypted: bool = True
```

### API-b36770d96ec1 · LargeDataReference.digest

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L494)

```python
digest: str = ""
```

### API-6ebaf56b0f2e · encode_large_data_reference_payload

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L497)

```python
def encode_large_data_reference_payload(reference: LargeDataReference) -> bytes:
```

原始接口说明：

```text
Encode a standard NDNSF large-data reference payload.
```

### API-50a697eceec5 · parse_large_data_reference_payload

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L510)

```python
def parse_large_data_reference_payload(payload: bytes) -> Optional[LargeDataReference]:
```

原始接口说明：

```text
Parse a standard NDNSF large-data reference payload, or return None.
```

### API-5b415dfa7a3c · SegmentedObjectProducer

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L526)

```python
class SegmentedObjectProducer:
```

原始接口说明：

```text
Serve one payload as signed segmented NDN Data.

This is a thin Python wrapper around ndn-cxx Segmenter. The base name is a
generic object name, not an AI artifact name; higher-level frameworks such
as NDNSF-DI decide whether the object is a model shard, runner, activation,
or some other application object.
```

### API-30dd52ecd09e · SegmentedObjectProducer.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L535)

```python
def __init__(
        self,
        base_name: str,
        payload: bytes,
        *,
        signing_identity: str = "",
        max_segment_size: int = 6000,
        freshness_ms: int = 60000,
    ) -> None:
```

### API-db99a1c16e2f · SegmentedObjectProducer.base_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L553)

```python
def base_name(self) -> str:
```

### API-5137aa308496 · SegmentedObjectProducer.versioned_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L557)

```python
def versioned_name(self) -> str:
```

### API-738676c94cdc · SegmentedObjectProducer.segment_count

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L561)

```python
def segment_count(self) -> int:
```

### API-be85a9509435 · SegmentedObjectProducer.error

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L565)

```python
def error(self) -> str:
```

### API-88c92efc67f9 · SegmentedObjectProducer.start

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L568)

```python
def start(self) -> "SegmentedObjectProducer":
```

### API-27b8981b3976 · SegmentedObjectProducer.stop

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L572)

```python
def stop(self) -> None:
```

### API-d0f24d1a490a · FileSegmentedObjectProducer

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L576)

```python
class FileSegmentedObjectProducer:
```

原始接口说明：

```text
Serve a file as on-demand signed segments with bounded producer memory.
```

### API-8db65058d12c · FileSegmentedObjectProducer.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L579)

```python
def __init__(
        self,
        base_name: str,
        file_path: str,
        *,
        signing_identity: str = "",
        max_segment_size: int = 6000,
        freshness_ms: int = 60000,
        digest_signing: bool = True,
    ) -> None:
```

### API-0c58733c31cc · FileSegmentedObjectProducer.start

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L598)

```python
def start(self) -> "FileSegmentedObjectProducer":
```

### API-f9a1c8a6e22f · FileSegmentedObjectProducer.stop

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L602)

```python
def stop(self) -> None:
```

### API-5f75c05d1898 · FileSegmentedObjectProducer.base_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L606)

```python
def base_name(self) -> str:
```

### API-638dfd55412b · FileSegmentedObjectProducer.versioned_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L610)

```python
def versioned_name(self) -> str:
```

### API-78c43b36882e · FileSegmentedObjectProducer.segment_count

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L614)

```python
def segment_count(self) -> int:
```

### API-e846a73f1322 · FileSegmentedObjectProducer.file_size

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L618)

```python
def file_size(self) -> int:
```

### API-d5ae1d3abd04 · FileSegmentedObjectProducer.data_count

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L622)

```python
def data_count(self) -> int:
```

### API-2b3a133440b1 · FileSegmentedObjectProducer.wire_bytes

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L626)

```python
def wire_bytes(self) -> int:
```

### API-87dceb40b44e · FileSegmentedObjectProducer.signing_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L630)

```python
def signing_ms(self) -> float:
```

### API-4619406b4523 · FileSegmentedObjectProducer.public_key_der

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L634)

```python
def public_key_der(self) -> bytes:
```

### API-c583cb0a033e · FileSegmentedObjectProducer.error

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L638)

```python
def error(self) -> str:
```

### API-c725d7b60a15 · DataPacket

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L643)

```python
class DataPacket:
```

原始接口说明：

```text
One immutable NDN Data packet encoded in wire format.
```

### API-2575ee270bdb · DataPacket.name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L646)

```python
name: str
```

### API-741101b08dba · DataPacket.segment

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L647)

```python
segment: int
```

### API-91181afe98c9 · DataPacket.wire

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L648)

```python
wire: bytes
```

### API-be7c6d6f9400 · DataPacket.content

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L649)

```python
content: bytes = b""
```

### API-233ed40f59c6 · verify_data_packet_signature

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L652)

```python
def verify_data_packet_signature(wire: bytes, public_key_der: bytes) -> bool:
```

原始接口说明：

```text
Verify one RSA/ECDSA signed Data packet against a DER public key.
```

### API-080b40a40f85 · verify_detached_sha256_signature

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L658)

```python
def verify_detached_sha256_signature(
    payload: bytes, signature: bytes, public_key_der: bytes
) -> bool:
```

原始接口说明：

```text
Verify one detached RSA/ECDSA SHA-256 signature in-process.
```

### API-1b74154b1cf9 · verify_data_packet_digest

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L668)

```python
def verify_data_packet_digest(wire: bytes) -> bool:
```

原始接口说明：

```text
Verify one self-verifying DigestSha256 Data packet.
```

### API-6abeec388023 · AdaptiveSegmentFetchResult

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L675)

```python
class AdaptiveSegmentFetchResult:
```

原始接口说明：

```text
Metrics from callback-streamed adaptive segmented delivery.
```

### API-7a01549f924d · AdaptiveSegmentFetchResult.total_segments

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L678)

```python
total_segments: int
```

### API-a6f1cf5fe8cf · AdaptiveSegmentFetchResult.delivered_segments

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L679)

```python
delivered_segments: int
```

### API-6f2d0a249649 · AdaptiveSegmentFetchResult.interest_count

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L680)

```python
interest_count: int
```

### API-f0fe9b365529 · AdaptiveSegmentFetchResult.retransmission_count

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L681)

```python
retransmission_count: int
```

### API-09d49351b648 · AdaptiveSegmentFetchResult.duplicate_count

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L682)

```python
duplicate_count: int
```

### API-965cf95b6dca · AdaptiveSegmentFetchResult.timeout_count

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L683)

```python
timeout_count: int
```

### API-e3cd6a04747f · AdaptiveSegmentFetchResult.logical_bytes

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L684)

```python
logical_bytes: int
```

### API-71a09659eca2 · AdaptiveSegmentFetchResult.data_wire_bytes

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L685)

```python
data_wire_bytes: int
```

### API-fd477f771056 · AdaptiveSegmentFetchResult.interest_wire_bytes

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L686)

```python
interest_wire_bytes: int
```

### API-7d85f8098c2e · AdaptiveSegmentFetchResult.wire_bytes

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L687)

```python
wire_bytes: int
```

### API-5f496ad4e0da · AdaptiveSegmentFetchResult.retransmitted_bytes

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L688)

```python
retransmitted_bytes: int
```

### API-cc22d1ae7906 · AdaptiveSegmentFetchResult.maximum_in_flight

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L689)

```python
maximum_in_flight: int
```

### API-914a1dd4a998 · AdaptiveSegmentFetchResult.final_window

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L690)

```python
final_window: float
```

### API-e12b589c5cba · SegmentHintRange

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L694)

```python
class SegmentHintRange:
```

原始接口说明：

```text
Forwarding hints that apply to a contiguous segment range.
```

### API-7b1261a3d36f · SegmentHintRange.start

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L697)

```python
start: int
```

### API-15c113b6e91d · SegmentHintRange.end

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L698)

```python
end: int
```

### API-c467c3853878 · SegmentHintRange.forwarding_hints

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L699)

```python
forwarding_hints: tuple[str, ...]
```

### API-7abc8b4bedb4 · StoredDataProducer

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L702)

```python
class StoredDataProducer:
```

原始接口说明：

```text
Serve already-signed NDN Data packets without rewriting them.
```

### API-8ed2baaac07f · StoredDataProducer.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L705)

```python
def __init__(
        self,
        base_name: str,
        packet_wires: list[bytes],
        *,
        signing_identity: str = "",
        forwarding_route_prefixes: Optional[list[str]] = None,
    ) -> None:
```

### API-234ab944ddcf · StoredDataProducer.segment_count

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L721)

```python
def segment_count(self) -> int:
```

### API-8e68f2dbf8a0 · StoredDataProducer.error

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L725)

```python
def error(self) -> str:
```

### API-70f7b145e3d4 · StoredDataProducer.start

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L728)

```python
def start(self) -> "StoredDataProducer":
```

### API-52bfc8026c7b · StoredDataProducer.stop

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L732)

```python
def stop(self) -> None:
```

### API-72e17c14b1cc · decode_data_packet

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L736)

```python
def decode_data_packet(wire: bytes) -> DataPacket:
```

原始接口说明：

```text
Decode one immutable NDN Data wire packet without rewriting it.
```

### API-2e7f6a9cd5ab · make_segmented_data_packets

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L748)

```python
def make_segmented_data_packets(
    base_name: str,
    payload: bytes,
    *,
    signing_identity: str = "",
    max_segment_size: int = 6000,
    freshness_ms: int = 60000,
) -> list[DataPacket]:
```

原始接口说明：

```text
Create signed segmented NDN Data packets for direct packet storage.
```

### API-986fda05ea53 · make_signed_data

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L776)

```python
def make_signed_data(
    name: str,
    content: bytes,
    *,
    signing_identity: str = "",
    freshness_ms: int = 300,
) -> bytes:
```

原始接口说明：

```text
Make a signed exact-name Data packet for tests and convenience only.

Production stream applications should sign with their own NDN keychain
(for example ``python-ndn``) and pass that exact wire to
:meth:`StreamPublisher.push`. This helper creates or uses an ndn-cxx
identity in the process keychain and is not the Provider-bound production
signing path.
```

### API-94b4ea7613c1 · make_predictive_data_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L796)

```python
def make_predictive_data_name(
    mapping_root: str, mapping_version: int, sequence: int
) -> str:
```

原始接口说明：

```text
Return the canonical predictive source Data name URI.
```

### API-d50edd4fb1cb · fetch_segmented_data_packets

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L805)

```python
def fetch_segmented_data_packets(
    base_name: str,
    *,
    timeout_ms: int = 30000,
    interest_lifetime_ms: Optional[int] = None,
    forwarding_hints: Optional[list[str]] = None,
) -> list[DataPacket]:
```

原始接口说明：

```text
Fetch segmented NDN Data and return the original Data wire packets.
```

### API-7ad5412696a8 · fetch_adaptive_segmented_data_packets

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L831)

```python
def fetch_adaptive_segmented_data_packets(
    base_name: str,
    on_packet: Callable[[DataPacket], None],
    *,
    timeout_ms: int = 30000,
    interest_lifetime_ms: Optional[int] = None,
    initial_window: int = 4,
    maximum_window: int = 64,
    maximum_retries: int = 5,
    persistence_backlog_limit: int = 16,
    forwarding_hints: Optional[list[str]] = None,
) -> AdaptiveSegmentFetchResult:
```

原始接口说明：

```text
Stream adaptive NDN segments into a synchronous verifier/persistence sink.

``on_packet`` must return only after the packet is verified and persisted.
Its execution is therefore the backpressure boundary; the native fetcher
never assembles the full artifact in memory.
```

### API-297c54b23440 · fetch_exact_data_packet

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L886)

```python
def fetch_exact_data_packet(
    data_name: str,
    *,
    timeout_ms: int = 30000,
    interest_lifetime_ms: Optional[int] = None,
    forwarding_hints: Optional[list[str]] = None,
) -> DataPacket:
```

原始接口说明：

```text
Fetch one stored Data packet by its complete exact name.
```

### API-80d528af496a · fetch_segmented_object

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L909)

```python
def fetch_segmented_object(
    base_name: str,
    *,
    timeout_ms: int = 30000,
    interest_lifetime_ms: Optional[int] = None,
    init_cwnd: float = 8.0,
    forwarding_hints: Optional[list[str]] = None,
) -> bytes:
```

原始接口说明：

```text
Fetch signed segmented NDN Data with ndn-cxx SegmentFetcher.
```

### API-31a31e361682 · fetch_segmented_object_with_segment_hints

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L928)

```python
def fetch_segmented_object_with_segment_hints(
    base_name: str,
    *,
    timeout_ms: int = 30000,
    interest_lifetime_ms: Optional[int] = None,
    hint_ranges: Optional[list[SegmentHintRange]] = None,
) -> bytes:
```

原始接口说明：

```text
Fetch segmented Data while allowing each segment range to use hints.
```

### API-9a278c195412 · fetch_known_segmented_object_with_segment_hints

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L952)

```python
def fetch_known_segmented_object_with_segment_hints(
    versioned_name: str,
    segment_count: int,
    *,
    timeout_ms: int = 30000,
    interest_lifetime_ms: Optional[int] = None,
    hint_ranges: Optional[list[SegmentHintRange]] = None,
) -> bytes:
```

原始接口说明：

```text
Fetch known signed segments with per-range forwarding hints.
```

### API-79e9afc2a891 · CollaborationRole

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L979)

```python
class CollaborationRole:
```

### API-270babdb2ddc · CollaborationRole.role

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L980)

```python
role: str
```

### API-163d680f0d9b · CollaborationRole.service

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L981)

```python
service: str = ""
```

### API-3f1afe74b1ae · CollaborationRole.artifact

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L982)

```python
artifact: str = ""
```

### API-361036cc55bf · CollaborationRole.allow_dynamic_provisioning

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L983)

```python
allow_dynamic_provisioning: bool = False
```

### API-290b544e129b · CollaborationRole.provisioning_timeout_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L984)

```python
provisioning_timeout_ms: int = 30000
```

### API-5b2c96050e29 · CollaborationRole.app_requirement

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L985)

```python
app_requirement: bytes = b""
```

### API-63a69d1c2546 · CollaborationRole.assignment_payload

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L986)

```python
assignment_payload: bytes = b""
```

### API-baba83225f4d · CollaborationRole.min_providers

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L987)

```python
min_providers: int = 1
```

### API-bf02d8db8a00 · CollaborationRole.max_providers

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L988)

```python
max_providers: int = 1
```

### API-8a705d6fc419 · CollaborationRole.terminal_response_owner

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L989)

```python
terminal_response_owner: bool = False
```

### API-82758a99a0dc · CollaborationDependency

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L993)

```python
class CollaborationDependency:
```

### API-1f39f5f4e249 · CollaborationDependency.producers

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L994)

```python
producers: list[str]
```

### API-da76f6dca281 · CollaborationDependency.consumers

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L995)

```python
consumers: list[str]
```

### API-2949a0aea9a4 · CollaborationDependency.key_scope

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L996)

```python
key_scope: str
```

### API-798348e01dc2 · CollaborationDependency.topic_prefix

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L997)

```python
topic_prefix: str
```

### API-e8b8c0fae490 · CollaborationDependency.required

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L998)

```python
required: bool = True
```

### API-7cc50d5da25c · CollaborationAssignment

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1002)

```python
class CollaborationAssignment:
```

### API-b89842c11afb · CollaborationAssignment.role

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1003)

```python
role: str
```

### API-258f023432f8 · CollaborationAssignment.service

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1004)

```python
service: str
```

### API-7e8adbd35426 · CollaborationAssignment.assigned_artifact

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1005)

```python
assigned_artifact: str
```

### API-759bba0c0174 · CollaborationAssignment.artifact_data_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1006)

```python
artifact_data_name: str = ""
```

### API-8269c4981849 · CollaborationAssignment.requires_provisioning

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1007)

```python
requires_provisioning: bool = False
```

### API-0e0a38d3c4ee · CollaborationAssignment.provisioning_timeout_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1008)

```python
provisioning_timeout_ms: int = 0
```

### API-7e86b16f4d75 · CollaborationAssignment.selection_digest

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1009)

```python
selection_digest: str = ""
```

### API-6765c2ed3282 · CollaborationAssignment.assignment_payload

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1010)

```python
assignment_payload: bytes = b""
```

### API-97ee5f837599 · CollaborationAssignment.role_providers

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1011)

```python
role_providers: dict[str, str] = field(default_factory=dict)
```

### API-19fc2628e787 · CollaborationData

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1015)

```python
class CollaborationData:
```

### API-d71523507836 · CollaborationData.session_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1016)

```python
session_id: str
```

### API-64e599986cf1 · CollaborationData.key_scope

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1017)

```python
key_scope: str
```

### API-02b7f03faa3d · CollaborationData.topic

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1018)

```python
topic: str
```

### API-d338b3e46acd · CollaborationData.producer

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1019)

```python
producer: str
```

### API-f581a542b0b5 · CollaborationData.producer_role

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1020)

```python
producer_role: str
```

### API-62d814d6cb0f · CollaborationData.sequence

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1021)

```python
sequence: int
```

### API-6249a4b3f8cf · CollaborationData.payload

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1022)

```python
payload: bytes
```

### API-4738017c6d40 · VerifiedCollaborationData

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1026)

```python
class VerifiedCollaborationData:
```

原始接口说明：

```text
Provider-signed collaboration Data admitted by the requesting User.
```

### API-86b8ff06d80e · VerifiedCollaborationData.data_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1029)

```python
data_name: str
```

### API-4244cef9d469 · VerifiedCollaborationData.request_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1030)

```python
request_id: str
```

### API-e012290cae29 · VerifiedCollaborationData.key_scope

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1031)

```python
key_scope: str
```

### API-52fa450f3eef · VerifiedCollaborationData.topic

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1032)

```python
topic: str
```

### API-63c4a7be7396 · VerifiedCollaborationData.producer

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1033)

```python
producer: str
```

### API-2336d6ea4a34 · VerifiedCollaborationData.producer_role

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1034)

```python
producer_role: str
```

### API-5ae542886044 · VerifiedCollaborationData.sequence

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1035)

```python
sequence: int
```

### API-b4c39707d2fc · VerifiedCollaborationData.payload

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1036)

```python
payload: bytes
```

### API-a0d2e2711c1d · VerifiedCollaborationData.signer_certificate

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1037)

```python
signer_certificate: str
```

### API-5f1ce9f76b89 · VerifiedCollaborationData.wire_digest

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1038)

```python
wire_digest: str
```

### API-d3b9796f2e36 · CollaborationContext

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1061)

```python
class CollaborationContext:
```

原始接口说明：

```text
Provider-side context for one collaborative service invocation.

The object is valid only while the Python collaboration handler is running.
Applications can publish scoped intermediate data, wait for peer data, fetch
assigned artifacts, and publish the final response.
```

### API-416ab0afe1d0 · CollaborationContext.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1069)

```python
def __init__(self, native) -> None:
```

### API-48171affe276 · CollaborationContext.session_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1073)

```python
def session_id(self) -> str:
```

### API-db5da52ecc54 · CollaborationContext.role

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1077)

```python
def role(self) -> str:
```

### API-06a89897af7b · CollaborationContext.requester_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1081)

```python
def requester_name(self) -> str:
```

原始接口说明：

```text
Authenticated requester identity bound to this invocation.
```

### API-e0aa2051dabb · CollaborationContext.local_provider

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1086)

```python
def local_provider(self) -> str:
```

### API-0816db15c620 · CollaborationContext.assignment

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1090)

```python
def assignment(self) -> CollaborationAssignment:
```

### API-a48047718079 · CollaborationContext.fetch_artifact

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1112)

```python
def fetch_artifact(self, artifact_name: str, timeout_ms: int = 5000) -> bool:
```

### API-46f505d8922f · CollaborationContext.get_artifact

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1115)

```python
def get_artifact(self, artifact_name: str) -> Optional[bytes]:
```

### API-b3bcf0d5bf1c · CollaborationContext.fetch_encrypted_large_data

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1121)

```python
def fetch_encrypted_large_data(
        self,
        data_name: str,
        service: str = "",
    ) -> Optional[bytes]:
```

### API-95753c7404fa · CollaborationContext.fail

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1131)

```python
def fail(self, reason: str) -> None:
```

### API-f82446727a6b · CollaborationContext.allow_data

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1134)

```python
def allow_data(self, key_scope: str, topic_prefix: str) -> None:
```

原始接口说明：

```text
Allow one request-scoped collaboration binding before decryption.

Providers receive a broad SVS collaboration subscription.  Registering
the local role's scope/topic bindings lets the native layer discard
other roles' ciphertext before trying authentication with a local key.
```

### API-b29d4c6900db · CollaborationContext.publish

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1143)

```python
def publish(self, key_scope: str, topic: str, payload: bytes) -> None:
```

### API-688489661014 · CollaborationContext.publish_large

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1146)

```python
def publish_large(
        self,
        key_scope: str,
        topic: str,
        payload: bytes,
        *,
        max_segment_size: int = 7000,
        freshness_ms: int = 60000,
    ) -> str:
```

原始接口说明：

```text
Publish large scoped collaboration data as signed segments.

The returned name is intended to be carried in a small collaboration
message. Peers use :meth:`fetch_large` to retrieve, validate, reassemble,
and decrypt the object.
```

### API-366105845e82 · CollaborationContext.publish_large_named

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1170)

```python
def publish_large_named(
        self,
        key_scope: str,
        data_name: str,
        payload: bytes,
        *,
        max_segment_size: int = 7000,
        freshness_ms: int = 60000,
    ) -> str:
```

原始接口说明：

```text
Publish large collaboration data at a deterministic Data name.

This keeps the same request-scoped hybrid encryption and segment
retrieval semantics as :meth:`publish_large`, but lets a distributed
plan make object names predictable for dataflow prefetch.
```

### API-279e646753c3 · CollaborationContext.publish_large_reference

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1194)

```python
def publish_large_reference(
        self,
        key_scope: str,
        data_topic: str,
        ref_topic: str,
        payload: bytes,
        *,
        object_type: str = "",
        object_id: str = "",
        digest: str = "",
        data_name: str = "",
        max_segment_size: int = 7000,
        freshness_ms: int = 60000,
    ) -> str:
```

原始接口说明：

```text
Publish a large collaboration object and advertise a standard reference.
```

### API-c42758e431f7 · CollaborationContext.fetch_large

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1243)

```python
def fetch_large(
        self,
        data_name: str,
        key_scope: str,
        timeout_ms: int = 5000,
    ) -> Optional[bytes]:
```

### API-0fb6fc14c6d9 · CollaborationContext.fetch_large_exact

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1254)

```python
def fetch_large_exact(
        self,
        data_name: str,
        key_scope: str,
        timeout_ms: int = 5000,
        expected_segments: int = 0,
    ) -> Optional[bytes]:
```

### API-b847d09b8e7b · CollaborationContext.fetch_large_reference

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1273)

```python
def fetch_large_reference(
        self,
        reference_payload: Union[bytes, Mapping[str, Any]],
        key_scope: str,
        timeout_ms: int = 5000,
    ) -> Optional[bytes]:
```

原始接口说明：

```text
Fetch and verify a large collaboration object reference.

The DI provider path passes the structured reference mapping directly
so that ``dataName``, size, content digest, encryption bit, and
authorization scope are checked before plaintext is returned. The
legacy wire-payload form remains supported for existing callers, but
it cannot supply the DI-only authorization-scope check.
```

### API-6bd330eef499 · CollaborationContext.wait_one

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1341)

```python
def wait_one(
        self,
        key_scope: str,
        topic_prefix: str,
        timeout_ms: int = 5000,
    ) -> Optional[CollaborationData]:
```

### API-d0f11eb412e7 · CollaborationContext.wait_for

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1352)

```python
def wait_for(
        self,
        key_scope: str,
        topic_prefix: str,
        min_count: int,
        timeout_ms: int = 5000,
    ) -> list[CollaborationData]:
```

### API-72e5c26357a5 · CollaborationContext.report_operation_status

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1364)

```python
def report_operation_status(self, status: ServiceOperationStatus) -> None:
```

原始接口说明：

```text
Report the latest observation through signed SELECTION-STATUS.

This is generic progress only. Applications still own any exact
readiness or completion certificate needed to authorize execution.
```

### API-47d8583e19ea · CollaborationContext.publish_final_response

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1376)

```python
def publish_final_response(self, payload: bytes) -> None:
```

### API-78a8faf91618 · CollaborationContext.is_streamed

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1380)

```python
def is_streamed(self) -> bool:
```

### API-d3de3c2d615d · CollaborationContext.publish_stream_event

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1383)

```python
def publish_stream_event(self, payload: bytes) -> int:
```

### API-9f67c1b175a2 · CollaborationContext.finish_stream

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1389)

```python
def finish_stream(self, payload: bytes = b"", *, reason: int = 4) -> bool:
```

### API-0519790daa6f · CollaborationContext.fail_stream

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1392)

```python
def fail_stream(self, code: int, message: str) -> bool:
```

### API-5c7f3559bf58 · CollaborationContext.stream_cancelled

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1396)

```python
def stream_cancelled(self) -> bool:
```

### API-975e1fb51665 · CollaborationInvocation

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1636)

```python
class CollaborationInvocation:
```

原始接口说明：

```text
One durable generic collaboration request with deferred plan commit.
```

### API-2a0942db5330 · CollaborationInvocation.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1643)

```python
def __init__(
        self,
        *,
        native,
        state: _CollaborationInvocationState,
        service: str,
        request_id: str,
        ack_timeout_ms: int,
        timeout_ms: int,
        fail_fast_terminal_selection: bool = False,
    ) -> None:
```

### API-0a56115b2573 · CollaborationInvocation.acks_closed

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1664)

```python
def acks_closed(self, timeout_ms: Optional[int] = None) -> CollaborationAckClosed:
```

### API-9233e5bcaa6c · CollaborationInvocation.commit_plan

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1684)

```python
def commit_plan(
        self,
        *,
        ack_closed_digest: str,
        roles: list[CollaborationRole | dict],
        key_scopes: dict[str, list[str]],
        dependencies: Optional[list[CollaborationDependency | dict]] = None,
        artifact_data_names: Optional[dict[str, str]] = None,
        scope_key_data_names: Optional[dict[str, str]] = None,
        role_scopes: Optional[dict[str, list[str]]] = None,
        role_provider_assignments: Optional[dict[str, str]] = None,
        assignment_payloads_by_role: Optional[Mapping[str, bytes]] = None,
    ) -> bool:
```

### API-b3fbc214eb67 · CollaborationInvocation.result

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1726)

```python
def result(self, timeout_ms: Optional[int] = None) -> ServiceResponse:
```

### API-cc7316b66bf9 · CollaborationInvocation.cancel

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1754)

```python
def cancel(self) -> None:
```

原始接口说明：

```text
Cancel local delivery and terminal acceptance for this stream.
```

### API-fc1f1768c985 · CollaborationInvocation.stream_metrics_for_test

public-by-name / test-helper；[源码](../../pythonWrapper/ndnsf/service.py#L1759)

```python
def stream_metrics_for_test(self) -> Mapping[str, int]:
```

原始接口说明：

```text
Return Core stream counters for registered integration gates.
```

### API-79f4eafe0bd8 · CollaborationInvocation.stream_events

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1764)

```python
def stream_events(self) -> tuple[bytes, ...]:
```

原始接口说明：

```text
Snapshot of verified stream events received for this request.
```

### API-e6849d6abc55 · CollaborationInvocation.stream_complete

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1770)

```python
def stream_complete(self) -> Optional[bytes]:
```

### API-b1ebe4a1d263 · CollaborationInvocation.stream_error

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1775)

```python
def stream_error(self) -> Optional[Mapping[str, Any]]:
```

### API-d815b8add55a · ServiceProvider

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1822)

```python
class ServiceProvider:
```

原始接口说明：

```text
Python API for writing NDNSF provider business logic.
```

### API-1901b0f96b94 · ServiceProvider.provider_identity

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1826)

```python
def provider_identity(self) -> str:
```

原始接口说明：

```text
Return the Provider identity used by the native runtime.
```

### API-1325c491fa8e · ServiceProvider.provider_signing_key_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1832)

```python
def provider_signing_key_name(self) -> str:
```

原始接口说明：

```text
Return the public NDN key name used for Provider-signed Data.
```

### API-2db9a3fd4e87 · ServiceProvider.provider_signing_certificate_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1838)

```python
def provider_signing_certificate_name(self) -> str:
```

原始接口说明：

```text
Return the public certificate name used for Provider-signed Data.
```

### API-a700b986e70c · ServiceProvider.signing_metadata

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1844)

```python
def signing_metadata(self) -> ProviderSigningMetadata:
```

原始接口说明：

```text
Return public signing metadata for an external application signer.
```

### API-749f02d5f452 · ServiceProvider.provider_boot_epoch

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1854)

```python
def provider_boot_epoch(self) -> str:
```

原始接口说明：

```text
Return Core's stable process-incarnation fence.
```

### API-fc251fdec0a1 · ServiceProvider.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1859)

```python
def __init__(
        self,
        *,
        provider_id: str = "",
        group: str = "/example/hello/group",
        controller: str = "/example/hello/controller",
        provider_prefix: str = "/example/hello/provider",
        trust_schema: str = "examples/trust-schema.conf",
        handler_threads: int = 4,
        ack_threads: int = 2,
        serve_certificates: bool = True,
        bootstrap_token: str = "",
        binary: str = "",
        binary_dir=None,
        library_dirs=None,
        cwd=None,
        env=None,
    ) -> None:
```

### API-fc7310e483a8 · ServiceProvider.create_live_stream

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1900)

```python
def create_live_stream(self, definition):
```

原始接口说明：

```text
Create the Core-owned publisher; application supplies opaque bytes only.
```

### API-f75dac8ebbfb · ServiceProvider.create_stream

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1909)

```python
def create_stream(self, config):
```

原始接口说明：

```text
Create the high-level stream facade with Core-owned defaults.
```

### API-f8f5cbceae36 · ServiceProvider.add_handler

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1918)

```python
def add_handler(
        self,
        service: str,
        handler: Callable[[bytes], bytes | ServiceResponse],
    ) -> None:
```

### API-9fdb46300e10 · ServiceProvider.add_streaming_handler

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1925)

```python
def add_streaming_handler(
        self,
        service: str,
        handler: Callable[[bytes, Any], object],
    ) -> None:
```

原始接口说明：

```text
Register a streamed handler.

The callback receives the opaque request bytes and a Core-owned
``StreamWriter``.  It must call ``publish_event`` zero or more times
and exactly one ``finish_stream`` or ``fail``; the C++ runtime owns
event cursors, encryption, signing, retention, and terminal fencing.
```

### API-a9b898e4bc7f · ServiceProvider.add_streaming_context_handler

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1942)

```python
def add_streaming_context_handler(
        self,
        service: str,
        handler: Callable[[Mapping[str, str], bytes, Any], object],
    ) -> None:
```

原始接口说明：

```text
Register a streamed handler with authenticated invocation names.
```

### API-0f3150b5862e · ServiceProvider.set_stream_publication_interceptor_for_test

public-by-name / test-helper；[源码](../../pythonWrapper/ndnsf/service.py#L1953)

```python
def set_stream_publication_interceptor_for_test(self, callback) -> None:
```

原始接口说明：

```text
Install a real-SVS publication interceptor for an integration test.
```

### API-09e1961b2ff7 · ServiceProvider.set_stream_retention_interceptor_for_test

public-by-name / test-helper；[源码](../../pythonWrapper/ndnsf/service.py#L1957)

```python
def set_stream_retention_interceptor_for_test(self, callback) -> None:
```

原始接口说明：

```text
Install a real-SVS retention interceptor for an integration test.
```

### API-96775f69b048 · ServiceProvider.publish_stream_packet_for_test

public-by-name / test-helper；[源码](../../pythonWrapper/ndnsf/service.py#L1961)

```python
def publish_stream_packet_for_test(self, wire: bytes) -> None:
```

原始接口说明：

```text
Re-publish one already signed stream packet through real SVS.
```

### API-66cf3bd91f8f · ServiceProvider.streaming_handler

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1965)

```python
def streaming_handler(self, service: str):
```

### API-68ddaae7d3d3 · ServiceProvider.streaming_context_handler

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1971)

```python
def streaming_context_handler(self, service: str):
```

### API-0baf56a08680 · ServiceProvider.handler

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1977)

```python
def handler(self, service: str):
```

### API-084732257d7b · ServiceProvider.add_context_handler

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1983)

```python
def add_context_handler(
        self,
        service: str,
        handler: Callable[[dict[str, str], bytes], bytes | ServiceResponse],
    ) -> None:
```

原始接口说明：

```text
Register a handler that receives authenticated invocation context.
```

### API-21e1775f1793 · ServiceProvider.set_ack_handler

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L1993)

```python
def set_ack_handler(
        self,
        service: str,
        handler: Callable[[bytes], bool | AckDecision],
    ) -> None:
```

### API-23e108d3cd32 · ServiceProvider.set_ack_context_handler

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2001)

```python
def set_ack_context_handler(
        self,
        service: str,
        handler: Callable[[Mapping[str, Any], bytes], bool | AckDecision],
    ) -> None:
```

原始接口说明：

```text
Register an ACK policy with authenticated Request metadata.

The context exposes negotiated capabilities and opaque encrypted input;
it never exposes decrypted application input.
```

### API-e21f88d3dd54 · ServiceProvider.ack_handler

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2014)

```python
def ack_handler(self, service: str):
```

### API-e9971bb38144 · ServiceProvider.configure_opaque_selection_store

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2020)

```python
def configure_opaque_selection_store(
        self,
        *,
        wal_path: str,
        storage_key: bytes,
        storage_key_epoch: str,
        max_prepare_ms: int = 1000,
    ) -> None:
```

原始接口说明：

```text
Configure Core's encrypted durable Selection transaction journal.

``storage_key`` must be an application-supplied 32-byte secret. Core
never derives it from a token or writes it into the journal. The
participant must be registered only after this store is configured.
```

### API-db9d1212b48d · ServiceProvider.register_opaque_selection_participant

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2045)

```python
def register_opaque_selection_participant(
        self,
        service: str,
        *,
        participant_id: str,
        participant_version: int,
        prepare: Callable[[Mapping[str, Any], bytes], Mapping[str, bytes]],
        on_committed: Callable[[Mapping[str, Any]], None],
        on_aborted: Callable[[str, str], None],
    ) -> None:
```

原始接口说明：

```text
Register service-owned policy behind Core's generic Selection seam.

``prepare`` receives an immutable authenticated context and the exact
opaque Selection payload. It must be side-effect free and return only
``commit_blob`` and ``acceptance_payload`` bytes. Core bounds its
execution, computes both digests, fsyncs the encrypted commit record,
and only then calls ``on_committed``. This API is model-agnostic.
```

### API-de37548ba8b9 · ServiceProvider.set_deployment_prepare_handler

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2095)

```python
def set_deployment_prepare_handler(
        self, handler: Callable[[dict[str, Any]], Mapping[str, str]],
    ) -> None:
```

原始接口说明：

```text
Register generic verify/load/warm preparation after Selection.

The callback returns readiness-specific fields such as
``artifactDigest``, ``deploymentInstanceId``, and ``operationId``.
Core overwrites all authority and identity bindings before sending the
signed ProviderReadyMessage.
```

### API-3e5e5449664a · ServiceProvider.set_r1_selection_decision_handler

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2111)

```python
def set_r1_selection_decision_handler(
        self, service: str,
        handler: Callable[[Mapping[str, str]], Mapping[str, str]],
    ) -> None:
```

原始接口说明：

```text
Apply a Core-authenticated R1 decision in application policy.

The handler owns reservation commit/release semantics and must return
a receipt bound to the supplied decision digest and reservation ID.
```

### API-c2ae7ee59461 · ServiceProvider.set_r1_reservation_terminal_handler

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2126)

```python
def set_r1_reservation_terminal_handler(
        self, service: str, handler: Callable[[str, str], None],
    ) -> None:
```

原始接口说明：

```text
Release a DI-owned committed reservation on local termination.
```

### API-dbd2a22d0bc3 · ServiceProvider.add_collaboration_handler

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2160)

```python
def add_collaboration_handler(
        self,
        service: str,
        allowed_roles: list[str],
        handler: Callable[[CollaborationContext, bytes], None],
        ack_handler: Optional[Callable[[bytes], bool | AckDecision]] = None,
        *,
        include_ack_context: bool = False,
    ) -> None:
```

### API-6035358deee2 · ServiceProvider.collaboration_handler

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2189)

```python
def collaboration_handler(
        self,
        service: str,
        allowed_roles: list[str],
        ack_handler: Optional[Callable[[bytes], bool | AckDecision]] = None,
    ):
```

### API-889f3a2d877a · ServiceProvider.run

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2200)

```python
def run(self, service: Optional[str] = None) -> int:
```

### API-083e25e32fcf · ServiceProvider.start

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2219)

```python
def start(self, service: Optional[str] = None) -> None:
```

原始接口说明：

```text
Start the native provider after synchronously installing handlers.

``run()`` remains the blocking compatibility entry point.  This
explicit start seam lets an application publish its own readiness
marker only after Core has installed all request/ACK registrations and
started the Face event loop; a subsequent ``run()`` is idempotent.
```

### API-4236bce3b589 · ServiceProvider.publish_service_info

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2251)

```python
def publish_service_info(self,
                             service_name: str,
                             service_lifetime_seconds: int = 30,
                             meta_info: Optional[dict[str, str]] = None) -> None:
```

原始接口说明：

```text
Publish service availability and capacity via NDNSD (requires NDNSF_ENABLE_NDNSD=1).
```

### API-8bf2a8f49253 · ServiceProvider.update_ndnsd_meta

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2258)

```python
def update_ndnsd_meta(self, key: str, value: str) -> None:
```

原始接口说明：

```text
Update one key in the internal NDNSD meta dict (thread-safe).

Changes are picked up by the next periodic heartbeat.
```

### API-471e6a7800a4 · ServiceProvider.set_ndnsd_meta

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2265)

```python
def set_ndnsd_meta(self, meta: dict[str, str]) -> None:
```

原始接口说明：

```text
Replace the entire internal NDNSD meta dict (thread-safe).
```

### API-87ac6b3fcb7a · ServiceProvider.start_ndnsd_heartbeat

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2269)

```python
def start_ndnsd_heartbeat(self, interval_seconds: int = 10) -> None:
```

原始接口说明：

```text
Start periodic NDNSD heartbeat using the C++ io_context scheduler.

Reads the internal meta dict (updated via update_ndnsd_meta) each tick.
Publishes for every registered service.
```

### API-62f03d817f57 · ServiceProvider.start_background

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2277)

```python
def start_background(self, service: Optional[str] = None) -> threading.Thread:
```

### API-48c2fee0f9b2 · ServiceProvider.stop

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2292)

```python
def stop(self) -> int:
```

### API-8604790136f2 · ServiceController

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2297)

```python
class ServiceController:
```

原始接口说明：

```text
Python API for running the NDNSF ServiceController role.
```

### API-2eea06f8782c · ServiceController.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2300)

```python
def __init__(
        self,
        *,
        controller_prefix: str = "/example/hello/controller",
        policy_file: str = "examples/hello.policies",
        trust_schema: str = "examples/trust-schema.conf",
        bootstrap_identities: Optional[list[str]] = None,
        serve_certificates: bool = True,
        bootstrap_token_file: str = "",
        binary: str = "",
        binary_dir=None,
        library_dirs=None,
        cwd=None,
        env=None,
    ) -> None:
```

### API-e991712343f0 · ServiceController.start

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2325)

```python
def start(self) -> None:
```

### API-a4cf53c11ba7 · ServiceController.run

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2336)

```python
def run(self) -> int:
```

### API-edb60bdd3637 · ServiceController.stop

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2340)

```python
def stop(self) -> int:
```

### API-f2d1c2edae85 · ServiceController.start_background

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2344)

```python
def start_background(self) -> threading.Thread:
```

### API-b2640c498728 · ServiceUser

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2358)

```python
class ServiceUser:
```

原始接口说明：

```text
Python API for issuing NDNSF service requests.
```

### API-574ecab0bb05 · ServiceUser.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2361)

```python
def __init__(
        self,
        *,
        group: str = "/example/hello/group",
        controller: str = "/example/hello/controller",
        user: str = "/example/hello/user",
        trust_schema: str = "examples/trust-schema.conf",
        permission_wait_ms: int = 1500,
        handler_threads: int = 2,
        ack_threads: int = 2,
        adaptive_admission: bool = False,
        serve_certificates: bool = True,
        bootstrap_token: str = "",
        binary: str = "",
        binary_dir=None,
        library_dirs=None,
        cwd=None,
        env=None,
    ) -> None:
```

### API-8fb1a1133d8d · ServiceUser.open_live_stream

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2398)

```python
def open_live_stream(
        self,
        descriptor,
        *,
        start: str = "latest",
        prefetch_policy: str | None = None,
        aggregate_interest_limit: int = 64,
        enable_fec_recovery: bool = False,
        interest_lifetime_ms: int = 500,
        on_item,
        on_status=None,
    ):
```

原始接口说明：

```text
Open semantic-name prefetch; callback receives validated opaque bytes.
```

### API-839525c26009 · ServiceUser.subscribe_stream

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2448)

```python
def subscribe_stream(self, descriptor, options):
```

原始接口说明：

```text
Open and start one high-level stream subscription.
```

### API-a574c8117f6c · ServiceUser.request_service

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2493)

```python
def request_service(
        self,
        service: str,
        payload: bytes,
        *,
        ack_timeout_ms: int = 300,
        timeout_ms: int = 5000,
        strategy: str = "first-responding",
        request_id: str = "",
        deployment_intent: Optional[
            Union[Mapping[str, str], _ndnsf.NativeDeploymentIntent]
        ] = None,
        request_capabilities: Optional[
            Union[Mapping[str, str], _ndnsf.NativeRequestCapabilities]
        ] = None,
    ) -> ServiceResponse:
```

### API-192a8b1914fa · ServiceUser.request_service_streaming

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2521)

```python
def request_service_streaming(
        self,
        service: str,
        request,
        *,
        options: Optional[StreamedInvocationOptions] = None,
        strategy: str = "first-responding",
        event_decoder: Optional[Callable[[bytes], Any]] = None,
        response_decoder: Optional[Callable[[bytes], Any]] = None,
        on_event: Optional[Callable[[Any], None]] = None,
        on_complete: Optional[Callable[[Any], None]] = None,
        on_error: Optional[Callable[[StreamedInvocationError], None]] = None,
        target_provider: Optional[str] = None,
    ) -> StreamedInvocation:
```

原始接口说明：

```text
Start one Core-owned streamed invocation and return its lifecycle.

Normal mode accepts no Provider list or target.  Targeted mode accepts
exactly one ``target_provider``.  Applications consume events either
through ``on_event`` or through ``async for`` on the returned handle.
```

### API-9efa59fae3e4 · ServiceUser.request_streaming

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2599)

```python
def request_streaming(
        self,
        service: str,
        payload: bytes,
        *,
        on_event: Callable[[bytes], None],
        on_complete: Callable[[bytes], None],
        on_error: Callable[[Mapping[str, Any]], None],
        provider: str = "",
    ) -> str:
```

原始接口说明：

```text
Start one request-scoped streamed invocation.

``provider`` is empty for Normal selection and identifies exactly one
Provider for Targeted mode.  The returned request ID is allocated by
the C++ runtime; callbacks receive opaque event/final payload bytes.
```

### API-7c6531e9f67d · ServiceUser.request_service_targeted

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2621)

```python
def request_service_targeted(
        self,
        provider: str,
        service: str,
        payload: bytes,
        *,
        timeout_ms: int = 5000,
    ) -> ServiceResponse:
```

原始接口说明：

```text
Invoke a known provider through NDNSF's authenticated Targeted path.

``timeout_ms`` is one total deadline beginning when the request is
submitted, including local admission, token bootstrap, and publication.
```

### API-77ef96165929 · ServiceUser.query_collaboration_status

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2643)

```python
def query_collaboration_status(
        self, *, provider: str, service: str, selection_digest: str,
        timeout_ms: int = 500,
    ) -> Optional[CollaborationSelectionStatus]:
```

### API-63b97e239535 · ServiceUser.collaboration_status

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2658)

```python
def collaboration_status(
        self, request_id: str, *, timeout_ms: int = 500,
    ) -> tuple[CollaborationSelectionStatus, ...]:
```

原始接口说明：

```text
Return the latest validated per-Provider snapshots for a request.
```

### API-4350094c06bb · ServiceUser.wait_for_verified_collaboration_data

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2670)

```python
def wait_for_verified_collaboration_data(
        self,
        request_id: str,
        *,
        key_scope: str,
        topic_prefix: str,
        min_count: int,
        timeout_ms: int,
        consume: bool = True,
    ) -> tuple[VerifiedCollaborationData, ...]:
```

原始接口说明：

```text
Wait off the Face thread for authenticated Provider publications.

The C++ User admits a record only after the outer Provider signature,
request/name binding, scope metadata, and request-scope AEAD all pass.
```

### API-0a3182cb443f · ServiceUser.clear_verified_collaboration_data

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2725)

```python
def clear_verified_collaboration_data(
        self, request_id: str, *, key_scope: str,
    ) -> None:
```

原始接口说明：

```text
Release authenticated collaboration records and their scope key.

``wait_for_verified_collaboration_data(..., consume=True)`` consumes
records but deliberately keeps the key alive for a subsequent
control/ack exchange.  Call this once the transaction is terminal.
```

### API-8ad358522682 · ServiceUser.publish_collaboration_data

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2739)

```python
def publish_collaboration_data(
        self,
        target_provider: str,
        request_id: str,
        *,
        key_scope: str,
        topic: str,
        payload: bytes,
    ) -> bool:
```

原始接口说明：

```text
Publish one requester-owned encrypted collaboration control record.
```

### API-0b36ec8cc3a8 · ServiceUser.watch_collaboration_request

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2758)

```python
def watch_collaboration_request(
        self, request_id: str, *, timeout_ms: int = 5000,
        query_interval_ms: int = 250,
        idle_timeout_ms: Optional[int] = None,
        hard_timeout_ms: Optional[int] = None,
        _clock: Callable[[], float] = time.monotonic,
        _sleep: Callable[[float], None] = time.sleep,
    ):
```

### API-43b43f87b697 · ServiceUser.wait_collaboration_request

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2843)

```python
def wait_collaboration_request(
        self, request_id: str, *, predicate,
        timeout_ms: int = 5000, query_interval_ms: int = 250,
        idle_timeout_ms: Optional[int] = None,
        hard_timeout_ms: Optional[int] = None,
    ) -> tuple[CollaborationSelectionStatus, ...]:
```

### API-97315ee8afb3 · ServiceUser.watch_collaboration_status

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2858)

```python
def watch_collaboration_status(
        self, *, provider: str, service: str, selection_digest: str,
        timeout_ms: int = 5000, query_interval_ms: int = 250,
    ):
```

原始接口说明：

```text
Yield only monotonic member snapshots until the local watch ends.
```

### API-60ddde37250d · ServiceUser.wait_collaboration_status

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2889)

```python
def wait_collaboration_status(
        self, *, provider: str, service: str, selection_digest: str,
        predicate: Callable[[CollaborationSelectionStatus], bool],
        timeout_ms: int = 5000, query_interval_ms: int = 250,
    ) -> CollaborationSelectionStatus:
```

### API-2617550b12f7 · ServiceUser.request_service_select

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2902)

```python
def request_service_select(
        self,
        service: str,
        payload: bytes,
        selector: Callable[[list[AckCandidate]], list[str]],
        *,
        ack_timeout_ms: int = 300,
        timeout_ms: int = 5000,
        request_strategy: str = "first-responding",
        deployment_intent: Optional[
            Union[Mapping[str, str], _ndnsf.NativeDeploymentIntent]
        ] = None,
        request_capabilities: Optional[
            Union[Mapping[str, str], _ndnsf.NativeRequestCapabilities]
        ] = None,
    ) -> ServiceResponse:
```

原始接口说明：

```text
Request a service using an application-defined ACK selector.

``selector`` receives all ACK candidates collected during
``ack_timeout_ms`` and returns provider names to select. This is the
generic hook used by DistributedRepo to select exactly N repo replicas
from one shared repo service name.
```

### API-dcca5f57e0cf · ServiceUser.request_service_async

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2963)

```python
def request_service_async(
        self,
        service: str,
        payload: bytes,
        *,
        on_response: Callable[[ServiceResponse], None],
        on_timeout: Callable[[str], None],
        ack_timeout_ms: int = 300,
        timeout_ms: int = 5000,
        strategy: str = "first-responding",
    ) -> None:
```

原始接口说明：

```text
Submit a request and return immediately.

The C++ runtime owns Face/SVS/NAC-ABE processing in a background event
loop. Python only receives final response or timeout callbacks.
```

### API-30e3451b0daf · ServiceUser.request_service_targeted_async

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L2990)

```python
def request_service_targeted_async(
        self,
        provider: str,
        service: str,
        payload: bytes,
        *,
        on_response: Callable[[ServiceResponse], None],
        on_timeout: Callable[[str], None],
        timeout_ms: int = 5000,
    ) -> None:
```

原始接口说明：

```text
Submit a known-provider Targeted request and return immediately.

Exactly one of ``on_response`` or ``on_timeout`` is delivered. The
total deadline includes admission, token bootstrap, and publication.
```

### API-d5ee6e3975c8 · ServiceUser.publish_encrypted_large_data

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L3015)

```python
def publish_encrypted_large_data(
        self,
        service: str,
        payload: bytes,
        *,
        object_label: str = "",
        freshness_ms: int = 60000,
    ) -> LargeDataPublishResult:
```

### API-d33f12db80c9 · ServiceUser.publish_signed_app_data

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L3031)

```python
def publish_signed_app_data(
        self,
        data_name: str,
        payload: bytes,
        *,
        freshness_ms: int = 60000,
    ) -> SignedAppDataResult:
```

原始接口说明：

```text
Publish signed APP Data at an exact name below this identity's DI prefix.
```

### API-dbf6ceebe6e6 · ServiceUser.fetch_signed_app_data

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L3044)

```python
def fetch_signed_app_data(
        self,
        data_name: str,
        expected_signer: str,
        *,
        timeout_ms: int = 5000,
    ) -> SignedAppDataResult:
```

原始接口说明：

```text
Fetch one exact APP record and validate its trust and signer identity.
```

### API-e8084ad3e77e · ServiceUser.begin_collaboration

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L3057)

```python
def begin_collaboration(
        self,
        service: str,
        payload: bytes,
        *,
        mode: str = "DEFERRED",
        ack_timeout_ms: int = 300,
        timeout_ms: int = 10000,
        request_id: str = "",
        fail_fast_terminal_selection: bool = False,
        ack_coverage_predicate: Optional[
            Callable[[tuple[AckCandidate, ...]], bool]
        ] = None,
        request_capabilities: Optional[
            Union[Mapping[str, str], _ndnsf.NativeRequestCapabilities]
        ] = None,
        stream_options: Optional[
            Union[StreamedInvocationOptions, Mapping[str, Any]]
        ] = None,
        on_stream_event: Optional[Callable[[bytes], None]] = None,
        on_stream_complete: Optional[Callable[[bytes], None]] = None,
        on_stream_error: Optional[Callable[[Mapping[str, Any]], None]] = None,
    ) -> CollaborationInvocation:
```

原始接口说明：

```text
Publish one generic Request and defer plan choice until ACK_CLOSED.

``ack_coverage_predicate`` is an application-owned early-close
optimization. It may only report that the observed ACK candidates
provide enough capability coverage; it cannot select providers or
mutate the immutable ACK_CLOSED/commit_plan lifecycle.
```

### API-52e302e296fd · ServiceUser.request_collaboration

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L3195)

```python
def request_collaboration(
        self,
        service: str,
        payload: bytes,
        *,
        roles: list[CollaborationRole | dict],
        key_scopes: dict[str, list[str]],
        dependencies: Optional[list[CollaborationDependency | dict]] = None,
        artifact_data_names: Optional[dict[str, str]] = None,
        scope_key_data_names: Optional[dict[str, str]] = None,
        role_scopes: Optional[dict[str, list[str]]] = None,
        ack_timeout_ms: int = 300,
        timeout_ms: int = 10000,
        ack_observer: Optional[Callable[[list[AckCandidate]], None]] = None,
        assignment_context: Optional[object] = None,
        request_id: str = "",
    ) -> ServiceResponse:
```

原始接口说明：

```text
Run a generic multi-provider collaboration.

Provider ACK payloads should include ``role=<role>;``. The built-in
Python selector assigns each requested role to the first successful ACK
advertising that role, then sends per-role assignment metadata with
artifact Data names and scope-key Data names.
``ack_observer`` receives the ACK candidates collected for the
collaboration request before the built-in role selector chooses
providers. It is observational only and must not return a value.
```

### API-b2a62479048f · ServiceUser.request_collaboration_async

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L3271)

```python
def request_collaboration_async(
        self,
        service: str,
        payload: bytes,
        *,
        roles: list[CollaborationRole | dict],
        key_scopes: dict[str, list[str]],
        dependencies: Optional[list[CollaborationDependency | dict]] = None,
        artifact_data_names: Optional[dict[str, str]] = None,
        scope_key_data_names: Optional[dict[str, str]] = None,
        role_scopes: Optional[dict[str, list[str]]] = None,
        on_response: Callable[[ServiceResponse], None],
        on_timeout: Callable[[str], None],
        ack_timeout_ms: int = 300,
        timeout_ms: int = 10000,
        assignment_context: Optional[object] = None,
        request_id: str = "",
    ) -> None:
```

原始接口说明：

```text
Submit a generic multi-provider collaboration without blocking.
```

### API-1d493ac4c84e · ServiceUser.start

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L3312)

```python
def start(self) -> None:
```

原始接口说明：

```text
Start the user's background Face event loop for async requests.
```

### API-ad5b05f6115f · ServiceUser.stop

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L3317)

```python
def stop(self) -> None:
```

原始接口说明：

```text
Stop the user's background Face event loop.
```

### API-40e181a75c50 · ServiceUser.get_allowed_services

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L3322)

```python
def get_allowed_services(self) -> list[AllowedService]:
```

原始接口说明：

```text
Return the current permission snapshot fetched from ServiceController.
```

### API-10ae1a19bfc8 · ServiceUser.refresh_permissions

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L3334)

```python
def refresh_permissions(self) -> None:
```

原始接口说明：

```text
Reissue the controller permission fetch for this existing identity.
```

### API-cc35dcf4dd0c · ServiceUser.get_ndnsd_services

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L3339)

```python
def get_ndnsd_services(self) -> list[dict[str, Any]]:
```

原始接口说明：

```text
Return received NDNSD service details from discovered providers.

Requires NDNSF_ENABLE_NDNSD=1. Each entry contains provider, serviceName,
serviceLifetime, publishTimestamp, and serviceMetaInfo dict.
```

### API-80db6fd69e6f · ServiceUser.pump

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service.py#L3350)

```python
def pump(self, milliseconds: int) -> None:
```

## pythonWrapper/ndnsf/service_discovery.py

源码 SHA-256：`207e1068a80894bef8ef772ab5c17657055efc4dfdc682d3c446c9270802868f`。

### API-fdf6340765cd · normalize_drain_state

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L34)

```python
def normalize_drain_state(value: Any) -> str:
```

### API-d4e118ae9894 · provider_ready_for_new_request

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L45)

```python
def provider_ready_for_new_request(record: Any, *, now_ms_value: int | None = None) -> bool:
```

原始接口说明：

```text
Return whether a provider should receive a new request now.
```

### API-4fdb2217b337 · ServiceDiscoveryRecord

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L62)

```python
class ServiceDiscoveryRecord:
```

### API-28932e77f2eb · ServiceDiscoveryRecord.provider_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L63)

```python
provider_name: str
```

### API-e8a805eb914b · ServiceDiscoveryRecord.service_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L64)

```python
service_name: str = ""
```

### API-b27262c66ba9 · ServiceDiscoveryRecord.ready

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L65)

```python
ready: bool = True
```

### API-026614dcc297 · ServiceDiscoveryRecord.drain_state

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L66)

```python
drain_state: str = DRAIN_ACTIVE
```

### API-698e52b8ff91 · ServiceDiscoveryRecord.reason_code

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L67)

```python
reason_code: str = ""
```

### API-ebe06e3209e2 · ServiceDiscoveryRecord.message

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L68)

```python
message: str = ""
```

### API-ada38d689337 · ServiceDiscoveryRecord.last_seen_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L69)

```python
last_seen_ms: int = field(default_factory=now_ms)
```

### API-86c12d350cd7 · ServiceDiscoveryRecord.freshness_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L70)

```python
freshness_ms: int = 0
```

### API-68606f8d653a · ServiceDiscoveryRecord.runtime_hint

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L71)

```python
runtime_hint: GenericProviderRuntimeHint | None = None
```

### API-892efb7e29ed · ServiceDiscoveryRecord.capability_hint

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L72)

```python
capability_hint: ProviderCapabilityHint | None = None
```

### API-929de3a01387 · ServiceDiscoveryRecord.metadata

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L73)

```python
metadata: dict[str, Any] = field(default_factory=dict)
```

### API-7c3504ef8335 · ServiceDiscoveryRecord.source

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L74)

```python
source: str = "manual"
```

### API-723e6c7d2298 · ServiceDiscoveryRecord.is_fresh

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L81)

```python
def is_fresh(self, *, now_ms_value: int | None = None) -> bool:
```

### API-42a56f794763 · ServiceDiscoveryRecord.ready_for_new_request

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L87)

```python
def ready_for_new_request(self, *, now_ms_value: int | None = None) -> bool:
```

### API-4e33101187ae · ServiceDiscoveryRecord.from_provider_capability_hint

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L95)

```python
def from_provider_capability_hint(
        cls,
        hint: ProviderCapabilityHint,
        *,
        source: str = "providerCapabilityHint",
    ) -> "ServiceDiscoveryRecord":
```

### API-010efcc11f9f · ServiceDiscoveryRecord.from_ndnsd_provider_state

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L119)

```python
def from_ndnsd_provider_state(
        cls,
        state: NdnsdProviderState,
        *,
        source: str = "ndnsd",
    ) -> "ServiceDiscoveryRecord":
```

### API-8ed0549e28a1 · ServiceDiscoveryRecord.from_dict

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L151)

```python
def from_dict(cls, payload: dict[str, Any]) -> "ServiceDiscoveryRecord":
```

### API-809c8a7e37c3 · ServiceDiscoverySnapshot

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L186)

```python
class ServiceDiscoverySnapshot:
```

### API-41b561d9297b · ServiceDiscoverySnapshot.service_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L187)

```python
service_name: str
```

### API-e4142061e06b · ServiceDiscoverySnapshot.records

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L188)

```python
records: tuple[ServiceDiscoveryRecord, ...] = ()
```

### API-d3399166f03e · ServiceDiscoverySnapshot.observed_at_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L189)

```python
observed_at_ms: int = field(default_factory=now_ms)
```

### API-34b8d060d9c3 · ServiceDiscoverySnapshot.from_records

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L192)

```python
def from_records(
        cls,
        service_name: str,
        records: Iterable[ServiceDiscoveryRecord | ProviderCapabilityHint | NdnsdProviderState | dict[str, Any]],
    ) -> "ServiceDiscoverySnapshot":
```

### API-01a955c252cb · ServiceDiscoverySnapshot.provider_names

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L209)

```python
def provider_names(self) -> tuple[str, ...]:
```

### API-d50208154f8b · ServiceDiscoverySnapshot.ready_records

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L212)

```python
def ready_records(self, *, now_ms_value: int | None = None) -> tuple[ServiceDiscoveryRecord, ...]:
```

### API-214bfaba8cb4 · ServiceDiscoverySnapshot.draining_records

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L218)

```python
def draining_records(self, *, now_ms_value: int | None = None) -> tuple[ServiceDiscoveryRecord, ...]:
```

### API-8cc569bc93bf · ServiceDiscoverySnapshot.stale_records

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L225)

```python
def stale_records(self, *, now_ms_value: int | None = None) -> tuple[ServiceDiscoveryRecord, ...]:
```

### API-0081f5ab1f0a · ServiceDiscoverySnapshot.unavailable_records

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/service_discovery.py#L231)

```python
def unavailable_records(self, *, now_ms_value: int | None = None) -> tuple[ServiceDiscoveryRecord, ...]:
```

## pythonWrapper/ndnsf/streaming.py

源码 SHA-256：`c1bad078774005a985a04629d2260bd93f30c863bdc38f6c0ad60ccd4bad40bc`。

显式导出：`STREAM_CHUNK_MAGIC`, `STREAM_NAME_MAP_CONTENT_TYPE_MANIFEST`, `STREAM_NAME_MAP_CONTRACT_VERSION`, `STREAM_NAME_MAP_CONTRACT_VERSION_V1`, `STREAM_NAME_MAP_CONTRACT_VERSION_V2`, `SampleClassProfile`, `SampleClassPredictionStatus`, `LiveStreamSamplePredictor`, `LiveStreamSampleReservation`, `StreamAdaptiveFetcherState`, `StreamAdvancedOptions`, `StreamChunk`, `StreamConfig`, `StreamConsumerReorderBuffer`, `StreamCursorFrontiers`, `StreamFecInfo`, `StreamFetchDecision`, `StreamHealth`, `StreamHealthState`, `StreamInfo`, `StreamMetrics`, `StreamNameMapAdmissionResult`, `StreamNameMapBlock`, `StreamNameMapCheckpoint`, `StreamNameMapEntry`, `StreamNameMapResolution`, `StreamNameMapResolverConfig`, `StreamNameResolver`, `StreamProducerBuffer`, `StreamPublisher`, `StreamSubscriptionOptions`, `PredictiveStreamCheckpoint`, `PredictiveStreamDescriptor`, `LiveStreamConsumerHandle`, `PredictiveStreamSubscriber`, `LiveStreamDefinition`, `LiveStreamDescriptor`, `LiveStreamFecOptions`, `LiveStreamGroupReservation`, `LiveStreamItemAdmission`, `LiveStreamItemReservation`, `LiveStreamPublisher`, `PublishedLiveStreamPacket`, `PublishedPacketFeed`, `VerifiedLiveStreamItem`, `decode_stream_chunk`, `encode_stream_chunk`, `make_stream_name_map_block_name`, `make_stream_name_map_root`, `stream_now_ms`。

### API-19570755591c · stream_now_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L59)

```python
def stream_now_ms() -> int:
```

### API-ac34817fc328 · StreamFecInfo

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L74)

```python
class StreamFecInfo:
```

原始接口说明：

```text
Codec-neutral FEC metadata attached to a stream chunk.

The core records the symbol layout, but it does not implement the repair
codec. A video application may use XOR parity; another application may use a
stronger code while keeping the same generic metadata shape.
```

### API-454dd21bda05 · StreamFecInfo.scheme

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L82)

```python
scheme: str = ""
```

### API-7ce547c3dc70 · StreamFecInfo.data_shards

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L83)

```python
data_shards: int = 0
```

### API-b0be2c1148fb · StreamFecInfo.parity_shards

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L84)

```python
parity_shards: int = 0
```

### API-9a1668e9397e · StreamFecInfo.symbol_index

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L85)

```python
symbol_index: int = 0
```

### API-89cb93bfe345 · StreamFecInfo.symbol_count

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L86)

```python
symbol_count: int = 0
```

### API-e20faa307555 · StreamFecInfo.data_lengths

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L87)

```python
data_lengths: tuple[int, ...] = ()
```

### API-d82d187112b6 · StreamFecInfo.source_block_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L88)

```python
source_block_id: str = ""
```

### API-13c79b67142a · StreamFecInfo.repair_symbol

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L89)

```python
repair_symbol: bool = False
```

### API-7ac093ecb3fa · StreamFecInfo.metadata

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L90)

```python
metadata: dict[str, Any] = field(default_factory=dict)
```

### API-b643882dedf6 · StreamFecInfo.to_dict

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L92)

```python
def to_dict(self) -> dict[str, Any]:
```

### API-d5a6b5c5e02d · StreamFecInfo.from_dict

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L106)

```python
def from_dict(cls, value: Optional[dict[str, Any]]) -> Optional["StreamFecInfo"]:
```

### API-9b331ee50261 · StreamFecInfo.enabled

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L122)

```python
def enabled(self) -> bool:
```

### API-c639d26ba87a · StreamInfo

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L127)

```python
class StreamInfo:
```

原始接口说明：

```text
Description returned by a stream start/control service.
```

### API-48f63737e3bb · StreamInfo.stream_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L130)

```python
stream_id: str
```

### API-fa2837f213b0 · StreamInfo.session_epoch

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L131)

```python
session_epoch: int
```

### API-160bfdf85771 · StreamInfo.stream_prefix

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L132)

```python
stream_prefix: str
```

### API-a3892d0fd68a · StreamInfo.next_seq

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L133)

```python
next_seq: int = 0
```

### API-c889efb7ff32 · StreamInfo.content_type

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L134)

```python
content_type: str = "application/octet-stream"
```

### API-87126521cd69 · StreamInfo.freshness_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L135)

```python
freshness_ms: int = 80
```

### API-0d95a130cc25 · StreamInfo.max_payload_bytes

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L136)

```python
max_payload_bytes: int = 3600
```

### API-e1e9e0060c9c · StreamInfo.window

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L137)

```python
window: int = 32
```

### API-e6fdc701cb2f · StreamInfo.lookahead

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L138)

```python
lookahead: int = 8
```

### API-b03ba7de4a78 · StreamInfo.interest_lifetime_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L139)

```python
interest_lifetime_ms: int = 500
```

### API-293d46c41374 · StreamInfo.missing_timeout_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L140)

```python
missing_timeout_ms: int = 300
```

### API-d786aca8d0ad · StreamInfo.reliability

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L141)

```python
reliability: str = "best-effort"
```

### API-898c24748e92 · StreamInfo.created_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L142)

```python
created_ms: int = field(default_factory=stream_now_ms)
```

### API-0e65fb769aef · StreamInfo.metadata

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L143)

```python
metadata: dict[str, Any] = field(default_factory=dict)
```

### API-e8b239fbc870 · StreamInfo.to_dict

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L145)

```python
def to_dict(self) -> dict[str, Any]:
```

### API-bf7a29a9a945 · StreamInfo.from_dict

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L164)

```python
def from_dict(cls, value: dict[str, Any]) -> "StreamInfo":
```

### API-881bb658e6fb · StreamChunk

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L184)

```python
class StreamChunk:
```

原始接口说明：

```text
One app-neutral stream chunk plus opaque application payload bytes.
```

### API-d5a88bfe996d · StreamChunk.stream_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L187)

```python
stream_id: str
```

### API-7fe8d9936dab · StreamChunk.session_epoch

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L188)

```python
session_epoch: int
```

### API-e18ff8315b36 · StreamChunk.seq

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L189)

```python
seq: int
```

### API-d92d05d2dcb1 · StreamChunk.payload

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L190)

```python
payload: bytes
```

### API-1930f9fff5df · StreamChunk.content_type

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L191)

```python
content_type: str = "application/octet-stream"
```

### API-5c5ff918ef2f · StreamChunk.capture_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L192)

```python
capture_ms: int = 0
```

### API-19ba9386c7bc · StreamChunk.arrival_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L193)

```python
arrival_ms: int = 0
```

### API-b4a8bc351631 · StreamChunk.deadline_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L194)

```python
deadline_ms: int = 0
```

### API-c58cc643b6fa · StreamChunk.key_chunk

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L195)

```python
key_chunk: bool = False
```

### API-782b4d3fd8e9 · StreamChunk.frame_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L196)

```python
frame_id: int = 0
```

### API-b8ddf3a5ebcc · StreamChunk.frame_first_seq

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L197)

```python
frame_first_seq: int = 0
```

### API-114c6fc22434 · StreamChunk.frame_last_seq

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L198)

```python
frame_last_seq: int = 0
```

### API-c76e659275d4 · StreamChunk.segment_index

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L199)

```python
segment_index: int = 0
```

### API-9014149f44f0 · StreamChunk.segment_count

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L200)

```python
segment_count: int = 1
```

### API-71581a3ab05d · StreamChunk.fec

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L201)

```python
fec: Optional[StreamFecInfo] = None
```

### API-426c13e9a7b7 · StreamChunk.metadata

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L202)

```python
metadata: dict[str, Any] = field(default_factory=dict)
```

### API-c7aa3e5e2a8b · StreamChunk.to_dict

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L204)

```python
def to_dict(self, *, include_payload: bool = False) -> dict[str, Any]:
```

### API-5efd59248e73 · StreamChunk.from_dict

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L229)

```python
def from_dict(cls, value: dict[str, Any], payload: bytes = b"") -> "StreamChunk":
```

### API-c26d9aa97372 · StreamChunk.with_arrival_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L251)

```python
def with_arrival_ms(self, arrival_ms: Optional[int] = None) -> "StreamChunk":
```

### API-08f220f8b1f8 · encode_stream_chunk

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L255)

```python
def encode_stream_chunk(chunk: StreamChunk) -> bytes:
```

原始接口说明：

```text
Encode a stream chunk as magic + header length + JSON header + payload.
```

### API-216137896c38 · decode_stream_chunk

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L266)

```python
def decode_stream_chunk(wire: bytes) -> StreamChunk:
```

### API-9b8ef084acc7 · StreamMetrics

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L282)

```python
class StreamMetrics:
```

### API-725bda7fd0bf · StreamMetrics.produced

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L283)

```python
produced: int = 0
```

### API-fe95b2b08b03 · StreamMetrics.evicted

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L284)

```python
evicted: int = 0
```

### API-626d99a66489 · StreamMetrics.received

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L285)

```python
received: int = 0
```

### API-3b3c60e89209 · StreamMetrics.emitted

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L286)

```python
emitted: int = 0
```

### API-becc899a59bd · StreamMetrics.duplicates

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L287)

```python
duplicates: int = 0
```

### API-7df78821cc7d · StreamMetrics.stale

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L288)

```python
stale: int = 0
```

### API-7a5eb164e0c9 · StreamMetrics.gaps

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L289)

```python
gaps: int = 0
```

### API-c1574f7d3289 · StreamMetrics.timeouts

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L290)

```python
timeouts: int = 0
```

### API-4124b896a54b · StreamMetrics.nacks

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L291)

```python
nacks: int = 0
```

### API-9c2f338c41dc · StreamMetrics.overflows

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L292)

```python
overflows: int = 0
```

### API-cf75e4740551 · StreamMetrics.max_pending

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L293)

```python
max_pending: int = 0
```

### API-db8628d89d41 · StreamMetrics.bytes_produced

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L294)

```python
bytes_produced: int = 0
```

### API-d6e5b37a248f · StreamMetrics.bytes_received

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L295)

```python
bytes_received: int = 0
```

### API-c9906a0fe364 · StreamMetrics.to_dict

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L297)

```python
def to_dict(self) -> dict[str, int]:
```

### API-0ef65d4bc6df · StreamProducerBuffer

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L407)

```python
class StreamProducerBuffer:
```

原始接口说明：

```text
Bounded sequence-indexed buffer for recently produced stream chunks.
```

### API-747d7c6962af · StreamProducerBuffer.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L410)

```python
def __init__(self, max_chunks: int = 600) -> None:
```

### API-859088e63a3c · StreamProducerBuffer.metrics

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L414)

```python
def metrics(self) -> StreamMetrics:
```

### API-0e25688bc154 · StreamProducerBuffer.put

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L417)

```python
def put(self, chunk: StreamChunk) -> None:
```

### API-7e2b0ed43bc0 · StreamProducerBuffer.get

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L420)

```python
def get(self, seq: int) -> Optional[StreamChunk]:
```

### API-5dfba0e2e5f9 · StreamProducerBuffer.encoded

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L424)

```python
def encoded(self, seq: int) -> Optional[bytes]:
```

### API-fa5a89763412 · StreamProducerBuffer.seqs

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L428)

```python
def seqs(self) -> list[int]:
```

### API-64c11fe0d397 · StreamConsumerReorderBuffer

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L435)

```python
class StreamConsumerReorderBuffer:
```

原始接口说明：

```text
Current-session reorder buffer with duplicate and stale-session guards.
```

### API-4eddde9f2556 · StreamConsumerReorderBuffer.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L438)

```python
def __init__(
        self,
        stream_id: str,
        session_epoch: int,
        *,
        next_seq: int = 0,
        max_pending: int = 512,
        history: int = 1024,
    ) -> None:
```

### API-6c3b63bfa26a · StreamConsumerReorderBuffer.next_seq

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L459)

```python
def next_seq(self) -> int:
```

### API-8980bee2e314 · StreamConsumerReorderBuffer.pending_count

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L463)

```python
def pending_count(self) -> int:
```

### API-e19befc1366e · StreamConsumerReorderBuffer.pending_bytes

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L467)

```python
def pending_bytes(self) -> int:
```

### API-3407d9bd6eb2 · StreamConsumerReorderBuffer.metrics

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L471)

```python
def metrics(self) -> StreamMetrics:
```

### API-e1594f4e8fd7 · StreamConsumerReorderBuffer.reset

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L474)

```python
def reset(self, stream_id: str, session_epoch: int, *, next_seq: int = 0) -> None:
```

### API-934a4e5ad345 · StreamConsumerReorderBuffer.push

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L479)

```python
def push(self, chunk: StreamChunk) -> list[StreamChunk]:
```

### API-c9cd12975af8 · StreamConsumerReorderBuffer.missing_sequences

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L482)

```python
def missing_sequences(self, *, limit: int = 32) -> list[int]:
```

### API-3db7955e2346 · StreamConsumerReorderBuffer.pending_sequences

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L485)

```python
def pending_sequences(self, *, limit: int = 0) -> list[int]:
```

### API-955bd4c24eda · StreamConsumerReorderBuffer.drain_ready

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L488)

```python
def drain_ready(self) -> list[StreamChunk]:
```

### API-6e4105e8a8cf · StreamConsumerReorderBuffer.skip_to

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L491)

```python
def skip_to(self, seq: int) -> None:
```

### API-bcf8a3950d27 · StreamFetchDecision

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L496)

```python
class StreamFetchDecision:
```

### API-fc713a01c17a · StreamFetchDecision.window

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L497)

```python
window: int
```

### API-bdaf1191528d · StreamFetchDecision.lookahead

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L498)

```python
lookahead: int
```

### API-4d7a0c24d194 · StreamFetchDecision.interest_lifetime_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L499)

```python
interest_lifetime_ms: int
```

### API-0aad6fe9b487 · StreamFetchDecision.missing_timeout_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L500)

```python
missing_timeout_ms: int
```

### API-ca489dad8f74 · StreamFetchDecision.sample_demand

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L501)

```python
sample_demand: int
```

### API-2a703795d0c4 · StreamFetchDecision.packet_demand

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L502)

```python
packet_demand: int
```

### API-d5d9255ee52f · StreamFetchDecision.hold_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L503)

```python
hold_ms: int
```

### API-1d02ad933235 · StreamFetchDecision.recovery_checkpoint_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L504)

```python
recovery_checkpoint_ms: int
```

### API-06299d3ef98e · StreamFetchDecision.pressure

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L505)

```python
pressure: float
```

### API-3dc3ac6dbffb · StreamFetchDecision.live_edge_confidence

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L506)

```python
live_edge_confidence: float
```

### API-91ee9696f3e4 · StreamFetchDecision.phase

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L507)

```python
phase: str
```

### API-a8fa1b168669 · StreamFetchDecision.policy_mode

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L508)

```python
policy_mode: str
```

### API-3a02a5a223c9 · StreamFetchDecision.reason

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L509)

```python
reason: str
```

### API-33568e16aa12 · StreamFetchDecision.remaining_recovery_budget_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L510)

```python
remaining_recovery_budget_ms: int = 0
```

### API-0c1b9a3d77b3 · StreamFetchDecision.mapping_begin_block

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L511)

```python
mapping_begin_block: int = 0
```

### API-195184ce6aac · StreamFetchDecision.mapping_end_block

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L512)

```python
mapping_end_block: int = 0
```

### API-513eb5cd5e56 · StreamFetchDecision.payload_begin_cursor

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L513)

```python
payload_begin_cursor: int = 0
```

### API-066b84f265df · StreamFetchDecision.payload_end_cursor

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L514)

```python
payload_end_cursor: int = 0
```

### API-c235e73bd674 · StreamFetchDecision.aggregate_in_flight_limit

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L515)

```python
aggregate_in_flight_limit: int = 0
```

### API-130505737df5 · StreamFetchDecision.mapping_budget

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L516)

```python
mapping_budget: int = 0
```

### API-a07af0e4d0dc · StreamFetchDecision.payload_budget

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L517)

```python
payload_budget: int = 0
```

### API-8d7d1bb32e57 · StreamFetchDecision.retransmission_budget

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L518)

```python
retransmission_budget: int = 0
```

### API-e9f1612d3f7e · StreamFetchDecision.future_wait_count

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L519)

```python
future_wait_count: int = 0
```

### API-503ea7d95273 · StreamFetchDecision.terminal_unproduced_advice

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L520)

```python
terminal_unproduced_advice: int = 0
```

### API-afbcb5b232e3 · StreamFetchDecision.later_cursor_advice

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L521)

```python
later_cursor_advice: int = 0
```

### API-8330b587801b · StreamFetchDecision.atomic_expansions

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L522)

```python
atomic_expansions: int = 0
```

### API-7f4a1efe537d · StreamFetchDecision.atomic_deferrals

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L523)

```python
atomic_deferrals: int = 0
```

### API-54d90d30b517 · StreamFetchDecision.mapping_ready

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L524)

```python
mapping_ready: bool = False
```

### API-2ee5b7ed79cb · StreamFetchDecision.future_wait

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L525)

```python
future_wait: bool = False
```

### API-7c03c4b4a820 · StreamFetchDecision.congestion_hold

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L526)

```python
congestion_hold: bool = False
```

### API-cd7e4e1ce3e4 · StreamFetchDecision.retransmission_eligible

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L527)

```python
retransmission_eligible: bool = False
```

### API-b11e6a9aafe3 · StreamFetchDecision.detector_profile

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L528)

```python
detector_profile: str = "none"
```

### API-389b8e4811af · StreamFetchDecision.mapping_wait_reason

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L529)

```python
mapping_wait_reason: str = "inactive"
```

### API-6cc42d0cab8a · StreamFetchDecision.capacity_reason

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L530)

```python
capacity_reason: str = ""
```

### API-02215a05f9ef · StreamHealthState

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L533)

```python
class StreamHealthState(str, Enum):
```

### API-ca123fcee71f · StreamHealth

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L542)

```python
class StreamHealth:
```

原始接口说明：

```text
App-neutral stream health snapshot.

Applications still own codecs and domain policy. This helper only reports
whether the generic stream session is fresh, congested, or producing gaps.
```

### API-30b27c85c387 · StreamHealth.stream_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L549)

```python
stream_id: str
```

### API-856974a06c91 · StreamHealth.session_epoch

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L550)

```python
session_epoch: int
```

### API-d7d23fb198a3 · StreamHealth.state

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L551)

```python
state: StreamHealthState = StreamHealthState.ACTIVE
```

### API-a1971fa57d9e · StreamHealth.next_seq

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L552)

```python
next_seq: int = 0
```

### API-e365bd5c1380 · StreamHealth.last_chunk_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L553)

```python
last_chunk_ms: int = 0
```

### API-abc9e32cb228 · StreamHealth.updated_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L554)

```python
updated_ms: int = field(default_factory=stream_now_ms)
```

### API-2b6cb10bd7c4 · StreamHealth.metrics

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L555)

```python
metrics: StreamMetrics = field(default_factory=StreamMetrics)
```

### API-ec220d93a833 · StreamHealth.fetch_decision

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L556)

```python
fetch_decision: StreamFetchDecision | None = None
```

### API-7697411d8309 · StreamHealth.reason

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L557)

```python
reason: str = ""
```

### API-6cadad4bfbdf · StreamHealth.metadata

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L558)

```python
metadata: dict[str, Any] = field(default_factory=dict)
```

### API-3bf2bc422444 · StreamHealth.from_stream

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L561)

```python
def from_stream(cls,
                    info: StreamInfo,
                    metrics: StreamMetrics,
                    *,
                    next_seq: int | None = None,
                    last_chunk_ms: int = 0,
                    fetch_decision: StreamFetchDecision | None = None,
                    stopped: bool = False,
                    stale_after_ms: int = 3000,
                    now_ms_value: int | None = None,
                    reason: str = "",
                    metadata: dict[str, Any] | None = None) -> "StreamHealth":
```

### API-269946b13b18 · StreamHealth.to_dict

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L601)

```python
def to_dict(self) -> dict[str, Any]:
```

### API-c3656c4a216c · StreamAdaptiveFetcherState

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L653)

```python
class StreamAdaptiveFetcherState:
```

原始接口说明：

```text
Generic adaptive fetch policy state.

The helper is intentionally conservative. Applications may layer
service-specific bitrate or codec decisions above this generic fetch policy.
```

### API-f85e2c5e5791 · StreamAdaptiveFetcherState.rtt_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L660)

```python
rtt_ms: float = 100.0
```

### API-6b330930035e · StreamAdaptiveFetcherState.timeout_pressure

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L661)

```python
timeout_pressure: float = 0.0
```

### API-404c23e42bff · StreamAdaptiveFetcherState.nack_pressure

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L662)

```python
nack_pressure: float = 0.0
```

### API-c92e075219ae · StreamAdaptiveFetcherState.duplicate_pressure

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L663)

```python
duplicate_pressure: float = 0.0
```

### API-f088d554d99f · StreamAdaptiveFetcherState.backlog_pressure

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L664)

```python
backlog_pressure: float = 0.0
```

### API-0ec3c83679ef · StreamAdaptiveFetcherState.min_window

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L665)

```python
min_window: int = 4
```

### API-4c631fbc3aea · StreamAdaptiveFetcherState.base_window

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L666)

```python
base_window: int = 32
```

### API-b7ace284dcf3 · StreamAdaptiveFetcherState.max_window

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L667)

```python
max_window: int = 256
```

### API-e87e586c0472 · StreamAdaptiveFetcherState.min_lookahead

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L668)

```python
min_lookahead: int = 2
```

### API-9c82414406ec · StreamAdaptiveFetcherState.base_lookahead

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L669)

```python
base_lookahead: int = 8
```

### API-9fef4f5e9746 · StreamAdaptiveFetcherState.max_lookahead

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L670)

```python
max_lookahead: int = 128
```

### API-40c448c9f10b · StreamAdaptiveFetcherState.min_interest_lifetime_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L671)

```python
min_interest_lifetime_ms: int = 100
```

### API-41687525af4c · StreamAdaptiveFetcherState.max_interest_lifetime_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L672)

```python
max_interest_lifetime_ms: int = 2000
```

### API-205962a93ed4 · StreamAdaptiveFetcherState.min_missing_timeout_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L673)

```python
min_missing_timeout_ms: int = 80
```

### API-5fefe725aaa9 · StreamAdaptiveFetcherState.max_missing_timeout_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L674)

```python
max_missing_timeout_ms: int = 1500
```

### API-a40643ddec31 · StreamAdaptiveFetcherState.live_edge_change_threshold

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L675)

```python
live_edge_change_threshold: float = 0.10
```

### API-1638e00782a5 · StreamAdaptiveFetcherState.live_edge_period_similarity

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L676)

```python
live_edge_period_similarity: float = 0.95
```

### API-c468a0de5c97 · StreamAdaptiveFetcherState.live_edge_window

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L677)

```python
live_edge_window: int = 30
```

### API-71c9fcc67cd6 · StreamAdaptiveFetcherState.live_edge_stable_required

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L678)

```python
live_edge_stable_required: int = 4
```

### API-683d2b0588fc · StreamAdaptiveFetcherState.detection_period_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L679)

```python
detection_period_ms: int = 1000
```

### API-4be56707bdab · StreamAdaptiveFetcherState.recovery_reserve_packets

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L680)

```python
recovery_reserve_packets: int = 1
```

### API-269696270367 · StreamAdaptiveFetcherState.aggregate_in_flight_limit

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L681)

```python
aggregate_in_flight_limit: int = 64
```

### API-554a3ed980b3 · StreamAdaptiveFetcherState.mapping_reserve

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L682)

```python
mapping_reserve: int = 4
```

### API-ba0134970e5f · StreamAdaptiveFetcherState.retransmission_reserve

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L683)

```python
retransmission_reserve: int = 1
```

### API-d9bae1d124df · StreamAdaptiveFetcherState.mapping_block_capacity

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L684)

```python
mapping_block_capacity: int = 16
```

### API-d468804d6e2e · StreamAdaptiveFetcherState.chase_multiplier

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L685)

```python
chase_multiplier: float = 2.0
```

### API-af459f4783c8 · StreamAdaptiveFetcherState.adjust_multiplier

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L686)

```python
adjust_multiplier: float = 0.75
```

### API-1cb3f23dd90d · StreamAdaptiveFetcherState.congestion_decrease_multiplier

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L687)

```python
congestion_decrease_multiplier: float = 0.5
```

### API-4bda4b67978a · StreamAdaptiveFetcherState.detector_profile

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L688)

```python
detector_profile: str = "ndnsf-conservative-seed"
```

### API-e662e1c3706b · StreamAdaptiveFetcherState.observe_rtt

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L724)

```python
def observe_rtt(self, sample_ms: float, *, alpha: float = 0.25) -> None:
```

### API-d09038a2eb07 · StreamAdaptiveFetcherState.record_timeout

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L729)

```python
def record_timeout(self) -> None:
```

### API-909bf0a142ab · StreamAdaptiveFetcherState.record_timeout_evidence

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L734)

```python
def record_timeout_evidence(self, cursor: int, *, known_produced: bool,
                                was_future: bool) -> None:
```

### API-2620349854de · StreamAdaptiveFetcherState.record_nack

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L741)

```python
def record_nack(self) -> None:
```

### API-30aded36babd · StreamAdaptiveFetcherState.record_nack_reason

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L746)

```python
def record_nack_reason(self, cursor: int, reason: str) -> None:
```

### API-c8fc18b51080 · StreamAdaptiveFetcherState.record_congestion_mark

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L751)

```python
def record_congestion_mark(self, cursor: int, mark: int) -> None:
```

### API-671fb6210ff7 · StreamAdaptiveFetcherState.record_duplicate

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L756)

```python
def record_duplicate(self) -> None:
```

### API-85f889ca6bbd · StreamAdaptiveFetcherState.set_backlog_pressure

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L761)

```python
def set_backlog_pressure(self, pressure: float) -> None:
```

### API-cbffe05a40a5 · StreamAdaptiveFetcherState.decay

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L766)

```python
def decay(self, factor: float = 0.85) -> None:
```

### API-eb3299990e94 · StreamAdaptiveFetcherState.reset_live

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L771)

```python
def reset_live(self, session_epoch: int, next_seq: int,
                   sample_period_ms: float, *, now_ms: int = 0) -> None:
```

### API-1e8b0b07dcae · StreamAdaptiveFetcherState.configure_mapped_live

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L777)

```python
def configure_mapped_live(self, *, aggregate_limit: int | None = None,
                              mapping_reserve: int | None = None,
                              retransmission_reserve: int | None = None,
                              block_capacity: int | None = None,
                              detector_profile: str | None = None) -> None:
```

### API-97b319e64a62 · StreamAdaptiveFetcherState.reset_mapped_live

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L802)

```python
def reset_mapped_live(self, session_epoch: int, next_cursor: int,
                          sample_period_ms: float, latest_produced_cursor: int,
                          mapping_committed_through_cursor: int,
                          next_reserved_cursor: int, *, now_ms: int = 0) -> None:
```

### API-2efa038e9511 · StreamAdaptiveFetcherState.update_mapping_frontier

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L813)

```python
def update_mapping_frontier(self, mapping_committed_through_cursor: int,
                                next_reserved_cursor: int) -> None:
```

### API-bcdd0c10aeb5 · StreamAdaptiveFetcherState.advance_next_cursor

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L818)

```python
def advance_next_cursor(self, next_cursor: int) -> None:
```

### API-c88399a2f017 · StreamAdaptiveFetcherState.set_mapped_live_policy_enabled

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L821)

```python
def set_mapped_live_policy_enabled(self, enabled: bool) -> None:
```

原始接口说明：

```text
Select live-v1 or its mapped-pressure rollback on the same wire.
```

### API-88b7a748f16c · StreamAdaptiveFetcherState.set_in_flight

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L825)

```python
def set_in_flight(self, *, mapping: int, payload: int,
                      retransmission: int) -> None:
```

### API-86e678ea532d · StreamAdaptiveFetcherState.observe_accepted_sample

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L830)

```python
def observe_accepted_sample(self, session_epoch: int, sample_id: int,
                                arrival_ms: int, retrieval_delay_ms: float,
                                *, segment_count: int = 1,
                                known_produced: bool = True) -> bool:
```

### API-f7c993423ac9 · StreamAdaptiveFetcherState.observe_sample_extent

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L841)

```python
def observe_sample_extent(self, predicted_count: int,
                              actual_count: int) -> None:
```

### API-7350a4b83b6d · StreamAdaptiveFetcherState.begin_recovery

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L846)

```python
def begin_recovery(self, now_ms: int, playout_deadline_ms: int) -> None:
```

### API-9b291ad2957f · StreamAdaptiveFetcherState.record_recovery

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L850)

```python
def record_recovery(self, completed: bool) -> None:
```

### API-4cffa16c2615 · StreamAdaptiveFetcherState.record_invalid_observation

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L853)

```python
def record_invalid_observation(self) -> None:
```

### API-bee1f365a63e · StreamAdaptiveFetcherState.stop_live

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L856)

```python
def stop_live(self) -> None:
```

### API-bb4ebb65778d · StreamAdaptiveFetcherState.phase

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L860)

```python
def phase(self) -> str:
```

### API-b31fdce83ed0 · StreamAdaptiveFetcherState.invalid_observations

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L864)

```python
def invalid_observations(self) -> int:
```

### API-0b4214e96c58 · StreamAdaptiveFetcherState.decide

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L867)

```python
def decide(self, *, now_ms: int = 0,
               playout_deadline_ms: int = 0) -> StreamFetchDecision:
```

### API-eeb80baec4d1 · StreamNameMapEntry

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L910)

```python
class StreamNameMapEntry:
```

原始接口说明：

```text
One immutable cursor slot in a :class:`StreamNameMapBlock`.

``original_name`` is the URI spelling of a real nested NDN Name TLV.  A
tombstone is declared before publication and deliberately has no name.
```

### API-be737dbf232d · StreamNameMapEntry.original_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L917)

```python
original_name: str = ""
```

### API-d11d75a556ed · StreamNameMapEntry.tombstone

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L918)

```python
tombstone: bool = False
```

### API-4b9029ce1221 · StreamNameMapEntry.group_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L919)

```python
group_id: str = ""
```

### API-a4e9b8cee909 · StreamNameMapEntry.sample_class

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L920)

```python
sample_class: str = ""
```

### API-516a14d63161 · StreamNameMapEntry.group_item_index

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L921)

```python
group_item_index: int = 0
```

### API-eb7e9173c81c · StreamNameMapEntry.predicted_source_items

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L922)

```python
predicted_source_items: int = 0
```

### API-78bf8b80dfd1 · StreamNameMapEntry.predicted_repair_items

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L923)

```python
predicted_repair_items: int = 0
```

### API-3fa2161a093a · StreamNameMapEntry.from_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L932)

```python
def from_name(cls, original_name: str) -> "StreamNameMapEntry":
```

### API-ccac7fc4b4ba · StreamNameMapEntry.from_grouped_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L936)

```python
def from_grouped_name(
            cls, original_name: str, group_id: str, sample_class: str,
            group_item_index: int, predicted_source_items: int,
            predicted_repair_items: int) -> "StreamNameMapEntry":
```

### API-7c9798f83714 · StreamNameMapEntry.make_tombstone

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L945)

```python
def make_tombstone(cls) -> "StreamNameMapEntry":
```

### API-5a7d96e479cb · StreamNameMapBlock

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L971)

```python
class StreamNameMapBlock:
```

原始接口说明：

```text
Canonical, fixed-capacity cursor-to-semantic-name Mapping content.

Encoding and hashing always delegate to the C++ implementation.  Python
therefore cannot drift into a second wire codec.
```

### API-ff7548f613d1 · StreamNameMapBlock.stream_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L978)

```python
stream_id: str
```

### API-d39acf0530d0 · StreamNameMapBlock.session_epoch

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L979)

```python
session_epoch: int
```

### API-eb5971e043bb · StreamNameMapBlock.mapping_version

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L980)

```python
mapping_version: int
```

### API-527fcfac422a · StreamNameMapBlock.block_number

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L981)

```python
block_number: int
```

### API-23e55c3832a7 · StreamNameMapBlock.block_capacity

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L982)

```python
block_capacity: int
```

### API-bfa2ea5eb727 · StreamNameMapBlock.first_cursor

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L983)

```python
first_cursor: int
```

### API-f46d973b0b81 · StreamNameMapBlock.entries

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L984)

```python
entries: tuple[StreamNameMapEntry, ...]
```

### API-5c94a447e5c9 · StreamNameMapBlock.previous_content_digest

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L985)

```python
previous_content_digest: bytes | None = None
```

### API-1f87bcc345dd · StreamNameMapBlock.contract_version

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L986)

```python
contract_version: int = STREAM_NAME_MAP_CONTRACT_VERSION
```

### API-fbcbb3bf804f · StreamNameMapBlock.wire_encode

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1023)

```python
def wire_encode(self) -> bytes:
```

### API-e7d942cfd7a5 · StreamNameMapBlock.validate

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1026)

```python
def validate(self) -> str | None:
```

原始接口说明：

```text
Return the native contract error, or ``None`` when canonical.
```

### API-1f95b8a6b602 · StreamNameMapBlock.decode

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1033)

```python
def decode(cls, wire: bytes) -> "StreamNameMapBlock":
```

### API-ccdf3457dd26 · StreamNameMapBlock.canonical_content

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1036)

```python
def canonical_content(self) -> bytes:
```

### API-db88d048cd36 · StreamNameMapBlock.content_digest

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1039)

```python
def content_digest(self) -> bytes:
```

### API-6e30ce9d7302 · StreamNameMapBlock.fits_signed_wire_budget

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1042)

```python
def fits_signed_wire_budget(self, signed_envelope_overhead: int,
                                configured_wire_cap: int) -> bool:
```

### API-dccbc873bb92 · StreamNameMapBlock.last_cursor

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1048)

```python
def last_cursor(self) -> int:
```

### API-56de7ee5afe4 · make_stream_name_map_root

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1052)

```python
def make_stream_name_map_root(provider: str, stream_id: str) -> str:
```

原始接口说明：

```text
Return the Mapping base name before typed Version/Sequence components.
```

### API-3a1434b2c53a · make_stream_name_map_block_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1058)

```python
def make_stream_name_map_block_name(mapping_root: str, mapping_version: int,
                                    block_number: int) -> str:
```

原始接口说明：

```text
Return one exact Mapping Data name with typed Version and SequenceNum.
```

### API-440b67a9f0fe · StreamCursorFrontiers

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1067)

```python
class StreamCursorFrontiers:
```

原始接口说明：

```text
The five ordered cursor frontiers advertised by a Provider.
```

### API-f8b96cf50623 · StreamCursorFrontiers.oldest_retained

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1070)

```python
oldest_retained: int
```

### API-98557d76a068 · StreamCursorFrontiers.latest_join

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1071)

```python
latest_join: int
```

### API-09add5327c0b · StreamCursorFrontiers.latest_produced

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1072)

```python
latest_produced: int
```

### API-9d9e32c40449 · StreamCursorFrontiers.mapping_committed_through

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1073)

```python
mapping_committed_through: int
```

### API-4d1f9ebff52e · StreamCursorFrontiers.next_reserved

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1074)

```python
next_reserved: int
```

### API-0a025311b35b · StreamCursorFrontiers.validate

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1095)

```python
def validate(self, block_capacity: int,
                 checkpoint_block: int) -> str | None:
```

### API-70e0d4524b36 · StreamNameMapCheckpoint

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1103)

```python
class StreamNameMapCheckpoint:
```

### API-990d488441a1 · StreamNameMapCheckpoint.frontiers

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1104)

```python
frontiers: StreamCursorFrontiers
```

### API-fd368f2540b7 · StreamNameMapCheckpoint.block_number

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1105)

```python
block_number: int
```

### API-9129ae54f7a8 · StreamNameMapCheckpoint.content_digest

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1106)

```python
content_digest: bytes
```

### API-914e36b62312 · StreamNameMapResolverConfig

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1128)

```python
class StreamNameMapResolverConfig:
```

### API-8004b1274bd3 · StreamNameMapResolverConfig.stream_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1129)

```python
stream_id: str
```

### API-cf9f92716551 · StreamNameMapResolverConfig.session_epoch

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1130)

```python
session_epoch: int
```

### API-05d4eae9beb5 · StreamNameMapResolverConfig.mapping_version

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1131)

```python
mapping_version: int
```

### API-c4ab647bf7d6 · StreamNameMapResolverConfig.block_capacity

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1132)

```python
block_capacity: int
```

### API-277f992af624 · StreamNameMapResolverConfig.expected_provider

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1133)

```python
expected_provider: str
```

### API-0b059745aeef · StreamNameMapResolverConfig.mapping_root

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1134)

```python
mapping_root: str
```

### API-8a6ba2e5d158 · StreamNameMapResolverConfig.payload_prefix

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1135)

```python
payload_prefix: str
```

### API-81adac56ec38 · StreamNameMapResolverConfig.signed_wire_cap

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1136)

```python
signed_wire_cap: int = 8800
```

### API-4e209cb39d3e · StreamNameMapResolverConfig.max_verified_blocks

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1137)

```python
max_verified_blocks: int = 32
```

### API-cd7daa52c3a8 · StreamNameMapResolverConfig.max_quarantine_blocks

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1138)

```python
max_quarantine_blocks: int = 8
```

### API-d0d41fd3833e · StreamNameMapResolverConfig.max_reverse_entries

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1139)

```python
max_reverse_entries: int = 65536
```

### API-6cd5aeb6ee64 · StreamNameMapResolverConfig.max_original_name_wire_bytes

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1140)

```python
max_original_name_wire_bytes: int = 4096
```

### API-47357bd88ca9 · StreamNameMapResolverConfig.contract_version

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1141)

```python
contract_version: int = STREAM_NAME_MAP_CONTRACT_VERSION
```

### API-a640e574bd12 · StreamNameMapAdmissionResult

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1162)

```python
class StreamNameMapAdmissionResult:
```

### API-3eb5767da98f · StreamNameMapAdmissionResult.disposition

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1163)

```python
disposition: str
```

### API-91154b0f4b15 · StreamNameMapAdmissionResult.timing

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1164)

```python
timing: str
```

### API-79b4ebb57d68 · StreamNameMapAdmissionResult.reason

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1165)

```python
reason: str
```

### API-a58439dfdb6c · StreamNameMapAdmissionResult.state_changed

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1166)

```python
state_changed: bool
```

### API-143a4026a947 · StreamNameMapAdmissionResult.mapping_committed_through

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1167)

```python
mapping_committed_through: int
```

### API-3f8e81466911 · StreamNameMapAdmissionResult.accepted

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1168)

```python
accepted: bool
```

### API-604a14863cac · StreamNameMapAdmissionResult.fatal

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1169)

```python
fatal: bool
```

### API-11931f91dff0 · StreamNameMapResolution

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1185)

```python
class StreamNameMapResolution:
```

### API-d8aa36b56001 · StreamNameMapResolution.cursor

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1186)

```python
cursor: int
```

### API-2828da1a204f · StreamNameMapResolution.original_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1187)

```python
original_name: str
```

### API-7e76a2b1d402 · StreamNameMapResolution.tombstone

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1188)

```python
tombstone: bool
```

### API-b535cc5c9f26 · StreamNameMapResolution.terminal_unproduced

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1189)

```python
terminal_unproduced: bool
```

### API-1137787f48fe · StreamNameMapResolution.timing

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1190)

```python
timing: str
```

### API-d70c6e6695d7 · StreamNameMapResolution.schedulable

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1191)

```python
schedulable: bool
```

### API-3e3f50cf2322 · StreamNameResolver

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1205)

```python
class StreamNameResolver:
```

原始接口说明：

```text
Bounded Python facade over the single native StreamNameMap resolver.

Callers must validate the Mapping Data signature and Provider chain before
calling :meth:`admit_verified_block`.  This object performs deterministic
envelope, continuity, frontier, immutability, and cache-bound checks only.
```

### API-97520e5ee1de · StreamNameResolver.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1213)

```python
def __init__(self, config: StreamNameMapResolverConfig,
                 checkpoint: StreamNameMapCheckpoint) -> None:
```

### API-7329b62da835 · StreamNameResolver.reset

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1218)

```python
def reset(self, config: StreamNameMapResolverConfig,
              checkpoint: StreamNameMapCheckpoint) -> None:
```

### API-e468c111e4f0 · StreamNameResolver.admit_verified_block

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1222)

```python
def admit_verified_block(
        self,
        *,
        data_name: str,
        verified_provider: str,
        content: bytes,
        signed_wire_size: int,
        content_type: int = STREAM_NAME_MAP_CONTENT_TYPE_MANIFEST,
        has_final_block: bool = False,
        received_monotonic_ms: int = 0,
        required_before_monotonic_ms: int = 0,
    ) -> StreamNameMapAdmissionResult:
```

### API-343a0bea23be · StreamNameResolver.refresh_checkpoint

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1245)

```python
def refresh_checkpoint(self, checkpoint: StreamNameMapCheckpoint) -> StreamNameMapAdmissionResult:
```

### API-2d618b061a83 · StreamNameResolver.lookup

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1249)

```python
def lookup(self, cursor: int) -> StreamNameMapResolution | None:
```

### API-8fcbebbb4a90 · StreamNameResolver.resolve

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1253)

```python
def resolve(self, cursor: int) -> str | None:
```

### API-306776d1bdfa · StreamNameResolver.reverse_resolve

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1257)

```python
def reverse_resolve(self, original_name: str) -> int | None:
```

### API-1f24551b4866 · StreamNameResolver.mark_terminal_unproduced

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1261)

```python
def mark_terminal_unproduced(self, cursor: int) -> bool:
```

### API-ba78db2d2110 · StreamNameResolver.evict_local_block

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1264)

```python
def evict_local_block(self, block_number: int) -> bool:
```

### API-6387cd644da2 · StreamNameResolver.frontiers

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1268)

```python
def frontiers(self) -> StreamCursorFrontiers:
```

### API-7d697d4bf4c0 · StreamNameResolver.checkpoint

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1272)

```python
def checkpoint(self) -> StreamNameMapCheckpoint:
```

### API-f2cb2b5fda0e · StreamNameResolver.faulted

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1276)

```python
def faulted(self) -> bool:
```

### API-63722f72494c · StreamNameResolver.verified_block_count

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1280)

```python
def verified_block_count(self) -> int:
```

### API-0357a1dd7abb · StreamNameResolver.quarantined_block_count

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1284)

```python
def quarantined_block_count(self) -> int:
```

### API-f32da685f417 · StreamNameResolver.binding_count

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1288)

```python
def binding_count(self) -> int:
```

### API-b388821acfb2 · StreamNameResolver.diagnostics

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1292)

```python
def diagnostics(self) -> dict[str, int]:
```

### API-bd4eac0d0048 · SampleClassProfile

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1298)

```python
class SampleClassProfile:
```

原始接口说明：

```text
Opaque bounded sample class; Core does not interpret ``class_id``.
```

### API-d4729e4a778d · SampleClassProfile.class_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1301)

```python
class_id: str
```

### API-a0ff2bcd489f · SampleClassProfile.seed_source_items

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1302)

```python
seed_source_items: int
```

### API-267013f4a72f · SampleClassProfile.hard_max_source_items

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1303)

```python
hard_max_source_items: int
```

### API-f5eefcfcfb04 · SampleClassProfile.history_capacity

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1304)

```python
history_capacity: int = 32
```

### API-e9dfcd7d554d · SampleClassProfile.safety_margin_items

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1305)

```python
safety_margin_items: int = 1
```

### API-1ed9527792fa · SampleClassPredictionStatus

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1325)

```python
class SampleClassPredictionStatus:
```

### API-0a3b5b3f62b7 · SampleClassPredictionStatus.class_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1326)

```python
class_id: str
```

### API-fd8ecc78f2a5 · SampleClassPredictionStatus.prediction

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1327)

```python
prediction: int
```

### API-58463a818a1d · SampleClassPredictionStatus.observations

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1328)

```python
observations: int
```

### API-c96c63d6568a · SampleClassPredictionStatus.underpredictions

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1329)

```python
underpredictions: int
```

### API-42e06e4d1b1f · SampleClassPredictionStatus.underpredicted_items

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1330)

```python
underpredicted_items: int
```

### API-ed97e980b7e9 · SampleClassPredictionStatus.overpredictions

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1331)

```python
overpredictions: int
```

### API-ae6c8b513074 · SampleClassPredictionStatus.overpredicted_items

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1332)

```python
overpredicted_items: int
```

### API-9a27d3e21b29 · LiveStreamSamplePredictor

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1345)

```python
class LiveStreamSamplePredictor:
```

原始接口说明：

```text
Bounded per-class predictor shared by C++ and Python applications.
```

### API-1fe18b7df614 · LiveStreamSamplePredictor.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1348)

```python
def __init__(self, profiles: tuple[SampleClassProfile, ...] | list[SampleClassProfile]):
```

### API-ac5eea2b30bf · LiveStreamSamplePredictor.predict

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1352)

```python
def predict(self, class_id: str) -> int:
```

### API-67567d46186e · LiveStreamSamplePredictor.observe

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1355)

```python
def observe(self, class_id: str, actual_source_items: int) -> bool:
```

### API-14956e458e76 · LiveStreamSamplePredictor.status

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1358)

```python
def status(self, class_id: str) -> SampleClassPredictionStatus | None:
```

### API-c8be58f97603 · LiveStreamSamplePredictor.statuses

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1363)

```python
def statuses(self) -> dict[str, SampleClassPredictionStatus]:
```

### API-d06c7b191f06 · LiveStreamFecOptions

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1369)

```python
class LiveStreamFecOptions:
```

原始接口说明：

```text
Optional Core recovery over opaque application bytes; disabled by default.
```

### API-ca40700f9bf6 · LiveStreamFecOptions.scheme

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1372)

```python
scheme: str = "none"
```

### API-c9e866e61627 · LiveStreamFecOptions.max_source_items

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1373)

```python
max_source_items: int = 0
```

### API-e8cdb6dbbffb · LiveStreamFecOptions.max_source_bytes

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1374)

```python
max_source_bytes: int = 0
```

### API-2a5d369e3fb9 · LiveStreamFecOptions.recovery_budget_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1375)

```python
recovery_budget_ms: int = 0
```

### API-dba3c0951766 · LiveStreamFecOptions.repair_symbols

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1376)

```python
repair_symbols: int = 0
```

### API-a5ea5cb1e431 · LiveStreamFecOptions.none

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1379)

```python
def none(cls) -> "LiveStreamFecOptions":
```

### API-539b322b8b83 · LiveStreamFecOptions.xor_one_repair

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1383)

```python
def xor_one_repair(
        cls, source_items: int, max_source_bytes: int,
        recovery_budget_ms: int = 500,
    ) -> "LiveStreamFecOptions":
```

### API-db848c07aa84 · LiveStreamFecOptions.gf256_two_repair

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1394)

```python
def gf256_two_repair(
        cls, source_items: int, max_source_bytes: int,
        recovery_budget_ms: int = 500,
    ) -> "LiveStreamFecOptions":
```

### API-5cee5ba6b25f · LiveStreamFecOptions.source_items

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1436)

```python
def source_items(self) -> int:
```

原始接口说明：

```text
Compatibility spelling; new code should use ``max_source_items``.
```

### API-ed5c04f003cf · StreamAdvancedOptions

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1442)

```python
class StreamAdvancedOptions:
```

### API-a17af18f53b2 · StreamAdvancedOptions.mapping_block_capacity

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1443)

```python
mapping_block_capacity: int = 16
```

### API-f0bf0668b78c · StreamAdvancedOptions.mapping_ahead_blocks

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1444)

```python
mapping_ahead_blocks: int = 4
```

### API-a02dd008ce53 · StreamAdvancedOptions.retained_items

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1445)

```python
retained_items: int = 600
```

### API-9211f6b1e17c · StreamAdvancedOptions.max_name_reservations

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1446)

```python
max_name_reservations: int = 65536
```

### API-0f7c958d725d · StreamAdvancedOptions.max_pending_interests

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1447)

```python
max_pending_interests: int = 256
```

### API-f75e4d318a7a · StreamAdvancedOptions.signed_wire_cap

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1448)

```python
signed_wire_cap: int = 8800
```

### API-67125b3d55bd · StreamAdvancedOptions.startup_timeout_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1449)

```python
startup_timeout_ms: int = 1000
```

### API-a10bc14aba58 · StreamConfig

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1464)

```python
class StreamConfig:
```

### API-1ecf376a363f · StreamConfig.stream_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1465)

```python
stream_id: str
```

### API-e96bd828be43 · StreamConfig.data_prefix

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1466)

```python
data_prefix: str
```

### API-350fd5f2cdcc · StreamConfig.sample_period_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1467)

```python
sample_period_ms: float
```

### API-fb3c459fc909 · StreamConfig.sample_classes

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1468)

```python
sample_classes: tuple[SampleClassProfile, ...]
```

### API-5092346d97b1 · StreamConfig.fec

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1469)

```python
fec: LiveStreamFecOptions = field(default_factory=LiveStreamFecOptions.none)
```

### API-e09a5ac10d4f · StreamConfig.session_epoch

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1470)

```python
session_epoch: int | None = None
```

### API-d7915fd2cd8e · StreamConfig.advanced

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1471)

```python
advanced: StreamAdvancedOptions = field(default_factory=StreamAdvancedOptions)
```

### API-f3fdf669e771 · LiveStreamDefinition

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1490)

```python
class LiveStreamDefinition:
```

### API-68c7d1ea2ede · LiveStreamDefinition.stream_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1491)

```python
stream_id: str
```

### API-5237194274a7 · LiveStreamDefinition.provider

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1492)

```python
provider: str
```

### API-bf5358c9a194 · LiveStreamDefinition.semantic_data_prefix

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1493)

```python
semantic_data_prefix: str
```

### API-94321d0b1fd6 · LiveStreamDefinition.session_epoch

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1494)

```python
session_epoch: int
```

### API-11a834cb8275 · LiveStreamDefinition.mapping_version

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1495)

```python
mapping_version: int
```

### API-80bfe923d0a5 · LiveStreamDefinition.contract_version

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1496)

```python
contract_version: int = STREAM_NAME_MAP_CONTRACT_VERSION
```

### API-05c6818942f7 · LiveStreamDefinition.mapping_block_capacity

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1497)

```python
mapping_block_capacity: int = 16
```

### API-0b7e96c92898 · LiveStreamDefinition.mapping_ahead_blocks

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1498)

```python
mapping_ahead_blocks: int = 4
```

### API-08ac94a88cd4 · LiveStreamDefinition.retained_items

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1499)

```python
retained_items: int = 600
```

### API-3ac9d9da5a15 · LiveStreamDefinition.max_name_reservations

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1500)

```python
max_name_reservations: int = 65536
```

### API-c4c8ff292dd6 · LiveStreamDefinition.max_pending_interests

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1501)

```python
max_pending_interests: int = 256
```

### API-8caba8415f4e · LiveStreamDefinition.signed_wire_cap

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1502)

```python
signed_wire_cap: int = 8800
```

### API-6a7e186d59b6 · LiveStreamDefinition.sample_period_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1503)

```python
sample_period_ms: float = 0.0
```

### API-0d7cbfe7bcb5 · LiveStreamDefinition.sample_classes

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1504)

```python
sample_classes: tuple[SampleClassProfile, ...] = ()
```

### API-083afba8ae93 · LiveStreamDefinition.fec

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1505)

```python
fec: LiveStreamFecOptions = field(default_factory=LiveStreamFecOptions.none)
```

### API-590791477a5f · LiveStreamDefinition.mapping_root

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1533)

```python
def mapping_root(self) -> str:
```

### API-a95fba80d4f3 · LiveStreamItemReservation

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1561)

```python
class LiveStreamItemReservation:
```

### API-b2b3e9bc1528 · LiveStreamItemReservation.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1562)

```python
def __init__(self, native) -> None:
```

### API-a50383ca27ce · LiveStreamItemReservation.cursor

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1566)

```python
def cursor(self) -> int:
```

### API-799053e623ea · LiveStreamItemReservation.original_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1570)

```python
def original_name(self) -> str:
```

### API-83a25126cdc5 · LiveStreamGroupReservation

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1574)

```python
class LiveStreamGroupReservation:
```

### API-96b46ff74ea9 · LiveStreamGroupReservation.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1575)

```python
def __init__(self, native) -> None:
```

### API-668144d424ad · LiveStreamGroupReservation.group_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1579)

```python
def group_id(self) -> str:
```

### API-01f766062f36 · LiveStreamGroupReservation.sources

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1583)

```python
def sources(self) -> tuple[LiveStreamItemReservation, ...]:
```

### API-504dae8866dc · LiveStreamGroupReservation.repairs

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1587)

```python
def repairs(self) -> tuple[LiveStreamItemReservation, ...]:
```

### API-49c859e968cd · LiveStreamSampleReservation

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1591)

```python
class LiveStreamSampleReservation:
```

### API-b0b5f0d916e2 · LiveStreamSampleReservation.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1592)

```python
def __init__(self, native) -> None:
```

### API-faf290f9a02c · LiveStreamSampleReservation.sample_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1596)

```python
def sample_id(self) -> int:
```

### API-81e857c6eff8 · LiveStreamSampleReservation.sample_class

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1600)

```python
def sample_class(self) -> str:
```

### API-4212d61a3a9d · LiveStreamSampleReservation.predicted_source_items

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1604)

```python
def predicted_source_items(self) -> int:
```

### API-3259cc59f2a3 · LiveStreamSampleReservation.group

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1608)

```python
def group(self) -> LiveStreamGroupReservation:
```

### API-e720a18a98c6 · LiveStreamDescriptor

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1612)

```python
class LiveStreamDescriptor:
```

### API-986512cb932a · LiveStreamDescriptor.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1613)

```python
def __init__(self, native: NativeLiveStreamDescriptor) -> None:
```

### API-9fd5b51f9319 · LiveStreamDescriptor.stream_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1617)

```python
def stream_id(self) -> str:
```

### API-8c88cf49d7dc · LiveStreamDescriptor.provider

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1621)

```python
def provider(self) -> str:
```

### API-bbe84d71b021 · LiveStreamDescriptor.contract_version

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1625)

```python
def contract_version(self) -> int:
```

### API-c24693619c11 · LiveStreamDescriptor.semantic_data_prefix

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1629)

```python
def semantic_data_prefix(self) -> str:
```

### API-16b85651a1e2 · LiveStreamDescriptor.safe_join_cursor

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1633)

```python
def safe_join_cursor(self) -> int:
```

### API-5d9ed0c1f12d · LiveStreamDescriptor.measured_sample_period_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1637)

```python
def measured_sample_period_ms(self) -> float:
```

### API-2adf7663aa87 · LiveStreamDescriptor.to_dict

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1640)

```python
def to_dict(self) -> dict[str, Any]:
```

### API-cab8888fc9ca · LiveStreamDescriptor.from_dict

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1695)

```python
def from_dict(cls, payload: dict[str, Any]) -> "LiveStreamDescriptor":
```

### API-c2a3c8ab43da · LiveStreamItemAdmission

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1765)

```python
class LiveStreamItemAdmission:
```

### API-e1a5594a82ce · LiveStreamItemAdmission.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1766)

```python
def __init__(self, native) -> None:
```

### API-63d34e742d1b · LiveStreamItemAdmission.accept_item

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1770)

```python
def accept_item(cls) -> "LiveStreamItemAdmission":
```

### API-46cfe52fcabc · LiveStreamItemAdmission.reject_item

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1774)

```python
def reject_item(cls, reason: str) -> "LiveStreamItemAdmission":
```

### API-c9393f6a95b6 · VerifiedLiveStreamItem

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1779)

```python
class VerifiedLiveStreamItem:
```

### API-3dc64f0fde57 · VerifiedLiveStreamItem.cursor

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1780)

```python
cursor: int
```

### API-84e80e984bf2 · VerifiedLiveStreamItem.original_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1781)

```python
original_name: str
```

### API-a2ea8dc38103 · VerifiedLiveStreamItem.verified_provider

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1782)

```python
verified_provider: str
```

### API-c9c1dbff81c8 · VerifiedLiveStreamItem.content

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1783)

```python
content: bytes
```

### API-5727b8bf94b0 · VerifiedLiveStreamItem.provenance

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1784)

```python
provenance: str
```

### API-fc3373f3e3c2 · VerifiedLiveStreamItem.received_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1785)

```python
received_ms: int
```

### API-acf202c66cbd · StreamSubscriptionOptions

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1796)

```python
class StreamSubscriptionOptions:
```

### API-50923567a529 · StreamSubscriptionOptions.on_item

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1797)

```python
on_item: Callable[
        ["VerifiedLiveStreamItem"], bool | "LiveStreamItemAdmission"
    ]
```

### API-4e6c6bb5dda4 · StreamSubscriptionOptions.start

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1800)

```python
start: str = "latest"
```

### API-7a3858c0864f · StreamSubscriptionOptions.prefetch_policy

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1801)

```python
prefetch_policy: str | None = None
```

### API-d42f44cc8ec1 · StreamSubscriptionOptions.aggregate_interest_limit

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1802)

```python
aggregate_interest_limit: int = 64
```

### API-203c11d9e06a · StreamSubscriptionOptions.enable_fec_recovery

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1803)

```python
enable_fec_recovery: bool = True
```

### API-21ece0606dfb · StreamSubscriptionOptions.require_full_delivery

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1804)

```python
require_full_delivery: bool = False
```

### API-1ced68f24962 · StreamSubscriptionOptions.interest_lifetime_ms

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1805)

```python
interest_lifetime_ms: int = 500
```

### API-988c26ee9d27 · StreamSubscriptionOptions.on_status

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1806)

```python
on_status: Callable[[Any], None] | None = None
```

### API-915eaae737ca · StreamPublisher

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1809)

```python
class StreamPublisher:
```

原始接口说明：

```text
Predictive high-level facade over the Core-owned stream publisher.
```

### API-3c386deb11bb · StreamPublisher.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1812)

```python
def __init__(self, native) -> None:
```

### API-d9b9b17185e7 · StreamPublisher.start

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1815)

```python
def start(self) -> "PredictiveStreamDescriptor":
```

原始接口说明：

```text
Start the predictive stream. No bootstrap data is required.
```

### API-ffb9bc17a489 · StreamPublisher.push

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1819)

```python
def push(self, signed_data: bytes) -> None:
```

原始接口说明：

```text
Push one exact-name, application-signed NDN Data packet.

The application owns the keychain and signature. ``ndnsf`` validates
and retains the supplied wire; it does not sign or wrap it again.
```

### API-bf0080eb8af9 · StreamPublisher.flush

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1827)

```python
def flush(self) -> None:
```

原始接口说明：

```text
Generate and publish FEC parity for pending source segments.
```

### API-5000fe57c70e · StreamPublisher.status

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1831)

```python
def status(self):
```

### API-fc0f62cf2589 · StreamPublisher.stop

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1834)

```python
def stop(self) -> None:
```

### API-d1b325cdc656 · PredictiveStreamCheckpoint

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1839)

```python
class PredictiveStreamCheckpoint:
```

### API-d927442964c5 · PredictiveStreamCheckpoint.initial_sample_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1840)

```python
initial_sample_id: int = 0
```

### API-e975b22ba30d · PredictiveStreamCheckpoint.oldest_retained_sample_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1841)

```python
oldest_retained_sample_id: int = 0
```

### API-0f28b850d2ce · PredictiveStreamCheckpoint.latest_produced_sample_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1842)

```python
latest_produced_sample_id: int = 0
```

### API-d3711fb83456 · PredictiveStreamCheckpoint.next_expected_sample_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1843)

```python
next_expected_sample_id: int = 0
```

### API-e8b76ece95bf · PredictiveStreamCheckpoint.to_dict

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1845)

```python
def to_dict(self) -> dict[str, int]:
```

### API-05bfb03a7e33 · PredictiveStreamDescriptor

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1854)

```python
class PredictiveStreamDescriptor:
```

### API-bc5b02016b8e · PredictiveStreamDescriptor.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1855)

```python
def __init__(self, native) -> None:
```

### API-69544988c6de · PredictiveStreamDescriptor.definition

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1859)

```python
def definition(self) -> "LiveStreamDefinition":
```

### API-7c30dad64f22 · PredictiveStreamDescriptor.checkpoint

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1863)

```python
def checkpoint(self) -> PredictiveStreamCheckpoint:
```

### API-8a874df53e7c · PredictiveStreamDescriptor.frontier_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1873)

```python
def frontier_name(self) -> str:
```

### API-e1c892d27b82 · PredictiveStreamDescriptor.to_dict

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1876)

```python
def to_dict(self) -> dict[str, Any]:
```

### API-1d94c4b9abd5 · PredictiveStreamDescriptor.from_dict

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1920)

```python
def from_dict(cls, payload: dict[str, Any]) -> "PredictiveStreamDescriptor":
```

### API-158c96c2f100 · LiveStreamPublisher

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L1999)

```python
class LiveStreamPublisher:
```

### API-c4bfea08b00e · LiveStreamPublisher.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2000)

```python
def __init__(self, native) -> None:
```

### API-689363f87ca9 · LiveStreamPublisher.reserve_ahead

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2003)

```python
def reserve_ahead(self, original_name: str) -> LiveStreamItemReservation:
```

### API-ba1f5aef34cf · LiveStreamPublisher.reserve_many_ahead

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2006)

```python
def reserve_many_ahead(self, original_names: Iterable[str]) -> tuple[LiveStreamItemReservation, ...]:
```

### API-60191d96be60 · LiveStreamPublisher.reserve_group

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2010)

```python
def reserve_group(self, group_id: str, source_names: Iterable[str],
                      repair_names: Iterable[str]) -> LiveStreamGroupReservation:
```

### API-1f5f60a643cd · LiveStreamPublisher.announce_sample

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2016)

```python
def announce_sample(self, sample_id: int, sample_class: str,
                        name_factory) -> LiveStreamSampleReservation:
```

### API-09b52c87d392 · LiveStreamPublisher.prepare_sample_extent

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2021)

```python
def prepare_sample_extent(
            self, reservation: LiveStreamSampleReservation,
            actual_source_items: int) -> tuple[LiveStreamItemReservation, ...]:
```

### API-f992b261e8d9 · LiveStreamPublisher.publish

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2028)

```python
def publish(self, reservation: LiveStreamItemReservation, opaque_content: bytes) -> None:
```

### API-b82a189217e2 · LiveStreamPublisher.publish_group

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2031)

```python
def publish_group(self, reservation: LiveStreamGroupReservation,
                      opaque_sources: Iterable[bytes]) -> None:
```

### API-3c07a19eddee · LiveStreamPublisher.publish_sample

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2036)

```python
def publish_sample(self, reservation: LiveStreamSampleReservation,
                       opaque_sources: Iterable[bytes]) -> None:
```

### API-f94b1229c664 · LiveStreamPublisher.activate

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2041)

```python
def activate(self, *, measured_sample_period_ms: float,
                 safe_join_cursor: int) -> LiveStreamDescriptor:
```

### API-ed8fe5a3d010 · LiveStreamPublisher.open_published_packet_feed

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2048)

```python
def open_published_packet_feed(
            self, *, from_cursor: int = 0, max_queued_packets: int = 1024,
            max_queued_bytes: int = 8 * 1024 * 1024) -> "PublishedPacketFeed":
```

### API-443cf55b1c2a · LiveStreamPublisher.status

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2057)

```python
def status(self):
```

### API-31af10c86e2d · LiveStreamPublisher.stop

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2060)

```python
def stop(self) -> None:
```

### API-1497c1ca47ba · PublishedLiveStreamPacket

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2065)

```python
class PublishedLiveStreamPacket:
```

### API-a056b598a638 · PublishedLiveStreamPacket.kind

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2066)

```python
kind: str
```

### API-0c176ca03ae7 · PublishedLiveStreamPacket.stream_id

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2067)

```python
stream_id: str
```

### API-88586afaf638 · PublishedLiveStreamPacket.session_epoch

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2068)

```python
session_epoch: int
```

### API-e77933d70a71 · PublishedLiveStreamPacket.mapping_version

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2069)

```python
mapping_version: int
```

### API-c9acf37d36c5 · PublishedLiveStreamPacket.cursor

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2070)

```python
cursor: Optional[int]
```

### API-03845d285c75 · PublishedLiveStreamPacket.data_name

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2071)

```python
data_name: str
```

### API-dad353e9e23c · PublishedLiveStreamPacket.provider

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2072)

```python
provider: str
```

### API-451b9c8e5906 · PublishedLiveStreamPacket.signed_data_wire

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2073)

```python
signed_data_wire: bytes
```

### API-0c623cfcb716 · PublishedLiveStreamPacket.wire_digest

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2074)

```python
wire_digest: bytes
```

### API-a9f746df04f0 · PublishedLiveStreamPacket.materialized_monotonic_us

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2075)

```python
materialized_monotonic_us: int
```

### API-aab916683701 · PublishedPacketFeed

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2088)

```python
class PublishedPacketFeed:
```

### API-03ff92c90c5a · PublishedPacketFeed.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2089)

```python
def __init__(self, native) -> None:
```

### API-e6a6d7af0299 · PublishedPacketFeed.take_available

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2092)

```python
def take_available(self, max_items: int = 128) -> tuple[PublishedLiveStreamPacket, ...]:
```

### API-9746db183de7 · PublishedPacketFeed.status

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2096)

```python
def status(self):
```

### API-8f858f6f33b7 · PublishedPacketFeed.close

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2099)

```python
def close(self) -> None:
```

### API-9c42ab5e7751 · LiveStreamConsumerHandle

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2103)

```python
class LiveStreamConsumerHandle:
```

### API-b60a0d1ca494 · LiveStreamConsumerHandle.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2104)

```python
def __init__(self, native) -> None:
```

### API-ff810478de83 · LiveStreamConsumerHandle.start

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2107)

```python
def start(self) -> None:
```

### API-6360f405bab4 · LiveStreamConsumerHandle.observe_accepted_sample

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2110)

```python
def observe_accepted_sample(self, sample_id: int, arrival_ms: int,
                                retrieval_delay_ms: float,
                                item_count: int = 1) -> bool:
```

### API-8929dd283f6f · LiveStreamConsumerHandle.status

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2120)

```python
def status(self):
```

### API-317100dace41 · LiveStreamConsumerHandle.stop

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2123)

```python
def stop(self) -> None:
```

### API-471147d4d904 · PredictiveStreamSubscriber

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2127)

```python
class PredictiveStreamSubscriber:
```

### API-09190ea4f10e · PredictiveStreamSubscriber.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2128)

```python
def __init__(self, native) -> None:
```

### API-d263942a7049 · PredictiveStreamSubscriber.start

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2131)

```python
def start(self) -> None:
```

### API-4df59390a0c6 · PredictiveStreamSubscriber.status

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2134)

```python
def status(self):
```

### API-2685bbfcd0b0 · PredictiveStreamSubscriber.stop

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/streaming.py#L2137)

```python
def stop(self) -> None:
```

## pythonWrapper/ndnsf/tracing.py

源码 SHA-256：`b6deb4c9a31870ceb276641bf6db93174d631c4b931016b1bedb295f6974343b`。

### API-561375075f60 · Span

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/tracing.py#L21)

```python
class Span:
```

### API-2e42509a0fc2 · Span.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/tracing.py#L22)

```python
def __init__(self, operation: str, trace_id: str, span_id: str,
                 parent_span_id: str = "", start_us: int = 0):
```

### API-508cf53cda0f · Span.set_tag

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/tracing.py#L33)

```python
def set_tag(self, key: str, value: str) -> None:
```

### API-ecf974aa4256 · Span.log

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/tracing.py#L36)

```python
def log(self, message: str) -> None:
```

### API-ac42064babcf · Span.finish

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/tracing.py#L39)

```python
def finish(self, finish_us: int = 0) -> None:
```

### API-225bcaa0597b · Span.to_jaeger

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/tracing.py#L42)

```python
def to_jaeger(self, service_name: str) -> dict[str, Any]:
```

### API-78adf4922dcd · TraceCollector

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/tracing.py#L59)

```python
class TraceCollector:
```

原始接口说明：

```text
Collect spans across providers and export as Jaeger JSON.
```

### API-ed973fe4948e · TraceCollector.__init__

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/tracing.py#L62)

```python
def __init__(self, service_name: str = "ndnsf"):
```

### API-15fd346492ed · TraceCollector.start_span

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/tracing.py#L66)

```python
def start_span(self, operation: str, trace_id: str, *,
                   span_id: str = "", parent_span_id: str = "",
                   provider: str = "", role: str = "") -> Span:
```

### API-955b90cb89de · TraceCollector.finish_span

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/tracing.py#L78)

```python
def finish_span(self, span_id: str, *, finish_us: int = 0) -> None:
```

### API-6f4e620c1f49 · TraceCollector.from_provider_lifecycle

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/tracing.py#L83)

```python
def from_provider_lifecycle(self,
                                 lifecycle: dict[str, Any],
                                 trace_id: str,
                                 provider: str,
                                 service: str) -> None:
```

原始接口说明：

```text
Create spans from a ProviderRequestLifecycleStatus dict.
```

### API-6be7eb97337b · TraceCollector.to_jaeger_traces

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/tracing.py#L104)

```python
def to_jaeger_traces(self) -> list[dict[str, Any]]:
```

### API-eba2c1245628 · TraceCollector.write_jaeger

public-by-name / declared-interface；[源码](../../pythonWrapper/ndnsf/tracing.py#L107)

```python
def write_jaeger(self, path: str | Path) -> None:
```
