TARGETS = consumer producer
CFLAGS = `pkg-config --cflags --libs libndn-cxx`

all: $(TARGETS)

consumer: consumer.cpp
	g++ -std=c++17 $< -o $@ -Wall -O2 $(CFLAGS)

producer: producer.cpp
	g++ -std=c++17 $< -o $@ -Wall -O2 $(CFLAGS)

clean:
	rm -rf *.dSYM
	rm -rf $(TARGETS)
