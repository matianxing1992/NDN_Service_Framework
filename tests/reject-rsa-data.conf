rule
{
  id "Reject RSA-signed test Data"
  for data
  filter
  {
    type name
    regex ^<>*$
  }
  checker
  {
    type customized
    sig-type ecdsa-sha256
    key-locator
    {
      type name
      regex ^<trusted><KEY><><><>$
    }
  }
}

trust-anchor
{
  type any
}
