# B189 Native Build Evidence — 2026-09-18

## Attempt b189-3-host-prefix

The first incremental build of the B189-3 C++ target set used the host
`/usr/local` NAC-ABE prefix. Configuration completed, but compilation stopped
in `ndn-service-framework/ServiceController.cpp` because that installed
`libnac-abe.so` and headers expose an older `KpAttributeAuthority` ABI. The
missing symbols were `getPublicParametersVersion`,
`setPublicParametersVersion`, `getPublicParametersWire`, `removePolicy`,
`replacePolicy`, and `rotateKeyGeneration`. The first boundary is therefore
the dependency header/library pair, before any Spec189 source was compiled;
this is not a product or runtime result.

The failed output directory is `build-spec189-b189-3/`. The selected target
set was `DI_NativeArtifactAuthority`, `DI_NativeRequester`,
`di-native-provider`, `di-native-assembly-worker`, and
`spec189-two-provider-oracle`, with `-j4`. The failed prefix was
`/usr/local` (`libnac-abe.so` SHA-256
`1ca3f33ce934e84df5cf6ce2497c42a68999fba39a22f5f6383abb04f482cd05`).

The matching local candidate is
`/home/tianxing/NDN/nac-abe-integration-182/install-spec187-nac-r1`; its
headers declare the required methods and its library SHA-256 is
`4c1fb004c6e516c8191c9e093cb86667cfd328ccc7585b905da00402ea7f24d1`.
That candidate was used only to repair the host installation. Its NAC-ABE
library is now installed at `/usr/local/lib/libnac-abe.so`; the validated ONNX
full-protobuf archives and tokenizer bridge are installed at
`/usr/local/lib/libonnx.a`, `/usr/local/lib/libonnx_proto.a` and
`/usr/local/lib/libndnsf_tokenizer_bridge.a`, with headers under
`/usr/local/include`. The local-prefix retry was deliberately interrupted
after 38/207 tasks when the global-dependency policy was adopted, so it is not
a build result. The next build must configure without checkout or `.codex-tmp`
library prefixes. No Spec189 task is marked complete by either attempt.

## Attempt b189-3-global-r1

The first global-only configuration succeeded, but compilation stopped at
`ndn-service-framework/ServiceProvider.cpp`: the installed
`/usr/local/include/ndn-svs` header lacked
`SVSPubSub::subscribeToProducerWithCatchUp`. The build was stopped while other
parallel jobs were still draining (`-j4`); no selector or oracle was linked.
The matching NDN-SVS library from the current source/build pair is now
installed globally as `/usr/local/lib/libndn-svs.so.0.1.0` (SHA-256
`0aae91aaf648d5f8fba26f7075b3ad69196c128bf5c8022ecc0951e668486dc5`) with the
matching header and generated `config.hpp`. This is a second dependency ABI
boundary, not a Spec189 product result.

That retry subsequently reached the new Spec189 marker code and stopped at
`ProtectedRuntime.cpp` because `logRuntimeEvidence` was used without its owning
`RuntimeTiming.hpp` declaration being included. This is a compile miss in the
marker change, not a runtime result. The repair adds that include and must pass
the read-only re-review before the next build.

## Attempt b189-3-global-r3 — compile/link and selector checks

After the include repair passed read-only re-review (`STATIC_PASS`), a fresh
global-only configuration and build completed successfully:

| Item | Result |
| --- | --- |
| Waf output | `build-spec189-b189-3-global-r3` |
| Configure inputs | system compiler/binutils, Boost `/usr/include` + `/usr/lib/x86_64-linux-gnu`, installed `/usr/local` closure, no temporary dependency environment variables |
| Targets | `DI_NativeArtifactAuthority`, `DI_NativeRequester`, `di-native-provider`, `di-native-assembly-worker`, `spec189-two-provider-oracle` |
| Build | 207/207, `-j2`, exit 0, 23m18.411s |
| Oracle | `examples/spec189-two-provider-oracle`, SHA-256 `163e6ef0e5e33daba3e8e381f41b76f35da417f9793bec0b76459c754abed417` |
| Provider | `examples/di-native-provider`, SHA-256 `f50d03765556fe0c37106b7f6a0b65bdcec06a48e518a4cb3e470b9f33461c2e` |
| Requester | `examples/DI_NativeRequester`, SHA-256 `edd26277c0a94a9346ef35b865b898da0dcbf3e35282a9e3df3da56eab910fd1` |
| Assembly worker | `DI_NativeOnnxAssemblyWorker`, SHA-256 `900d8d1533e66864c76b79210f0f9f1b57b95441efa105ef0e304c7a4bbca61e` |
| DI library | `libndnsf-distributed-inference.so`, SHA-256 `90d6d57f3e2e25a2be062b36fab096264c11b9dd07c58db37f5e3ec634930d91` |

`ldd` for the requester, provider and DI library resolved NDN-CXX, NDN-SVS,
NAC-ABE, OpenABE/Relic and NDNSD to `/usr/local`; Boost and protobuf resolved
to the system pair; ONNX Runtime resolved to `/opt/onnxruntime`. No `not found`
entry or `.local-boost171` path was present. Waf cache records
`INCLUDES_NDN_SVS=/usr/local/include`, `LIBPATH_NDN_SVS=/usr/local/lib`,
`INCLUDES_ONNX=/usr/local/include`, `LIBPATH_ONNX=/usr/local/lib` and the
tokenizer archive at `/usr/local/lib/libndnsf_tokenizer_bridge.a`.

The C++ oracle selector was then run against an isolated synthetic positive
fixture and a plan-digest mismatch negative fixture. Positive returned exit 0
and `SPEC189_CPP_ORACLE_PASS`; negative returned exit 1 with
`boundary=EXECUTION_ENTERED reason=identity-mismatch`. These are selector
checks only: they prove identity/order enforcement and compile/link closure,
not a real provider execution or MiniNDN qualification.

## Global dependency policy review

The read-only review agent re-reviewed the global dependency changes after the
initial failure. It returned `STATIC_PASS` for the repaired scope:

* `install_ndnsf_stack.sh` no longer rewrites `OPENABE_PREFIX` to
  `$DEPS_DIR/local`; OpenABE is installed under `/usr/local`, followed by
  `ldconfig`, and the NAC-ABE CMake include/library/RPATH inputs use that same
  global prefix.
* `docs/native-dependency-closure.md` now states that
  `--enable-local-dependency-prefix` is rejected and that historical build
  prefixes are not current acceptance inputs.
* The Waf global-root rejection, installation order, and documentation are
  consistent. This was a read-only static result; no build or runtime claim
  is derived from the review itself.

Shell syntax, Python syntax, Spec Kit synchronization (`11/11 PASS`) and
`git diff --check` passed after the repair. The existing `global-r3` build and
C++ oracle selector evidence above remain the compile/link and selector
evidence; no MiniNDN qualification status changed.

## Host global dependency closure repair and configure gate — 2026-09-18

The installed `/usr/local/lib/pkgconfig/libnac-abe.pc` was found to contain the retired
`nac-abe-integration-182/install-spec187-nac-r1` prefix. This was a real global-metadata
boundary, not a reason to add another build prefix. The matching NAC-ABE source/build pair was
reconfigured and installed with `CMAKE_INSTALL_PREFIX=/usr/local` under a sanitized environment.
The resulting checks were:

| Check | Result |
| --- | --- |
| `pkg-config --variable=prefix libnac-abe` | `/usr/local` |
| `pkg-config --variable=includedir libnac-abe` | `/usr/local/include` |
| `pkg-config --variable=libdir libnac-abe` | `/usr/local/lib` |
| NAC-ABE rebuild/install | completed with CMake `-j2` |
| OpenABE required exports | `OpenABEalloc`, `OpenABEfree`, `openSslInitialize` present |
| NAC-ABE `ldd` | `/usr/local` and system `/lib` only; no `not found`, checkout or temporary path |

The same audit found and repaired a malformed `/usr/local/lib/pkgconfig/ndnsd.pc`
whose `Cflags` pointed to the nonexistent `/usr/local/includeabc`. The installed
record now uses `-I/usr/local/include`; a fresh global configure completed with no
`includeabc`, checkout, temporary or retired-prefix entries in its Waf cache.

The new Waf gate was then exercised with fresh configuration trees. A host configuration carrying
`NDNSF_RUNTIME_RPATH='$ORIGIN/../lib:/opt/ndn-base/lib'` failed with the explicit
`--allow-container-runtime-rpath` diagnostic. A normal host configuration completed successfully
with system Boost 1.71, `/usr/local` direct libraries and versioned ONNX Runtime
`/opt/onnxruntime-1.26.0`; its cache contains no `.local-boost171`, `.codex-tmp`, `/tmp` or old
NAC-ABE checkout path. An explicit container-RPATH configuration with the allow flag and
`/opt/ndn-base` also completed; this is a configuration gate check, not a container runtime test.

After reconfiguring the intended `build-spec189-b189-3-global-r3` tree, the affected native
targets `di-native-provider` and `spec189-two-provider-oracle` rebuilt successfully in 1m14.674s
with `-j2`. Provider SHA-256 is `80949be08f18cd4379e8403d64094de9e3bbc87df6a07274a4d372fc1cc85620`;
the oracle remains `163e6ef0e5e33daba3e8e381f41b76f35da417f9793bec0b76459c754abed417`. `ldd` for
both binaries has no unresolved or temporary/checkout dependency path. This is compile/link and
loader-closure evidence only; the real MiniNDN post-grant boundary remains unresolved.

## Python binding global-closure follow-up — 2026-09-18

The two Python binding build entry points now apply the same external dependency
policy as Waf. `pkg-config` include/library/linker paths and explicit
NDN-SVS/NAC-ABE prefixes must resolve under `/usr`, `/usr/local`,
`/opt/ndn-base` or the versioned ONNX Runtime root. `/tmp`, checkout trees and
the retired `.local-boost171` prefix fail closed. The APP's own
`NDNSF_LIBRARY_DIR` remains a separate candidate-library boundary; it does not
authorize external dependency paths.

The only container exception is explicit: the maintained Tiger template exports
`NDNSF_CONTAINER_BUILD=1` before binding builds, which allows the sealed APP
stage `/opt/ndnsf-stage` for its NAC-ABE/SVS inputs. Without that marker the
same path is rejected. This keeps host and container closures separate.

Frozen review snapshot:
`.codex-tmp/spec189-global-python-closure-review-20260918-v2/`, diff SHA-256
`6821f36dcb9f2bd4f81ab719f7659df51aa6dea3d97020ec20a70a7e8db2ac5c`.
The official read-only review-agent returned `STATIC_PASS`; it found no P0-P2
defect after the `/opt/ndnsf-stage` marker fix. The review did not observe a
real Python build, ELF loader closure, or container runtime.

Focused validation after the repair:

| Check | Result |
| --- | --- |
| `tests/python/test_python_wrapper_native_closure.py` | 28 passed |
| `tests/python/test_spec180_native_build.py -k setup_` | 12 passed, 74 deselected |
| Tiger template + binding/setup selection | 54 passed, 74 deselected in combined run |
| Python syntax and `git diff --check` | passed |

These checks establish the fail-closed configuration and test contracts only;
they do not advance T001–T010 or change the unresolved post-grant MiniNDN
qualification boundary.

## Global install and binding loader closure — 2026-09-18

The validated `build-spec189-b189-3-global-r3` tree was installed from inside
that tree (`cd build-spec189-b189-3-global-r3; sudo -n ../waf install`); invoking
Waf with `-o` from the repository root is forbidden by the recorded failure
boundary because Waf locks onto the most recent configured output directory.
Core/DI and both Python binding extensions were then built against the installed
global closure:

| Artifact | Result |
| --- | --- |
| `/usr/local/lib/libndnsf-distributed-inference.so` | installed; SHA-256 `484fc76b11a06d5cda6dd9b3321c165d9cda3df5e114606b86e4c7548b4fc9c3` |
| root `ndnsf` editable binding | built/installed by Waf; extension SHA-256 `645a68871f4446a2e9b8f4a2e3828bba56000d9c2d1ecbbddcd105f128c41a23` |
| Repo `_py_repoclient` binding | built with `NDNSF_LIBRARY_DIR=/usr/local/lib`; extension SHA-256 `b32322ae83d9d607e2de309dd7315ba82bc795c36650fd6505fcb314c0d72b09` |
| DI pkg-config metadata | installed at `/usr/local/lib/pkgconfig/ndnsf-distributed-inference.pc` |

`pkg-config --variable=libdir` returned `/usr/local/lib` for
`libndn-cxx`, `libndn-svs`, `libnac-abe`, `ndnsd`,
`ndnsf-distributed-inference` and `libndn-service-framework`. `ldd` for both
extensions resolved NDN-CXX, NDN-SVS, NAC-ABE, NDNSD, OpenABE/Relic, Core and
DI to `/usr/local`; Boost 1.71 resolved to `/lib/x86_64-linux-gnu`; ONNX Runtime
resolved to `/opt/onnxruntime`. No `not found`, checkout, `.local-boost171` or
`.codex-tmp` entry was present.

This is a real host install and Python loader-closure result. It does not prove
the Spec189 request chain, hidden-state handoff, resource drain or MiniNDN
qualification; T001–T010 remain unchanged.
