# Spec189 real Qwen r56 liveness boundary — 2026-09-19

## Scope and candidate

This is a fresh local MiniNDN attempt using the reviewed global-r3 native
candidate. It is retained as `UNQUALIFIED`; no protocol or product PASS is
derived from the cleanup result.

- Run: `two-provider-global-r56`
- Candidate: `qwen06b-553e2d82160a`
- Candidate digest: `sha256:553e2d82160a749ba3b41d433cc55d969cd918e8457c4889d8c6dee34a1d49f5`
- Build tree: `build-spec189-b189-3-global-r3`
- Profile: `spec189-qwen06b-global-r2`
- Run command metadata: `.codex-tmp/spec189-b189-3-r4-20260919/two-provider-global-r56-launch.json`
- Raw workload: `.codex-tmp/spec189-qwen-two-provider-20260918/runs/two-provider-global-r56/workload/`

The launch tuple bound the stage manifest, tokenizer, topology, build receipt,
all native binaries, canonical ONNX source/initializer and node mapping. The
host guard limits were `maxSwapIoBytes=268435456`,
`minAvailableBytes=1610612736`, `minDiskFreeBytes=4294967296`, and a 3600-second
run timeout.

## First proven boundary

Both native Providers reached `READY`, emitted signed ACK offers, and recorded
`NDNSF_DI_GRANT_VERIFICATION` with `boundary=BEFORE_ASSEMBLY`. The requester
then ended with:

```text
NATIVE_REQUEST_STAGE_FAILED code=NATIVE_STREAM_FAILED boundary=stream
NATIVE_STREAM_FAILED domain=provider boundary=stream message=Core stream failed: stream event gap exceeded retry budget
```

Neither Provider log contains `ASSEMBLY_STARTED`, `ROOT_VERIFIED`,
`RUNNER_READY`, execution completion, a stage output, or a terminal response.
The supervisor recorded `returncode=1`, `cleanup=PASS`, and no remaining
processes. Resource samples recorded zero swap-I/O delta and zero swap used;
the minimum available memory was approximately 3.85 GiB, so this run is not
classified as a resource boundary.

The first failure is therefore the requester/provider stream-liveness boundary
after grant verification and before an observable assembly admission event. It
does not establish an ORT failure, a Repo receipt failure, a hidden-state
failure, or Provider idleness.

## Changed gate and local verification

The previous progress reporter first ran after the root fetch. That left the
silent admission interval unobservable. The reviewed repair now emits the
authenticated `ASSEMBLY_STARTED` milestone immediately before the first root
material fetch. The immutable review snapshot is:

- `.codex-tmp/spec189-r4-start-static-review-20260919-v1/changed.diff`
  SHA-256 `0800a7f9a8b111ef88bda76359f5c7487f1db1a1b4cc419d8aa6a3b0ff`
- snapshot files hash:
  `.codex-tmp/spec189-r4-start-static-review-20260919-v1/files.sha256`
  SHA-256 `0e9e05d6a20cee3b80c7be9d5fdcd3768eae94ea97156fe943425574d2d00891`
- official read-only review: `STATIC_PASS`

The affected global-r3 closure rebuilt successfully with Waf `-j3` in
35.411 seconds. C++ verification then passed:

```text
Spec175InvocationStreamLifecycle                 15/15 PASS
Spec175NativeAssembly/AssignmentBoundRoot...       PASS
Spec175NativeAssembly                              9/9 PASS
```

These are local C++ regression results only. A fresh real run with the rebuilt
candidate is still required; r56 used the pre-repair binary and cannot test
the new admission milestone.

## Five-lane classification

| Lane | Result |
| --- | --- |
| Static | `STATIC_PASS` for the changed assembly-admission scope |
| Compile/link | `PASS` for affected DI, unit, integration and worker closure (`-j3`) |
| Runtime-test | `PASS` for the C++ lifecycle/assembly selectors (15/15, 9/9) |
| Protocol/qualification | `UNQUALIFIED`; r56 stopped before assembly and terminal output |
| Migration/evidence | `PARTIAL`; a fresh post-repair candidate and two-request repeat remain open |

## Decision

Do not increase timeouts or add another component batch before one fresh run of
the rebuilt candidate. The stable next gate is to observe whether
`ASSEMBLY_STARTED` reaches the requester before its stream gap expires. If it
does, classify the next first boundary (material fetch, handoff, runner,
output, or drain) and repair only that boundary. If it does not, inspect the
authenticated progress publication/consumer path before changing model or
resource design.
