# B189-1c / T003-R3 Filesystem fd ownership evidence

**Status**: `FOCUSED_CXX_PASS / QUALIFICATION_OPEN`

This record closes the F09 focused boundary only. It does not close T003,
does not prove the full preparation quota, and does not grant any Qwen or
MiniNDN qualification.

## Frozen review

- Snapshot: `.codex-tmp/spec189-f09-review-r4/`
- Review: official read-only `review-agent`, `STATIC_PASS`; snapshot hashes are
  in `SHA256SUMS`.
- Scope: `FilesystemRepoStoreBackend.cpp`, the narrow test-only POSIX hook
  header, `spec189-repo-fd-owner.t.cpp`, and its Waf target. No unrelated
  working-tree files were included in the snapshot.

The production backend now transfers fd ownership immediately after every
`fsync`/`close` attempt. A pre-rename failure unlinks only the temporary
sidecar and preserves the old manifest. A post-rename directory-sync failure
is reported as `repo-file-ambiguous-commit`; rollback rechecks the visible
manifest and does not delete the newly referenced payload.

## C++ validation

Build from the repository root using the existing global dependency build tree:

```text
cd build-spec189-b189-3-global-r3
../waf build --targets=spec189-repo-fd-owner -j4
```

- build: `rc=0`, 4.266 s; raw log `.codex-tmp/spec189-f09-build-r2/build.log`
- selector: `./build-spec189-b189-3-global-r3/spec189-repo-fd-owner --log_level=test_suite`
- runtime: `rc=0`, 3/3 cases, no errors; raw log
  `.codex-tmp/spec189-f09-build-r2/test.log`

The selector injects deterministic failures through the C++ production
backend and checks all of the following:

1. metadata `fsync` failure closes the descriptor once, leaves the previous
   manifest readable, and removes the temporary sidecar;
2. metadata `close` failure transfers ownership without retrying the numeric
   descriptor, with the same old-manifest and temporary-file guarantees;
3. directory `fsync` failure after rename is ambiguous while the new manifest
   and payload remain readable.

The selector additionally requires exactly the expected two hooked closes for
the failed replacement path, zero duplicate-close observations, and an empty
tracked-fd set. This covers the rollback directory-sync close as a legitimate
second close rather than mistaking it for a retry of the metadata fd.

## Boundary

`F02` preparation peak accounting, `F05` mixed committed/staged/range quota,
the protected Core→Provider ingress, and the complete Qwen two-provider chain
remain open. Rename failure and crash-recovery qualification remain
`UNOBSERVED`; they are outside this focused F09 selector.
