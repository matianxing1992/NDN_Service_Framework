# B189-3 host-native MiniNDN retry r59

**Date**: 2026-09-19
**Status**: `PARTIAL` / `NATIVE_STREAM_FAILED`
**Candidate**: `qwen06b-553e2d82160a`, run `two-provider-global-r59`

## Host cleanup and runtime boundary

This retry used the host-native MiniNDN path. It did not invoke SIF,
Apptainer or TigerCluster. The verified native build tree retained the six
runtime binaries and shared libraries; 808 rebuildable `.o`, `.d` and `.pyc`
files (4,612,282,160 bytes) were removed. The cleanup manifest and before/after
binary hashes are in `.codex-tmp/spec189-native-build-cleanup-20260919-r1/`.

The key binary digests remained identical to r58, including the Provider
`e468b196af29c491a8368b68e0ff801d58f00bc069b8a2748db31218ca5cf2da` and
assembly worker
`22a33e2f28cb61ca6410479088ecab1b091f8e78e4a167bb2c4c40afa863c661`.
`ldd` reported no unresolved libraries. The run started with about 8.3 GiB
free disk and stayed above the 4 GiB disk floor; available memory stayed above
the 1.5 GiB guard and swap remained unused.

## Production boundary

Both native Providers reached `READY` and emitted signed `ACK_DECISION`
offers. Both then emitted authenticated `GRANT_VERIFICATION` for the same
request and attempt. The requester next failed with:

```text
NATIVE_REQUEST_STAGE_FAILED code=NATIVE_STREAM_FAILED boundary=stream
NATIVE_STREAM_FAILED domain=provider boundary=stream message=Core stream failed: stream event gap exceeded retry budget
```

No `ASSEMBLY_ADMISSION`, `ASSEMBLY_STARTED`, material fetch, runner,
execution, terminal output or qualification result was observed. The host
supervisor reported `cleanup=PASS`, `returncode=1`, and no remaining
processes. Resource samples did not classify this run as `RESOURCE_BOUNDARY`.

This is the first post-grant stream-liveness boundary after the disk problem
was removed. It does not prove an ORT, Repo, model, assembly or SIF failure.

## Raw evidence

Raw run data is retained at
`.codex-tmp/spec189-qwen-two-provider-20260918/runs/two-provider-global-r59/`.
The launch and command identities are
`.codex-tmp/spec189-b189-3-r4-20260919/two-provider-global-r59-launch.json`
and `two-provider-global-r59.command.json`. The next repair must target the
post-grant stream event path, freeze a new review snapshot, and use a new run
ID; changing timeout or deleting the r59 evidence is not a repair.
