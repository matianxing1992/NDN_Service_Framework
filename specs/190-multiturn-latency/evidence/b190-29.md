# B190-29 — Face-backed exact missing-grant negative

## Status

`ADVANCE` for the T006 exact-fetch negative sub-gate；T006 remains `PARTIAL`
and T007 remains locked.

## First boundary and repair

本轮先在已经通过的 served-Provider selector 后追加一个错误 grant name
`/spec185/missing-grant`。第一次运行在该负例处中止，原始结果为
`memory access violation`，仅通过 `10/11` assertions。受控 GDB 回溯确认
首因不是生产 Provider、Face 或 `PendingInterestHandle`，而是测试代码把
`protectedGrantFetcher` `std::move` 到 `Provider::fromServiceProviderForTest`
后又调用 moved-from lambda；其 `environment` 捕获为空。

修复只保留同一 lambda 的副本 `protectedGrantFetcherProbe` 用于显式负例，
生产 Provider 仍接收原 callback；没有改变生产 API、grant wire、取消或
deadline 语义。首次失败 raw run 保留在
`.codex-tmp/spec190-b190-29-missing-grant-selector-r1/`，GDB 边界保留在
`.codex-tmp/spec190-b190-29-missing-grant-gdb-r2/`。

## Changed gate

同一个 `Provider::serve` 默认 protected-grant factory selector 现在验证：

`successful exact signed grant fetch → GRANT_VERIFIED → assembly → runner →
terminal`，随后用同一 Face-backed fetcher 请求不存在的 exact name，要求
超时 fail-closed，且 `grantDataFetches` 与 `exactGrantFetches` 均不增加。

这只关闭 grant transport 的错误名称负例；不等同于缺失 encrypted layer
object 的 placement-bound 精确补取，也不解除 protected assembler miss 门。

## Validation

- `compile-link`: `./waf build --out=build --targets=spec185-prepared-request -j4`
  passed，证据见 `.codex-tmp/spec190-b190-29-missing-grant-build-r3/build.rc`。
- `runtime-test`: `PreparedRequestCompletesThroughServedProvider` 修复后
  `r3=0`，独立重复 `r4=0`、`r5=0`、`r6=0`；每次均保留成功链路和
  missing-grant 无 Data hit 断言。
- `static`: `git diff --check` passed；`python3
  skills/speckit-code-design/scripts/verify-spec-kit-sync.py --require-entrypoints`
  passed `11/11`；本轮没有修改生产接口或跨模块设计原则。
- `unobserved`: real NFD/MiniNDN route、online Controller/revoke/rebind、
  durable protected restart、missing layer object、真实 Qwen 和完整 T006
  仍未观测。

## Closure decision

`OPEN_FOR_NEXT_BATCH` within T006。精确 grant 错误名称负例已通过；T006
仍为 `PARTIAL`，T007 继续锁定，下一步按任务顺序处理剩余授权重绑定、
missing layer object 和真实 route/restart gates。
