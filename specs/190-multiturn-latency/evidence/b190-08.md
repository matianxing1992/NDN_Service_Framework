# B190-08 Startup Boundary And Direct Retry

2026-09-23, the direct MiniNDN launch was retried after the r26 native stream-budget
fix. These attempts did not start MiniNDN and are retained as startup evidence.

## Boundaries

1. The first r27 invocation stopped at `MODEL_STAGE_MANIFEST_DIGEST_MISMATCH` because
   the command supplied bare hexadecimal digests while the launcher contract requires
   `sha256:<hex>`. No child process or MiniNDN state was created.
2. The corrected invocation passed model, topology, node-mapping, binary, and build
   receipt checks, then stopped at
   `ENCRYPTED_REPOSITORY_PATH_MUST_BE_OUTSIDE_RUN_ROOT`. The launcher deliberately
   keeps run evidence and disposable encrypted Repo staging in separate roots. No
   MiniNDN child process or provider execution was started.

The next retry uses one direct launcher invocation, the immutable prepared model/cache,
one new run-scoped evidence directory, and one fixed `/var/tmp` encrypted-Repo staging
directory. This is the minimal directory separation required by the existing cleanup
contract; it is not a second launcher layer or a second model preparation path.

## Coverage

| lane | result |
|---|---|
| static | PASS: direct-start path and preflight boundaries identified |
| compile-link | PASS: affected DI library built and installed before retry |
| runtime-test | PASS: native stream-budget focused checks; no MiniNDN result in this record |
| unobserved | two-provider terminal/EOS, three rounds, KV reuse |

Closure decision: `OPEN_FOR_NEXT_BATCH`. T003 remains `PARTIAL`.
