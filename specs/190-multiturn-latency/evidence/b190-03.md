# B190-03 — Live turns checkpoint

**Status**: PARTIAL / NOT_CLOSED
**Changed gate**: T003 `LiveTurns`
**Date**: 2026-09-22

## Current Changed gate

针对 run-05 的首边界，`ProviderGroupCoordinator::beginOperation(nowMs)` 已在每个
`NDNSF_DATA_V1` dependency/publication operation 开始时刷新 no-progress baseline，但不改变
group hard deadline。C++ 回归覆盖长 idle gap 后仍可开始新 operation，以及 operation 内停滞
仍失败；未修改 timeout、run id、日志级别或绕过 selection/Repo/授权路径。该 gate 经
compile-link 和 standalone C++ selectors 验证，但尚未通过真实三轮运行闭合。

## Closure decision

T003 的原生实现和静态 gate 已完成，但本批不能闭合。`DI_NativeRequester`
现在支持 `turns[]`，在一次 `Runtime → User.prepare → PreparedModel → Conversation`
生命周期内顺序提交；streaming path 在 `result()` 前打开 `EventReader`，对空读继续等待，
并在首个事件文本 flush 后提供受 `test_only` 保护且有界的 parent/pipe barrier。
MiniNDN launcher 改为一次启动 requester，并生成 per-turn compatibility config、checkpoint
和 oracle log view；默认 profile 为三轮。

之前的 PreparedConversation focused selector 失败点已定位为测试 fixture 在
`Runtime::user().prepare()` 前没有绑定带可路由 Attribute Authority 的 LocalMock
`ServiceUser`，并且没有独立 pump 处理同步 prepare 发布的异步回调；这不是生产
Conversation/EventReader 的失败。T003 fixture 现已在 prepare 前完成 binding，并由 C++
pump thread 驱动 AA/Face。修复后 served-provider 和 two-native-turn selectors 均通过。
随后使用固定 candidate、真实 MiniNDN 上下文和 normal protected Repo mode 执行 run-04。
该运行确实进入两 Provider 生产路径，Provider-0 到达 `RUNNER_READY`，Provider-1 持续执行
material bundle assembly；约 12.43 分钟后 host guard 以
`RESOURCE_BOUNDARY:ownedSwap` 停止，`ownedSwapBytes=348352512` 超过
`maxOwnedSwapBytes=268435456`，峰值 RSS 为 `7897657344`，累计 `swapIoBytes=599013564416`，
而当时 `availableBytes=1755144192` 仍高于最小可用内存门。supervisor 报告 `cleanup=PASS`
且无残留进程，但三轮 Qwen、terminal、第二轮失败/取消负例及完整 cleanup 验收仍未闭合。
随后在 T003 内完成了针对该资源边界的原生内存修复和 focused C++ 验证。run-05 使用同一
真实两 Provider 入口重新执行，两个 Provider 都完成约 752 MB 的 cold assembly 并到达
`RUNNER_READY`；supervisor 没有触发资源边界（`maxOwnedSwapBytes=109174784`、
`minAvailableBytes=2230706176`），cleanup 仍为 `PASS`。但 Provider-1 在下一次
`DEPENDENCY_FETCH` 的 exact fetch 前报告 `NDNSF_DATA_V1 group is terminal before exact fetch`。
冻结配置的 `request.no_progress_ms=180000`，而首次 dependency 完成到下一次 operation
开始间隔约 341 秒，说明当前 coordinator 把 runner preparation/operation 间空闲错误地计入
传输 no-progress 窗口。run-05 没有 terminal、三轮结果或完整 request cleanup，因此
T003 仍为 `PARTIAL`，T004 不得启动。

## Coverage matrix

| Lane | Result | Evidence |
| --- | --- | --- |
| static | PASS | Kant read-only review：turns contract、single-process launcher、bounded result wait、ordinary queued-stream compatibility、per-turn oracle views；no remaining finding |
| compile-link | PASS | Current full `spec190-live-turns` target `201/201` finished; [current build](../../../.codex-tmp/spec190-t003-live-target-rebuild.log). |
| runtime-test | PARTIAL | 两项 fixture selector、内存相关 focused C++ selector 和 group-progress selectors PASS；run-04 在 `RESOURCE_BOUNDARY:ownedSwap` 停止，run-05 越过内存门并双 Provider `RUNNER_READY`，run-07 通过 ACK/Selection、Provider-0 `RUNNER_READY` 和 Provider-1 首次 dependency fetch 后在并发 cold assembly 触发 `RESOURCE_BOUNDARY:MemAvailable`；三次 supervisor cleanup PASS，均无 terminal/三轮结果；[run-04](../../../.codex-tmp/spec190-t003-real-20260922/run-04/)、[run-05](../../../.codex-tmp/spec190-t003-real-20260922/run-05/)、[run-07](../../../.codex-tmp/spec190-t003-real-20260922-rerun/run-07/)。 |
| unobserved | BLOCK | Real requester/config pipe, three-turn Qwen, provider terminal/cleanup and performance remain unobserved |

## Failure boundary

`DI_NATIVE_ENCRYPTED_PUBLICATION_FAILED: NAC-ABE produced no wrapped large-data
MessageKey` occurred during the earlier prepared-conversation fixture after
`NDNSF_INTEGRATION_BOOTSTRAP_READY`. Static diagnosis showed `Runtime::user().prepare()`
was running on an unroutable production `ServiceUser` before the fixture binding and
without a C++ pump. The repaired fixture now binds the environment user before prepare
and pumps while prepare runs; both focused selectors pass. The original failure and
repair boundary remain recorded in [`docs/failure-log.md`](../../../docs/failure-log.md).

run-04 的资源边界修复后，run-05 的首个真实 request-chain 边界变为 Provider-1 下一次
`DEPENDENCY_FETCH` 的 `NDNSF_DATA_V1 group is terminal before exact fetch`。两个 Provider
在该边界前均已 `RUNNER_READY`，资源 supervisor 没有触发；复核冻结配置和时间线可得
`no_progress_ms=180000`、两次 operation 间空闲约 341 秒。该 no-progress 基线必须只约束
一次正在执行的 transport operation，不能覆盖 runner preparation 或 operation 间空闲。

## Miss retrospective

- `static`: no remaining T003 finding after the final read-only review.
- `compile-link`: the broad `unit-tests` target was attempted but stopped before link at the
  pre-existing Spec182 `di-native-planning.t.cpp` aggregate-initializer mismatch; the failure is
  recorded in `docs/failure-log.md` and `.codex-tmp/spec190-t003-group-progress-build.log`.
  The affected `spec190-live-turns` target then rebuilt `201/201`; a standalone C++ group
  regression binary also compiled and linked against the rebuilt native library.
- `runtime-test`: the repaired prepared-provider and two-native-turn selectors pass; run-04
  stopped at the owned-swap boundary, run-05 crossed the memory gate and reached
  `RUNNER_READY` on both Providers before the stale group no-progress boundary, and run-07
  crossed ACK/Selection plus Provider-0 `RUNNER_READY` and Provider-1 first dependency fetch
  before the concurrent cold-assembly memory boundary. The new C++ idle-gap and in-operation
  inverse selectors both pass; serialization/limiting of cold-start admission and the real
  three-turn retry remain outstanding.
- `unobserved`: three-turn request IDs, terminal output, second-turn failure/no-third-turn,
  cancellation/busy negative cases and full post-run cleanup remain unobserved; cleanup PASS
  only proves supervisor process cleanup for this failed attempt.

## Stable exit and next step

The focused commands exited deterministically, while run-04 and run-05 preserved their raw logs,
resource samples and supervisor decisions. The native Changed gate, static review, Spec190
compile-link and both C++ progress selectors now pass. The next T003-only step is a new immutable
real run using the rebuilt binaries. The first run-06 launch-preflight attempt stopped before
MiniNDN because the historical parent run directory was root-owned; this produced no protocol
result. A new user-owned parent is required. The successful real run must still traverse ACK,
Selection, placement-bound fetch,
assembly, execution, terminal output and cleanup. The retry then confirmed that this launcher
requires `sudo -E` for MiniNDN and stopped before any Provider/Repo protocol work; only preflight
supervisor/resource files exist. The root run-07 then passed ACK/Selection, Provider-0 reached
`RUNNER_READY`, and Provider-1 completed its first dependency fetch before the host guard stopped
concurrent cold assembly at `RESOURCE_BOUNDARY:MemAvailable`:
`availableBytes=1437327360 < 1610612736`, `ownedSwapBytes=230567936`, `rssBytes=7731904512`;
cleanup was `PASS`, but no Provider-1 runner, terminal or three-turn result exists. The next
Changed gate must serialize/limit cold-start admission and retain the current resource threshold.
The next attempt must use that required root boundary. Do not
mark T003 DONE and do not start T004 from this checkpoint.
