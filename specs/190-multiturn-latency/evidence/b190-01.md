# B190-01 Evidence: T001 Phase Timing

**Status**: DONE (focused T001 closure; Spec190 overall remains PARTIAL)
**Updated**: 2026-09-22 04:33 -0500

## Closure decision

T001 的阶段计时基础设施已完成并通过本项验收。Core、DI provider、native requester/CLI
现在输出带有 `requestId`、`attempt`、`providerBootId`、`sessionId`、`conversationId`、
`inferenceEpoch`、`contextEpoch` 和 steady/wall timestamps 的 canonical phase record。
C++ validator 会拒绝跨 request/attempt/identity 拼接、缺失或重复阶段、未闭合的 begin/end、
steady-clock 倒退、错误的 checkpoint/context epoch 转换和 token lane 混用。

本项只证明 phase timing 的实现与 focused C++ contract；没有运行 MiniNDN、Qwen 两节点实验，
没有测得多轮 latency improvement，也没有宣称 Spec190 的完整验收通过。

## Implemented scope

- `NDNSF-DistributedInference/cpp/ndnsf-di/RuntimeTiming.hpp/.cpp`
  - canonical parser/emitter and sequence validator;
  - identity/attempt/provider/session/conversation/epoch checks;
  - paired role, preparation, dependency and ORT phases;
  - independent API `tokenReceived`/`tokenDelivered` and CLI `tokenEmitted` lanes;
  - ACK receive/verify pairing and scoped multi-provider ordering;
  - checkpoint-to-`turnReady` context transition and close/drain ordering.
- `ndn-service-framework/TimelineTrace.hpp/.cpp` and `ServiceUser.cpp`
  - real Core request/ACK boundaries, including `ackVerified` after authentication,
    provider/token/replay checks and before ACK insertion/closure/selection.
- `NDNSF-DistributedInference/cpp/ndnsf-di/NativeInferenceClient.cpp`
  - acceptance/delivery, durable checkpoint, turn-ready and close/drain observations.
- `NDNSF-DistributedInference/cpp/ndnsf-di/ProviderRoleWorker.cpp`,
  `NDNSF-DistributedInference/cpp/adapters/onnx/OnnxRuntimeModelRunner.cpp`, and
  `DiTimelineTrace.hpp`
  - provider boot identity and phase boundaries around the actual ORT run and stage edges.
- `examples/DI_NativeRequester.cpp`
  - real CLI text-delta output and flushed `cli-output/tokenEmitted` observations.
- `tests/unit-tests/spec190-timing.t.cpp` and `tests/wscript`
  - C++ parser, ordering, identity, token, epoch, ACK and canonical-emission regression target.

## Coverage matrix

| Lane | Result | Durable evidence |
| --- | --- | --- |
| static | PASS | Final read-only review agent: PASS; no remaining P0/P1/P2 blockers. The review specifically confirmed `ackVerified` placement in `ServiceUser::handleRequestAckByName`. |
| compile-link | PASS | `./waf -o build-spec189-oracle build --targets=spec190-latency-tests,DI_NativeRequester,ndn-service-framework,ndnsf-distributed-inference,di-native-provider -j2 -v`; 216/216; [build log](../../../.codex-tmp/spec190-t001-final-build.log) |
| runtime-test | PASS | `build-spec189-oracle/spec190-latency-tests --run_test=Spec190Timing --log_level=test_suite`, three independent runs; [run 1](../../../.codex-tmp/spec190-t001-final-test-1.log), [run 2](../../../.codex-tmp/spec190-t001-final-test-2.log), [run 3](../../../.codex-tmp/spec190-t001-final-test-3.log) |
| review-agent trace | PASS | Read-only final review after the last ACK-boundary repair; no files changed and no tests run by the reviewer. |
| unobserved | NOT_RUN | MiniNDN/Qwen two-provider production chain, measured token latency, network transfer budget, resident model/cache reuse and full Spec190 acceptance. |

## Regression result

三次 `Spec190Timing` 均输出 `*** No errors detected`。测试覆盖：

- parser boundary、canonical emission、wall-clock rollback and steady-clock ordering;
- request/attempt/identity mismatch and duplicate/unclosed phase rejection;
- API token queue delivery and separate CLI emission lane;
- checkpoint/context epoch transition and invalid post-checkpoint identity;
- interleaved provider ACK identity and duplicate verification;
- scoped dependency pairs and phase order.

## Failed-attempt boundary

早期失败尝试没有覆盖或改写：

- fixture stale-epoch failure: `.codex-tmp/spec190-t001-repair3-test-1.log`;
- positional-attempt compile/test boundary: `.codex-tmp/spec190-t001-repair5-build.log`;
- `goto` declaration compile boundary: `.codex-tmp/spec190-t001-repair7-build.log`;
- requester API-name compile boundary: `.codex-tmp/spec190-t001-repair8-build.log`.

这些失败及其首边界已登记在 `docs/failure-log.md`；raw logs 保留在 `.codex-tmp/`，不纳入 Git。

## Next sequential task

T002（ACK window）是唯一允许启动的下一项，当前尚未开始。不得把本证据中的 timing
contract、focused C++ regression 或静态 PASS 解读为 T002 或完整 Spec190 已完成。
