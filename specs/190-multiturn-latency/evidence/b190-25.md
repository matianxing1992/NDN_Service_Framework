# B190-25 / T006 Protected Assembled Cache Across Provider Exec

**Status**: `ADVANCE` / T006 remains `PARTIAL`
**Updated**: 2026-09-23 20:37 -05:00
**Scope**: close the remaining T006 cross-process protected assembled-cache
boundary without persisting a parent request key, plaintext, KV state, or old
grant.

## Static review

The previous B190-24 selector proves Provider `SegmentFetcher` and AES-GCM
decryption only after an explicitly injected current receive key. The existing
`ProtectedAssembledCacheUsesStableCiphertextAfterNewAuthorization` selector
proves a new in-process grant can reopen stable ciphertext, but its
`ProtectedRuntime`, grant wire, and provider boot identity all remain in one
process.

The changed gate keeps the production `tryLoadNativeCanonicalOnnxRoleFromCache`
and `openNativeAssembledEntryToFile` path unchanged. The parent performs the
existing protected cold assembly, completes its runtime, and writes only a
current child Selection projection, a newly issued grant fixture, non-secret
cache identity, and operator test credentials to a private temporary fixture.
The `fork`+`exec` child constructs a new `ProtectedRuntime` with a different
`providerBootId`, verifies the new grant, reopens the stable ciphertext, decrypts
it to a registered request-scoped plaintext directory, checks the assembled
digest, and completes the plaintext lease. The cache lookup remains protected
by the new runtime and key-reference digest; no protected miss gate is removed.

`static`: `PASS` for the test-only change. The caller, binding fields, grant
verification, file-backed decrypt, child cleanup, and existing Waf target were
checked. The gate does not claim online Controller confirmation, real network
Selection, revoke invalidation, or a full Provider process with NDN routes.

## First-boundary attempts

The first three child attempts stopped before entering the selector because the
Boost filter omitted the nested `Spec188ProviderReferenceAssembly` suite. Their
raw logs are retained as `selector-1.log` through `selector-3.log`; no protected
cache assertion ran in those attempts. The filter was corrected to the exact
three-level test path and the selector was rebuilt.

## Validation ledger

| Lane | Result |
| --- | --- |
| `static` | `PASS`; exact diff and production caller review complete |
| `compile-link` | `PASS`; ordinary root `build/` `spec185-provider-assembly` rebuilt successfully (`23.3s`) |
| `runtime-test` | `PASS`; corrected selector passed 3 consecutive times; complete target passed `21/21` |
| `resource/cleanup` | `PASS` at fixture scope; child plaintext directory was removed by the runtime lease and both cache/exec roots were absent after the parent cleanup |
| `unobserved` | full online grant/Selection/placement, revoke, missing-object fetch, real Qwen/MiniNDN, and T007 resident session |

## Changed gate

The corrected selector passed with a different child `providerBootId`, a newly
issued grant digest, stable ciphertext digest, byte-for-byte assembled plaintext
digest equality, and child plaintext lease cleanup. The complete target also
passed. This advances the cross-process protected assembled-cache gate; it does
not close T006 because online Controller confirmation, real network
grant/Selection/placement rebinding, revoke invalidation, and precise
missing-object fetch remain unobserved.
