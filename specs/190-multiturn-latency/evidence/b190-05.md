# B190-05 — Stable cache-root plumbing (PARTIAL)

## Scope

本记录只覆盖 LocalExperiment `prepare` 到 native runner 的稳定缓存根接线，不声称
T004 RepoRestart、T005 prepare lookup/reuse 或 Spec189/190 实验资格完成。

`Experiments/NDNSF_DI_Qwen06B_Native_Minindn.py` 已有两类受身份约束的缓存：

- canonical source Repo 使用 `model-source/<identity-digest>/`，由模型名、manifest、graph、
  initializer、node mapping、tokenizer 等完整 identity 派生；对象仍按 SHA-256 内容寻址并验证大小/hash。
- Provider assembled cache 使用已认证 Selection 后的 recipe digest；普通 protected 路径不会因为
  本地缓存存在而绕过 grant、ACK、Selection、placement 或 encrypted Repo 契约。

本单元只在 `Experiments/NDNSF_DI_Qwen06B_LocalExperiment.py` 增加 `--cache-dir`（并接受
`--artifact-cache-root` 别名），prepare 生成的 launch record 记录绝对 `cacheRoot`，命令显式
传递 `--artifact-cache-root`。共享根必须位于 run workload/evidence 根之外；已有非目录路径和
run-scoped cache 路径在准备阶段 fail closed。cleanup 不接管这个共享根。

## Validation

| Lane | Result | Evidence |
| --- | --- | --- |
| static | PASS | CodeGraph/source review confirmed existing source/assembled cache lookup owners and the new wrapper-only argument path |
| compile-link | PASS | `python3 -m py_compile Experiments/NDNSF_DI_Qwen06B_LocalExperiment.py Experiments/NDNSF_DI_Qwen06B_Native_Minindn.py tests/python/test_spec184_qwen06b_local_experiment.py` |
| runtime-test | PASS | `pytest -q tests/python/test_spec184_qwen06b_local_experiment.py`: `32 passed`; covers command propagation, run-root rejection, exact identity reuse, digest-verified object hit, and identity miss |
| unobserved | OPEN | No new normal Repo run; cross-run source hit, restart recovery, protected-material reuse, two-provider terminal and latency remain unobserved |

## Closure decision

Implementation-level cache-root plumbing is complete for this unit. This is not a qualification
result and does not unlock T004 under STRICT_SERIAL; T003 remains `PARTIAL`, T004 remains
`NOT_STARTED`. The worktree contains unrelated/parallel changes, so no mixed local checkpoint
commit was created.
