# Repo Python-only base repack

Status: IN_PROGRESS. This updates the base-library candidate, not runtime qualification.

The Repo ordering fix is the only changed row among117 original base workspace
files. All pinned NAC-ABE/NDN-SVS/NDNSD dependency archives must remain identical.
`repack-base-python.py` validates both complete source seals, the parent SIF
digest, package-only Python changes, and the exact file set. It rejects native,
dependency, new/deleted-file and no-op changes. Five boundary checks pass.

The update inside the extracted image compares the installed predecessor seal,
Python bytes, and six native artifacts. It installs the new Python, removes
only its generated bytecode, updates wheel RECORD hashes, records the new source
seal and parent provenance, and reruns native imports/ldd closure. Actual apply:
PASS/PYTHON_REPACK_ONLY, nativeArtifactsUnchanged=6. No compiler ran.

The external-app builder can explicitly use `--python-repacked-base` with
`--build-cache-from`. It reads the record from the hashed new image, binds the
new source seal and predecessor SIF, validates the predecessor app/cache, and
still runs the new image's verifier, configure, and Waf. It does not relabel
old compiler provenance without executing that dependency check.22 combined
repack/cache checks pass; stricter record-row validation then passes5 focused
checks. Actual application rebinding has not yet executed.

## Failures and extraction fallback

The first new seal omitted the original `--derive-ndn-svs-version`. The strict
dependency comparison rejected its VERSION.info provenance/archive difference;
same revision/file bytes do not authorize silently changing the sealed archive.
Fresh `repo-order-source-r2` uses the original option and passes validation.

Apptainer's localimage bootstrap fails decompressing the inherited libnccl.so.2
with gzip error-5, before applying the update. RAM SIF still hashes to d4031191….
Host unsquashfs4.4 lacks the attempted -cat/-offset flags; that command is not
evidence. A read-only loop at SIF partition offset49152 and host unsquashfs with
one processor extracts the same file successfully, SHA
`78df2f31f6db8142ec546a1e5a31cb066f7892d12d2f665b448f8069a08ef807`.
Full single-processor extraction then succeeds (66002 files,9141 directories,
8419 symlinks); the owned loop is detached by an EXIT trap. The precise cause
of the Apptainer extraction failure is not established.

The same apply script runs inside that extracted sandbox, then Apptainer packs
it using two processors and512MiB squashfs memory. The generated localimage
definition was the failed first route; retain the actual fallback commands and
logs as provenance instead of claiming that definition completed successfully.

## Artifacts and remaining work

Source/repack/sandbox are under `Experiments/TigerCluster/.cache/layered-base-20260908/`:
`repo-order-source-r2`, `repo-order-repack`, `repo-order-rootfs`.
Logs live under `Experiments/TigerCluster/results/yolo-layered-20260908/preflight/`:
`base-python-repack-build.log`, `base-single-extract.log`,
`base-python-repack-apply.log`, `base-python-repack-pack.log`.

Next: verify the completed SIF independently; build/rebind the unchanged app
through the explicit incremental path; then perform actual full CPU/MiniNDN
qualification. Keep old SIF/application and failure evidence until replacement
verification succeeds. Remove only the task-owned expanded sandbox afterwards
to recover space. Never use the diagnostic Python overlay for qualification.
