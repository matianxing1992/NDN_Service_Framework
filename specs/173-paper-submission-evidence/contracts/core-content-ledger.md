# Core Content Ledger

This ledger prevents page-pressure edits from silently removing the May 20
paper's scientific core. “Preserve” means retain the concept and supporting
evidence at appropriate precision; it does not require retaining an unsupported
historical number or identical prose.

| Core item | Required disposition | Evidence boundary |
|---|---|---|
| Data-centric service transaction and unified service naming | Preserve and foreground | Current protocol/API implementation and diagrams |
| Dynamic multi-Provider discovery through the ACK window | Preserve and foreground | Current transaction path; do not claim universal superiority |
| ACK-metadata-based Provider selection | Preserve | Correctness regression plus registered supporting performance cell |
| ABE-backed service-semantic authorization | Preserve and foreground | Current attribute routing, authorization regressions, and cited ABE/NDN context |
| One-time UserToken/ProviderToken replay protection | Preserve | Current code and negative security regressions |
| Producer-namespace response naming | Preserve | Current naming contract and verification/forwarding rationale |
| Admission control | Preserve as bounded overload mechanism | Registered enabled/disabled comparison; disclose throughput tradeoff |
| Loss recovery | Preserve | Recovered canonical 1%, 3%, and 10% summaries |
| Physical deployment feasibility | Preserve only as qualitative integration evidence | Exact old table removed unless raw artifacts are recovered |
| Mobility with four Providers | Preserve with existing bounded conclusion | Existing registered holdout; no new broad matrix in this Spec |
| Work-efficiency versus endpoint retry | Preserve | Existing Provider-execution evidence and explicit baseline assumptions |
| High-level streaming/retrieval APIs | Preserve concise API contribution | Current implementation/tests, not unmeasured performance claims |
| Limitations and scope | Preserve and sharpen | Explicitly distinguish correctness, contextual measurements, and central claims |

## Required checks before submission

- Every table and figure in the final PDF has a ledger disposition and artifact-index entry.
- A removed historical table does not leave unsupported prose elsewhere.
- A mechanism supported only by a regression is described as correctness, not population performance.
- The ten-page body still explains transaction flow, authorization, selection, implementation, evaluation method, results, related work, and limitations.
