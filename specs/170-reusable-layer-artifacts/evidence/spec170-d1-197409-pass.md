# Spec170 D1 exact-SIF network pass — 2026-08-17

The retry used workload release
`spec170-runtime-dd5c11cc-localfix-20260817-r2-controller-start-race` and
reused the immutable SIF bytes from r1:

```text
job:       197409
node:      itiger08
SIF SHA:   sha256:f6521a8226190279cb961f2e100245d783c8ba90723a4b004f1f773df71b5874
SIF bytes: 4398788608
exit:      0:0
```

The bundle preflight and isolated-PIB preflight both passed. The exact runtime
preflight for the same SIF had already passed as Job `197394` on `itiger08`
with `CUDAExecutionProvider`, Torch CUDA, and `cpuFallback=false`.

Runtime evidence from the D1 workload:

```text
Controller: ServiceController started...
Provider:  READY; activeRoles=4
Roles:     /Backbone, /Head/Shard/0, /Head/Shard/1, /Merge
Runner:    onnxruntime-cuda; realCompute=true; cpuFallbackUsed=false
Selection: candidates=1; roles=4; selected=4; all four roles -> same Provider
Response:  NDNSF_DI_NATIVE_FINAL_RESPONSE_DECISION local_full_plan=true
User:      status=executed; failureCount=0; mean successful latency=57.0799 ms
Terminal:  REQUEST -> ACK -> SELECTION -> RESPONSE PASS
```

The first r1 workload attempt (`197399`) is retained separately as a negative
orchestration result: it raced `ndnsec cert-dump` against Controller PIB
initialization and aborted with `database is locked`. The readiness barrier in
r2 removed that race without changing the SIF or source revision.

Durable raw logs are retained on Tiger at:

```text
/project/tma1/ndnsf-di/evidence/spec170/d1-current-197409/
```

