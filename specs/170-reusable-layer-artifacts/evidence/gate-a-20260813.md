# Spec 170 Gate A checkpoint (2026-08-13)

**Status**: local contract checkpoint PASS; not a pre-freeze or Tiger PASS.

This record uses the repaired candidate at `59f3b9375916acbcf64f3186cd3d36922661a8ea`.
The worktree contains unrelated user edits and generated build-copy edits; no
such edits are included in this candidate identity. The source/runtime hashes
used by the local run are:

```text
ndn-service-framework/ServiceProvider.cpp
b9c03c76190f35bcf2df08e1b8dc86ccb9a381ef7b218f4870df3e6753a6cac0

build/libndn-service-framework.so
088fbec4cc1339d7b0bca228eff918c14aaa12dc52a9c6bd000cd5a518ee30d4

pythonWrapper/ndnsf/_ndnsf.cpython-310-x86_64-linux-gnu.so
50ac1e9c933bee3fcbadfb6719aa66b19d803f87d88938251d2b289678b7e5e3

pythonWrapper/ndnsf/_ndnsf.cpython-38-x86_64-linux-gnu.so
43d7d367e3a9658639bdf74a152b4fd0728e4b3757fa3e61073f0a24d4217af9
```

## Executed checks

```bash
PYTHONPATH=pythonWrapper:NDNSF-DistributedRepo/pythonWrapper \
  pytest -q tests/python/test_spec170*.py tests/container/unit/test_spec170*.py
```

Result: **65 passed, 2 skipped, 1 warning in 4.71 s**. The skips are the
explicit real-model/optional-fixture gates; they are not GPU or Qwen evidence.

The eight non-fault C++ integration cases passed:

```bash
./build/integration-tests --run_test='Spec170NdnsfDiCoreFlow/AttemptPlanAndDependencyNamesRemainBound:Spec170NdnsfDiCoreFlow/AsyncDataflowRunsThreeStagePipelineAndRejectsMissingOutput:Spec170NdnsfDiCoreFlow/DependencyWaitCoversCompletionCancellationAndDeadline:Spec170NdnsfDiCoreFlow/ExecutionEvidenceRoundTripsAndRejectsSecrets:Spec170NdnsfDiCoreFlow/PreconfiguredEnvironmentSeparatesBootstrapFromRequests:Spec170NdnsfDiCoreFlow/PreconfiguredEnvironmentBootstrapsThreeProviders:Spec170NdnsfDiCoreFlow/PreconfiguredEnvironmentRunsGenericRequestLifecycle:Spec170NdnsfDiCoreFlow/PreconfiguredEnvironmentRunsThreeProviderCustomSelection' --log_level=message
```

Result: `*** No errors detected`. The separately known fault-injection counter
case is not silently folded into this PASS; its observed duplicate count remains
documented in the prior diagnostic evidence.

```bash
./build/unit-tests --log_level=message
```

Result: **471 test cases, `*** No errors detected`**. Optional ONNX/model tests
reported their explicit fixture skips.

## Candidate staging

The tracked source subset for this checkpoint was archived without unrelated
large release/UAV payloads:

```text
archive: /project/tma1/ndnsf-di/candidates/spec170-source-59f3b93/source.tar.gz
sha256: 49f68e9a2bc570389488dbba0023cc69bcdafb62482722935a2069d049b96f87
```

The archive was verified after transfer and extracted under the same candidate
directory. It is source staging only; it is not an OCI/SIF identity.

## Boundary

Spec170 T025/T026/T027/T028/T029 remain open. In particular, no exact SIF,
real-Qwen three-block Gate B campaign, or frozen-candidate manifest exists.
This document therefore does not authorize D0/D1/D2a/D2b/D2h.

