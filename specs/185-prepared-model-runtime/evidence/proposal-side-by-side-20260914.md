# Proposal Side-by-Side Comparison

## Scope

用户否定密集红蓝文字标记的可读性，要求左右对照。保持59页Origin和50页当前稿两份冻结PDF，按主题组织完整页面并排，不修改正文或实验。

## First Boundary

首轮 `r1/build.log` 在说明页标题 `insert_textbox` 高度校验处失败（剩余高度 `-3.7320017218589783`），未产生交付PDF。原始目录 `.codex-tmp/proposal-side-by-side-20260914/r1/` 保留；改用明确字体度量／基线排版并校验宽高，另开r2。属于排版工具边界，不是产品失败。

## Current Checkpoint

R2标题排版通过，但对比页14右侧（当前稿p11）的逐像素完全相等断言失败；文字保持相同，差异只在5×8像素范围，1,454,112个颜色通道值中57个不同，平均绝对误差0.001675/255。原始r2日志和PDF保留。嵌入采用源PDF矢量页面、单位缩放，重新定位后光栅化存在细小字形边缘差异；r3保留精确文字检查并增加明确光栅容差：不同通道比例≤0.001、平均通道误差≤0.03/255。报告每个非零差异，不声称所有像素精确相等。

R3在对比p30右侧的光栅容差处失败：不同通道比例0.07168，平均误差仅0.07269/255，所有变化通道差值≤8。对全部面板的只读诊断发现21个非零差异面板，最大平均误差0.07269/255，最大差值>8的通道比例0.00011760。此前“所有非零通道”门禁将广泛的一级灰度取整也视为显著变化。r4改为平均误差≤1/255且差值>8的通道比例≤0.001；每个差异仍记录，文本继续精确相等，单位缩放保持。r3候选与日志保留。

DOCUMENT_PASS：r4为79页，19主题，130个页面面板，原59／现50页全部覆盖。每个面板文本与源页精确相同、单位缩放；全部光栅差异低于记录的容差，原PDF哈希不变。79页渲染、索引与书签检查完成。输出替换 `main_comparison.pdf` 及其报告／说明；旧标记稿仍可从 `ab9ee6f7` 和旧raw目录恢复。没有正文、slides、实验或Spec185 native状态变更；下一步按首页主题索引审阅。
