# Spec 175 historical progress checkpoint (superseded 2026-08-27)

This checkpoint records an earlier implementation boundary. It is superseded by
`progress-20260825.md`, `progress-20260827.md`, and
`t022-g3-current-20260825n.md`; it is not a closure
report and does not promote a SIF or authorize TigerCluster. In particular, its
T022/G3 PASS statement predates the retained current-source M09 signal exit and
must not unlock a new SIF.

Task acceptance status in this historical snapshot reported T020 and T022 as
closed in addition to the previously
accepted implementation tasks. T021 has a corrected local build/preflight
checkpoint: the builder now enforces the sealed G3 manifest, but T021 remains
open until the final SIF contains the exact-SIF replay driver and fixture. T023
has a hash-bound candidate and remains open until the 30-case replay passes.
The later SIF/Tiger tasks remain open until their current-source gates pass.
Other tasks may have implementation or focused-test evidence, but remain open
until their stated current-source gate and repetition requirements pass.

## Promotion state

| Gate | Current state | Evidence | Interpretation |
|---|---|---|---|
| G0 contract/source seal | HISTORICAL PASS | `results/spec175/g0/qualification-manifest-final-20260825.json` | Zero blockers for that source subject; later source changes invalidate it. |
| G1 unit/Python regression | HISTORICAL PASS | `results/spec175/g1/qualification-manifest-final-20260825.json` | 131 passed for that source subject; not current evidence. |
| G2 CPU integration | HISTORICAL PASS | `results/spec175/g2/qualification-manifest-final-20260825.json` | 27/27 registered processes for that source subject; not current evidence. |
| G3 host/CPU MiniNDN | HISTORICAL 30/30 | `evidence/t022-g3-host-minindn-20260825.md` and `results/spec175/g3/host-minindn-manifest-final-20260825.json` | M01--M10 passed for that subject, but later M09 signal evidence reopened promotion. |
| G4 final SIF/replay | READY FOR REPLAY | `evidence/g4-local-sif-20260825.md`, `results/spec175/candidate-final-20260825b.json` | Corrected candidate SIF/preflight and host-gate binding PASS; exact 30-case SIF replay has not run, so the candidate is not promotable. |
| G5--G7 Tiger | NOT STARTED | — | Correctly blocked by the lower gates and the missing valid Qwen3.6-27B stateful ONNX subject. |

## Correct execution order

Spec 175 now uses this irreversible promotion order:

```text
G0 source/contract seal
  -> G1 unit and repository regression inventory
  -> G2 CPU native integration process matrix
  -> G3 real host MiniNDN tiny-ONNX M01--M10
  -> one final local SIF build and native preflight
  -> exact-SIF replay of the G3 manifest
  -> Tiger G5 readiness, G6 functional, and G7 performance
```

No SIF rebuild is a valid response to a G1--G3 defect. A host gate failure
returns to the owning source/build/test gate; the SIF is built only once after
the host manifest is `PASS`.

This is now a hard Spec175 execution rule: the SIF builder, SIF preflight/replay,
upload, Slurm allocation, and Tiger commands were deferred until T020/T022
closed. The corrected candidate was built from the frozen source/workload seal,
was bound to the sealed G3 manifest, and was preflighted locally, but remains
`READY_FOR_G4_REPLAY`; any later source change invalidates it and returns to
G0.

## Remaining blockers in dependency order

1. Embed the exact replay inputs/driver into the corrected T021 candidate and
   replay the same 30-case G3 manifest once. Do not rebuild unless the candidate
   source or definition changes.
2. Only after G4 passes, validate the Qwen3.6-27B stateful CUDA ONNX artifacts
   and submit bounded Tiger jobs.

The old `qualification-manifest-current-*` files are retained as historical
expected-negative development records. The sealed manifest above is the only
current G0 authority.
