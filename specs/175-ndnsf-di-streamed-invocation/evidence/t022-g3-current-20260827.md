# T022 current-source G3 host/CPU matrix (2026-08-27)

## Subject

The matrix was run from a fresh output root using the T020 source seal copied
to `evidence/t020-source-seal-20260827.json`, the checked-in tiny causal ONNX
fixture, fixed workload seed `1750001`, four separate Providers, the frozen
100 Mbit/s/10 ms MiniNDN topology, and `admissionControl=false`. Each case was
run in a fresh root MiniNDN process with a 300-second per-process bound.

## Result

All fourteen cases were run three times. Every process returned exit code 0 and
every case result had schema
`ndnsf-di-spec175-minindn-case-result-v1`, status `PASS`, four Providers, the
expected terminal classification, and a complete Provider timing record.

```text
cases=M01..M14
repetitionsPerCase=3
total=42
passed=42
failed=0
missingCases=0
sourceSeal=evidence/t020-source-seal-20260827.json
runRoot=/tmp/spec175-g3-run-w3Mh18
validator=results/spec175/g3/spec175-g3-current-20260827.json
durableManifestSha256=5cb968e5b17202277fbb5e725c1f1f9e700c691a66d78722591523a16d412b1d
status=PASS
```

The strict `scripts/spec175_g3_manifest.py` validator accepted all 42 explicit
directories. It checked case identity, fixed seed/campaign identity, runtime,
Provider count, disabled admission, expected terminal behavior, topology,
required user/runner logs, artifact hashes, and for M11--M14 the network
conversation evidence including zero state-tensor bytes on NDN and zero runner
calls after rejected validation.

The first M01 attempt in a reused directory failed before publication because a
stale `spec175-repo-registration.json` already existed. It is excluded from
the matrix; the fresh-root rerun passed and no stale-directory result was
accepted.

## Closure boundary

This closes T022 and the current host/CPU G3 gate. It does not claim Qwen3.6
CUDA execution, an exact SIF, G4 replay, Tiger G5/G6/G6C/G7, or the 20-token/s
performance target. T023 is the next task and must build exactly one final SIF
from this source-sealed subject before any Tiger allocation.
