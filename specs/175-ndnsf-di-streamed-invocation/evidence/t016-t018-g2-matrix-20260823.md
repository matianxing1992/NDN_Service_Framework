# Spec175 I04-I15 matrix refresh

> Historical matrix snapshot. The current complete manifest is
> `evidence/g2-live-i13-20260823.md`.

Command:

```text
python3 scripts/run_spec175_integration_gate.py \
  --binary build/integration-tests --cases I01-I15 \
  --healthy-repeats 3 --seed 1750001 \
  --output results/spec175/g2/qualification-manifest-v1.json
```

Result: **BLOCKED_MISSING_CASES**, with 24 registered processes executed.

- I01-I03, I04-I11, I13-I15: all registered executions passed; no failed
  process remained after the I08 cancellation assertion was corrected.
- I14 was repeated three times as a healthy unary control; it passed all three.
- Controlled I12 is now registered and passes its focused native replacement
  case; this older matrix predates that registration and is not the current
  G2 manifest.
- I13 is registered as the lower-bound post-prefix-loss/no-replacement oracle,
  not a live
  Provider-kill proof.

The corrected I08 expectation is three delivered events: the test cancels
synchronously from the third callback, so accepting a fourth event would
violate the terminal guard. The full manifest is the machine-readable result;
this note records only the interpretation boundary.

## Task impact

This run provides the required process evidence for the I04-I07 event
reorder/duplicate/loss/permanent-gap cases and for I08/I10/I11 callback,
cancellation, and capacity cases. T017 remains partial because the broader
token/replay/policy mutation matrix is not yet exhaustive; T018 remains partial
until the explicit deadline/publication-race inventory is recorded. T020/G2
remains blocked by the live-boundary I13 proof and the complete gate manifest
requirements.
