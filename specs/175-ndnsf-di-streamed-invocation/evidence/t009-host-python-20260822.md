# T009 host Python diagnostic checkpoint

**Date**: 2026-08-22  
**Status**: host Python 3.8 diagnostic closed; Python 3.10/SIF remains pending

## Boundary decision

The deployable SIF ABI is not assembled from a host-built extension.  The host
extension is only a diagnostic artifact used to catch API and linkage mistakes.
The Python 3.10 diagnostic build was attempted and stopped at the correct
boundary because this host does not provide `Python.h`.  No Python 3.10 object
was copied into the candidate runtime.

## Verified host diagnostic

The available Python 3.8 interpreter rebuilt `pythonWrapper/ndnsf/_ndnsf.so`
against the current `build/libndn-service-framework.so.0.1.0`, with the build
directory in its runtime path.  The following checks passed:

```text
PYTHONPATH=pythonWrapper:NDNSF-DistributedInference:
  NDNSF-DistributedRepo/pythonWrapper python3 -c 'import ndnsf'
  -> IMPORT_OK
non-Python ldd -r unresolved-symbol filter
  -> HOST_EXTENSION_NONPYTHON_ABI_CLOSED
test_spec175_onnx_deployment_boundary.py
  -> 5 passed
test_streamed_invocation_api.py
  -> 3 passed
```

The ordinary Python interpreter symbols reported by `ldd -r` are expected for
an extension loaded by CPython and are excluded from the non-Python ABI check.
The stale pre-existing host extension was not reused for this evidence.

## Remaining T009 acceptance

Build the Python 3.10 extension inside the sealed candidate SIF or an
ABI-identical builder, then repeat import, non-Python `ldd`, native streamed
callback, lifetime, exception-containment, and SIF checks.  This checkpoint does
not authorize SIF or Tiger promotion.
