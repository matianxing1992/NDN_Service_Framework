# Spec175 M12 real Provider-path checkpoint

Date: 2026-08-27  
Subject: current working tree, tiny causal ONNX fixture, four separate MiniNDN Providers  
Run directory: `/tmp/spec175-m12-real-7KRMpl`

## Result

The real M12 path passed with exit code 0. The User issued three independent
conversation pairs through the native User and four independent Provider
processes. Each Provider moved its committed state to the bounded host tier
after COMMIT, and the next APPEND_DELTA request caused Provider-local
single-flight prefetch back to the GPU tier (the CPU gate uses a bounded host
tier emulation; it does not claim CUDA execution).

```text
LLM_PIPELINE_SPEC175_CONVERSATION_PASS case=M12 realProviderPath=true networkRequests=6 freshRequests=6 freshGenerations=6 conversationHits=12 hostPrefetches=12 hostPauseTransitions=24
NDNSF_DI_SPEC175_CASE_PASS case=M12
```

The runner independently counted the Provider log markers before writing the
case result:

| Field | Value |
|---|---:|
| isolated conversations | 3 |
| fresh network Requests | 6 |
| fresh generation identities | 6 |
| Provider conversation entries | 24 |
| conversation-state hits | 12 |
| Provider HOST pause transitions | 24 |
| Provider HOST→GPU prefetches | 12 |
| request-local entries after cleanup | 0 |
| state tensor bytes on NDN | 0 |
| runner calls after rejected validation | 0 |

## Qualification boundary

This is a real host/CPU checkpoint, not the final G3 matrix. M13 still needs
independent Provider restart/expiry/mismatch controls and an authenticated
fallback-or-failure record. M14 still needs real concurrent same-parent
requests and cancellation during Provider prefetch. The source must be
resealed after those controls are complete before running the 42-case matrix.
