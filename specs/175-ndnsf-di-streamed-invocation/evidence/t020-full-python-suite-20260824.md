# T020 Python-suite closure checkpoint (2026-08-24)

The focused Spec175 suite was rerun after fixing the repository-local Python
wrapper import closure and the MiniNDN stage-layout guard:

```text
python3 -m pytest -q tests/python/test_spec175_*.py
82 passed, 1 skipped
```

The project Python suite (`tests/python`) was also run from the dirty working
tree with the same repository-local import closure:

```text
2002 passed, 47 failed, 18 skipped
```

The 47 failures are not being silently attributed to Spec175.  The inventory
is dominated by historical Spec127--Spec173 frozen-source/result assertions,
plus current-worktree baseline drift. They remain a diagnostic inventory and
are not part of the bounded Spec175 G1 subject. A separate
unscoped `pytest` collection also reaches an external gRPC negative test that
intentionally exits when gRPC is installed; that collection failure is not
counted as the project Python suite and is not used as G1 evidence. This
checkpoint proves the focused Spec175 Python contract; the executable bounded
G1 manifest is recorded separately in
`results/spec175/g1/qualification-manifest-v1.json`.

The native integration gate remains independently recorded at 67/67 for the
pre-Face-fix subject, and the G2 I01-I15 process manifest has 27/27 successful
process records. The complete native unit gate now passes 567/567 after two
minimal timing-test determinism fixes documented in
`evidence/t020-native-unit-20260824.md`. These counts do
not substitute for the remaining host-extension/hash and G3 clean-candidate
requirements.

## Historical G0 source-seal checkpoint (pre-Face-fix)

The gate now accepts an intentionally dirty development subject only through a
content-bound seal. The seal binds the current HEAD, exact in-scope status set,
and SHA-256 of every changed file; any edit, new path, deletion, or HEAD change
invalidates it. The pre-Face-fix sealed subject passed G0:

```text
source seal: results/spec175/g0/source-seal-current.json
qualification manifest: results/spec175/g0/qualification-manifest-sealed-20260824.json
status=PASS, source-subject dirtyFileCount=135
```

The two file hashes are intentionally generated by the final G0 command rather
than copied into this prose: the development seal includes the current source
subject and timestamp. Reviewers can reproduce the exact values with
`sha256sum` immediately after regenerating both files.

This is a reproducible historical source subject, not an exemption from hashing.
The later Provider Face-event-loop publication change invalidated this subject
for promotion. T020 still requires a newly generated seal to be used for the
rebuilt extension and native binaries before G0-G2 can be closed as one
promotion record.
