# Spec175 current Core/stream focused closure

Date: 2026-08-26  
Branch: `Experimental`  
Subject: current rebuilt native integration binary and current Python sources

## Verification

The complete native integration suite was run without a filter:

```text
build/integration-tests --log_level=error
  PASS; 70 test cases; no errors detected
```

The stream API/transport subset was then run independently:

```text
build/integration-tests --run_test='Spec175InvocationStream*' --log_level=error
  PASS; 16 test cases; no errors detected
```

The current I04--I13 tiny-ONNX fault/recovery subset was run in three fresh
processes:

```text
build/integration-tests \
  --run_test='Spec170NdnsfDiCoreFlow/Spec175NativeTinyOnnxI*' \
  --log_level=error
  REPEAT 1: PASS; 14 test cases
  REPEAT 2: PASS; 14 test cases
  REPEAT 3: PASS; 14 test cases
```

This includes reorder, duplicate suppression, one-loss retry, permanent-gap
failure, cancellation, deadline cleanup, tamper/unselected-provider
protection, callback failure, capacity-one backpressure, opt-in replacement,
and default-disabled no-replacement behavior. Each run reported no errors.

The current Python contract/stream/recovery/conversation subject was also run:

```text
PYTHONPATH=NDNSF-DistributedInference:pythonWrapper python3 -m pytest -q \
  tests/python/test_streamed_invocation_api.py \
  tests/python/test_spec175_evidence.py \
  tests/python/test_spec175_workload.py \
  tests/python/test_spec175_integration_gate.py \
  tests/python/test_spec175_conversation.py \
  tests/python/test_spec168_provider_generation.py
  69 passed
```

The existing full native unit gate also passes (`590` cases). These results
close the implementation/focused-test boundary for T005--T008 and T016--T018,
and provide three current-process repetitions for the T019 I12/I13 behavior.
They do not create a formal G0 source seal, close the expanded MiniNDN M01--M14
matrix, or qualify an SIF/Tiger candidate.
