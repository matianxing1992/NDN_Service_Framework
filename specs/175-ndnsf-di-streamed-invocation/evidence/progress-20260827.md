# Spec175 progress and audit follow-up — 2026-08-27

## Source subject

The current dirty worktree was sealed for development diagnostics as
`results/spec175/g0/source-seal-current-20260827.json`.

```text
sourceRevision=8350cad1c5a0b013c56f9f79450b3eb3686b4e21
dirtyFileCount=168
sourceSealSha256=sha256:1b9986238c8e3d0d6365ce3fc0ebcc07288be6b13b09d2ce28d8883e5d446822
```

This is a content-bound diagnostic subject, not a clean-tree promotion seal.
It predates the current streaming-timing source/docs edits and is therefore
historical evidence only. The contract gate without a seal correctly blocks
the dirty worktree; the same gate with this exact seal passed only the then-
sealed content and contract checks. A later section records the replacement
T020 closure after the implementation queue closed.

## Review and fixes completed in this follow-up

The implementation status was reconciled with the named task gates. T013 is
now closed at the implementation boundary: the shared state identity,
request-local transaction, exact state-input/output mapping, causal-position
materializer, persistent ORT binding, and cleanup paths are covered by the
current source and local mutation/regression suites. Exact Qwen3.6-27B graph
identity, CUDA residency, and zero host round trips remain qualification work
owned by T025. The accepted-task count is therefore 21/34 at this historical
checkpoint; T014--T015, T020, T022--T023, T025--T028, T030--T031, T033, and
T034 remain open in that earlier snapshot.

The implementation queue and audit/traceability headers were updated to use
this same count. No historical MiniNDN, SIF, or Tiger result was promoted.

`llm_pipeline/provider.py::_qwen_generation_spec()` accidentally returned its
validated generation dictionary only inside the conversation branch. A normal
DI envelope with `TOKEN_STREAMING` and `useCache=true` consequently returned
`None`, breaking the existing Python provider contract. The return path is now
outside that branch; malformed conversation metadata and the
`useCache=false` rejection remain fail-closed.

The feature-status text was corrected at the same time: it now reports the
current focused Spec175/Provider-generation Python regression (203 passed) and
the
dedicated conversation regression (36 passed), instead of the stale 160/24-case
checkpoint. This correction does not close T014/T015, T030--T033, or any
formal MiniNDN/SIF/Tiger acceptance gate.

## Verified local gates

All results below use the current rebuilt native library and current Python
source. They are local contract/regression evidence only.

```text
Focused Spec175 + Spec168 provider-generation regression: 195 passed. The
broader Spec175/168/170 regression passes 349 tests with 10 expected skips and
one warning; it is regression evidence, not a Spec175 qualification gate.
Python Spec175 conversation regression: 33 passed
Historical Spec175 Python gate: 197 passed, 0 failed, 0 skipped (19 registered
targets); manifest preserved at
`results/spec175/g1/python-gate-current-20260827-final-r3.json`
Historical Spec175 native integration gate: 20/20 registered cases passed
Three-repeat historical native gate: 38 results (the 9 registered healthy
cases repeated three times, remaining cases once), 0 failures; manifest
preserved at
`results/spec175/g2/integration-gate-current-20260827-final-r5.json`
Native unit-tests: 600 cases, no errors detected
Streaming-handle timing regression: 12 cases, including real monotonic
TTFT/ITL/terminal callback timing and metadata-only summary checks
Spec175 launcher stale/active socket safety: 35 passed
Spec175 invocation-stream native unit subset: 29 cases, no errors detected
Spec175 contract gate with this source seal: PASS (0 blockers)
```

`git diff --check` and Python byte-compilation passed. The native extension was
rebuilt against the current Boost 1.71 closure; its import and `ldd` checks
passed before these gates.

## Current real Provider checkpoints (same implementation subject)

The subsequent real host/CPU MiniNDN runs extend, but do not replace, the
diagnostic source seal above. M11 passed with two fresh network Requests and
four Provider roles participating in both turns. M12 passed with three
isolated conversations: six fresh Requests, twelve fresh conversation-state
hits, 24 Provider-controlled HOST pause transitions, and twelve HOST-to-GPU
prefetches. M13 passed with four Provider restarts, a malformed checkpoint
rejected before publication, a valid post-restart continuation rejected
closed, and one explicit full-prefill fallback. M14 passed with three fresh
child Requests, four HOST pauses, four HOST prefetches, one Provider-side
prefetch cancellation, and a sibling that completed through the shared flight.
All four cases recorded zero state-tensor bytes on NDN edges and zero
request-local entries after cleanup. M14's cancellation is an explicit
Provider-side test fault because the current User `cancel()` is local; no
remote cancellation Interest is claimed. These are current implementation
checkpoints; they do not close the repeated 42-case G3 matrix or any SIF,
CUDA, Tiger, or performance gate.

## Remaining blockers (not silently waived)

At the earlier checkpoint before the current T020/T022 closure updates, the
local implementation and focused gates did not close the following
acceptance requirements:

1. T025 still requires the exact Qwen3.6-27B canonical ONNX graph on CUDA,
   persistent device-resident state, and zero complete-state host round trips.
2. T014/T015 still require the production Python automatic multi-role workload
   and measured stream spans rather than only contract/fixture coverage.
3. T030/T031/T033 still require repeated formal acceptance of the real
   independent Provider processes that publish requester-encrypted
   Ready/receipt records, receive COMMIT/ROLLBACK, and reuse conversation state
   across fresh requests. M11–M14 now each have a real host/CPU checkpoint;
   M13 uses restart/fallback controls and M14 uses the explicit Provider-side
   cancellation fault plus shared-flight waiter protection. The test-only
   User-process emulation remains forbidden for formal M11–M14, and these
   checkpoints do not replace the required source-sealed repetitions.
4. At that earlier checkpoint T022/T023 remained open. The current T022
   closure now supplies the required 42-process MiniNDN matrix; only the new
   source-bound exact SIF remains. The current shell cannot start MiniNet
   because it is not root; stale root-owned `/run/nfd` paths are therefore
   reported as deferred rather than deleted or treated as a passing run.
5. T025–T027/T034 (current-SIF control, Tiger CUDA multi-Provider, performance,
   and conversation-residency) remain blocked behind those gates.

Therefore Spec175 remains **implementation-progress / promotion BLOCKED**,
despite the local source and regression suites passing.

## Follow-up after seeded M11--M14 reruns (2026-08-27)

The real four-Provider M11--M14 entry points were rerun once each with
`seed=1750001` after the case-result seed field was wired into the inner
runner. All four completed with structured `status=PASS`, and their bounded
metrics and result/log hashes are recorded in
`evidence/current-conversation-reruns-20260827.md`. The current-source G2
runner also executed all 20 registered I01--I20 cases once with zero failures;
its diagnostic seal and manifest hashes are in
`evidence/current-g2-one-repeat-20260827.md`.

At that checkpoint, this closed the T033 harness/case implementation boundary
and updated the then-current accepted count to 22/34. It does not replace the required clean-source
repeated T020/T022 qualification, exact-SIF replay, CUDA, Tiger, or G7 gates.
The final timing-parser test subject was then rerun once more: source-seal
SHA-256 `sha256:60bb8b3bdd81ac2fe0f9e28fdb487510cf91f0cd13025105c2a5f4799399c08c`,
G2 manifest SHA-256
`sha256:817a65855b641a2c06166a58baf01aefe842b18429eec7824217cf2779ea2a01`,
20/20 cases PASS. See
`evidence/current-g2-one-repeat-final3-20260827.md`.

## Timing/report and current G2 rerun (2026-08-27)

The Spec175 launcher now binds strict metadata-only Provider handler spans to
each case result. A current M11 run produced eight matched spans across all
four roles; malformed, unmatched, negative, or missing-role timing evidence is
rejected before result publication. Earlier source-seal and G2 hashes in the
preceding diagnostic records are historical intermediate checkpoints and are
not current authority (`current-g2-one-repeat-post-timing-20260827.md` and
`current-g2-one-repeat-final-20260827.md`).

The sole current one-repeat checkpoint uses source-seal SHA-256
`sha256:de92e6714a53012ffde6c9c337af6a2a78c8db7248c1009954e75961af1e51ad`,
G2 manifest SHA-256
`sha256:bac03d367b9e09878865c190763a7d219a33b54843f08e7e4598fe0f6d8215c7`,
and 20/20 registered I-cases PASS. It remains diagnostic evidence until the
implementation queue closes and repeated qualification is rerun. See
`evidence/t015-provider-timing-20260827.md` and
`evidence/current-g2-one-repeat-integrity-20260827.md`.

## Streamed-mode correction and current seal (2026-08-27)

The real four-Provider M01 process was rerun after correcting the tiny
application context and terminal response from `FULL` to `TOKEN_STREAMING`.
The run delivered eight ordered tokens (`4,5,6,7,8,9,10,2`), one terminal
response, four matched Provider spans, and exit code 0. Its result/log hashes
are recorded in `evidence/m01-real-stream-mode-20260827.md`.

The latest source-bound diagnostic seal is
`sha256:6749e4a67505661a962e94c3d1ad353cd3a4cece85bd02208a97b884dd998d62`;
the contract manifest is
`sha256:d896a7cb37a7ec5166f54ef8e9f84094e87758e34fa14bd677b9f16ef640ccc3`;
and the current one-repeat I01--I20 G2 manifest is
`sha256:f12f29168da7af7c45f281085d372ec1bf05a1aaf71d95935fa98c1bce269a64`.
The contract gate and all 20 G2 cases pass for this dirty development subject.
At that historical checkpoint the accepted task count remained 22/34. The
later implementation-closure audit supersedes that count; repeated G2/G3,
exact SIF, CUDA, Tiger, and performance gates remain open.

## Documentation-synchronized current diagnostic seal (2026-08-27)

After correcting the stale focused-regression counts and documenting the
streamed `TOKEN_STREAMING` response contract, the current dirty source was
sealed again. The contract gate passed, and a one-repeat diagnostic G2 run
covered all 20 registered I01--I20 cases with no missing case or failure.

```text
sourceSealSha256=sha256:1b72098ed037197e2fc08e0c8520887a0855c8cd7b614660939b598e5ba91767
contractManifestSha256=sha256:9786bad2b168fd37621bc08230dd7f60ce41b8c35edc7db9574c21355056a220
g2ManifestSha256=sha256:fc8a6b2789d8d3f2299310e161d528b1e6b6c656f1bdf33b36ae091f8f9e9c4b
registeredCases=I01-I20
healthyRepeats=1
seed=1750001
resultCount=20
missingCases=0
failures=0
status=PASS
```

This remains a dirty-tree, tiny-CPU development checkpoint. It does not close
the required repeated G2/G3 qualification, exact SIF replay, Qwen3.6-27B
CUDA run, Tiger control, or conversation-residency gate.

## Implementation queue closure and Request-ID correction (2026-08-27)

A later audit found that T014/T015/T030/T031 and T020/T022 had circular
acceptance dependencies. Implementation/focused real-process evidence is now
owned by the former tasks; final same-source repetitions are owned only by
T020/T022. The accepted count is therefore corrected to **26/34**, with eight
qualification/promotion tasks still open.

The audit also found that the tiny-ONNX branch did not forward the campaign
Request ID into the actual streaming call. The branch now forwards and verifies
that ID. A focused 112-test suite passes, a post-fix real M01 uses
`/spec175-M01-1750001` end to end with eight events and four closed Provider
spans, and a current real M11 completes two fresh Requests through four
Providers with four conversation hits, delta prefill, epochs 1 then 2, no
fallback, no state bytes on NDN, and no leaked request-local entries.

See `implementation-closure-request-id-20260827.md`. Earlier M01--M07 results
that used the campaign ID only as a label remain diagnostic. T020 then passed
with G0 zero blockers, G1 218 passed/zero failed/skipped, and G2 38/38 results
across I01--I20. The accepted count was then **27/34**. At that checkpoint no
G3--G7, SIF, CUDA, or Tiger gate was closed; the following section records the
subsequent T022 result.

## T022 G3 closure (2026-08-27)

T022 completed from a fresh root. M01--M14 each ran three times (42 fresh
processes total) with the T020 source seal, fixed seed `1750001`, four
Providers, and disabled admission control. All 42 returned `0` with case
status `PASS`, and `scripts/spec175_g3_manifest.py` accepted the complete
matrix. The durable summary is `evidence/t022-g3-current-20260827.md`; the
machine manifest is `results/spec175/g3/spec175-g3-current-20260827.json`
(SHA-256 `5cb968e5b17202277fbb5e725c1f1f9e700c691a66d78722591523a16d412b1d`).

The first reused-directory M01 attempt was rejected because a stale repository
registration file already existed; it is excluded. T022 is closed and T023 is
now the next task: one final SIF and exact-SIF G4 replay.
