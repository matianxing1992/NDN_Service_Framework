# T020 native unit-gate checkpoint (2026-08-24)

The complete native unit binary initially exposed two timing-sensitive tests
under a busy full-suite process. The owning tests were made deterministic with
small observation margins only; no production code or protocol behavior was
changed:

* the Stream lookahead test now observes the injected Nack retry after three
  pump rounds instead of sampling the 100--166 ms PIT-expiry boundary;
* the UAV GStreamer wall-clock test now allows a documented 5 ms scheduler/
  encoder jitter margin around the frame-period bound.

The complete binary was then rerun:

```text
build/unit-tests --log_level=message
Running 567 test cases...
*** No errors detected
return code 0
```

The bounded Spec175/shared-contract Python subject remains independent:

```text
scripts/run_spec175_python_gate.py: 112 passed, 0 failed, 0 skipped
```

The clean integration result and G2 process matrix are also independent. This
closes the native unit portion of T020; host extension/framework hashes and the
real host/CPU MiniNDN gate remain open. No SIF build or Tiger submission is
authorized from this checkpoint.
