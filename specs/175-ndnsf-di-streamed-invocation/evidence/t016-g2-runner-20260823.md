# G2 runner checkpoint and evidence-scope correction (2026-08-23)

> Historical checkpoint captured before native I01 was registered. The current
> I01 evidence is `evidence/t014-native-i01-20260823.md`; the statements below
> intentionally preserve the earlier fail-closed snapshot.

`scripts/run_spec175_integration_gate.py` implements the formal G2 interface
(`--binary`, `--cases`, `--healthy-repeats`, `--seed`, `--output`). Missing
I-cases block rather than skip, and healthy I01-I03/I14/I15 repeat three times
while fault I04-I13 run once.

## Corrected evidence boundary

The earlier checkpoint registered generic one-Provider Core stream tests as
I04-I11. That was not valid: `contracts/validation-contract.md` defines I04-I11
as 2-role tiny-ONNX NDNSF-DI generation cases with exact token, lineage, and
fault oracles. Matching only the fault shape is insufficient.

The formal registry is therefore intentionally empty until T014/T016-T019 add
conforming native tests. The current result is:

```text
I01-I15, registered repetition policy: BLOCKED_MISSING_CASES
missing: I01,I02,I03,I04,I05,I06,I07,I08,I09,I10,I11,I12,I13,I14,I15
```

This is a correction of evidence classification, not a regression in Core
transport. The following generic one-Provider prerequisite tests still pass in
the full native integration binary:

- ordered events plus End/Response;
- one suppressed publication recovered from Provider IMS;
- reorder and duplicate suppression;
- permanent unretained gap failure;
- tamper rejection before callback;
- callback-exception containment;
- cancellation fencing;
- capacity-one slow-consumer/concurrent-cancel race;
- Targeted bootstrap and Selection.

These tests should be reused inside the real multi-role tiny-ONNX fixture, not
relabeled. Formal G2 remains open until every I01-I15 mapping names a conforming
native test and the runner emits its complete process/evidence manifest.
