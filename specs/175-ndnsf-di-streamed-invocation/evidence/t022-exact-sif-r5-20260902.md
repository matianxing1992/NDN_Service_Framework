# T022 exact-SIF smoke — candidate r5

**Status:** PASS  
**Date:** 2026-09-02  
**Purpose:** minimal local release gate before the single TigerCluster deployment.

The host-orchestrated exact-SIF replay completed one registered M01 workload
with the same candidate-bound host manifest and source seal. It is a release
gate, not a matrix or performance experiment.

| Item | Evidence |
|---|---|
| Replay manifest | `results/spec175/g0/local-release-r5/exact-sif-smoke` (`status=PASS`, one completed/one passed entry) |
| Case result | `results/spec175/g0/local-release-r5/M01-r1/spec175-case-result.json` (`status=PASS`, terminal evidence and cleanup present) |
| Host gate | `results/spec175/g0/local-release-r5/host-m01.json` (`status=PASS`, four Providers, `tiny-onnx`) |
| SIF | `.local-tmp/spec175-final-candidate-r5/spec175-runtime.sif` (`sha256:220386ade81eebdfed3efd4831f8b22ab3774d4039f19796b71631c1e4a00068`) |
| SIF runtime | Apptainer 1.5.3; `org.ndnsf.di.build-boundary=container-runtime-in-sif`; `org.ndnsf.di.backend=onnxruntime-cuda` |
| Source seal | semantic seal `sha256:7c0bb2366f0ea2b790a3ff963e25664ed9ec254c2d41ad11c1879843429fb238` in the SIF label |
| Child/process evidence | all required readiness records present; terminal cleanup recorded; no surviving owned processes |

The exact-SIF smoke closes the local packaging/runtime check for r5. The next
action is T025: submit this unchanged candidate once through the checked-in
Tiger profile. Do not add seeds, matrix rows, timeout variants, or another
local replay unless a behavior-affecting repair invalidates r5.
