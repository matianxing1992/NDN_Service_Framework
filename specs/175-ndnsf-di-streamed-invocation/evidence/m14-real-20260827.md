# Spec175 M14 real Provider checkpoint — 2026-08-27

This is a host/CPU MiniNDN implementation checkpoint for the current dirty
worktree. It is not a G3 promotion seal and does not qualify CUDA, SIF, Tiger,
or the Qwen3.6-27B workload.

## Command and subject

```text
SPEC175_RUN_REAL_MININDN=1
case=M14 seed=1750001
tiny-fixture-root=tests/fixtures/spec175/tiny-causal-lm-v1
topology=Experiments/Topology/spec175-host-gate.conf
run=/tmp/spec175-m14-real-r7-3xmTDK
```

The run used one controller, repository, user, four independent Provider
processes, real NFD/SVS/ABE, the tiny ONNX CPU fixture, and disabled admission.
M14 also enabled the explicit Provider-side test control that aborts the
designated child during HOST-to-GPU prefetch. Core user cancellation remains a
local lifecycle operation; the control therefore avoids claiming an
unimplemented remote cancellation Interest.

## Result

The user log emitted:

```text
LLM_PIPELINE_SPEC175_EXPECTED_TERMINAL case=M14 terminal=Cancelled events=0 cause=Cancelled
LLM_PIPELINE_TINY_ONNX_STREAM requestId=/spec175-m14-winner events=7 tokens=5,6,7,8,9,10,2 retries=11 duplicates=0
LLM_PIPELINE_SPEC175_CONVERSATION_PASS case=M14 realProviderPath=true networkRequests=3 freshRequests=3 cancelledPrefetches=1 successorCheckpointEpoch=2
NDNSF_DI_SPEC175_CASE_PASS case=M14
```

The sealed case result reported:

```text
status=PASS
realProviderPath=true
networkRequests=3
freshRequestIds=3
hostPrefetches=4
hostPauseTransitions=4
cancelledPrefetches=1
conversationHits=4
runnerCallsAfterRejectedValidation=0
requestLocalEntriesAfterCleanup=0
stateTensorBytesOnNdn=0
successorCheckpointEpoch=2
```

Each Provider emitted one
`LLM_PIPELINE_SPEC175_M14_PREFETCH_CANCELLED` marker. The shared-flight
regression confirms that cancelling one waiter does not revoke a concurrent
waiter's HOST-to-GPU transfer; the winner alone commits the successor.
