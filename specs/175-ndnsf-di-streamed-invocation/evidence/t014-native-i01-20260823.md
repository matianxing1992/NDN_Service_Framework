# Spec175 native I01 evidence

**Date**: 2026-08-23  
**Case**: `I01` — one Provider, one role, tiny stateful ONNX CPU baseline  
**Status**: `PASS` for the registered I01 case only

## Subject and boundary

The case enters through the production C++ `BeginCollaboration` Request/ACK
path, closes an ACK-driven plan, publishes encrypted Selection, invokes the
checked-in tiny ONNX graph through `NativeProviderHandler` and
`OnnxRuntimeModelRunner`, and observes streamed Event, End, and terminal
Response callbacks. The ACK capability payload supplies the role/provider
selection and assignment bytes; the caller does not pass a precomputed
role-to-Provider map to the selection policy. The request ID is the canonical
single-component V2 ID and is retained through the collaboration.

This file qualifies I01 only. I02/I03/I15 are qualified by separate native
evidence; I04-I14, G1, and every SIF/MiniNDN/Tiger gate remain open.

## Reproduction

Binary hash at the recorded run:

```text
104950ebe94cddb087b7e68fee1bfea9bf50d1c20f0bbd075c62c45e122f3eb9  build/integration-tests
```

Focused direct test:

```bash
./build/integration-tests \
  --run_test=Spec170NdnsfDiCoreFlow/Spec175NativeTinyOnnxI01OneProvider \
  --log_level=message
```

Formal healthy-process gate:

```bash
python3 scripts/run_spec175_integration_gate.py \
  --binary build/integration-tests \
  --cases I01-I03,I15 \
  --healthy-repeats 3 \
  --seed 1750001 \
  --output results/spec175/g2/qualification-manifest-v1.json
```

The gate returned `PASS` for the registered healthy subset, with three fresh
processes per case, fixed seed `1750001`, zero missing requested cases, and
exit code zero for every repetition. The machine-readable manifest is
`results/spec175/g2/qualification-manifest-v1.json`. This subset result is not
the complete I01-I15 G2 verdict.

## Observed oracle

Each repetition reported:

```text
ackCandidates=1
ackClosed=1
planCommitted=1
requestObserved=1
ackPublicationObserved=1
streamedContext=1
completed=1
failed=0
timedOut=0
events=8
```

The token oracle is eight ordered events with first token `4` and EOS token
`2`, followed by one accepted End and one final Response. The full final
binaries pass 567/567 unit and 57/57 integration tests; the Spec175 Python
aggregate passes 86/1 skip. This case-specific record plus the subset manifest is
the evidence for formal I01 registration.
