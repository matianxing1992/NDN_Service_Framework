# Spec175 G4 candidate: local SIF build and preflight

> **Historical superseded record.** This file describes candidate `b`; it is
> retained for audit history and must not be used for promotion. Candidate `l`
> in `t021-local-sif-current-20260825l.md` is also invalidated; current status
> is recorded by `progress-20260825.md` and
> `t022-g3-current-20260825n.md`.

Date: 2026-08-25  
Candidate: `spec175-final-candidate-20260825b`  
Decision: **READY FOR EXACT-SIF REPLAY; NOT PROMOTED**

## Passed checks

- Local `/opt/apptainer/1.5.3/bin/apptainer` matched the compute declaration:
  1.5.3.
- The definition passed the Spec170 container-native build-boundary validator.
- NAC-ABE, NDN-SVS, NDNSF, the native Provider/controller targets, and the
  Python extension were built inside the SIF builder stage; no host binary
  input was used.
- The final SIF contains exactly one CPython-3.10 `_ndnsf` extension. Import,
  `ldd`, and the native Provider closure passed inside the exact SIF.
- The exact runtime preflight passed Qwen3.6-27B workload identity, ONNX
  Runtime 1.20.0, `CUDAExecutionProvider`, and absence of deployed PyTorch,
  Transformers, and functorch.
- Lower gates G0, G1, G2, and G3 are bound to the candidate manifest and are
  all PASS. G3 is the current-source host/CPU tiny-ONNX M01--M10 matrix:
  30/30 independent processes.
- The build record binds the sealed G3 manifest (`total=30`, `passed=30`) and
  rejects a build when that manifest is absent, incomplete, or mismatched.

## Deliberately not claimed

This build and preflight do **not** prove G4. The SIF has not yet replayed the
same 30-case M01--M10 MiniNDN manifest. They also do not prove stateful
Qwen3.6-27B CUDA stage readiness or the Tiger multi-Provider result. The
candidate must remain local and immutable until exact-SIF replay passes.

The exact-SIF command probe also reports `minindn=absent`, `ndn=absent`, and
`pytest=absent` inside the image. Therefore the existing host pytest launcher
cannot be relabeled as G4: the image needs a sealed replay driver and its
fixture/source inputs before the 30-case replay can be executed.

Bound manifest: `results/spec175/candidate-final-20260825b.json`.
