# T021 r30 local SIF candidate evidence

Status: **executed / candidate-preflight PASS; not promoted**

This is a diagnostic candidate produced while the project is still waiting for
the host/CPU MiniNDN G3 gate. It is not G4 or Tiger evidence.

## Inputs and identity

- Apptainer: `/usr/local/bin/apptainer` 1.3.4
- Definition: `<local-scratch-root>/spec175-deployment-inputs/spec175-runtime-r30.def`
- Definition SHA-256: recorded in
  `<local-scratch-root>/spec175-deployment-inputs/runtime-r30-build-record.json`
- Source seal: `<local-scratch-root>/spec175-deployment-inputs/source-current-v2/source-seal.json`
- Source seal file SHA-256:
  `sha256:060c5b316cec7c0be3fb0c8a781c6936437b4085b0ad3ef4ab9f1ddd8aa55b87`
- Embedded source-seal semantic digest:
  `sha256:db303600d7523c109b255879b9ed4b8967a642a99d49b50d82d5f9daceca980b`
- SIF: `<local-scratch-root>/spec175-deployment-inputs/runtime-r30.sif`
- SIF SHA-256:
  `sha256:9b5c08368ff56a65caf10358b6abc5b5df6d26e8fe7b8c942243c322c98b6baf`
- SIF bytes: `3661418496`
- Build record digest:
  `sha256:7cddd02da3a9e8f584fe2f37bf29bbca4a0331b0418302f139a6e804162d3d86`

## Checks

The local build record reports `status=PASS` and `spec175Preflight.status=PASS`.
The in-SIF probe passed CPython 3.10/SOABI, `import ndnsf._ndnsf`, ONNX Runtime
CUDA/CPU provider inventory, native Provider and extension `ldd` closure, and
absence of deployed `torch`, `transformers`, and `functorch` modules.

The SIF labels were independently inspected. In particular,
`org.ndnsf.di.source-seal` equals the embedded semantic seal digest above;
this rejects the stale-label error found in r29.

The exact bundle-cwd wrapper was run with the SIF digest above and the sealed
`source-current-v2` bundle. It captured 762 bytes of Provider help and verified
the expected nonzero `--help` status plus `--plan` and `--execution-policy`
markers. No Tiger allocation or MiniNDN qualification was performed.

## Decision

r30 is a valid diagnostic local candidate, but remains ineligible for G4/Tiger
until the host/CPU MiniNDN M01-M10 gate passes. The source-seal label check is
now an explicit acceptance item for the eventual final candidate.
