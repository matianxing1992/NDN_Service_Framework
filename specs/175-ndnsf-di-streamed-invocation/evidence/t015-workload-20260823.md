# Spec175 frozen workload evidence

**Date**: 2026-08-23  
**Status**: `PASS` for workload construction/validation; T015 remains open for
production execution evidence

## Frozen identity

The checked-in workload is
`packaging/ndnsf-di-container/jobs/spec175/workload.json`. It contains exactly
P1 and P2 from `contracts/validation-contract.md`, the pinned
`Qwen/Qwen3.6-27B@6a9e13bd6fc8f0983b9b99948120bc37f49c13e9` identity, text-only
mode, MTP disabled, `thinkingMode=disabled`, greedy sampling, 64 maximum
generated tokens, 65 maximum events, and a 120000 ms deadline.

The tokenizer inputs were fetched from the pinned public model revision and
validated with the standalone `tokenizers` library plus the pinned Jinja
template. The sealed file digests are:

| artifact | SHA-256 | bytes |
|---|---|---:|
| `tokenizer.json` | `5f9e4d4901a92b997e463c1f46055088b6cca5ca61a6522d1b9f64c4bb81cb42` | 12,807,982 |
| `tokenizer_config.json` | `5186f0defcd7f232382c7f0aebcd2252d073bb921ab240e407b7ae8745d2b29b` | 16,718 |
| `chat_template.jinja` | `e84f32a23fdda27689f868aa4a1a5621f41133e51a48d7f3efcbea2839574259` | 7,764 |

The combined tokenizer artifact digest is
`c285703a48b5f7b43cc2ead73cce26fdb1799424358dd7f4fee4552d70741e3d`.
The derived prompt input lengths are P1=35 and P2=37 tokens. The complete IDs,
prompt hashes, rendered-template hashes, and canonical UTF-8 message bytes are
stored in the workload file; operational stream evidence must retain only the
prompt IDs, hashes, and lengths.

## Validation

The following command passed against the exact pinned tokenizer directory:

```bash
python3 scripts/build_spec175_workload.py \
  --tokenizer-dir "$HF_SNAPSHOT" \
  --check packaging/ndnsf-di-container/jobs/spec175/workload.json
```

The self-contained mutation suite passed 2/2 cases for deterministic rebuild,
state/generation mutation rejection, and checked-in P1/P2 identity. The source
seal test passed 5/5 and confirms that both the workload and validator are
included in the local SIF source archive. No model weights were copied into the
repository or workload artifact.

## Remaining T015 boundary

This proves the immutable inputs, not model output. TTFT/ITL spans, exact output
oracles, and a fresh process from the Python model/task-first User through
automatic V3 planning into native Providers are still required before T015 or
G2 can be closed.
