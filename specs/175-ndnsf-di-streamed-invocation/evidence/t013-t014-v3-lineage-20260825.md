# T013/T014 authenticated V3 lineage checkpoint

**Date**: 2026-08-25  
**Status**: historical focused CPU implementation checkpoint; T014 remained
open at the time. T013's implementation gate is closed by the 2026-08-27
source review; exact Qwen3.6/CUDA qualification remains T025.

## Subject

- Base HEAD: `8350cad1c5a0b013c56f9f79450b3eb3686b4e21`
- Dirty current worktree; this record is not a G0 source seal or G2 promotion
  manifest.
- Integration binary SHA-256:
  `10f18e4735d5f7a4fa996d0f1e98e2cf6aa4a804d9b64ef015c6f53f56d7c242`

## Implemented boundary

- `NativeProviderHandler` derives the complete static decode-state identity
  from authenticated V3 assembly/projection, artifact/runner identity,
  Provider/boot identity, and runtime ABI instead of accepting runner metadata
  as independent authority.
- `GenerationEpochLineageV1` carries a bounded canonical logical token-prefix
  digest/count and position commitment in authenticated activation and
  TOKEN_FEEDBACK plaintext.
- State and predecessor inference epochs are explicit and contiguous;
  `cacheEpoch` remains the cache-incarnation identity for one lineage.
- Provider-owned state lookup/commit/rollback/cleanup remains local. Decode
  state tensors are excluded from NDN dependency publication.
- The I09 fault is injected on the Provider-to-User bridge. The authentic IMS
  packet is retained for the exact Interest, its normal SVS duplicate is
  suppressed, and the bridge mutates the one delivered cursor-1 packet. This
  prevents an untampered copy from racing into the callback and tests the
  intended on-link corruption boundary.

## Verification

```text
./waf build --targets=unit-tests -j2
PASS

./build/unit-tests --run_test='<nine state/lineage cases>'
9/9 PASS

./waf build --targets=integration-tests -j2
PASS

./build/integration-tests \
  --run_test='*/Spec175NativeTinyOnnxI*' \
  --log_level=test_suite
14/14 PASS; no errors detected
```

The integration set covers I01-I13 and I15: one/two/four Provider healthy
oracles, permuted role placement, reorder, duplicate, recoverable/permanent
loss, cancel, tamper, callback failure, bounded slow consumer, opt-in
replacement, and default no-replacement failure.

I09 evidence: one tampered bridge packet, zero delivered events, cursor-1
`DecryptionFailed`, two Provider coordinator completions, zero Provider
failures, and zero unselected-Provider executions.

## Remaining acceptance work

This checkpoint does not close T013 or T014. Required work still includes the
full identity-field and lifecycle mutation matrix, concurrent generations and
attempts, Provider restart/capacity/deadline/publication-failure cases, the
real Python model/task-first workload reaching native Providers, a fresh
source-bound G0/G1/G2 rerun, and CUDA-resident Qwen3.6-27B state continuity.
