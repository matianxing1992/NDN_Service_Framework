# G2 live Provider-transport boundary rerun

The integration fixture now has an explicit test-only operation that detaches a
selected Provider's user-facing Interest/Data bridges. I13 invokes it from the
final Provider's publication interceptor exactly when event cursor 4 is about
to cross the boundary. The Provider object is not destroyed; this is a live
transport-unavailability boundary, not a claim about sending `SIGKILL` to an
external process.

The complete registered gate was rerun:

```text
python3 scripts/run_spec175_integration_gate.py \
  --binary build/integration-tests \
  --cases I01-I15 \
  --healthy-repeats 3 \
  --seed 1750001 \
  --output results/spec175/g2/qualification-manifest-live-i13-20260823.json
```

Result: `PASS`, `missingCases=[]`, `resultCount=27`. Healthy I01, I02, I03,
I12, I14, and I15 each passed three fresh processes; fault/control I04-I11 and
I13 passed once.

The I13 focused result after the live detachment was:

```text
requests=1 completed=0 failed=1 events=3 transportDetachments=1
error=...failed to fetch NDNSF_DATA_V1 SVS segments.../4
```

The full suite still passes 67/67 integration cases. I12 remains a controlled
opt-in replacement proof with two Requests, a fresh spare Provider execution,
and one terminal Response. I13 remains default-disabled: the detached
Provider produces no second Request, no replacement, and no successful
Response.

Hashes:

```text
build/unit-tests:        sha256:13a37b4ab5c7dfecbe05f2d6c0359319130bfa4ededf8a4ca1cb49e095a27493
build/integration-tests: sha256:a6a8dd638d467483c0758fc69503f51a4774eedb53126e930628e641c22799e6
manifest:                sha256:7eeb74bcc029c256187b4c295dda4c73ecd8f74a8098361292d174f5ffc7a040
```

This closes the G2 I13 live-transport boundary. It does not by itself close
G0/G1 or authorize SIF/MiniNDN/Tiger promotion; the shared source tree still
needs a sealed candidate and the automatic Python-DI workload proof remains
T015 work.
