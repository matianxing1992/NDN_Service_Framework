# T021 local SIF preflight progress

The local release path now includes:

- `packaging/ndnsf-di-container/bin/ndnsf-di-spec175-preflight`, a
  fail-closed source/workload/SIF checker;
- exact Qwen3.6-27B revision and tokenizer/workload validation;
- source-seal membership and digest checking for `workload.json`;
- rejection of compiled host overlays and deployment path names containing
  PyTorch/Transformers/runtime replacement artifacts;
- a builder invocation before Apptainer build, with the result recorded under
  `spec175Preflight` in the local SIF build record;
- mutation/unit coverage in `tests/python/test_spec175_sif_preflight.py` and
  the existing local-builder regression.

Verification on 2026-08-23:

```text
tests/python/test_spec175_workload.py
tests/python/test_spec175_sif_preflight.py
tests/python/test_prepare_local_sif_source.py
tests/python/test_build_local_sif_record.py
13 passed, 1 skipped
```

The skipped case requires a newly prepared source seal. T021 remains open:
the preflight still needs to be extended with the candidate SIF's native
Python/SOABI/import, RPATH/ldd, Boost/NDN-SVS/ORT, bundle-cwd, writable-path,
secret-scan, and runtime artifact checks, followed by one actual local
promotable SIF.
