# Design R3 Revision

## Work Unit

D-DESIGN-R3，用户授权审计并修正 Design。状态 PASS（本轮文档修订与工具检查）。
依据 [逐章审阅](../../../Design/reviews/chapter-audit-20260908.md)，修正事实错误，
补充四模块 API 行为、参数、调用例子、状态与失败边界，独立维护目标设计。
不修改产品源代码，不将文档修订作为 Spec182 功能验收。

## Evidence and Boundary

先核对最新 Design 审阅失败边界与既有证据，沿 canonical 源文件检查关键实现。
Context Mode 稳定层 PASS，active 层 rc=4（tasks 摘要过期），采用仓库权威文件。
CodeGraph 检索中排除 compare/staging 副本，未使用辅助草稿。
首次合并契约时补丁输出被截断，apply_patch 在写入前拒绝；改为逐卡片小批补丁。
一次进度补丁上下文不完整被拒绝，修正为完整行。均为编辑工具边界，不是产品失败。

## Validation

首轮双 PDF 构建完成，但版面门拒绝：两侧 AC-13 表格中 catalogDelta(sinceEpoch)
和 catalogLookup(objectName) 未被长标识符断行规则识别，分别溢出 2.98/17.63 pt。
原始构建保留于 `.codex-tmp/design-pdf-20260908T054921005169Z/`，检查日志为
`.codex-tmp/design-r3-verify-r1.log`。修正 renderer 的标识符识别并增加对应回归断言；
不放宽版面门，不把首轮算作通过。首轮 API 检查与 4 项工具回归已 PASS。

第二次编辑将标识符长度阈值改短，原有普通文本转义回归拒绝；保留阈值 24 字符，
只扩展括号识别，避免改变短文本转义语义。第二次构建目录
`.codex-tmp/design-pdf-20260908T055058680674Z/` 保留，不作为最终验收；最终构建在回归通过后执行。

第三次 PDF/签名/两侧还原/版面检查 PASS（78/84 页），原始结果为
`.codex-tmp/design-r3-verify-r3.log`。随后补充目标侧可读声明参考和生成内容检查，
并按实际页面审阅把 API 行为解释放在签名前，避免第 53 章先出现数页声明。
第四轮 pre-build 门发现并行工作修改 NativeOnnxRecipeAssembler.hpp，拒绝旧快照；
已核对新增 NativeOnnxGraphInspection/inspectNativeOnnxPlanningGraph 声明与实现差异，
按新工作区重新采样。新增入口仍为声明覆盖，不升级为完成 requester 接线或本轮功能验收。

第五轮全量声明扫描期间同一 ONNX 头文件继续变化，API 摘要与新快照不一致，门禁再次拒绝。
为避免每次仅一个头文件变化都重新扫描全部 Python AST，增加显式 --changed-only：
只复用路径/模块/SHA 相同的记录，变化项仍走原解析器；发布清单前重新检查全部 API 文件
集合和 SHA，发现扫描期间变化即拒绝发布。默认全量扫描保持可用，后续校验仍核对全集。
另绑定生成器/解析器/范围脚本摘要：改变工具时自动全量，旧记录不能按新解析规则静默复用。
新增回归在隔离 Git fixture 中比较增量与全量（改签名、增文件、删文件），并模拟扫描期间
修改，要求拒绝且保留先前 inventory。五项工具回归 PASS，原始结果
`.codex-tmp/design-r3-tests-final.log`；这些测试仅验证文档工具。

最终构建为 `.codex-tmp/design-pdf-20260908T060641412515Z/`，
完整机器结果见 [verification](design-r3-verification-20260908.json)。

- 当前 82 页 / 58 章，目标 87 页 / 63 章；第 53 章在两侧第 70 页开始，先解释机制与例子。
- 两侧各三遍 XeLaTeX；无 Overfull、Missing character、Warning，字体嵌入，全部目录条目/页码 PASS。
- 5 项文档工具回归 PASS：新增/删除与签名变化的增量/全量等价、扫描期间漂移拒绝、
  目标独立声明、所属符号和行为优先顺序、转义/长方法名/表格/代码块、旧 PDF 和重复声明身份。
- API 全集 295 文件 / 16610 条目 / 5752 函数声明（5751 唯一函数 ID）；852 绑定操作，
  23 组关键中文契约 / 78 份签名。八份当前/冻结目标 Markdown 与清单逐字节一致。
- 当前 460 文件从 `354a54a182228111ec3ed68c8208d8efd8424b24` 加精确 ONNX 工作区补丁还原，
  目标 350 文件从独立冻结提交/补丁还原，全部 SHA PASS；检查时 source drift 为空。
- 人工检查当前全 82 页缩略布局、当前第 70 页生成正文、目标第 83–87 页目标章节；
  保留原始预览，未发现裁切/空白内容错误。文本逐章处理保留 1–67 原审阅 ID，机械覆盖 PASS。
- 目标未自动刷新为当前源码；当前新增描述符/候选说明与冻结侧保持有意差异。
- 提交前 Spec182 validate_design.py 返回 ok=true、errors=[]（586 个本地链接），
  原始记录 `.codex-tmp/design-r3-spec-validation.json`；它仍报告 design_readiness=DRAFT，
  不将文档链接/依赖检查当成整个 Spec 已设计完成。最终 diff whitespace 与 PDF 输入身份检查 PASS。
- 本轮未宣称全部函数语义契约或独立读者试用完成；具体限制见 [逐章修订](../../../Design/reviews/chapter-revision-r3-20260908.md)。

最终命令：`python3 Design/build-api-reference.py --changed-only`、
`python3 Design/render-api-reference.py --target`、`python3 Design/render-api-contracts.py --target`、
`python3 Design/build-behavior-coverage.py`、`python3 Design/refresh-snapshot.py`、
`python3 Design/test_design_state.py`、`python3 Design/verify-api-reference.py`、
`python3 Design/build.py`、`python3 Design/verify.py .codex-tmp/design-pdf-20260908T060641412515Z`。
产品编译、单元/集成、MiniNDN、SIF、Tiger：NOT_RUN（本轮仅文档与文档工具）。
