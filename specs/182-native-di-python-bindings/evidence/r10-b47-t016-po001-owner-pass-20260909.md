# R10-B47 T016 PO-001 Owner/Runner Pass 2026-09-09

## Scope and first boundaries

本批是 T016-A 的一次真实 owner/runner 重试，目标是让唯一的 PO-001 native business selector
在 MiniNDN owner namespace 存活期间执行。历史失败保留在独立 raw run 目录，不覆盖：

1. `owner/`：root owner 启动前的 system-first `PATH` 没有 `/usr/local/bin/infoconv`，
   Minindn 的外部转换命令输出不是 JSON，owner 在 `MININDN_OWNER_FAILED:JSONDecodeError`
   停止。
2. `owner3/`：加入 `infoconv` 后，历史 runner manifest 的 process role
   `native-di-integration` 不在 runner 的固定角色词汇表，静态 preflight 拒绝。
3. `owner4/`：把角色改为 `requester` 后，当前增量 `integration-tests` 二进制与旧 manifest
   的 SHA-256 不同，artifact identity preflight 拒绝。

这些都是 owner/tooling/artifact 边界，不是协议结果。修复后的 transient manifest
`.codex-tmp/spec182-r10-b46-native-di-manifest.json` 只重算当前 executable digest，并保留
`requester` node binding；没有改动版本化 source 或冻结的原始 manifest。

## Five-lane coverage matrix

| Lane | Evidence |
| --- | --- |
| production entry/callers | owner `Experiments/NDNSF_DI_NativeClosure_Minindn.py` → canonical `tests/standalone/run-spec182-native-closure.py` → staged `integration-tests` → `NativeInferenceClient`/real R4-B6 `ServiceProvider` fixture |
| implementation and wire | staged executable `/bin/integration-tests` runs `Spec170NdnsfDiCoreFlow/Spec182R4B6RealProviderConversation`; native result marker is emitted after both conversation result assertions |
| test/harness/oracle | PO-001 case manifest requires `SPEC182_NATIVE_DI_REQUEST_RESULT_OK`; runner validates seven evidence classes and rejects missing/invalid process role, node context, executable digest or trace integrity |
| build/source closure | current source `.codex-tmp/spec182-r4-b2/build/integration-tests`; executable digest recomputed from the source artifact before staging; owner namespace uses `/run/nfd/requester.sock` and peer metadata `provider` |
| migration/evidence | fresh raw output `.codex-tmp/spec182-t016-r10-b46-owner5/` contains `node-context.json`, `runner-result.json`, `closure-run/{stdout,stderr,trace}` and `result.json`; no Python runtime is linked into the staged executable |

## Review trace

官方 `/home/tianxing/.codex/skills/review-agent/SKILL.md` SHA-256
`07079efd0dc76f05fade424e5dfb048dce1de2df7626e1a4f56292a4f3f92228` 的只读检查覆盖 owner
role/path handling、runner artifact digest and process/node binding、business marker、trace
evidence and cleanup. The transient manifest contains no versioned source diff; no P1/P2/P3
finding was introduced.

## Command and result

```text
sudo -n env PATH=/usr/bin:/bin:/usr/sbin:/sbin:/usr/local/bin python3 \
  Experiments/NDNSF_DI_NativeClosure_Minindn.py \
  --manifest .codex-tmp/spec182-t016-r10-b17-20260909050945-manifest.json \
  --output .codex-tmp/spec182-t016-r10-b46-owner5 \
  --execute-owner \
  --runner-manifest .codex-tmp/spec182-r10-b46-native-di-manifest.json \
  --runner-case PO-001
```

Exit `0`. `result.json` is `PASS`; the staged native process returned `0` in `8001ms`. The
business marker was present in `closure-run/stdout.log`. Runner observation is `complete=true`,
`successfulExecs=1`, `timedOut=false`, with complete `identity`, `process-tree`, `namespace`,
`exec-map`, `endpoints`, `business-oracle` and `cleanup` evidence. `integrityViolations`,
`policyViolations` and `violations` are all empty. The trace records the requester node's real
namespace inode `4026533022`, owner PID `1629977`, NFD socket `/run/nfd/requester.sock`, and the
provider peer binding.

## Result and limits

This closes `FOCUSED_QUALIFICATION_PASS` for PO-001's isolated native-process observation and
proves the owner can hold a real MiniNDN requester namespace while the runner executes the native
selector. The selector itself still uses an in-process Provider callback fixture; no independent
Provider executable or requester/Provider transport was exercised. I02-I08, PO-002-PO-014,
maintained caller/no-Python migration, numeric parity and full T016 therefore remain
`PARTIAL`/`UNQUALIFIED`.

## Batch retrospective

- **Static miss:** the historical role name was rejected only after owner setup; the next manifest
  generator must validate runner role vocabulary before launching MiniNDN.
- **Compile miss:** stale executable SHA was caught by staging preflight; no rebuild was needed.
- **Runtime miss:** none after the corrected manifest; the native process and business oracle both
  returned successfully.
- **Unobserved:** independent Provider process, cross-process NDN request, all negative cases and
  the remaining T016 matrix are still unobserved.

## Closure decision

`CLOSED_FOR_VALIDATION` for PO-001 owner/runner evidence; `OPEN_FOR_NEXT_BATCH` for I01-I08,
PO-002-PO-014 and independent requester/Provider transport. T016-A remains `PARTIAL` until the
complete matrix and maintained caller/no-Python gates pass.
