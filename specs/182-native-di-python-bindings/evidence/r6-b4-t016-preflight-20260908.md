# R6-B4 T016 Qualification Preflight

日期：2026-09-08。此批次只执行 T016-A 的启动前 preflight，确认工具和外部 node/NFD
前置是否存在。没有启动业务进程、namespace、网络请求或 C++ 构建。

## Observed first boundary

`/usr/bin/bwrap`、`/usr/bin/strace`、`/usr/bin/nsenter` 均存在，运行用户为 UID 1000；
但 `/run/nfd/nfd.sock` 不存在，且本机没有由 MiniNDN owner 提供的 node/netns metadata。
使用冻结 manifest 的 `I01` campaign（临时副本加入 `campaignCase=I01`）执行：

```text
python3 Experiments/NDNSF_DI_NativeClosure_Minindn.py \
  --manifest .codex-tmp/spec182-t016-r2/manifest.json \
  --output .codex-tmp/spec182-t016-r2/result
# exit 2
# result.status=UNQUALIFIED
# result.reason=MININDN_NODE_CONTEXT_NOT_PROVIDED
```

campaign owner 在业务启动前写出 fresh `result.json`，其中保留 22 个注册 case、canonical
runner 和 `runSeconds=180`/`cleanupSeconds=15`/`traceBytes=268435456`。没有生成 trace、
进程树、namespace 或 endpoint 证据，因此不能解释为 I01 PASS/FAIL，也不能解释为 no-Python
资格结论。

## Review trace

- Skill: `/home/tianxing/.codex/skills/review-agent/SKILL.md`
- SHA-256: `07079efd0dc76f05fade424e5dfb048dce1de2df7626e1a4f56292a4f3f92228`
- Baseline: `c258f93f`
- Scope: preflight command, campaign owner exception/status boundary, manifest identity and
  raw output directory
- Result: no product defect; missing external node/NFD context was correctly classified as
  `UNQUALIFIED` before business execution.

## Validation and evidence

The raw run is retained at `.codex-tmp/spec182-t016-r2/` with temporary manifest, stdout/stderr
and `result/result.json`. The first boundary is also recorded in
[`docs/failure-log.md`](../../../docs/failure-log.md) under the 2026-09-08 T016 preflight entry.
No build was applicable; `-j4` was not invoked. The active task validator and prior R6-B1/R6-B2
Python suites remain green, but they do not substitute for this missing runtime context.

## Result and next step

`STATIC_PASS`; `BUILD_NOT_APPLICABLE`; `UNQUALIFIED` preflight; `PARTIAL`; not
`QUALIFICATION_PASS`.

Closure decision: `OPEN_FOR_NEXT_BATCH`. A later attempt must obtain real MiniNDN node/netns
identity, isolated NFD socket and peer topology, use a new raw run directory, then execute the
complete I01--I08 and PO matrix. Do not overwrite this run or classify it as a protocol failure.
