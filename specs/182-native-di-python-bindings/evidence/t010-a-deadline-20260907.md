# T010-A Active Deadline and Private Test Port

## Scope and Review

基线 40059ae7。修复只在 dispatch 检查预算的缺口：工作排队/阻塞时，独立
steady timer 也能触发同一个终态 gate；terminal 移除定时项，晚到 expiry 不复活。
timer 只持 weak operation；取消函数在 operation 锁外释放。测试时钟/分派构造
改为 private TestPort/friend，生产构造不能覆盖时钟。ACK budget 必须严格小于
总预算，毫秒上限匹配 Core int 参数；负 result wait 拒绝，异步异常进入失败 handle。
源码审查覆盖 client/handle/executor 锁顺序、时钟、取消/close、timer 生命周期，
以及 5 个新增 case 的生产触发路径；完整 Core orchestration 仍未接通，不关闭 T010。

## Checks

复用 build-nac182，system GCC9.4/binutils2.34、Boost1.71、同一 NAC/SVS/ONNX/Rust
依赖缓存；无 reconfigure、清理或并发构建。`waf -o build-nac182 build
--targets=unit-tests -j4 -v` exit0，21.40s；GNU time max RSS 1090972 KiB
仅为该工具报告的进程统计，不当成并发整机峰值或 -j2 对比。vmstat 后续两行 si/so=0/0。
原始日志 `.codex-tmp/spec182-t010a-deadline-r1/build.log`。

首次测试命令误把两个 suite selector 用逗号连接，Boost.Test exit200：
`no test cases matching filter or all test cases were disabled`。首边界为命令过滤器，
没有执行产品用例。原始 `.codex-tmp/spec182-t010a-deadline-r1/client-state.log` 保留。
下一次分别用单 suite 命令执行，写新 r2 目录。

修正命令后，`timeout --kill-after=5s 120s ./build-nac182/unit-tests
--run_test='Spec182ClientState/*' --report_level=detailed` exit0，8/8 cases、38/38
assertions；相同命令选择 `Spec182NativeInferenceClient/*` exit0，2/2、9/9。
原始 `.codex-tmp/spec182-t010a-deadline-r2/client-state.log`、`client-existing.log`。
真实 timer case 使用生产 steady clock/scheduler，故意不泵工作队列并阻塞通知回调，
50ms 请求仍通过 result 得到 NATIVE_REQUEST_TIMEOUT；测试 port 只替换工作分派。
另以可控 timer 验证 wait 不重设 deadline、terminal 取消定时项及 late expiry/dispatch
不覆盖 Cancelled/Failed。未运行 full regression、integration 或 MiniNDN。

## Remaining

本轮必要构建和相关单测 PASS；T010-A PARTIAL。完整 Core 请求、成功/取消竞争、Core 回调 fencing、
有界通知/溢出与后续 T010-B/C、T011--017 保留原验收要求。
后续 production 接线须先解决 Core executor 入口：当前 ServiceUser::BeginCollaboration
直接访问 m_pendingCalls，未提供公开 Face 调度入口；不得在 DI worker 直接调用或自建 Face。
Context Mode project health rc5（NO_REAL_SESSION_EVENTS）、active rc4（source hashes stale），
使用 repository fallback；CodeGraph symbol 查询混入旧 compare 副本、精确 native 文件未命中，
已回退生产路径定向读取。两份既有 native dependency/generation 契约 diff 保持原样。
Spec182 design validator exit0（36 进度单元、父任务 5/17），git diff --check PASS。
修正 spec.md 的旧 NOT_STARTED 状态，使其引用 tasks 当前进度，不回写历史验收证据。
