# T001-C Dispatch and Selector Freeze Evidence

## Scope

2026-09-07：按 [execution-units](../contracts/execution-units.md) T001-C 卡执行
Dispatch and Selector Freeze。Verify = DOC；从实际 Waf 注册推导命令；所有 planned
测试有 author owner；父 T001 完整验收满足后才允许 T002。本单元只冻结设计期身份与
selector，不运行产品构建/unit/integration（仍 NOT_RUN，各实现卡执行）。

## Frozen Build Identity（写入 case-manifest `buildIdentity`）

| Item | Frozen value | Evidence |
| --- | --- | --- |
| C++ compiler | `/usr/bin/g++ -B/usr/bin`，版本 9.4.0（binutils 2.34） | 系统 compiler/binutils，T001-A/B 探针与现 Waf 缓存一致 |
| Max jobs | 2（不并发操作同一 Waf 树） | plan.md Technical Context |
| unit-tests 注册 | tests/wscript `bld.program(target='../unit-tests', name='unit-tests')`，source = main.cpp + `ant_glob('unit-tests/**/*.cpp', excl=['unit-tests/sanitizer/**'])` + 框架生产源 | tests/wscript 实际注册行 |
| 构建命令 | `./waf build --targets=unit-tests -j2` | 由上述注册推导 |
| focused 执行 | `build/unit-tests --run_test=<CaseName>[,<CaseName>...]`；`--list_content` 确认注册非空 | docs/build-and-test.md 既有文档化用法 |
| 时间上限 | unit supervisor 120s；初始网络 case 180s；cleanup 15s | proof-design 既有约定，本次冻结不再延长 |
| rust toolchain | 1.90.0，prefix `.codex-tmp/spec182-t001-dependencies/rust-prefix`，cargo-home 独立 registry | T001-A 核验 |
| tokenizer bridge crate | NDNSF-DistributedInference/cpp/adapters/qwen/tokenizer-bridge | T001-A release build rc=0 |
| source identity | 每次 dispatch 用 `git rev-parse HEAD` 记录 | case-manifest `buildIdentity.sourceCommit` |

## Frozen L0 Command

consumer `tests/standalone/spec182-installed-consumer.cpp`（已有）；T002-A 交付
wscript 安装段与 `ndnsf-distributed-inference.pc.in` 后稳定为：waf install 到
fresh staging prefix（新 run-id 目录，非空即拒绝）→
`PKG_CONFIG_PATH=<staging>/lib/pkgconfig` 下 `/usr/bin/g++ -B/usr/bin -std=c++17`
编译 consumer 链接 `ndnsf-distributed-inference` → 依赖/文件检查无 libpython、
无 DI 源码副本。完整文字与 NAC-ABE gate 见 proof-design.md Revision 7
（L0 Installed-Library Command）。NAC-ABE ABI 5 符号缺口
（getPublicParamsDataName、getPublicParamsDigest、clearCache、
refreshPublicParameters、refreshDecryptionKey）为首次成功 L0 的前置
（docs/failure-log.md 2026-09-07），本轮不触碰。

## Case Manifest（tests/fixtures/spec182/case-manifest.json）

schema `spec182-case-manifest-v1`、frozenAt 2026-09-07、freezeOwner T001-C、
status FROZEN_PLANNED（NOT_RUN 至实现卡执行）。

- cppSuites 23 项：T003-A/B/C、T004-A、T005-A/B、T006-A/B/C/D、T007-A/B、
  T008-A/B、T009-A/B/C、T010-A/B/C、T011-A/B/C 各一个 selector，含 owning
  文件（planned 文件标注 "(planned)"）、现有 suite/case 原位登记、layers、
  executeOwner、namedCasesPlanned。
- pythonEntries 6 项：T012-A/B、T013-A/B、T014-A/B 的 (file, kexpr) 对。
- systemEntries 3 项：closure runner、MiniNDN 实验、installed-consumer。
- fixtures 段登记 tokenizer/identity 向量 sha256（tokenizer vectors.json
  `6f44a2e6…`、spec181 assembly `5e035fcc…`）与 planned native-wire-vectors.json。

**双向核对（脚本执行，见 Validation）**：
- 23 个 CPP selector 与 execution-units 各卡 Verify `CPP(Spec182…/*)` 逐一对照：
  manifest→卡定义、卡定义→manifest 双向均无遗漏、无错配、无重复。
- 6 个 python (file, kexpr) 与各卡 Verify `PY(<file>, <kexpr>)` 一致。
- named cases 保留 16 项具名义务：6 Registration + 3 SharedLease/Closing +
  4 Sampling + 6 Stream 负例 + tokenizer 84-vector full 对照（其余 planned
  cases 为空数组 = 不承诺未冻结的具名列表，不表示 suite 为空）。
- 所有条目均带 card（author owner）与 executeOwner；L3+/L6 执行 owner 统一
  为 T016，符合 proof-design 执行归属。

## Write 清单处置（execution-units T001-C Write）

| Target | Disposition |
| --- | --- |
| F/case-manifest.json | 新建（本单元主交付） |
| contracts/proof-design.md | Revision 6 → 7：冻结句 + 实际 Waf 注册/runner 命令 + L0 子节 |
| contracts/code-design.md | Revision 7 → 8：Open Questions O-002 → CLOSED (design)、O-004 → CLOSED (mapping)，表内无 OPEN 项 |
| contracts/work-units.md | Revision 7 → 8：T001 section 与 Common Boundary 状态句同步为全关闭 + 冻结身份 |
| contracts/spark-execution.md | 冻结句：实现卡按 case-manifest 领取 |
| contracts/compatibility-manifest.json | 无源变更；O-004 收口产物保持 6fa1f756（344 项，UNREVIEWED 0） |
| plan.md | Revision 7 → 8：Summary/Current Planning Result 更新为 T001 DONE、O-001--005 全关闭 |
| tasks.md | Revision 10 → 11：T001-C row → DONE、T002-A BLOCKED 原因改为 NAC-ABE 工具链缺口、顶部 checkpoint |
| audit.md | 保持不动：frozen review trail；A7-08/A7-09 的处置义务按原行归 T010/T011 产品实现，不受设计关闭影响 |
| traceability.md | 保持不动：FR↔task 映射无变化 |

## Validation

- 双向 selector 核对脚本（对照 execution-units.md 全部 23 张卡 Verify 文本）：
  cpp mismatches 0，python mismatches 0，双向无遗漏（脚本输出记录于本文件命令节
  —— `python3 -m json.tool` 校验 JSON VALID，selectors 无重复，每项带
  card/file/layers/executeOwner）。
- `python3 specs/182-native-di-python-bindings/checklists/validate_design.py`：rc=0
  （含本 evidence 链接解析通过）。
- `git diff --check`：PASS。
- check-prerequisites：本单元不新增依赖/不重跑探针，沿用 T001-A/B 的 exit 0。
- 产品 native 构建、unit、integration、MiniNDN：NOT_RUN（按卡执行，T002-A 起）。

## Routing

父 T001 三张卡（A/B/C）均 DONE：O-001--O-005 全部关闭、叶子 schema/依赖/调用方/
selectors 冻结、tasks 修订至可执行，满足 plan Gate Order G0 的 DOC 验收；行为证明
（PO-001/PO-012 等）仍按各卡与 T016 运行。下一构建边界：NAC-ABE ABI 5 符号
缺口匹配后重跑 T002-A 首次 L0（T002-A 依赖现已满足，任务行 BLOCKED 原因仅剩该
工具链边界，failure-log 2026-09-07）。
