# T001-A/B Design Closure Evidence

## Scope

2026-09-07：按 [execution-units](../contracts/execution-units.md) T001-A / T001-B
卡执行设计关闭核对。本单元只关闭设计卡本身（Verify 均为 DOC + 依赖契约/双向
映射核对），不执行产品验收；字段/方法/错误 parity 与真实 native 运行证明按各
契约原文仍归 T003/T006/T007/T010/T011/T012/T016 及对应 owner 任务。

## T001-A Cross-Checks

依赖身份契约 [native-dependencies.json](../contracts/native-dependencies.json)
（DESIGN_LOCK_WITH_BOUNDED_PROBES）与持久探针逐项核对：

| Check | Contract claim | Observed | Result |
| --- | --- | --- | --- |
| ONNX probe rerun | fixture `tests/fixtures/spec181/assembly-vectors-v1.json` sha256 `5e035fcc…`，exactCases 4 | sha match；`ONNX_DEPENDENCY_PROBE PASS cases=4` rc=0（onnx-r1/onnx-probe，30s 有界） | PASS |
| tokenizer ABI probe fresh repro | vectors.json sha256 `6f44a2e6…`，exact 84 / negative 14 | sha match；rust-prefix 工具链（rustc/cargo 1.90.0）全新构建+链接+执行均 rc=0；`TOKENIZER_ABI_PROBE PASS cases=84 negatives=14` | PASS |
| product bridge release build | Cargo.lock sha256 `a9352585…`；`--locked` 构建 | Cargo.lock sha match；`cargo build --release --locked -j2` rc=0，产出 `libndnsf_tokenizer_bridge.{a,so}` | PASS |
| rust 1.90.0 identity | 组件 SHA256（rustc/cargo/rust-std 已核验） | rust-prefix 内 rustc 1.90.0 / cargo 1.90.0 可执行，独立 prefix 未改全局环境 | PASS |
| numeric identity vectors | identity-vectors.json 24 cases（12 pairs） | fixture 24；reference extraction 记录 PASS_REFERENCE_EXTRACTION（12 pairs）；graph.py SHA256 仍等于冻结值 `e5532328…`，generator guard 生效 | PASS |
| extended identity | 14 稳定扩展 + 缺陷诊断 | identity-extended-vectors.json 16 = 14 稳定 + complex64/128 诊断（A7-07 typed-complex 旧转换缺陷按 failure-log 保留，未标为 oracle） | PASS |
| v2 normalization oracle | initializer-stable-v2-vectors.json 17 手工 expected（bfloat16/complex/string/int4，revision v2） | fixture 17，manual oracle 声明一致 | PASS |
| descriptor/identity findings | A7-07a/b（BFLOAT16 raw digest mismatch、STRING nondeterministic）已确认保留 | `identity-reference-20260906.json` findings 原位，不升级 oracle 抹平 | 保留（正确处置） |

失败边界处置：failure-log 2026-09-07 记录的 `cargo: command not found`
（exit 127）与 NAC-ABE ABI 缺口仍属工具链边界。Cargo 边界本单元在安装好的
rust-prefix 上同源重跑通过（raw root
`.codex-tmp/spec182-t001-dependencies/tokenizer-r2/`，含 build/link/probe 日志
与 `bridge-build/`）；NAC-ABE ABI（getPublicParamsDataName 等 5 符号）未在本单元
触碰，保留为下一构建边界。

## T001-B Cross-Checks

按卡 Verify（DOC + source symbol→contract→caller→task/PO 双向核对）完成：

- O-004 静态映射收口（62157804，manifest 344 项、UNREVIEWED 0）已把
  formal api/sdk/root 导出逐项登记 nativeOwner/ownerTask；本单元把
  `runtime-boundaries.md`（Revision 7→8）与 `symbol-design.md` 中 O-004
  收口前遗留的 “需 O-004 关闭 / O-004 未证实则 BLOCK” 条款更新为已核实的
  O-004 处置：现有 API 无公开逐服务注销/registration token；
  `NativeServiceRegistration::close` 保持 planned Core scoped registration
  扩展（lifecycle Registration Generation Decision），T009 落地前不包装旧 API。
- registration generation、late ACK/Selection、共享 lease 与 stop 的设计决定
  已由 [native-provider-lifecycle-design](../contracts/native-provider-lifecycle-design.md)
  （Verified Registration Boundary / Shared Lease Ownership / Closing Targets /
  Registration Generation Decision）冻结：move-only ServiceRegistration +
  共享 RegistrationState、Provider 单调 generation、ACK 发布/Selection 派发/
  CollaborationWorkFence 三处 fence、重复注册拒绝、shared lease 状态路由。
- journal 兼容与原生端口清单以
  [native-token-stream-design](../contracts/native-token-stream-design.md)
  Production Caller Inventory / Requester Acceptance 两节关闭设计疑问；
  A7-08 产品实现证明按具名负例转 T010/T011/T016（NOT_RUN 记录原位）。
- 没有注销 API 的场合未编造 unregister 调用：全部 close/清理语义均为
  planned 声明并指向精确 Core API 扩展表。

## Validation

- `.specify/scripts/bash/check-prerequisites.sh --json --require-tasks --include-tasks`：exit 0。
- `python3 specs/182-native-di-python-bindings/checklists/validate_design.py`：无 errors。
- `git diff --check`：PASS。
- 产品 native 构建、unit、integration、MiniNDN：NOT_RUN（T001-C 冻结后按卡执行）。

## Routing

T001-A/B 卡验收满足后 T001-C（Dispatch and Selector Freeze）解除 BLOCKED；
product 构建仍被 NAC-ABE ABI 缺口阻塞（.codex-tmp/spec182-native-build-20260907-r1/），
按 [failure-log](../../../docs/failure-log.md) 在下一构建单元匹配 NAC-ABE 头/库后重跑。
