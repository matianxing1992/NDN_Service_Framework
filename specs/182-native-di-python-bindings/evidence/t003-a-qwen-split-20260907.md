# T003-A Qwen Split Candidates — 验收执行证据（2026-09-07）

Status: DONE。Selector：CPP(Spec182QwenSplit/*)，承载 suite
`Spec182NativePlanning`（[tests/unit-tests/di-native-planning.t.cpp](../../../tests/unit-tests/di-native-planning.t.cpp)），
manifest cppSuites[0]（card T003-A；buildIdentity：unit-tests target、
`/usr/bin/g++ -B/usr/bin`、-j2、timeout 120s）。

## 执行命令与结果

```
./waf -o build-nac182 build --targets=unit-tests -j2      # rc=0（增量 9.2s）
build-nac182/unit-tests --run_test=Spec182NativePlanning  # rc=0，5 cases，*** No errors detected
```

运行时 LD_LIBRARY_PATH 前置 nac-abe-integration-182/install/lib（T002-A 工具链边界）。
case 时间合计 ~2ms，全部通过；raw 输出见 `.codex-tmp/t003a-build2.log`。

## Verify 对照

- 固定小图合法 cover：
  `QwenLayerSplitProducesCanonicalRankOneCandidate`（existing，manifest
  existingCases[0]）：3 段 roles 连续 ranges {{0,2},{2,3},{3,4}}、
  embedding→final-norm-head 图、确定性 candidateDigest、rank-one
  tensorDegreesByRole、单候选。通过。
- 边界 budget（本卡新增 `QwenLayerSplitEnforcesBudgetBoundaries`）：
  enumerate 入口即拒绝 budget 越界（maxCandidates=0/1025、
  maxPolicyMs=0/60001、maxReentries=17 → invalid_argument）；闭区间边界
  {1024, 60000, 16} 正常产出确定性单候选。
- 非法 rank/图输入（本卡新增 `QwenLayerSplitRejectsInvalidRankAndGraph`）：
  - 非法 rank：tensorDegrees 含 2 → 构造拒绝（native 保持冻结支持范围
    rank-one；Python hybrid 需显式 rank artifacts，见下）；
  - 非法构造：range 起点非 0、range 不连续、roles 重复/长度不符、
    artifact/weight 角色覆盖不齐、weight=0、非 digest 值 → 构造拒绝；
  - 非法图：错误 adapter（yolo26n）、节点数不符（layerCount+2）、
    embedding/final-norm-head 边界缺失、层序非 canonical（layer-02 先行）、
    graphDigest 与 model 不符 → enumerate 拒绝；
  - 全部拒绝后同一合法输入仍产出 1 候选。
- expected 来自冻结旧 splitter：
  `ndnsf_distributed_inference/adapters/qwen/placement.py`（仓库内生产路径，
  非 .codex-tmp 副本）`QwenThreeStageSplitter`（line 90-251）语义逐条对应：
  `__post_init__` 的 ranges 连续/起点 0/artifact+weight 角色覆盖/degree 检查
  ↔ native ctor 守卫；`enumerate_candidates` 的 node 边界
  （embedding→roles[0]、layer-NN 规范化、final-norm-head→roles[-1]）与
  cut-edge/dependency/requirements 结构 ↔ native `enumerate` 输出字段
  （source PRE_SPLIT、3 段 roles、crossPartitionTensors 2、
  requirements backends/workspace/activation/transient/1.10）。
  支持范围保持：Python 冻结语义中 degree!=1 且无显式 rank artifacts 即
  ValueError，native 明确拒绝任何 degree!=1（无 rank-artifact 建模），
  不产出伪 hybrid 候选。native digest/fragment 为 native 独立字节契约
  （byte-exact wire 对照属 T004/T006，不在此卡）。

## 修改

- tests/unit-tests/di-native-planning.t.cpp：新增两个 case
  （QwenLayerSplitRejectsInvalidRankAndGraph、
  QwenLayerSplitEnforcesBudgetBoundaries），实现即切片已有
  （NativeQwenPlanner.cpp/.hpp，48b1c7b9 起），本卡未改动生产 planner。
