# T003-B Yolo Split Candidates — 验收执行证据（2026-09-07）

Status: DONE。Selector：CPP(Spec182YoloSplit/*)，承载 suite
`Spec182NativePlanning`（[tests/unit-tests/di-native-planning.t.cpp](../../../tests/unit-tests/di-native-planning.t.cpp)），
manifest cppSuites card T003-B（existing case
`YoloComponentSplitRejectsUncoveredGraphAndSortsPriority`）。

## 执行命令与结果

```
./waf -o build-nac182 build --targets=unit-tests -j2      # rc=0（增量 ~9s）
build-nac182/unit-tests --run_test=Spec182NativePlanning  # rc=0，7 cases，*** No errors detected
```

## Verify 对照

- 固定 cover：existing case（split-v1 priority 1 在 atomic-v1 priority 10 前；
  semantic partition 按 role 覆盖 backbone/neck/detect/output；
  Backbone ingress → Head egress、Merge postprocess）。通过。
- 错误 component/rank（本卡新增
  `YoloComponentSplitRejectsInvalidComponentsAndForeignModel`）：
  - 构造拒绝：空注册表、重复 candidateId、空/重复 roles、空 ingress/egress、
    非 digest candidateDigest；
  - enumerate 拒绝：非 yolo 模型（qwen adapter）、ingress/egress 未声明于
    roles、semantic node set 为空、节点跨 role 二次分配、partition 节点名与
    图不符（size 相同但 id 错位）；
  - 全部拒绝后同一合法 component 仍产出 1 候选。
  Yolo component 模型无 tensor rank 维度；rank/hybrid 拒绝面在 Qwen 卡
  （T003-A case）覆盖，本卡不重复。
- 确定性重复调用与候选排序（本卡新增
  `YoloComponentSplitIsDeterministicAndBudgetTruncates`）：
  5 候选乱序注册（priority 9/1/5/1/3）→ 稳定序
  （priority 升序 + candidateId 字典序）；两次枚举 candidateDigest 逐字节
  相同；budget maxCandidates=3/1 截断保留同一稳定序头部
  （{1024,100,16} 预算下全量 5 候选与截断头部一致）。
- expected 来自冻结旧 splitter：
  `ndnsf_distributed_inference/adapters/yolo/adapter.py::Yolo26Splitter`
  （`enumerate_candidates` 按 registered 序产出、priority 非负校验于
  candidates.py:203-205）+ `candidates.py::RegisteredYoloCandidate`
  （candidateId/priority/roles/nodeNames/ingress/egress/mergeKind 对应 native
  `NativeYoloComponentSpec` 字段）。native 显式稳定排序是冻结行为
  （enumerate 内 stable_sort by priority,id；Python catalogue 顺序切片时
  已对齐）；`selectionPriority` 透传为候选 selectionPriority。
- 不加载服务协作：cases 不触达 Provider/网络路径（纯本地 planner 调用，
  NativePlanning/NativeYoloPlanner 无网络符号）。

## 修改

- tests/unit-tests/di-native-planning.t.cpp：新增两个 case
  （YoloComponentSplitRejectsInvalidComponentsAndForeignModel、
  YoloComponentSplitIsDeterministicAndBudgetTruncates）。实现即切片已有
  （NativeYoloPlanner.cpp/.hpp），本卡未改动生产 planner。
