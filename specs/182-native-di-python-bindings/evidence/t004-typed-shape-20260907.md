# T004 Typed Tensor Shape Repair

## Status

2026-09-07 / PARTIAL。NativeAssemblyTensorContractV3 维度使用 int64/string variant，
Selection parser 在既有语义比较前恢复 JSON 类型；拒绝重复 key 和非法维度类型。
ONNX worker metadata 保留维度类型，最终模型 I/O 语义比较仍沿既有 normalize 规则。
完整 projection encoder 已新增，旧 sealer 入口尚未切换，不关闭 T004。

## Attempt r1

`.codex-tmp/spec182-t004-shape-r1/` 保存 configure.log/build.log。
新 ABI build 目录，system g++/binutils、Boost 1.71、既定 NAC/SVS/ONNX/Rust 闭包。
-j4 build exit 1，首边界 NativeYoloMergeRunner.cpp:335：旧 metadata 路径试图把 variant
直接拼到 std::string。CodeGraph 确认这里只构造 runner 的文本 expectedOutputShape，
修复为显式转换整数或取符号字符串；没有改变被签名的 shape 类型或另加模型 runner。
r2 复用本轮新的 ABI 目录，另建 raw run 目录保留修复后的构建和测试，不覆盖 r1。

## Attempt r2

r2 build exit 1，在 NativeProviderHandler.cpp:1363/1483 暴露两个旧字符串消费者。
前者是 YOLO runner 文本 metadata，显式文本转换；后者为 decode state tensor identity，
增加 i/s 类型标记和原有长度界定，避免数字与数字样式符号复用同一状态身份。
原始 `.codex-tmp/spec182-t004-shape-r2/build.log` 保留。源码检索核对剩余 expectedInputs/
expectedOutputs 消费者，r3 继续使用本轮新 ABI 树，独立记录。

## Complete Wire Encoder Checkpoint

新增 nativeSelectionProjectionV3ToJson：显式序列化 role/assembly/execution/dataflow/device/
dependencies/grant/generation/conversation typed 字段，复用 nativeCanonicalJson。
返回前调用既有生产 parser 执行完整语义检查；不发出缺项片段。
当前 NativePlanSealer::project 仍缺完整输入，旧 encode 待切换，不能称为 requester 主链闭合。

## Attempt r3 / Focused PASS

`.codex-tmp/spec182-t004-shape-r3/build.log`：同一新 ABI 树，-j4 build exit 0，119.84s。
`core-focused.log`：unit-tests 使用
`--run_test=NativeV3*,Spec182CanonicalJson,Spec182OnnxExtraction,Spec182PlanSealer,Spec182NativePlanning,Spec182Preparation`，
73/73 cases、2491/2491 assertions PASS，包含完整 encoder→生产 parser 的往返及错误绑定拒绝。
`focused.log`：首个混合层级 selector 实际只选到 Spec182OnnxWorkerProtocol/Metadata*，
已核对实际 case 列表，因此仅记录该 4/4 cases、30/30 assertions；另跑上述无斜杠 selector
补齐核心检查，没有把零覆盖或小覆盖误报成完整通过。
shape 同时保留整数与数字样式符号字符串，类型替换、bool/float/null/超 int64 维度拒绝。
没有运行全量回归、网络集成或资格验收。当前只证明 typed wire 和语义 parser 接受；
完整 Python PlanSealer 字节 oracle、真实 core digest、project 输入和 requester 接线仍未关闭。

## Source Review and Remaining Work

NativeSelectionJson.cpp 显式列出已有 DTO 的 wire 字段，复用 typed canonical helper；
canonicalArtifactName 是 Core assignment 的本地授权字段，继续不从 Selection JSON 获取。
内部 segmentNaming/legacy plan metadata 不冒充 V3 wire 身份。
后续必须让真实 sealer 生成完整 role/assembly/device/dataflow，再切换旧 encode；
不能由 harness 或 caller 临时补伪身份/默认 CPU。当前设计 validator 与 diff check PASS。
Context Mode stats 可调用；project health exit 5（NO_REAL_SESSION_EVENTS），active health
exit 4（tasks.md indexed source hash stale）；原始诊断在 r3，
当前进度与源码均使用仓库权威记录及 CodeGraph，未把检索统计当作 host capture 验收。

生产 Selection parser 对数字/数字样式符号字符串保真，并拒绝 role/assembly 类型替换；
bool、float、null、超 int64 的维度拒绝。worker metadata 数字和字符串 roundtrip。
既有 ONNX extraction/worker 单测与 canonical helper 对照一起验证，不进行集成/资格验收。
