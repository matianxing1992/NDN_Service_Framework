# T002-A Installed Library Boundary — L0 执行证据（2026-09-07）

Status: DONE（首次 L0 成功，run-id `.codex-tmp/spec182-t002a-l0-r2/`，fresh staging）。

## 前置修正：registry empty-freeze 语义自修正

- 负结果与根因记录在 [failure-log](../../../docs/failure-log.md) 2026-09-07 条目
  “T002-A L0 consumer aborts: freeze() rejects an empty native adapter
  registry”（原始 run-dir `.codex-tmp/spec182-t002a-l0-r1/` 保留）。
- 修正：`NDNSF-DistributedInference/cpp/ndnsf-di/NativePlanning.cpp`
  `NativeAdapterRegistry::freeze()` 移除 empty 前置，改为无条件 latch；
  “至少一个 adapter”的前置（若调用方需要）在使用点以 `find()`-null 检查
  实施。本卡随 checkpoint 提交（NDNSF commit，见 docs/failure-log Ref）。

## L0 Installed-Library Command（T001-C 冻结句）逐条执行

### 0. 安装链

- configure（fresh staging，新 run-id 目录非空即拒绝）：
  `./waf -o build-nac182 configure --prefix=<r2>/staging --with-tests
  --with-examples --toolchain-root=/usr/bin --disable-local-dependency-prefix
  --nac-abe-prefix=/home/tianxing/NDN/nac-abe-integration-182/install
  --ndn-svs-source-tree=/home/tianxing/NDN/ndn-svs
  --ndn-svs-build-tree=/home/tianxing/NDN/ndn-svs/build` → rc=0
  （configure.log，4.8s）。
- `NDNSF_SKIP_DEV_PIP_INSTALL=1 ./waf -o build-nac182 install -j2` → rc=0，
  `'install' finished successfully`（install.log，4m25s；pythonWrapper 的
  dev pip 段按 NDNSF_SKIP_DEV_PIP_INSTALL=1 跳过，日志可见）。
- 安装物：`lib/{libndn-service-framework.so(.0.1.0),
  libndnsf-distributed-inference.so,
  pkgconfig/{libndn-service-framework.pc, ndnsf-distributed-inference.pc}}`、
  `include/NDNSF-DistributedInference/cpp/ndnsf-di/`（54 公共头）、
  `include/ndn-service-framework/`、`bin/`。

### 1. Consumer 独立编译（installed headers + pkg-config，/usr/bin g++）

```
PKG_CONFIG_PATH=<staging>/lib/pkgconfig /usr/bin/g++ -B/usr/bin -std=c++17 \
  tests/standalone/spec182-installed-consumer.cpp \
  $(pkg-config --cflags --libs ndnsf-distributed-inference) \
  -o <r2>/spec182-installed-consumer
```

COMPILE_RC=0（无 libpython、无源码副本参与；头文件来自 staging include）。

### 2. Consumer 运行（patched NAC-ABE prefix 前置）

```
LD_LIBRARY_PATH=/home/tianxing/NDN/nac-abe-integration-182/install/lib:<staging>/lib:<ndn-svs>/build:/usr/local/lib \
  <r2>/spec182-installed-consumer
```

输出 `SPEC182_INSTALLED_CONSUMER_NATIVE_DI_OK`，RUN_RC=0。
（该 marker 证明安装库符号解析、registry 构造/freeze/frozen()/find()
 对缺省 adapter 返回 null 全部按冻结 consumer 语义工作。）

### 3. 依赖与文件边界检查

- 无 Python link：`ldd` consumer 与 `libndnsf-distributed-inference.so`
  均无 `libpython`（NO_LIBPYTHON_OK / NO_LIBPYTHON_DILIB_OK）。
- 无 DI 源码副本：staging 下 `.cpp`/`.cc` 文件数为 0；86 个
  `.hpp` 全部位于 include 树（54 DI 公共头 + 32 框架头）。
- NAC-ABE 5-symbol gate（spec179 patch，pre-179 对照缺失）：
  - patched `nac-abe-integration-182/install/lib/libnac-abe.so` 导出
    `ndn::nacabe::{CacheProducer,Consumer,ParamFetcher}::clearCache`、
    `Consumer::refreshDecryptionKey`、`Producer::refreshPublicParameters`
    （nm -D -C T 符号）；
  - pre-179 `/usr/local/lib/libnac-abe.so` 对照仅导出
    `CacheProducer::clearCache`/`ABESupport::clearCachedContentKeys`，
    refresh 系列缺失 —— patched prefix 前置是必要的；
  - `getPublicParamsDataName`/`getPublicParamsDigest` 为 patched header
    inline getter（consumer.hpp/producer.hpp/param-fetcher.hpp），非 .so
    导出符号；
  - consumer 运行时 `ldd` 证实加载
    `libnac-abe.so => nac-abe-integration-182/install/lib/libnac-abe.so`；
    `ldd -r` 对安装 DI lib 无 undefined/not-found（ALL_SYMBOLS_RESOLVED_OK），
    consumer RUN_RC=0 无 ABI crash —— gate 通过。
- 框架层对 NAC-ABE 的调用点在 `ndn-service-framework/{ServiceUser,
  ServiceProvider}.cpp` 的 `activeNacConsumer()` 路径（U 引用，运行时解析）。

## Verify 判定

按卡 Verify（L0；独立 consumer 无复制 DI 源、无 Python link）全部满足；
后续 T006/T007 各自新源将接入同一库（planned 目标不在本卡实现）。
T002-A DONE；父 T002 仍以 [x] 记账为准，不视为 release 级验收
（见 tasks.md Execution Progress 记账原则）。

执行工具链/环境、configure 与 install 完整输出：run-id
`.codex-tmp/spec182-t002a-l0-r2/`（configure.log、install.log、consumer 二进制），
raw 目录保留。
