# R4-B1 Native Sampling Contract Repair

## Scope and Status

基线70b9be63；**DONE (local sampling batch)**。这是R4内既有生成内核的局部契约修复，
对应T011-A；不提前关闭T010-C stream acceptance或T011整体验收依赖。
保留现有sampleToken/lastLogits/SplitMix64，不新增public sampler、协议或RNG。

| Member | Work | Status |
| --- | --- | --- |
| S1 | float32→double惩罚、统一参数校验，保留已修好的去重/retained mass | DONE (local) |
| S2 | Spec182Sampling四个具名case，经真实epoch驱动私有sampler，与旧Python参考对照 | DONE (local) |

Write：NativeEpochCoordinator.cpp、tests/unit-tests/di-native-conversation.t.cpp；需要复用
已有epoch fixture时允许提取test-only helper并同步原unit caller，不复制采样算法。
批末共同静态门后增量unit-tests，选择Spec182Sampling及已有epoch generation用例。
两份并发dependency/generation设计文档不纳入本批提交。

## Current Evidence

最终：sampler四case/40 assertions PASS，见
[r2 focused](../../../.codex-tmp/spec182-r4-b1-r2/focused.log)；未变化的既有
NativeEpochCoordinatorProducesTextAndTerminalFeedback在
[r1 focused](../../../.codex-tmp/spec182-r4-b1/focused.log)中28/28 assertions PASS。
增量unit构建分别33.992s与16.161s，实际命令/退出码在对应build-record.json。
实际DI共享库增量构建PASS，16.304s，见
[library record](../../../.codex-tmp/spec182-r4-b1-r2/library-record.json)。复用R3构建树，
仅构建ndnsf-distributed-inference目标；因先前swap压力沿用-j2。
独立参考见 [float32 vectors](../../../.codex-tmp/spec182-r4-b1/reference.json) 与
[wrap vectors](../../../.codex-tmp/spec182-r4-b1/wrap-reference.json)：现有Python sample_token
返回与native断言相同的预期；没有复制native算法作为oracle。
这只证明采样内核与已有epoch调用，stream acceptance、stable text、真实多Provider
生成/会话和完整T011仍未完成。以下为保留的实施/验证历史。

提交前diff whitespace检查PASS；checklists/validate_design.py exit0/ok=true，
745个local links、36张执行卡与进度映射检查PASS。case-manifest记录四个实际sampling cases。
Context Mode active层因plan/tasks索引hash过期不可作为checkpoint依据，使用当前仓库文件回退。

CodeGraph定位sampleToken后核对实际源码：dedup set与retainedTotal已存在，不能重复
宣称这两项是本轮新增修复。余下缺口是float32惩罚舍入、Greedy未校验penalty/有限性/
top_k上限，以及随机模式静默min(top_k,vocabulary)。依现有Sampling Source Changes
契约统一校验，再提升double计算。保留相同token顺序、严格draw比较、并列最低ID和seed/step。
S2必须覆盖非有限/边界配置与float32量化后的参考向量，未完成前不授予STATIC_PASS或DONE。

## Batch Static Review

四个具名case已写入di-native-conversation.t.cpp；test-only runSamplingEpochs复用
distributed-inference-async-runtime.t.cpp中的BlockingDependencyIo和exactStateIdentity，
只提供float32 tensor/runner，不复制采样数学。共享声明位于native-sampling-epoch.hpp。
官方review-agent只读审查参数分支、double提升、dedup、retainedTotal、稳定排序和
epoch测试入口/feedback终态；配置在惩罚前拒绝，未知mode不会落入Greedy，原RNG未改。
**READY_FOR_BATCH_TESTS**：增量unit-tests，先Spec182Sampling，再既有
NativeEpochCoordinatorProducesTextAndTerminalFeedback；无ABI布局变化，不新建构建树。
日志 `.codex-tmp/spec182-r4-b1/`，实际结果见上方Current Evidence。

首轮增量build PASS33.992s；4 sampler+1既有epoch case PASS，54 assertions。
独立参考四组float32预期为0/0/1/1，见原始reference.json。契约核对仍缺seed/step
回绕、有效边界和非有限logits覆盖，补入现有四个case后仅增量编译测试并重跑sampler，
不重跑未变化的旧epoch case。原始日志保留，续验证目录为spec182-r4-b1-r2。
