# Design R2 Baseline and Contracts

## Scope

用户授权依次修复设计基线/校验、补关键 API 行为、纳入目标改进。仅修改设计工具与文档，
不实现 TG-01 至 TG-05 产品改动，不关闭 Spec182 功能任务。并行源码改动不暂存。

## First Boundary

r1 API 检查 FAIL：旧声明清单中 initialStreamTranscriptDigest 的两处前置声明使用同一 API ID，
新覆盖检查错误地要求函数声明记录与唯一 ID 数量相同。原始结果：
[r1 API](../../../.codex-tmp/design-r2-check-r1/api.json)。这是文档覆盖模型问题，未运行产品。
修复方向为每个 API ID 一条状态，保留全部声明位置，不更改产品 API 或丢弃重复位置。

## Validation

普通 git commit 被本机 pre-commit 的全索引助手引用扫描拒绝；首边界为提交钩子，文档检查已通过。
读取 .git/hooks/pre-commit 后确认其显式支持 NDNSF_LOCAL_CHECKPOINT=1：本地 checkpoint
仅跳过内容引用扫描，仍检查被禁止的文件路径。按仓库自动本地 checkpoint 授权使用该入口，
不修改钩子、不禁用文件路径检查、不 push。AGENTS.md 仍为本机文件，可交付规则在 MANAGEMENT.md。

r4 提交前并行新增模型描述符 Python 绑定，检查再次准确拒绝漂移。r5 核对新增字段、
canonical_json、descriptor_digest/model_digest 和 source_revision 的继承归属后更新绑定清单。
绑定校验现分别检查 matches_commit 标志真实性和源码快照摘要；允许精确补丁覆盖的未提交绑定，
其源码仍由 Git+patch 还原验证，不再错误要求工作区字节必须已经提交。r5 最终结果见机器记录。

提交前并行开发再次修改 NativeInferenceClient.hpp、NativePlanning.cpp/.hpp、NativeRequestPreparation.cpp；
新检查正确拒绝这些漂移，保留 [r4 刷新前](../../../.codex-tmp/design-r2-check-r4/pre-refresh-drift.json)。
新增 adapter/model 描述符、规范 JSON 身份比较及 sourceRevision 继承归属已核对源码，
只刷新当前快照；目标冻结不动。这些未提交实现不因此获得本轮产品资格。最终以 r4 结果为准。

r2 工具回归 4 PASS；API 295 文件/16562 条声明/5739 函数记录（5738 个不同 API ID）与
830 绑定检查 PASS；当前 460 文件和目标 350 文件源码还原 PASS。
首次 PDF 构建完成，但严格校验拒绝新增 BC/TG 段落的三个长标识符横向溢出；
[校验日志](../../../.codex-tmp/design-r2-check-r2/pdf-check.log)，
[构建目录](../../../.codex-tmp/design-pdf-20260908T042731896237Z/)。先修正断行再独立 r3 构建。

独立 r3 **PASS**（后续被并行源码修改触发刷新）：当前 65 页/62 目录项，目标 69 页/67 目录项；字体嵌入、全部目录页码、
双 PDF 输入身份、生成契约、API、当前/目标源码还原通过，源码漂移为空。
无 LaTeX 警告/溢出/缺字；人工抽查当前第 65 页与目标第 67/69 页。
当前 SHA-256：091c7b51ad79e931c557ac0f8deccd85e137da3c19d6c2b761874256fc41443a。
目标 SHA-256：34cfca1519fc27733bb0e1aeb86d248c1d09efcc6c93cedad18b413c2a73054f。
[机器结果](design-r2-verification.json)、[回归](../../../.codex-tmp/design-r2-check-r2/regression.log)、
[r3 校验](../../../.codex-tmp/design-r2-check-r3/pdf-check.log)。
四个回归覆盖新增源/API 文件、旧 TeX/PDF、目标签名独立性、重复声明位置保留。
当前基线 9ba91d1665339e74444441e8be85929073e782dd 加精确补丁；目标保留 e9fe33994a6ca3ff81893591bd24c3fae43f933f。
行为补充 BC-01 至 BC-04 为源码核对；全部 API ID 仅保守登记，不宣称逐项语义闭合。
TG-01 至 TG-05 全部 PLANNED；本工作单元完成不关闭任何产品功能任务。

r4 **PASS**（之后绑定更新触发 r5）：当前 66 页/62 目录项、目标 69 页/67 目录项；全部字体/目录/输入身份、
295 API 文件/16586 声明/5744 函数记录（5743 唯一 ID）/830 绑定、两侧源码还原通过，漂移为空。
4 项工具回归重验 PASS。当前基线 ca585ab5365189203325a8462d8726d2ea32c98f 加精确补丁；
目标冻结身份不变。当前 PDF SHA-256 a301ed74f1aea565466067451f9057a6f1466a8dbbd8ba400479d2e6e6861cef；
目标 PDF SHA-256 ec13cd2091eaf8e42cef0cde9e808130bdd75eebf88f925e562927f975cc3795。
最终机器结果为 [design-r2-verification.json](design-r2-verification.json)；
[r4 校验](../../../.codex-tmp/design-r2-check-r4/pdf-check.log)、
[r4 回归](../../../.codex-tmp/design-r2-check-r4/regression.log)。
构建目录 `.codex-tmp/design-pdf-20260908T043323295294Z/`。后续源码变更不自动继承此 PASS。

最终 r5 **PASS**：双 PDF 66/69 页、62/67 目录项，无警告，字体嵌入；API 为 295 文件、
16586 声明、5744 函数记录及 852 绑定；两侧 460/350 文件还原和构建身份 PASS，漂移为空。
4 个工具回归重验 PASS。最终机器记录 design-r2-verification.json 已更新到 r5。
[r5 校验](../../../.codex-tmp/design-r2-check-r5/pdf-check.log)、
[绑定漂移](../../../.codex-tmp/design-r2-check-r5/pre-refresh-drift.json)、
[r5 回归](../../../.codex-tmp/design-r2-check-r5/regression.log)。
最终构建目录 `.codex-tmp/design-pdf-20260908T043646760539Z/`；PDF 哈希以机器记录为准。

Context Mode 活动健康检查 rc=4（tasks 已索引摘要过期），按规定使用仓库权威文件；不修改检索存储。
CodeGraph 查询命中暂存副本，
拒绝这些结果并核对 canonical 源文件。无产品构建、MiniNDN、SIF 或 Tiger 验收。
