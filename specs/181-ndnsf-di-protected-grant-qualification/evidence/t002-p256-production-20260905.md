# T002 P-256 Production Grant Path

**Status**: PASS (focused P-256 production control); T002 full acceptance IN_PROGRESS
**Evidence layer**: implemented / wired / executed (focused checks and isolated native control)

## First Boundary

native 凭据入口修复后，源码追踪发现 Python requester/Provider 仍
调用只接受 Ed25519 的 loader；runner 又把显式收件人映射覆盖为
offer 签名密钥映射。P-256 收件人与 Ed25519 offer 签名必须允许
分别配置。

R1 定向测试 5 FAIL：4 项在 fixture 创建 EC 密钥前因当前 cryptography
要求显式 `backend` 参数失败，不计作协议或生产 loader 结果；另 1 项
真实证明 runner 覆盖专用映射。原始输出保留在 ignored workspace
temporary directory 下 `spec181-p256-production-20260905-r1/red.log`。
先修正 fixture 调用，再复现生产 loader 边界。

R2 fixture 修正后为 3 FAIL / 2 PASS：requester 与 Python Provider
均报 `private key is not Ed25519`；runner 专用映射断言失败。
P-384 拒绝用例通过。原始结果保留在
`spec181-p256-production-20260905-r2/red.log`。

R3 修复三处入口后，相关 131 项检查中 130 PASS，P-256 正例继续
深入实际 `wrap_content_key`，发现生产 EC 临时密钥生成也遗漏当前
cryptography 所需的 `backend`。该结果属于实际信封创建失败，记录为
`spec181-p256-production-20260905-r3/focused.log`，不能计作签发成功。

## Focused Repair

R4 修复生产 EC 临时密钥生成后，相关 131 项检查 PASS；再增加
收件人私钥权限、符号链接和大小上界拒绝检查，最终 134 PASS
（1.81s），见 `spec181-p256-production-20260905-r4/final-focused.log`。
共 8 个新增用例覆盖真实 Python P-256 签发/解包、Provider 密钥加载、
错误曲线、受限文件读取与 runner 映射保留；此处没有网络声明。

`load_grant_recipient_private_key` 只接受 Ed25519 或 P-256，打开
同一文件描述符后验证 regular/0600/65536-byte 上界并拒绝 symlink。
requester 和 Python Provider 共用该入口；请求方/offer 的签名算法
维持既有约定。runner 优先保留显式 recipient map，未配置时才回退
到原 offer-key map。此阶段的统一 native 重建及 P-256 Y-B 控制尚未
执行；其后续结果见下节。

## Live Protected Control

源码检查点 `835f20f9`；统一 native 构建 exit 0，Waf Provider 构建
4m4.232s，Python 扩展及 `SPEC180_NATIVE_IDENTITY_OK` 均通过。
原始日志 `spec181-p256-production-20260905-r4/native-build.log`。
构建清单 `build-system-j2/spec180-native-build.json` 的 SHA-256 为
`3276a7cf1f67ba6b8e6d4b8a339294aab1c5a662657dfa0dc6106d678b2ad266`。

独立运行 `spec181-p256-production-20260905-live-r1/` 经
user/network/mount/PID namespace 隔离，调用维护入口
`_run_live_case_once("Y-B", ...)`。四个 Provider 使用本次生成的
独立 P-256 私钥映射；offer 签名仍使用原 Ed25519 配置。
`recipient-inventory.json` 记录四个 public-key digest 与算法，
私钥只在 ignored workspace temporary directory 内，不进入 Git。

- runner exit 0；`case/subcase-result.json` 为 PASS，原因
  `TERMINAL_RESPONSE_VERIFIED`，`childCount=7`。
- 四个实际 native Provider 各产生一次 `VERIFIED / BEFORE_ASSEMBLY`。
  结合实际传入的 P-256-only 收件人映射及 verifier 的密钥类型检查，
  该控制验证 requester 发布、精确名获取、native ECDH 信封解包链路。
- 三个执行角色的 ORT profile 均记录 `CPUExecutionProvider`；Merge
  日志记录 `native-yolo-postprocess`。
- `case/yolo-numerical.json`：`matched=true`，shape `[1, 50, 6]`，
  最大绝对误差 `0.0005340576171875`，`atol=0.001`、`rtol=0.0001`。
- `case/child-exits.json` 收集全部 7 个子进程；user exit 0，其他
  进程由 runner 请求终止（SIGINT `-2`，repo `130`）。隔离 PID
  namespace 已退出；宿主五个原有 NFD 的 PID/PPID 不变。
- 三个 `.staging` 父目录为空；缓存没有 `.onnx`、`.weights` 或
  `.bin` 明文文件，仅保留密文、manifest、签名和 ORT profile。

## Remaining Acceptance

此证据闭合 P-256 正向生产控制，不是冻结候选或正式资格矩阵。
T002 仍需任务指定的维护 integration 测试、剩余 handler 源码闭包
及完整资源/异常路径验收；T001 Python Provider 真进程验收仍开放。
T007 保持 BLOCK。
